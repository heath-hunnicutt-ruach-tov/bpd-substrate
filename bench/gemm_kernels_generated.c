// =================================================================
// gemm_kernels_generated.c — auto-generated, do not edit.
// Generator: bench/generate_gemm_kernels.py
// Parameter space: lib/gemm_kernel.pl gemm_pattern/6
// Total kernels: 4328
//
// PyTorch CPU on AVX1 (Tesla P4 enclave) bit-identical match:
//   gemm_p768_q384_um16_un4_simd8_adaptive_half
// Verified at commit 50f1f4d.
// =================================================================

// gemm_p1_q1_um1_un1_simd1_single_block
//   P=1 Q=1 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p1_q1_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q1_um1_un1_simd1_fixed_q
//   P=1 Q=1 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1_q1_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q1_um1_un1_simd1_adaptive_half
//   P=1 Q=1 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1_q1_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q1_um1_un1_simd1_equal_split
//   P=1 Q=1 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p1_q1_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q1_um1_un4_simd1_single_block
//   P=1 Q=1 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p1_q1_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q1_um1_un4_simd1_fixed_q
//   P=1 Q=1 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p1_q1_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q1_um1_un4_simd1_adaptive_half
//   P=1 Q=1 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p1_q1_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q1_um1_un4_simd1_equal_split
//   P=1 Q=1 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p1_q1_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q64_um1_un1_simd1_single_block
//   P=1 Q=64 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p1_q64_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q64_um1_un1_simd1_fixed_q
//   P=1 Q=64 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1_q64_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q64_um1_un1_simd1_adaptive_half
//   P=1 Q=64 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1_q64_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q64_um1_un1_simd1_equal_split
//   P=1 Q=64 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p1_q64_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q64_um1_un4_simd1_single_block
//   P=1 Q=64 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p1_q64_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q64_um1_un4_simd1_fixed_q
//   P=1 Q=64 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p1_q64_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q64_um1_un4_simd1_adaptive_half
//   P=1 Q=64 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p1_q64_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q64_um1_un4_simd1_equal_split
//   P=1 Q=64 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p1_q64_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q128_um1_un1_simd1_single_block
//   P=1 Q=128 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p1_q128_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q128_um1_un1_simd1_fixed_q
//   P=1 Q=128 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1_q128_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q128_um1_un1_simd1_adaptive_half
//   P=1 Q=128 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1_q128_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q128_um1_un1_simd1_equal_split
//   P=1 Q=128 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p1_q128_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q128_um1_un4_simd1_single_block
//   P=1 Q=128 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p1_q128_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q128_um1_un4_simd1_fixed_q
//   P=1 Q=128 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p1_q128_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q128_um1_un4_simd1_adaptive_half
//   P=1 Q=128 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p1_q128_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q128_um1_un4_simd1_equal_split
//   P=1 Q=128 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p1_q128_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q192_um1_un1_simd1_single_block
//   P=1 Q=192 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p1_q192_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q192_um1_un1_simd1_fixed_q
//   P=1 Q=192 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1_q192_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q192_um1_un1_simd1_adaptive_half
//   P=1 Q=192 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1_q192_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q192_um1_un1_simd1_equal_split
//   P=1 Q=192 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p1_q192_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q192_um1_un4_simd1_single_block
//   P=1 Q=192 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p1_q192_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q192_um1_un4_simd1_fixed_q
//   P=1 Q=192 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p1_q192_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q192_um1_un4_simd1_adaptive_half
//   P=1 Q=192 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p1_q192_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q192_um1_un4_simd1_equal_split
//   P=1 Q=192 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p1_q192_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q240_um1_un1_simd1_single_block
//   P=1 Q=240 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p1_q240_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q240_um1_un1_simd1_fixed_q
//   P=1 Q=240 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1_q240_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q240_um1_un1_simd1_adaptive_half
//   P=1 Q=240 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1_q240_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q240_um1_un1_simd1_equal_split
//   P=1 Q=240 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p1_q240_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q240_um1_un4_simd1_single_block
//   P=1 Q=240 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p1_q240_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q240_um1_un4_simd1_fixed_q
//   P=1 Q=240 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p1_q240_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q240_um1_un4_simd1_adaptive_half
//   P=1 Q=240 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p1_q240_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q240_um1_un4_simd1_equal_split
//   P=1 Q=240 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p1_q240_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q248_um1_un1_simd1_single_block
//   P=1 Q=248 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p1_q248_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q248_um1_un1_simd1_fixed_q
//   P=1 Q=248 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1_q248_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q248_um1_un1_simd1_adaptive_half
//   P=1 Q=248 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1_q248_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q248_um1_un1_simd1_equal_split
//   P=1 Q=248 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p1_q248_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q248_um1_un4_simd1_single_block
//   P=1 Q=248 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p1_q248_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q248_um1_un4_simd1_fixed_q
//   P=1 Q=248 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p1_q248_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q248_um1_un4_simd1_adaptive_half
//   P=1 Q=248 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p1_q248_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q248_um1_un4_simd1_equal_split
//   P=1 Q=248 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p1_q248_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q256_um1_un1_simd1_single_block
//   P=1 Q=256 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p1_q256_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q256_um1_un1_simd1_fixed_q
//   P=1 Q=256 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1_q256_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q256_um1_un1_simd1_adaptive_half
//   P=1 Q=256 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1_q256_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q256_um1_un1_simd1_equal_split
//   P=1 Q=256 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p1_q256_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q256_um1_un4_simd1_single_block
//   P=1 Q=256 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p1_q256_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q256_um1_un4_simd1_fixed_q
//   P=1 Q=256 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p1_q256_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q256_um1_un4_simd1_adaptive_half
//   P=1 Q=256 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p1_q256_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q256_um1_un4_simd1_equal_split
//   P=1 Q=256 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p1_q256_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q384_um1_un1_simd1_single_block
//   P=1 Q=384 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p1_q384_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q384_um1_un1_simd1_fixed_q
//   P=1 Q=384 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1_q384_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q384_um1_un1_simd1_adaptive_half
//   P=1 Q=384 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1_q384_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q384_um1_un1_simd1_equal_split
//   P=1 Q=384 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p1_q384_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q384_um1_un4_simd1_single_block
//   P=1 Q=384 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p1_q384_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q384_um1_un4_simd1_fixed_q
//   P=1 Q=384 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p1_q384_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q384_um1_un4_simd1_adaptive_half
//   P=1 Q=384 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p1_q384_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q384_um1_un4_simd1_equal_split
//   P=1 Q=384 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p1_q384_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q512_um1_un1_simd1_single_block
//   P=1 Q=512 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p1_q512_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q512_um1_un1_simd1_fixed_q
//   P=1 Q=512 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1_q512_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q512_um1_un1_simd1_adaptive_half
//   P=1 Q=512 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1_q512_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q512_um1_un1_simd1_equal_split
//   P=1 Q=512 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p1_q512_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q512_um1_un4_simd1_single_block
//   P=1 Q=512 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p1_q512_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q512_um1_un4_simd1_fixed_q
//   P=1 Q=512 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p1_q512_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q512_um1_un4_simd1_adaptive_half
//   P=1 Q=512 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p1_q512_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1_q512_um1_un4_simd1_equal_split
//   P=1 Q=512 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p1_q512_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um1_un1_simd1_single_block
//   P=64 Q=1 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q1_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um1_un1_simd1_fixed_q
//   P=64 Q=1 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q1_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um1_un1_simd1_adaptive_half
//   P=64 Q=1 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q1_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um1_un1_simd1_equal_split
//   P=64 Q=1 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q1_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um1_un4_simd1_single_block
//   P=64 Q=1 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p64_q1_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um1_un4_simd1_fixed_q
//   P=64 Q=1 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p64_q1_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um1_un4_simd1_adaptive_half
//   P=64 Q=1 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p64_q1_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um1_un4_simd1_equal_split
//   P=64 Q=1 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p64_q1_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um4_un1_simd1_single_block
//   P=64 Q=1 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q1_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um4_un1_simd1_fixed_q
//   P=64 Q=1 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q1_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um4_un1_simd1_adaptive_half
//   P=64 Q=1 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q1_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um4_un1_simd1_equal_split
//   P=64 Q=1 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q1_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um4_un1_simd4_single_block
//   P=64 Q=1 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q1_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um4_un1_simd4_fixed_q
//   P=64 Q=1 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q1_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um4_un1_simd4_adaptive_half
//   P=64 Q=1 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q1_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um4_un1_simd4_equal_split
//   P=64 Q=1 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q1_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um4_un4_simd4_single_block
//   P=64 Q=1 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p64_q1_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um4_un4_simd4_fixed_q
//   P=64 Q=1 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p64_q1_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um4_un4_simd4_adaptive_half
//   P=64 Q=1 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p64_q1_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um4_un4_simd4_equal_split
//   P=64 Q=1 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p64_q1_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um8_un1_simd1_single_block
//   P=64 Q=1 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q1_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um8_un1_simd1_fixed_q
//   P=64 Q=1 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q1_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um8_un1_simd1_adaptive_half
//   P=64 Q=1 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q1_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um8_un1_simd1_equal_split
//   P=64 Q=1 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q1_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um8_un1_simd4_single_block
//   P=64 Q=1 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q1_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um8_un1_simd4_fixed_q
//   P=64 Q=1 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q1_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um8_un1_simd4_adaptive_half
//   P=64 Q=1 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q1_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um8_un1_simd4_equal_split
//   P=64 Q=1 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q1_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um8_un1_simd8_single_block
//   P=64 Q=1 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p64_q1_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um8_un1_simd8_fixed_q
//   P=64 Q=1 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p64_q1_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um8_un1_simd8_adaptive_half
//   P=64 Q=1 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p64_q1_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um8_un1_simd8_equal_split
//   P=64 Q=1 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p64_q1_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um8_un4_simd4_single_block
//   P=64 Q=1 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p64_q1_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um8_un4_simd4_fixed_q
//   P=64 Q=1 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p64_q1_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um8_un4_simd4_adaptive_half
//   P=64 Q=1 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p64_q1_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um8_un4_simd4_equal_split
//   P=64 Q=1 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p64_q1_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um8_un4_simd8_single_block
//   P=64 Q=1 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p64_q1_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um8_un4_simd8_fixed_q
//   P=64 Q=1 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p64_q1_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um8_un4_simd8_adaptive_half
//   P=64 Q=1 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p64_q1_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um8_un4_simd8_equal_split
//   P=64 Q=1 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p64_q1_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um16_un1_simd4_single_block
//   P=64 Q=1 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q1_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um16_un1_simd4_fixed_q
//   P=64 Q=1 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q1_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um16_un1_simd4_adaptive_half
//   P=64 Q=1 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q1_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um16_un1_simd4_equal_split
//   P=64 Q=1 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q1_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um16_un1_simd8_single_block
//   P=64 Q=1 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p64_q1_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um16_un1_simd8_fixed_q
//   P=64 Q=1 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p64_q1_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um16_un1_simd8_adaptive_half
//   P=64 Q=1 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p64_q1_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um16_un1_simd8_equal_split
//   P=64 Q=1 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p64_q1_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um16_un1_simd16_single_block
//   P=64 Q=1 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p64_q1_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um16_un1_simd16_fixed_q
//   P=64 Q=1 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p64_q1_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um16_un1_simd16_adaptive_half
//   P=64 Q=1 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p64_q1_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um16_un1_simd16_equal_split
//   P=64 Q=1 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p64_q1_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um16_un4_simd8_single_block
//   P=64 Q=1 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p64_q1_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um16_un4_simd8_fixed_q
//   P=64 Q=1 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p64_q1_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um16_un4_simd8_adaptive_half
//   P=64 Q=1 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p64_q1_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um16_un4_simd8_equal_split
//   P=64 Q=1 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p64_q1_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um16_un4_simd16_single_block
//   P=64 Q=1 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p64_q1_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um16_un4_simd16_fixed_q
//   P=64 Q=1 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p64_q1_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um16_un4_simd16_adaptive_half
//   P=64 Q=1 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p64_q1_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um16_un4_simd16_equal_split
//   P=64 Q=1 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p64_q1_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um32_un1_simd4_single_block
//   P=64 Q=1 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q1_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um32_un1_simd4_fixed_q
//   P=64 Q=1 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q1_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um32_un1_simd4_adaptive_half
//   P=64 Q=1 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q1_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um32_un1_simd4_equal_split
//   P=64 Q=1 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q1_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um32_un1_simd8_single_block
//   P=64 Q=1 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p64_q1_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um32_un1_simd8_fixed_q
//   P=64 Q=1 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p64_q1_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um32_un1_simd8_adaptive_half
//   P=64 Q=1 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p64_q1_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um32_un1_simd8_equal_split
//   P=64 Q=1 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p64_q1_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um32_un1_simd16_single_block
//   P=64 Q=1 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p64_q1_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um32_un1_simd16_fixed_q
//   P=64 Q=1 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p64_q1_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um32_un1_simd16_adaptive_half
//   P=64 Q=1 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p64_q1_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um32_un1_simd16_equal_split
//   P=64 Q=1 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p64_q1_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um32_un4_simd16_single_block
//   P=64 Q=1 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p64_q1_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um32_un4_simd16_fixed_q
//   P=64 Q=1 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p64_q1_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um32_un4_simd16_adaptive_half
//   P=64 Q=1 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p64_q1_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q1_um32_un4_simd16_equal_split
//   P=64 Q=1 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p64_q1_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um1_un1_simd1_single_block
//   P=64 Q=64 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q64_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um1_un1_simd1_fixed_q
//   P=64 Q=64 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q64_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um1_un1_simd1_adaptive_half
//   P=64 Q=64 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q64_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um1_un1_simd1_equal_split
//   P=64 Q=64 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q64_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um1_un4_simd1_single_block
//   P=64 Q=64 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p64_q64_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um1_un4_simd1_fixed_q
//   P=64 Q=64 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p64_q64_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um1_un4_simd1_adaptive_half
//   P=64 Q=64 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p64_q64_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um1_un4_simd1_equal_split
//   P=64 Q=64 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p64_q64_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um4_un1_simd1_single_block
//   P=64 Q=64 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q64_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um4_un1_simd1_fixed_q
//   P=64 Q=64 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q64_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um4_un1_simd1_adaptive_half
//   P=64 Q=64 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q64_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um4_un1_simd1_equal_split
//   P=64 Q=64 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q64_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um4_un1_simd4_single_block
//   P=64 Q=64 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q64_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um4_un1_simd4_fixed_q
//   P=64 Q=64 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q64_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um4_un1_simd4_adaptive_half
//   P=64 Q=64 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q64_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um4_un1_simd4_equal_split
//   P=64 Q=64 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q64_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um4_un4_simd4_single_block
//   P=64 Q=64 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p64_q64_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um4_un4_simd4_fixed_q
//   P=64 Q=64 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p64_q64_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um4_un4_simd4_adaptive_half
//   P=64 Q=64 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p64_q64_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um4_un4_simd4_equal_split
//   P=64 Q=64 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p64_q64_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um8_un1_simd1_single_block
//   P=64 Q=64 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q64_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um8_un1_simd1_fixed_q
//   P=64 Q=64 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q64_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um8_un1_simd1_adaptive_half
//   P=64 Q=64 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q64_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um8_un1_simd1_equal_split
//   P=64 Q=64 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q64_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um8_un1_simd4_single_block
//   P=64 Q=64 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q64_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um8_un1_simd4_fixed_q
//   P=64 Q=64 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q64_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um8_un1_simd4_adaptive_half
//   P=64 Q=64 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q64_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um8_un1_simd4_equal_split
//   P=64 Q=64 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q64_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um8_un1_simd8_single_block
//   P=64 Q=64 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p64_q64_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um8_un1_simd8_fixed_q
//   P=64 Q=64 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p64_q64_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um8_un1_simd8_adaptive_half
//   P=64 Q=64 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p64_q64_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um8_un1_simd8_equal_split
//   P=64 Q=64 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p64_q64_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um8_un4_simd4_single_block
//   P=64 Q=64 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p64_q64_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um8_un4_simd4_fixed_q
//   P=64 Q=64 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p64_q64_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um8_un4_simd4_adaptive_half
//   P=64 Q=64 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p64_q64_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um8_un4_simd4_equal_split
//   P=64 Q=64 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p64_q64_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um8_un4_simd8_single_block
//   P=64 Q=64 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p64_q64_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um8_un4_simd8_fixed_q
//   P=64 Q=64 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p64_q64_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um8_un4_simd8_adaptive_half
//   P=64 Q=64 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p64_q64_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um8_un4_simd8_equal_split
//   P=64 Q=64 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p64_q64_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um16_un1_simd4_single_block
//   P=64 Q=64 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q64_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um16_un1_simd4_fixed_q
//   P=64 Q=64 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q64_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um16_un1_simd4_adaptive_half
//   P=64 Q=64 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q64_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um16_un1_simd4_equal_split
//   P=64 Q=64 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q64_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um16_un1_simd8_single_block
//   P=64 Q=64 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p64_q64_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um16_un1_simd8_fixed_q
//   P=64 Q=64 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p64_q64_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um16_un1_simd8_adaptive_half
//   P=64 Q=64 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p64_q64_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um16_un1_simd8_equal_split
//   P=64 Q=64 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p64_q64_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um16_un1_simd16_single_block
//   P=64 Q=64 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p64_q64_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um16_un1_simd16_fixed_q
//   P=64 Q=64 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p64_q64_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um16_un1_simd16_adaptive_half
//   P=64 Q=64 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p64_q64_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um16_un1_simd16_equal_split
//   P=64 Q=64 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p64_q64_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um16_un4_simd8_single_block
//   P=64 Q=64 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p64_q64_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um16_un4_simd8_fixed_q
//   P=64 Q=64 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p64_q64_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um16_un4_simd8_adaptive_half
//   P=64 Q=64 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p64_q64_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um16_un4_simd8_equal_split
//   P=64 Q=64 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p64_q64_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um16_un4_simd16_single_block
//   P=64 Q=64 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p64_q64_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um16_un4_simd16_fixed_q
//   P=64 Q=64 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p64_q64_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um16_un4_simd16_adaptive_half
//   P=64 Q=64 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p64_q64_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um16_un4_simd16_equal_split
//   P=64 Q=64 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p64_q64_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um32_un1_simd4_single_block
//   P=64 Q=64 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q64_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um32_un1_simd4_fixed_q
//   P=64 Q=64 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q64_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um32_un1_simd4_adaptive_half
//   P=64 Q=64 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q64_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um32_un1_simd4_equal_split
//   P=64 Q=64 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q64_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um32_un1_simd8_single_block
//   P=64 Q=64 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p64_q64_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um32_un1_simd8_fixed_q
//   P=64 Q=64 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p64_q64_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um32_un1_simd8_adaptive_half
//   P=64 Q=64 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p64_q64_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um32_un1_simd8_equal_split
//   P=64 Q=64 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p64_q64_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um32_un1_simd16_single_block
//   P=64 Q=64 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p64_q64_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um32_un1_simd16_fixed_q
//   P=64 Q=64 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p64_q64_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um32_un1_simd16_adaptive_half
//   P=64 Q=64 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p64_q64_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um32_un1_simd16_equal_split
//   P=64 Q=64 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p64_q64_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um32_un4_simd16_single_block
//   P=64 Q=64 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p64_q64_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um32_un4_simd16_fixed_q
//   P=64 Q=64 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p64_q64_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um32_un4_simd16_adaptive_half
//   P=64 Q=64 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p64_q64_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q64_um32_un4_simd16_equal_split
//   P=64 Q=64 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p64_q64_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um1_un1_simd1_single_block
//   P=64 Q=128 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q128_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um1_un1_simd1_fixed_q
//   P=64 Q=128 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q128_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um1_un1_simd1_adaptive_half
//   P=64 Q=128 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q128_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um1_un1_simd1_equal_split
//   P=64 Q=128 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q128_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um1_un4_simd1_single_block
//   P=64 Q=128 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p64_q128_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um1_un4_simd1_fixed_q
//   P=64 Q=128 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p64_q128_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um1_un4_simd1_adaptive_half
//   P=64 Q=128 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p64_q128_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um1_un4_simd1_equal_split
//   P=64 Q=128 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p64_q128_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um4_un1_simd1_single_block
//   P=64 Q=128 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q128_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um4_un1_simd1_fixed_q
//   P=64 Q=128 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q128_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um4_un1_simd1_adaptive_half
//   P=64 Q=128 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q128_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um4_un1_simd1_equal_split
//   P=64 Q=128 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q128_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um4_un1_simd4_single_block
//   P=64 Q=128 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q128_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um4_un1_simd4_fixed_q
//   P=64 Q=128 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q128_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um4_un1_simd4_adaptive_half
//   P=64 Q=128 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q128_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um4_un1_simd4_equal_split
//   P=64 Q=128 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q128_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um4_un4_simd4_single_block
//   P=64 Q=128 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p64_q128_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um4_un4_simd4_fixed_q
//   P=64 Q=128 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p64_q128_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um4_un4_simd4_adaptive_half
//   P=64 Q=128 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p64_q128_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um4_un4_simd4_equal_split
//   P=64 Q=128 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p64_q128_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um8_un1_simd1_single_block
//   P=64 Q=128 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q128_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um8_un1_simd1_fixed_q
//   P=64 Q=128 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q128_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um8_un1_simd1_adaptive_half
//   P=64 Q=128 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q128_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um8_un1_simd1_equal_split
//   P=64 Q=128 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q128_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um8_un1_simd4_single_block
//   P=64 Q=128 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q128_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um8_un1_simd4_fixed_q
//   P=64 Q=128 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q128_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um8_un1_simd4_adaptive_half
//   P=64 Q=128 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q128_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um8_un1_simd4_equal_split
//   P=64 Q=128 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q128_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um8_un1_simd8_single_block
//   P=64 Q=128 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p64_q128_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um8_un1_simd8_fixed_q
//   P=64 Q=128 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p64_q128_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um8_un1_simd8_adaptive_half
//   P=64 Q=128 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p64_q128_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um8_un1_simd8_equal_split
//   P=64 Q=128 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p64_q128_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um8_un4_simd4_single_block
//   P=64 Q=128 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p64_q128_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um8_un4_simd4_fixed_q
//   P=64 Q=128 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p64_q128_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um8_un4_simd4_adaptive_half
//   P=64 Q=128 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p64_q128_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um8_un4_simd4_equal_split
//   P=64 Q=128 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p64_q128_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um8_un4_simd8_single_block
//   P=64 Q=128 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p64_q128_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um8_un4_simd8_fixed_q
//   P=64 Q=128 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p64_q128_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um8_un4_simd8_adaptive_half
//   P=64 Q=128 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p64_q128_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um8_un4_simd8_equal_split
//   P=64 Q=128 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p64_q128_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um16_un1_simd4_single_block
//   P=64 Q=128 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q128_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um16_un1_simd4_fixed_q
//   P=64 Q=128 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q128_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um16_un1_simd4_adaptive_half
//   P=64 Q=128 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q128_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um16_un1_simd4_equal_split
//   P=64 Q=128 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q128_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um16_un1_simd8_single_block
//   P=64 Q=128 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p64_q128_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um16_un1_simd8_fixed_q
//   P=64 Q=128 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p64_q128_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um16_un1_simd8_adaptive_half
//   P=64 Q=128 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p64_q128_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um16_un1_simd8_equal_split
//   P=64 Q=128 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p64_q128_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um16_un1_simd16_single_block
//   P=64 Q=128 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p64_q128_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um16_un1_simd16_fixed_q
//   P=64 Q=128 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p64_q128_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um16_un1_simd16_adaptive_half
//   P=64 Q=128 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p64_q128_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um16_un1_simd16_equal_split
//   P=64 Q=128 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p64_q128_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um16_un4_simd8_single_block
//   P=64 Q=128 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p64_q128_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um16_un4_simd8_fixed_q
//   P=64 Q=128 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p64_q128_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um16_un4_simd8_adaptive_half
//   P=64 Q=128 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p64_q128_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um16_un4_simd8_equal_split
//   P=64 Q=128 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p64_q128_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um16_un4_simd16_single_block
//   P=64 Q=128 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p64_q128_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um16_un4_simd16_fixed_q
//   P=64 Q=128 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p64_q128_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um16_un4_simd16_adaptive_half
//   P=64 Q=128 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p64_q128_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um16_un4_simd16_equal_split
//   P=64 Q=128 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p64_q128_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um32_un1_simd4_single_block
//   P=64 Q=128 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q128_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um32_un1_simd4_fixed_q
//   P=64 Q=128 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q128_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um32_un1_simd4_adaptive_half
//   P=64 Q=128 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q128_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um32_un1_simd4_equal_split
//   P=64 Q=128 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q128_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um32_un1_simd8_single_block
//   P=64 Q=128 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p64_q128_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um32_un1_simd8_fixed_q
//   P=64 Q=128 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p64_q128_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um32_un1_simd8_adaptive_half
//   P=64 Q=128 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p64_q128_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um32_un1_simd8_equal_split
//   P=64 Q=128 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p64_q128_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um32_un1_simd16_single_block
//   P=64 Q=128 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p64_q128_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um32_un1_simd16_fixed_q
//   P=64 Q=128 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p64_q128_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um32_un1_simd16_adaptive_half
//   P=64 Q=128 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p64_q128_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um32_un1_simd16_equal_split
//   P=64 Q=128 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p64_q128_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um32_un4_simd16_single_block
//   P=64 Q=128 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p64_q128_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um32_un4_simd16_fixed_q
//   P=64 Q=128 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p64_q128_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um32_un4_simd16_adaptive_half
//   P=64 Q=128 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p64_q128_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q128_um32_un4_simd16_equal_split
//   P=64 Q=128 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p64_q128_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um1_un1_simd1_single_block
//   P=64 Q=192 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q192_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um1_un1_simd1_fixed_q
//   P=64 Q=192 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q192_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um1_un1_simd1_adaptive_half
//   P=64 Q=192 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q192_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um1_un1_simd1_equal_split
//   P=64 Q=192 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q192_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um1_un4_simd1_single_block
//   P=64 Q=192 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p64_q192_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um1_un4_simd1_fixed_q
//   P=64 Q=192 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p64_q192_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um1_un4_simd1_adaptive_half
//   P=64 Q=192 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p64_q192_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um1_un4_simd1_equal_split
//   P=64 Q=192 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p64_q192_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um4_un1_simd1_single_block
//   P=64 Q=192 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q192_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um4_un1_simd1_fixed_q
//   P=64 Q=192 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q192_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um4_un1_simd1_adaptive_half
//   P=64 Q=192 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q192_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um4_un1_simd1_equal_split
//   P=64 Q=192 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q192_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um4_un1_simd4_single_block
//   P=64 Q=192 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q192_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um4_un1_simd4_fixed_q
//   P=64 Q=192 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q192_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um4_un1_simd4_adaptive_half
//   P=64 Q=192 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q192_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um4_un1_simd4_equal_split
//   P=64 Q=192 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q192_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um4_un4_simd4_single_block
//   P=64 Q=192 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p64_q192_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um4_un4_simd4_fixed_q
//   P=64 Q=192 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p64_q192_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um4_un4_simd4_adaptive_half
//   P=64 Q=192 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p64_q192_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um4_un4_simd4_equal_split
//   P=64 Q=192 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p64_q192_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um8_un1_simd1_single_block
//   P=64 Q=192 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q192_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um8_un1_simd1_fixed_q
//   P=64 Q=192 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q192_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um8_un1_simd1_adaptive_half
//   P=64 Q=192 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q192_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um8_un1_simd1_equal_split
//   P=64 Q=192 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q192_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um8_un1_simd4_single_block
//   P=64 Q=192 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q192_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um8_un1_simd4_fixed_q
//   P=64 Q=192 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q192_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um8_un1_simd4_adaptive_half
//   P=64 Q=192 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q192_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um8_un1_simd4_equal_split
//   P=64 Q=192 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q192_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um8_un1_simd8_single_block
//   P=64 Q=192 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p64_q192_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um8_un1_simd8_fixed_q
//   P=64 Q=192 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p64_q192_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um8_un1_simd8_adaptive_half
//   P=64 Q=192 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p64_q192_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um8_un1_simd8_equal_split
//   P=64 Q=192 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p64_q192_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um8_un4_simd4_single_block
//   P=64 Q=192 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p64_q192_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um8_un4_simd4_fixed_q
//   P=64 Q=192 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p64_q192_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um8_un4_simd4_adaptive_half
//   P=64 Q=192 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p64_q192_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um8_un4_simd4_equal_split
//   P=64 Q=192 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p64_q192_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um8_un4_simd8_single_block
//   P=64 Q=192 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p64_q192_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um8_un4_simd8_fixed_q
//   P=64 Q=192 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p64_q192_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um8_un4_simd8_adaptive_half
//   P=64 Q=192 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p64_q192_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um8_un4_simd8_equal_split
//   P=64 Q=192 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p64_q192_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um16_un1_simd4_single_block
//   P=64 Q=192 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q192_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um16_un1_simd4_fixed_q
//   P=64 Q=192 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q192_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um16_un1_simd4_adaptive_half
//   P=64 Q=192 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q192_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um16_un1_simd4_equal_split
//   P=64 Q=192 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q192_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um16_un1_simd8_single_block
//   P=64 Q=192 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p64_q192_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um16_un1_simd8_fixed_q
//   P=64 Q=192 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p64_q192_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um16_un1_simd8_adaptive_half
//   P=64 Q=192 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p64_q192_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um16_un1_simd8_equal_split
//   P=64 Q=192 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p64_q192_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um16_un1_simd16_single_block
//   P=64 Q=192 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p64_q192_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um16_un1_simd16_fixed_q
//   P=64 Q=192 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p64_q192_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um16_un1_simd16_adaptive_half
//   P=64 Q=192 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p64_q192_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um16_un1_simd16_equal_split
//   P=64 Q=192 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p64_q192_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um16_un4_simd8_single_block
//   P=64 Q=192 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p64_q192_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um16_un4_simd8_fixed_q
//   P=64 Q=192 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p64_q192_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um16_un4_simd8_adaptive_half
//   P=64 Q=192 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p64_q192_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um16_un4_simd8_equal_split
//   P=64 Q=192 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p64_q192_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um16_un4_simd16_single_block
//   P=64 Q=192 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p64_q192_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um16_un4_simd16_fixed_q
//   P=64 Q=192 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p64_q192_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um16_un4_simd16_adaptive_half
//   P=64 Q=192 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p64_q192_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um16_un4_simd16_equal_split
//   P=64 Q=192 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p64_q192_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um32_un1_simd4_single_block
//   P=64 Q=192 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q192_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um32_un1_simd4_fixed_q
//   P=64 Q=192 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q192_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um32_un1_simd4_adaptive_half
//   P=64 Q=192 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q192_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um32_un1_simd4_equal_split
//   P=64 Q=192 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q192_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um32_un1_simd8_single_block
//   P=64 Q=192 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p64_q192_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um32_un1_simd8_fixed_q
//   P=64 Q=192 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p64_q192_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um32_un1_simd8_adaptive_half
//   P=64 Q=192 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p64_q192_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um32_un1_simd8_equal_split
//   P=64 Q=192 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p64_q192_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um32_un1_simd16_single_block
//   P=64 Q=192 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p64_q192_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um32_un1_simd16_fixed_q
//   P=64 Q=192 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p64_q192_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um32_un1_simd16_adaptive_half
//   P=64 Q=192 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p64_q192_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um32_un1_simd16_equal_split
//   P=64 Q=192 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p64_q192_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um32_un4_simd16_single_block
//   P=64 Q=192 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p64_q192_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um32_un4_simd16_fixed_q
//   P=64 Q=192 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p64_q192_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um32_un4_simd16_adaptive_half
//   P=64 Q=192 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p64_q192_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q192_um32_un4_simd16_equal_split
//   P=64 Q=192 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p64_q192_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um1_un1_simd1_single_block
//   P=64 Q=240 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q240_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um1_un1_simd1_fixed_q
//   P=64 Q=240 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q240_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um1_un1_simd1_adaptive_half
//   P=64 Q=240 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q240_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um1_un1_simd1_equal_split
//   P=64 Q=240 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q240_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um1_un4_simd1_single_block
//   P=64 Q=240 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p64_q240_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um1_un4_simd1_fixed_q
//   P=64 Q=240 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p64_q240_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um1_un4_simd1_adaptive_half
//   P=64 Q=240 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p64_q240_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um1_un4_simd1_equal_split
//   P=64 Q=240 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p64_q240_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um4_un1_simd1_single_block
//   P=64 Q=240 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q240_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um4_un1_simd1_fixed_q
//   P=64 Q=240 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q240_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um4_un1_simd1_adaptive_half
//   P=64 Q=240 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q240_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um4_un1_simd1_equal_split
//   P=64 Q=240 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q240_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um4_un1_simd4_single_block
//   P=64 Q=240 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q240_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um4_un1_simd4_fixed_q
//   P=64 Q=240 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q240_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um4_un1_simd4_adaptive_half
//   P=64 Q=240 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q240_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um4_un1_simd4_equal_split
//   P=64 Q=240 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q240_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um4_un4_simd4_single_block
//   P=64 Q=240 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p64_q240_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um4_un4_simd4_fixed_q
//   P=64 Q=240 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p64_q240_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um4_un4_simd4_adaptive_half
//   P=64 Q=240 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p64_q240_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um4_un4_simd4_equal_split
//   P=64 Q=240 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p64_q240_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um8_un1_simd1_single_block
//   P=64 Q=240 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q240_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um8_un1_simd1_fixed_q
//   P=64 Q=240 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q240_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um8_un1_simd1_adaptive_half
//   P=64 Q=240 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q240_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um8_un1_simd1_equal_split
//   P=64 Q=240 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q240_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um8_un1_simd4_single_block
//   P=64 Q=240 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q240_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um8_un1_simd4_fixed_q
//   P=64 Q=240 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q240_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um8_un1_simd4_adaptive_half
//   P=64 Q=240 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q240_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um8_un1_simd4_equal_split
//   P=64 Q=240 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q240_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um8_un1_simd8_single_block
//   P=64 Q=240 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p64_q240_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um8_un1_simd8_fixed_q
//   P=64 Q=240 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p64_q240_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um8_un1_simd8_adaptive_half
//   P=64 Q=240 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p64_q240_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um8_un1_simd8_equal_split
//   P=64 Q=240 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p64_q240_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um8_un4_simd4_single_block
//   P=64 Q=240 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p64_q240_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um8_un4_simd4_fixed_q
//   P=64 Q=240 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p64_q240_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um8_un4_simd4_adaptive_half
//   P=64 Q=240 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p64_q240_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um8_un4_simd4_equal_split
//   P=64 Q=240 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p64_q240_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um8_un4_simd8_single_block
//   P=64 Q=240 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p64_q240_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um8_un4_simd8_fixed_q
//   P=64 Q=240 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p64_q240_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um8_un4_simd8_adaptive_half
//   P=64 Q=240 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p64_q240_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um8_un4_simd8_equal_split
//   P=64 Q=240 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p64_q240_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um16_un1_simd4_single_block
//   P=64 Q=240 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q240_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um16_un1_simd4_fixed_q
//   P=64 Q=240 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q240_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um16_un1_simd4_adaptive_half
//   P=64 Q=240 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q240_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um16_un1_simd4_equal_split
//   P=64 Q=240 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q240_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um16_un1_simd8_single_block
//   P=64 Q=240 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p64_q240_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um16_un1_simd8_fixed_q
//   P=64 Q=240 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p64_q240_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um16_un1_simd8_adaptive_half
//   P=64 Q=240 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p64_q240_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um16_un1_simd8_equal_split
//   P=64 Q=240 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p64_q240_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um16_un1_simd16_single_block
//   P=64 Q=240 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p64_q240_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um16_un1_simd16_fixed_q
//   P=64 Q=240 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p64_q240_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um16_un1_simd16_adaptive_half
//   P=64 Q=240 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p64_q240_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um16_un1_simd16_equal_split
//   P=64 Q=240 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p64_q240_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um16_un4_simd8_single_block
//   P=64 Q=240 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p64_q240_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um16_un4_simd8_fixed_q
//   P=64 Q=240 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p64_q240_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um16_un4_simd8_adaptive_half
//   P=64 Q=240 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p64_q240_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um16_un4_simd8_equal_split
//   P=64 Q=240 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p64_q240_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um16_un4_simd16_single_block
//   P=64 Q=240 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p64_q240_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um16_un4_simd16_fixed_q
//   P=64 Q=240 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p64_q240_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um16_un4_simd16_adaptive_half
//   P=64 Q=240 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p64_q240_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um16_un4_simd16_equal_split
//   P=64 Q=240 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p64_q240_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um32_un1_simd4_single_block
//   P=64 Q=240 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q240_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um32_un1_simd4_fixed_q
//   P=64 Q=240 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q240_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um32_un1_simd4_adaptive_half
//   P=64 Q=240 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q240_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um32_un1_simd4_equal_split
//   P=64 Q=240 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q240_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um32_un1_simd8_single_block
//   P=64 Q=240 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p64_q240_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um32_un1_simd8_fixed_q
//   P=64 Q=240 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p64_q240_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um32_un1_simd8_adaptive_half
//   P=64 Q=240 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p64_q240_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um32_un1_simd8_equal_split
//   P=64 Q=240 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p64_q240_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um32_un1_simd16_single_block
//   P=64 Q=240 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p64_q240_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um32_un1_simd16_fixed_q
//   P=64 Q=240 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p64_q240_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um32_un1_simd16_adaptive_half
//   P=64 Q=240 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p64_q240_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q240_um32_un1_simd16_equal_split
//   P=64 Q=240 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p64_q240_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um1_un1_simd1_single_block
//   P=64 Q=248 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q248_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um1_un1_simd1_fixed_q
//   P=64 Q=248 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q248_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um1_un1_simd1_adaptive_half
//   P=64 Q=248 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q248_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um1_un1_simd1_equal_split
//   P=64 Q=248 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q248_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um1_un4_simd1_single_block
//   P=64 Q=248 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p64_q248_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um1_un4_simd1_fixed_q
//   P=64 Q=248 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p64_q248_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um1_un4_simd1_adaptive_half
//   P=64 Q=248 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p64_q248_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um1_un4_simd1_equal_split
//   P=64 Q=248 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p64_q248_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um4_un1_simd1_single_block
//   P=64 Q=248 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q248_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um4_un1_simd1_fixed_q
//   P=64 Q=248 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q248_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um4_un1_simd1_adaptive_half
//   P=64 Q=248 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q248_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um4_un1_simd1_equal_split
//   P=64 Q=248 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q248_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um4_un1_simd4_single_block
//   P=64 Q=248 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q248_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um4_un1_simd4_fixed_q
//   P=64 Q=248 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q248_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um4_un1_simd4_adaptive_half
//   P=64 Q=248 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q248_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um4_un1_simd4_equal_split
//   P=64 Q=248 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q248_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um4_un4_simd4_single_block
//   P=64 Q=248 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p64_q248_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um4_un4_simd4_fixed_q
//   P=64 Q=248 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p64_q248_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um4_un4_simd4_adaptive_half
//   P=64 Q=248 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p64_q248_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um4_un4_simd4_equal_split
//   P=64 Q=248 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p64_q248_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um8_un1_simd1_single_block
//   P=64 Q=248 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q248_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um8_un1_simd1_fixed_q
//   P=64 Q=248 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q248_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um8_un1_simd1_adaptive_half
//   P=64 Q=248 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q248_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um8_un1_simd1_equal_split
//   P=64 Q=248 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q248_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um8_un1_simd4_single_block
//   P=64 Q=248 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q248_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um8_un1_simd4_fixed_q
//   P=64 Q=248 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q248_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um8_un1_simd4_adaptive_half
//   P=64 Q=248 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q248_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um8_un1_simd4_equal_split
//   P=64 Q=248 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q248_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um8_un1_simd8_single_block
//   P=64 Q=248 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p64_q248_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um8_un1_simd8_fixed_q
//   P=64 Q=248 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p64_q248_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um8_un1_simd8_adaptive_half
//   P=64 Q=248 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p64_q248_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um8_un1_simd8_equal_split
//   P=64 Q=248 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p64_q248_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um8_un4_simd4_single_block
//   P=64 Q=248 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p64_q248_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um8_un4_simd4_fixed_q
//   P=64 Q=248 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p64_q248_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um8_un4_simd4_adaptive_half
//   P=64 Q=248 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p64_q248_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um8_un4_simd4_equal_split
//   P=64 Q=248 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p64_q248_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um8_un4_simd8_single_block
//   P=64 Q=248 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p64_q248_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um8_un4_simd8_fixed_q
//   P=64 Q=248 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p64_q248_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um8_un4_simd8_adaptive_half
//   P=64 Q=248 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p64_q248_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um8_un4_simd8_equal_split
//   P=64 Q=248 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p64_q248_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um16_un1_simd4_single_block
//   P=64 Q=248 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q248_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um16_un1_simd4_fixed_q
//   P=64 Q=248 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q248_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um16_un1_simd4_adaptive_half
//   P=64 Q=248 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q248_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um16_un1_simd4_equal_split
//   P=64 Q=248 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q248_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um16_un1_simd8_single_block
//   P=64 Q=248 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p64_q248_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um16_un1_simd8_fixed_q
//   P=64 Q=248 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p64_q248_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um16_un1_simd8_adaptive_half
//   P=64 Q=248 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p64_q248_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um16_un1_simd8_equal_split
//   P=64 Q=248 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p64_q248_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um16_un1_simd16_single_block
//   P=64 Q=248 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p64_q248_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um16_un1_simd16_fixed_q
//   P=64 Q=248 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p64_q248_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um16_un1_simd16_adaptive_half
//   P=64 Q=248 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p64_q248_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um16_un1_simd16_equal_split
//   P=64 Q=248 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p64_q248_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um16_un4_simd8_single_block
//   P=64 Q=248 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p64_q248_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um16_un4_simd8_fixed_q
//   P=64 Q=248 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p64_q248_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um16_un4_simd8_adaptive_half
//   P=64 Q=248 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p64_q248_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um16_un4_simd8_equal_split
//   P=64 Q=248 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p64_q248_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um16_un4_simd16_single_block
//   P=64 Q=248 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p64_q248_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um16_un4_simd16_fixed_q
//   P=64 Q=248 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p64_q248_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um16_un4_simd16_adaptive_half
//   P=64 Q=248 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p64_q248_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um16_un4_simd16_equal_split
//   P=64 Q=248 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p64_q248_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um32_un1_simd4_single_block
//   P=64 Q=248 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q248_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um32_un1_simd4_fixed_q
//   P=64 Q=248 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q248_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um32_un1_simd4_adaptive_half
//   P=64 Q=248 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q248_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um32_un1_simd4_equal_split
//   P=64 Q=248 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q248_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um32_un1_simd8_single_block
//   P=64 Q=248 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p64_q248_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um32_un1_simd8_fixed_q
//   P=64 Q=248 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p64_q248_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um32_un1_simd8_adaptive_half
//   P=64 Q=248 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p64_q248_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um32_un1_simd8_equal_split
//   P=64 Q=248 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p64_q248_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um32_un1_simd16_single_block
//   P=64 Q=248 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p64_q248_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um32_un1_simd16_fixed_q
//   P=64 Q=248 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p64_q248_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um32_un1_simd16_adaptive_half
//   P=64 Q=248 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p64_q248_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q248_um32_un1_simd16_equal_split
//   P=64 Q=248 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p64_q248_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um1_un1_simd1_single_block
//   P=64 Q=256 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q256_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um1_un1_simd1_fixed_q
//   P=64 Q=256 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q256_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um1_un1_simd1_adaptive_half
//   P=64 Q=256 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q256_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um1_un1_simd1_equal_split
//   P=64 Q=256 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q256_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um1_un4_simd1_single_block
//   P=64 Q=256 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p64_q256_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um1_un4_simd1_fixed_q
//   P=64 Q=256 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p64_q256_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um1_un4_simd1_adaptive_half
//   P=64 Q=256 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p64_q256_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um1_un4_simd1_equal_split
//   P=64 Q=256 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p64_q256_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um4_un1_simd1_single_block
//   P=64 Q=256 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q256_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um4_un1_simd1_fixed_q
//   P=64 Q=256 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q256_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um4_un1_simd1_adaptive_half
//   P=64 Q=256 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q256_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um4_un1_simd1_equal_split
//   P=64 Q=256 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q256_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um4_un1_simd4_single_block
//   P=64 Q=256 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q256_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um4_un1_simd4_fixed_q
//   P=64 Q=256 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q256_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um4_un1_simd4_adaptive_half
//   P=64 Q=256 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q256_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um4_un1_simd4_equal_split
//   P=64 Q=256 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q256_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um4_un4_simd4_single_block
//   P=64 Q=256 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p64_q256_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um4_un4_simd4_fixed_q
//   P=64 Q=256 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p64_q256_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um4_un4_simd4_adaptive_half
//   P=64 Q=256 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p64_q256_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um4_un4_simd4_equal_split
//   P=64 Q=256 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p64_q256_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um8_un1_simd1_single_block
//   P=64 Q=256 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q256_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um8_un1_simd1_fixed_q
//   P=64 Q=256 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q256_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um8_un1_simd1_adaptive_half
//   P=64 Q=256 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q256_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um8_un1_simd1_equal_split
//   P=64 Q=256 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q256_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um8_un1_simd4_single_block
//   P=64 Q=256 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q256_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um8_un1_simd4_fixed_q
//   P=64 Q=256 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q256_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um8_un1_simd4_adaptive_half
//   P=64 Q=256 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q256_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um8_un1_simd4_equal_split
//   P=64 Q=256 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q256_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um8_un1_simd8_single_block
//   P=64 Q=256 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p64_q256_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um8_un1_simd8_fixed_q
//   P=64 Q=256 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p64_q256_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um8_un1_simd8_adaptive_half
//   P=64 Q=256 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p64_q256_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um8_un1_simd8_equal_split
//   P=64 Q=256 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p64_q256_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um8_un4_simd4_single_block
//   P=64 Q=256 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p64_q256_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um8_un4_simd4_fixed_q
//   P=64 Q=256 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p64_q256_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um8_un4_simd4_adaptive_half
//   P=64 Q=256 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p64_q256_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um8_un4_simd4_equal_split
//   P=64 Q=256 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p64_q256_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um8_un4_simd8_single_block
//   P=64 Q=256 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p64_q256_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um8_un4_simd8_fixed_q
//   P=64 Q=256 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p64_q256_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um8_un4_simd8_adaptive_half
//   P=64 Q=256 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p64_q256_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um8_un4_simd8_equal_split
//   P=64 Q=256 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p64_q256_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um16_un1_simd4_single_block
//   P=64 Q=256 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q256_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um16_un1_simd4_fixed_q
//   P=64 Q=256 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q256_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um16_un1_simd4_adaptive_half
//   P=64 Q=256 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q256_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um16_un1_simd4_equal_split
//   P=64 Q=256 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q256_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um16_un1_simd8_single_block
//   P=64 Q=256 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p64_q256_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um16_un1_simd8_fixed_q
//   P=64 Q=256 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p64_q256_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um16_un1_simd8_adaptive_half
//   P=64 Q=256 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p64_q256_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um16_un1_simd8_equal_split
//   P=64 Q=256 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p64_q256_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um16_un1_simd16_single_block
//   P=64 Q=256 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p64_q256_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um16_un1_simd16_fixed_q
//   P=64 Q=256 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p64_q256_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um16_un1_simd16_adaptive_half
//   P=64 Q=256 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p64_q256_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um16_un1_simd16_equal_split
//   P=64 Q=256 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p64_q256_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um16_un4_simd8_single_block
//   P=64 Q=256 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p64_q256_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um16_un4_simd8_fixed_q
//   P=64 Q=256 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p64_q256_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um16_un4_simd8_adaptive_half
//   P=64 Q=256 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p64_q256_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um16_un4_simd8_equal_split
//   P=64 Q=256 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p64_q256_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um16_un4_simd16_single_block
//   P=64 Q=256 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p64_q256_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um16_un4_simd16_fixed_q
//   P=64 Q=256 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p64_q256_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um16_un4_simd16_adaptive_half
//   P=64 Q=256 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p64_q256_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q256_um16_un4_simd16_equal_split
//   P=64 Q=256 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p64_q256_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um1_un1_simd1_single_block
//   P=64 Q=384 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q384_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um1_un1_simd1_fixed_q
//   P=64 Q=384 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q384_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um1_un1_simd1_adaptive_half
//   P=64 Q=384 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q384_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um1_un1_simd1_equal_split
//   P=64 Q=384 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q384_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um1_un4_simd1_single_block
//   P=64 Q=384 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p64_q384_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um1_un4_simd1_fixed_q
//   P=64 Q=384 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p64_q384_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um1_un4_simd1_adaptive_half
//   P=64 Q=384 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p64_q384_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um1_un4_simd1_equal_split
//   P=64 Q=384 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p64_q384_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um4_un1_simd1_single_block
//   P=64 Q=384 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q384_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um4_un1_simd1_fixed_q
//   P=64 Q=384 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q384_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um4_un1_simd1_adaptive_half
//   P=64 Q=384 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q384_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um4_un1_simd1_equal_split
//   P=64 Q=384 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q384_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um4_un1_simd4_single_block
//   P=64 Q=384 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q384_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um4_un1_simd4_fixed_q
//   P=64 Q=384 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q384_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um4_un1_simd4_adaptive_half
//   P=64 Q=384 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q384_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um4_un1_simd4_equal_split
//   P=64 Q=384 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q384_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um4_un4_simd4_single_block
//   P=64 Q=384 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p64_q384_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um4_un4_simd4_fixed_q
//   P=64 Q=384 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p64_q384_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um4_un4_simd4_adaptive_half
//   P=64 Q=384 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p64_q384_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um4_un4_simd4_equal_split
//   P=64 Q=384 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p64_q384_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um8_un1_simd1_single_block
//   P=64 Q=384 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q384_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um8_un1_simd1_fixed_q
//   P=64 Q=384 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q384_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um8_un1_simd1_adaptive_half
//   P=64 Q=384 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q384_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um8_un1_simd1_equal_split
//   P=64 Q=384 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q384_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um8_un1_simd4_single_block
//   P=64 Q=384 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q384_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um8_un1_simd4_fixed_q
//   P=64 Q=384 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q384_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um8_un1_simd4_adaptive_half
//   P=64 Q=384 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q384_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um8_un1_simd4_equal_split
//   P=64 Q=384 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q384_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um8_un1_simd8_single_block
//   P=64 Q=384 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p64_q384_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um8_un1_simd8_fixed_q
//   P=64 Q=384 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p64_q384_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um8_un1_simd8_adaptive_half
//   P=64 Q=384 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p64_q384_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um8_un1_simd8_equal_split
//   P=64 Q=384 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p64_q384_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um8_un4_simd4_single_block
//   P=64 Q=384 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p64_q384_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um8_un4_simd4_fixed_q
//   P=64 Q=384 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p64_q384_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um8_un4_simd4_adaptive_half
//   P=64 Q=384 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p64_q384_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um8_un4_simd4_equal_split
//   P=64 Q=384 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p64_q384_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um8_un4_simd8_single_block
//   P=64 Q=384 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p64_q384_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um8_un4_simd8_fixed_q
//   P=64 Q=384 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p64_q384_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um8_un4_simd8_adaptive_half
//   P=64 Q=384 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p64_q384_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um8_un4_simd8_equal_split
//   P=64 Q=384 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p64_q384_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um16_un1_simd4_single_block
//   P=64 Q=384 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q384_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um16_un1_simd4_fixed_q
//   P=64 Q=384 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q384_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um16_un1_simd4_adaptive_half
//   P=64 Q=384 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q384_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um16_un1_simd4_equal_split
//   P=64 Q=384 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q384_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um16_un1_simd8_single_block
//   P=64 Q=384 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p64_q384_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um16_un1_simd8_fixed_q
//   P=64 Q=384 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p64_q384_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um16_un1_simd8_adaptive_half
//   P=64 Q=384 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p64_q384_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um16_un1_simd8_equal_split
//   P=64 Q=384 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p64_q384_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um16_un1_simd16_single_block
//   P=64 Q=384 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p64_q384_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um16_un1_simd16_fixed_q
//   P=64 Q=384 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p64_q384_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um16_un1_simd16_adaptive_half
//   P=64 Q=384 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p64_q384_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um16_un1_simd16_equal_split
//   P=64 Q=384 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p64_q384_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um16_un4_simd8_single_block
//   P=64 Q=384 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p64_q384_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um16_un4_simd8_fixed_q
//   P=64 Q=384 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p64_q384_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um16_un4_simd8_adaptive_half
//   P=64 Q=384 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p64_q384_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um16_un4_simd8_equal_split
//   P=64 Q=384 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p64_q384_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um16_un4_simd16_single_block
//   P=64 Q=384 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p64_q384_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um16_un4_simd16_fixed_q
//   P=64 Q=384 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p64_q384_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um16_un4_simd16_adaptive_half
//   P=64 Q=384 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p64_q384_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q384_um16_un4_simd16_equal_split
//   P=64 Q=384 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p64_q384_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um1_un1_simd1_single_block
//   P=64 Q=512 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q512_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um1_un1_simd1_fixed_q
//   P=64 Q=512 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q512_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um1_un1_simd1_adaptive_half
//   P=64 Q=512 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q512_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um1_un1_simd1_equal_split
//   P=64 Q=512 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q512_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um1_un4_simd1_single_block
//   P=64 Q=512 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p64_q512_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um1_un4_simd1_fixed_q
//   P=64 Q=512 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p64_q512_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um1_un4_simd1_adaptive_half
//   P=64 Q=512 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p64_q512_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um1_un4_simd1_equal_split
//   P=64 Q=512 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p64_q512_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um4_un1_simd1_single_block
//   P=64 Q=512 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q512_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um4_un1_simd1_fixed_q
//   P=64 Q=512 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q512_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um4_un1_simd1_adaptive_half
//   P=64 Q=512 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q512_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um4_un1_simd1_equal_split
//   P=64 Q=512 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q512_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um4_un1_simd4_single_block
//   P=64 Q=512 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q512_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um4_un1_simd4_fixed_q
//   P=64 Q=512 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q512_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um4_un1_simd4_adaptive_half
//   P=64 Q=512 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q512_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um4_un1_simd4_equal_split
//   P=64 Q=512 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q512_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um4_un4_simd4_single_block
//   P=64 Q=512 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p64_q512_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um4_un4_simd4_fixed_q
//   P=64 Q=512 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p64_q512_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um4_un4_simd4_adaptive_half
//   P=64 Q=512 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p64_q512_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um4_un4_simd4_equal_split
//   P=64 Q=512 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p64_q512_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um8_un1_simd1_single_block
//   P=64 Q=512 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p64_q512_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um8_un1_simd1_fixed_q
//   P=64 Q=512 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p64_q512_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um8_un1_simd1_adaptive_half
//   P=64 Q=512 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p64_q512_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um8_un1_simd1_equal_split
//   P=64 Q=512 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p64_q512_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um8_un1_simd4_single_block
//   P=64 Q=512 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p64_q512_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um8_un1_simd4_fixed_q
//   P=64 Q=512 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p64_q512_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um8_un1_simd4_adaptive_half
//   P=64 Q=512 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p64_q512_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um8_un1_simd4_equal_split
//   P=64 Q=512 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p64_q512_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um8_un1_simd8_single_block
//   P=64 Q=512 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p64_q512_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um8_un1_simd8_fixed_q
//   P=64 Q=512 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p64_q512_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um8_un1_simd8_adaptive_half
//   P=64 Q=512 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p64_q512_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um8_un1_simd8_equal_split
//   P=64 Q=512 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p64_q512_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um8_un4_simd4_single_block
//   P=64 Q=512 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p64_q512_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um8_un4_simd4_fixed_q
//   P=64 Q=512 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p64_q512_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um8_un4_simd4_adaptive_half
//   P=64 Q=512 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p64_q512_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um8_un4_simd4_equal_split
//   P=64 Q=512 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p64_q512_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um8_un4_simd8_single_block
//   P=64 Q=512 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p64_q512_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um8_un4_simd8_fixed_q
//   P=64 Q=512 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p64_q512_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um8_un4_simd8_adaptive_half
//   P=64 Q=512 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p64_q512_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p64_q512_um8_un4_simd8_equal_split
//   P=64 Q=512 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p64_q512_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um1_un1_simd1_single_block
//   P=128 Q=1 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q1_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um1_un1_simd1_fixed_q
//   P=128 Q=1 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q1_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um1_un1_simd1_adaptive_half
//   P=128 Q=1 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q1_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um1_un1_simd1_equal_split
//   P=128 Q=1 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q1_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um1_un4_simd1_single_block
//   P=128 Q=1 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p128_q1_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um1_un4_simd1_fixed_q
//   P=128 Q=1 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p128_q1_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um1_un4_simd1_adaptive_half
//   P=128 Q=1 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p128_q1_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um1_un4_simd1_equal_split
//   P=128 Q=1 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p128_q1_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um4_un1_simd1_single_block
//   P=128 Q=1 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q1_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um4_un1_simd1_fixed_q
//   P=128 Q=1 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q1_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um4_un1_simd1_adaptive_half
//   P=128 Q=1 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q1_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um4_un1_simd1_equal_split
//   P=128 Q=1 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q1_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um4_un1_simd4_single_block
//   P=128 Q=1 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q1_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um4_un1_simd4_fixed_q
//   P=128 Q=1 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q1_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um4_un1_simd4_adaptive_half
//   P=128 Q=1 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q1_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um4_un1_simd4_equal_split
//   P=128 Q=1 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q1_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um4_un4_simd4_single_block
//   P=128 Q=1 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p128_q1_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um4_un4_simd4_fixed_q
//   P=128 Q=1 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p128_q1_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um4_un4_simd4_adaptive_half
//   P=128 Q=1 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p128_q1_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um4_un4_simd4_equal_split
//   P=128 Q=1 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p128_q1_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um8_un1_simd1_single_block
//   P=128 Q=1 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q1_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um8_un1_simd1_fixed_q
//   P=128 Q=1 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q1_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um8_un1_simd1_adaptive_half
//   P=128 Q=1 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q1_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um8_un1_simd1_equal_split
//   P=128 Q=1 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q1_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um8_un1_simd4_single_block
//   P=128 Q=1 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q1_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um8_un1_simd4_fixed_q
//   P=128 Q=1 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q1_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um8_un1_simd4_adaptive_half
//   P=128 Q=1 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q1_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um8_un1_simd4_equal_split
//   P=128 Q=1 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q1_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um8_un1_simd8_single_block
//   P=128 Q=1 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p128_q1_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um8_un1_simd8_fixed_q
//   P=128 Q=1 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p128_q1_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um8_un1_simd8_adaptive_half
//   P=128 Q=1 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p128_q1_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um8_un1_simd8_equal_split
//   P=128 Q=1 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p128_q1_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um8_un4_simd4_single_block
//   P=128 Q=1 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p128_q1_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um8_un4_simd4_fixed_q
//   P=128 Q=1 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p128_q1_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um8_un4_simd4_adaptive_half
//   P=128 Q=1 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p128_q1_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um8_un4_simd4_equal_split
//   P=128 Q=1 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p128_q1_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um8_un4_simd8_single_block
//   P=128 Q=1 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p128_q1_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um8_un4_simd8_fixed_q
//   P=128 Q=1 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p128_q1_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um8_un4_simd8_adaptive_half
//   P=128 Q=1 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p128_q1_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um8_un4_simd8_equal_split
//   P=128 Q=1 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p128_q1_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um16_un1_simd4_single_block
//   P=128 Q=1 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q1_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um16_un1_simd4_fixed_q
//   P=128 Q=1 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q1_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um16_un1_simd4_adaptive_half
//   P=128 Q=1 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q1_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um16_un1_simd4_equal_split
//   P=128 Q=1 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q1_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um16_un1_simd8_single_block
//   P=128 Q=1 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p128_q1_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um16_un1_simd8_fixed_q
//   P=128 Q=1 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p128_q1_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um16_un1_simd8_adaptive_half
//   P=128 Q=1 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p128_q1_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um16_un1_simd8_equal_split
//   P=128 Q=1 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p128_q1_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um16_un1_simd16_single_block
//   P=128 Q=1 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p128_q1_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um16_un1_simd16_fixed_q
//   P=128 Q=1 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p128_q1_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um16_un1_simd16_adaptive_half
//   P=128 Q=1 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p128_q1_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um16_un1_simd16_equal_split
//   P=128 Q=1 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p128_q1_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um16_un4_simd8_single_block
//   P=128 Q=1 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p128_q1_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um16_un4_simd8_fixed_q
//   P=128 Q=1 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p128_q1_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um16_un4_simd8_adaptive_half
//   P=128 Q=1 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p128_q1_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um16_un4_simd8_equal_split
//   P=128 Q=1 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p128_q1_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um16_un4_simd16_single_block
//   P=128 Q=1 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p128_q1_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um16_un4_simd16_fixed_q
//   P=128 Q=1 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p128_q1_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um16_un4_simd16_adaptive_half
//   P=128 Q=1 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p128_q1_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um16_un4_simd16_equal_split
//   P=128 Q=1 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p128_q1_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um32_un1_simd4_single_block
//   P=128 Q=1 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q1_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um32_un1_simd4_fixed_q
//   P=128 Q=1 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q1_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um32_un1_simd4_adaptive_half
//   P=128 Q=1 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q1_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um32_un1_simd4_equal_split
//   P=128 Q=1 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q1_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um32_un1_simd8_single_block
//   P=128 Q=1 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p128_q1_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um32_un1_simd8_fixed_q
//   P=128 Q=1 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p128_q1_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um32_un1_simd8_adaptive_half
//   P=128 Q=1 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p128_q1_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um32_un1_simd8_equal_split
//   P=128 Q=1 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p128_q1_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um32_un1_simd16_single_block
//   P=128 Q=1 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p128_q1_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um32_un1_simd16_fixed_q
//   P=128 Q=1 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p128_q1_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um32_un1_simd16_adaptive_half
//   P=128 Q=1 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p128_q1_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um32_un1_simd16_equal_split
//   P=128 Q=1 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p128_q1_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um32_un4_simd16_single_block
//   P=128 Q=1 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p128_q1_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um32_un4_simd16_fixed_q
//   P=128 Q=1 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p128_q1_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um32_un4_simd16_adaptive_half
//   P=128 Q=1 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p128_q1_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q1_um32_un4_simd16_equal_split
//   P=128 Q=1 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p128_q1_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um1_un1_simd1_single_block
//   P=128 Q=64 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q64_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um1_un1_simd1_fixed_q
//   P=128 Q=64 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q64_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um1_un1_simd1_adaptive_half
//   P=128 Q=64 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q64_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um1_un1_simd1_equal_split
//   P=128 Q=64 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q64_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um1_un4_simd1_single_block
//   P=128 Q=64 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p128_q64_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um1_un4_simd1_fixed_q
//   P=128 Q=64 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p128_q64_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um1_un4_simd1_adaptive_half
//   P=128 Q=64 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p128_q64_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um1_un4_simd1_equal_split
//   P=128 Q=64 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p128_q64_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um4_un1_simd1_single_block
//   P=128 Q=64 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q64_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um4_un1_simd1_fixed_q
//   P=128 Q=64 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q64_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um4_un1_simd1_adaptive_half
//   P=128 Q=64 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q64_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um4_un1_simd1_equal_split
//   P=128 Q=64 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q64_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um4_un1_simd4_single_block
//   P=128 Q=64 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q64_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um4_un1_simd4_fixed_q
//   P=128 Q=64 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q64_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um4_un1_simd4_adaptive_half
//   P=128 Q=64 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q64_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um4_un1_simd4_equal_split
//   P=128 Q=64 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q64_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um4_un4_simd4_single_block
//   P=128 Q=64 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p128_q64_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um4_un4_simd4_fixed_q
//   P=128 Q=64 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p128_q64_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um4_un4_simd4_adaptive_half
//   P=128 Q=64 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p128_q64_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um4_un4_simd4_equal_split
//   P=128 Q=64 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p128_q64_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um8_un1_simd1_single_block
//   P=128 Q=64 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q64_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um8_un1_simd1_fixed_q
//   P=128 Q=64 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q64_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um8_un1_simd1_adaptive_half
//   P=128 Q=64 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q64_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um8_un1_simd1_equal_split
//   P=128 Q=64 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q64_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um8_un1_simd4_single_block
//   P=128 Q=64 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q64_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um8_un1_simd4_fixed_q
//   P=128 Q=64 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q64_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um8_un1_simd4_adaptive_half
//   P=128 Q=64 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q64_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um8_un1_simd4_equal_split
//   P=128 Q=64 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q64_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um8_un1_simd8_single_block
//   P=128 Q=64 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p128_q64_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um8_un1_simd8_fixed_q
//   P=128 Q=64 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p128_q64_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um8_un1_simd8_adaptive_half
//   P=128 Q=64 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p128_q64_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um8_un1_simd8_equal_split
//   P=128 Q=64 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p128_q64_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um8_un4_simd4_single_block
//   P=128 Q=64 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p128_q64_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um8_un4_simd4_fixed_q
//   P=128 Q=64 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p128_q64_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um8_un4_simd4_adaptive_half
//   P=128 Q=64 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p128_q64_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um8_un4_simd4_equal_split
//   P=128 Q=64 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p128_q64_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um8_un4_simd8_single_block
//   P=128 Q=64 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p128_q64_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um8_un4_simd8_fixed_q
//   P=128 Q=64 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p128_q64_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um8_un4_simd8_adaptive_half
//   P=128 Q=64 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p128_q64_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um8_un4_simd8_equal_split
//   P=128 Q=64 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p128_q64_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um16_un1_simd4_single_block
//   P=128 Q=64 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q64_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um16_un1_simd4_fixed_q
//   P=128 Q=64 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q64_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um16_un1_simd4_adaptive_half
//   P=128 Q=64 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q64_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um16_un1_simd4_equal_split
//   P=128 Q=64 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q64_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um16_un1_simd8_single_block
//   P=128 Q=64 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p128_q64_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um16_un1_simd8_fixed_q
//   P=128 Q=64 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p128_q64_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um16_un1_simd8_adaptive_half
//   P=128 Q=64 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p128_q64_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um16_un1_simd8_equal_split
//   P=128 Q=64 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p128_q64_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um16_un1_simd16_single_block
//   P=128 Q=64 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p128_q64_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um16_un1_simd16_fixed_q
//   P=128 Q=64 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p128_q64_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um16_un1_simd16_adaptive_half
//   P=128 Q=64 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p128_q64_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um16_un1_simd16_equal_split
//   P=128 Q=64 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p128_q64_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um16_un4_simd8_single_block
//   P=128 Q=64 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p128_q64_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um16_un4_simd8_fixed_q
//   P=128 Q=64 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p128_q64_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um16_un4_simd8_adaptive_half
//   P=128 Q=64 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p128_q64_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um16_un4_simd8_equal_split
//   P=128 Q=64 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p128_q64_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um16_un4_simd16_single_block
//   P=128 Q=64 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p128_q64_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um16_un4_simd16_fixed_q
//   P=128 Q=64 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p128_q64_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um16_un4_simd16_adaptive_half
//   P=128 Q=64 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p128_q64_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um16_un4_simd16_equal_split
//   P=128 Q=64 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p128_q64_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um32_un1_simd4_single_block
//   P=128 Q=64 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q64_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um32_un1_simd4_fixed_q
//   P=128 Q=64 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q64_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um32_un1_simd4_adaptive_half
//   P=128 Q=64 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q64_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um32_un1_simd4_equal_split
//   P=128 Q=64 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q64_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um32_un1_simd8_single_block
//   P=128 Q=64 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p128_q64_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um32_un1_simd8_fixed_q
//   P=128 Q=64 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p128_q64_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um32_un1_simd8_adaptive_half
//   P=128 Q=64 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p128_q64_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um32_un1_simd8_equal_split
//   P=128 Q=64 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p128_q64_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um32_un1_simd16_single_block
//   P=128 Q=64 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p128_q64_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um32_un1_simd16_fixed_q
//   P=128 Q=64 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p128_q64_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um32_un1_simd16_adaptive_half
//   P=128 Q=64 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p128_q64_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um32_un1_simd16_equal_split
//   P=128 Q=64 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p128_q64_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um32_un4_simd16_single_block
//   P=128 Q=64 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p128_q64_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um32_un4_simd16_fixed_q
//   P=128 Q=64 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p128_q64_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um32_un4_simd16_adaptive_half
//   P=128 Q=64 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p128_q64_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q64_um32_un4_simd16_equal_split
//   P=128 Q=64 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p128_q64_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um1_un1_simd1_single_block
//   P=128 Q=128 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q128_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um1_un1_simd1_fixed_q
//   P=128 Q=128 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q128_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um1_un1_simd1_adaptive_half
//   P=128 Q=128 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q128_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um1_un1_simd1_equal_split
//   P=128 Q=128 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q128_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um1_un4_simd1_single_block
//   P=128 Q=128 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p128_q128_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um1_un4_simd1_fixed_q
//   P=128 Q=128 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p128_q128_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um1_un4_simd1_adaptive_half
//   P=128 Q=128 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p128_q128_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um1_un4_simd1_equal_split
//   P=128 Q=128 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p128_q128_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um4_un1_simd1_single_block
//   P=128 Q=128 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q128_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um4_un1_simd1_fixed_q
//   P=128 Q=128 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q128_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um4_un1_simd1_adaptive_half
//   P=128 Q=128 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q128_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um4_un1_simd1_equal_split
//   P=128 Q=128 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q128_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um4_un1_simd4_single_block
//   P=128 Q=128 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q128_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um4_un1_simd4_fixed_q
//   P=128 Q=128 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q128_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um4_un1_simd4_adaptive_half
//   P=128 Q=128 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q128_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um4_un1_simd4_equal_split
//   P=128 Q=128 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q128_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um4_un4_simd4_single_block
//   P=128 Q=128 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p128_q128_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um4_un4_simd4_fixed_q
//   P=128 Q=128 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p128_q128_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um4_un4_simd4_adaptive_half
//   P=128 Q=128 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p128_q128_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um4_un4_simd4_equal_split
//   P=128 Q=128 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p128_q128_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um8_un1_simd1_single_block
//   P=128 Q=128 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q128_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um8_un1_simd1_fixed_q
//   P=128 Q=128 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q128_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um8_un1_simd1_adaptive_half
//   P=128 Q=128 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q128_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um8_un1_simd1_equal_split
//   P=128 Q=128 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q128_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um8_un1_simd4_single_block
//   P=128 Q=128 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q128_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um8_un1_simd4_fixed_q
//   P=128 Q=128 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q128_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um8_un1_simd4_adaptive_half
//   P=128 Q=128 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q128_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um8_un1_simd4_equal_split
//   P=128 Q=128 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q128_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um8_un1_simd8_single_block
//   P=128 Q=128 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p128_q128_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um8_un1_simd8_fixed_q
//   P=128 Q=128 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p128_q128_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um8_un1_simd8_adaptive_half
//   P=128 Q=128 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p128_q128_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um8_un1_simd8_equal_split
//   P=128 Q=128 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p128_q128_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um8_un4_simd4_single_block
//   P=128 Q=128 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p128_q128_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um8_un4_simd4_fixed_q
//   P=128 Q=128 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p128_q128_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um8_un4_simd4_adaptive_half
//   P=128 Q=128 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p128_q128_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um8_un4_simd4_equal_split
//   P=128 Q=128 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p128_q128_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um8_un4_simd8_single_block
//   P=128 Q=128 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p128_q128_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um8_un4_simd8_fixed_q
//   P=128 Q=128 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p128_q128_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um8_un4_simd8_adaptive_half
//   P=128 Q=128 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p128_q128_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um8_un4_simd8_equal_split
//   P=128 Q=128 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p128_q128_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um16_un1_simd4_single_block
//   P=128 Q=128 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q128_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um16_un1_simd4_fixed_q
//   P=128 Q=128 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q128_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um16_un1_simd4_adaptive_half
//   P=128 Q=128 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q128_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um16_un1_simd4_equal_split
//   P=128 Q=128 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q128_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um16_un1_simd8_single_block
//   P=128 Q=128 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p128_q128_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um16_un1_simd8_fixed_q
//   P=128 Q=128 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p128_q128_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um16_un1_simd8_adaptive_half
//   P=128 Q=128 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p128_q128_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um16_un1_simd8_equal_split
//   P=128 Q=128 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p128_q128_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um16_un1_simd16_single_block
//   P=128 Q=128 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p128_q128_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um16_un1_simd16_fixed_q
//   P=128 Q=128 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p128_q128_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um16_un1_simd16_adaptive_half
//   P=128 Q=128 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p128_q128_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um16_un1_simd16_equal_split
//   P=128 Q=128 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p128_q128_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um16_un4_simd8_single_block
//   P=128 Q=128 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p128_q128_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um16_un4_simd8_fixed_q
//   P=128 Q=128 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p128_q128_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um16_un4_simd8_adaptive_half
//   P=128 Q=128 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p128_q128_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um16_un4_simd8_equal_split
//   P=128 Q=128 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p128_q128_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um16_un4_simd16_single_block
//   P=128 Q=128 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p128_q128_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um16_un4_simd16_fixed_q
//   P=128 Q=128 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p128_q128_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um16_un4_simd16_adaptive_half
//   P=128 Q=128 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p128_q128_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um16_un4_simd16_equal_split
//   P=128 Q=128 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p128_q128_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um32_un1_simd4_single_block
//   P=128 Q=128 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q128_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um32_un1_simd4_fixed_q
//   P=128 Q=128 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q128_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um32_un1_simd4_adaptive_half
//   P=128 Q=128 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q128_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um32_un1_simd4_equal_split
//   P=128 Q=128 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q128_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um32_un1_simd8_single_block
//   P=128 Q=128 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p128_q128_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um32_un1_simd8_fixed_q
//   P=128 Q=128 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p128_q128_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um32_un1_simd8_adaptive_half
//   P=128 Q=128 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p128_q128_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um32_un1_simd8_equal_split
//   P=128 Q=128 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p128_q128_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um32_un1_simd16_single_block
//   P=128 Q=128 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p128_q128_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um32_un1_simd16_fixed_q
//   P=128 Q=128 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p128_q128_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um32_un1_simd16_adaptive_half
//   P=128 Q=128 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p128_q128_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um32_un1_simd16_equal_split
//   P=128 Q=128 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p128_q128_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um32_un4_simd16_single_block
//   P=128 Q=128 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p128_q128_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um32_un4_simd16_fixed_q
//   P=128 Q=128 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p128_q128_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um32_un4_simd16_adaptive_half
//   P=128 Q=128 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p128_q128_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q128_um32_un4_simd16_equal_split
//   P=128 Q=128 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p128_q128_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um1_un1_simd1_single_block
//   P=128 Q=192 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q192_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um1_un1_simd1_fixed_q
//   P=128 Q=192 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q192_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um1_un1_simd1_adaptive_half
//   P=128 Q=192 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q192_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um1_un1_simd1_equal_split
//   P=128 Q=192 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q192_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um1_un4_simd1_single_block
//   P=128 Q=192 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p128_q192_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um1_un4_simd1_fixed_q
//   P=128 Q=192 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p128_q192_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um1_un4_simd1_adaptive_half
//   P=128 Q=192 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p128_q192_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um1_un4_simd1_equal_split
//   P=128 Q=192 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p128_q192_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um4_un1_simd1_single_block
//   P=128 Q=192 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q192_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um4_un1_simd1_fixed_q
//   P=128 Q=192 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q192_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um4_un1_simd1_adaptive_half
//   P=128 Q=192 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q192_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um4_un1_simd1_equal_split
//   P=128 Q=192 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q192_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um4_un1_simd4_single_block
//   P=128 Q=192 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q192_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um4_un1_simd4_fixed_q
//   P=128 Q=192 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q192_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um4_un1_simd4_adaptive_half
//   P=128 Q=192 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q192_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um4_un1_simd4_equal_split
//   P=128 Q=192 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q192_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um4_un4_simd4_single_block
//   P=128 Q=192 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p128_q192_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um4_un4_simd4_fixed_q
//   P=128 Q=192 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p128_q192_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um4_un4_simd4_adaptive_half
//   P=128 Q=192 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p128_q192_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um4_un4_simd4_equal_split
//   P=128 Q=192 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p128_q192_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um8_un1_simd1_single_block
//   P=128 Q=192 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q192_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um8_un1_simd1_fixed_q
//   P=128 Q=192 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q192_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um8_un1_simd1_adaptive_half
//   P=128 Q=192 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q192_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um8_un1_simd1_equal_split
//   P=128 Q=192 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q192_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um8_un1_simd4_single_block
//   P=128 Q=192 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q192_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um8_un1_simd4_fixed_q
//   P=128 Q=192 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q192_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um8_un1_simd4_adaptive_half
//   P=128 Q=192 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q192_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um8_un1_simd4_equal_split
//   P=128 Q=192 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q192_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um8_un1_simd8_single_block
//   P=128 Q=192 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p128_q192_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um8_un1_simd8_fixed_q
//   P=128 Q=192 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p128_q192_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um8_un1_simd8_adaptive_half
//   P=128 Q=192 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p128_q192_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um8_un1_simd8_equal_split
//   P=128 Q=192 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p128_q192_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um8_un4_simd4_single_block
//   P=128 Q=192 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p128_q192_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um8_un4_simd4_fixed_q
//   P=128 Q=192 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p128_q192_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um8_un4_simd4_adaptive_half
//   P=128 Q=192 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p128_q192_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um8_un4_simd4_equal_split
//   P=128 Q=192 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p128_q192_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um8_un4_simd8_single_block
//   P=128 Q=192 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p128_q192_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um8_un4_simd8_fixed_q
//   P=128 Q=192 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p128_q192_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um8_un4_simd8_adaptive_half
//   P=128 Q=192 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p128_q192_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um8_un4_simd8_equal_split
//   P=128 Q=192 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p128_q192_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um16_un1_simd4_single_block
//   P=128 Q=192 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q192_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um16_un1_simd4_fixed_q
//   P=128 Q=192 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q192_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um16_un1_simd4_adaptive_half
//   P=128 Q=192 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q192_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um16_un1_simd4_equal_split
//   P=128 Q=192 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q192_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um16_un1_simd8_single_block
//   P=128 Q=192 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p128_q192_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um16_un1_simd8_fixed_q
//   P=128 Q=192 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p128_q192_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um16_un1_simd8_adaptive_half
//   P=128 Q=192 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p128_q192_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um16_un1_simd8_equal_split
//   P=128 Q=192 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p128_q192_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um16_un1_simd16_single_block
//   P=128 Q=192 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p128_q192_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um16_un1_simd16_fixed_q
//   P=128 Q=192 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p128_q192_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um16_un1_simd16_adaptive_half
//   P=128 Q=192 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p128_q192_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um16_un1_simd16_equal_split
//   P=128 Q=192 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p128_q192_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um16_un4_simd8_single_block
//   P=128 Q=192 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p128_q192_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um16_un4_simd8_fixed_q
//   P=128 Q=192 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p128_q192_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um16_un4_simd8_adaptive_half
//   P=128 Q=192 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p128_q192_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um16_un4_simd8_equal_split
//   P=128 Q=192 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p128_q192_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um16_un4_simd16_single_block
//   P=128 Q=192 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p128_q192_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um16_un4_simd16_fixed_q
//   P=128 Q=192 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p128_q192_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um16_un4_simd16_adaptive_half
//   P=128 Q=192 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p128_q192_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um16_un4_simd16_equal_split
//   P=128 Q=192 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p128_q192_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um32_un1_simd4_single_block
//   P=128 Q=192 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q192_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um32_un1_simd4_fixed_q
//   P=128 Q=192 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q192_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um32_un1_simd4_adaptive_half
//   P=128 Q=192 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q192_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um32_un1_simd4_equal_split
//   P=128 Q=192 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q192_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um32_un1_simd8_single_block
//   P=128 Q=192 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p128_q192_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um32_un1_simd8_fixed_q
//   P=128 Q=192 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p128_q192_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um32_un1_simd8_adaptive_half
//   P=128 Q=192 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p128_q192_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um32_un1_simd8_equal_split
//   P=128 Q=192 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p128_q192_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um32_un1_simd16_single_block
//   P=128 Q=192 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p128_q192_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um32_un1_simd16_fixed_q
//   P=128 Q=192 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p128_q192_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um32_un1_simd16_adaptive_half
//   P=128 Q=192 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p128_q192_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um32_un1_simd16_equal_split
//   P=128 Q=192 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p128_q192_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um32_un4_simd16_single_block
//   P=128 Q=192 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p128_q192_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um32_un4_simd16_fixed_q
//   P=128 Q=192 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p128_q192_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um32_un4_simd16_adaptive_half
//   P=128 Q=192 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p128_q192_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q192_um32_un4_simd16_equal_split
//   P=128 Q=192 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p128_q192_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um1_un1_simd1_single_block
//   P=128 Q=240 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q240_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um1_un1_simd1_fixed_q
//   P=128 Q=240 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q240_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um1_un1_simd1_adaptive_half
//   P=128 Q=240 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q240_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um1_un1_simd1_equal_split
//   P=128 Q=240 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q240_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um1_un4_simd1_single_block
//   P=128 Q=240 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p128_q240_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um1_un4_simd1_fixed_q
//   P=128 Q=240 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p128_q240_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um1_un4_simd1_adaptive_half
//   P=128 Q=240 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p128_q240_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um1_un4_simd1_equal_split
//   P=128 Q=240 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p128_q240_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um4_un1_simd1_single_block
//   P=128 Q=240 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q240_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um4_un1_simd1_fixed_q
//   P=128 Q=240 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q240_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um4_un1_simd1_adaptive_half
//   P=128 Q=240 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q240_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um4_un1_simd1_equal_split
//   P=128 Q=240 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q240_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um4_un1_simd4_single_block
//   P=128 Q=240 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q240_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um4_un1_simd4_fixed_q
//   P=128 Q=240 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q240_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um4_un1_simd4_adaptive_half
//   P=128 Q=240 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q240_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um4_un1_simd4_equal_split
//   P=128 Q=240 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q240_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um4_un4_simd4_single_block
//   P=128 Q=240 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p128_q240_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um4_un4_simd4_fixed_q
//   P=128 Q=240 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p128_q240_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um4_un4_simd4_adaptive_half
//   P=128 Q=240 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p128_q240_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um4_un4_simd4_equal_split
//   P=128 Q=240 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p128_q240_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um8_un1_simd1_single_block
//   P=128 Q=240 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q240_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um8_un1_simd1_fixed_q
//   P=128 Q=240 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q240_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um8_un1_simd1_adaptive_half
//   P=128 Q=240 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q240_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um8_un1_simd1_equal_split
//   P=128 Q=240 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q240_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um8_un1_simd4_single_block
//   P=128 Q=240 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q240_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um8_un1_simd4_fixed_q
//   P=128 Q=240 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q240_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um8_un1_simd4_adaptive_half
//   P=128 Q=240 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q240_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um8_un1_simd4_equal_split
//   P=128 Q=240 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q240_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um8_un1_simd8_single_block
//   P=128 Q=240 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p128_q240_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um8_un1_simd8_fixed_q
//   P=128 Q=240 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p128_q240_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um8_un1_simd8_adaptive_half
//   P=128 Q=240 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p128_q240_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um8_un1_simd8_equal_split
//   P=128 Q=240 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p128_q240_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um8_un4_simd4_single_block
//   P=128 Q=240 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p128_q240_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um8_un4_simd4_fixed_q
//   P=128 Q=240 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p128_q240_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um8_un4_simd4_adaptive_half
//   P=128 Q=240 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p128_q240_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um8_un4_simd4_equal_split
//   P=128 Q=240 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p128_q240_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um8_un4_simd8_single_block
//   P=128 Q=240 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p128_q240_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um8_un4_simd8_fixed_q
//   P=128 Q=240 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p128_q240_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um8_un4_simd8_adaptive_half
//   P=128 Q=240 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p128_q240_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um8_un4_simd8_equal_split
//   P=128 Q=240 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p128_q240_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um16_un1_simd4_single_block
//   P=128 Q=240 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q240_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um16_un1_simd4_fixed_q
//   P=128 Q=240 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q240_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um16_un1_simd4_adaptive_half
//   P=128 Q=240 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q240_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um16_un1_simd4_equal_split
//   P=128 Q=240 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q240_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um16_un1_simd8_single_block
//   P=128 Q=240 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p128_q240_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um16_un1_simd8_fixed_q
//   P=128 Q=240 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p128_q240_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um16_un1_simd8_adaptive_half
//   P=128 Q=240 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p128_q240_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um16_un1_simd8_equal_split
//   P=128 Q=240 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p128_q240_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um16_un1_simd16_single_block
//   P=128 Q=240 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p128_q240_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um16_un1_simd16_fixed_q
//   P=128 Q=240 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p128_q240_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um16_un1_simd16_adaptive_half
//   P=128 Q=240 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p128_q240_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um16_un1_simd16_equal_split
//   P=128 Q=240 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p128_q240_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um16_un4_simd8_single_block
//   P=128 Q=240 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p128_q240_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um16_un4_simd8_fixed_q
//   P=128 Q=240 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p128_q240_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um16_un4_simd8_adaptive_half
//   P=128 Q=240 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p128_q240_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um16_un4_simd8_equal_split
//   P=128 Q=240 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p128_q240_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um16_un4_simd16_single_block
//   P=128 Q=240 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p128_q240_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um16_un4_simd16_fixed_q
//   P=128 Q=240 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p128_q240_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um16_un4_simd16_adaptive_half
//   P=128 Q=240 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p128_q240_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um16_un4_simd16_equal_split
//   P=128 Q=240 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p128_q240_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um32_un1_simd4_single_block
//   P=128 Q=240 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q240_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um32_un1_simd4_fixed_q
//   P=128 Q=240 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q240_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um32_un1_simd4_adaptive_half
//   P=128 Q=240 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q240_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um32_un1_simd4_equal_split
//   P=128 Q=240 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q240_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um32_un1_simd8_single_block
//   P=128 Q=240 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p128_q240_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um32_un1_simd8_fixed_q
//   P=128 Q=240 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p128_q240_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um32_un1_simd8_adaptive_half
//   P=128 Q=240 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p128_q240_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um32_un1_simd8_equal_split
//   P=128 Q=240 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p128_q240_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um32_un1_simd16_single_block
//   P=128 Q=240 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p128_q240_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um32_un1_simd16_fixed_q
//   P=128 Q=240 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p128_q240_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um32_un1_simd16_adaptive_half
//   P=128 Q=240 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p128_q240_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q240_um32_un1_simd16_equal_split
//   P=128 Q=240 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p128_q240_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um1_un1_simd1_single_block
//   P=128 Q=248 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q248_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um1_un1_simd1_fixed_q
//   P=128 Q=248 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q248_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um1_un1_simd1_adaptive_half
//   P=128 Q=248 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q248_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um1_un1_simd1_equal_split
//   P=128 Q=248 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q248_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um1_un4_simd1_single_block
//   P=128 Q=248 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p128_q248_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um1_un4_simd1_fixed_q
//   P=128 Q=248 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p128_q248_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um1_un4_simd1_adaptive_half
//   P=128 Q=248 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p128_q248_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um1_un4_simd1_equal_split
//   P=128 Q=248 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p128_q248_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um4_un1_simd1_single_block
//   P=128 Q=248 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q248_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um4_un1_simd1_fixed_q
//   P=128 Q=248 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q248_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um4_un1_simd1_adaptive_half
//   P=128 Q=248 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q248_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um4_un1_simd1_equal_split
//   P=128 Q=248 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q248_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um4_un1_simd4_single_block
//   P=128 Q=248 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q248_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um4_un1_simd4_fixed_q
//   P=128 Q=248 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q248_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um4_un1_simd4_adaptive_half
//   P=128 Q=248 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q248_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um4_un1_simd4_equal_split
//   P=128 Q=248 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q248_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um4_un4_simd4_single_block
//   P=128 Q=248 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p128_q248_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um4_un4_simd4_fixed_q
//   P=128 Q=248 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p128_q248_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um4_un4_simd4_adaptive_half
//   P=128 Q=248 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p128_q248_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um4_un4_simd4_equal_split
//   P=128 Q=248 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p128_q248_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um8_un1_simd1_single_block
//   P=128 Q=248 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q248_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um8_un1_simd1_fixed_q
//   P=128 Q=248 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q248_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um8_un1_simd1_adaptive_half
//   P=128 Q=248 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q248_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um8_un1_simd1_equal_split
//   P=128 Q=248 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q248_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um8_un1_simd4_single_block
//   P=128 Q=248 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q248_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um8_un1_simd4_fixed_q
//   P=128 Q=248 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q248_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um8_un1_simd4_adaptive_half
//   P=128 Q=248 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q248_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um8_un1_simd4_equal_split
//   P=128 Q=248 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q248_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um8_un1_simd8_single_block
//   P=128 Q=248 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p128_q248_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um8_un1_simd8_fixed_q
//   P=128 Q=248 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p128_q248_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um8_un1_simd8_adaptive_half
//   P=128 Q=248 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p128_q248_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um8_un1_simd8_equal_split
//   P=128 Q=248 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p128_q248_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um8_un4_simd4_single_block
//   P=128 Q=248 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p128_q248_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um8_un4_simd4_fixed_q
//   P=128 Q=248 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p128_q248_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um8_un4_simd4_adaptive_half
//   P=128 Q=248 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p128_q248_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um8_un4_simd4_equal_split
//   P=128 Q=248 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p128_q248_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um8_un4_simd8_single_block
//   P=128 Q=248 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p128_q248_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um8_un4_simd8_fixed_q
//   P=128 Q=248 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p128_q248_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um8_un4_simd8_adaptive_half
//   P=128 Q=248 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p128_q248_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um8_un4_simd8_equal_split
//   P=128 Q=248 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p128_q248_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um16_un1_simd4_single_block
//   P=128 Q=248 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q248_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um16_un1_simd4_fixed_q
//   P=128 Q=248 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q248_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um16_un1_simd4_adaptive_half
//   P=128 Q=248 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q248_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um16_un1_simd4_equal_split
//   P=128 Q=248 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q248_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um16_un1_simd8_single_block
//   P=128 Q=248 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p128_q248_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um16_un1_simd8_fixed_q
//   P=128 Q=248 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p128_q248_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um16_un1_simd8_adaptive_half
//   P=128 Q=248 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p128_q248_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um16_un1_simd8_equal_split
//   P=128 Q=248 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p128_q248_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um16_un1_simd16_single_block
//   P=128 Q=248 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p128_q248_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um16_un1_simd16_fixed_q
//   P=128 Q=248 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p128_q248_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um16_un1_simd16_adaptive_half
//   P=128 Q=248 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p128_q248_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um16_un1_simd16_equal_split
//   P=128 Q=248 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p128_q248_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um16_un4_simd8_single_block
//   P=128 Q=248 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p128_q248_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um16_un4_simd8_fixed_q
//   P=128 Q=248 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p128_q248_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um16_un4_simd8_adaptive_half
//   P=128 Q=248 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p128_q248_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um16_un4_simd8_equal_split
//   P=128 Q=248 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p128_q248_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um16_un4_simd16_single_block
//   P=128 Q=248 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p128_q248_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um16_un4_simd16_fixed_q
//   P=128 Q=248 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p128_q248_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um16_un4_simd16_adaptive_half
//   P=128 Q=248 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p128_q248_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um16_un4_simd16_equal_split
//   P=128 Q=248 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p128_q248_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um32_un1_simd4_single_block
//   P=128 Q=248 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q248_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um32_un1_simd4_fixed_q
//   P=128 Q=248 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q248_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um32_un1_simd4_adaptive_half
//   P=128 Q=248 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q248_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um32_un1_simd4_equal_split
//   P=128 Q=248 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q248_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um32_un1_simd8_single_block
//   P=128 Q=248 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p128_q248_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um32_un1_simd8_fixed_q
//   P=128 Q=248 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p128_q248_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um32_un1_simd8_adaptive_half
//   P=128 Q=248 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p128_q248_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um32_un1_simd8_equal_split
//   P=128 Q=248 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p128_q248_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um32_un1_simd16_single_block
//   P=128 Q=248 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p128_q248_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um32_un1_simd16_fixed_q
//   P=128 Q=248 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p128_q248_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um32_un1_simd16_adaptive_half
//   P=128 Q=248 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p128_q248_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q248_um32_un1_simd16_equal_split
//   P=128 Q=248 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p128_q248_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um1_un1_simd1_single_block
//   P=128 Q=256 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q256_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um1_un1_simd1_fixed_q
//   P=128 Q=256 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q256_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um1_un1_simd1_adaptive_half
//   P=128 Q=256 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q256_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um1_un1_simd1_equal_split
//   P=128 Q=256 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q256_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um1_un4_simd1_single_block
//   P=128 Q=256 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p128_q256_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um1_un4_simd1_fixed_q
//   P=128 Q=256 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p128_q256_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um1_un4_simd1_adaptive_half
//   P=128 Q=256 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p128_q256_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um1_un4_simd1_equal_split
//   P=128 Q=256 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p128_q256_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um4_un1_simd1_single_block
//   P=128 Q=256 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q256_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um4_un1_simd1_fixed_q
//   P=128 Q=256 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q256_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um4_un1_simd1_adaptive_half
//   P=128 Q=256 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q256_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um4_un1_simd1_equal_split
//   P=128 Q=256 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q256_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um4_un1_simd4_single_block
//   P=128 Q=256 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q256_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um4_un1_simd4_fixed_q
//   P=128 Q=256 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q256_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um4_un1_simd4_adaptive_half
//   P=128 Q=256 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q256_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um4_un1_simd4_equal_split
//   P=128 Q=256 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q256_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um4_un4_simd4_single_block
//   P=128 Q=256 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p128_q256_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um4_un4_simd4_fixed_q
//   P=128 Q=256 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p128_q256_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um4_un4_simd4_adaptive_half
//   P=128 Q=256 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p128_q256_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um4_un4_simd4_equal_split
//   P=128 Q=256 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p128_q256_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um8_un1_simd1_single_block
//   P=128 Q=256 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q256_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um8_un1_simd1_fixed_q
//   P=128 Q=256 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q256_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um8_un1_simd1_adaptive_half
//   P=128 Q=256 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q256_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um8_un1_simd1_equal_split
//   P=128 Q=256 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q256_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um8_un1_simd4_single_block
//   P=128 Q=256 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q256_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um8_un1_simd4_fixed_q
//   P=128 Q=256 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q256_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um8_un1_simd4_adaptive_half
//   P=128 Q=256 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q256_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um8_un1_simd4_equal_split
//   P=128 Q=256 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q256_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um8_un1_simd8_single_block
//   P=128 Q=256 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p128_q256_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um8_un1_simd8_fixed_q
//   P=128 Q=256 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p128_q256_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um8_un1_simd8_adaptive_half
//   P=128 Q=256 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p128_q256_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um8_un1_simd8_equal_split
//   P=128 Q=256 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p128_q256_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um8_un4_simd4_single_block
//   P=128 Q=256 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p128_q256_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um8_un4_simd4_fixed_q
//   P=128 Q=256 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p128_q256_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um8_un4_simd4_adaptive_half
//   P=128 Q=256 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p128_q256_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um8_un4_simd4_equal_split
//   P=128 Q=256 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p128_q256_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um8_un4_simd8_single_block
//   P=128 Q=256 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p128_q256_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um8_un4_simd8_fixed_q
//   P=128 Q=256 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p128_q256_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um8_un4_simd8_adaptive_half
//   P=128 Q=256 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p128_q256_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um8_un4_simd8_equal_split
//   P=128 Q=256 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p128_q256_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um16_un1_simd4_single_block
//   P=128 Q=256 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q256_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um16_un1_simd4_fixed_q
//   P=128 Q=256 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q256_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um16_un1_simd4_adaptive_half
//   P=128 Q=256 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q256_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um16_un1_simd4_equal_split
//   P=128 Q=256 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q256_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um16_un1_simd8_single_block
//   P=128 Q=256 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p128_q256_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um16_un1_simd8_fixed_q
//   P=128 Q=256 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p128_q256_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um16_un1_simd8_adaptive_half
//   P=128 Q=256 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p128_q256_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um16_un1_simd8_equal_split
//   P=128 Q=256 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p128_q256_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um16_un1_simd16_single_block
//   P=128 Q=256 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p128_q256_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um16_un1_simd16_fixed_q
//   P=128 Q=256 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p128_q256_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um16_un1_simd16_adaptive_half
//   P=128 Q=256 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p128_q256_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um16_un1_simd16_equal_split
//   P=128 Q=256 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p128_q256_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um16_un4_simd8_single_block
//   P=128 Q=256 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p128_q256_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um16_un4_simd8_fixed_q
//   P=128 Q=256 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p128_q256_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um16_un4_simd8_adaptive_half
//   P=128 Q=256 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p128_q256_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um16_un4_simd8_equal_split
//   P=128 Q=256 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p128_q256_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um16_un4_simd16_single_block
//   P=128 Q=256 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p128_q256_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um16_un4_simd16_fixed_q
//   P=128 Q=256 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p128_q256_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um16_un4_simd16_adaptive_half
//   P=128 Q=256 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p128_q256_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q256_um16_un4_simd16_equal_split
//   P=128 Q=256 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p128_q256_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um1_un1_simd1_single_block
//   P=128 Q=384 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q384_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um1_un1_simd1_fixed_q
//   P=128 Q=384 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q384_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um1_un1_simd1_adaptive_half
//   P=128 Q=384 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q384_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um1_un1_simd1_equal_split
//   P=128 Q=384 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q384_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um1_un4_simd1_single_block
//   P=128 Q=384 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p128_q384_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um1_un4_simd1_fixed_q
//   P=128 Q=384 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p128_q384_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um1_un4_simd1_adaptive_half
//   P=128 Q=384 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p128_q384_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um1_un4_simd1_equal_split
//   P=128 Q=384 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p128_q384_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um4_un1_simd1_single_block
//   P=128 Q=384 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q384_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um4_un1_simd1_fixed_q
//   P=128 Q=384 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q384_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um4_un1_simd1_adaptive_half
//   P=128 Q=384 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q384_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um4_un1_simd1_equal_split
//   P=128 Q=384 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q384_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um4_un1_simd4_single_block
//   P=128 Q=384 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q384_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um4_un1_simd4_fixed_q
//   P=128 Q=384 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q384_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um4_un1_simd4_adaptive_half
//   P=128 Q=384 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q384_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um4_un1_simd4_equal_split
//   P=128 Q=384 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q384_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um4_un4_simd4_single_block
//   P=128 Q=384 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p128_q384_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um4_un4_simd4_fixed_q
//   P=128 Q=384 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p128_q384_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um4_un4_simd4_adaptive_half
//   P=128 Q=384 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p128_q384_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um4_un4_simd4_equal_split
//   P=128 Q=384 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p128_q384_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um8_un1_simd1_single_block
//   P=128 Q=384 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q384_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um8_un1_simd1_fixed_q
//   P=128 Q=384 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q384_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um8_un1_simd1_adaptive_half
//   P=128 Q=384 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q384_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um8_un1_simd1_equal_split
//   P=128 Q=384 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q384_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um8_un1_simd4_single_block
//   P=128 Q=384 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q384_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um8_un1_simd4_fixed_q
//   P=128 Q=384 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q384_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um8_un1_simd4_adaptive_half
//   P=128 Q=384 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q384_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um8_un1_simd4_equal_split
//   P=128 Q=384 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q384_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um8_un1_simd8_single_block
//   P=128 Q=384 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p128_q384_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um8_un1_simd8_fixed_q
//   P=128 Q=384 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p128_q384_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um8_un1_simd8_adaptive_half
//   P=128 Q=384 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p128_q384_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um8_un1_simd8_equal_split
//   P=128 Q=384 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p128_q384_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um8_un4_simd4_single_block
//   P=128 Q=384 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p128_q384_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um8_un4_simd4_fixed_q
//   P=128 Q=384 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p128_q384_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um8_un4_simd4_adaptive_half
//   P=128 Q=384 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p128_q384_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um8_un4_simd4_equal_split
//   P=128 Q=384 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p128_q384_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um8_un4_simd8_single_block
//   P=128 Q=384 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p128_q384_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um8_un4_simd8_fixed_q
//   P=128 Q=384 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p128_q384_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um8_un4_simd8_adaptive_half
//   P=128 Q=384 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p128_q384_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um8_un4_simd8_equal_split
//   P=128 Q=384 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p128_q384_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um16_un1_simd4_single_block
//   P=128 Q=384 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q384_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um16_un1_simd4_fixed_q
//   P=128 Q=384 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q384_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um16_un1_simd4_adaptive_half
//   P=128 Q=384 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q384_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um16_un1_simd4_equal_split
//   P=128 Q=384 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q384_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um16_un1_simd8_single_block
//   P=128 Q=384 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p128_q384_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um16_un1_simd8_fixed_q
//   P=128 Q=384 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p128_q384_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um16_un1_simd8_adaptive_half
//   P=128 Q=384 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p128_q384_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um16_un1_simd8_equal_split
//   P=128 Q=384 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p128_q384_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um16_un1_simd16_single_block
//   P=128 Q=384 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p128_q384_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um16_un1_simd16_fixed_q
//   P=128 Q=384 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p128_q384_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um16_un1_simd16_adaptive_half
//   P=128 Q=384 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p128_q384_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um16_un1_simd16_equal_split
//   P=128 Q=384 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p128_q384_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um16_un4_simd8_single_block
//   P=128 Q=384 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p128_q384_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um16_un4_simd8_fixed_q
//   P=128 Q=384 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p128_q384_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um16_un4_simd8_adaptive_half
//   P=128 Q=384 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p128_q384_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um16_un4_simd8_equal_split
//   P=128 Q=384 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p128_q384_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um16_un4_simd16_single_block
//   P=128 Q=384 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p128_q384_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um16_un4_simd16_fixed_q
//   P=128 Q=384 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p128_q384_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um16_un4_simd16_adaptive_half
//   P=128 Q=384 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p128_q384_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q384_um16_un4_simd16_equal_split
//   P=128 Q=384 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p128_q384_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um1_un1_simd1_single_block
//   P=128 Q=512 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q512_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um1_un1_simd1_fixed_q
//   P=128 Q=512 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q512_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um1_un1_simd1_adaptive_half
//   P=128 Q=512 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q512_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um1_un1_simd1_equal_split
//   P=128 Q=512 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q512_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um1_un4_simd1_single_block
//   P=128 Q=512 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p128_q512_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um1_un4_simd1_fixed_q
//   P=128 Q=512 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p128_q512_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um1_un4_simd1_adaptive_half
//   P=128 Q=512 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p128_q512_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um1_un4_simd1_equal_split
//   P=128 Q=512 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p128_q512_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um4_un1_simd1_single_block
//   P=128 Q=512 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q512_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um4_un1_simd1_fixed_q
//   P=128 Q=512 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q512_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um4_un1_simd1_adaptive_half
//   P=128 Q=512 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q512_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um4_un1_simd1_equal_split
//   P=128 Q=512 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q512_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um4_un1_simd4_single_block
//   P=128 Q=512 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q512_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um4_un1_simd4_fixed_q
//   P=128 Q=512 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q512_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um4_un1_simd4_adaptive_half
//   P=128 Q=512 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q512_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um4_un1_simd4_equal_split
//   P=128 Q=512 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q512_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um4_un4_simd4_single_block
//   P=128 Q=512 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p128_q512_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um4_un4_simd4_fixed_q
//   P=128 Q=512 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p128_q512_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um4_un4_simd4_adaptive_half
//   P=128 Q=512 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p128_q512_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um4_un4_simd4_equal_split
//   P=128 Q=512 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p128_q512_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um8_un1_simd1_single_block
//   P=128 Q=512 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p128_q512_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um8_un1_simd1_fixed_q
//   P=128 Q=512 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p128_q512_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um8_un1_simd1_adaptive_half
//   P=128 Q=512 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p128_q512_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um8_un1_simd1_equal_split
//   P=128 Q=512 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p128_q512_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um8_un1_simd4_single_block
//   P=128 Q=512 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p128_q512_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um8_un1_simd4_fixed_q
//   P=128 Q=512 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p128_q512_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um8_un1_simd4_adaptive_half
//   P=128 Q=512 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p128_q512_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um8_un1_simd4_equal_split
//   P=128 Q=512 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p128_q512_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um8_un1_simd8_single_block
//   P=128 Q=512 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p128_q512_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um8_un1_simd8_fixed_q
//   P=128 Q=512 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p128_q512_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um8_un1_simd8_adaptive_half
//   P=128 Q=512 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p128_q512_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um8_un1_simd8_equal_split
//   P=128 Q=512 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p128_q512_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um8_un4_simd4_single_block
//   P=128 Q=512 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p128_q512_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um8_un4_simd4_fixed_q
//   P=128 Q=512 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p128_q512_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um8_un4_simd4_adaptive_half
//   P=128 Q=512 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p128_q512_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um8_un4_simd4_equal_split
//   P=128 Q=512 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p128_q512_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um8_un4_simd8_single_block
//   P=128 Q=512 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p128_q512_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um8_un4_simd8_fixed_q
//   P=128 Q=512 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p128_q512_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um8_un4_simd8_adaptive_half
//   P=128 Q=512 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p128_q512_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p128_q512_um8_un4_simd8_equal_split
//   P=128 Q=512 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p128_q512_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um1_un1_simd1_single_block
//   P=256 Q=1 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q1_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um1_un1_simd1_fixed_q
//   P=256 Q=1 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q1_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um1_un1_simd1_adaptive_half
//   P=256 Q=1 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q1_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um1_un1_simd1_equal_split
//   P=256 Q=1 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q1_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um1_un4_simd1_single_block
//   P=256 Q=1 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p256_q1_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um1_un4_simd1_fixed_q
//   P=256 Q=1 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p256_q1_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um1_un4_simd1_adaptive_half
//   P=256 Q=1 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p256_q1_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um1_un4_simd1_equal_split
//   P=256 Q=1 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p256_q1_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um4_un1_simd1_single_block
//   P=256 Q=1 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q1_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um4_un1_simd1_fixed_q
//   P=256 Q=1 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q1_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um4_un1_simd1_adaptive_half
//   P=256 Q=1 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q1_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um4_un1_simd1_equal_split
//   P=256 Q=1 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q1_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um4_un1_simd4_single_block
//   P=256 Q=1 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q1_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um4_un1_simd4_fixed_q
//   P=256 Q=1 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q1_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um4_un1_simd4_adaptive_half
//   P=256 Q=1 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q1_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um4_un1_simd4_equal_split
//   P=256 Q=1 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q1_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um4_un4_simd4_single_block
//   P=256 Q=1 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p256_q1_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um4_un4_simd4_fixed_q
//   P=256 Q=1 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p256_q1_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um4_un4_simd4_adaptive_half
//   P=256 Q=1 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p256_q1_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um4_un4_simd4_equal_split
//   P=256 Q=1 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p256_q1_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um8_un1_simd1_single_block
//   P=256 Q=1 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q1_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um8_un1_simd1_fixed_q
//   P=256 Q=1 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q1_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um8_un1_simd1_adaptive_half
//   P=256 Q=1 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q1_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um8_un1_simd1_equal_split
//   P=256 Q=1 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q1_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um8_un1_simd4_single_block
//   P=256 Q=1 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q1_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um8_un1_simd4_fixed_q
//   P=256 Q=1 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q1_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um8_un1_simd4_adaptive_half
//   P=256 Q=1 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q1_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um8_un1_simd4_equal_split
//   P=256 Q=1 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q1_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um8_un1_simd8_single_block
//   P=256 Q=1 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p256_q1_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um8_un1_simd8_fixed_q
//   P=256 Q=1 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p256_q1_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um8_un1_simd8_adaptive_half
//   P=256 Q=1 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p256_q1_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um8_un1_simd8_equal_split
//   P=256 Q=1 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p256_q1_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um8_un4_simd4_single_block
//   P=256 Q=1 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p256_q1_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um8_un4_simd4_fixed_q
//   P=256 Q=1 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p256_q1_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um8_un4_simd4_adaptive_half
//   P=256 Q=1 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p256_q1_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um8_un4_simd4_equal_split
//   P=256 Q=1 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p256_q1_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um8_un4_simd8_single_block
//   P=256 Q=1 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p256_q1_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um8_un4_simd8_fixed_q
//   P=256 Q=1 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p256_q1_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um8_un4_simd8_adaptive_half
//   P=256 Q=1 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p256_q1_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um8_un4_simd8_equal_split
//   P=256 Q=1 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p256_q1_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um16_un1_simd4_single_block
//   P=256 Q=1 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q1_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um16_un1_simd4_fixed_q
//   P=256 Q=1 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q1_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um16_un1_simd4_adaptive_half
//   P=256 Q=1 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q1_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um16_un1_simd4_equal_split
//   P=256 Q=1 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q1_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um16_un1_simd8_single_block
//   P=256 Q=1 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p256_q1_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um16_un1_simd8_fixed_q
//   P=256 Q=1 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p256_q1_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um16_un1_simd8_adaptive_half
//   P=256 Q=1 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p256_q1_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um16_un1_simd8_equal_split
//   P=256 Q=1 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p256_q1_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um16_un1_simd16_single_block
//   P=256 Q=1 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p256_q1_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um16_un1_simd16_fixed_q
//   P=256 Q=1 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p256_q1_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um16_un1_simd16_adaptive_half
//   P=256 Q=1 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p256_q1_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um16_un1_simd16_equal_split
//   P=256 Q=1 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p256_q1_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um16_un4_simd8_single_block
//   P=256 Q=1 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p256_q1_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um16_un4_simd8_fixed_q
//   P=256 Q=1 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p256_q1_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um16_un4_simd8_adaptive_half
//   P=256 Q=1 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p256_q1_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um16_un4_simd8_equal_split
//   P=256 Q=1 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p256_q1_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um16_un4_simd16_single_block
//   P=256 Q=1 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p256_q1_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um16_un4_simd16_fixed_q
//   P=256 Q=1 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p256_q1_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um16_un4_simd16_adaptive_half
//   P=256 Q=1 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p256_q1_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um16_un4_simd16_equal_split
//   P=256 Q=1 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p256_q1_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um32_un1_simd4_single_block
//   P=256 Q=1 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q1_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um32_un1_simd4_fixed_q
//   P=256 Q=1 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q1_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um32_un1_simd4_adaptive_half
//   P=256 Q=1 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q1_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um32_un1_simd4_equal_split
//   P=256 Q=1 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q1_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um32_un1_simd8_single_block
//   P=256 Q=1 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p256_q1_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um32_un1_simd8_fixed_q
//   P=256 Q=1 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p256_q1_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um32_un1_simd8_adaptive_half
//   P=256 Q=1 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p256_q1_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um32_un1_simd8_equal_split
//   P=256 Q=1 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p256_q1_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um32_un1_simd16_single_block
//   P=256 Q=1 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p256_q1_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um32_un1_simd16_fixed_q
//   P=256 Q=1 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p256_q1_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um32_un1_simd16_adaptive_half
//   P=256 Q=1 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p256_q1_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um32_un1_simd16_equal_split
//   P=256 Q=1 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p256_q1_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um32_un4_simd16_single_block
//   P=256 Q=1 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p256_q1_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um32_un4_simd16_fixed_q
//   P=256 Q=1 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p256_q1_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um32_un4_simd16_adaptive_half
//   P=256 Q=1 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p256_q1_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q1_um32_un4_simd16_equal_split
//   P=256 Q=1 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p256_q1_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um1_un1_simd1_single_block
//   P=256 Q=64 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q64_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um1_un1_simd1_fixed_q
//   P=256 Q=64 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q64_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um1_un1_simd1_adaptive_half
//   P=256 Q=64 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q64_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um1_un1_simd1_equal_split
//   P=256 Q=64 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q64_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um1_un4_simd1_single_block
//   P=256 Q=64 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p256_q64_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um1_un4_simd1_fixed_q
//   P=256 Q=64 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p256_q64_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um1_un4_simd1_adaptive_half
//   P=256 Q=64 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p256_q64_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um1_un4_simd1_equal_split
//   P=256 Q=64 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p256_q64_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um4_un1_simd1_single_block
//   P=256 Q=64 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q64_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um4_un1_simd1_fixed_q
//   P=256 Q=64 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q64_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um4_un1_simd1_adaptive_half
//   P=256 Q=64 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q64_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um4_un1_simd1_equal_split
//   P=256 Q=64 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q64_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um4_un1_simd4_single_block
//   P=256 Q=64 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q64_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um4_un1_simd4_fixed_q
//   P=256 Q=64 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q64_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um4_un1_simd4_adaptive_half
//   P=256 Q=64 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q64_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um4_un1_simd4_equal_split
//   P=256 Q=64 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q64_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um4_un4_simd4_single_block
//   P=256 Q=64 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p256_q64_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um4_un4_simd4_fixed_q
//   P=256 Q=64 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p256_q64_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um4_un4_simd4_adaptive_half
//   P=256 Q=64 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p256_q64_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um4_un4_simd4_equal_split
//   P=256 Q=64 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p256_q64_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um8_un1_simd1_single_block
//   P=256 Q=64 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q64_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um8_un1_simd1_fixed_q
//   P=256 Q=64 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q64_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um8_un1_simd1_adaptive_half
//   P=256 Q=64 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q64_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um8_un1_simd1_equal_split
//   P=256 Q=64 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q64_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um8_un1_simd4_single_block
//   P=256 Q=64 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q64_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um8_un1_simd4_fixed_q
//   P=256 Q=64 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q64_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um8_un1_simd4_adaptive_half
//   P=256 Q=64 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q64_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um8_un1_simd4_equal_split
//   P=256 Q=64 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q64_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um8_un1_simd8_single_block
//   P=256 Q=64 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p256_q64_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um8_un1_simd8_fixed_q
//   P=256 Q=64 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p256_q64_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um8_un1_simd8_adaptive_half
//   P=256 Q=64 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p256_q64_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um8_un1_simd8_equal_split
//   P=256 Q=64 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p256_q64_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um8_un4_simd4_single_block
//   P=256 Q=64 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p256_q64_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um8_un4_simd4_fixed_q
//   P=256 Q=64 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p256_q64_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um8_un4_simd4_adaptive_half
//   P=256 Q=64 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p256_q64_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um8_un4_simd4_equal_split
//   P=256 Q=64 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p256_q64_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um8_un4_simd8_single_block
//   P=256 Q=64 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p256_q64_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um8_un4_simd8_fixed_q
//   P=256 Q=64 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p256_q64_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um8_un4_simd8_adaptive_half
//   P=256 Q=64 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p256_q64_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um8_un4_simd8_equal_split
//   P=256 Q=64 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p256_q64_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um16_un1_simd4_single_block
//   P=256 Q=64 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q64_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um16_un1_simd4_fixed_q
//   P=256 Q=64 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q64_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um16_un1_simd4_adaptive_half
//   P=256 Q=64 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q64_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um16_un1_simd4_equal_split
//   P=256 Q=64 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q64_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um16_un1_simd8_single_block
//   P=256 Q=64 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p256_q64_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um16_un1_simd8_fixed_q
//   P=256 Q=64 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p256_q64_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um16_un1_simd8_adaptive_half
//   P=256 Q=64 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p256_q64_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um16_un1_simd8_equal_split
//   P=256 Q=64 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p256_q64_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um16_un1_simd16_single_block
//   P=256 Q=64 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p256_q64_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um16_un1_simd16_fixed_q
//   P=256 Q=64 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p256_q64_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um16_un1_simd16_adaptive_half
//   P=256 Q=64 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p256_q64_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um16_un1_simd16_equal_split
//   P=256 Q=64 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p256_q64_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um16_un4_simd8_single_block
//   P=256 Q=64 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p256_q64_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um16_un4_simd8_fixed_q
//   P=256 Q=64 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p256_q64_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um16_un4_simd8_adaptive_half
//   P=256 Q=64 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p256_q64_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um16_un4_simd8_equal_split
//   P=256 Q=64 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p256_q64_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um16_un4_simd16_single_block
//   P=256 Q=64 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p256_q64_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um16_un4_simd16_fixed_q
//   P=256 Q=64 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p256_q64_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um16_un4_simd16_adaptive_half
//   P=256 Q=64 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p256_q64_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um16_un4_simd16_equal_split
//   P=256 Q=64 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p256_q64_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um32_un1_simd4_single_block
//   P=256 Q=64 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q64_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um32_un1_simd4_fixed_q
//   P=256 Q=64 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q64_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um32_un1_simd4_adaptive_half
//   P=256 Q=64 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q64_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um32_un1_simd4_equal_split
//   P=256 Q=64 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q64_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um32_un1_simd8_single_block
//   P=256 Q=64 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p256_q64_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um32_un1_simd8_fixed_q
//   P=256 Q=64 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p256_q64_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um32_un1_simd8_adaptive_half
//   P=256 Q=64 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p256_q64_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um32_un1_simd8_equal_split
//   P=256 Q=64 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p256_q64_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um32_un1_simd16_single_block
//   P=256 Q=64 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p256_q64_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um32_un1_simd16_fixed_q
//   P=256 Q=64 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p256_q64_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um32_un1_simd16_adaptive_half
//   P=256 Q=64 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p256_q64_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um32_un1_simd16_equal_split
//   P=256 Q=64 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p256_q64_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um32_un4_simd16_single_block
//   P=256 Q=64 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p256_q64_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um32_un4_simd16_fixed_q
//   P=256 Q=64 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p256_q64_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um32_un4_simd16_adaptive_half
//   P=256 Q=64 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p256_q64_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q64_um32_un4_simd16_equal_split
//   P=256 Q=64 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p256_q64_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um1_un1_simd1_single_block
//   P=256 Q=128 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q128_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um1_un1_simd1_fixed_q
//   P=256 Q=128 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q128_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um1_un1_simd1_adaptive_half
//   P=256 Q=128 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q128_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um1_un1_simd1_equal_split
//   P=256 Q=128 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q128_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um1_un4_simd1_single_block
//   P=256 Q=128 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p256_q128_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um1_un4_simd1_fixed_q
//   P=256 Q=128 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p256_q128_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um1_un4_simd1_adaptive_half
//   P=256 Q=128 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p256_q128_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um1_un4_simd1_equal_split
//   P=256 Q=128 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p256_q128_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um4_un1_simd1_single_block
//   P=256 Q=128 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q128_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um4_un1_simd1_fixed_q
//   P=256 Q=128 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q128_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um4_un1_simd1_adaptive_half
//   P=256 Q=128 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q128_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um4_un1_simd1_equal_split
//   P=256 Q=128 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q128_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um4_un1_simd4_single_block
//   P=256 Q=128 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q128_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um4_un1_simd4_fixed_q
//   P=256 Q=128 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q128_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um4_un1_simd4_adaptive_half
//   P=256 Q=128 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q128_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um4_un1_simd4_equal_split
//   P=256 Q=128 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q128_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um4_un4_simd4_single_block
//   P=256 Q=128 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p256_q128_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um4_un4_simd4_fixed_q
//   P=256 Q=128 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p256_q128_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um4_un4_simd4_adaptive_half
//   P=256 Q=128 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p256_q128_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um4_un4_simd4_equal_split
//   P=256 Q=128 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p256_q128_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um8_un1_simd1_single_block
//   P=256 Q=128 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q128_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um8_un1_simd1_fixed_q
//   P=256 Q=128 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q128_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um8_un1_simd1_adaptive_half
//   P=256 Q=128 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q128_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um8_un1_simd1_equal_split
//   P=256 Q=128 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q128_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um8_un1_simd4_single_block
//   P=256 Q=128 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q128_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um8_un1_simd4_fixed_q
//   P=256 Q=128 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q128_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um8_un1_simd4_adaptive_half
//   P=256 Q=128 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q128_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um8_un1_simd4_equal_split
//   P=256 Q=128 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q128_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um8_un1_simd8_single_block
//   P=256 Q=128 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p256_q128_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um8_un1_simd8_fixed_q
//   P=256 Q=128 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p256_q128_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um8_un1_simd8_adaptive_half
//   P=256 Q=128 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p256_q128_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um8_un1_simd8_equal_split
//   P=256 Q=128 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p256_q128_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um8_un4_simd4_single_block
//   P=256 Q=128 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p256_q128_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um8_un4_simd4_fixed_q
//   P=256 Q=128 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p256_q128_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um8_un4_simd4_adaptive_half
//   P=256 Q=128 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p256_q128_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um8_un4_simd4_equal_split
//   P=256 Q=128 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p256_q128_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um8_un4_simd8_single_block
//   P=256 Q=128 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p256_q128_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um8_un4_simd8_fixed_q
//   P=256 Q=128 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p256_q128_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um8_un4_simd8_adaptive_half
//   P=256 Q=128 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p256_q128_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um8_un4_simd8_equal_split
//   P=256 Q=128 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p256_q128_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um16_un1_simd4_single_block
//   P=256 Q=128 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q128_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um16_un1_simd4_fixed_q
//   P=256 Q=128 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q128_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um16_un1_simd4_adaptive_half
//   P=256 Q=128 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q128_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um16_un1_simd4_equal_split
//   P=256 Q=128 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q128_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um16_un1_simd8_single_block
//   P=256 Q=128 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p256_q128_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um16_un1_simd8_fixed_q
//   P=256 Q=128 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p256_q128_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um16_un1_simd8_adaptive_half
//   P=256 Q=128 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p256_q128_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um16_un1_simd8_equal_split
//   P=256 Q=128 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p256_q128_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um16_un1_simd16_single_block
//   P=256 Q=128 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p256_q128_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um16_un1_simd16_fixed_q
//   P=256 Q=128 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p256_q128_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um16_un1_simd16_adaptive_half
//   P=256 Q=128 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p256_q128_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um16_un1_simd16_equal_split
//   P=256 Q=128 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p256_q128_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um16_un4_simd8_single_block
//   P=256 Q=128 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p256_q128_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um16_un4_simd8_fixed_q
//   P=256 Q=128 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p256_q128_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um16_un4_simd8_adaptive_half
//   P=256 Q=128 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p256_q128_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um16_un4_simd8_equal_split
//   P=256 Q=128 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p256_q128_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um16_un4_simd16_single_block
//   P=256 Q=128 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p256_q128_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um16_un4_simd16_fixed_q
//   P=256 Q=128 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p256_q128_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um16_un4_simd16_adaptive_half
//   P=256 Q=128 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p256_q128_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um16_un4_simd16_equal_split
//   P=256 Q=128 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p256_q128_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um32_un1_simd4_single_block
//   P=256 Q=128 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q128_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um32_un1_simd4_fixed_q
//   P=256 Q=128 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q128_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um32_un1_simd4_adaptive_half
//   P=256 Q=128 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q128_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um32_un1_simd4_equal_split
//   P=256 Q=128 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q128_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um32_un1_simd8_single_block
//   P=256 Q=128 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p256_q128_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um32_un1_simd8_fixed_q
//   P=256 Q=128 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p256_q128_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um32_un1_simd8_adaptive_half
//   P=256 Q=128 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p256_q128_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um32_un1_simd8_equal_split
//   P=256 Q=128 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p256_q128_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um32_un1_simd16_single_block
//   P=256 Q=128 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p256_q128_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um32_un1_simd16_fixed_q
//   P=256 Q=128 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p256_q128_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um32_un1_simd16_adaptive_half
//   P=256 Q=128 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p256_q128_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um32_un1_simd16_equal_split
//   P=256 Q=128 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p256_q128_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um32_un4_simd16_single_block
//   P=256 Q=128 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p256_q128_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um32_un4_simd16_fixed_q
//   P=256 Q=128 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p256_q128_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um32_un4_simd16_adaptive_half
//   P=256 Q=128 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p256_q128_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q128_um32_un4_simd16_equal_split
//   P=256 Q=128 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p256_q128_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um1_un1_simd1_single_block
//   P=256 Q=192 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q192_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um1_un1_simd1_fixed_q
//   P=256 Q=192 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q192_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um1_un1_simd1_adaptive_half
//   P=256 Q=192 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q192_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um1_un1_simd1_equal_split
//   P=256 Q=192 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q192_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um1_un4_simd1_single_block
//   P=256 Q=192 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p256_q192_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um1_un4_simd1_fixed_q
//   P=256 Q=192 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p256_q192_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um1_un4_simd1_adaptive_half
//   P=256 Q=192 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p256_q192_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um1_un4_simd1_equal_split
//   P=256 Q=192 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p256_q192_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um4_un1_simd1_single_block
//   P=256 Q=192 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q192_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um4_un1_simd1_fixed_q
//   P=256 Q=192 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q192_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um4_un1_simd1_adaptive_half
//   P=256 Q=192 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q192_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um4_un1_simd1_equal_split
//   P=256 Q=192 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q192_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um4_un1_simd4_single_block
//   P=256 Q=192 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q192_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um4_un1_simd4_fixed_q
//   P=256 Q=192 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q192_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um4_un1_simd4_adaptive_half
//   P=256 Q=192 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q192_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um4_un1_simd4_equal_split
//   P=256 Q=192 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q192_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um4_un4_simd4_single_block
//   P=256 Q=192 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p256_q192_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um4_un4_simd4_fixed_q
//   P=256 Q=192 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p256_q192_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um4_un4_simd4_adaptive_half
//   P=256 Q=192 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p256_q192_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um4_un4_simd4_equal_split
//   P=256 Q=192 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p256_q192_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um8_un1_simd1_single_block
//   P=256 Q=192 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q192_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um8_un1_simd1_fixed_q
//   P=256 Q=192 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q192_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um8_un1_simd1_adaptive_half
//   P=256 Q=192 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q192_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um8_un1_simd1_equal_split
//   P=256 Q=192 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q192_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um8_un1_simd4_single_block
//   P=256 Q=192 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q192_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um8_un1_simd4_fixed_q
//   P=256 Q=192 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q192_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um8_un1_simd4_adaptive_half
//   P=256 Q=192 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q192_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um8_un1_simd4_equal_split
//   P=256 Q=192 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q192_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um8_un1_simd8_single_block
//   P=256 Q=192 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p256_q192_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um8_un1_simd8_fixed_q
//   P=256 Q=192 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p256_q192_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um8_un1_simd8_adaptive_half
//   P=256 Q=192 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p256_q192_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um8_un1_simd8_equal_split
//   P=256 Q=192 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p256_q192_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um8_un4_simd4_single_block
//   P=256 Q=192 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p256_q192_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um8_un4_simd4_fixed_q
//   P=256 Q=192 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p256_q192_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um8_un4_simd4_adaptive_half
//   P=256 Q=192 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p256_q192_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um8_un4_simd4_equal_split
//   P=256 Q=192 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p256_q192_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um8_un4_simd8_single_block
//   P=256 Q=192 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p256_q192_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um8_un4_simd8_fixed_q
//   P=256 Q=192 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p256_q192_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um8_un4_simd8_adaptive_half
//   P=256 Q=192 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p256_q192_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um8_un4_simd8_equal_split
//   P=256 Q=192 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p256_q192_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um16_un1_simd4_single_block
//   P=256 Q=192 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q192_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um16_un1_simd4_fixed_q
//   P=256 Q=192 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q192_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um16_un1_simd4_adaptive_half
//   P=256 Q=192 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q192_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um16_un1_simd4_equal_split
//   P=256 Q=192 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q192_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um16_un1_simd8_single_block
//   P=256 Q=192 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p256_q192_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um16_un1_simd8_fixed_q
//   P=256 Q=192 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p256_q192_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um16_un1_simd8_adaptive_half
//   P=256 Q=192 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p256_q192_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um16_un1_simd8_equal_split
//   P=256 Q=192 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p256_q192_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um16_un1_simd16_single_block
//   P=256 Q=192 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p256_q192_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um16_un1_simd16_fixed_q
//   P=256 Q=192 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p256_q192_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um16_un1_simd16_adaptive_half
//   P=256 Q=192 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p256_q192_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um16_un1_simd16_equal_split
//   P=256 Q=192 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p256_q192_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um16_un4_simd8_single_block
//   P=256 Q=192 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p256_q192_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um16_un4_simd8_fixed_q
//   P=256 Q=192 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p256_q192_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um16_un4_simd8_adaptive_half
//   P=256 Q=192 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p256_q192_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um16_un4_simd8_equal_split
//   P=256 Q=192 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p256_q192_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um16_un4_simd16_single_block
//   P=256 Q=192 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p256_q192_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um16_un4_simd16_fixed_q
//   P=256 Q=192 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p256_q192_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um16_un4_simd16_adaptive_half
//   P=256 Q=192 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p256_q192_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um16_un4_simd16_equal_split
//   P=256 Q=192 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p256_q192_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um32_un1_simd4_single_block
//   P=256 Q=192 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q192_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um32_un1_simd4_fixed_q
//   P=256 Q=192 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q192_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um32_un1_simd4_adaptive_half
//   P=256 Q=192 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q192_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um32_un1_simd4_equal_split
//   P=256 Q=192 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q192_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um32_un1_simd8_single_block
//   P=256 Q=192 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p256_q192_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um32_un1_simd8_fixed_q
//   P=256 Q=192 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p256_q192_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um32_un1_simd8_adaptive_half
//   P=256 Q=192 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p256_q192_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um32_un1_simd8_equal_split
//   P=256 Q=192 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p256_q192_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um32_un1_simd16_single_block
//   P=256 Q=192 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p256_q192_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um32_un1_simd16_fixed_q
//   P=256 Q=192 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p256_q192_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um32_un1_simd16_adaptive_half
//   P=256 Q=192 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p256_q192_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um32_un1_simd16_equal_split
//   P=256 Q=192 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p256_q192_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um32_un4_simd16_single_block
//   P=256 Q=192 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p256_q192_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um32_un4_simd16_fixed_q
//   P=256 Q=192 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p256_q192_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um32_un4_simd16_adaptive_half
//   P=256 Q=192 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p256_q192_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q192_um32_un4_simd16_equal_split
//   P=256 Q=192 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p256_q192_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um1_un1_simd1_single_block
//   P=256 Q=240 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q240_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um1_un1_simd1_fixed_q
//   P=256 Q=240 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q240_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um1_un1_simd1_adaptive_half
//   P=256 Q=240 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q240_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um1_un1_simd1_equal_split
//   P=256 Q=240 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q240_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um1_un4_simd1_single_block
//   P=256 Q=240 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p256_q240_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um1_un4_simd1_fixed_q
//   P=256 Q=240 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p256_q240_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um1_un4_simd1_adaptive_half
//   P=256 Q=240 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p256_q240_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um1_un4_simd1_equal_split
//   P=256 Q=240 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p256_q240_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um4_un1_simd1_single_block
//   P=256 Q=240 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q240_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um4_un1_simd1_fixed_q
//   P=256 Q=240 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q240_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um4_un1_simd1_adaptive_half
//   P=256 Q=240 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q240_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um4_un1_simd1_equal_split
//   P=256 Q=240 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q240_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um4_un1_simd4_single_block
//   P=256 Q=240 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q240_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um4_un1_simd4_fixed_q
//   P=256 Q=240 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q240_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um4_un1_simd4_adaptive_half
//   P=256 Q=240 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q240_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um4_un1_simd4_equal_split
//   P=256 Q=240 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q240_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um4_un4_simd4_single_block
//   P=256 Q=240 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p256_q240_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um4_un4_simd4_fixed_q
//   P=256 Q=240 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p256_q240_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um4_un4_simd4_adaptive_half
//   P=256 Q=240 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p256_q240_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um4_un4_simd4_equal_split
//   P=256 Q=240 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p256_q240_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um8_un1_simd1_single_block
//   P=256 Q=240 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q240_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um8_un1_simd1_fixed_q
//   P=256 Q=240 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q240_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um8_un1_simd1_adaptive_half
//   P=256 Q=240 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q240_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um8_un1_simd1_equal_split
//   P=256 Q=240 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q240_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um8_un1_simd4_single_block
//   P=256 Q=240 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q240_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um8_un1_simd4_fixed_q
//   P=256 Q=240 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q240_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um8_un1_simd4_adaptive_half
//   P=256 Q=240 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q240_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um8_un1_simd4_equal_split
//   P=256 Q=240 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q240_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um8_un1_simd8_single_block
//   P=256 Q=240 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p256_q240_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um8_un1_simd8_fixed_q
//   P=256 Q=240 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p256_q240_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um8_un1_simd8_adaptive_half
//   P=256 Q=240 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p256_q240_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um8_un1_simd8_equal_split
//   P=256 Q=240 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p256_q240_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um8_un4_simd4_single_block
//   P=256 Q=240 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p256_q240_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um8_un4_simd4_fixed_q
//   P=256 Q=240 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p256_q240_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um8_un4_simd4_adaptive_half
//   P=256 Q=240 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p256_q240_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um8_un4_simd4_equal_split
//   P=256 Q=240 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p256_q240_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um8_un4_simd8_single_block
//   P=256 Q=240 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p256_q240_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um8_un4_simd8_fixed_q
//   P=256 Q=240 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p256_q240_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um8_un4_simd8_adaptive_half
//   P=256 Q=240 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p256_q240_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um8_un4_simd8_equal_split
//   P=256 Q=240 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p256_q240_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um16_un1_simd4_single_block
//   P=256 Q=240 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q240_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um16_un1_simd4_fixed_q
//   P=256 Q=240 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q240_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um16_un1_simd4_adaptive_half
//   P=256 Q=240 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q240_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um16_un1_simd4_equal_split
//   P=256 Q=240 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q240_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um16_un1_simd8_single_block
//   P=256 Q=240 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p256_q240_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um16_un1_simd8_fixed_q
//   P=256 Q=240 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p256_q240_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um16_un1_simd8_adaptive_half
//   P=256 Q=240 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p256_q240_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um16_un1_simd8_equal_split
//   P=256 Q=240 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p256_q240_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um16_un1_simd16_single_block
//   P=256 Q=240 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p256_q240_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um16_un1_simd16_fixed_q
//   P=256 Q=240 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p256_q240_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um16_un1_simd16_adaptive_half
//   P=256 Q=240 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p256_q240_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um16_un1_simd16_equal_split
//   P=256 Q=240 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p256_q240_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um16_un4_simd8_single_block
//   P=256 Q=240 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p256_q240_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um16_un4_simd8_fixed_q
//   P=256 Q=240 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p256_q240_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um16_un4_simd8_adaptive_half
//   P=256 Q=240 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p256_q240_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um16_un4_simd8_equal_split
//   P=256 Q=240 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p256_q240_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um16_un4_simd16_single_block
//   P=256 Q=240 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p256_q240_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um16_un4_simd16_fixed_q
//   P=256 Q=240 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p256_q240_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um16_un4_simd16_adaptive_half
//   P=256 Q=240 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p256_q240_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um16_un4_simd16_equal_split
//   P=256 Q=240 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p256_q240_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um32_un1_simd4_single_block
//   P=256 Q=240 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q240_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um32_un1_simd4_fixed_q
//   P=256 Q=240 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q240_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um32_un1_simd4_adaptive_half
//   P=256 Q=240 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q240_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um32_un1_simd4_equal_split
//   P=256 Q=240 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q240_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um32_un1_simd8_single_block
//   P=256 Q=240 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p256_q240_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um32_un1_simd8_fixed_q
//   P=256 Q=240 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p256_q240_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um32_un1_simd8_adaptive_half
//   P=256 Q=240 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p256_q240_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um32_un1_simd8_equal_split
//   P=256 Q=240 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p256_q240_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um32_un1_simd16_single_block
//   P=256 Q=240 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p256_q240_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um32_un1_simd16_fixed_q
//   P=256 Q=240 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p256_q240_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um32_un1_simd16_adaptive_half
//   P=256 Q=240 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p256_q240_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q240_um32_un1_simd16_equal_split
//   P=256 Q=240 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p256_q240_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um1_un1_simd1_single_block
//   P=256 Q=248 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q248_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um1_un1_simd1_fixed_q
//   P=256 Q=248 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q248_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um1_un1_simd1_adaptive_half
//   P=256 Q=248 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q248_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um1_un1_simd1_equal_split
//   P=256 Q=248 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q248_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um1_un4_simd1_single_block
//   P=256 Q=248 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p256_q248_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um1_un4_simd1_fixed_q
//   P=256 Q=248 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p256_q248_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um1_un4_simd1_adaptive_half
//   P=256 Q=248 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p256_q248_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um1_un4_simd1_equal_split
//   P=256 Q=248 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p256_q248_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um4_un1_simd1_single_block
//   P=256 Q=248 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q248_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um4_un1_simd1_fixed_q
//   P=256 Q=248 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q248_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um4_un1_simd1_adaptive_half
//   P=256 Q=248 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q248_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um4_un1_simd1_equal_split
//   P=256 Q=248 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q248_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um4_un1_simd4_single_block
//   P=256 Q=248 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q248_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um4_un1_simd4_fixed_q
//   P=256 Q=248 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q248_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um4_un1_simd4_adaptive_half
//   P=256 Q=248 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q248_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um4_un1_simd4_equal_split
//   P=256 Q=248 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q248_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um4_un4_simd4_single_block
//   P=256 Q=248 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p256_q248_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um4_un4_simd4_fixed_q
//   P=256 Q=248 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p256_q248_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um4_un4_simd4_adaptive_half
//   P=256 Q=248 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p256_q248_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um4_un4_simd4_equal_split
//   P=256 Q=248 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p256_q248_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um8_un1_simd1_single_block
//   P=256 Q=248 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q248_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um8_un1_simd1_fixed_q
//   P=256 Q=248 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q248_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um8_un1_simd1_adaptive_half
//   P=256 Q=248 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q248_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um8_un1_simd1_equal_split
//   P=256 Q=248 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q248_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um8_un1_simd4_single_block
//   P=256 Q=248 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q248_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um8_un1_simd4_fixed_q
//   P=256 Q=248 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q248_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um8_un1_simd4_adaptive_half
//   P=256 Q=248 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q248_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um8_un1_simd4_equal_split
//   P=256 Q=248 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q248_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um8_un1_simd8_single_block
//   P=256 Q=248 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p256_q248_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um8_un1_simd8_fixed_q
//   P=256 Q=248 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p256_q248_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um8_un1_simd8_adaptive_half
//   P=256 Q=248 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p256_q248_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um8_un1_simd8_equal_split
//   P=256 Q=248 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p256_q248_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um8_un4_simd4_single_block
//   P=256 Q=248 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p256_q248_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um8_un4_simd4_fixed_q
//   P=256 Q=248 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p256_q248_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um8_un4_simd4_adaptive_half
//   P=256 Q=248 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p256_q248_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um8_un4_simd4_equal_split
//   P=256 Q=248 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p256_q248_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um8_un4_simd8_single_block
//   P=256 Q=248 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p256_q248_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um8_un4_simd8_fixed_q
//   P=256 Q=248 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p256_q248_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um8_un4_simd8_adaptive_half
//   P=256 Q=248 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p256_q248_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um8_un4_simd8_equal_split
//   P=256 Q=248 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p256_q248_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um16_un1_simd4_single_block
//   P=256 Q=248 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q248_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um16_un1_simd4_fixed_q
//   P=256 Q=248 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q248_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um16_un1_simd4_adaptive_half
//   P=256 Q=248 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q248_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um16_un1_simd4_equal_split
//   P=256 Q=248 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q248_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um16_un1_simd8_single_block
//   P=256 Q=248 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p256_q248_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um16_un1_simd8_fixed_q
//   P=256 Q=248 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p256_q248_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um16_un1_simd8_adaptive_half
//   P=256 Q=248 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p256_q248_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um16_un1_simd8_equal_split
//   P=256 Q=248 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p256_q248_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um16_un1_simd16_single_block
//   P=256 Q=248 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p256_q248_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um16_un1_simd16_fixed_q
//   P=256 Q=248 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p256_q248_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um16_un1_simd16_adaptive_half
//   P=256 Q=248 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p256_q248_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um16_un1_simd16_equal_split
//   P=256 Q=248 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p256_q248_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um16_un4_simd8_single_block
//   P=256 Q=248 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p256_q248_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um16_un4_simd8_fixed_q
//   P=256 Q=248 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p256_q248_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um16_un4_simd8_adaptive_half
//   P=256 Q=248 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p256_q248_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um16_un4_simd8_equal_split
//   P=256 Q=248 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p256_q248_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um16_un4_simd16_single_block
//   P=256 Q=248 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p256_q248_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um16_un4_simd16_fixed_q
//   P=256 Q=248 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p256_q248_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um16_un4_simd16_adaptive_half
//   P=256 Q=248 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p256_q248_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um16_un4_simd16_equal_split
//   P=256 Q=248 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p256_q248_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um32_un1_simd4_single_block
//   P=256 Q=248 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q248_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um32_un1_simd4_fixed_q
//   P=256 Q=248 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q248_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um32_un1_simd4_adaptive_half
//   P=256 Q=248 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q248_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um32_un1_simd4_equal_split
//   P=256 Q=248 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q248_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um32_un1_simd8_single_block
//   P=256 Q=248 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p256_q248_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um32_un1_simd8_fixed_q
//   P=256 Q=248 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p256_q248_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um32_un1_simd8_adaptive_half
//   P=256 Q=248 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p256_q248_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um32_un1_simd8_equal_split
//   P=256 Q=248 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p256_q248_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um32_un1_simd16_single_block
//   P=256 Q=248 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p256_q248_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um32_un1_simd16_fixed_q
//   P=256 Q=248 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p256_q248_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um32_un1_simd16_adaptive_half
//   P=256 Q=248 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p256_q248_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q248_um32_un1_simd16_equal_split
//   P=256 Q=248 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p256_q248_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um1_un1_simd1_single_block
//   P=256 Q=256 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q256_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um1_un1_simd1_fixed_q
//   P=256 Q=256 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q256_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um1_un1_simd1_adaptive_half
//   P=256 Q=256 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q256_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um1_un1_simd1_equal_split
//   P=256 Q=256 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q256_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um1_un4_simd1_single_block
//   P=256 Q=256 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p256_q256_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um1_un4_simd1_fixed_q
//   P=256 Q=256 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p256_q256_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um1_un4_simd1_adaptive_half
//   P=256 Q=256 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p256_q256_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um1_un4_simd1_equal_split
//   P=256 Q=256 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p256_q256_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um4_un1_simd1_single_block
//   P=256 Q=256 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q256_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um4_un1_simd1_fixed_q
//   P=256 Q=256 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q256_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um4_un1_simd1_adaptive_half
//   P=256 Q=256 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q256_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um4_un1_simd1_equal_split
//   P=256 Q=256 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q256_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um4_un1_simd4_single_block
//   P=256 Q=256 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q256_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um4_un1_simd4_fixed_q
//   P=256 Q=256 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q256_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um4_un1_simd4_adaptive_half
//   P=256 Q=256 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q256_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um4_un1_simd4_equal_split
//   P=256 Q=256 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q256_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um4_un4_simd4_single_block
//   P=256 Q=256 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p256_q256_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um4_un4_simd4_fixed_q
//   P=256 Q=256 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p256_q256_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um4_un4_simd4_adaptive_half
//   P=256 Q=256 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p256_q256_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um4_un4_simd4_equal_split
//   P=256 Q=256 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p256_q256_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um8_un1_simd1_single_block
//   P=256 Q=256 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q256_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um8_un1_simd1_fixed_q
//   P=256 Q=256 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q256_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um8_un1_simd1_adaptive_half
//   P=256 Q=256 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q256_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um8_un1_simd1_equal_split
//   P=256 Q=256 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q256_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um8_un1_simd4_single_block
//   P=256 Q=256 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q256_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um8_un1_simd4_fixed_q
//   P=256 Q=256 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q256_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um8_un1_simd4_adaptive_half
//   P=256 Q=256 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q256_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um8_un1_simd4_equal_split
//   P=256 Q=256 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q256_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um8_un1_simd8_single_block
//   P=256 Q=256 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p256_q256_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um8_un1_simd8_fixed_q
//   P=256 Q=256 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p256_q256_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um8_un1_simd8_adaptive_half
//   P=256 Q=256 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p256_q256_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um8_un1_simd8_equal_split
//   P=256 Q=256 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p256_q256_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um8_un4_simd4_single_block
//   P=256 Q=256 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p256_q256_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um8_un4_simd4_fixed_q
//   P=256 Q=256 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p256_q256_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um8_un4_simd4_adaptive_half
//   P=256 Q=256 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p256_q256_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um8_un4_simd4_equal_split
//   P=256 Q=256 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p256_q256_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um8_un4_simd8_single_block
//   P=256 Q=256 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p256_q256_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um8_un4_simd8_fixed_q
//   P=256 Q=256 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p256_q256_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um8_un4_simd8_adaptive_half
//   P=256 Q=256 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p256_q256_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um8_un4_simd8_equal_split
//   P=256 Q=256 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p256_q256_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um16_un1_simd4_single_block
//   P=256 Q=256 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q256_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um16_un1_simd4_fixed_q
//   P=256 Q=256 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q256_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um16_un1_simd4_adaptive_half
//   P=256 Q=256 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q256_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um16_un1_simd4_equal_split
//   P=256 Q=256 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q256_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um16_un1_simd8_single_block
//   P=256 Q=256 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p256_q256_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um16_un1_simd8_fixed_q
//   P=256 Q=256 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p256_q256_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um16_un1_simd8_adaptive_half
//   P=256 Q=256 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p256_q256_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um16_un1_simd8_equal_split
//   P=256 Q=256 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p256_q256_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um16_un1_simd16_single_block
//   P=256 Q=256 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p256_q256_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um16_un1_simd16_fixed_q
//   P=256 Q=256 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p256_q256_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um16_un1_simd16_adaptive_half
//   P=256 Q=256 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p256_q256_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um16_un1_simd16_equal_split
//   P=256 Q=256 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p256_q256_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um16_un4_simd8_single_block
//   P=256 Q=256 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p256_q256_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um16_un4_simd8_fixed_q
//   P=256 Q=256 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p256_q256_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um16_un4_simd8_adaptive_half
//   P=256 Q=256 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p256_q256_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um16_un4_simd8_equal_split
//   P=256 Q=256 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p256_q256_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um16_un4_simd16_single_block
//   P=256 Q=256 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p256_q256_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um16_un4_simd16_fixed_q
//   P=256 Q=256 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p256_q256_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um16_un4_simd16_adaptive_half
//   P=256 Q=256 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p256_q256_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q256_um16_un4_simd16_equal_split
//   P=256 Q=256 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p256_q256_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um1_un1_simd1_single_block
//   P=256 Q=384 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q384_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um1_un1_simd1_fixed_q
//   P=256 Q=384 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q384_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um1_un1_simd1_adaptive_half
//   P=256 Q=384 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q384_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um1_un1_simd1_equal_split
//   P=256 Q=384 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q384_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um1_un4_simd1_single_block
//   P=256 Q=384 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p256_q384_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um1_un4_simd1_fixed_q
//   P=256 Q=384 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p256_q384_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um1_un4_simd1_adaptive_half
//   P=256 Q=384 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p256_q384_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um1_un4_simd1_equal_split
//   P=256 Q=384 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p256_q384_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um4_un1_simd1_single_block
//   P=256 Q=384 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q384_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um4_un1_simd1_fixed_q
//   P=256 Q=384 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q384_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um4_un1_simd1_adaptive_half
//   P=256 Q=384 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q384_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um4_un1_simd1_equal_split
//   P=256 Q=384 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q384_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um4_un1_simd4_single_block
//   P=256 Q=384 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q384_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um4_un1_simd4_fixed_q
//   P=256 Q=384 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q384_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um4_un1_simd4_adaptive_half
//   P=256 Q=384 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q384_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um4_un1_simd4_equal_split
//   P=256 Q=384 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q384_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um4_un4_simd4_single_block
//   P=256 Q=384 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p256_q384_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um4_un4_simd4_fixed_q
//   P=256 Q=384 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p256_q384_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um4_un4_simd4_adaptive_half
//   P=256 Q=384 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p256_q384_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um4_un4_simd4_equal_split
//   P=256 Q=384 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p256_q384_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um8_un1_simd1_single_block
//   P=256 Q=384 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q384_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um8_un1_simd1_fixed_q
//   P=256 Q=384 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q384_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um8_un1_simd1_adaptive_half
//   P=256 Q=384 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q384_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um8_un1_simd1_equal_split
//   P=256 Q=384 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q384_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um8_un1_simd4_single_block
//   P=256 Q=384 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q384_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um8_un1_simd4_fixed_q
//   P=256 Q=384 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q384_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um8_un1_simd4_adaptive_half
//   P=256 Q=384 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q384_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um8_un1_simd4_equal_split
//   P=256 Q=384 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q384_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um8_un1_simd8_single_block
//   P=256 Q=384 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p256_q384_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um8_un1_simd8_fixed_q
//   P=256 Q=384 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p256_q384_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um8_un1_simd8_adaptive_half
//   P=256 Q=384 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p256_q384_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um8_un1_simd8_equal_split
//   P=256 Q=384 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p256_q384_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um8_un4_simd4_single_block
//   P=256 Q=384 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p256_q384_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um8_un4_simd4_fixed_q
//   P=256 Q=384 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p256_q384_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um8_un4_simd4_adaptive_half
//   P=256 Q=384 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p256_q384_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um8_un4_simd4_equal_split
//   P=256 Q=384 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p256_q384_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um8_un4_simd8_single_block
//   P=256 Q=384 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p256_q384_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um8_un4_simd8_fixed_q
//   P=256 Q=384 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p256_q384_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um8_un4_simd8_adaptive_half
//   P=256 Q=384 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p256_q384_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um8_un4_simd8_equal_split
//   P=256 Q=384 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p256_q384_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um16_un1_simd4_single_block
//   P=256 Q=384 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q384_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um16_un1_simd4_fixed_q
//   P=256 Q=384 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q384_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um16_un1_simd4_adaptive_half
//   P=256 Q=384 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q384_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um16_un1_simd4_equal_split
//   P=256 Q=384 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q384_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um16_un1_simd8_single_block
//   P=256 Q=384 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p256_q384_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um16_un1_simd8_fixed_q
//   P=256 Q=384 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p256_q384_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um16_un1_simd8_adaptive_half
//   P=256 Q=384 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p256_q384_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um16_un1_simd8_equal_split
//   P=256 Q=384 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p256_q384_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um16_un1_simd16_single_block
//   P=256 Q=384 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p256_q384_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um16_un1_simd16_fixed_q
//   P=256 Q=384 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p256_q384_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um16_un1_simd16_adaptive_half
//   P=256 Q=384 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p256_q384_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um16_un1_simd16_equal_split
//   P=256 Q=384 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p256_q384_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um16_un4_simd8_single_block
//   P=256 Q=384 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p256_q384_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um16_un4_simd8_fixed_q
//   P=256 Q=384 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p256_q384_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um16_un4_simd8_adaptive_half
//   P=256 Q=384 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p256_q384_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um16_un4_simd8_equal_split
//   P=256 Q=384 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p256_q384_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um16_un4_simd16_single_block
//   P=256 Q=384 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p256_q384_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um16_un4_simd16_fixed_q
//   P=256 Q=384 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p256_q384_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um16_un4_simd16_adaptive_half
//   P=256 Q=384 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p256_q384_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q384_um16_un4_simd16_equal_split
//   P=256 Q=384 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p256_q384_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um1_un1_simd1_single_block
//   P=256 Q=512 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q512_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um1_un1_simd1_fixed_q
//   P=256 Q=512 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q512_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um1_un1_simd1_adaptive_half
//   P=256 Q=512 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q512_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um1_un1_simd1_equal_split
//   P=256 Q=512 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q512_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um1_un4_simd1_single_block
//   P=256 Q=512 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p256_q512_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um1_un4_simd1_fixed_q
//   P=256 Q=512 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p256_q512_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um1_un4_simd1_adaptive_half
//   P=256 Q=512 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p256_q512_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um1_un4_simd1_equal_split
//   P=256 Q=512 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p256_q512_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um4_un1_simd1_single_block
//   P=256 Q=512 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q512_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um4_un1_simd1_fixed_q
//   P=256 Q=512 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q512_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um4_un1_simd1_adaptive_half
//   P=256 Q=512 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q512_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um4_un1_simd1_equal_split
//   P=256 Q=512 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q512_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um4_un1_simd4_single_block
//   P=256 Q=512 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q512_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um4_un1_simd4_fixed_q
//   P=256 Q=512 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q512_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um4_un1_simd4_adaptive_half
//   P=256 Q=512 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q512_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um4_un1_simd4_equal_split
//   P=256 Q=512 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q512_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um4_un4_simd4_single_block
//   P=256 Q=512 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p256_q512_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um4_un4_simd4_fixed_q
//   P=256 Q=512 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p256_q512_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um4_un4_simd4_adaptive_half
//   P=256 Q=512 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p256_q512_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um4_un4_simd4_equal_split
//   P=256 Q=512 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p256_q512_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um8_un1_simd1_single_block
//   P=256 Q=512 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p256_q512_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um8_un1_simd1_fixed_q
//   P=256 Q=512 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p256_q512_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um8_un1_simd1_adaptive_half
//   P=256 Q=512 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p256_q512_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um8_un1_simd1_equal_split
//   P=256 Q=512 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p256_q512_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um8_un1_simd4_single_block
//   P=256 Q=512 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p256_q512_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um8_un1_simd4_fixed_q
//   P=256 Q=512 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p256_q512_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um8_un1_simd4_adaptive_half
//   P=256 Q=512 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p256_q512_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um8_un1_simd4_equal_split
//   P=256 Q=512 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p256_q512_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um8_un1_simd8_single_block
//   P=256 Q=512 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p256_q512_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um8_un1_simd8_fixed_q
//   P=256 Q=512 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p256_q512_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um8_un1_simd8_adaptive_half
//   P=256 Q=512 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p256_q512_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um8_un1_simd8_equal_split
//   P=256 Q=512 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p256_q512_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um8_un4_simd4_single_block
//   P=256 Q=512 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p256_q512_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um8_un4_simd4_fixed_q
//   P=256 Q=512 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p256_q512_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um8_un4_simd4_adaptive_half
//   P=256 Q=512 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p256_q512_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um8_un4_simd4_equal_split
//   P=256 Q=512 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p256_q512_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um8_un4_simd8_single_block
//   P=256 Q=512 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p256_q512_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um8_un4_simd8_fixed_q
//   P=256 Q=512 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p256_q512_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um8_un4_simd8_adaptive_half
//   P=256 Q=512 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p256_q512_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p256_q512_um8_un4_simd8_equal_split
//   P=256 Q=512 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p256_q512_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um1_un1_simd1_single_block
//   P=384 Q=1 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q1_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um1_un1_simd1_fixed_q
//   P=384 Q=1 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q1_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um1_un1_simd1_adaptive_half
//   P=384 Q=1 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q1_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um1_un1_simd1_equal_split
//   P=384 Q=1 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q1_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um1_un4_simd1_single_block
//   P=384 Q=1 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p384_q1_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um1_un4_simd1_fixed_q
//   P=384 Q=1 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p384_q1_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um1_un4_simd1_adaptive_half
//   P=384 Q=1 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p384_q1_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um1_un4_simd1_equal_split
//   P=384 Q=1 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p384_q1_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um4_un1_simd1_single_block
//   P=384 Q=1 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q1_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um4_un1_simd1_fixed_q
//   P=384 Q=1 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q1_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um4_un1_simd1_adaptive_half
//   P=384 Q=1 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q1_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um4_un1_simd1_equal_split
//   P=384 Q=1 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q1_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um4_un1_simd4_single_block
//   P=384 Q=1 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q1_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um4_un1_simd4_fixed_q
//   P=384 Q=1 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q1_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um4_un1_simd4_adaptive_half
//   P=384 Q=1 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q1_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um4_un1_simd4_equal_split
//   P=384 Q=1 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q1_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um4_un4_simd4_single_block
//   P=384 Q=1 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p384_q1_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um4_un4_simd4_fixed_q
//   P=384 Q=1 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p384_q1_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um4_un4_simd4_adaptive_half
//   P=384 Q=1 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p384_q1_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um4_un4_simd4_equal_split
//   P=384 Q=1 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p384_q1_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um8_un1_simd1_single_block
//   P=384 Q=1 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q1_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um8_un1_simd1_fixed_q
//   P=384 Q=1 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q1_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um8_un1_simd1_adaptive_half
//   P=384 Q=1 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q1_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um8_un1_simd1_equal_split
//   P=384 Q=1 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q1_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um8_un1_simd4_single_block
//   P=384 Q=1 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q1_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um8_un1_simd4_fixed_q
//   P=384 Q=1 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q1_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um8_un1_simd4_adaptive_half
//   P=384 Q=1 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q1_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um8_un1_simd4_equal_split
//   P=384 Q=1 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q1_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um8_un1_simd8_single_block
//   P=384 Q=1 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p384_q1_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um8_un1_simd8_fixed_q
//   P=384 Q=1 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p384_q1_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um8_un1_simd8_adaptive_half
//   P=384 Q=1 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p384_q1_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um8_un1_simd8_equal_split
//   P=384 Q=1 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p384_q1_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um8_un4_simd4_single_block
//   P=384 Q=1 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p384_q1_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um8_un4_simd4_fixed_q
//   P=384 Q=1 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p384_q1_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um8_un4_simd4_adaptive_half
//   P=384 Q=1 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p384_q1_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um8_un4_simd4_equal_split
//   P=384 Q=1 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p384_q1_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um8_un4_simd8_single_block
//   P=384 Q=1 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p384_q1_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um8_un4_simd8_fixed_q
//   P=384 Q=1 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p384_q1_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um8_un4_simd8_adaptive_half
//   P=384 Q=1 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p384_q1_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um8_un4_simd8_equal_split
//   P=384 Q=1 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p384_q1_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um16_un1_simd4_single_block
//   P=384 Q=1 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q1_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um16_un1_simd4_fixed_q
//   P=384 Q=1 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q1_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um16_un1_simd4_adaptive_half
//   P=384 Q=1 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q1_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um16_un1_simd4_equal_split
//   P=384 Q=1 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q1_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um16_un1_simd8_single_block
//   P=384 Q=1 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p384_q1_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um16_un1_simd8_fixed_q
//   P=384 Q=1 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p384_q1_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um16_un1_simd8_adaptive_half
//   P=384 Q=1 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p384_q1_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um16_un1_simd8_equal_split
//   P=384 Q=1 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p384_q1_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um16_un1_simd16_single_block
//   P=384 Q=1 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p384_q1_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um16_un1_simd16_fixed_q
//   P=384 Q=1 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p384_q1_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um16_un1_simd16_adaptive_half
//   P=384 Q=1 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p384_q1_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um16_un1_simd16_equal_split
//   P=384 Q=1 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p384_q1_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um16_un4_simd8_single_block
//   P=384 Q=1 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p384_q1_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um16_un4_simd8_fixed_q
//   P=384 Q=1 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p384_q1_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um16_un4_simd8_adaptive_half
//   P=384 Q=1 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p384_q1_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um16_un4_simd8_equal_split
//   P=384 Q=1 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p384_q1_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um16_un4_simd16_single_block
//   P=384 Q=1 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p384_q1_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um16_un4_simd16_fixed_q
//   P=384 Q=1 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p384_q1_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um16_un4_simd16_adaptive_half
//   P=384 Q=1 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p384_q1_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um16_un4_simd16_equal_split
//   P=384 Q=1 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p384_q1_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um32_un1_simd4_single_block
//   P=384 Q=1 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q1_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um32_un1_simd4_fixed_q
//   P=384 Q=1 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q1_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um32_un1_simd4_adaptive_half
//   P=384 Q=1 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q1_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um32_un1_simd4_equal_split
//   P=384 Q=1 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q1_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um32_un1_simd8_single_block
//   P=384 Q=1 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p384_q1_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um32_un1_simd8_fixed_q
//   P=384 Q=1 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p384_q1_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um32_un1_simd8_adaptive_half
//   P=384 Q=1 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p384_q1_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um32_un1_simd8_equal_split
//   P=384 Q=1 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p384_q1_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um32_un1_simd16_single_block
//   P=384 Q=1 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p384_q1_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um32_un1_simd16_fixed_q
//   P=384 Q=1 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p384_q1_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um32_un1_simd16_adaptive_half
//   P=384 Q=1 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p384_q1_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um32_un1_simd16_equal_split
//   P=384 Q=1 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p384_q1_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um32_un4_simd16_single_block
//   P=384 Q=1 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p384_q1_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um32_un4_simd16_fixed_q
//   P=384 Q=1 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p384_q1_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um32_un4_simd16_adaptive_half
//   P=384 Q=1 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p384_q1_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q1_um32_un4_simd16_equal_split
//   P=384 Q=1 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p384_q1_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um1_un1_simd1_single_block
//   P=384 Q=64 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q64_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um1_un1_simd1_fixed_q
//   P=384 Q=64 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q64_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um1_un1_simd1_adaptive_half
//   P=384 Q=64 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q64_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um1_un1_simd1_equal_split
//   P=384 Q=64 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q64_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um1_un4_simd1_single_block
//   P=384 Q=64 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p384_q64_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um1_un4_simd1_fixed_q
//   P=384 Q=64 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p384_q64_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um1_un4_simd1_adaptive_half
//   P=384 Q=64 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p384_q64_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um1_un4_simd1_equal_split
//   P=384 Q=64 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p384_q64_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um4_un1_simd1_single_block
//   P=384 Q=64 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q64_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um4_un1_simd1_fixed_q
//   P=384 Q=64 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q64_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um4_un1_simd1_adaptive_half
//   P=384 Q=64 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q64_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um4_un1_simd1_equal_split
//   P=384 Q=64 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q64_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um4_un1_simd4_single_block
//   P=384 Q=64 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q64_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um4_un1_simd4_fixed_q
//   P=384 Q=64 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q64_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um4_un1_simd4_adaptive_half
//   P=384 Q=64 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q64_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um4_un1_simd4_equal_split
//   P=384 Q=64 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q64_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um4_un4_simd4_single_block
//   P=384 Q=64 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p384_q64_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um4_un4_simd4_fixed_q
//   P=384 Q=64 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p384_q64_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um4_un4_simd4_adaptive_half
//   P=384 Q=64 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p384_q64_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um4_un4_simd4_equal_split
//   P=384 Q=64 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p384_q64_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um8_un1_simd1_single_block
//   P=384 Q=64 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q64_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um8_un1_simd1_fixed_q
//   P=384 Q=64 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q64_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um8_un1_simd1_adaptive_half
//   P=384 Q=64 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q64_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um8_un1_simd1_equal_split
//   P=384 Q=64 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q64_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um8_un1_simd4_single_block
//   P=384 Q=64 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q64_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um8_un1_simd4_fixed_q
//   P=384 Q=64 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q64_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um8_un1_simd4_adaptive_half
//   P=384 Q=64 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q64_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um8_un1_simd4_equal_split
//   P=384 Q=64 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q64_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um8_un1_simd8_single_block
//   P=384 Q=64 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p384_q64_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um8_un1_simd8_fixed_q
//   P=384 Q=64 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p384_q64_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um8_un1_simd8_adaptive_half
//   P=384 Q=64 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p384_q64_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um8_un1_simd8_equal_split
//   P=384 Q=64 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p384_q64_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um8_un4_simd4_single_block
//   P=384 Q=64 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p384_q64_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um8_un4_simd4_fixed_q
//   P=384 Q=64 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p384_q64_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um8_un4_simd4_adaptive_half
//   P=384 Q=64 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p384_q64_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um8_un4_simd4_equal_split
//   P=384 Q=64 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p384_q64_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um8_un4_simd8_single_block
//   P=384 Q=64 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p384_q64_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um8_un4_simd8_fixed_q
//   P=384 Q=64 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p384_q64_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um8_un4_simd8_adaptive_half
//   P=384 Q=64 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p384_q64_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um8_un4_simd8_equal_split
//   P=384 Q=64 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p384_q64_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um16_un1_simd4_single_block
//   P=384 Q=64 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q64_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um16_un1_simd4_fixed_q
//   P=384 Q=64 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q64_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um16_un1_simd4_adaptive_half
//   P=384 Q=64 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q64_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um16_un1_simd4_equal_split
//   P=384 Q=64 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q64_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um16_un1_simd8_single_block
//   P=384 Q=64 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p384_q64_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um16_un1_simd8_fixed_q
//   P=384 Q=64 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p384_q64_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um16_un1_simd8_adaptive_half
//   P=384 Q=64 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p384_q64_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um16_un1_simd8_equal_split
//   P=384 Q=64 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p384_q64_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um16_un1_simd16_single_block
//   P=384 Q=64 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p384_q64_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um16_un1_simd16_fixed_q
//   P=384 Q=64 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p384_q64_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um16_un1_simd16_adaptive_half
//   P=384 Q=64 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p384_q64_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um16_un1_simd16_equal_split
//   P=384 Q=64 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p384_q64_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um16_un4_simd8_single_block
//   P=384 Q=64 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p384_q64_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um16_un4_simd8_fixed_q
//   P=384 Q=64 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p384_q64_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um16_un4_simd8_adaptive_half
//   P=384 Q=64 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p384_q64_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um16_un4_simd8_equal_split
//   P=384 Q=64 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p384_q64_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um16_un4_simd16_single_block
//   P=384 Q=64 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p384_q64_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um16_un4_simd16_fixed_q
//   P=384 Q=64 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p384_q64_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um16_un4_simd16_adaptive_half
//   P=384 Q=64 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p384_q64_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um16_un4_simd16_equal_split
//   P=384 Q=64 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p384_q64_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um32_un1_simd4_single_block
//   P=384 Q=64 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q64_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um32_un1_simd4_fixed_q
//   P=384 Q=64 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q64_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um32_un1_simd4_adaptive_half
//   P=384 Q=64 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q64_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um32_un1_simd4_equal_split
//   P=384 Q=64 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q64_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um32_un1_simd8_single_block
//   P=384 Q=64 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p384_q64_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um32_un1_simd8_fixed_q
//   P=384 Q=64 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p384_q64_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um32_un1_simd8_adaptive_half
//   P=384 Q=64 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p384_q64_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um32_un1_simd8_equal_split
//   P=384 Q=64 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p384_q64_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um32_un1_simd16_single_block
//   P=384 Q=64 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p384_q64_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um32_un1_simd16_fixed_q
//   P=384 Q=64 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p384_q64_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um32_un1_simd16_adaptive_half
//   P=384 Q=64 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p384_q64_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um32_un1_simd16_equal_split
//   P=384 Q=64 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p384_q64_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um32_un4_simd16_single_block
//   P=384 Q=64 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p384_q64_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um32_un4_simd16_fixed_q
//   P=384 Q=64 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p384_q64_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um32_un4_simd16_adaptive_half
//   P=384 Q=64 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p384_q64_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q64_um32_un4_simd16_equal_split
//   P=384 Q=64 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p384_q64_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um1_un1_simd1_single_block
//   P=384 Q=128 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q128_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um1_un1_simd1_fixed_q
//   P=384 Q=128 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q128_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um1_un1_simd1_adaptive_half
//   P=384 Q=128 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q128_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um1_un1_simd1_equal_split
//   P=384 Q=128 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q128_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um1_un4_simd1_single_block
//   P=384 Q=128 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p384_q128_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um1_un4_simd1_fixed_q
//   P=384 Q=128 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p384_q128_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um1_un4_simd1_adaptive_half
//   P=384 Q=128 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p384_q128_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um1_un4_simd1_equal_split
//   P=384 Q=128 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p384_q128_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um4_un1_simd1_single_block
//   P=384 Q=128 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q128_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um4_un1_simd1_fixed_q
//   P=384 Q=128 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q128_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um4_un1_simd1_adaptive_half
//   P=384 Q=128 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q128_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um4_un1_simd1_equal_split
//   P=384 Q=128 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q128_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um4_un1_simd4_single_block
//   P=384 Q=128 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q128_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um4_un1_simd4_fixed_q
//   P=384 Q=128 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q128_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um4_un1_simd4_adaptive_half
//   P=384 Q=128 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q128_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um4_un1_simd4_equal_split
//   P=384 Q=128 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q128_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um4_un4_simd4_single_block
//   P=384 Q=128 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p384_q128_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um4_un4_simd4_fixed_q
//   P=384 Q=128 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p384_q128_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um4_un4_simd4_adaptive_half
//   P=384 Q=128 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p384_q128_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um4_un4_simd4_equal_split
//   P=384 Q=128 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p384_q128_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um8_un1_simd1_single_block
//   P=384 Q=128 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q128_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um8_un1_simd1_fixed_q
//   P=384 Q=128 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q128_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um8_un1_simd1_adaptive_half
//   P=384 Q=128 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q128_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um8_un1_simd1_equal_split
//   P=384 Q=128 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q128_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um8_un1_simd4_single_block
//   P=384 Q=128 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q128_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um8_un1_simd4_fixed_q
//   P=384 Q=128 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q128_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um8_un1_simd4_adaptive_half
//   P=384 Q=128 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q128_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um8_un1_simd4_equal_split
//   P=384 Q=128 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q128_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um8_un1_simd8_single_block
//   P=384 Q=128 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p384_q128_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um8_un1_simd8_fixed_q
//   P=384 Q=128 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p384_q128_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um8_un1_simd8_adaptive_half
//   P=384 Q=128 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p384_q128_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um8_un1_simd8_equal_split
//   P=384 Q=128 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p384_q128_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um8_un4_simd4_single_block
//   P=384 Q=128 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p384_q128_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um8_un4_simd4_fixed_q
//   P=384 Q=128 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p384_q128_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um8_un4_simd4_adaptive_half
//   P=384 Q=128 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p384_q128_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um8_un4_simd4_equal_split
//   P=384 Q=128 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p384_q128_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um8_un4_simd8_single_block
//   P=384 Q=128 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p384_q128_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um8_un4_simd8_fixed_q
//   P=384 Q=128 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p384_q128_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um8_un4_simd8_adaptive_half
//   P=384 Q=128 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p384_q128_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um8_un4_simd8_equal_split
//   P=384 Q=128 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p384_q128_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um16_un1_simd4_single_block
//   P=384 Q=128 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q128_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um16_un1_simd4_fixed_q
//   P=384 Q=128 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q128_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um16_un1_simd4_adaptive_half
//   P=384 Q=128 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q128_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um16_un1_simd4_equal_split
//   P=384 Q=128 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q128_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um16_un1_simd8_single_block
//   P=384 Q=128 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p384_q128_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um16_un1_simd8_fixed_q
//   P=384 Q=128 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p384_q128_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um16_un1_simd8_adaptive_half
//   P=384 Q=128 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p384_q128_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um16_un1_simd8_equal_split
//   P=384 Q=128 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p384_q128_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um16_un1_simd16_single_block
//   P=384 Q=128 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p384_q128_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um16_un1_simd16_fixed_q
//   P=384 Q=128 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p384_q128_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um16_un1_simd16_adaptive_half
//   P=384 Q=128 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p384_q128_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um16_un1_simd16_equal_split
//   P=384 Q=128 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p384_q128_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um16_un4_simd8_single_block
//   P=384 Q=128 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p384_q128_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um16_un4_simd8_fixed_q
//   P=384 Q=128 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p384_q128_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um16_un4_simd8_adaptive_half
//   P=384 Q=128 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p384_q128_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um16_un4_simd8_equal_split
//   P=384 Q=128 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p384_q128_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um16_un4_simd16_single_block
//   P=384 Q=128 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p384_q128_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um16_un4_simd16_fixed_q
//   P=384 Q=128 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p384_q128_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um16_un4_simd16_adaptive_half
//   P=384 Q=128 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p384_q128_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um16_un4_simd16_equal_split
//   P=384 Q=128 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p384_q128_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um32_un1_simd4_single_block
//   P=384 Q=128 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q128_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um32_un1_simd4_fixed_q
//   P=384 Q=128 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q128_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um32_un1_simd4_adaptive_half
//   P=384 Q=128 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q128_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um32_un1_simd4_equal_split
//   P=384 Q=128 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q128_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um32_un1_simd8_single_block
//   P=384 Q=128 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p384_q128_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um32_un1_simd8_fixed_q
//   P=384 Q=128 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p384_q128_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um32_un1_simd8_adaptive_half
//   P=384 Q=128 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p384_q128_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um32_un1_simd8_equal_split
//   P=384 Q=128 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p384_q128_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um32_un1_simd16_single_block
//   P=384 Q=128 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p384_q128_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um32_un1_simd16_fixed_q
//   P=384 Q=128 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p384_q128_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um32_un1_simd16_adaptive_half
//   P=384 Q=128 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p384_q128_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um32_un1_simd16_equal_split
//   P=384 Q=128 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p384_q128_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um32_un4_simd16_single_block
//   P=384 Q=128 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p384_q128_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um32_un4_simd16_fixed_q
//   P=384 Q=128 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p384_q128_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um32_un4_simd16_adaptive_half
//   P=384 Q=128 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p384_q128_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q128_um32_un4_simd16_equal_split
//   P=384 Q=128 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p384_q128_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um1_un1_simd1_single_block
//   P=384 Q=192 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q192_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um1_un1_simd1_fixed_q
//   P=384 Q=192 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q192_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um1_un1_simd1_adaptive_half
//   P=384 Q=192 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q192_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um1_un1_simd1_equal_split
//   P=384 Q=192 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q192_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um1_un4_simd1_single_block
//   P=384 Q=192 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p384_q192_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um1_un4_simd1_fixed_q
//   P=384 Q=192 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p384_q192_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um1_un4_simd1_adaptive_half
//   P=384 Q=192 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p384_q192_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um1_un4_simd1_equal_split
//   P=384 Q=192 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p384_q192_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um4_un1_simd1_single_block
//   P=384 Q=192 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q192_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um4_un1_simd1_fixed_q
//   P=384 Q=192 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q192_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um4_un1_simd1_adaptive_half
//   P=384 Q=192 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q192_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um4_un1_simd1_equal_split
//   P=384 Q=192 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q192_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um4_un1_simd4_single_block
//   P=384 Q=192 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q192_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um4_un1_simd4_fixed_q
//   P=384 Q=192 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q192_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um4_un1_simd4_adaptive_half
//   P=384 Q=192 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q192_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um4_un1_simd4_equal_split
//   P=384 Q=192 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q192_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um4_un4_simd4_single_block
//   P=384 Q=192 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p384_q192_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um4_un4_simd4_fixed_q
//   P=384 Q=192 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p384_q192_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um4_un4_simd4_adaptive_half
//   P=384 Q=192 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p384_q192_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um4_un4_simd4_equal_split
//   P=384 Q=192 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p384_q192_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um8_un1_simd1_single_block
//   P=384 Q=192 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q192_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um8_un1_simd1_fixed_q
//   P=384 Q=192 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q192_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um8_un1_simd1_adaptive_half
//   P=384 Q=192 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q192_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um8_un1_simd1_equal_split
//   P=384 Q=192 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q192_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um8_un1_simd4_single_block
//   P=384 Q=192 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q192_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um8_un1_simd4_fixed_q
//   P=384 Q=192 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q192_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um8_un1_simd4_adaptive_half
//   P=384 Q=192 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q192_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um8_un1_simd4_equal_split
//   P=384 Q=192 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q192_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um8_un1_simd8_single_block
//   P=384 Q=192 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p384_q192_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um8_un1_simd8_fixed_q
//   P=384 Q=192 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p384_q192_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um8_un1_simd8_adaptive_half
//   P=384 Q=192 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p384_q192_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um8_un1_simd8_equal_split
//   P=384 Q=192 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p384_q192_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um8_un4_simd4_single_block
//   P=384 Q=192 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p384_q192_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um8_un4_simd4_fixed_q
//   P=384 Q=192 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p384_q192_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um8_un4_simd4_adaptive_half
//   P=384 Q=192 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p384_q192_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um8_un4_simd4_equal_split
//   P=384 Q=192 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p384_q192_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um8_un4_simd8_single_block
//   P=384 Q=192 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p384_q192_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um8_un4_simd8_fixed_q
//   P=384 Q=192 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p384_q192_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um8_un4_simd8_adaptive_half
//   P=384 Q=192 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p384_q192_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um8_un4_simd8_equal_split
//   P=384 Q=192 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p384_q192_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um16_un1_simd4_single_block
//   P=384 Q=192 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q192_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um16_un1_simd4_fixed_q
//   P=384 Q=192 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q192_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um16_un1_simd4_adaptive_half
//   P=384 Q=192 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q192_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um16_un1_simd4_equal_split
//   P=384 Q=192 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q192_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um16_un1_simd8_single_block
//   P=384 Q=192 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p384_q192_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um16_un1_simd8_fixed_q
//   P=384 Q=192 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p384_q192_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um16_un1_simd8_adaptive_half
//   P=384 Q=192 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p384_q192_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um16_un1_simd8_equal_split
//   P=384 Q=192 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p384_q192_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um16_un1_simd16_single_block
//   P=384 Q=192 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p384_q192_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um16_un1_simd16_fixed_q
//   P=384 Q=192 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p384_q192_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um16_un1_simd16_adaptive_half
//   P=384 Q=192 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p384_q192_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um16_un1_simd16_equal_split
//   P=384 Q=192 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p384_q192_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um16_un4_simd8_single_block
//   P=384 Q=192 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p384_q192_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um16_un4_simd8_fixed_q
//   P=384 Q=192 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p384_q192_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um16_un4_simd8_adaptive_half
//   P=384 Q=192 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p384_q192_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um16_un4_simd8_equal_split
//   P=384 Q=192 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p384_q192_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um16_un4_simd16_single_block
//   P=384 Q=192 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p384_q192_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um16_un4_simd16_fixed_q
//   P=384 Q=192 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p384_q192_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um16_un4_simd16_adaptive_half
//   P=384 Q=192 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p384_q192_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um16_un4_simd16_equal_split
//   P=384 Q=192 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p384_q192_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um32_un1_simd4_single_block
//   P=384 Q=192 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q192_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um32_un1_simd4_fixed_q
//   P=384 Q=192 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q192_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um32_un1_simd4_adaptive_half
//   P=384 Q=192 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q192_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um32_un1_simd4_equal_split
//   P=384 Q=192 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q192_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um32_un1_simd8_single_block
//   P=384 Q=192 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p384_q192_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um32_un1_simd8_fixed_q
//   P=384 Q=192 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p384_q192_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um32_un1_simd8_adaptive_half
//   P=384 Q=192 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p384_q192_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um32_un1_simd8_equal_split
//   P=384 Q=192 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p384_q192_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um32_un1_simd16_single_block
//   P=384 Q=192 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p384_q192_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um32_un1_simd16_fixed_q
//   P=384 Q=192 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p384_q192_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um32_un1_simd16_adaptive_half
//   P=384 Q=192 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p384_q192_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um32_un1_simd16_equal_split
//   P=384 Q=192 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p384_q192_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um32_un4_simd16_single_block
//   P=384 Q=192 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p384_q192_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um32_un4_simd16_fixed_q
//   P=384 Q=192 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p384_q192_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um32_un4_simd16_adaptive_half
//   P=384 Q=192 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p384_q192_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q192_um32_un4_simd16_equal_split
//   P=384 Q=192 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p384_q192_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um1_un1_simd1_single_block
//   P=384 Q=240 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q240_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um1_un1_simd1_fixed_q
//   P=384 Q=240 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q240_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um1_un1_simd1_adaptive_half
//   P=384 Q=240 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q240_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um1_un1_simd1_equal_split
//   P=384 Q=240 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q240_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um1_un4_simd1_single_block
//   P=384 Q=240 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p384_q240_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um1_un4_simd1_fixed_q
//   P=384 Q=240 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p384_q240_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um1_un4_simd1_adaptive_half
//   P=384 Q=240 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p384_q240_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um1_un4_simd1_equal_split
//   P=384 Q=240 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p384_q240_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um4_un1_simd1_single_block
//   P=384 Q=240 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q240_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um4_un1_simd1_fixed_q
//   P=384 Q=240 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q240_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um4_un1_simd1_adaptive_half
//   P=384 Q=240 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q240_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um4_un1_simd1_equal_split
//   P=384 Q=240 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q240_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um4_un1_simd4_single_block
//   P=384 Q=240 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q240_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um4_un1_simd4_fixed_q
//   P=384 Q=240 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q240_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um4_un1_simd4_adaptive_half
//   P=384 Q=240 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q240_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um4_un1_simd4_equal_split
//   P=384 Q=240 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q240_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um4_un4_simd4_single_block
//   P=384 Q=240 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p384_q240_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um4_un4_simd4_fixed_q
//   P=384 Q=240 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p384_q240_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um4_un4_simd4_adaptive_half
//   P=384 Q=240 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p384_q240_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um4_un4_simd4_equal_split
//   P=384 Q=240 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p384_q240_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um8_un1_simd1_single_block
//   P=384 Q=240 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q240_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um8_un1_simd1_fixed_q
//   P=384 Q=240 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q240_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um8_un1_simd1_adaptive_half
//   P=384 Q=240 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q240_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um8_un1_simd1_equal_split
//   P=384 Q=240 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q240_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um8_un1_simd4_single_block
//   P=384 Q=240 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q240_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um8_un1_simd4_fixed_q
//   P=384 Q=240 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q240_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um8_un1_simd4_adaptive_half
//   P=384 Q=240 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q240_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um8_un1_simd4_equal_split
//   P=384 Q=240 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q240_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um8_un1_simd8_single_block
//   P=384 Q=240 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p384_q240_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um8_un1_simd8_fixed_q
//   P=384 Q=240 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p384_q240_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um8_un1_simd8_adaptive_half
//   P=384 Q=240 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p384_q240_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um8_un1_simd8_equal_split
//   P=384 Q=240 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p384_q240_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um8_un4_simd4_single_block
//   P=384 Q=240 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p384_q240_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um8_un4_simd4_fixed_q
//   P=384 Q=240 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p384_q240_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um8_un4_simd4_adaptive_half
//   P=384 Q=240 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p384_q240_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um8_un4_simd4_equal_split
//   P=384 Q=240 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p384_q240_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um8_un4_simd8_single_block
//   P=384 Q=240 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p384_q240_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um8_un4_simd8_fixed_q
//   P=384 Q=240 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p384_q240_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um8_un4_simd8_adaptive_half
//   P=384 Q=240 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p384_q240_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um8_un4_simd8_equal_split
//   P=384 Q=240 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p384_q240_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um16_un1_simd4_single_block
//   P=384 Q=240 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q240_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um16_un1_simd4_fixed_q
//   P=384 Q=240 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q240_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um16_un1_simd4_adaptive_half
//   P=384 Q=240 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q240_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um16_un1_simd4_equal_split
//   P=384 Q=240 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q240_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um16_un1_simd8_single_block
//   P=384 Q=240 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p384_q240_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um16_un1_simd8_fixed_q
//   P=384 Q=240 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p384_q240_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um16_un1_simd8_adaptive_half
//   P=384 Q=240 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p384_q240_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um16_un1_simd8_equal_split
//   P=384 Q=240 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p384_q240_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um16_un1_simd16_single_block
//   P=384 Q=240 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p384_q240_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um16_un1_simd16_fixed_q
//   P=384 Q=240 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p384_q240_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um16_un1_simd16_adaptive_half
//   P=384 Q=240 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p384_q240_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um16_un1_simd16_equal_split
//   P=384 Q=240 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p384_q240_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um16_un4_simd8_single_block
//   P=384 Q=240 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p384_q240_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um16_un4_simd8_fixed_q
//   P=384 Q=240 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p384_q240_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um16_un4_simd8_adaptive_half
//   P=384 Q=240 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p384_q240_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um16_un4_simd8_equal_split
//   P=384 Q=240 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p384_q240_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um16_un4_simd16_single_block
//   P=384 Q=240 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p384_q240_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um16_un4_simd16_fixed_q
//   P=384 Q=240 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p384_q240_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um16_un4_simd16_adaptive_half
//   P=384 Q=240 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p384_q240_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um16_un4_simd16_equal_split
//   P=384 Q=240 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p384_q240_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um32_un1_simd4_single_block
//   P=384 Q=240 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q240_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um32_un1_simd4_fixed_q
//   P=384 Q=240 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q240_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um32_un1_simd4_adaptive_half
//   P=384 Q=240 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q240_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um32_un1_simd4_equal_split
//   P=384 Q=240 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q240_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um32_un1_simd8_single_block
//   P=384 Q=240 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p384_q240_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um32_un1_simd8_fixed_q
//   P=384 Q=240 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p384_q240_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um32_un1_simd8_adaptive_half
//   P=384 Q=240 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p384_q240_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um32_un1_simd8_equal_split
//   P=384 Q=240 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p384_q240_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um32_un1_simd16_single_block
//   P=384 Q=240 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p384_q240_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um32_un1_simd16_fixed_q
//   P=384 Q=240 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p384_q240_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um32_un1_simd16_adaptive_half
//   P=384 Q=240 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p384_q240_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q240_um32_un1_simd16_equal_split
//   P=384 Q=240 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p384_q240_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um1_un1_simd1_single_block
//   P=384 Q=248 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q248_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um1_un1_simd1_fixed_q
//   P=384 Q=248 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q248_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um1_un1_simd1_adaptive_half
//   P=384 Q=248 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q248_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um1_un1_simd1_equal_split
//   P=384 Q=248 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q248_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um1_un4_simd1_single_block
//   P=384 Q=248 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p384_q248_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um1_un4_simd1_fixed_q
//   P=384 Q=248 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p384_q248_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um1_un4_simd1_adaptive_half
//   P=384 Q=248 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p384_q248_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um1_un4_simd1_equal_split
//   P=384 Q=248 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p384_q248_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um4_un1_simd1_single_block
//   P=384 Q=248 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q248_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um4_un1_simd1_fixed_q
//   P=384 Q=248 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q248_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um4_un1_simd1_adaptive_half
//   P=384 Q=248 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q248_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um4_un1_simd1_equal_split
//   P=384 Q=248 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q248_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um4_un1_simd4_single_block
//   P=384 Q=248 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q248_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um4_un1_simd4_fixed_q
//   P=384 Q=248 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q248_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um4_un1_simd4_adaptive_half
//   P=384 Q=248 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q248_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um4_un1_simd4_equal_split
//   P=384 Q=248 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q248_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um4_un4_simd4_single_block
//   P=384 Q=248 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p384_q248_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um4_un4_simd4_fixed_q
//   P=384 Q=248 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p384_q248_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um4_un4_simd4_adaptive_half
//   P=384 Q=248 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p384_q248_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um4_un4_simd4_equal_split
//   P=384 Q=248 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p384_q248_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um8_un1_simd1_single_block
//   P=384 Q=248 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q248_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um8_un1_simd1_fixed_q
//   P=384 Q=248 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q248_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um8_un1_simd1_adaptive_half
//   P=384 Q=248 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q248_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um8_un1_simd1_equal_split
//   P=384 Q=248 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q248_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um8_un1_simd4_single_block
//   P=384 Q=248 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q248_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um8_un1_simd4_fixed_q
//   P=384 Q=248 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q248_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um8_un1_simd4_adaptive_half
//   P=384 Q=248 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q248_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um8_un1_simd4_equal_split
//   P=384 Q=248 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q248_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um8_un1_simd8_single_block
//   P=384 Q=248 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p384_q248_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um8_un1_simd8_fixed_q
//   P=384 Q=248 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p384_q248_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um8_un1_simd8_adaptive_half
//   P=384 Q=248 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p384_q248_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um8_un1_simd8_equal_split
//   P=384 Q=248 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p384_q248_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um8_un4_simd4_single_block
//   P=384 Q=248 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p384_q248_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um8_un4_simd4_fixed_q
//   P=384 Q=248 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p384_q248_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um8_un4_simd4_adaptive_half
//   P=384 Q=248 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p384_q248_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um8_un4_simd4_equal_split
//   P=384 Q=248 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p384_q248_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um8_un4_simd8_single_block
//   P=384 Q=248 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p384_q248_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um8_un4_simd8_fixed_q
//   P=384 Q=248 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p384_q248_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um8_un4_simd8_adaptive_half
//   P=384 Q=248 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p384_q248_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um8_un4_simd8_equal_split
//   P=384 Q=248 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p384_q248_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um16_un1_simd4_single_block
//   P=384 Q=248 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q248_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um16_un1_simd4_fixed_q
//   P=384 Q=248 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q248_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um16_un1_simd4_adaptive_half
//   P=384 Q=248 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q248_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um16_un1_simd4_equal_split
//   P=384 Q=248 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q248_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um16_un1_simd8_single_block
//   P=384 Q=248 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p384_q248_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um16_un1_simd8_fixed_q
//   P=384 Q=248 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p384_q248_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um16_un1_simd8_adaptive_half
//   P=384 Q=248 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p384_q248_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um16_un1_simd8_equal_split
//   P=384 Q=248 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p384_q248_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um16_un1_simd16_single_block
//   P=384 Q=248 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p384_q248_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um16_un1_simd16_fixed_q
//   P=384 Q=248 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p384_q248_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um16_un1_simd16_adaptive_half
//   P=384 Q=248 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p384_q248_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um16_un1_simd16_equal_split
//   P=384 Q=248 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p384_q248_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um16_un4_simd8_single_block
//   P=384 Q=248 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p384_q248_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um16_un4_simd8_fixed_q
//   P=384 Q=248 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p384_q248_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um16_un4_simd8_adaptive_half
//   P=384 Q=248 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p384_q248_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um16_un4_simd8_equal_split
//   P=384 Q=248 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p384_q248_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um16_un4_simd16_single_block
//   P=384 Q=248 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p384_q248_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um16_un4_simd16_fixed_q
//   P=384 Q=248 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p384_q248_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um16_un4_simd16_adaptive_half
//   P=384 Q=248 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p384_q248_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um16_un4_simd16_equal_split
//   P=384 Q=248 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p384_q248_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um32_un1_simd4_single_block
//   P=384 Q=248 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q248_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um32_un1_simd4_fixed_q
//   P=384 Q=248 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q248_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um32_un1_simd4_adaptive_half
//   P=384 Q=248 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q248_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um32_un1_simd4_equal_split
//   P=384 Q=248 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q248_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um32_un1_simd8_single_block
//   P=384 Q=248 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p384_q248_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um32_un1_simd8_fixed_q
//   P=384 Q=248 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p384_q248_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um32_un1_simd8_adaptive_half
//   P=384 Q=248 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p384_q248_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um32_un1_simd8_equal_split
//   P=384 Q=248 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p384_q248_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um32_un1_simd16_single_block
//   P=384 Q=248 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p384_q248_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um32_un1_simd16_fixed_q
//   P=384 Q=248 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p384_q248_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um32_un1_simd16_adaptive_half
//   P=384 Q=248 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p384_q248_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q248_um32_un1_simd16_equal_split
//   P=384 Q=248 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p384_q248_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um1_un1_simd1_single_block
//   P=384 Q=256 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q256_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um1_un1_simd1_fixed_q
//   P=384 Q=256 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q256_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um1_un1_simd1_adaptive_half
//   P=384 Q=256 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q256_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um1_un1_simd1_equal_split
//   P=384 Q=256 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q256_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um1_un4_simd1_single_block
//   P=384 Q=256 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p384_q256_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um1_un4_simd1_fixed_q
//   P=384 Q=256 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p384_q256_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um1_un4_simd1_adaptive_half
//   P=384 Q=256 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p384_q256_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um1_un4_simd1_equal_split
//   P=384 Q=256 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p384_q256_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um4_un1_simd1_single_block
//   P=384 Q=256 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q256_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um4_un1_simd1_fixed_q
//   P=384 Q=256 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q256_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um4_un1_simd1_adaptive_half
//   P=384 Q=256 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q256_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um4_un1_simd1_equal_split
//   P=384 Q=256 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q256_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um4_un1_simd4_single_block
//   P=384 Q=256 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q256_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um4_un1_simd4_fixed_q
//   P=384 Q=256 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q256_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um4_un1_simd4_adaptive_half
//   P=384 Q=256 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q256_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um4_un1_simd4_equal_split
//   P=384 Q=256 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q256_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um4_un4_simd4_single_block
//   P=384 Q=256 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p384_q256_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um4_un4_simd4_fixed_q
//   P=384 Q=256 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p384_q256_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um4_un4_simd4_adaptive_half
//   P=384 Q=256 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p384_q256_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um4_un4_simd4_equal_split
//   P=384 Q=256 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p384_q256_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um8_un1_simd1_single_block
//   P=384 Q=256 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q256_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um8_un1_simd1_fixed_q
//   P=384 Q=256 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q256_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um8_un1_simd1_adaptive_half
//   P=384 Q=256 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q256_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um8_un1_simd1_equal_split
//   P=384 Q=256 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q256_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um8_un1_simd4_single_block
//   P=384 Q=256 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q256_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um8_un1_simd4_fixed_q
//   P=384 Q=256 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q256_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um8_un1_simd4_adaptive_half
//   P=384 Q=256 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q256_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um8_un1_simd4_equal_split
//   P=384 Q=256 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q256_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um8_un1_simd8_single_block
//   P=384 Q=256 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p384_q256_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um8_un1_simd8_fixed_q
//   P=384 Q=256 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p384_q256_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um8_un1_simd8_adaptive_half
//   P=384 Q=256 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p384_q256_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um8_un1_simd8_equal_split
//   P=384 Q=256 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p384_q256_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um8_un4_simd4_single_block
//   P=384 Q=256 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p384_q256_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um8_un4_simd4_fixed_q
//   P=384 Q=256 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p384_q256_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um8_un4_simd4_adaptive_half
//   P=384 Q=256 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p384_q256_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um8_un4_simd4_equal_split
//   P=384 Q=256 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p384_q256_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um8_un4_simd8_single_block
//   P=384 Q=256 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p384_q256_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um8_un4_simd8_fixed_q
//   P=384 Q=256 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p384_q256_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um8_un4_simd8_adaptive_half
//   P=384 Q=256 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p384_q256_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um8_un4_simd8_equal_split
//   P=384 Q=256 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p384_q256_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um16_un1_simd4_single_block
//   P=384 Q=256 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q256_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um16_un1_simd4_fixed_q
//   P=384 Q=256 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q256_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um16_un1_simd4_adaptive_half
//   P=384 Q=256 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q256_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um16_un1_simd4_equal_split
//   P=384 Q=256 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q256_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um16_un1_simd8_single_block
//   P=384 Q=256 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p384_q256_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um16_un1_simd8_fixed_q
//   P=384 Q=256 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p384_q256_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um16_un1_simd8_adaptive_half
//   P=384 Q=256 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p384_q256_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um16_un1_simd8_equal_split
//   P=384 Q=256 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p384_q256_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um16_un1_simd16_single_block
//   P=384 Q=256 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p384_q256_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um16_un1_simd16_fixed_q
//   P=384 Q=256 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p384_q256_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um16_un1_simd16_adaptive_half
//   P=384 Q=256 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p384_q256_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um16_un1_simd16_equal_split
//   P=384 Q=256 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p384_q256_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um16_un4_simd8_single_block
//   P=384 Q=256 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p384_q256_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um16_un4_simd8_fixed_q
//   P=384 Q=256 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p384_q256_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um16_un4_simd8_adaptive_half
//   P=384 Q=256 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p384_q256_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um16_un4_simd8_equal_split
//   P=384 Q=256 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p384_q256_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um16_un4_simd16_single_block
//   P=384 Q=256 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p384_q256_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um16_un4_simd16_fixed_q
//   P=384 Q=256 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p384_q256_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um16_un4_simd16_adaptive_half
//   P=384 Q=256 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p384_q256_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q256_um16_un4_simd16_equal_split
//   P=384 Q=256 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p384_q256_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um1_un1_simd1_single_block
//   P=384 Q=384 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q384_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um1_un1_simd1_fixed_q
//   P=384 Q=384 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q384_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um1_un1_simd1_adaptive_half
//   P=384 Q=384 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q384_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um1_un1_simd1_equal_split
//   P=384 Q=384 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q384_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um1_un4_simd1_single_block
//   P=384 Q=384 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p384_q384_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um1_un4_simd1_fixed_q
//   P=384 Q=384 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p384_q384_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um1_un4_simd1_adaptive_half
//   P=384 Q=384 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p384_q384_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um1_un4_simd1_equal_split
//   P=384 Q=384 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p384_q384_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um4_un1_simd1_single_block
//   P=384 Q=384 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q384_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um4_un1_simd1_fixed_q
//   P=384 Q=384 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q384_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um4_un1_simd1_adaptive_half
//   P=384 Q=384 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q384_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um4_un1_simd1_equal_split
//   P=384 Q=384 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q384_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um4_un1_simd4_single_block
//   P=384 Q=384 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q384_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um4_un1_simd4_fixed_q
//   P=384 Q=384 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q384_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um4_un1_simd4_adaptive_half
//   P=384 Q=384 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q384_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um4_un1_simd4_equal_split
//   P=384 Q=384 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q384_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um4_un4_simd4_single_block
//   P=384 Q=384 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p384_q384_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um4_un4_simd4_fixed_q
//   P=384 Q=384 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p384_q384_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um4_un4_simd4_adaptive_half
//   P=384 Q=384 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p384_q384_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um4_un4_simd4_equal_split
//   P=384 Q=384 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p384_q384_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um8_un1_simd1_single_block
//   P=384 Q=384 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q384_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um8_un1_simd1_fixed_q
//   P=384 Q=384 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q384_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um8_un1_simd1_adaptive_half
//   P=384 Q=384 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q384_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um8_un1_simd1_equal_split
//   P=384 Q=384 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q384_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um8_un1_simd4_single_block
//   P=384 Q=384 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q384_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um8_un1_simd4_fixed_q
//   P=384 Q=384 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q384_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um8_un1_simd4_adaptive_half
//   P=384 Q=384 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q384_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um8_un1_simd4_equal_split
//   P=384 Q=384 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q384_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um8_un1_simd8_single_block
//   P=384 Q=384 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p384_q384_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um8_un1_simd8_fixed_q
//   P=384 Q=384 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p384_q384_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um8_un1_simd8_adaptive_half
//   P=384 Q=384 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p384_q384_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um8_un1_simd8_equal_split
//   P=384 Q=384 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p384_q384_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um8_un4_simd4_single_block
//   P=384 Q=384 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p384_q384_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um8_un4_simd4_fixed_q
//   P=384 Q=384 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p384_q384_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um8_un4_simd4_adaptive_half
//   P=384 Q=384 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p384_q384_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um8_un4_simd4_equal_split
//   P=384 Q=384 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p384_q384_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um8_un4_simd8_single_block
//   P=384 Q=384 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p384_q384_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um8_un4_simd8_fixed_q
//   P=384 Q=384 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p384_q384_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um8_un4_simd8_adaptive_half
//   P=384 Q=384 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p384_q384_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um8_un4_simd8_equal_split
//   P=384 Q=384 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p384_q384_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um16_un1_simd4_single_block
//   P=384 Q=384 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q384_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um16_un1_simd4_fixed_q
//   P=384 Q=384 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q384_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um16_un1_simd4_adaptive_half
//   P=384 Q=384 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q384_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um16_un1_simd4_equal_split
//   P=384 Q=384 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q384_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um16_un1_simd8_single_block
//   P=384 Q=384 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p384_q384_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um16_un1_simd8_fixed_q
//   P=384 Q=384 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p384_q384_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um16_un1_simd8_adaptive_half
//   P=384 Q=384 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p384_q384_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um16_un1_simd8_equal_split
//   P=384 Q=384 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p384_q384_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um16_un1_simd16_single_block
//   P=384 Q=384 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p384_q384_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um16_un1_simd16_fixed_q
//   P=384 Q=384 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p384_q384_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um16_un1_simd16_adaptive_half
//   P=384 Q=384 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p384_q384_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um16_un1_simd16_equal_split
//   P=384 Q=384 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p384_q384_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um16_un4_simd8_single_block
//   P=384 Q=384 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p384_q384_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um16_un4_simd8_fixed_q
//   P=384 Q=384 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p384_q384_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um16_un4_simd8_adaptive_half
//   P=384 Q=384 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p384_q384_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um16_un4_simd8_equal_split
//   P=384 Q=384 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p384_q384_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um16_un4_simd16_single_block
//   P=384 Q=384 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p384_q384_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um16_un4_simd16_fixed_q
//   P=384 Q=384 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p384_q384_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um16_un4_simd16_adaptive_half
//   P=384 Q=384 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p384_q384_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q384_um16_un4_simd16_equal_split
//   P=384 Q=384 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p384_q384_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um1_un1_simd1_single_block
//   P=384 Q=512 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q512_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um1_un1_simd1_fixed_q
//   P=384 Q=512 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q512_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um1_un1_simd1_adaptive_half
//   P=384 Q=512 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q512_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um1_un1_simd1_equal_split
//   P=384 Q=512 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q512_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um1_un4_simd1_single_block
//   P=384 Q=512 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p384_q512_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um1_un4_simd1_fixed_q
//   P=384 Q=512 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p384_q512_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um1_un4_simd1_adaptive_half
//   P=384 Q=512 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p384_q512_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um1_un4_simd1_equal_split
//   P=384 Q=512 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p384_q512_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um4_un1_simd1_single_block
//   P=384 Q=512 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q512_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um4_un1_simd1_fixed_q
//   P=384 Q=512 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q512_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um4_un1_simd1_adaptive_half
//   P=384 Q=512 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q512_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um4_un1_simd1_equal_split
//   P=384 Q=512 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q512_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um4_un1_simd4_single_block
//   P=384 Q=512 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q512_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um4_un1_simd4_fixed_q
//   P=384 Q=512 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q512_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um4_un1_simd4_adaptive_half
//   P=384 Q=512 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q512_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um4_un1_simd4_equal_split
//   P=384 Q=512 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q512_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um4_un4_simd4_single_block
//   P=384 Q=512 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p384_q512_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um4_un4_simd4_fixed_q
//   P=384 Q=512 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p384_q512_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um4_un4_simd4_adaptive_half
//   P=384 Q=512 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p384_q512_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um4_un4_simd4_equal_split
//   P=384 Q=512 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p384_q512_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um8_un1_simd1_single_block
//   P=384 Q=512 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p384_q512_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um8_un1_simd1_fixed_q
//   P=384 Q=512 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p384_q512_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um8_un1_simd1_adaptive_half
//   P=384 Q=512 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p384_q512_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um8_un1_simd1_equal_split
//   P=384 Q=512 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p384_q512_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um8_un1_simd4_single_block
//   P=384 Q=512 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p384_q512_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um8_un1_simd4_fixed_q
//   P=384 Q=512 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p384_q512_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um8_un1_simd4_adaptive_half
//   P=384 Q=512 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p384_q512_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um8_un1_simd4_equal_split
//   P=384 Q=512 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p384_q512_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um8_un1_simd8_single_block
//   P=384 Q=512 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p384_q512_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um8_un1_simd8_fixed_q
//   P=384 Q=512 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p384_q512_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um8_un1_simd8_adaptive_half
//   P=384 Q=512 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p384_q512_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um8_un1_simd8_equal_split
//   P=384 Q=512 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p384_q512_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um8_un4_simd4_single_block
//   P=384 Q=512 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p384_q512_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um8_un4_simd4_fixed_q
//   P=384 Q=512 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p384_q512_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um8_un4_simd4_adaptive_half
//   P=384 Q=512 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p384_q512_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um8_un4_simd4_equal_split
//   P=384 Q=512 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p384_q512_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um8_un4_simd8_single_block
//   P=384 Q=512 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p384_q512_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um8_un4_simd8_fixed_q
//   P=384 Q=512 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p384_q512_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um8_un4_simd8_adaptive_half
//   P=384 Q=512 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p384_q512_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p384_q512_um8_un4_simd8_equal_split
//   P=384 Q=512 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p384_q512_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um1_un1_simd1_single_block
//   P=512 Q=1 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q1_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um1_un1_simd1_fixed_q
//   P=512 Q=1 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q1_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um1_un1_simd1_adaptive_half
//   P=512 Q=1 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q1_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um1_un1_simd1_equal_split
//   P=512 Q=1 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q1_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um1_un4_simd1_single_block
//   P=512 Q=1 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p512_q1_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um1_un4_simd1_fixed_q
//   P=512 Q=1 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p512_q1_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um1_un4_simd1_adaptive_half
//   P=512 Q=1 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p512_q1_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um1_un4_simd1_equal_split
//   P=512 Q=1 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p512_q1_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um4_un1_simd1_single_block
//   P=512 Q=1 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q1_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um4_un1_simd1_fixed_q
//   P=512 Q=1 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q1_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um4_un1_simd1_adaptive_half
//   P=512 Q=1 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q1_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um4_un1_simd1_equal_split
//   P=512 Q=1 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q1_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um4_un1_simd4_single_block
//   P=512 Q=1 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q1_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um4_un1_simd4_fixed_q
//   P=512 Q=1 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q1_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um4_un1_simd4_adaptive_half
//   P=512 Q=1 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q1_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um4_un1_simd4_equal_split
//   P=512 Q=1 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q1_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um4_un4_simd4_single_block
//   P=512 Q=1 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p512_q1_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um4_un4_simd4_fixed_q
//   P=512 Q=1 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p512_q1_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um4_un4_simd4_adaptive_half
//   P=512 Q=1 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p512_q1_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um4_un4_simd4_equal_split
//   P=512 Q=1 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p512_q1_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um8_un1_simd1_single_block
//   P=512 Q=1 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q1_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um8_un1_simd1_fixed_q
//   P=512 Q=1 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q1_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um8_un1_simd1_adaptive_half
//   P=512 Q=1 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q1_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um8_un1_simd1_equal_split
//   P=512 Q=1 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q1_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um8_un1_simd4_single_block
//   P=512 Q=1 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q1_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um8_un1_simd4_fixed_q
//   P=512 Q=1 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q1_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um8_un1_simd4_adaptive_half
//   P=512 Q=1 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q1_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um8_un1_simd4_equal_split
//   P=512 Q=1 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q1_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um8_un1_simd8_single_block
//   P=512 Q=1 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p512_q1_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um8_un1_simd8_fixed_q
//   P=512 Q=1 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p512_q1_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um8_un1_simd8_adaptive_half
//   P=512 Q=1 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p512_q1_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um8_un1_simd8_equal_split
//   P=512 Q=1 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p512_q1_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um8_un4_simd4_single_block
//   P=512 Q=1 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p512_q1_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um8_un4_simd4_fixed_q
//   P=512 Q=1 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p512_q1_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um8_un4_simd4_adaptive_half
//   P=512 Q=1 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p512_q1_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um8_un4_simd4_equal_split
//   P=512 Q=1 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p512_q1_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um8_un4_simd8_single_block
//   P=512 Q=1 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p512_q1_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um8_un4_simd8_fixed_q
//   P=512 Q=1 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p512_q1_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um8_un4_simd8_adaptive_half
//   P=512 Q=1 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p512_q1_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um8_un4_simd8_equal_split
//   P=512 Q=1 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p512_q1_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um16_un1_simd4_single_block
//   P=512 Q=1 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q1_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um16_un1_simd4_fixed_q
//   P=512 Q=1 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q1_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um16_un1_simd4_adaptive_half
//   P=512 Q=1 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q1_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um16_un1_simd4_equal_split
//   P=512 Q=1 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q1_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um16_un1_simd8_single_block
//   P=512 Q=1 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p512_q1_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um16_un1_simd8_fixed_q
//   P=512 Q=1 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p512_q1_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um16_un1_simd8_adaptive_half
//   P=512 Q=1 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p512_q1_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um16_un1_simd8_equal_split
//   P=512 Q=1 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p512_q1_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um16_un1_simd16_single_block
//   P=512 Q=1 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p512_q1_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um16_un1_simd16_fixed_q
//   P=512 Q=1 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p512_q1_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um16_un1_simd16_adaptive_half
//   P=512 Q=1 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p512_q1_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um16_un1_simd16_equal_split
//   P=512 Q=1 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p512_q1_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um16_un4_simd8_single_block
//   P=512 Q=1 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p512_q1_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um16_un4_simd8_fixed_q
//   P=512 Q=1 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p512_q1_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um16_un4_simd8_adaptive_half
//   P=512 Q=1 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p512_q1_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um16_un4_simd8_equal_split
//   P=512 Q=1 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p512_q1_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um16_un4_simd16_single_block
//   P=512 Q=1 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p512_q1_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um16_un4_simd16_fixed_q
//   P=512 Q=1 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p512_q1_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um16_un4_simd16_adaptive_half
//   P=512 Q=1 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p512_q1_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um16_un4_simd16_equal_split
//   P=512 Q=1 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p512_q1_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um32_un1_simd4_single_block
//   P=512 Q=1 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q1_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um32_un1_simd4_fixed_q
//   P=512 Q=1 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q1_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um32_un1_simd4_adaptive_half
//   P=512 Q=1 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q1_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um32_un1_simd4_equal_split
//   P=512 Q=1 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q1_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um32_un1_simd8_single_block
//   P=512 Q=1 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p512_q1_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um32_un1_simd8_fixed_q
//   P=512 Q=1 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p512_q1_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um32_un1_simd8_adaptive_half
//   P=512 Q=1 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p512_q1_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um32_un1_simd8_equal_split
//   P=512 Q=1 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p512_q1_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um32_un1_simd16_single_block
//   P=512 Q=1 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p512_q1_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um32_un1_simd16_fixed_q
//   P=512 Q=1 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p512_q1_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um32_un1_simd16_adaptive_half
//   P=512 Q=1 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p512_q1_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um32_un1_simd16_equal_split
//   P=512 Q=1 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p512_q1_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um32_un4_simd16_single_block
//   P=512 Q=1 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p512_q1_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um32_un4_simd16_fixed_q
//   P=512 Q=1 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p512_q1_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um32_un4_simd16_adaptive_half
//   P=512 Q=1 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p512_q1_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q1_um32_un4_simd16_equal_split
//   P=512 Q=1 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p512_q1_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um1_un1_simd1_single_block
//   P=512 Q=64 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q64_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um1_un1_simd1_fixed_q
//   P=512 Q=64 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q64_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um1_un1_simd1_adaptive_half
//   P=512 Q=64 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q64_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um1_un1_simd1_equal_split
//   P=512 Q=64 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q64_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um1_un4_simd1_single_block
//   P=512 Q=64 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p512_q64_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um1_un4_simd1_fixed_q
//   P=512 Q=64 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p512_q64_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um1_un4_simd1_adaptive_half
//   P=512 Q=64 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p512_q64_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um1_un4_simd1_equal_split
//   P=512 Q=64 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p512_q64_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um4_un1_simd1_single_block
//   P=512 Q=64 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q64_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um4_un1_simd1_fixed_q
//   P=512 Q=64 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q64_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um4_un1_simd1_adaptive_half
//   P=512 Q=64 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q64_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um4_un1_simd1_equal_split
//   P=512 Q=64 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q64_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um4_un1_simd4_single_block
//   P=512 Q=64 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q64_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um4_un1_simd4_fixed_q
//   P=512 Q=64 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q64_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um4_un1_simd4_adaptive_half
//   P=512 Q=64 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q64_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um4_un1_simd4_equal_split
//   P=512 Q=64 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q64_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um4_un4_simd4_single_block
//   P=512 Q=64 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p512_q64_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um4_un4_simd4_fixed_q
//   P=512 Q=64 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p512_q64_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um4_un4_simd4_adaptive_half
//   P=512 Q=64 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p512_q64_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um4_un4_simd4_equal_split
//   P=512 Q=64 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p512_q64_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um8_un1_simd1_single_block
//   P=512 Q=64 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q64_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um8_un1_simd1_fixed_q
//   P=512 Q=64 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q64_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um8_un1_simd1_adaptive_half
//   P=512 Q=64 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q64_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um8_un1_simd1_equal_split
//   P=512 Q=64 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q64_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um8_un1_simd4_single_block
//   P=512 Q=64 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q64_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um8_un1_simd4_fixed_q
//   P=512 Q=64 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q64_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um8_un1_simd4_adaptive_half
//   P=512 Q=64 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q64_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um8_un1_simd4_equal_split
//   P=512 Q=64 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q64_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um8_un1_simd8_single_block
//   P=512 Q=64 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p512_q64_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um8_un1_simd8_fixed_q
//   P=512 Q=64 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p512_q64_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um8_un1_simd8_adaptive_half
//   P=512 Q=64 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p512_q64_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um8_un1_simd8_equal_split
//   P=512 Q=64 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p512_q64_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um8_un4_simd4_single_block
//   P=512 Q=64 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p512_q64_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um8_un4_simd4_fixed_q
//   P=512 Q=64 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p512_q64_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um8_un4_simd4_adaptive_half
//   P=512 Q=64 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p512_q64_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um8_un4_simd4_equal_split
//   P=512 Q=64 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p512_q64_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um8_un4_simd8_single_block
//   P=512 Q=64 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p512_q64_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um8_un4_simd8_fixed_q
//   P=512 Q=64 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p512_q64_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um8_un4_simd8_adaptive_half
//   P=512 Q=64 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p512_q64_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um8_un4_simd8_equal_split
//   P=512 Q=64 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p512_q64_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um16_un1_simd4_single_block
//   P=512 Q=64 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q64_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um16_un1_simd4_fixed_q
//   P=512 Q=64 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q64_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um16_un1_simd4_adaptive_half
//   P=512 Q=64 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q64_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um16_un1_simd4_equal_split
//   P=512 Q=64 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q64_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um16_un1_simd8_single_block
//   P=512 Q=64 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p512_q64_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um16_un1_simd8_fixed_q
//   P=512 Q=64 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p512_q64_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um16_un1_simd8_adaptive_half
//   P=512 Q=64 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p512_q64_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um16_un1_simd8_equal_split
//   P=512 Q=64 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p512_q64_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um16_un1_simd16_single_block
//   P=512 Q=64 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p512_q64_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um16_un1_simd16_fixed_q
//   P=512 Q=64 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p512_q64_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um16_un1_simd16_adaptive_half
//   P=512 Q=64 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p512_q64_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um16_un1_simd16_equal_split
//   P=512 Q=64 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p512_q64_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um16_un4_simd8_single_block
//   P=512 Q=64 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p512_q64_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um16_un4_simd8_fixed_q
//   P=512 Q=64 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p512_q64_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um16_un4_simd8_adaptive_half
//   P=512 Q=64 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p512_q64_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um16_un4_simd8_equal_split
//   P=512 Q=64 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p512_q64_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um16_un4_simd16_single_block
//   P=512 Q=64 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p512_q64_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um16_un4_simd16_fixed_q
//   P=512 Q=64 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p512_q64_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um16_un4_simd16_adaptive_half
//   P=512 Q=64 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p512_q64_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um16_un4_simd16_equal_split
//   P=512 Q=64 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p512_q64_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um32_un1_simd4_single_block
//   P=512 Q=64 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q64_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um32_un1_simd4_fixed_q
//   P=512 Q=64 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q64_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um32_un1_simd4_adaptive_half
//   P=512 Q=64 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q64_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um32_un1_simd4_equal_split
//   P=512 Q=64 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q64_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um32_un1_simd8_single_block
//   P=512 Q=64 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p512_q64_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um32_un1_simd8_fixed_q
//   P=512 Q=64 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p512_q64_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um32_un1_simd8_adaptive_half
//   P=512 Q=64 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p512_q64_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um32_un1_simd8_equal_split
//   P=512 Q=64 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p512_q64_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um32_un1_simd16_single_block
//   P=512 Q=64 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p512_q64_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um32_un1_simd16_fixed_q
//   P=512 Q=64 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p512_q64_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um32_un1_simd16_adaptive_half
//   P=512 Q=64 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p512_q64_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um32_un1_simd16_equal_split
//   P=512 Q=64 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p512_q64_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um32_un4_simd16_single_block
//   P=512 Q=64 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p512_q64_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um32_un4_simd16_fixed_q
//   P=512 Q=64 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p512_q64_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um32_un4_simd16_adaptive_half
//   P=512 Q=64 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p512_q64_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q64_um32_un4_simd16_equal_split
//   P=512 Q=64 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p512_q64_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um1_un1_simd1_single_block
//   P=512 Q=128 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q128_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um1_un1_simd1_fixed_q
//   P=512 Q=128 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q128_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um1_un1_simd1_adaptive_half
//   P=512 Q=128 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q128_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um1_un1_simd1_equal_split
//   P=512 Q=128 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q128_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um1_un4_simd1_single_block
//   P=512 Q=128 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p512_q128_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um1_un4_simd1_fixed_q
//   P=512 Q=128 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p512_q128_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um1_un4_simd1_adaptive_half
//   P=512 Q=128 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p512_q128_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um1_un4_simd1_equal_split
//   P=512 Q=128 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p512_q128_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um4_un1_simd1_single_block
//   P=512 Q=128 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q128_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um4_un1_simd1_fixed_q
//   P=512 Q=128 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q128_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um4_un1_simd1_adaptive_half
//   P=512 Q=128 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q128_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um4_un1_simd1_equal_split
//   P=512 Q=128 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q128_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um4_un1_simd4_single_block
//   P=512 Q=128 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q128_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um4_un1_simd4_fixed_q
//   P=512 Q=128 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q128_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um4_un1_simd4_adaptive_half
//   P=512 Q=128 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q128_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um4_un1_simd4_equal_split
//   P=512 Q=128 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q128_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um4_un4_simd4_single_block
//   P=512 Q=128 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p512_q128_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um4_un4_simd4_fixed_q
//   P=512 Q=128 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p512_q128_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um4_un4_simd4_adaptive_half
//   P=512 Q=128 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p512_q128_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um4_un4_simd4_equal_split
//   P=512 Q=128 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p512_q128_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um8_un1_simd1_single_block
//   P=512 Q=128 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q128_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um8_un1_simd1_fixed_q
//   P=512 Q=128 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q128_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um8_un1_simd1_adaptive_half
//   P=512 Q=128 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q128_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um8_un1_simd1_equal_split
//   P=512 Q=128 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q128_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um8_un1_simd4_single_block
//   P=512 Q=128 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q128_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um8_un1_simd4_fixed_q
//   P=512 Q=128 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q128_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um8_un1_simd4_adaptive_half
//   P=512 Q=128 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q128_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um8_un1_simd4_equal_split
//   P=512 Q=128 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q128_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um8_un1_simd8_single_block
//   P=512 Q=128 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p512_q128_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um8_un1_simd8_fixed_q
//   P=512 Q=128 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p512_q128_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um8_un1_simd8_adaptive_half
//   P=512 Q=128 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p512_q128_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um8_un1_simd8_equal_split
//   P=512 Q=128 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p512_q128_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um8_un4_simd4_single_block
//   P=512 Q=128 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p512_q128_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um8_un4_simd4_fixed_q
//   P=512 Q=128 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p512_q128_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um8_un4_simd4_adaptive_half
//   P=512 Q=128 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p512_q128_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um8_un4_simd4_equal_split
//   P=512 Q=128 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p512_q128_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um8_un4_simd8_single_block
//   P=512 Q=128 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p512_q128_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um8_un4_simd8_fixed_q
//   P=512 Q=128 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p512_q128_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um8_un4_simd8_adaptive_half
//   P=512 Q=128 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p512_q128_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um8_un4_simd8_equal_split
//   P=512 Q=128 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p512_q128_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um16_un1_simd4_single_block
//   P=512 Q=128 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q128_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um16_un1_simd4_fixed_q
//   P=512 Q=128 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q128_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um16_un1_simd4_adaptive_half
//   P=512 Q=128 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q128_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um16_un1_simd4_equal_split
//   P=512 Q=128 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q128_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um16_un1_simd8_single_block
//   P=512 Q=128 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p512_q128_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um16_un1_simd8_fixed_q
//   P=512 Q=128 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p512_q128_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um16_un1_simd8_adaptive_half
//   P=512 Q=128 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p512_q128_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um16_un1_simd8_equal_split
//   P=512 Q=128 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p512_q128_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um16_un1_simd16_single_block
//   P=512 Q=128 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p512_q128_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um16_un1_simd16_fixed_q
//   P=512 Q=128 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p512_q128_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um16_un1_simd16_adaptive_half
//   P=512 Q=128 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p512_q128_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um16_un1_simd16_equal_split
//   P=512 Q=128 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p512_q128_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um16_un4_simd8_single_block
//   P=512 Q=128 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p512_q128_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um16_un4_simd8_fixed_q
//   P=512 Q=128 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p512_q128_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um16_un4_simd8_adaptive_half
//   P=512 Q=128 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p512_q128_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um16_un4_simd8_equal_split
//   P=512 Q=128 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p512_q128_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um16_un4_simd16_single_block
//   P=512 Q=128 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p512_q128_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um16_un4_simd16_fixed_q
//   P=512 Q=128 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p512_q128_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um16_un4_simd16_adaptive_half
//   P=512 Q=128 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p512_q128_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um16_un4_simd16_equal_split
//   P=512 Q=128 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p512_q128_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um32_un1_simd4_single_block
//   P=512 Q=128 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q128_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um32_un1_simd4_fixed_q
//   P=512 Q=128 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q128_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um32_un1_simd4_adaptive_half
//   P=512 Q=128 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q128_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um32_un1_simd4_equal_split
//   P=512 Q=128 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q128_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um32_un1_simd8_single_block
//   P=512 Q=128 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p512_q128_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um32_un1_simd8_fixed_q
//   P=512 Q=128 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p512_q128_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um32_un1_simd8_adaptive_half
//   P=512 Q=128 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p512_q128_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um32_un1_simd8_equal_split
//   P=512 Q=128 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p512_q128_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um32_un1_simd16_single_block
//   P=512 Q=128 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p512_q128_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um32_un1_simd16_fixed_q
//   P=512 Q=128 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p512_q128_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um32_un1_simd16_adaptive_half
//   P=512 Q=128 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p512_q128_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um32_un1_simd16_equal_split
//   P=512 Q=128 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p512_q128_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um32_un4_simd16_single_block
//   P=512 Q=128 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p512_q128_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um32_un4_simd16_fixed_q
//   P=512 Q=128 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p512_q128_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um32_un4_simd16_adaptive_half
//   P=512 Q=128 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p512_q128_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q128_um32_un4_simd16_equal_split
//   P=512 Q=128 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p512_q128_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um1_un1_simd1_single_block
//   P=512 Q=192 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q192_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um1_un1_simd1_fixed_q
//   P=512 Q=192 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q192_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um1_un1_simd1_adaptive_half
//   P=512 Q=192 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q192_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um1_un1_simd1_equal_split
//   P=512 Q=192 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q192_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um1_un4_simd1_single_block
//   P=512 Q=192 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p512_q192_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um1_un4_simd1_fixed_q
//   P=512 Q=192 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p512_q192_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um1_un4_simd1_adaptive_half
//   P=512 Q=192 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p512_q192_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um1_un4_simd1_equal_split
//   P=512 Q=192 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p512_q192_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um4_un1_simd1_single_block
//   P=512 Q=192 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q192_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um4_un1_simd1_fixed_q
//   P=512 Q=192 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q192_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um4_un1_simd1_adaptive_half
//   P=512 Q=192 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q192_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um4_un1_simd1_equal_split
//   P=512 Q=192 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q192_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um4_un1_simd4_single_block
//   P=512 Q=192 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q192_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um4_un1_simd4_fixed_q
//   P=512 Q=192 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q192_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um4_un1_simd4_adaptive_half
//   P=512 Q=192 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q192_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um4_un1_simd4_equal_split
//   P=512 Q=192 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q192_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um4_un4_simd4_single_block
//   P=512 Q=192 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p512_q192_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um4_un4_simd4_fixed_q
//   P=512 Q=192 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p512_q192_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um4_un4_simd4_adaptive_half
//   P=512 Q=192 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p512_q192_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um4_un4_simd4_equal_split
//   P=512 Q=192 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p512_q192_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um8_un1_simd1_single_block
//   P=512 Q=192 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q192_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um8_un1_simd1_fixed_q
//   P=512 Q=192 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q192_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um8_un1_simd1_adaptive_half
//   P=512 Q=192 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q192_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um8_un1_simd1_equal_split
//   P=512 Q=192 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q192_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um8_un1_simd4_single_block
//   P=512 Q=192 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q192_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um8_un1_simd4_fixed_q
//   P=512 Q=192 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q192_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um8_un1_simd4_adaptive_half
//   P=512 Q=192 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q192_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um8_un1_simd4_equal_split
//   P=512 Q=192 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q192_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um8_un1_simd8_single_block
//   P=512 Q=192 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p512_q192_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um8_un1_simd8_fixed_q
//   P=512 Q=192 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p512_q192_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um8_un1_simd8_adaptive_half
//   P=512 Q=192 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p512_q192_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um8_un1_simd8_equal_split
//   P=512 Q=192 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p512_q192_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um8_un4_simd4_single_block
//   P=512 Q=192 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p512_q192_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um8_un4_simd4_fixed_q
//   P=512 Q=192 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p512_q192_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um8_un4_simd4_adaptive_half
//   P=512 Q=192 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p512_q192_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um8_un4_simd4_equal_split
//   P=512 Q=192 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p512_q192_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um8_un4_simd8_single_block
//   P=512 Q=192 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p512_q192_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um8_un4_simd8_fixed_q
//   P=512 Q=192 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p512_q192_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um8_un4_simd8_adaptive_half
//   P=512 Q=192 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p512_q192_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um8_un4_simd8_equal_split
//   P=512 Q=192 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p512_q192_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um16_un1_simd4_single_block
//   P=512 Q=192 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q192_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um16_un1_simd4_fixed_q
//   P=512 Q=192 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q192_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um16_un1_simd4_adaptive_half
//   P=512 Q=192 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q192_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um16_un1_simd4_equal_split
//   P=512 Q=192 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q192_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um16_un1_simd8_single_block
//   P=512 Q=192 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p512_q192_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um16_un1_simd8_fixed_q
//   P=512 Q=192 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p512_q192_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um16_un1_simd8_adaptive_half
//   P=512 Q=192 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p512_q192_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um16_un1_simd8_equal_split
//   P=512 Q=192 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p512_q192_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um16_un1_simd16_single_block
//   P=512 Q=192 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p512_q192_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um16_un1_simd16_fixed_q
//   P=512 Q=192 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p512_q192_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um16_un1_simd16_adaptive_half
//   P=512 Q=192 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p512_q192_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um16_un1_simd16_equal_split
//   P=512 Q=192 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p512_q192_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um16_un4_simd8_single_block
//   P=512 Q=192 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p512_q192_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um16_un4_simd8_fixed_q
//   P=512 Q=192 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p512_q192_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um16_un4_simd8_adaptive_half
//   P=512 Q=192 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p512_q192_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um16_un4_simd8_equal_split
//   P=512 Q=192 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p512_q192_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um16_un4_simd16_single_block
//   P=512 Q=192 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p512_q192_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um16_un4_simd16_fixed_q
//   P=512 Q=192 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p512_q192_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um16_un4_simd16_adaptive_half
//   P=512 Q=192 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p512_q192_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um16_un4_simd16_equal_split
//   P=512 Q=192 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p512_q192_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um32_un1_simd4_single_block
//   P=512 Q=192 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q192_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um32_un1_simd4_fixed_q
//   P=512 Q=192 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q192_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um32_un1_simd4_adaptive_half
//   P=512 Q=192 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q192_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um32_un1_simd4_equal_split
//   P=512 Q=192 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q192_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um32_un1_simd8_single_block
//   P=512 Q=192 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p512_q192_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um32_un1_simd8_fixed_q
//   P=512 Q=192 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p512_q192_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um32_un1_simd8_adaptive_half
//   P=512 Q=192 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p512_q192_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um32_un1_simd8_equal_split
//   P=512 Q=192 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p512_q192_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um32_un1_simd16_single_block
//   P=512 Q=192 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p512_q192_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um32_un1_simd16_fixed_q
//   P=512 Q=192 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p512_q192_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um32_un1_simd16_adaptive_half
//   P=512 Q=192 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p512_q192_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um32_un1_simd16_equal_split
//   P=512 Q=192 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p512_q192_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um32_un4_simd16_single_block
//   P=512 Q=192 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p512_q192_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um32_un4_simd16_fixed_q
//   P=512 Q=192 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p512_q192_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um32_un4_simd16_adaptive_half
//   P=512 Q=192 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p512_q192_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q192_um32_un4_simd16_equal_split
//   P=512 Q=192 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p512_q192_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um1_un1_simd1_single_block
//   P=512 Q=240 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q240_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um1_un1_simd1_fixed_q
//   P=512 Q=240 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q240_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um1_un1_simd1_adaptive_half
//   P=512 Q=240 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q240_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um1_un1_simd1_equal_split
//   P=512 Q=240 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q240_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um1_un4_simd1_single_block
//   P=512 Q=240 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p512_q240_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um1_un4_simd1_fixed_q
//   P=512 Q=240 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p512_q240_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um1_un4_simd1_adaptive_half
//   P=512 Q=240 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p512_q240_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um1_un4_simd1_equal_split
//   P=512 Q=240 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p512_q240_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um4_un1_simd1_single_block
//   P=512 Q=240 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q240_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um4_un1_simd1_fixed_q
//   P=512 Q=240 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q240_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um4_un1_simd1_adaptive_half
//   P=512 Q=240 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q240_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um4_un1_simd1_equal_split
//   P=512 Q=240 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q240_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um4_un1_simd4_single_block
//   P=512 Q=240 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q240_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um4_un1_simd4_fixed_q
//   P=512 Q=240 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q240_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um4_un1_simd4_adaptive_half
//   P=512 Q=240 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q240_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um4_un1_simd4_equal_split
//   P=512 Q=240 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q240_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um4_un4_simd4_single_block
//   P=512 Q=240 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p512_q240_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um4_un4_simd4_fixed_q
//   P=512 Q=240 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p512_q240_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um4_un4_simd4_adaptive_half
//   P=512 Q=240 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p512_q240_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um4_un4_simd4_equal_split
//   P=512 Q=240 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p512_q240_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um8_un1_simd1_single_block
//   P=512 Q=240 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q240_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um8_un1_simd1_fixed_q
//   P=512 Q=240 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q240_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um8_un1_simd1_adaptive_half
//   P=512 Q=240 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q240_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um8_un1_simd1_equal_split
//   P=512 Q=240 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q240_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um8_un1_simd4_single_block
//   P=512 Q=240 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q240_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um8_un1_simd4_fixed_q
//   P=512 Q=240 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q240_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um8_un1_simd4_adaptive_half
//   P=512 Q=240 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q240_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um8_un1_simd4_equal_split
//   P=512 Q=240 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q240_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um8_un1_simd8_single_block
//   P=512 Q=240 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p512_q240_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um8_un1_simd8_fixed_q
//   P=512 Q=240 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p512_q240_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um8_un1_simd8_adaptive_half
//   P=512 Q=240 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p512_q240_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um8_un1_simd8_equal_split
//   P=512 Q=240 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p512_q240_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um8_un4_simd4_single_block
//   P=512 Q=240 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p512_q240_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um8_un4_simd4_fixed_q
//   P=512 Q=240 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p512_q240_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um8_un4_simd4_adaptive_half
//   P=512 Q=240 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p512_q240_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um8_un4_simd4_equal_split
//   P=512 Q=240 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p512_q240_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um8_un4_simd8_single_block
//   P=512 Q=240 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p512_q240_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um8_un4_simd8_fixed_q
//   P=512 Q=240 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p512_q240_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um8_un4_simd8_adaptive_half
//   P=512 Q=240 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p512_q240_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um8_un4_simd8_equal_split
//   P=512 Q=240 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p512_q240_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um16_un1_simd4_single_block
//   P=512 Q=240 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q240_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um16_un1_simd4_fixed_q
//   P=512 Q=240 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q240_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um16_un1_simd4_adaptive_half
//   P=512 Q=240 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q240_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um16_un1_simd4_equal_split
//   P=512 Q=240 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q240_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um16_un1_simd8_single_block
//   P=512 Q=240 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p512_q240_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um16_un1_simd8_fixed_q
//   P=512 Q=240 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p512_q240_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um16_un1_simd8_adaptive_half
//   P=512 Q=240 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p512_q240_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um16_un1_simd8_equal_split
//   P=512 Q=240 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p512_q240_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um16_un1_simd16_single_block
//   P=512 Q=240 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p512_q240_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um16_un1_simd16_fixed_q
//   P=512 Q=240 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p512_q240_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um16_un1_simd16_adaptive_half
//   P=512 Q=240 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p512_q240_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um16_un1_simd16_equal_split
//   P=512 Q=240 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p512_q240_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um16_un4_simd8_single_block
//   P=512 Q=240 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p512_q240_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um16_un4_simd8_fixed_q
//   P=512 Q=240 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p512_q240_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um16_un4_simd8_adaptive_half
//   P=512 Q=240 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p512_q240_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um16_un4_simd8_equal_split
//   P=512 Q=240 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p512_q240_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um16_un4_simd16_single_block
//   P=512 Q=240 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p512_q240_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um16_un4_simd16_fixed_q
//   P=512 Q=240 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p512_q240_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um16_un4_simd16_adaptive_half
//   P=512 Q=240 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p512_q240_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um16_un4_simd16_equal_split
//   P=512 Q=240 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p512_q240_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um32_un1_simd4_single_block
//   P=512 Q=240 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q240_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um32_un1_simd4_fixed_q
//   P=512 Q=240 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q240_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um32_un1_simd4_adaptive_half
//   P=512 Q=240 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q240_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um32_un1_simd4_equal_split
//   P=512 Q=240 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q240_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um32_un1_simd8_single_block
//   P=512 Q=240 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p512_q240_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um32_un1_simd8_fixed_q
//   P=512 Q=240 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p512_q240_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um32_un1_simd8_adaptive_half
//   P=512 Q=240 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p512_q240_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um32_un1_simd8_equal_split
//   P=512 Q=240 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p512_q240_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um32_un1_simd16_single_block
//   P=512 Q=240 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p512_q240_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um32_un1_simd16_fixed_q
//   P=512 Q=240 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p512_q240_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um32_un1_simd16_adaptive_half
//   P=512 Q=240 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p512_q240_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q240_um32_un1_simd16_equal_split
//   P=512 Q=240 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p512_q240_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um1_un1_simd1_single_block
//   P=512 Q=248 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q248_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um1_un1_simd1_fixed_q
//   P=512 Q=248 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q248_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um1_un1_simd1_adaptive_half
//   P=512 Q=248 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q248_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um1_un1_simd1_equal_split
//   P=512 Q=248 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q248_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um1_un4_simd1_single_block
//   P=512 Q=248 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p512_q248_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um1_un4_simd1_fixed_q
//   P=512 Q=248 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p512_q248_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um1_un4_simd1_adaptive_half
//   P=512 Q=248 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p512_q248_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um1_un4_simd1_equal_split
//   P=512 Q=248 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p512_q248_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um4_un1_simd1_single_block
//   P=512 Q=248 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q248_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um4_un1_simd1_fixed_q
//   P=512 Q=248 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q248_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um4_un1_simd1_adaptive_half
//   P=512 Q=248 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q248_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um4_un1_simd1_equal_split
//   P=512 Q=248 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q248_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um4_un1_simd4_single_block
//   P=512 Q=248 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q248_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um4_un1_simd4_fixed_q
//   P=512 Q=248 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q248_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um4_un1_simd4_adaptive_half
//   P=512 Q=248 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q248_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um4_un1_simd4_equal_split
//   P=512 Q=248 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q248_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um4_un4_simd4_single_block
//   P=512 Q=248 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p512_q248_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um4_un4_simd4_fixed_q
//   P=512 Q=248 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p512_q248_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um4_un4_simd4_adaptive_half
//   P=512 Q=248 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p512_q248_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um4_un4_simd4_equal_split
//   P=512 Q=248 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p512_q248_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um8_un1_simd1_single_block
//   P=512 Q=248 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q248_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um8_un1_simd1_fixed_q
//   P=512 Q=248 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q248_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um8_un1_simd1_adaptive_half
//   P=512 Q=248 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q248_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um8_un1_simd1_equal_split
//   P=512 Q=248 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q248_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um8_un1_simd4_single_block
//   P=512 Q=248 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q248_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um8_un1_simd4_fixed_q
//   P=512 Q=248 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q248_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um8_un1_simd4_adaptive_half
//   P=512 Q=248 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q248_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um8_un1_simd4_equal_split
//   P=512 Q=248 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q248_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um8_un1_simd8_single_block
//   P=512 Q=248 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p512_q248_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um8_un1_simd8_fixed_q
//   P=512 Q=248 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p512_q248_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um8_un1_simd8_adaptive_half
//   P=512 Q=248 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p512_q248_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um8_un1_simd8_equal_split
//   P=512 Q=248 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p512_q248_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um8_un4_simd4_single_block
//   P=512 Q=248 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p512_q248_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um8_un4_simd4_fixed_q
//   P=512 Q=248 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p512_q248_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um8_un4_simd4_adaptive_half
//   P=512 Q=248 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p512_q248_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um8_un4_simd4_equal_split
//   P=512 Q=248 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p512_q248_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um8_un4_simd8_single_block
//   P=512 Q=248 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p512_q248_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um8_un4_simd8_fixed_q
//   P=512 Q=248 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p512_q248_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um8_un4_simd8_adaptive_half
//   P=512 Q=248 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p512_q248_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um8_un4_simd8_equal_split
//   P=512 Q=248 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p512_q248_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um16_un1_simd4_single_block
//   P=512 Q=248 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q248_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um16_un1_simd4_fixed_q
//   P=512 Q=248 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q248_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um16_un1_simd4_adaptive_half
//   P=512 Q=248 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q248_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um16_un1_simd4_equal_split
//   P=512 Q=248 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q248_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um16_un1_simd8_single_block
//   P=512 Q=248 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p512_q248_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um16_un1_simd8_fixed_q
//   P=512 Q=248 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p512_q248_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um16_un1_simd8_adaptive_half
//   P=512 Q=248 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p512_q248_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um16_un1_simd8_equal_split
//   P=512 Q=248 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p512_q248_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um16_un1_simd16_single_block
//   P=512 Q=248 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p512_q248_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um16_un1_simd16_fixed_q
//   P=512 Q=248 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p512_q248_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um16_un1_simd16_adaptive_half
//   P=512 Q=248 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p512_q248_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um16_un1_simd16_equal_split
//   P=512 Q=248 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p512_q248_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um16_un4_simd8_single_block
//   P=512 Q=248 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p512_q248_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um16_un4_simd8_fixed_q
//   P=512 Q=248 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p512_q248_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um16_un4_simd8_adaptive_half
//   P=512 Q=248 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p512_q248_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um16_un4_simd8_equal_split
//   P=512 Q=248 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p512_q248_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um16_un4_simd16_single_block
//   P=512 Q=248 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p512_q248_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um16_un4_simd16_fixed_q
//   P=512 Q=248 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p512_q248_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um16_un4_simd16_adaptive_half
//   P=512 Q=248 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p512_q248_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um16_un4_simd16_equal_split
//   P=512 Q=248 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p512_q248_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um32_un1_simd4_single_block
//   P=512 Q=248 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q248_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um32_un1_simd4_fixed_q
//   P=512 Q=248 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q248_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um32_un1_simd4_adaptive_half
//   P=512 Q=248 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q248_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um32_un1_simd4_equal_split
//   P=512 Q=248 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q248_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um32_un1_simd8_single_block
//   P=512 Q=248 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p512_q248_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um32_un1_simd8_fixed_q
//   P=512 Q=248 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p512_q248_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um32_un1_simd8_adaptive_half
//   P=512 Q=248 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p512_q248_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um32_un1_simd8_equal_split
//   P=512 Q=248 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p512_q248_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um32_un1_simd16_single_block
//   P=512 Q=248 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p512_q248_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um32_un1_simd16_fixed_q
//   P=512 Q=248 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p512_q248_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um32_un1_simd16_adaptive_half
//   P=512 Q=248 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p512_q248_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q248_um32_un1_simd16_equal_split
//   P=512 Q=248 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p512_q248_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um1_un1_simd1_single_block
//   P=512 Q=256 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q256_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um1_un1_simd1_fixed_q
//   P=512 Q=256 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q256_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um1_un1_simd1_adaptive_half
//   P=512 Q=256 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q256_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um1_un1_simd1_equal_split
//   P=512 Q=256 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q256_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um1_un4_simd1_single_block
//   P=512 Q=256 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p512_q256_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um1_un4_simd1_fixed_q
//   P=512 Q=256 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p512_q256_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um1_un4_simd1_adaptive_half
//   P=512 Q=256 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p512_q256_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um1_un4_simd1_equal_split
//   P=512 Q=256 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p512_q256_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um4_un1_simd1_single_block
//   P=512 Q=256 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q256_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um4_un1_simd1_fixed_q
//   P=512 Q=256 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q256_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um4_un1_simd1_adaptive_half
//   P=512 Q=256 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q256_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um4_un1_simd1_equal_split
//   P=512 Q=256 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q256_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um4_un1_simd4_single_block
//   P=512 Q=256 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q256_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um4_un1_simd4_fixed_q
//   P=512 Q=256 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q256_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um4_un1_simd4_adaptive_half
//   P=512 Q=256 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q256_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um4_un1_simd4_equal_split
//   P=512 Q=256 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q256_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um4_un4_simd4_single_block
//   P=512 Q=256 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p512_q256_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um4_un4_simd4_fixed_q
//   P=512 Q=256 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p512_q256_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um4_un4_simd4_adaptive_half
//   P=512 Q=256 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p512_q256_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um4_un4_simd4_equal_split
//   P=512 Q=256 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p512_q256_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um8_un1_simd1_single_block
//   P=512 Q=256 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q256_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um8_un1_simd1_fixed_q
//   P=512 Q=256 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q256_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um8_un1_simd1_adaptive_half
//   P=512 Q=256 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q256_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um8_un1_simd1_equal_split
//   P=512 Q=256 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q256_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um8_un1_simd4_single_block
//   P=512 Q=256 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q256_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um8_un1_simd4_fixed_q
//   P=512 Q=256 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q256_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um8_un1_simd4_adaptive_half
//   P=512 Q=256 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q256_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um8_un1_simd4_equal_split
//   P=512 Q=256 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q256_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um8_un1_simd8_single_block
//   P=512 Q=256 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p512_q256_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um8_un1_simd8_fixed_q
//   P=512 Q=256 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p512_q256_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um8_un1_simd8_adaptive_half
//   P=512 Q=256 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p512_q256_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um8_un1_simd8_equal_split
//   P=512 Q=256 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p512_q256_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um8_un4_simd4_single_block
//   P=512 Q=256 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p512_q256_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um8_un4_simd4_fixed_q
//   P=512 Q=256 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p512_q256_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um8_un4_simd4_adaptive_half
//   P=512 Q=256 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p512_q256_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um8_un4_simd4_equal_split
//   P=512 Q=256 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p512_q256_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um8_un4_simd8_single_block
//   P=512 Q=256 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p512_q256_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um8_un4_simd8_fixed_q
//   P=512 Q=256 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p512_q256_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um8_un4_simd8_adaptive_half
//   P=512 Q=256 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p512_q256_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um8_un4_simd8_equal_split
//   P=512 Q=256 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p512_q256_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um16_un1_simd4_single_block
//   P=512 Q=256 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q256_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um16_un1_simd4_fixed_q
//   P=512 Q=256 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q256_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um16_un1_simd4_adaptive_half
//   P=512 Q=256 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q256_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um16_un1_simd4_equal_split
//   P=512 Q=256 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q256_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um16_un1_simd8_single_block
//   P=512 Q=256 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p512_q256_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um16_un1_simd8_fixed_q
//   P=512 Q=256 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p512_q256_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um16_un1_simd8_adaptive_half
//   P=512 Q=256 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p512_q256_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um16_un1_simd8_equal_split
//   P=512 Q=256 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p512_q256_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um16_un1_simd16_single_block
//   P=512 Q=256 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p512_q256_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um16_un1_simd16_fixed_q
//   P=512 Q=256 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p512_q256_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um16_un1_simd16_adaptive_half
//   P=512 Q=256 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p512_q256_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um16_un1_simd16_equal_split
//   P=512 Q=256 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p512_q256_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um16_un4_simd8_single_block
//   P=512 Q=256 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p512_q256_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um16_un4_simd8_fixed_q
//   P=512 Q=256 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p512_q256_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um16_un4_simd8_adaptive_half
//   P=512 Q=256 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p512_q256_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um16_un4_simd8_equal_split
//   P=512 Q=256 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p512_q256_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um16_un4_simd16_single_block
//   P=512 Q=256 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p512_q256_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um16_un4_simd16_fixed_q
//   P=512 Q=256 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p512_q256_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um16_un4_simd16_adaptive_half
//   P=512 Q=256 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p512_q256_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q256_um16_un4_simd16_equal_split
//   P=512 Q=256 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p512_q256_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um1_un1_simd1_single_block
//   P=512 Q=384 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q384_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um1_un1_simd1_fixed_q
//   P=512 Q=384 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q384_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um1_un1_simd1_adaptive_half
//   P=512 Q=384 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q384_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um1_un1_simd1_equal_split
//   P=512 Q=384 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q384_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um1_un4_simd1_single_block
//   P=512 Q=384 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p512_q384_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um1_un4_simd1_fixed_q
//   P=512 Q=384 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p512_q384_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um1_un4_simd1_adaptive_half
//   P=512 Q=384 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p512_q384_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um1_un4_simd1_equal_split
//   P=512 Q=384 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p512_q384_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um4_un1_simd1_single_block
//   P=512 Q=384 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q384_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um4_un1_simd1_fixed_q
//   P=512 Q=384 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q384_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um4_un1_simd1_adaptive_half
//   P=512 Q=384 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q384_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um4_un1_simd1_equal_split
//   P=512 Q=384 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q384_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um4_un1_simd4_single_block
//   P=512 Q=384 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q384_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um4_un1_simd4_fixed_q
//   P=512 Q=384 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q384_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um4_un1_simd4_adaptive_half
//   P=512 Q=384 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q384_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um4_un1_simd4_equal_split
//   P=512 Q=384 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q384_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um4_un4_simd4_single_block
//   P=512 Q=384 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p512_q384_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um4_un4_simd4_fixed_q
//   P=512 Q=384 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p512_q384_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um4_un4_simd4_adaptive_half
//   P=512 Q=384 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p512_q384_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um4_un4_simd4_equal_split
//   P=512 Q=384 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p512_q384_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um8_un1_simd1_single_block
//   P=512 Q=384 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q384_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um8_un1_simd1_fixed_q
//   P=512 Q=384 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q384_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um8_un1_simd1_adaptive_half
//   P=512 Q=384 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q384_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um8_un1_simd1_equal_split
//   P=512 Q=384 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q384_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um8_un1_simd4_single_block
//   P=512 Q=384 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q384_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um8_un1_simd4_fixed_q
//   P=512 Q=384 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q384_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um8_un1_simd4_adaptive_half
//   P=512 Q=384 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q384_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um8_un1_simd4_equal_split
//   P=512 Q=384 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q384_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um8_un1_simd8_single_block
//   P=512 Q=384 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p512_q384_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um8_un1_simd8_fixed_q
//   P=512 Q=384 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p512_q384_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um8_un1_simd8_adaptive_half
//   P=512 Q=384 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p512_q384_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um8_un1_simd8_equal_split
//   P=512 Q=384 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p512_q384_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um8_un4_simd4_single_block
//   P=512 Q=384 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p512_q384_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um8_un4_simd4_fixed_q
//   P=512 Q=384 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p512_q384_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um8_un4_simd4_adaptive_half
//   P=512 Q=384 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p512_q384_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um8_un4_simd4_equal_split
//   P=512 Q=384 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p512_q384_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um8_un4_simd8_single_block
//   P=512 Q=384 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p512_q384_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um8_un4_simd8_fixed_q
//   P=512 Q=384 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p512_q384_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um8_un4_simd8_adaptive_half
//   P=512 Q=384 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p512_q384_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um8_un4_simd8_equal_split
//   P=512 Q=384 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p512_q384_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um16_un1_simd4_single_block
//   P=512 Q=384 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q384_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um16_un1_simd4_fixed_q
//   P=512 Q=384 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q384_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um16_un1_simd4_adaptive_half
//   P=512 Q=384 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q384_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um16_un1_simd4_equal_split
//   P=512 Q=384 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q384_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um16_un1_simd8_single_block
//   P=512 Q=384 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p512_q384_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um16_un1_simd8_fixed_q
//   P=512 Q=384 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p512_q384_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um16_un1_simd8_adaptive_half
//   P=512 Q=384 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p512_q384_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um16_un1_simd8_equal_split
//   P=512 Q=384 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p512_q384_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um16_un1_simd16_single_block
//   P=512 Q=384 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p512_q384_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um16_un1_simd16_fixed_q
//   P=512 Q=384 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p512_q384_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um16_un1_simd16_adaptive_half
//   P=512 Q=384 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p512_q384_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um16_un1_simd16_equal_split
//   P=512 Q=384 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p512_q384_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um16_un4_simd8_single_block
//   P=512 Q=384 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p512_q384_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um16_un4_simd8_fixed_q
//   P=512 Q=384 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p512_q384_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um16_un4_simd8_adaptive_half
//   P=512 Q=384 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p512_q384_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um16_un4_simd8_equal_split
//   P=512 Q=384 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p512_q384_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um16_un4_simd16_single_block
//   P=512 Q=384 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p512_q384_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um16_un4_simd16_fixed_q
//   P=512 Q=384 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p512_q384_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um16_un4_simd16_adaptive_half
//   P=512 Q=384 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p512_q384_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q384_um16_un4_simd16_equal_split
//   P=512 Q=384 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p512_q384_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um1_un1_simd1_single_block
//   P=512 Q=512 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q512_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um1_un1_simd1_fixed_q
//   P=512 Q=512 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q512_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um1_un1_simd1_adaptive_half
//   P=512 Q=512 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q512_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um1_un1_simd1_equal_split
//   P=512 Q=512 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q512_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um1_un4_simd1_single_block
//   P=512 Q=512 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p512_q512_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um1_un4_simd1_fixed_q
//   P=512 Q=512 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p512_q512_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um1_un4_simd1_adaptive_half
//   P=512 Q=512 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p512_q512_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um1_un4_simd1_equal_split
//   P=512 Q=512 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p512_q512_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um4_un1_simd1_single_block
//   P=512 Q=512 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q512_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um4_un1_simd1_fixed_q
//   P=512 Q=512 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q512_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um4_un1_simd1_adaptive_half
//   P=512 Q=512 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q512_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um4_un1_simd1_equal_split
//   P=512 Q=512 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q512_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um4_un1_simd4_single_block
//   P=512 Q=512 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q512_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um4_un1_simd4_fixed_q
//   P=512 Q=512 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q512_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um4_un1_simd4_adaptive_half
//   P=512 Q=512 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q512_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um4_un1_simd4_equal_split
//   P=512 Q=512 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q512_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um4_un4_simd4_single_block
//   P=512 Q=512 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p512_q512_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um4_un4_simd4_fixed_q
//   P=512 Q=512 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p512_q512_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um4_un4_simd4_adaptive_half
//   P=512 Q=512 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p512_q512_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um4_un4_simd4_equal_split
//   P=512 Q=512 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p512_q512_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um8_un1_simd1_single_block
//   P=512 Q=512 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p512_q512_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um8_un1_simd1_fixed_q
//   P=512 Q=512 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p512_q512_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um8_un1_simd1_adaptive_half
//   P=512 Q=512 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p512_q512_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um8_un1_simd1_equal_split
//   P=512 Q=512 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p512_q512_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um8_un1_simd4_single_block
//   P=512 Q=512 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p512_q512_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um8_un1_simd4_fixed_q
//   P=512 Q=512 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p512_q512_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um8_un1_simd4_adaptive_half
//   P=512 Q=512 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p512_q512_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um8_un1_simd4_equal_split
//   P=512 Q=512 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p512_q512_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um8_un1_simd8_single_block
//   P=512 Q=512 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p512_q512_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um8_un1_simd8_fixed_q
//   P=512 Q=512 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p512_q512_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um8_un1_simd8_adaptive_half
//   P=512 Q=512 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p512_q512_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um8_un1_simd8_equal_split
//   P=512 Q=512 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p512_q512_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um8_un4_simd4_single_block
//   P=512 Q=512 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p512_q512_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um8_un4_simd4_fixed_q
//   P=512 Q=512 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p512_q512_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um8_un4_simd4_adaptive_half
//   P=512 Q=512 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p512_q512_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um8_un4_simd4_equal_split
//   P=512 Q=512 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p512_q512_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um8_un4_simd8_single_block
//   P=512 Q=512 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p512_q512_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um8_un4_simd8_fixed_q
//   P=512 Q=512 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p512_q512_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um8_un4_simd8_adaptive_half
//   P=512 Q=512 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p512_q512_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p512_q512_um8_un4_simd8_equal_split
//   P=512 Q=512 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p512_q512_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um1_un1_simd1_single_block
//   P=768 Q=1 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q1_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um1_un1_simd1_fixed_q
//   P=768 Q=1 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q1_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um1_un1_simd1_adaptive_half
//   P=768 Q=1 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q1_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um1_un1_simd1_equal_split
//   P=768 Q=1 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q1_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um1_un4_simd1_single_block
//   P=768 Q=1 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p768_q1_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um1_un4_simd1_fixed_q
//   P=768 Q=1 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p768_q1_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um1_un4_simd1_adaptive_half
//   P=768 Q=1 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p768_q1_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um1_un4_simd1_equal_split
//   P=768 Q=1 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p768_q1_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um4_un1_simd1_single_block
//   P=768 Q=1 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q1_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um4_un1_simd1_fixed_q
//   P=768 Q=1 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q1_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um4_un1_simd1_adaptive_half
//   P=768 Q=1 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q1_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um4_un1_simd1_equal_split
//   P=768 Q=1 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q1_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um4_un1_simd4_single_block
//   P=768 Q=1 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q1_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um4_un1_simd4_fixed_q
//   P=768 Q=1 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q1_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um4_un1_simd4_adaptive_half
//   P=768 Q=1 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q1_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um4_un1_simd4_equal_split
//   P=768 Q=1 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q1_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um4_un4_simd4_single_block
//   P=768 Q=1 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p768_q1_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um4_un4_simd4_fixed_q
//   P=768 Q=1 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p768_q1_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um4_un4_simd4_adaptive_half
//   P=768 Q=1 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p768_q1_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um4_un4_simd4_equal_split
//   P=768 Q=1 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p768_q1_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um8_un1_simd1_single_block
//   P=768 Q=1 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q1_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um8_un1_simd1_fixed_q
//   P=768 Q=1 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q1_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um8_un1_simd1_adaptive_half
//   P=768 Q=1 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q1_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um8_un1_simd1_equal_split
//   P=768 Q=1 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q1_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um8_un1_simd4_single_block
//   P=768 Q=1 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q1_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um8_un1_simd4_fixed_q
//   P=768 Q=1 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q1_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um8_un1_simd4_adaptive_half
//   P=768 Q=1 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q1_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um8_un1_simd4_equal_split
//   P=768 Q=1 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q1_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um8_un1_simd8_single_block
//   P=768 Q=1 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p768_q1_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um8_un1_simd8_fixed_q
//   P=768 Q=1 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p768_q1_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um8_un1_simd8_adaptive_half
//   P=768 Q=1 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p768_q1_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um8_un1_simd8_equal_split
//   P=768 Q=1 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p768_q1_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um8_un4_simd4_single_block
//   P=768 Q=1 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p768_q1_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um8_un4_simd4_fixed_q
//   P=768 Q=1 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p768_q1_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um8_un4_simd4_adaptive_half
//   P=768 Q=1 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p768_q1_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um8_un4_simd4_equal_split
//   P=768 Q=1 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p768_q1_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um8_un4_simd8_single_block
//   P=768 Q=1 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p768_q1_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um8_un4_simd8_fixed_q
//   P=768 Q=1 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p768_q1_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um8_un4_simd8_adaptive_half
//   P=768 Q=1 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p768_q1_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um8_un4_simd8_equal_split
//   P=768 Q=1 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p768_q1_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um16_un1_simd4_single_block
//   P=768 Q=1 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q1_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um16_un1_simd4_fixed_q
//   P=768 Q=1 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q1_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um16_un1_simd4_adaptive_half
//   P=768 Q=1 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q1_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um16_un1_simd4_equal_split
//   P=768 Q=1 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q1_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um16_un1_simd8_single_block
//   P=768 Q=1 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p768_q1_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um16_un1_simd8_fixed_q
//   P=768 Q=1 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p768_q1_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um16_un1_simd8_adaptive_half
//   P=768 Q=1 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p768_q1_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um16_un1_simd8_equal_split
//   P=768 Q=1 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p768_q1_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um16_un1_simd16_single_block
//   P=768 Q=1 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p768_q1_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um16_un1_simd16_fixed_q
//   P=768 Q=1 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p768_q1_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um16_un1_simd16_adaptive_half
//   P=768 Q=1 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p768_q1_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um16_un1_simd16_equal_split
//   P=768 Q=1 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p768_q1_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um16_un4_simd8_single_block
//   P=768 Q=1 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p768_q1_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um16_un4_simd8_fixed_q
//   P=768 Q=1 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p768_q1_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um16_un4_simd8_adaptive_half
//   P=768 Q=1 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p768_q1_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um16_un4_simd8_equal_split
//   P=768 Q=1 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p768_q1_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um16_un4_simd16_single_block
//   P=768 Q=1 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p768_q1_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um16_un4_simd16_fixed_q
//   P=768 Q=1 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p768_q1_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um16_un4_simd16_adaptive_half
//   P=768 Q=1 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p768_q1_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um16_un4_simd16_equal_split
//   P=768 Q=1 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p768_q1_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um32_un1_simd4_single_block
//   P=768 Q=1 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q1_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um32_un1_simd4_fixed_q
//   P=768 Q=1 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q1_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um32_un1_simd4_adaptive_half
//   P=768 Q=1 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q1_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um32_un1_simd4_equal_split
//   P=768 Q=1 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q1_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um32_un1_simd8_single_block
//   P=768 Q=1 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p768_q1_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um32_un1_simd8_fixed_q
//   P=768 Q=1 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p768_q1_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um32_un1_simd8_adaptive_half
//   P=768 Q=1 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p768_q1_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um32_un1_simd8_equal_split
//   P=768 Q=1 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p768_q1_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um32_un1_simd16_single_block
//   P=768 Q=1 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p768_q1_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um32_un1_simd16_fixed_q
//   P=768 Q=1 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p768_q1_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um32_un1_simd16_adaptive_half
//   P=768 Q=1 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p768_q1_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um32_un1_simd16_equal_split
//   P=768 Q=1 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p768_q1_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um32_un4_simd16_single_block
//   P=768 Q=1 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p768_q1_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um32_un4_simd16_fixed_q
//   P=768 Q=1 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p768_q1_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um32_un4_simd16_adaptive_half
//   P=768 Q=1 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p768_q1_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q1_um32_un4_simd16_equal_split
//   P=768 Q=1 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p768_q1_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um1_un1_simd1_single_block
//   P=768 Q=64 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q64_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um1_un1_simd1_fixed_q
//   P=768 Q=64 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q64_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um1_un1_simd1_adaptive_half
//   P=768 Q=64 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q64_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um1_un1_simd1_equal_split
//   P=768 Q=64 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q64_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um1_un4_simd1_single_block
//   P=768 Q=64 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p768_q64_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um1_un4_simd1_fixed_q
//   P=768 Q=64 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p768_q64_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um1_un4_simd1_adaptive_half
//   P=768 Q=64 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p768_q64_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um1_un4_simd1_equal_split
//   P=768 Q=64 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p768_q64_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um4_un1_simd1_single_block
//   P=768 Q=64 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q64_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um4_un1_simd1_fixed_q
//   P=768 Q=64 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q64_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um4_un1_simd1_adaptive_half
//   P=768 Q=64 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q64_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um4_un1_simd1_equal_split
//   P=768 Q=64 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q64_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um4_un1_simd4_single_block
//   P=768 Q=64 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q64_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um4_un1_simd4_fixed_q
//   P=768 Q=64 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q64_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um4_un1_simd4_adaptive_half
//   P=768 Q=64 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q64_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um4_un1_simd4_equal_split
//   P=768 Q=64 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q64_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um4_un4_simd4_single_block
//   P=768 Q=64 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p768_q64_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um4_un4_simd4_fixed_q
//   P=768 Q=64 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p768_q64_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um4_un4_simd4_adaptive_half
//   P=768 Q=64 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p768_q64_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um4_un4_simd4_equal_split
//   P=768 Q=64 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p768_q64_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um8_un1_simd1_single_block
//   P=768 Q=64 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q64_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um8_un1_simd1_fixed_q
//   P=768 Q=64 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q64_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um8_un1_simd1_adaptive_half
//   P=768 Q=64 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q64_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um8_un1_simd1_equal_split
//   P=768 Q=64 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q64_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um8_un1_simd4_single_block
//   P=768 Q=64 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q64_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um8_un1_simd4_fixed_q
//   P=768 Q=64 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q64_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um8_un1_simd4_adaptive_half
//   P=768 Q=64 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q64_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um8_un1_simd4_equal_split
//   P=768 Q=64 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q64_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um8_un1_simd8_single_block
//   P=768 Q=64 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p768_q64_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um8_un1_simd8_fixed_q
//   P=768 Q=64 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p768_q64_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um8_un1_simd8_adaptive_half
//   P=768 Q=64 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p768_q64_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um8_un1_simd8_equal_split
//   P=768 Q=64 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p768_q64_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um8_un4_simd4_single_block
//   P=768 Q=64 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p768_q64_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um8_un4_simd4_fixed_q
//   P=768 Q=64 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p768_q64_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um8_un4_simd4_adaptive_half
//   P=768 Q=64 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p768_q64_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um8_un4_simd4_equal_split
//   P=768 Q=64 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p768_q64_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um8_un4_simd8_single_block
//   P=768 Q=64 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p768_q64_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um8_un4_simd8_fixed_q
//   P=768 Q=64 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p768_q64_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um8_un4_simd8_adaptive_half
//   P=768 Q=64 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p768_q64_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um8_un4_simd8_equal_split
//   P=768 Q=64 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p768_q64_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um16_un1_simd4_single_block
//   P=768 Q=64 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q64_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um16_un1_simd4_fixed_q
//   P=768 Q=64 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q64_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um16_un1_simd4_adaptive_half
//   P=768 Q=64 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q64_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um16_un1_simd4_equal_split
//   P=768 Q=64 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q64_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um16_un1_simd8_single_block
//   P=768 Q=64 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p768_q64_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um16_un1_simd8_fixed_q
//   P=768 Q=64 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p768_q64_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um16_un1_simd8_adaptive_half
//   P=768 Q=64 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p768_q64_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um16_un1_simd8_equal_split
//   P=768 Q=64 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p768_q64_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um16_un1_simd16_single_block
//   P=768 Q=64 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p768_q64_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um16_un1_simd16_fixed_q
//   P=768 Q=64 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p768_q64_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um16_un1_simd16_adaptive_half
//   P=768 Q=64 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p768_q64_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um16_un1_simd16_equal_split
//   P=768 Q=64 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p768_q64_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um16_un4_simd8_single_block
//   P=768 Q=64 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p768_q64_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um16_un4_simd8_fixed_q
//   P=768 Q=64 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p768_q64_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um16_un4_simd8_adaptive_half
//   P=768 Q=64 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p768_q64_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um16_un4_simd8_equal_split
//   P=768 Q=64 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p768_q64_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um16_un4_simd16_single_block
//   P=768 Q=64 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p768_q64_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um16_un4_simd16_fixed_q
//   P=768 Q=64 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p768_q64_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um16_un4_simd16_adaptive_half
//   P=768 Q=64 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p768_q64_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um16_un4_simd16_equal_split
//   P=768 Q=64 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p768_q64_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um32_un1_simd4_single_block
//   P=768 Q=64 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q64_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um32_un1_simd4_fixed_q
//   P=768 Q=64 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q64_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um32_un1_simd4_adaptive_half
//   P=768 Q=64 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q64_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um32_un1_simd4_equal_split
//   P=768 Q=64 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q64_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um32_un1_simd8_single_block
//   P=768 Q=64 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p768_q64_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um32_un1_simd8_fixed_q
//   P=768 Q=64 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p768_q64_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um32_un1_simd8_adaptive_half
//   P=768 Q=64 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p768_q64_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um32_un1_simd8_equal_split
//   P=768 Q=64 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p768_q64_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um32_un1_simd16_single_block
//   P=768 Q=64 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p768_q64_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um32_un1_simd16_fixed_q
//   P=768 Q=64 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p768_q64_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um32_un1_simd16_adaptive_half
//   P=768 Q=64 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p768_q64_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um32_un1_simd16_equal_split
//   P=768 Q=64 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p768_q64_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um32_un4_simd16_single_block
//   P=768 Q=64 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p768_q64_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um32_un4_simd16_fixed_q
//   P=768 Q=64 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p768_q64_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um32_un4_simd16_adaptive_half
//   P=768 Q=64 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p768_q64_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q64_um32_un4_simd16_equal_split
//   P=768 Q=64 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p768_q64_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um1_un1_simd1_single_block
//   P=768 Q=128 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q128_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um1_un1_simd1_fixed_q
//   P=768 Q=128 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q128_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um1_un1_simd1_adaptive_half
//   P=768 Q=128 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q128_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um1_un1_simd1_equal_split
//   P=768 Q=128 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q128_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um1_un4_simd1_single_block
//   P=768 Q=128 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p768_q128_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um1_un4_simd1_fixed_q
//   P=768 Q=128 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p768_q128_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um1_un4_simd1_adaptive_half
//   P=768 Q=128 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p768_q128_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um1_un4_simd1_equal_split
//   P=768 Q=128 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p768_q128_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um4_un1_simd1_single_block
//   P=768 Q=128 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q128_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um4_un1_simd1_fixed_q
//   P=768 Q=128 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q128_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um4_un1_simd1_adaptive_half
//   P=768 Q=128 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q128_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um4_un1_simd1_equal_split
//   P=768 Q=128 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q128_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um4_un1_simd4_single_block
//   P=768 Q=128 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q128_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um4_un1_simd4_fixed_q
//   P=768 Q=128 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q128_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um4_un1_simd4_adaptive_half
//   P=768 Q=128 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q128_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um4_un1_simd4_equal_split
//   P=768 Q=128 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q128_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um4_un4_simd4_single_block
//   P=768 Q=128 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p768_q128_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um4_un4_simd4_fixed_q
//   P=768 Q=128 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p768_q128_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um4_un4_simd4_adaptive_half
//   P=768 Q=128 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p768_q128_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um4_un4_simd4_equal_split
//   P=768 Q=128 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p768_q128_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um8_un1_simd1_single_block
//   P=768 Q=128 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q128_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um8_un1_simd1_fixed_q
//   P=768 Q=128 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q128_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um8_un1_simd1_adaptive_half
//   P=768 Q=128 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q128_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um8_un1_simd1_equal_split
//   P=768 Q=128 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q128_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um8_un1_simd4_single_block
//   P=768 Q=128 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q128_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um8_un1_simd4_fixed_q
//   P=768 Q=128 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q128_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um8_un1_simd4_adaptive_half
//   P=768 Q=128 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q128_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um8_un1_simd4_equal_split
//   P=768 Q=128 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q128_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um8_un1_simd8_single_block
//   P=768 Q=128 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p768_q128_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um8_un1_simd8_fixed_q
//   P=768 Q=128 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p768_q128_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um8_un1_simd8_adaptive_half
//   P=768 Q=128 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p768_q128_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um8_un1_simd8_equal_split
//   P=768 Q=128 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p768_q128_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um8_un4_simd4_single_block
//   P=768 Q=128 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p768_q128_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um8_un4_simd4_fixed_q
//   P=768 Q=128 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p768_q128_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um8_un4_simd4_adaptive_half
//   P=768 Q=128 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p768_q128_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um8_un4_simd4_equal_split
//   P=768 Q=128 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p768_q128_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um8_un4_simd8_single_block
//   P=768 Q=128 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p768_q128_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um8_un4_simd8_fixed_q
//   P=768 Q=128 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p768_q128_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um8_un4_simd8_adaptive_half
//   P=768 Q=128 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p768_q128_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um8_un4_simd8_equal_split
//   P=768 Q=128 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p768_q128_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um16_un1_simd4_single_block
//   P=768 Q=128 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q128_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um16_un1_simd4_fixed_q
//   P=768 Q=128 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q128_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um16_un1_simd4_adaptive_half
//   P=768 Q=128 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q128_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um16_un1_simd4_equal_split
//   P=768 Q=128 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q128_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um16_un1_simd8_single_block
//   P=768 Q=128 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p768_q128_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um16_un1_simd8_fixed_q
//   P=768 Q=128 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p768_q128_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um16_un1_simd8_adaptive_half
//   P=768 Q=128 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p768_q128_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um16_un1_simd8_equal_split
//   P=768 Q=128 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p768_q128_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um16_un1_simd16_single_block
//   P=768 Q=128 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p768_q128_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um16_un1_simd16_fixed_q
//   P=768 Q=128 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p768_q128_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um16_un1_simd16_adaptive_half
//   P=768 Q=128 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p768_q128_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um16_un1_simd16_equal_split
//   P=768 Q=128 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p768_q128_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um16_un4_simd8_single_block
//   P=768 Q=128 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p768_q128_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um16_un4_simd8_fixed_q
//   P=768 Q=128 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p768_q128_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um16_un4_simd8_adaptive_half
//   P=768 Q=128 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p768_q128_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um16_un4_simd8_equal_split
//   P=768 Q=128 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p768_q128_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um16_un4_simd16_single_block
//   P=768 Q=128 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p768_q128_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um16_un4_simd16_fixed_q
//   P=768 Q=128 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p768_q128_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um16_un4_simd16_adaptive_half
//   P=768 Q=128 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p768_q128_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um16_un4_simd16_equal_split
//   P=768 Q=128 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p768_q128_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um32_un1_simd4_single_block
//   P=768 Q=128 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q128_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um32_un1_simd4_fixed_q
//   P=768 Q=128 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q128_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um32_un1_simd4_adaptive_half
//   P=768 Q=128 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q128_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um32_un1_simd4_equal_split
//   P=768 Q=128 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q128_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um32_un1_simd8_single_block
//   P=768 Q=128 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p768_q128_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um32_un1_simd8_fixed_q
//   P=768 Q=128 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p768_q128_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um32_un1_simd8_adaptive_half
//   P=768 Q=128 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p768_q128_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um32_un1_simd8_equal_split
//   P=768 Q=128 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p768_q128_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um32_un1_simd16_single_block
//   P=768 Q=128 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p768_q128_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um32_un1_simd16_fixed_q
//   P=768 Q=128 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p768_q128_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um32_un1_simd16_adaptive_half
//   P=768 Q=128 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p768_q128_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um32_un1_simd16_equal_split
//   P=768 Q=128 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p768_q128_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um32_un4_simd16_single_block
//   P=768 Q=128 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p768_q128_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um32_un4_simd16_fixed_q
//   P=768 Q=128 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p768_q128_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um32_un4_simd16_adaptive_half
//   P=768 Q=128 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p768_q128_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q128_um32_un4_simd16_equal_split
//   P=768 Q=128 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p768_q128_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um1_un1_simd1_single_block
//   P=768 Q=192 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q192_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um1_un1_simd1_fixed_q
//   P=768 Q=192 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q192_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um1_un1_simd1_adaptive_half
//   P=768 Q=192 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q192_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um1_un1_simd1_equal_split
//   P=768 Q=192 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q192_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um1_un4_simd1_single_block
//   P=768 Q=192 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p768_q192_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um1_un4_simd1_fixed_q
//   P=768 Q=192 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p768_q192_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um1_un4_simd1_adaptive_half
//   P=768 Q=192 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p768_q192_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um1_un4_simd1_equal_split
//   P=768 Q=192 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p768_q192_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um4_un1_simd1_single_block
//   P=768 Q=192 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q192_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um4_un1_simd1_fixed_q
//   P=768 Q=192 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q192_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um4_un1_simd1_adaptive_half
//   P=768 Q=192 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q192_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um4_un1_simd1_equal_split
//   P=768 Q=192 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q192_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um4_un1_simd4_single_block
//   P=768 Q=192 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q192_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um4_un1_simd4_fixed_q
//   P=768 Q=192 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q192_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um4_un1_simd4_adaptive_half
//   P=768 Q=192 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q192_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um4_un1_simd4_equal_split
//   P=768 Q=192 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q192_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um4_un4_simd4_single_block
//   P=768 Q=192 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p768_q192_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um4_un4_simd4_fixed_q
//   P=768 Q=192 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p768_q192_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um4_un4_simd4_adaptive_half
//   P=768 Q=192 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p768_q192_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um4_un4_simd4_equal_split
//   P=768 Q=192 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p768_q192_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um8_un1_simd1_single_block
//   P=768 Q=192 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q192_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um8_un1_simd1_fixed_q
//   P=768 Q=192 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q192_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um8_un1_simd1_adaptive_half
//   P=768 Q=192 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q192_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um8_un1_simd1_equal_split
//   P=768 Q=192 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q192_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um8_un1_simd4_single_block
//   P=768 Q=192 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q192_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um8_un1_simd4_fixed_q
//   P=768 Q=192 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q192_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um8_un1_simd4_adaptive_half
//   P=768 Q=192 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q192_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um8_un1_simd4_equal_split
//   P=768 Q=192 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q192_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um8_un1_simd8_single_block
//   P=768 Q=192 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p768_q192_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um8_un1_simd8_fixed_q
//   P=768 Q=192 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p768_q192_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um8_un1_simd8_adaptive_half
//   P=768 Q=192 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p768_q192_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um8_un1_simd8_equal_split
//   P=768 Q=192 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p768_q192_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um8_un4_simd4_single_block
//   P=768 Q=192 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p768_q192_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um8_un4_simd4_fixed_q
//   P=768 Q=192 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p768_q192_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um8_un4_simd4_adaptive_half
//   P=768 Q=192 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p768_q192_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um8_un4_simd4_equal_split
//   P=768 Q=192 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p768_q192_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um8_un4_simd8_single_block
//   P=768 Q=192 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p768_q192_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um8_un4_simd8_fixed_q
//   P=768 Q=192 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p768_q192_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um8_un4_simd8_adaptive_half
//   P=768 Q=192 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p768_q192_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um8_un4_simd8_equal_split
//   P=768 Q=192 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p768_q192_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um16_un1_simd4_single_block
//   P=768 Q=192 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q192_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um16_un1_simd4_fixed_q
//   P=768 Q=192 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q192_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um16_un1_simd4_adaptive_half
//   P=768 Q=192 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q192_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um16_un1_simd4_equal_split
//   P=768 Q=192 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q192_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um16_un1_simd8_single_block
//   P=768 Q=192 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p768_q192_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um16_un1_simd8_fixed_q
//   P=768 Q=192 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p768_q192_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um16_un1_simd8_adaptive_half
//   P=768 Q=192 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p768_q192_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um16_un1_simd8_equal_split
//   P=768 Q=192 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p768_q192_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um16_un1_simd16_single_block
//   P=768 Q=192 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p768_q192_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um16_un1_simd16_fixed_q
//   P=768 Q=192 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p768_q192_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um16_un1_simd16_adaptive_half
//   P=768 Q=192 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p768_q192_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um16_un1_simd16_equal_split
//   P=768 Q=192 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p768_q192_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um16_un4_simd8_single_block
//   P=768 Q=192 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p768_q192_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um16_un4_simd8_fixed_q
//   P=768 Q=192 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p768_q192_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um16_un4_simd8_adaptive_half
//   P=768 Q=192 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p768_q192_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um16_un4_simd8_equal_split
//   P=768 Q=192 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p768_q192_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um16_un4_simd16_single_block
//   P=768 Q=192 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p768_q192_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um16_un4_simd16_fixed_q
//   P=768 Q=192 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p768_q192_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um16_un4_simd16_adaptive_half
//   P=768 Q=192 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p768_q192_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um16_un4_simd16_equal_split
//   P=768 Q=192 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p768_q192_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um32_un1_simd4_single_block
//   P=768 Q=192 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q192_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um32_un1_simd4_fixed_q
//   P=768 Q=192 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q192_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um32_un1_simd4_adaptive_half
//   P=768 Q=192 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q192_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um32_un1_simd4_equal_split
//   P=768 Q=192 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q192_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um32_un1_simd8_single_block
//   P=768 Q=192 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p768_q192_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um32_un1_simd8_fixed_q
//   P=768 Q=192 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p768_q192_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um32_un1_simd8_adaptive_half
//   P=768 Q=192 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p768_q192_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um32_un1_simd8_equal_split
//   P=768 Q=192 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p768_q192_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um32_un1_simd16_single_block
//   P=768 Q=192 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p768_q192_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um32_un1_simd16_fixed_q
//   P=768 Q=192 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p768_q192_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um32_un1_simd16_adaptive_half
//   P=768 Q=192 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p768_q192_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um32_un1_simd16_equal_split
//   P=768 Q=192 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p768_q192_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um32_un4_simd16_single_block
//   P=768 Q=192 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p768_q192_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um32_un4_simd16_fixed_q
//   P=768 Q=192 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p768_q192_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um32_un4_simd16_adaptive_half
//   P=768 Q=192 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p768_q192_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q192_um32_un4_simd16_equal_split
//   P=768 Q=192 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p768_q192_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um1_un1_simd1_single_block
//   P=768 Q=240 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q240_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um1_un1_simd1_fixed_q
//   P=768 Q=240 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q240_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um1_un1_simd1_adaptive_half
//   P=768 Q=240 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q240_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um1_un1_simd1_equal_split
//   P=768 Q=240 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q240_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um1_un4_simd1_single_block
//   P=768 Q=240 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p768_q240_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um1_un4_simd1_fixed_q
//   P=768 Q=240 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p768_q240_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um1_un4_simd1_adaptive_half
//   P=768 Q=240 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p768_q240_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um1_un4_simd1_equal_split
//   P=768 Q=240 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p768_q240_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um4_un1_simd1_single_block
//   P=768 Q=240 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q240_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um4_un1_simd1_fixed_q
//   P=768 Q=240 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q240_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um4_un1_simd1_adaptive_half
//   P=768 Q=240 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q240_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um4_un1_simd1_equal_split
//   P=768 Q=240 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q240_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um4_un1_simd4_single_block
//   P=768 Q=240 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q240_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um4_un1_simd4_fixed_q
//   P=768 Q=240 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q240_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um4_un1_simd4_adaptive_half
//   P=768 Q=240 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q240_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um4_un1_simd4_equal_split
//   P=768 Q=240 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q240_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um4_un4_simd4_single_block
//   P=768 Q=240 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p768_q240_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um4_un4_simd4_fixed_q
//   P=768 Q=240 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p768_q240_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um4_un4_simd4_adaptive_half
//   P=768 Q=240 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p768_q240_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um4_un4_simd4_equal_split
//   P=768 Q=240 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p768_q240_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um8_un1_simd1_single_block
//   P=768 Q=240 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q240_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um8_un1_simd1_fixed_q
//   P=768 Q=240 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q240_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um8_un1_simd1_adaptive_half
//   P=768 Q=240 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q240_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um8_un1_simd1_equal_split
//   P=768 Q=240 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q240_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um8_un1_simd4_single_block
//   P=768 Q=240 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q240_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um8_un1_simd4_fixed_q
//   P=768 Q=240 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q240_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um8_un1_simd4_adaptive_half
//   P=768 Q=240 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q240_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um8_un1_simd4_equal_split
//   P=768 Q=240 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q240_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um8_un1_simd8_single_block
//   P=768 Q=240 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p768_q240_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um8_un1_simd8_fixed_q
//   P=768 Q=240 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p768_q240_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um8_un1_simd8_adaptive_half
//   P=768 Q=240 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p768_q240_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um8_un1_simd8_equal_split
//   P=768 Q=240 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p768_q240_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um8_un4_simd4_single_block
//   P=768 Q=240 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p768_q240_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um8_un4_simd4_fixed_q
//   P=768 Q=240 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p768_q240_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um8_un4_simd4_adaptive_half
//   P=768 Q=240 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p768_q240_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um8_un4_simd4_equal_split
//   P=768 Q=240 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p768_q240_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um8_un4_simd8_single_block
//   P=768 Q=240 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p768_q240_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um8_un4_simd8_fixed_q
//   P=768 Q=240 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p768_q240_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um8_un4_simd8_adaptive_half
//   P=768 Q=240 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p768_q240_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um8_un4_simd8_equal_split
//   P=768 Q=240 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p768_q240_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um16_un1_simd4_single_block
//   P=768 Q=240 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q240_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um16_un1_simd4_fixed_q
//   P=768 Q=240 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q240_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um16_un1_simd4_adaptive_half
//   P=768 Q=240 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q240_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um16_un1_simd4_equal_split
//   P=768 Q=240 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q240_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um16_un1_simd8_single_block
//   P=768 Q=240 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p768_q240_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um16_un1_simd8_fixed_q
//   P=768 Q=240 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p768_q240_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um16_un1_simd8_adaptive_half
//   P=768 Q=240 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p768_q240_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um16_un1_simd8_equal_split
//   P=768 Q=240 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p768_q240_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um16_un1_simd16_single_block
//   P=768 Q=240 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p768_q240_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um16_un1_simd16_fixed_q
//   P=768 Q=240 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p768_q240_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um16_un1_simd16_adaptive_half
//   P=768 Q=240 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p768_q240_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um16_un1_simd16_equal_split
//   P=768 Q=240 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p768_q240_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um16_un4_simd8_single_block
//   P=768 Q=240 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p768_q240_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um16_un4_simd8_fixed_q
//   P=768 Q=240 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p768_q240_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um16_un4_simd8_adaptive_half
//   P=768 Q=240 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p768_q240_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um16_un4_simd8_equal_split
//   P=768 Q=240 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p768_q240_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um16_un4_simd16_single_block
//   P=768 Q=240 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p768_q240_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um16_un4_simd16_fixed_q
//   P=768 Q=240 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p768_q240_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um16_un4_simd16_adaptive_half
//   P=768 Q=240 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p768_q240_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um16_un4_simd16_equal_split
//   P=768 Q=240 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p768_q240_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um32_un1_simd4_single_block
//   P=768 Q=240 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q240_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um32_un1_simd4_fixed_q
//   P=768 Q=240 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q240_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um32_un1_simd4_adaptive_half
//   P=768 Q=240 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q240_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um32_un1_simd4_equal_split
//   P=768 Q=240 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q240_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um32_un1_simd8_single_block
//   P=768 Q=240 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p768_q240_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um32_un1_simd8_fixed_q
//   P=768 Q=240 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p768_q240_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um32_un1_simd8_adaptive_half
//   P=768 Q=240 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p768_q240_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um32_un1_simd8_equal_split
//   P=768 Q=240 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p768_q240_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um32_un1_simd16_single_block
//   P=768 Q=240 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p768_q240_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um32_un1_simd16_fixed_q
//   P=768 Q=240 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p768_q240_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um32_un1_simd16_adaptive_half
//   P=768 Q=240 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p768_q240_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q240_um32_un1_simd16_equal_split
//   P=768 Q=240 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p768_q240_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um1_un1_simd1_single_block
//   P=768 Q=248 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q248_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um1_un1_simd1_fixed_q
//   P=768 Q=248 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q248_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um1_un1_simd1_adaptive_half
//   P=768 Q=248 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q248_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um1_un1_simd1_equal_split
//   P=768 Q=248 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q248_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um1_un4_simd1_single_block
//   P=768 Q=248 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p768_q248_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um1_un4_simd1_fixed_q
//   P=768 Q=248 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p768_q248_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um1_un4_simd1_adaptive_half
//   P=768 Q=248 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p768_q248_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um1_un4_simd1_equal_split
//   P=768 Q=248 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p768_q248_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um4_un1_simd1_single_block
//   P=768 Q=248 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q248_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um4_un1_simd1_fixed_q
//   P=768 Q=248 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q248_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um4_un1_simd1_adaptive_half
//   P=768 Q=248 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q248_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um4_un1_simd1_equal_split
//   P=768 Q=248 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q248_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um4_un1_simd4_single_block
//   P=768 Q=248 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q248_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um4_un1_simd4_fixed_q
//   P=768 Q=248 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q248_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um4_un1_simd4_adaptive_half
//   P=768 Q=248 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q248_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um4_un1_simd4_equal_split
//   P=768 Q=248 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q248_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um4_un4_simd4_single_block
//   P=768 Q=248 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p768_q248_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um4_un4_simd4_fixed_q
//   P=768 Q=248 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p768_q248_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um4_un4_simd4_adaptive_half
//   P=768 Q=248 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p768_q248_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um4_un4_simd4_equal_split
//   P=768 Q=248 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p768_q248_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um8_un1_simd1_single_block
//   P=768 Q=248 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q248_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um8_un1_simd1_fixed_q
//   P=768 Q=248 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q248_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um8_un1_simd1_adaptive_half
//   P=768 Q=248 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q248_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um8_un1_simd1_equal_split
//   P=768 Q=248 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q248_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um8_un1_simd4_single_block
//   P=768 Q=248 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q248_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um8_un1_simd4_fixed_q
//   P=768 Q=248 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q248_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um8_un1_simd4_adaptive_half
//   P=768 Q=248 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q248_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um8_un1_simd4_equal_split
//   P=768 Q=248 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q248_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um8_un1_simd8_single_block
//   P=768 Q=248 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p768_q248_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um8_un1_simd8_fixed_q
//   P=768 Q=248 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p768_q248_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um8_un1_simd8_adaptive_half
//   P=768 Q=248 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p768_q248_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um8_un1_simd8_equal_split
//   P=768 Q=248 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p768_q248_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um8_un4_simd4_single_block
//   P=768 Q=248 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p768_q248_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um8_un4_simd4_fixed_q
//   P=768 Q=248 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p768_q248_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um8_un4_simd4_adaptive_half
//   P=768 Q=248 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p768_q248_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um8_un4_simd4_equal_split
//   P=768 Q=248 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p768_q248_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um8_un4_simd8_single_block
//   P=768 Q=248 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p768_q248_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um8_un4_simd8_fixed_q
//   P=768 Q=248 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p768_q248_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um8_un4_simd8_adaptive_half
//   P=768 Q=248 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p768_q248_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um8_un4_simd8_equal_split
//   P=768 Q=248 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p768_q248_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um16_un1_simd4_single_block
//   P=768 Q=248 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q248_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um16_un1_simd4_fixed_q
//   P=768 Q=248 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q248_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um16_un1_simd4_adaptive_half
//   P=768 Q=248 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q248_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um16_un1_simd4_equal_split
//   P=768 Q=248 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q248_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um16_un1_simd8_single_block
//   P=768 Q=248 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p768_q248_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um16_un1_simd8_fixed_q
//   P=768 Q=248 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p768_q248_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um16_un1_simd8_adaptive_half
//   P=768 Q=248 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p768_q248_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um16_un1_simd8_equal_split
//   P=768 Q=248 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p768_q248_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um16_un1_simd16_single_block
//   P=768 Q=248 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p768_q248_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um16_un1_simd16_fixed_q
//   P=768 Q=248 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p768_q248_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um16_un1_simd16_adaptive_half
//   P=768 Q=248 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p768_q248_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um16_un1_simd16_equal_split
//   P=768 Q=248 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p768_q248_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um16_un4_simd8_single_block
//   P=768 Q=248 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p768_q248_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um16_un4_simd8_fixed_q
//   P=768 Q=248 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p768_q248_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um16_un4_simd8_adaptive_half
//   P=768 Q=248 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p768_q248_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um16_un4_simd8_equal_split
//   P=768 Q=248 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p768_q248_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um16_un4_simd16_single_block
//   P=768 Q=248 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p768_q248_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um16_un4_simd16_fixed_q
//   P=768 Q=248 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p768_q248_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um16_un4_simd16_adaptive_half
//   P=768 Q=248 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p768_q248_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um16_un4_simd16_equal_split
//   P=768 Q=248 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p768_q248_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um32_un1_simd4_single_block
//   P=768 Q=248 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q248_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um32_un1_simd4_fixed_q
//   P=768 Q=248 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q248_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um32_un1_simd4_adaptive_half
//   P=768 Q=248 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q248_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um32_un1_simd4_equal_split
//   P=768 Q=248 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q248_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um32_un1_simd8_single_block
//   P=768 Q=248 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p768_q248_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um32_un1_simd8_fixed_q
//   P=768 Q=248 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p768_q248_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um32_un1_simd8_adaptive_half
//   P=768 Q=248 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p768_q248_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um32_un1_simd8_equal_split
//   P=768 Q=248 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p768_q248_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um32_un1_simd16_single_block
//   P=768 Q=248 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p768_q248_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um32_un1_simd16_fixed_q
//   P=768 Q=248 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p768_q248_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um32_un1_simd16_adaptive_half
//   P=768 Q=248 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p768_q248_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q248_um32_un1_simd16_equal_split
//   P=768 Q=248 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p768_q248_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um1_un1_simd1_single_block
//   P=768 Q=256 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q256_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um1_un1_simd1_fixed_q
//   P=768 Q=256 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q256_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um1_un1_simd1_adaptive_half
//   P=768 Q=256 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q256_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um1_un1_simd1_equal_split
//   P=768 Q=256 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q256_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um1_un4_simd1_single_block
//   P=768 Q=256 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p768_q256_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um1_un4_simd1_fixed_q
//   P=768 Q=256 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p768_q256_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um1_un4_simd1_adaptive_half
//   P=768 Q=256 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p768_q256_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um1_un4_simd1_equal_split
//   P=768 Q=256 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p768_q256_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um4_un1_simd1_single_block
//   P=768 Q=256 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q256_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um4_un1_simd1_fixed_q
//   P=768 Q=256 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q256_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um4_un1_simd1_adaptive_half
//   P=768 Q=256 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q256_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um4_un1_simd1_equal_split
//   P=768 Q=256 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q256_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um4_un1_simd4_single_block
//   P=768 Q=256 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q256_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um4_un1_simd4_fixed_q
//   P=768 Q=256 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q256_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um4_un1_simd4_adaptive_half
//   P=768 Q=256 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q256_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um4_un1_simd4_equal_split
//   P=768 Q=256 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q256_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um4_un4_simd4_single_block
//   P=768 Q=256 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p768_q256_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um4_un4_simd4_fixed_q
//   P=768 Q=256 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p768_q256_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um4_un4_simd4_adaptive_half
//   P=768 Q=256 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p768_q256_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um4_un4_simd4_equal_split
//   P=768 Q=256 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p768_q256_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um8_un1_simd1_single_block
//   P=768 Q=256 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q256_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um8_un1_simd1_fixed_q
//   P=768 Q=256 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q256_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um8_un1_simd1_adaptive_half
//   P=768 Q=256 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q256_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um8_un1_simd1_equal_split
//   P=768 Q=256 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q256_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um8_un1_simd4_single_block
//   P=768 Q=256 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q256_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um8_un1_simd4_fixed_q
//   P=768 Q=256 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q256_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um8_un1_simd4_adaptive_half
//   P=768 Q=256 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q256_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um8_un1_simd4_equal_split
//   P=768 Q=256 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q256_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um8_un1_simd8_single_block
//   P=768 Q=256 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p768_q256_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um8_un1_simd8_fixed_q
//   P=768 Q=256 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p768_q256_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um8_un1_simd8_adaptive_half
//   P=768 Q=256 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p768_q256_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um8_un1_simd8_equal_split
//   P=768 Q=256 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p768_q256_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um8_un4_simd4_single_block
//   P=768 Q=256 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p768_q256_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um8_un4_simd4_fixed_q
//   P=768 Q=256 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p768_q256_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um8_un4_simd4_adaptive_half
//   P=768 Q=256 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p768_q256_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um8_un4_simd4_equal_split
//   P=768 Q=256 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p768_q256_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um8_un4_simd8_single_block
//   P=768 Q=256 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p768_q256_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um8_un4_simd8_fixed_q
//   P=768 Q=256 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p768_q256_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um8_un4_simd8_adaptive_half
//   P=768 Q=256 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p768_q256_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um8_un4_simd8_equal_split
//   P=768 Q=256 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p768_q256_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um16_un1_simd4_single_block
//   P=768 Q=256 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q256_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um16_un1_simd4_fixed_q
//   P=768 Q=256 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q256_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um16_un1_simd4_adaptive_half
//   P=768 Q=256 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q256_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um16_un1_simd4_equal_split
//   P=768 Q=256 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q256_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um16_un1_simd8_single_block
//   P=768 Q=256 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p768_q256_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um16_un1_simd8_fixed_q
//   P=768 Q=256 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p768_q256_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um16_un1_simd8_adaptive_half
//   P=768 Q=256 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p768_q256_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um16_un1_simd8_equal_split
//   P=768 Q=256 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p768_q256_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um16_un1_simd16_single_block
//   P=768 Q=256 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p768_q256_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um16_un1_simd16_fixed_q
//   P=768 Q=256 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p768_q256_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um16_un1_simd16_adaptive_half
//   P=768 Q=256 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p768_q256_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um16_un1_simd16_equal_split
//   P=768 Q=256 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p768_q256_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um16_un4_simd8_single_block
//   P=768 Q=256 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p768_q256_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um16_un4_simd8_fixed_q
//   P=768 Q=256 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p768_q256_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um16_un4_simd8_adaptive_half
//   P=768 Q=256 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p768_q256_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um16_un4_simd8_equal_split
//   P=768 Q=256 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p768_q256_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um16_un4_simd16_single_block
//   P=768 Q=256 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p768_q256_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um16_un4_simd16_fixed_q
//   P=768 Q=256 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p768_q256_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um16_un4_simd16_adaptive_half
//   P=768 Q=256 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p768_q256_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q256_um16_un4_simd16_equal_split
//   P=768 Q=256 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p768_q256_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um1_un1_simd1_single_block
//   P=768 Q=384 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q384_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um1_un1_simd1_fixed_q
//   P=768 Q=384 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q384_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um1_un1_simd1_adaptive_half
//   P=768 Q=384 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q384_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um1_un1_simd1_equal_split
//   P=768 Q=384 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q384_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um1_un4_simd1_single_block
//   P=768 Q=384 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p768_q384_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um1_un4_simd1_fixed_q
//   P=768 Q=384 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p768_q384_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um1_un4_simd1_adaptive_half
//   P=768 Q=384 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p768_q384_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um1_un4_simd1_equal_split
//   P=768 Q=384 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p768_q384_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um4_un1_simd1_single_block
//   P=768 Q=384 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q384_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um4_un1_simd1_fixed_q
//   P=768 Q=384 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q384_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um4_un1_simd1_adaptive_half
//   P=768 Q=384 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q384_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um4_un1_simd1_equal_split
//   P=768 Q=384 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q384_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um4_un1_simd4_single_block
//   P=768 Q=384 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q384_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um4_un1_simd4_fixed_q
//   P=768 Q=384 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q384_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um4_un1_simd4_adaptive_half
//   P=768 Q=384 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q384_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um4_un1_simd4_equal_split
//   P=768 Q=384 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q384_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um4_un4_simd4_single_block
//   P=768 Q=384 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p768_q384_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um4_un4_simd4_fixed_q
//   P=768 Q=384 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p768_q384_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um4_un4_simd4_adaptive_half
//   P=768 Q=384 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p768_q384_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um4_un4_simd4_equal_split
//   P=768 Q=384 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p768_q384_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um8_un1_simd1_single_block
//   P=768 Q=384 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q384_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um8_un1_simd1_fixed_q
//   P=768 Q=384 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q384_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um8_un1_simd1_adaptive_half
//   P=768 Q=384 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q384_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um8_un1_simd1_equal_split
//   P=768 Q=384 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q384_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um8_un1_simd4_single_block
//   P=768 Q=384 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q384_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um8_un1_simd4_fixed_q
//   P=768 Q=384 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q384_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um8_un1_simd4_adaptive_half
//   P=768 Q=384 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q384_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um8_un1_simd4_equal_split
//   P=768 Q=384 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q384_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um8_un1_simd8_single_block
//   P=768 Q=384 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p768_q384_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um8_un1_simd8_fixed_q
//   P=768 Q=384 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p768_q384_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um8_un1_simd8_adaptive_half
//   P=768 Q=384 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p768_q384_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um8_un1_simd8_equal_split
//   P=768 Q=384 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p768_q384_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um8_un4_simd4_single_block
//   P=768 Q=384 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p768_q384_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um8_un4_simd4_fixed_q
//   P=768 Q=384 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p768_q384_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um8_un4_simd4_adaptive_half
//   P=768 Q=384 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p768_q384_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um8_un4_simd4_equal_split
//   P=768 Q=384 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p768_q384_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um8_un4_simd8_single_block
//   P=768 Q=384 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p768_q384_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um8_un4_simd8_fixed_q
//   P=768 Q=384 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p768_q384_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um8_un4_simd8_adaptive_half
//   P=768 Q=384 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p768_q384_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um8_un4_simd8_equal_split
//   P=768 Q=384 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p768_q384_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um16_un1_simd4_single_block
//   P=768 Q=384 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q384_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um16_un1_simd4_fixed_q
//   P=768 Q=384 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q384_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um16_un1_simd4_adaptive_half
//   P=768 Q=384 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q384_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um16_un1_simd4_equal_split
//   P=768 Q=384 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q384_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um16_un1_simd8_single_block
//   P=768 Q=384 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p768_q384_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um16_un1_simd8_fixed_q
//   P=768 Q=384 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p768_q384_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um16_un1_simd8_adaptive_half
//   P=768 Q=384 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p768_q384_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um16_un1_simd8_equal_split
//   P=768 Q=384 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p768_q384_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um16_un1_simd16_single_block
//   P=768 Q=384 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p768_q384_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um16_un1_simd16_fixed_q
//   P=768 Q=384 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p768_q384_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um16_un1_simd16_adaptive_half
//   P=768 Q=384 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p768_q384_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um16_un1_simd16_equal_split
//   P=768 Q=384 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p768_q384_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um16_un4_simd8_single_block
//   P=768 Q=384 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p768_q384_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um16_un4_simd8_fixed_q
//   P=768 Q=384 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p768_q384_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um16_un4_simd8_adaptive_half
//   P=768 Q=384 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p768_q384_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um16_un4_simd8_equal_split
//   P=768 Q=384 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p768_q384_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um16_un4_simd16_single_block
//   P=768 Q=384 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p768_q384_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um16_un4_simd16_fixed_q
//   P=768 Q=384 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p768_q384_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um16_un4_simd16_adaptive_half
//   P=768 Q=384 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p768_q384_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q384_um16_un4_simd16_equal_split
//   P=768 Q=384 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p768_q384_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um1_un1_simd1_single_block
//   P=768 Q=512 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q512_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um1_un1_simd1_fixed_q
//   P=768 Q=512 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q512_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um1_un1_simd1_adaptive_half
//   P=768 Q=512 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q512_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um1_un1_simd1_equal_split
//   P=768 Q=512 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q512_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um1_un4_simd1_single_block
//   P=768 Q=512 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p768_q512_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um1_un4_simd1_fixed_q
//   P=768 Q=512 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p768_q512_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um1_un4_simd1_adaptive_half
//   P=768 Q=512 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p768_q512_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um1_un4_simd1_equal_split
//   P=768 Q=512 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p768_q512_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um4_un1_simd1_single_block
//   P=768 Q=512 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q512_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um4_un1_simd1_fixed_q
//   P=768 Q=512 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q512_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um4_un1_simd1_adaptive_half
//   P=768 Q=512 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q512_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um4_un1_simd1_equal_split
//   P=768 Q=512 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q512_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um4_un1_simd4_single_block
//   P=768 Q=512 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q512_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um4_un1_simd4_fixed_q
//   P=768 Q=512 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q512_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um4_un1_simd4_adaptive_half
//   P=768 Q=512 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q512_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um4_un1_simd4_equal_split
//   P=768 Q=512 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q512_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um4_un4_simd4_single_block
//   P=768 Q=512 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p768_q512_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um4_un4_simd4_fixed_q
//   P=768 Q=512 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p768_q512_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um4_un4_simd4_adaptive_half
//   P=768 Q=512 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p768_q512_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um4_un4_simd4_equal_split
//   P=768 Q=512 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p768_q512_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um8_un1_simd1_single_block
//   P=768 Q=512 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p768_q512_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um8_un1_simd1_fixed_q
//   P=768 Q=512 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p768_q512_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um8_un1_simd1_adaptive_half
//   P=768 Q=512 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p768_q512_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um8_un1_simd1_equal_split
//   P=768 Q=512 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p768_q512_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um8_un1_simd4_single_block
//   P=768 Q=512 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p768_q512_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um8_un1_simd4_fixed_q
//   P=768 Q=512 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p768_q512_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um8_un1_simd4_adaptive_half
//   P=768 Q=512 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p768_q512_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um8_un1_simd4_equal_split
//   P=768 Q=512 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p768_q512_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um8_un1_simd8_single_block
//   P=768 Q=512 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p768_q512_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um8_un1_simd8_fixed_q
//   P=768 Q=512 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p768_q512_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um8_un1_simd8_adaptive_half
//   P=768 Q=512 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p768_q512_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um8_un1_simd8_equal_split
//   P=768 Q=512 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p768_q512_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um8_un4_simd4_single_block
//   P=768 Q=512 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p768_q512_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um8_un4_simd4_fixed_q
//   P=768 Q=512 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p768_q512_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um8_un4_simd4_adaptive_half
//   P=768 Q=512 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p768_q512_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um8_un4_simd4_equal_split
//   P=768 Q=512 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p768_q512_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um8_un4_simd8_single_block
//   P=768 Q=512 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p768_q512_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um8_un4_simd8_fixed_q
//   P=768 Q=512 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p768_q512_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um8_un4_simd8_adaptive_half
//   P=768 Q=512 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p768_q512_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p768_q512_um8_un4_simd8_equal_split
//   P=768 Q=512 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p768_q512_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um1_un1_simd1_single_block
//   P=1024 Q=1 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q1_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um1_un1_simd1_fixed_q
//   P=1024 Q=1 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q1_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um1_un1_simd1_adaptive_half
//   P=1024 Q=1 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q1_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um1_un1_simd1_equal_split
//   P=1024 Q=1 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q1_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um1_un4_simd1_single_block
//   P=1024 Q=1 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p1024_q1_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um1_un4_simd1_fixed_q
//   P=1024 Q=1 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p1024_q1_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um1_un4_simd1_adaptive_half
//   P=1024 Q=1 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p1024_q1_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um1_un4_simd1_equal_split
//   P=1024 Q=1 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p1024_q1_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um4_un1_simd1_single_block
//   P=1024 Q=1 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q1_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um4_un1_simd1_fixed_q
//   P=1024 Q=1 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q1_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um4_un1_simd1_adaptive_half
//   P=1024 Q=1 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q1_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um4_un1_simd1_equal_split
//   P=1024 Q=1 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q1_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um4_un1_simd4_single_block
//   P=1024 Q=1 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q1_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um4_un1_simd4_fixed_q
//   P=1024 Q=1 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q1_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um4_un1_simd4_adaptive_half
//   P=1024 Q=1 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q1_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um4_un1_simd4_equal_split
//   P=1024 Q=1 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q1_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um4_un4_simd4_single_block
//   P=1024 Q=1 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p1024_q1_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um4_un4_simd4_fixed_q
//   P=1024 Q=1 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p1024_q1_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um4_un4_simd4_adaptive_half
//   P=1024 Q=1 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p1024_q1_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um4_un4_simd4_equal_split
//   P=1024 Q=1 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p1024_q1_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um8_un1_simd1_single_block
//   P=1024 Q=1 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q1_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um8_un1_simd1_fixed_q
//   P=1024 Q=1 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q1_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um8_un1_simd1_adaptive_half
//   P=1024 Q=1 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q1_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um8_un1_simd1_equal_split
//   P=1024 Q=1 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q1_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um8_un1_simd4_single_block
//   P=1024 Q=1 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q1_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um8_un1_simd4_fixed_q
//   P=1024 Q=1 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q1_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um8_un1_simd4_adaptive_half
//   P=1024 Q=1 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q1_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um8_un1_simd4_equal_split
//   P=1024 Q=1 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q1_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um8_un1_simd8_single_block
//   P=1024 Q=1 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p1024_q1_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um8_un1_simd8_fixed_q
//   P=1024 Q=1 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p1024_q1_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um8_un1_simd8_adaptive_half
//   P=1024 Q=1 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p1024_q1_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um8_un1_simd8_equal_split
//   P=1024 Q=1 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p1024_q1_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um8_un4_simd4_single_block
//   P=1024 Q=1 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p1024_q1_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um8_un4_simd4_fixed_q
//   P=1024 Q=1 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p1024_q1_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um8_un4_simd4_adaptive_half
//   P=1024 Q=1 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p1024_q1_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um8_un4_simd4_equal_split
//   P=1024 Q=1 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p1024_q1_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um8_un4_simd8_single_block
//   P=1024 Q=1 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p1024_q1_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um8_un4_simd8_fixed_q
//   P=1024 Q=1 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p1024_q1_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um8_un4_simd8_adaptive_half
//   P=1024 Q=1 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p1024_q1_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um8_un4_simd8_equal_split
//   P=1024 Q=1 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p1024_q1_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um16_un1_simd4_single_block
//   P=1024 Q=1 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q1_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um16_un1_simd4_fixed_q
//   P=1024 Q=1 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q1_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um16_un1_simd4_adaptive_half
//   P=1024 Q=1 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q1_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um16_un1_simd4_equal_split
//   P=1024 Q=1 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q1_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um16_un1_simd8_single_block
//   P=1024 Q=1 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p1024_q1_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um16_un1_simd8_fixed_q
//   P=1024 Q=1 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p1024_q1_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um16_un1_simd8_adaptive_half
//   P=1024 Q=1 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p1024_q1_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um16_un1_simd8_equal_split
//   P=1024 Q=1 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p1024_q1_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um16_un1_simd16_single_block
//   P=1024 Q=1 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p1024_q1_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um16_un1_simd16_fixed_q
//   P=1024 Q=1 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p1024_q1_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um16_un1_simd16_adaptive_half
//   P=1024 Q=1 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p1024_q1_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um16_un1_simd16_equal_split
//   P=1024 Q=1 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p1024_q1_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um16_un4_simd8_single_block
//   P=1024 Q=1 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p1024_q1_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um16_un4_simd8_fixed_q
//   P=1024 Q=1 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p1024_q1_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um16_un4_simd8_adaptive_half
//   P=1024 Q=1 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p1024_q1_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um16_un4_simd8_equal_split
//   P=1024 Q=1 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p1024_q1_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um16_un4_simd16_single_block
//   P=1024 Q=1 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p1024_q1_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um16_un4_simd16_fixed_q
//   P=1024 Q=1 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p1024_q1_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um16_un4_simd16_adaptive_half
//   P=1024 Q=1 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p1024_q1_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um16_un4_simd16_equal_split
//   P=1024 Q=1 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p1024_q1_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um32_un1_simd4_single_block
//   P=1024 Q=1 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q1_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um32_un1_simd4_fixed_q
//   P=1024 Q=1 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q1_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um32_un1_simd4_adaptive_half
//   P=1024 Q=1 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q1_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um32_un1_simd4_equal_split
//   P=1024 Q=1 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q1_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um32_un1_simd8_single_block
//   P=1024 Q=1 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p1024_q1_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um32_un1_simd8_fixed_q
//   P=1024 Q=1 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p1024_q1_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um32_un1_simd8_adaptive_half
//   P=1024 Q=1 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p1024_q1_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um32_un1_simd8_equal_split
//   P=1024 Q=1 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p1024_q1_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um32_un1_simd16_single_block
//   P=1024 Q=1 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p1024_q1_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um32_un1_simd16_fixed_q
//   P=1024 Q=1 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p1024_q1_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um32_un1_simd16_adaptive_half
//   P=1024 Q=1 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p1024_q1_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um32_un1_simd16_equal_split
//   P=1024 Q=1 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p1024_q1_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um32_un4_simd16_single_block
//   P=1024 Q=1 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p1024_q1_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um32_un4_simd16_fixed_q
//   P=1024 Q=1 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p1024_q1_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 1 ? 1 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um32_un4_simd16_adaptive_half
//   P=1024 Q=1 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p1024_q1_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 1) { min_l = 1; }
        else if (rem > 1) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q1_um32_un4_simd16_equal_split
//   P=1024 Q=1 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p1024_q1_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 1 - 1) / 1;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um1_un1_simd1_single_block
//   P=1024 Q=64 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q64_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um1_un1_simd1_fixed_q
//   P=1024 Q=64 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q64_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um1_un1_simd1_adaptive_half
//   P=1024 Q=64 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q64_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um1_un1_simd1_equal_split
//   P=1024 Q=64 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q64_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um1_un4_simd1_single_block
//   P=1024 Q=64 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p1024_q64_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um1_un4_simd1_fixed_q
//   P=1024 Q=64 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p1024_q64_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um1_un4_simd1_adaptive_half
//   P=1024 Q=64 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p1024_q64_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um1_un4_simd1_equal_split
//   P=1024 Q=64 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p1024_q64_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um4_un1_simd1_single_block
//   P=1024 Q=64 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q64_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um4_un1_simd1_fixed_q
//   P=1024 Q=64 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q64_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um4_un1_simd1_adaptive_half
//   P=1024 Q=64 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q64_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um4_un1_simd1_equal_split
//   P=1024 Q=64 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q64_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um4_un1_simd4_single_block
//   P=1024 Q=64 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q64_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um4_un1_simd4_fixed_q
//   P=1024 Q=64 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q64_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um4_un1_simd4_adaptive_half
//   P=1024 Q=64 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q64_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um4_un1_simd4_equal_split
//   P=1024 Q=64 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q64_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um4_un4_simd4_single_block
//   P=1024 Q=64 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p1024_q64_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um4_un4_simd4_fixed_q
//   P=1024 Q=64 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p1024_q64_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um4_un4_simd4_adaptive_half
//   P=1024 Q=64 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p1024_q64_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um4_un4_simd4_equal_split
//   P=1024 Q=64 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p1024_q64_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um8_un1_simd1_single_block
//   P=1024 Q=64 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q64_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um8_un1_simd1_fixed_q
//   P=1024 Q=64 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q64_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um8_un1_simd1_adaptive_half
//   P=1024 Q=64 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q64_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um8_un1_simd1_equal_split
//   P=1024 Q=64 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q64_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um8_un1_simd4_single_block
//   P=1024 Q=64 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q64_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um8_un1_simd4_fixed_q
//   P=1024 Q=64 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q64_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um8_un1_simd4_adaptive_half
//   P=1024 Q=64 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q64_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um8_un1_simd4_equal_split
//   P=1024 Q=64 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q64_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um8_un1_simd8_single_block
//   P=1024 Q=64 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p1024_q64_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um8_un1_simd8_fixed_q
//   P=1024 Q=64 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p1024_q64_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um8_un1_simd8_adaptive_half
//   P=1024 Q=64 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p1024_q64_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um8_un1_simd8_equal_split
//   P=1024 Q=64 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p1024_q64_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um8_un4_simd4_single_block
//   P=1024 Q=64 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p1024_q64_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um8_un4_simd4_fixed_q
//   P=1024 Q=64 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p1024_q64_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um8_un4_simd4_adaptive_half
//   P=1024 Q=64 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p1024_q64_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um8_un4_simd4_equal_split
//   P=1024 Q=64 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p1024_q64_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um8_un4_simd8_single_block
//   P=1024 Q=64 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p1024_q64_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um8_un4_simd8_fixed_q
//   P=1024 Q=64 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p1024_q64_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um8_un4_simd8_adaptive_half
//   P=1024 Q=64 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p1024_q64_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um8_un4_simd8_equal_split
//   P=1024 Q=64 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p1024_q64_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um16_un1_simd4_single_block
//   P=1024 Q=64 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q64_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um16_un1_simd4_fixed_q
//   P=1024 Q=64 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q64_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um16_un1_simd4_adaptive_half
//   P=1024 Q=64 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q64_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um16_un1_simd4_equal_split
//   P=1024 Q=64 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q64_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um16_un1_simd8_single_block
//   P=1024 Q=64 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p1024_q64_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um16_un1_simd8_fixed_q
//   P=1024 Q=64 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p1024_q64_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um16_un1_simd8_adaptive_half
//   P=1024 Q=64 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p1024_q64_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um16_un1_simd8_equal_split
//   P=1024 Q=64 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p1024_q64_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um16_un1_simd16_single_block
//   P=1024 Q=64 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p1024_q64_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um16_un1_simd16_fixed_q
//   P=1024 Q=64 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p1024_q64_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um16_un1_simd16_adaptive_half
//   P=1024 Q=64 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p1024_q64_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um16_un1_simd16_equal_split
//   P=1024 Q=64 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p1024_q64_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um16_un4_simd8_single_block
//   P=1024 Q=64 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p1024_q64_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um16_un4_simd8_fixed_q
//   P=1024 Q=64 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p1024_q64_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um16_un4_simd8_adaptive_half
//   P=1024 Q=64 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p1024_q64_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um16_un4_simd8_equal_split
//   P=1024 Q=64 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p1024_q64_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um16_un4_simd16_single_block
//   P=1024 Q=64 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p1024_q64_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um16_un4_simd16_fixed_q
//   P=1024 Q=64 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p1024_q64_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um16_un4_simd16_adaptive_half
//   P=1024 Q=64 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p1024_q64_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um16_un4_simd16_equal_split
//   P=1024 Q=64 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p1024_q64_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um32_un1_simd4_single_block
//   P=1024 Q=64 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q64_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um32_un1_simd4_fixed_q
//   P=1024 Q=64 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q64_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um32_un1_simd4_adaptive_half
//   P=1024 Q=64 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q64_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um32_un1_simd4_equal_split
//   P=1024 Q=64 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q64_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um32_un1_simd8_single_block
//   P=1024 Q=64 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p1024_q64_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um32_un1_simd8_fixed_q
//   P=1024 Q=64 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p1024_q64_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um32_un1_simd8_adaptive_half
//   P=1024 Q=64 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p1024_q64_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um32_un1_simd8_equal_split
//   P=1024 Q=64 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p1024_q64_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um32_un1_simd16_single_block
//   P=1024 Q=64 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p1024_q64_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um32_un1_simd16_fixed_q
//   P=1024 Q=64 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p1024_q64_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um32_un1_simd16_adaptive_half
//   P=1024 Q=64 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p1024_q64_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um32_un1_simd16_equal_split
//   P=1024 Q=64 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p1024_q64_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um32_un4_simd16_single_block
//   P=1024 Q=64 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p1024_q64_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um32_un4_simd16_fixed_q
//   P=1024 Q=64 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p1024_q64_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 64 ? 64 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um32_un4_simd16_adaptive_half
//   P=1024 Q=64 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p1024_q64_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 64) { min_l = 64; }
        else if (rem > 64) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q64_um32_un4_simd16_equal_split
//   P=1024 Q=64 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p1024_q64_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 64 - 1) / 64;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um1_un1_simd1_single_block
//   P=1024 Q=128 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q128_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um1_un1_simd1_fixed_q
//   P=1024 Q=128 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q128_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um1_un1_simd1_adaptive_half
//   P=1024 Q=128 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q128_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um1_un1_simd1_equal_split
//   P=1024 Q=128 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q128_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um1_un4_simd1_single_block
//   P=1024 Q=128 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p1024_q128_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um1_un4_simd1_fixed_q
//   P=1024 Q=128 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p1024_q128_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um1_un4_simd1_adaptive_half
//   P=1024 Q=128 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p1024_q128_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um1_un4_simd1_equal_split
//   P=1024 Q=128 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p1024_q128_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um4_un1_simd1_single_block
//   P=1024 Q=128 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q128_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um4_un1_simd1_fixed_q
//   P=1024 Q=128 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q128_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um4_un1_simd1_adaptive_half
//   P=1024 Q=128 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q128_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um4_un1_simd1_equal_split
//   P=1024 Q=128 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q128_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um4_un1_simd4_single_block
//   P=1024 Q=128 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q128_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um4_un1_simd4_fixed_q
//   P=1024 Q=128 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q128_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um4_un1_simd4_adaptive_half
//   P=1024 Q=128 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q128_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um4_un1_simd4_equal_split
//   P=1024 Q=128 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q128_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um4_un4_simd4_single_block
//   P=1024 Q=128 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p1024_q128_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um4_un4_simd4_fixed_q
//   P=1024 Q=128 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p1024_q128_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um4_un4_simd4_adaptive_half
//   P=1024 Q=128 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p1024_q128_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um4_un4_simd4_equal_split
//   P=1024 Q=128 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p1024_q128_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um8_un1_simd1_single_block
//   P=1024 Q=128 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q128_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um8_un1_simd1_fixed_q
//   P=1024 Q=128 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q128_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um8_un1_simd1_adaptive_half
//   P=1024 Q=128 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q128_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um8_un1_simd1_equal_split
//   P=1024 Q=128 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q128_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um8_un1_simd4_single_block
//   P=1024 Q=128 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q128_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um8_un1_simd4_fixed_q
//   P=1024 Q=128 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q128_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um8_un1_simd4_adaptive_half
//   P=1024 Q=128 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q128_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um8_un1_simd4_equal_split
//   P=1024 Q=128 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q128_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um8_un1_simd8_single_block
//   P=1024 Q=128 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p1024_q128_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um8_un1_simd8_fixed_q
//   P=1024 Q=128 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p1024_q128_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um8_un1_simd8_adaptive_half
//   P=1024 Q=128 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p1024_q128_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um8_un1_simd8_equal_split
//   P=1024 Q=128 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p1024_q128_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um8_un4_simd4_single_block
//   P=1024 Q=128 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p1024_q128_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um8_un4_simd4_fixed_q
//   P=1024 Q=128 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p1024_q128_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um8_un4_simd4_adaptive_half
//   P=1024 Q=128 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p1024_q128_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um8_un4_simd4_equal_split
//   P=1024 Q=128 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p1024_q128_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um8_un4_simd8_single_block
//   P=1024 Q=128 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p1024_q128_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um8_un4_simd8_fixed_q
//   P=1024 Q=128 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p1024_q128_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um8_un4_simd8_adaptive_half
//   P=1024 Q=128 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p1024_q128_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um8_un4_simd8_equal_split
//   P=1024 Q=128 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p1024_q128_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um16_un1_simd4_single_block
//   P=1024 Q=128 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q128_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um16_un1_simd4_fixed_q
//   P=1024 Q=128 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q128_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um16_un1_simd4_adaptive_half
//   P=1024 Q=128 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q128_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um16_un1_simd4_equal_split
//   P=1024 Q=128 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q128_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um16_un1_simd8_single_block
//   P=1024 Q=128 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p1024_q128_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um16_un1_simd8_fixed_q
//   P=1024 Q=128 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p1024_q128_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um16_un1_simd8_adaptive_half
//   P=1024 Q=128 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p1024_q128_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um16_un1_simd8_equal_split
//   P=1024 Q=128 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p1024_q128_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um16_un1_simd16_single_block
//   P=1024 Q=128 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p1024_q128_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um16_un1_simd16_fixed_q
//   P=1024 Q=128 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p1024_q128_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um16_un1_simd16_adaptive_half
//   P=1024 Q=128 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p1024_q128_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um16_un1_simd16_equal_split
//   P=1024 Q=128 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p1024_q128_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um16_un4_simd8_single_block
//   P=1024 Q=128 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p1024_q128_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um16_un4_simd8_fixed_q
//   P=1024 Q=128 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p1024_q128_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um16_un4_simd8_adaptive_half
//   P=1024 Q=128 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p1024_q128_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um16_un4_simd8_equal_split
//   P=1024 Q=128 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p1024_q128_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um16_un4_simd16_single_block
//   P=1024 Q=128 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p1024_q128_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um16_un4_simd16_fixed_q
//   P=1024 Q=128 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p1024_q128_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um16_un4_simd16_adaptive_half
//   P=1024 Q=128 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p1024_q128_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um16_un4_simd16_equal_split
//   P=1024 Q=128 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p1024_q128_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um32_un1_simd4_single_block
//   P=1024 Q=128 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q128_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um32_un1_simd4_fixed_q
//   P=1024 Q=128 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q128_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um32_un1_simd4_adaptive_half
//   P=1024 Q=128 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q128_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um32_un1_simd4_equal_split
//   P=1024 Q=128 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q128_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um32_un1_simd8_single_block
//   P=1024 Q=128 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p1024_q128_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um32_un1_simd8_fixed_q
//   P=1024 Q=128 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p1024_q128_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um32_un1_simd8_adaptive_half
//   P=1024 Q=128 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p1024_q128_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um32_un1_simd8_equal_split
//   P=1024 Q=128 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p1024_q128_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um32_un1_simd16_single_block
//   P=1024 Q=128 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p1024_q128_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um32_un1_simd16_fixed_q
//   P=1024 Q=128 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p1024_q128_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um32_un1_simd16_adaptive_half
//   P=1024 Q=128 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p1024_q128_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um32_un1_simd16_equal_split
//   P=1024 Q=128 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p1024_q128_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um32_un4_simd16_single_block
//   P=1024 Q=128 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p1024_q128_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um32_un4_simd16_fixed_q
//   P=1024 Q=128 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p1024_q128_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 128 ? 128 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um32_un4_simd16_adaptive_half
//   P=1024 Q=128 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p1024_q128_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 128) { min_l = 128; }
        else if (rem > 128) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q128_um32_un4_simd16_equal_split
//   P=1024 Q=128 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p1024_q128_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 128 - 1) / 128;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um1_un1_simd1_single_block
//   P=1024 Q=192 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q192_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um1_un1_simd1_fixed_q
//   P=1024 Q=192 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q192_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um1_un1_simd1_adaptive_half
//   P=1024 Q=192 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q192_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um1_un1_simd1_equal_split
//   P=1024 Q=192 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q192_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um1_un4_simd1_single_block
//   P=1024 Q=192 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p1024_q192_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um1_un4_simd1_fixed_q
//   P=1024 Q=192 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p1024_q192_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um1_un4_simd1_adaptive_half
//   P=1024 Q=192 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p1024_q192_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um1_un4_simd1_equal_split
//   P=1024 Q=192 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p1024_q192_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um4_un1_simd1_single_block
//   P=1024 Q=192 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q192_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um4_un1_simd1_fixed_q
//   P=1024 Q=192 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q192_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um4_un1_simd1_adaptive_half
//   P=1024 Q=192 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q192_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um4_un1_simd1_equal_split
//   P=1024 Q=192 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q192_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um4_un1_simd4_single_block
//   P=1024 Q=192 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q192_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um4_un1_simd4_fixed_q
//   P=1024 Q=192 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q192_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um4_un1_simd4_adaptive_half
//   P=1024 Q=192 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q192_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um4_un1_simd4_equal_split
//   P=1024 Q=192 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q192_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um4_un4_simd4_single_block
//   P=1024 Q=192 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p1024_q192_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um4_un4_simd4_fixed_q
//   P=1024 Q=192 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p1024_q192_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um4_un4_simd4_adaptive_half
//   P=1024 Q=192 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p1024_q192_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um4_un4_simd4_equal_split
//   P=1024 Q=192 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p1024_q192_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um8_un1_simd1_single_block
//   P=1024 Q=192 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q192_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um8_un1_simd1_fixed_q
//   P=1024 Q=192 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q192_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um8_un1_simd1_adaptive_half
//   P=1024 Q=192 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q192_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um8_un1_simd1_equal_split
//   P=1024 Q=192 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q192_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um8_un1_simd4_single_block
//   P=1024 Q=192 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q192_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um8_un1_simd4_fixed_q
//   P=1024 Q=192 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q192_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um8_un1_simd4_adaptive_half
//   P=1024 Q=192 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q192_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um8_un1_simd4_equal_split
//   P=1024 Q=192 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q192_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um8_un1_simd8_single_block
//   P=1024 Q=192 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p1024_q192_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um8_un1_simd8_fixed_q
//   P=1024 Q=192 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p1024_q192_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um8_un1_simd8_adaptive_half
//   P=1024 Q=192 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p1024_q192_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um8_un1_simd8_equal_split
//   P=1024 Q=192 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p1024_q192_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um8_un4_simd4_single_block
//   P=1024 Q=192 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p1024_q192_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um8_un4_simd4_fixed_q
//   P=1024 Q=192 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p1024_q192_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um8_un4_simd4_adaptive_half
//   P=1024 Q=192 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p1024_q192_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um8_un4_simd4_equal_split
//   P=1024 Q=192 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p1024_q192_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um8_un4_simd8_single_block
//   P=1024 Q=192 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p1024_q192_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um8_un4_simd8_fixed_q
//   P=1024 Q=192 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p1024_q192_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um8_un4_simd8_adaptive_half
//   P=1024 Q=192 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p1024_q192_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um8_un4_simd8_equal_split
//   P=1024 Q=192 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p1024_q192_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um16_un1_simd4_single_block
//   P=1024 Q=192 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q192_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um16_un1_simd4_fixed_q
//   P=1024 Q=192 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q192_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um16_un1_simd4_adaptive_half
//   P=1024 Q=192 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q192_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um16_un1_simd4_equal_split
//   P=1024 Q=192 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q192_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um16_un1_simd8_single_block
//   P=1024 Q=192 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p1024_q192_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um16_un1_simd8_fixed_q
//   P=1024 Q=192 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p1024_q192_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um16_un1_simd8_adaptive_half
//   P=1024 Q=192 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p1024_q192_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um16_un1_simd8_equal_split
//   P=1024 Q=192 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p1024_q192_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um16_un1_simd16_single_block
//   P=1024 Q=192 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p1024_q192_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um16_un1_simd16_fixed_q
//   P=1024 Q=192 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p1024_q192_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um16_un1_simd16_adaptive_half
//   P=1024 Q=192 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p1024_q192_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um16_un1_simd16_equal_split
//   P=1024 Q=192 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p1024_q192_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um16_un4_simd8_single_block
//   P=1024 Q=192 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p1024_q192_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um16_un4_simd8_fixed_q
//   P=1024 Q=192 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p1024_q192_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um16_un4_simd8_adaptive_half
//   P=1024 Q=192 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p1024_q192_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um16_un4_simd8_equal_split
//   P=1024 Q=192 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p1024_q192_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um16_un4_simd16_single_block
//   P=1024 Q=192 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p1024_q192_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um16_un4_simd16_fixed_q
//   P=1024 Q=192 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p1024_q192_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um16_un4_simd16_adaptive_half
//   P=1024 Q=192 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p1024_q192_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um16_un4_simd16_equal_split
//   P=1024 Q=192 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p1024_q192_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um32_un1_simd4_single_block
//   P=1024 Q=192 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q192_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um32_un1_simd4_fixed_q
//   P=1024 Q=192 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q192_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um32_un1_simd4_adaptive_half
//   P=1024 Q=192 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q192_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um32_un1_simd4_equal_split
//   P=1024 Q=192 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q192_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um32_un1_simd8_single_block
//   P=1024 Q=192 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p1024_q192_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um32_un1_simd8_fixed_q
//   P=1024 Q=192 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p1024_q192_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um32_un1_simd8_adaptive_half
//   P=1024 Q=192 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p1024_q192_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um32_un1_simd8_equal_split
//   P=1024 Q=192 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p1024_q192_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um32_un1_simd16_single_block
//   P=1024 Q=192 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p1024_q192_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um32_un1_simd16_fixed_q
//   P=1024 Q=192 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p1024_q192_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um32_un1_simd16_adaptive_half
//   P=1024 Q=192 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p1024_q192_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um32_un1_simd16_equal_split
//   P=1024 Q=192 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p1024_q192_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um32_un4_simd16_single_block
//   P=1024 Q=192 UM=32 UN=4 SIMD=16 KRule=single_block
void gemm_p1024_q192_um32_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um32_un4_simd16_fixed_q
//   P=1024 Q=192 UM=32 UN=4 SIMD=16 KRule=fixed_q
void gemm_p1024_q192_um32_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 192 ? 192 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um32_un4_simd16_adaptive_half
//   P=1024 Q=192 UM=32 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p1024_q192_um32_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 192) { min_l = 192; }
        else if (rem > 192) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q192_um32_un4_simd16_equal_split
//   P=1024 Q=192 UM=32 UN=4 SIMD=16 KRule=equal_split
void gemm_p1024_q192_um32_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 192 - 1) / 192;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um1_un1_simd1_single_block
//   P=1024 Q=240 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q240_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um1_un1_simd1_fixed_q
//   P=1024 Q=240 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q240_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um1_un1_simd1_adaptive_half
//   P=1024 Q=240 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q240_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um1_un1_simd1_equal_split
//   P=1024 Q=240 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q240_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um1_un4_simd1_single_block
//   P=1024 Q=240 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p1024_q240_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um1_un4_simd1_fixed_q
//   P=1024 Q=240 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p1024_q240_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um1_un4_simd1_adaptive_half
//   P=1024 Q=240 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p1024_q240_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um1_un4_simd1_equal_split
//   P=1024 Q=240 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p1024_q240_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um4_un1_simd1_single_block
//   P=1024 Q=240 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q240_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um4_un1_simd1_fixed_q
//   P=1024 Q=240 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q240_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um4_un1_simd1_adaptive_half
//   P=1024 Q=240 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q240_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um4_un1_simd1_equal_split
//   P=1024 Q=240 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q240_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um4_un1_simd4_single_block
//   P=1024 Q=240 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q240_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um4_un1_simd4_fixed_q
//   P=1024 Q=240 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q240_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um4_un1_simd4_adaptive_half
//   P=1024 Q=240 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q240_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um4_un1_simd4_equal_split
//   P=1024 Q=240 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q240_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um4_un4_simd4_single_block
//   P=1024 Q=240 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p1024_q240_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um4_un4_simd4_fixed_q
//   P=1024 Q=240 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p1024_q240_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um4_un4_simd4_adaptive_half
//   P=1024 Q=240 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p1024_q240_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um4_un4_simd4_equal_split
//   P=1024 Q=240 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p1024_q240_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um8_un1_simd1_single_block
//   P=1024 Q=240 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q240_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um8_un1_simd1_fixed_q
//   P=1024 Q=240 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q240_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um8_un1_simd1_adaptive_half
//   P=1024 Q=240 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q240_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um8_un1_simd1_equal_split
//   P=1024 Q=240 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q240_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um8_un1_simd4_single_block
//   P=1024 Q=240 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q240_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um8_un1_simd4_fixed_q
//   P=1024 Q=240 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q240_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um8_un1_simd4_adaptive_half
//   P=1024 Q=240 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q240_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um8_un1_simd4_equal_split
//   P=1024 Q=240 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q240_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um8_un1_simd8_single_block
//   P=1024 Q=240 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p1024_q240_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um8_un1_simd8_fixed_q
//   P=1024 Q=240 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p1024_q240_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um8_un1_simd8_adaptive_half
//   P=1024 Q=240 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p1024_q240_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um8_un1_simd8_equal_split
//   P=1024 Q=240 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p1024_q240_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um8_un4_simd4_single_block
//   P=1024 Q=240 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p1024_q240_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um8_un4_simd4_fixed_q
//   P=1024 Q=240 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p1024_q240_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um8_un4_simd4_adaptive_half
//   P=1024 Q=240 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p1024_q240_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um8_un4_simd4_equal_split
//   P=1024 Q=240 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p1024_q240_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um8_un4_simd8_single_block
//   P=1024 Q=240 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p1024_q240_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um8_un4_simd8_fixed_q
//   P=1024 Q=240 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p1024_q240_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um8_un4_simd8_adaptive_half
//   P=1024 Q=240 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p1024_q240_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um8_un4_simd8_equal_split
//   P=1024 Q=240 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p1024_q240_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um16_un1_simd4_single_block
//   P=1024 Q=240 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q240_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um16_un1_simd4_fixed_q
//   P=1024 Q=240 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q240_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um16_un1_simd4_adaptive_half
//   P=1024 Q=240 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q240_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um16_un1_simd4_equal_split
//   P=1024 Q=240 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q240_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um16_un1_simd8_single_block
//   P=1024 Q=240 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p1024_q240_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um16_un1_simd8_fixed_q
//   P=1024 Q=240 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p1024_q240_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um16_un1_simd8_adaptive_half
//   P=1024 Q=240 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p1024_q240_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um16_un1_simd8_equal_split
//   P=1024 Q=240 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p1024_q240_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um16_un1_simd16_single_block
//   P=1024 Q=240 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p1024_q240_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um16_un1_simd16_fixed_q
//   P=1024 Q=240 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p1024_q240_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um16_un1_simd16_adaptive_half
//   P=1024 Q=240 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p1024_q240_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um16_un1_simd16_equal_split
//   P=1024 Q=240 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p1024_q240_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um16_un4_simd8_single_block
//   P=1024 Q=240 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p1024_q240_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um16_un4_simd8_fixed_q
//   P=1024 Q=240 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p1024_q240_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um16_un4_simd8_adaptive_half
//   P=1024 Q=240 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p1024_q240_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um16_un4_simd8_equal_split
//   P=1024 Q=240 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p1024_q240_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um16_un4_simd16_single_block
//   P=1024 Q=240 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p1024_q240_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um16_un4_simd16_fixed_q
//   P=1024 Q=240 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p1024_q240_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um16_un4_simd16_adaptive_half
//   P=1024 Q=240 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p1024_q240_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um16_un4_simd16_equal_split
//   P=1024 Q=240 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p1024_q240_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um32_un1_simd4_single_block
//   P=1024 Q=240 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q240_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um32_un1_simd4_fixed_q
//   P=1024 Q=240 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q240_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um32_un1_simd4_adaptive_half
//   P=1024 Q=240 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q240_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um32_un1_simd4_equal_split
//   P=1024 Q=240 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q240_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um32_un1_simd8_single_block
//   P=1024 Q=240 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p1024_q240_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um32_un1_simd8_fixed_q
//   P=1024 Q=240 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p1024_q240_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um32_un1_simd8_adaptive_half
//   P=1024 Q=240 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p1024_q240_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um32_un1_simd8_equal_split
//   P=1024 Q=240 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p1024_q240_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um32_un1_simd16_single_block
//   P=1024 Q=240 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p1024_q240_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um32_un1_simd16_fixed_q
//   P=1024 Q=240 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p1024_q240_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 240 ? 240 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um32_un1_simd16_adaptive_half
//   P=1024 Q=240 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p1024_q240_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 240) { min_l = 240; }
        else if (rem > 240) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q240_um32_un1_simd16_equal_split
//   P=1024 Q=240 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p1024_q240_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 240 - 1) / 240;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um1_un1_simd1_single_block
//   P=1024 Q=248 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q248_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um1_un1_simd1_fixed_q
//   P=1024 Q=248 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q248_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um1_un1_simd1_adaptive_half
//   P=1024 Q=248 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q248_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um1_un1_simd1_equal_split
//   P=1024 Q=248 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q248_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um1_un4_simd1_single_block
//   P=1024 Q=248 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p1024_q248_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um1_un4_simd1_fixed_q
//   P=1024 Q=248 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p1024_q248_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um1_un4_simd1_adaptive_half
//   P=1024 Q=248 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p1024_q248_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um1_un4_simd1_equal_split
//   P=1024 Q=248 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p1024_q248_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um4_un1_simd1_single_block
//   P=1024 Q=248 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q248_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um4_un1_simd1_fixed_q
//   P=1024 Q=248 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q248_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um4_un1_simd1_adaptive_half
//   P=1024 Q=248 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q248_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um4_un1_simd1_equal_split
//   P=1024 Q=248 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q248_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um4_un1_simd4_single_block
//   P=1024 Q=248 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q248_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um4_un1_simd4_fixed_q
//   P=1024 Q=248 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q248_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um4_un1_simd4_adaptive_half
//   P=1024 Q=248 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q248_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um4_un1_simd4_equal_split
//   P=1024 Q=248 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q248_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um4_un4_simd4_single_block
//   P=1024 Q=248 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p1024_q248_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um4_un4_simd4_fixed_q
//   P=1024 Q=248 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p1024_q248_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um4_un4_simd4_adaptive_half
//   P=1024 Q=248 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p1024_q248_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um4_un4_simd4_equal_split
//   P=1024 Q=248 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p1024_q248_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um8_un1_simd1_single_block
//   P=1024 Q=248 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q248_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um8_un1_simd1_fixed_q
//   P=1024 Q=248 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q248_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um8_un1_simd1_adaptive_half
//   P=1024 Q=248 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q248_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um8_un1_simd1_equal_split
//   P=1024 Q=248 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q248_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um8_un1_simd4_single_block
//   P=1024 Q=248 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q248_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um8_un1_simd4_fixed_q
//   P=1024 Q=248 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q248_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um8_un1_simd4_adaptive_half
//   P=1024 Q=248 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q248_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um8_un1_simd4_equal_split
//   P=1024 Q=248 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q248_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um8_un1_simd8_single_block
//   P=1024 Q=248 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p1024_q248_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um8_un1_simd8_fixed_q
//   P=1024 Q=248 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p1024_q248_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um8_un1_simd8_adaptive_half
//   P=1024 Q=248 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p1024_q248_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um8_un1_simd8_equal_split
//   P=1024 Q=248 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p1024_q248_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um8_un4_simd4_single_block
//   P=1024 Q=248 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p1024_q248_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um8_un4_simd4_fixed_q
//   P=1024 Q=248 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p1024_q248_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um8_un4_simd4_adaptive_half
//   P=1024 Q=248 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p1024_q248_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um8_un4_simd4_equal_split
//   P=1024 Q=248 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p1024_q248_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um8_un4_simd8_single_block
//   P=1024 Q=248 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p1024_q248_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um8_un4_simd8_fixed_q
//   P=1024 Q=248 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p1024_q248_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um8_un4_simd8_adaptive_half
//   P=1024 Q=248 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p1024_q248_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um8_un4_simd8_equal_split
//   P=1024 Q=248 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p1024_q248_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um16_un1_simd4_single_block
//   P=1024 Q=248 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q248_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um16_un1_simd4_fixed_q
//   P=1024 Q=248 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q248_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um16_un1_simd4_adaptive_half
//   P=1024 Q=248 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q248_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um16_un1_simd4_equal_split
//   P=1024 Q=248 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q248_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um16_un1_simd8_single_block
//   P=1024 Q=248 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p1024_q248_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um16_un1_simd8_fixed_q
//   P=1024 Q=248 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p1024_q248_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um16_un1_simd8_adaptive_half
//   P=1024 Q=248 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p1024_q248_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um16_un1_simd8_equal_split
//   P=1024 Q=248 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p1024_q248_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um16_un1_simd16_single_block
//   P=1024 Q=248 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p1024_q248_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um16_un1_simd16_fixed_q
//   P=1024 Q=248 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p1024_q248_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um16_un1_simd16_adaptive_half
//   P=1024 Q=248 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p1024_q248_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um16_un1_simd16_equal_split
//   P=1024 Q=248 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p1024_q248_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um16_un4_simd8_single_block
//   P=1024 Q=248 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p1024_q248_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um16_un4_simd8_fixed_q
//   P=1024 Q=248 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p1024_q248_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um16_un4_simd8_adaptive_half
//   P=1024 Q=248 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p1024_q248_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um16_un4_simd8_equal_split
//   P=1024 Q=248 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p1024_q248_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um16_un4_simd16_single_block
//   P=1024 Q=248 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p1024_q248_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um16_un4_simd16_fixed_q
//   P=1024 Q=248 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p1024_q248_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um16_un4_simd16_adaptive_half
//   P=1024 Q=248 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p1024_q248_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um16_un4_simd16_equal_split
//   P=1024 Q=248 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p1024_q248_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um32_un1_simd4_single_block
//   P=1024 Q=248 UM=32 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q248_um32_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um32_un1_simd4_fixed_q
//   P=1024 Q=248 UM=32 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q248_um32_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um32_un1_simd4_adaptive_half
//   P=1024 Q=248 UM=32 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q248_um32_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um32_un1_simd4_equal_split
//   P=1024 Q=248 UM=32 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q248_um32_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um32_un1_simd8_single_block
//   P=1024 Q=248 UM=32 UN=1 SIMD=8 KRule=single_block
void gemm_p1024_q248_um32_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um32_un1_simd8_fixed_q
//   P=1024 Q=248 UM=32 UN=1 SIMD=8 KRule=fixed_q
void gemm_p1024_q248_um32_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um32_un1_simd8_adaptive_half
//   P=1024 Q=248 UM=32 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p1024_q248_um32_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um32_un1_simd8_equal_split
//   P=1024 Q=248 UM=32 UN=1 SIMD=8 KRule=equal_split
void gemm_p1024_q248_um32_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um32_un1_simd16_single_block
//   P=1024 Q=248 UM=32 UN=1 SIMD=16 KRule=single_block
void gemm_p1024_q248_um32_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um32_un1_simd16_fixed_q
//   P=1024 Q=248 UM=32 UN=1 SIMD=16 KRule=fixed_q
void gemm_p1024_q248_um32_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 248 ? 248 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um32_un1_simd16_adaptive_half
//   P=1024 Q=248 UM=32 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p1024_q248_um32_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 248) { min_l = 248; }
        else if (rem > 248) { min_l = ((rem / 2 + 32 - 1) / 32) * 32; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q248_um32_un1_simd16_equal_split
//   P=1024 Q=248 UM=32 UN=1 SIMD=16 KRule=equal_split
void gemm_p1024_q248_um32_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 248 - 1) / 248;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 32 - 1) / 32) * 32;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um1_un1_simd1_single_block
//   P=1024 Q=256 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q256_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um1_un1_simd1_fixed_q
//   P=1024 Q=256 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q256_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um1_un1_simd1_adaptive_half
//   P=1024 Q=256 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q256_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um1_un1_simd1_equal_split
//   P=1024 Q=256 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q256_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um1_un4_simd1_single_block
//   P=1024 Q=256 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p1024_q256_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um1_un4_simd1_fixed_q
//   P=1024 Q=256 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p1024_q256_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um1_un4_simd1_adaptive_half
//   P=1024 Q=256 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p1024_q256_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um1_un4_simd1_equal_split
//   P=1024 Q=256 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p1024_q256_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um4_un1_simd1_single_block
//   P=1024 Q=256 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q256_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um4_un1_simd1_fixed_q
//   P=1024 Q=256 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q256_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um4_un1_simd1_adaptive_half
//   P=1024 Q=256 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q256_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um4_un1_simd1_equal_split
//   P=1024 Q=256 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q256_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um4_un1_simd4_single_block
//   P=1024 Q=256 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q256_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um4_un1_simd4_fixed_q
//   P=1024 Q=256 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q256_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um4_un1_simd4_adaptive_half
//   P=1024 Q=256 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q256_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um4_un1_simd4_equal_split
//   P=1024 Q=256 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q256_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um4_un4_simd4_single_block
//   P=1024 Q=256 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p1024_q256_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um4_un4_simd4_fixed_q
//   P=1024 Q=256 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p1024_q256_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um4_un4_simd4_adaptive_half
//   P=1024 Q=256 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p1024_q256_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um4_un4_simd4_equal_split
//   P=1024 Q=256 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p1024_q256_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um8_un1_simd1_single_block
//   P=1024 Q=256 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q256_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um8_un1_simd1_fixed_q
//   P=1024 Q=256 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q256_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um8_un1_simd1_adaptive_half
//   P=1024 Q=256 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q256_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um8_un1_simd1_equal_split
//   P=1024 Q=256 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q256_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um8_un1_simd4_single_block
//   P=1024 Q=256 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q256_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um8_un1_simd4_fixed_q
//   P=1024 Q=256 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q256_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um8_un1_simd4_adaptive_half
//   P=1024 Q=256 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q256_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um8_un1_simd4_equal_split
//   P=1024 Q=256 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q256_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um8_un1_simd8_single_block
//   P=1024 Q=256 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p1024_q256_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um8_un1_simd8_fixed_q
//   P=1024 Q=256 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p1024_q256_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um8_un1_simd8_adaptive_half
//   P=1024 Q=256 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p1024_q256_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um8_un1_simd8_equal_split
//   P=1024 Q=256 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p1024_q256_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um8_un4_simd4_single_block
//   P=1024 Q=256 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p1024_q256_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um8_un4_simd4_fixed_q
//   P=1024 Q=256 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p1024_q256_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um8_un4_simd4_adaptive_half
//   P=1024 Q=256 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p1024_q256_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um8_un4_simd4_equal_split
//   P=1024 Q=256 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p1024_q256_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um8_un4_simd8_single_block
//   P=1024 Q=256 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p1024_q256_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um8_un4_simd8_fixed_q
//   P=1024 Q=256 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p1024_q256_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um8_un4_simd8_adaptive_half
//   P=1024 Q=256 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p1024_q256_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um8_un4_simd8_equal_split
//   P=1024 Q=256 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p1024_q256_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um16_un1_simd4_single_block
//   P=1024 Q=256 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q256_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um16_un1_simd4_fixed_q
//   P=1024 Q=256 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q256_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um16_un1_simd4_adaptive_half
//   P=1024 Q=256 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q256_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um16_un1_simd4_equal_split
//   P=1024 Q=256 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q256_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um16_un1_simd8_single_block
//   P=1024 Q=256 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p1024_q256_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um16_un1_simd8_fixed_q
//   P=1024 Q=256 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p1024_q256_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um16_un1_simd8_adaptive_half
//   P=1024 Q=256 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p1024_q256_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um16_un1_simd8_equal_split
//   P=1024 Q=256 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p1024_q256_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um16_un1_simd16_single_block
//   P=1024 Q=256 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p1024_q256_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um16_un1_simd16_fixed_q
//   P=1024 Q=256 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p1024_q256_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um16_un1_simd16_adaptive_half
//   P=1024 Q=256 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p1024_q256_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um16_un1_simd16_equal_split
//   P=1024 Q=256 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p1024_q256_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um16_un4_simd8_single_block
//   P=1024 Q=256 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p1024_q256_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um16_un4_simd8_fixed_q
//   P=1024 Q=256 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p1024_q256_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um16_un4_simd8_adaptive_half
//   P=1024 Q=256 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p1024_q256_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um16_un4_simd8_equal_split
//   P=1024 Q=256 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p1024_q256_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um16_un4_simd16_single_block
//   P=1024 Q=256 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p1024_q256_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um16_un4_simd16_fixed_q
//   P=1024 Q=256 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p1024_q256_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 256 ? 256 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um16_un4_simd16_adaptive_half
//   P=1024 Q=256 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p1024_q256_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 256) { min_l = 256; }
        else if (rem > 256) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q256_um16_un4_simd16_equal_split
//   P=1024 Q=256 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p1024_q256_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 256 - 1) / 256;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um1_un1_simd1_single_block
//   P=1024 Q=384 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q384_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um1_un1_simd1_fixed_q
//   P=1024 Q=384 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q384_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um1_un1_simd1_adaptive_half
//   P=1024 Q=384 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q384_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um1_un1_simd1_equal_split
//   P=1024 Q=384 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q384_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um1_un4_simd1_single_block
//   P=1024 Q=384 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p1024_q384_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um1_un4_simd1_fixed_q
//   P=1024 Q=384 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p1024_q384_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um1_un4_simd1_adaptive_half
//   P=1024 Q=384 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p1024_q384_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um1_un4_simd1_equal_split
//   P=1024 Q=384 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p1024_q384_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um4_un1_simd1_single_block
//   P=1024 Q=384 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q384_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um4_un1_simd1_fixed_q
//   P=1024 Q=384 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q384_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um4_un1_simd1_adaptive_half
//   P=1024 Q=384 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q384_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um4_un1_simd1_equal_split
//   P=1024 Q=384 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q384_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um4_un1_simd4_single_block
//   P=1024 Q=384 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q384_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um4_un1_simd4_fixed_q
//   P=1024 Q=384 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q384_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um4_un1_simd4_adaptive_half
//   P=1024 Q=384 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q384_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um4_un1_simd4_equal_split
//   P=1024 Q=384 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q384_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um4_un4_simd4_single_block
//   P=1024 Q=384 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p1024_q384_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um4_un4_simd4_fixed_q
//   P=1024 Q=384 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p1024_q384_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um4_un4_simd4_adaptive_half
//   P=1024 Q=384 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p1024_q384_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um4_un4_simd4_equal_split
//   P=1024 Q=384 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p1024_q384_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um8_un1_simd1_single_block
//   P=1024 Q=384 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q384_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um8_un1_simd1_fixed_q
//   P=1024 Q=384 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q384_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um8_un1_simd1_adaptive_half
//   P=1024 Q=384 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q384_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um8_un1_simd1_equal_split
//   P=1024 Q=384 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q384_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um8_un1_simd4_single_block
//   P=1024 Q=384 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q384_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um8_un1_simd4_fixed_q
//   P=1024 Q=384 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q384_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um8_un1_simd4_adaptive_half
//   P=1024 Q=384 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q384_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um8_un1_simd4_equal_split
//   P=1024 Q=384 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q384_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um8_un1_simd8_single_block
//   P=1024 Q=384 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p1024_q384_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um8_un1_simd8_fixed_q
//   P=1024 Q=384 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p1024_q384_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um8_un1_simd8_adaptive_half
//   P=1024 Q=384 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p1024_q384_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um8_un1_simd8_equal_split
//   P=1024 Q=384 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p1024_q384_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um8_un4_simd4_single_block
//   P=1024 Q=384 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p1024_q384_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um8_un4_simd4_fixed_q
//   P=1024 Q=384 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p1024_q384_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um8_un4_simd4_adaptive_half
//   P=1024 Q=384 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p1024_q384_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um8_un4_simd4_equal_split
//   P=1024 Q=384 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p1024_q384_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um8_un4_simd8_single_block
//   P=1024 Q=384 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p1024_q384_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um8_un4_simd8_fixed_q
//   P=1024 Q=384 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p1024_q384_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um8_un4_simd8_adaptive_half
//   P=1024 Q=384 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p1024_q384_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um8_un4_simd8_equal_split
//   P=1024 Q=384 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p1024_q384_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um16_un1_simd4_single_block
//   P=1024 Q=384 UM=16 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q384_um16_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um16_un1_simd4_fixed_q
//   P=1024 Q=384 UM=16 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q384_um16_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um16_un1_simd4_adaptive_half
//   P=1024 Q=384 UM=16 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q384_um16_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um16_un1_simd4_equal_split
//   P=1024 Q=384 UM=16 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q384_um16_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um16_un1_simd8_single_block
//   P=1024 Q=384 UM=16 UN=1 SIMD=8 KRule=single_block
void gemm_p1024_q384_um16_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um16_un1_simd8_fixed_q
//   P=1024 Q=384 UM=16 UN=1 SIMD=8 KRule=fixed_q
void gemm_p1024_q384_um16_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um16_un1_simd8_adaptive_half
//   P=1024 Q=384 UM=16 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p1024_q384_um16_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um16_un1_simd8_equal_split
//   P=1024 Q=384 UM=16 UN=1 SIMD=8 KRule=equal_split
void gemm_p1024_q384_um16_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um16_un1_simd16_single_block
//   P=1024 Q=384 UM=16 UN=1 SIMD=16 KRule=single_block
void gemm_p1024_q384_um16_un1_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um16_un1_simd16_fixed_q
//   P=1024 Q=384 UM=16 UN=1 SIMD=16 KRule=fixed_q
void gemm_p1024_q384_um16_un1_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um16_un1_simd16_adaptive_half
//   P=1024 Q=384 UM=16 UN=1 SIMD=16 KRule=adaptive_half
void gemm_p1024_q384_um16_un1_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um16_un1_simd16_equal_split
//   P=1024 Q=384 UM=16 UN=1 SIMD=16 KRule=equal_split
void gemm_p1024_q384_um16_un1_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um16_un4_simd8_single_block
//   P=1024 Q=384 UM=16 UN=4 SIMD=8 KRule=single_block
void gemm_p1024_q384_um16_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um16_un4_simd8_fixed_q
//   P=1024 Q=384 UM=16 UN=4 SIMD=8 KRule=fixed_q
void gemm_p1024_q384_um16_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um16_un4_simd8_adaptive_half
//   P=1024 Q=384 UM=16 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p1024_q384_um16_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um16_un4_simd8_equal_split
//   P=1024 Q=384 UM=16 UN=4 SIMD=8 KRule=equal_split
void gemm_p1024_q384_um16_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um16_un4_simd16_single_block
//   P=1024 Q=384 UM=16 UN=4 SIMD=16 KRule=single_block
void gemm_p1024_q384_um16_un4_simd16_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um16_un4_simd16_fixed_q
//   P=1024 Q=384 UM=16 UN=4 SIMD=16 KRule=fixed_q
void gemm_p1024_q384_um16_un4_simd16_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 384 ? 384 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um16_un4_simd16_adaptive_half
//   P=1024 Q=384 UM=16 UN=4 SIMD=16 KRule=adaptive_half
void gemm_p1024_q384_um16_un4_simd16_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 384) { min_l = 384; }
        else if (rem > 384) { min_l = ((rem / 2 + 16 - 1) / 16) * 16; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q384_um16_un4_simd16_equal_split
//   P=1024 Q=384 UM=16 UN=4 SIMD=16 KRule=equal_split
void gemm_p1024_q384_um16_un4_simd16_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 384 - 1) / 384;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 16 - 1) / 16) * 16;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um1_un1_simd1_single_block
//   P=1024 Q=512 UM=1 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q512_um1_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um1_un1_simd1_fixed_q
//   P=1024 Q=512 UM=1 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q512_um1_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um1_un1_simd1_adaptive_half
//   P=1024 Q=512 UM=1 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q512_um1_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um1_un1_simd1_equal_split
//   P=1024 Q=512 UM=1 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q512_um1_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um1_un4_simd1_single_block
//   P=1024 Q=512 UM=1 UN=4 SIMD=1 KRule=single_block
void gemm_p1024_q512_um1_un4_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um1_un4_simd1_fixed_q
//   P=1024 Q=512 UM=1 UN=4 SIMD=1 KRule=fixed_q
void gemm_p1024_q512_um1_un4_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um1_un4_simd1_adaptive_half
//   P=1024 Q=512 UM=1 UN=4 SIMD=1 KRule=adaptive_half
void gemm_p1024_q512_um1_un4_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 1 - 1) / 1) * 1; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um1_un4_simd1_equal_split
//   P=1024 Q=512 UM=1 UN=4 SIMD=1 KRule=equal_split
void gemm_p1024_q512_um1_un4_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 1 - 1) / 1) * 1;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um4_un1_simd1_single_block
//   P=1024 Q=512 UM=4 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q512_um4_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um4_un1_simd1_fixed_q
//   P=1024 Q=512 UM=4 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q512_um4_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um4_un1_simd1_adaptive_half
//   P=1024 Q=512 UM=4 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q512_um4_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um4_un1_simd1_equal_split
//   P=1024 Q=512 UM=4 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q512_um4_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um4_un1_simd4_single_block
//   P=1024 Q=512 UM=4 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q512_um4_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um4_un1_simd4_fixed_q
//   P=1024 Q=512 UM=4 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q512_um4_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um4_un1_simd4_adaptive_half
//   P=1024 Q=512 UM=4 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q512_um4_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um4_un1_simd4_equal_split
//   P=1024 Q=512 UM=4 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q512_um4_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um4_un4_simd4_single_block
//   P=1024 Q=512 UM=4 UN=4 SIMD=4 KRule=single_block
void gemm_p1024_q512_um4_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um4_un4_simd4_fixed_q
//   P=1024 Q=512 UM=4 UN=4 SIMD=4 KRule=fixed_q
void gemm_p1024_q512_um4_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um4_un4_simd4_adaptive_half
//   P=1024 Q=512 UM=4 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p1024_q512_um4_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 4 - 1) / 4) * 4; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um4_un4_simd4_equal_split
//   P=1024 Q=512 UM=4 UN=4 SIMD=4 KRule=equal_split
void gemm_p1024_q512_um4_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 4 - 1) / 4) * 4;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um8_un1_simd1_single_block
//   P=1024 Q=512 UM=8 UN=1 SIMD=1 KRule=single_block
void gemm_p1024_q512_um8_un1_simd1_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um8_un1_simd1_fixed_q
//   P=1024 Q=512 UM=8 UN=1 SIMD=1 KRule=fixed_q
void gemm_p1024_q512_um8_un1_simd1_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um8_un1_simd1_adaptive_half
//   P=1024 Q=512 UM=8 UN=1 SIMD=1 KRule=adaptive_half
void gemm_p1024_q512_um8_un1_simd1_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um8_un1_simd1_equal_split
//   P=1024 Q=512 UM=8 UN=1 SIMD=1 KRule=equal_split
void gemm_p1024_q512_um8_un1_simd1_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um8_un1_simd4_single_block
//   P=1024 Q=512 UM=8 UN=1 SIMD=4 KRule=single_block
void gemm_p1024_q512_um8_un1_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um8_un1_simd4_fixed_q
//   P=1024 Q=512 UM=8 UN=1 SIMD=4 KRule=fixed_q
void gemm_p1024_q512_um8_un1_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um8_un1_simd4_adaptive_half
//   P=1024 Q=512 UM=8 UN=1 SIMD=4 KRule=adaptive_half
void gemm_p1024_q512_um8_un1_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um8_un1_simd4_equal_split
//   P=1024 Q=512 UM=8 UN=1 SIMD=4 KRule=equal_split
void gemm_p1024_q512_um8_un1_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um8_un1_simd8_single_block
//   P=1024 Q=512 UM=8 UN=1 SIMD=8 KRule=single_block
void gemm_p1024_q512_um8_un1_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um8_un1_simd8_fixed_q
//   P=1024 Q=512 UM=8 UN=1 SIMD=8 KRule=fixed_q
void gemm_p1024_q512_um8_un1_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um8_un1_simd8_adaptive_half
//   P=1024 Q=512 UM=8 UN=1 SIMD=8 KRule=adaptive_half
void gemm_p1024_q512_um8_un1_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um8_un1_simd8_equal_split
//   P=1024 Q=512 UM=8 UN=1 SIMD=8 KRule=equal_split
void gemm_p1024_q512_um8_un1_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um8_un4_simd4_single_block
//   P=1024 Q=512 UM=8 UN=4 SIMD=4 KRule=single_block
void gemm_p1024_q512_um8_un4_simd4_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um8_un4_simd4_fixed_q
//   P=1024 Q=512 UM=8 UN=4 SIMD=4 KRule=fixed_q
void gemm_p1024_q512_um8_un4_simd4_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um8_un4_simd4_adaptive_half
//   P=1024 Q=512 UM=8 UN=4 SIMD=4 KRule=adaptive_half
void gemm_p1024_q512_um8_un4_simd4_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um8_un4_simd4_equal_split
//   P=1024 Q=512 UM=8 UN=4 SIMD=4 KRule=equal_split
void gemm_p1024_q512_um8_un4_simd4_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um8_un4_simd8_single_block
//   P=1024 Q=512 UM=8 UN=4 SIMD=8 KRule=single_block
void gemm_p1024_q512_um8_un4_simd8_single_block(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem;  /* single_block */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um8_un4_simd8_fixed_q
//   P=1024 Q=512 UM=8 UN=4 SIMD=8 KRule=fixed_q
void gemm_p1024_q512_um8_un4_simd8_fixed_q(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l = rem > 512 ? 512 : rem;  /* fixed_q */
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um8_un4_simd8_adaptive_half
//   P=1024 Q=512 UM=8 UN=4 SIMD=8 KRule=adaptive_half
void gemm_p1024_q512_um8_un4_simd8_adaptive_half(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        if (rem >= 2 * 512) { min_l = 512; }
        else if (rem > 512) { min_l = ((rem / 2 + 8 - 1) / 8) * 8; }
        else { min_l = rem; }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

// gemm_p1024_q512_um8_un4_simd8_equal_split
//   P=1024 Q=512 UM=8 UN=4 SIMD=8 KRule=equal_split
void gemm_p1024_q512_um8_un4_simd8_equal_split(const float* A, const float* B, float* C,
            int M, int N, int K) {
    for (int i = 0; i < M * N; ++i) C[i] = 0.0f;
    int ls = 0;
    while (ls < K) {
        int rem = K - ls;
        int min_l;
        {
            int nblocks = (K + 512 - 1) / 512;
            int per_block = (K + nblocks - 1) / nblocks;
            per_block = ((per_block + 8 - 1) / 8) * 8;
            if (per_block > rem) per_block = rem;
            min_l = per_block;
        }
        for (int row = 0; row < M; ++row) {
            for (int col = 0; col < N; ++col) {
                float partial = 0.0f;
                for (int k = ls; k < ls + min_l; ++k) {
                    partial += A[row * K + k] * B[k * N + col];
                }
                C[row * N + col] += partial;
            }
        }
        ls += min_l;
    }
}

typedef void (*gemm_kernel_fn)(const float* A, const float* B,
                               float* C, int M, int N, int K);

const int gemm_dispatch_count = 4328;

gemm_kernel_fn gemm_dispatch[] = {
    gemm_p1_q1_um1_un1_simd1_single_block,
    gemm_p1_q1_um1_un1_simd1_fixed_q,
    gemm_p1_q1_um1_un1_simd1_adaptive_half,
    gemm_p1_q1_um1_un1_simd1_equal_split,
    gemm_p1_q1_um1_un4_simd1_single_block,
    gemm_p1_q1_um1_un4_simd1_fixed_q,
    gemm_p1_q1_um1_un4_simd1_adaptive_half,
    gemm_p1_q1_um1_un4_simd1_equal_split,
    gemm_p1_q64_um1_un1_simd1_single_block,
    gemm_p1_q64_um1_un1_simd1_fixed_q,
    gemm_p1_q64_um1_un1_simd1_adaptive_half,
    gemm_p1_q64_um1_un1_simd1_equal_split,
    gemm_p1_q64_um1_un4_simd1_single_block,
    gemm_p1_q64_um1_un4_simd1_fixed_q,
    gemm_p1_q64_um1_un4_simd1_adaptive_half,
    gemm_p1_q64_um1_un4_simd1_equal_split,
    gemm_p1_q128_um1_un1_simd1_single_block,
    gemm_p1_q128_um1_un1_simd1_fixed_q,
    gemm_p1_q128_um1_un1_simd1_adaptive_half,
    gemm_p1_q128_um1_un1_simd1_equal_split,
    gemm_p1_q128_um1_un4_simd1_single_block,
    gemm_p1_q128_um1_un4_simd1_fixed_q,
    gemm_p1_q128_um1_un4_simd1_adaptive_half,
    gemm_p1_q128_um1_un4_simd1_equal_split,
    gemm_p1_q192_um1_un1_simd1_single_block,
    gemm_p1_q192_um1_un1_simd1_fixed_q,
    gemm_p1_q192_um1_un1_simd1_adaptive_half,
    gemm_p1_q192_um1_un1_simd1_equal_split,
    gemm_p1_q192_um1_un4_simd1_single_block,
    gemm_p1_q192_um1_un4_simd1_fixed_q,
    gemm_p1_q192_um1_un4_simd1_adaptive_half,
    gemm_p1_q192_um1_un4_simd1_equal_split,
    gemm_p1_q240_um1_un1_simd1_single_block,
    gemm_p1_q240_um1_un1_simd1_fixed_q,
    gemm_p1_q240_um1_un1_simd1_adaptive_half,
    gemm_p1_q240_um1_un1_simd1_equal_split,
    gemm_p1_q240_um1_un4_simd1_single_block,
    gemm_p1_q240_um1_un4_simd1_fixed_q,
    gemm_p1_q240_um1_un4_simd1_adaptive_half,
    gemm_p1_q240_um1_un4_simd1_equal_split,
    gemm_p1_q248_um1_un1_simd1_single_block,
    gemm_p1_q248_um1_un1_simd1_fixed_q,
    gemm_p1_q248_um1_un1_simd1_adaptive_half,
    gemm_p1_q248_um1_un1_simd1_equal_split,
    gemm_p1_q248_um1_un4_simd1_single_block,
    gemm_p1_q248_um1_un4_simd1_fixed_q,
    gemm_p1_q248_um1_un4_simd1_adaptive_half,
    gemm_p1_q248_um1_un4_simd1_equal_split,
    gemm_p1_q256_um1_un1_simd1_single_block,
    gemm_p1_q256_um1_un1_simd1_fixed_q,
    gemm_p1_q256_um1_un1_simd1_adaptive_half,
    gemm_p1_q256_um1_un1_simd1_equal_split,
    gemm_p1_q256_um1_un4_simd1_single_block,
    gemm_p1_q256_um1_un4_simd1_fixed_q,
    gemm_p1_q256_um1_un4_simd1_adaptive_half,
    gemm_p1_q256_um1_un4_simd1_equal_split,
    gemm_p1_q384_um1_un1_simd1_single_block,
    gemm_p1_q384_um1_un1_simd1_fixed_q,
    gemm_p1_q384_um1_un1_simd1_adaptive_half,
    gemm_p1_q384_um1_un1_simd1_equal_split,
    gemm_p1_q384_um1_un4_simd1_single_block,
    gemm_p1_q384_um1_un4_simd1_fixed_q,
    gemm_p1_q384_um1_un4_simd1_adaptive_half,
    gemm_p1_q384_um1_un4_simd1_equal_split,
    gemm_p1_q512_um1_un1_simd1_single_block,
    gemm_p1_q512_um1_un1_simd1_fixed_q,
    gemm_p1_q512_um1_un1_simd1_adaptive_half,
    gemm_p1_q512_um1_un1_simd1_equal_split,
    gemm_p1_q512_um1_un4_simd1_single_block,
    gemm_p1_q512_um1_un4_simd1_fixed_q,
    gemm_p1_q512_um1_un4_simd1_adaptive_half,
    gemm_p1_q512_um1_un4_simd1_equal_split,
    gemm_p64_q1_um1_un1_simd1_single_block,
    gemm_p64_q1_um1_un1_simd1_fixed_q,
    gemm_p64_q1_um1_un1_simd1_adaptive_half,
    gemm_p64_q1_um1_un1_simd1_equal_split,
    gemm_p64_q1_um1_un4_simd1_single_block,
    gemm_p64_q1_um1_un4_simd1_fixed_q,
    gemm_p64_q1_um1_un4_simd1_adaptive_half,
    gemm_p64_q1_um1_un4_simd1_equal_split,
    gemm_p64_q1_um4_un1_simd1_single_block,
    gemm_p64_q1_um4_un1_simd1_fixed_q,
    gemm_p64_q1_um4_un1_simd1_adaptive_half,
    gemm_p64_q1_um4_un1_simd1_equal_split,
    gemm_p64_q1_um4_un1_simd4_single_block,
    gemm_p64_q1_um4_un1_simd4_fixed_q,
    gemm_p64_q1_um4_un1_simd4_adaptive_half,
    gemm_p64_q1_um4_un1_simd4_equal_split,
    gemm_p64_q1_um4_un4_simd4_single_block,
    gemm_p64_q1_um4_un4_simd4_fixed_q,
    gemm_p64_q1_um4_un4_simd4_adaptive_half,
    gemm_p64_q1_um4_un4_simd4_equal_split,
    gemm_p64_q1_um8_un1_simd1_single_block,
    gemm_p64_q1_um8_un1_simd1_fixed_q,
    gemm_p64_q1_um8_un1_simd1_adaptive_half,
    gemm_p64_q1_um8_un1_simd1_equal_split,
    gemm_p64_q1_um8_un1_simd4_single_block,
    gemm_p64_q1_um8_un1_simd4_fixed_q,
    gemm_p64_q1_um8_un1_simd4_adaptive_half,
    gemm_p64_q1_um8_un1_simd4_equal_split,
    gemm_p64_q1_um8_un1_simd8_single_block,
    gemm_p64_q1_um8_un1_simd8_fixed_q,
    gemm_p64_q1_um8_un1_simd8_adaptive_half,
    gemm_p64_q1_um8_un1_simd8_equal_split,
    gemm_p64_q1_um8_un4_simd4_single_block,
    gemm_p64_q1_um8_un4_simd4_fixed_q,
    gemm_p64_q1_um8_un4_simd4_adaptive_half,
    gemm_p64_q1_um8_un4_simd4_equal_split,
    gemm_p64_q1_um8_un4_simd8_single_block,
    gemm_p64_q1_um8_un4_simd8_fixed_q,
    gemm_p64_q1_um8_un4_simd8_adaptive_half,
    gemm_p64_q1_um8_un4_simd8_equal_split,
    gemm_p64_q1_um16_un1_simd4_single_block,
    gemm_p64_q1_um16_un1_simd4_fixed_q,
    gemm_p64_q1_um16_un1_simd4_adaptive_half,
    gemm_p64_q1_um16_un1_simd4_equal_split,
    gemm_p64_q1_um16_un1_simd8_single_block,
    gemm_p64_q1_um16_un1_simd8_fixed_q,
    gemm_p64_q1_um16_un1_simd8_adaptive_half,
    gemm_p64_q1_um16_un1_simd8_equal_split,
    gemm_p64_q1_um16_un1_simd16_single_block,
    gemm_p64_q1_um16_un1_simd16_fixed_q,
    gemm_p64_q1_um16_un1_simd16_adaptive_half,
    gemm_p64_q1_um16_un1_simd16_equal_split,
    gemm_p64_q1_um16_un4_simd8_single_block,
    gemm_p64_q1_um16_un4_simd8_fixed_q,
    gemm_p64_q1_um16_un4_simd8_adaptive_half,
    gemm_p64_q1_um16_un4_simd8_equal_split,
    gemm_p64_q1_um16_un4_simd16_single_block,
    gemm_p64_q1_um16_un4_simd16_fixed_q,
    gemm_p64_q1_um16_un4_simd16_adaptive_half,
    gemm_p64_q1_um16_un4_simd16_equal_split,
    gemm_p64_q1_um32_un1_simd4_single_block,
    gemm_p64_q1_um32_un1_simd4_fixed_q,
    gemm_p64_q1_um32_un1_simd4_adaptive_half,
    gemm_p64_q1_um32_un1_simd4_equal_split,
    gemm_p64_q1_um32_un1_simd8_single_block,
    gemm_p64_q1_um32_un1_simd8_fixed_q,
    gemm_p64_q1_um32_un1_simd8_adaptive_half,
    gemm_p64_q1_um32_un1_simd8_equal_split,
    gemm_p64_q1_um32_un1_simd16_single_block,
    gemm_p64_q1_um32_un1_simd16_fixed_q,
    gemm_p64_q1_um32_un1_simd16_adaptive_half,
    gemm_p64_q1_um32_un1_simd16_equal_split,
    gemm_p64_q1_um32_un4_simd16_single_block,
    gemm_p64_q1_um32_un4_simd16_fixed_q,
    gemm_p64_q1_um32_un4_simd16_adaptive_half,
    gemm_p64_q1_um32_un4_simd16_equal_split,
    gemm_p64_q64_um1_un1_simd1_single_block,
    gemm_p64_q64_um1_un1_simd1_fixed_q,
    gemm_p64_q64_um1_un1_simd1_adaptive_half,
    gemm_p64_q64_um1_un1_simd1_equal_split,
    gemm_p64_q64_um1_un4_simd1_single_block,
    gemm_p64_q64_um1_un4_simd1_fixed_q,
    gemm_p64_q64_um1_un4_simd1_adaptive_half,
    gemm_p64_q64_um1_un4_simd1_equal_split,
    gemm_p64_q64_um4_un1_simd1_single_block,
    gemm_p64_q64_um4_un1_simd1_fixed_q,
    gemm_p64_q64_um4_un1_simd1_adaptive_half,
    gemm_p64_q64_um4_un1_simd1_equal_split,
    gemm_p64_q64_um4_un1_simd4_single_block,
    gemm_p64_q64_um4_un1_simd4_fixed_q,
    gemm_p64_q64_um4_un1_simd4_adaptive_half,
    gemm_p64_q64_um4_un1_simd4_equal_split,
    gemm_p64_q64_um4_un4_simd4_single_block,
    gemm_p64_q64_um4_un4_simd4_fixed_q,
    gemm_p64_q64_um4_un4_simd4_adaptive_half,
    gemm_p64_q64_um4_un4_simd4_equal_split,
    gemm_p64_q64_um8_un1_simd1_single_block,
    gemm_p64_q64_um8_un1_simd1_fixed_q,
    gemm_p64_q64_um8_un1_simd1_adaptive_half,
    gemm_p64_q64_um8_un1_simd1_equal_split,
    gemm_p64_q64_um8_un1_simd4_single_block,
    gemm_p64_q64_um8_un1_simd4_fixed_q,
    gemm_p64_q64_um8_un1_simd4_adaptive_half,
    gemm_p64_q64_um8_un1_simd4_equal_split,
    gemm_p64_q64_um8_un1_simd8_single_block,
    gemm_p64_q64_um8_un1_simd8_fixed_q,
    gemm_p64_q64_um8_un1_simd8_adaptive_half,
    gemm_p64_q64_um8_un1_simd8_equal_split,
    gemm_p64_q64_um8_un4_simd4_single_block,
    gemm_p64_q64_um8_un4_simd4_fixed_q,
    gemm_p64_q64_um8_un4_simd4_adaptive_half,
    gemm_p64_q64_um8_un4_simd4_equal_split,
    gemm_p64_q64_um8_un4_simd8_single_block,
    gemm_p64_q64_um8_un4_simd8_fixed_q,
    gemm_p64_q64_um8_un4_simd8_adaptive_half,
    gemm_p64_q64_um8_un4_simd8_equal_split,
    gemm_p64_q64_um16_un1_simd4_single_block,
    gemm_p64_q64_um16_un1_simd4_fixed_q,
    gemm_p64_q64_um16_un1_simd4_adaptive_half,
    gemm_p64_q64_um16_un1_simd4_equal_split,
    gemm_p64_q64_um16_un1_simd8_single_block,
    gemm_p64_q64_um16_un1_simd8_fixed_q,
    gemm_p64_q64_um16_un1_simd8_adaptive_half,
    gemm_p64_q64_um16_un1_simd8_equal_split,
    gemm_p64_q64_um16_un1_simd16_single_block,
    gemm_p64_q64_um16_un1_simd16_fixed_q,
    gemm_p64_q64_um16_un1_simd16_adaptive_half,
    gemm_p64_q64_um16_un1_simd16_equal_split,
    gemm_p64_q64_um16_un4_simd8_single_block,
    gemm_p64_q64_um16_un4_simd8_fixed_q,
    gemm_p64_q64_um16_un4_simd8_adaptive_half,
    gemm_p64_q64_um16_un4_simd8_equal_split,
    gemm_p64_q64_um16_un4_simd16_single_block,
    gemm_p64_q64_um16_un4_simd16_fixed_q,
    gemm_p64_q64_um16_un4_simd16_adaptive_half,
    gemm_p64_q64_um16_un4_simd16_equal_split,
    gemm_p64_q64_um32_un1_simd4_single_block,
    gemm_p64_q64_um32_un1_simd4_fixed_q,
    gemm_p64_q64_um32_un1_simd4_adaptive_half,
    gemm_p64_q64_um32_un1_simd4_equal_split,
    gemm_p64_q64_um32_un1_simd8_single_block,
    gemm_p64_q64_um32_un1_simd8_fixed_q,
    gemm_p64_q64_um32_un1_simd8_adaptive_half,
    gemm_p64_q64_um32_un1_simd8_equal_split,
    gemm_p64_q64_um32_un1_simd16_single_block,
    gemm_p64_q64_um32_un1_simd16_fixed_q,
    gemm_p64_q64_um32_un1_simd16_adaptive_half,
    gemm_p64_q64_um32_un1_simd16_equal_split,
    gemm_p64_q64_um32_un4_simd16_single_block,
    gemm_p64_q64_um32_un4_simd16_fixed_q,
    gemm_p64_q64_um32_un4_simd16_adaptive_half,
    gemm_p64_q64_um32_un4_simd16_equal_split,
    gemm_p64_q128_um1_un1_simd1_single_block,
    gemm_p64_q128_um1_un1_simd1_fixed_q,
    gemm_p64_q128_um1_un1_simd1_adaptive_half,
    gemm_p64_q128_um1_un1_simd1_equal_split,
    gemm_p64_q128_um1_un4_simd1_single_block,
    gemm_p64_q128_um1_un4_simd1_fixed_q,
    gemm_p64_q128_um1_un4_simd1_adaptive_half,
    gemm_p64_q128_um1_un4_simd1_equal_split,
    gemm_p64_q128_um4_un1_simd1_single_block,
    gemm_p64_q128_um4_un1_simd1_fixed_q,
    gemm_p64_q128_um4_un1_simd1_adaptive_half,
    gemm_p64_q128_um4_un1_simd1_equal_split,
    gemm_p64_q128_um4_un1_simd4_single_block,
    gemm_p64_q128_um4_un1_simd4_fixed_q,
    gemm_p64_q128_um4_un1_simd4_adaptive_half,
    gemm_p64_q128_um4_un1_simd4_equal_split,
    gemm_p64_q128_um4_un4_simd4_single_block,
    gemm_p64_q128_um4_un4_simd4_fixed_q,
    gemm_p64_q128_um4_un4_simd4_adaptive_half,
    gemm_p64_q128_um4_un4_simd4_equal_split,
    gemm_p64_q128_um8_un1_simd1_single_block,
    gemm_p64_q128_um8_un1_simd1_fixed_q,
    gemm_p64_q128_um8_un1_simd1_adaptive_half,
    gemm_p64_q128_um8_un1_simd1_equal_split,
    gemm_p64_q128_um8_un1_simd4_single_block,
    gemm_p64_q128_um8_un1_simd4_fixed_q,
    gemm_p64_q128_um8_un1_simd4_adaptive_half,
    gemm_p64_q128_um8_un1_simd4_equal_split,
    gemm_p64_q128_um8_un1_simd8_single_block,
    gemm_p64_q128_um8_un1_simd8_fixed_q,
    gemm_p64_q128_um8_un1_simd8_adaptive_half,
    gemm_p64_q128_um8_un1_simd8_equal_split,
    gemm_p64_q128_um8_un4_simd4_single_block,
    gemm_p64_q128_um8_un4_simd4_fixed_q,
    gemm_p64_q128_um8_un4_simd4_adaptive_half,
    gemm_p64_q128_um8_un4_simd4_equal_split,
    gemm_p64_q128_um8_un4_simd8_single_block,
    gemm_p64_q128_um8_un4_simd8_fixed_q,
    gemm_p64_q128_um8_un4_simd8_adaptive_half,
    gemm_p64_q128_um8_un4_simd8_equal_split,
    gemm_p64_q128_um16_un1_simd4_single_block,
    gemm_p64_q128_um16_un1_simd4_fixed_q,
    gemm_p64_q128_um16_un1_simd4_adaptive_half,
    gemm_p64_q128_um16_un1_simd4_equal_split,
    gemm_p64_q128_um16_un1_simd8_single_block,
    gemm_p64_q128_um16_un1_simd8_fixed_q,
    gemm_p64_q128_um16_un1_simd8_adaptive_half,
    gemm_p64_q128_um16_un1_simd8_equal_split,
    gemm_p64_q128_um16_un1_simd16_single_block,
    gemm_p64_q128_um16_un1_simd16_fixed_q,
    gemm_p64_q128_um16_un1_simd16_adaptive_half,
    gemm_p64_q128_um16_un1_simd16_equal_split,
    gemm_p64_q128_um16_un4_simd8_single_block,
    gemm_p64_q128_um16_un4_simd8_fixed_q,
    gemm_p64_q128_um16_un4_simd8_adaptive_half,
    gemm_p64_q128_um16_un4_simd8_equal_split,
    gemm_p64_q128_um16_un4_simd16_single_block,
    gemm_p64_q128_um16_un4_simd16_fixed_q,
    gemm_p64_q128_um16_un4_simd16_adaptive_half,
    gemm_p64_q128_um16_un4_simd16_equal_split,
    gemm_p64_q128_um32_un1_simd4_single_block,
    gemm_p64_q128_um32_un1_simd4_fixed_q,
    gemm_p64_q128_um32_un1_simd4_adaptive_half,
    gemm_p64_q128_um32_un1_simd4_equal_split,
    gemm_p64_q128_um32_un1_simd8_single_block,
    gemm_p64_q128_um32_un1_simd8_fixed_q,
    gemm_p64_q128_um32_un1_simd8_adaptive_half,
    gemm_p64_q128_um32_un1_simd8_equal_split,
    gemm_p64_q128_um32_un1_simd16_single_block,
    gemm_p64_q128_um32_un1_simd16_fixed_q,
    gemm_p64_q128_um32_un1_simd16_adaptive_half,
    gemm_p64_q128_um32_un1_simd16_equal_split,
    gemm_p64_q128_um32_un4_simd16_single_block,
    gemm_p64_q128_um32_un4_simd16_fixed_q,
    gemm_p64_q128_um32_un4_simd16_adaptive_half,
    gemm_p64_q128_um32_un4_simd16_equal_split,
    gemm_p64_q192_um1_un1_simd1_single_block,
    gemm_p64_q192_um1_un1_simd1_fixed_q,
    gemm_p64_q192_um1_un1_simd1_adaptive_half,
    gemm_p64_q192_um1_un1_simd1_equal_split,
    gemm_p64_q192_um1_un4_simd1_single_block,
    gemm_p64_q192_um1_un4_simd1_fixed_q,
    gemm_p64_q192_um1_un4_simd1_adaptive_half,
    gemm_p64_q192_um1_un4_simd1_equal_split,
    gemm_p64_q192_um4_un1_simd1_single_block,
    gemm_p64_q192_um4_un1_simd1_fixed_q,
    gemm_p64_q192_um4_un1_simd1_adaptive_half,
    gemm_p64_q192_um4_un1_simd1_equal_split,
    gemm_p64_q192_um4_un1_simd4_single_block,
    gemm_p64_q192_um4_un1_simd4_fixed_q,
    gemm_p64_q192_um4_un1_simd4_adaptive_half,
    gemm_p64_q192_um4_un1_simd4_equal_split,
    gemm_p64_q192_um4_un4_simd4_single_block,
    gemm_p64_q192_um4_un4_simd4_fixed_q,
    gemm_p64_q192_um4_un4_simd4_adaptive_half,
    gemm_p64_q192_um4_un4_simd4_equal_split,
    gemm_p64_q192_um8_un1_simd1_single_block,
    gemm_p64_q192_um8_un1_simd1_fixed_q,
    gemm_p64_q192_um8_un1_simd1_adaptive_half,
    gemm_p64_q192_um8_un1_simd1_equal_split,
    gemm_p64_q192_um8_un1_simd4_single_block,
    gemm_p64_q192_um8_un1_simd4_fixed_q,
    gemm_p64_q192_um8_un1_simd4_adaptive_half,
    gemm_p64_q192_um8_un1_simd4_equal_split,
    gemm_p64_q192_um8_un1_simd8_single_block,
    gemm_p64_q192_um8_un1_simd8_fixed_q,
    gemm_p64_q192_um8_un1_simd8_adaptive_half,
    gemm_p64_q192_um8_un1_simd8_equal_split,
    gemm_p64_q192_um8_un4_simd4_single_block,
    gemm_p64_q192_um8_un4_simd4_fixed_q,
    gemm_p64_q192_um8_un4_simd4_adaptive_half,
    gemm_p64_q192_um8_un4_simd4_equal_split,
    gemm_p64_q192_um8_un4_simd8_single_block,
    gemm_p64_q192_um8_un4_simd8_fixed_q,
    gemm_p64_q192_um8_un4_simd8_adaptive_half,
    gemm_p64_q192_um8_un4_simd8_equal_split,
    gemm_p64_q192_um16_un1_simd4_single_block,
    gemm_p64_q192_um16_un1_simd4_fixed_q,
    gemm_p64_q192_um16_un1_simd4_adaptive_half,
    gemm_p64_q192_um16_un1_simd4_equal_split,
    gemm_p64_q192_um16_un1_simd8_single_block,
    gemm_p64_q192_um16_un1_simd8_fixed_q,
    gemm_p64_q192_um16_un1_simd8_adaptive_half,
    gemm_p64_q192_um16_un1_simd8_equal_split,
    gemm_p64_q192_um16_un1_simd16_single_block,
    gemm_p64_q192_um16_un1_simd16_fixed_q,
    gemm_p64_q192_um16_un1_simd16_adaptive_half,
    gemm_p64_q192_um16_un1_simd16_equal_split,
    gemm_p64_q192_um16_un4_simd8_single_block,
    gemm_p64_q192_um16_un4_simd8_fixed_q,
    gemm_p64_q192_um16_un4_simd8_adaptive_half,
    gemm_p64_q192_um16_un4_simd8_equal_split,
    gemm_p64_q192_um16_un4_simd16_single_block,
    gemm_p64_q192_um16_un4_simd16_fixed_q,
    gemm_p64_q192_um16_un4_simd16_adaptive_half,
    gemm_p64_q192_um16_un4_simd16_equal_split,
    gemm_p64_q192_um32_un1_simd4_single_block,
    gemm_p64_q192_um32_un1_simd4_fixed_q,
    gemm_p64_q192_um32_un1_simd4_adaptive_half,
    gemm_p64_q192_um32_un1_simd4_equal_split,
    gemm_p64_q192_um32_un1_simd8_single_block,
    gemm_p64_q192_um32_un1_simd8_fixed_q,
    gemm_p64_q192_um32_un1_simd8_adaptive_half,
    gemm_p64_q192_um32_un1_simd8_equal_split,
    gemm_p64_q192_um32_un1_simd16_single_block,
    gemm_p64_q192_um32_un1_simd16_fixed_q,
    gemm_p64_q192_um32_un1_simd16_adaptive_half,
    gemm_p64_q192_um32_un1_simd16_equal_split,
    gemm_p64_q192_um32_un4_simd16_single_block,
    gemm_p64_q192_um32_un4_simd16_fixed_q,
    gemm_p64_q192_um32_un4_simd16_adaptive_half,
    gemm_p64_q192_um32_un4_simd16_equal_split,
    gemm_p64_q240_um1_un1_simd1_single_block,
    gemm_p64_q240_um1_un1_simd1_fixed_q,
    gemm_p64_q240_um1_un1_simd1_adaptive_half,
    gemm_p64_q240_um1_un1_simd1_equal_split,
    gemm_p64_q240_um1_un4_simd1_single_block,
    gemm_p64_q240_um1_un4_simd1_fixed_q,
    gemm_p64_q240_um1_un4_simd1_adaptive_half,
    gemm_p64_q240_um1_un4_simd1_equal_split,
    gemm_p64_q240_um4_un1_simd1_single_block,
    gemm_p64_q240_um4_un1_simd1_fixed_q,
    gemm_p64_q240_um4_un1_simd1_adaptive_half,
    gemm_p64_q240_um4_un1_simd1_equal_split,
    gemm_p64_q240_um4_un1_simd4_single_block,
    gemm_p64_q240_um4_un1_simd4_fixed_q,
    gemm_p64_q240_um4_un1_simd4_adaptive_half,
    gemm_p64_q240_um4_un1_simd4_equal_split,
    gemm_p64_q240_um4_un4_simd4_single_block,
    gemm_p64_q240_um4_un4_simd4_fixed_q,
    gemm_p64_q240_um4_un4_simd4_adaptive_half,
    gemm_p64_q240_um4_un4_simd4_equal_split,
    gemm_p64_q240_um8_un1_simd1_single_block,
    gemm_p64_q240_um8_un1_simd1_fixed_q,
    gemm_p64_q240_um8_un1_simd1_adaptive_half,
    gemm_p64_q240_um8_un1_simd1_equal_split,
    gemm_p64_q240_um8_un1_simd4_single_block,
    gemm_p64_q240_um8_un1_simd4_fixed_q,
    gemm_p64_q240_um8_un1_simd4_adaptive_half,
    gemm_p64_q240_um8_un1_simd4_equal_split,
    gemm_p64_q240_um8_un1_simd8_single_block,
    gemm_p64_q240_um8_un1_simd8_fixed_q,
    gemm_p64_q240_um8_un1_simd8_adaptive_half,
    gemm_p64_q240_um8_un1_simd8_equal_split,
    gemm_p64_q240_um8_un4_simd4_single_block,
    gemm_p64_q240_um8_un4_simd4_fixed_q,
    gemm_p64_q240_um8_un4_simd4_adaptive_half,
    gemm_p64_q240_um8_un4_simd4_equal_split,
    gemm_p64_q240_um8_un4_simd8_single_block,
    gemm_p64_q240_um8_un4_simd8_fixed_q,
    gemm_p64_q240_um8_un4_simd8_adaptive_half,
    gemm_p64_q240_um8_un4_simd8_equal_split,
    gemm_p64_q240_um16_un1_simd4_single_block,
    gemm_p64_q240_um16_un1_simd4_fixed_q,
    gemm_p64_q240_um16_un1_simd4_adaptive_half,
    gemm_p64_q240_um16_un1_simd4_equal_split,
    gemm_p64_q240_um16_un1_simd8_single_block,
    gemm_p64_q240_um16_un1_simd8_fixed_q,
    gemm_p64_q240_um16_un1_simd8_adaptive_half,
    gemm_p64_q240_um16_un1_simd8_equal_split,
    gemm_p64_q240_um16_un1_simd16_single_block,
    gemm_p64_q240_um16_un1_simd16_fixed_q,
    gemm_p64_q240_um16_un1_simd16_adaptive_half,
    gemm_p64_q240_um16_un1_simd16_equal_split,
    gemm_p64_q240_um16_un4_simd8_single_block,
    gemm_p64_q240_um16_un4_simd8_fixed_q,
    gemm_p64_q240_um16_un4_simd8_adaptive_half,
    gemm_p64_q240_um16_un4_simd8_equal_split,
    gemm_p64_q240_um16_un4_simd16_single_block,
    gemm_p64_q240_um16_un4_simd16_fixed_q,
    gemm_p64_q240_um16_un4_simd16_adaptive_half,
    gemm_p64_q240_um16_un4_simd16_equal_split,
    gemm_p64_q240_um32_un1_simd4_single_block,
    gemm_p64_q240_um32_un1_simd4_fixed_q,
    gemm_p64_q240_um32_un1_simd4_adaptive_half,
    gemm_p64_q240_um32_un1_simd4_equal_split,
    gemm_p64_q240_um32_un1_simd8_single_block,
    gemm_p64_q240_um32_un1_simd8_fixed_q,
    gemm_p64_q240_um32_un1_simd8_adaptive_half,
    gemm_p64_q240_um32_un1_simd8_equal_split,
    gemm_p64_q240_um32_un1_simd16_single_block,
    gemm_p64_q240_um32_un1_simd16_fixed_q,
    gemm_p64_q240_um32_un1_simd16_adaptive_half,
    gemm_p64_q240_um32_un1_simd16_equal_split,
    gemm_p64_q248_um1_un1_simd1_single_block,
    gemm_p64_q248_um1_un1_simd1_fixed_q,
    gemm_p64_q248_um1_un1_simd1_adaptive_half,
    gemm_p64_q248_um1_un1_simd1_equal_split,
    gemm_p64_q248_um1_un4_simd1_single_block,
    gemm_p64_q248_um1_un4_simd1_fixed_q,
    gemm_p64_q248_um1_un4_simd1_adaptive_half,
    gemm_p64_q248_um1_un4_simd1_equal_split,
    gemm_p64_q248_um4_un1_simd1_single_block,
    gemm_p64_q248_um4_un1_simd1_fixed_q,
    gemm_p64_q248_um4_un1_simd1_adaptive_half,
    gemm_p64_q248_um4_un1_simd1_equal_split,
    gemm_p64_q248_um4_un1_simd4_single_block,
    gemm_p64_q248_um4_un1_simd4_fixed_q,
    gemm_p64_q248_um4_un1_simd4_adaptive_half,
    gemm_p64_q248_um4_un1_simd4_equal_split,
    gemm_p64_q248_um4_un4_simd4_single_block,
    gemm_p64_q248_um4_un4_simd4_fixed_q,
    gemm_p64_q248_um4_un4_simd4_adaptive_half,
    gemm_p64_q248_um4_un4_simd4_equal_split,
    gemm_p64_q248_um8_un1_simd1_single_block,
    gemm_p64_q248_um8_un1_simd1_fixed_q,
    gemm_p64_q248_um8_un1_simd1_adaptive_half,
    gemm_p64_q248_um8_un1_simd1_equal_split,
    gemm_p64_q248_um8_un1_simd4_single_block,
    gemm_p64_q248_um8_un1_simd4_fixed_q,
    gemm_p64_q248_um8_un1_simd4_adaptive_half,
    gemm_p64_q248_um8_un1_simd4_equal_split,
    gemm_p64_q248_um8_un1_simd8_single_block,
    gemm_p64_q248_um8_un1_simd8_fixed_q,
    gemm_p64_q248_um8_un1_simd8_adaptive_half,
    gemm_p64_q248_um8_un1_simd8_equal_split,
    gemm_p64_q248_um8_un4_simd4_single_block,
    gemm_p64_q248_um8_un4_simd4_fixed_q,
    gemm_p64_q248_um8_un4_simd4_adaptive_half,
    gemm_p64_q248_um8_un4_simd4_equal_split,
    gemm_p64_q248_um8_un4_simd8_single_block,
    gemm_p64_q248_um8_un4_simd8_fixed_q,
    gemm_p64_q248_um8_un4_simd8_adaptive_half,
    gemm_p64_q248_um8_un4_simd8_equal_split,
    gemm_p64_q248_um16_un1_simd4_single_block,
    gemm_p64_q248_um16_un1_simd4_fixed_q,
    gemm_p64_q248_um16_un1_simd4_adaptive_half,
    gemm_p64_q248_um16_un1_simd4_equal_split,
    gemm_p64_q248_um16_un1_simd8_single_block,
    gemm_p64_q248_um16_un1_simd8_fixed_q,
    gemm_p64_q248_um16_un1_simd8_adaptive_half,
    gemm_p64_q248_um16_un1_simd8_equal_split,
    gemm_p64_q248_um16_un1_simd16_single_block,
    gemm_p64_q248_um16_un1_simd16_fixed_q,
    gemm_p64_q248_um16_un1_simd16_adaptive_half,
    gemm_p64_q248_um16_un1_simd16_equal_split,
    gemm_p64_q248_um16_un4_simd8_single_block,
    gemm_p64_q248_um16_un4_simd8_fixed_q,
    gemm_p64_q248_um16_un4_simd8_adaptive_half,
    gemm_p64_q248_um16_un4_simd8_equal_split,
    gemm_p64_q248_um16_un4_simd16_single_block,
    gemm_p64_q248_um16_un4_simd16_fixed_q,
    gemm_p64_q248_um16_un4_simd16_adaptive_half,
    gemm_p64_q248_um16_un4_simd16_equal_split,
    gemm_p64_q248_um32_un1_simd4_single_block,
    gemm_p64_q248_um32_un1_simd4_fixed_q,
    gemm_p64_q248_um32_un1_simd4_adaptive_half,
    gemm_p64_q248_um32_un1_simd4_equal_split,
    gemm_p64_q248_um32_un1_simd8_single_block,
    gemm_p64_q248_um32_un1_simd8_fixed_q,
    gemm_p64_q248_um32_un1_simd8_adaptive_half,
    gemm_p64_q248_um32_un1_simd8_equal_split,
    gemm_p64_q248_um32_un1_simd16_single_block,
    gemm_p64_q248_um32_un1_simd16_fixed_q,
    gemm_p64_q248_um32_un1_simd16_adaptive_half,
    gemm_p64_q248_um32_un1_simd16_equal_split,
    gemm_p64_q256_um1_un1_simd1_single_block,
    gemm_p64_q256_um1_un1_simd1_fixed_q,
    gemm_p64_q256_um1_un1_simd1_adaptive_half,
    gemm_p64_q256_um1_un1_simd1_equal_split,
    gemm_p64_q256_um1_un4_simd1_single_block,
    gemm_p64_q256_um1_un4_simd1_fixed_q,
    gemm_p64_q256_um1_un4_simd1_adaptive_half,
    gemm_p64_q256_um1_un4_simd1_equal_split,
    gemm_p64_q256_um4_un1_simd1_single_block,
    gemm_p64_q256_um4_un1_simd1_fixed_q,
    gemm_p64_q256_um4_un1_simd1_adaptive_half,
    gemm_p64_q256_um4_un1_simd1_equal_split,
    gemm_p64_q256_um4_un1_simd4_single_block,
    gemm_p64_q256_um4_un1_simd4_fixed_q,
    gemm_p64_q256_um4_un1_simd4_adaptive_half,
    gemm_p64_q256_um4_un1_simd4_equal_split,
    gemm_p64_q256_um4_un4_simd4_single_block,
    gemm_p64_q256_um4_un4_simd4_fixed_q,
    gemm_p64_q256_um4_un4_simd4_adaptive_half,
    gemm_p64_q256_um4_un4_simd4_equal_split,
    gemm_p64_q256_um8_un1_simd1_single_block,
    gemm_p64_q256_um8_un1_simd1_fixed_q,
    gemm_p64_q256_um8_un1_simd1_adaptive_half,
    gemm_p64_q256_um8_un1_simd1_equal_split,
    gemm_p64_q256_um8_un1_simd4_single_block,
    gemm_p64_q256_um8_un1_simd4_fixed_q,
    gemm_p64_q256_um8_un1_simd4_adaptive_half,
    gemm_p64_q256_um8_un1_simd4_equal_split,
    gemm_p64_q256_um8_un1_simd8_single_block,
    gemm_p64_q256_um8_un1_simd8_fixed_q,
    gemm_p64_q256_um8_un1_simd8_adaptive_half,
    gemm_p64_q256_um8_un1_simd8_equal_split,
    gemm_p64_q256_um8_un4_simd4_single_block,
    gemm_p64_q256_um8_un4_simd4_fixed_q,
    gemm_p64_q256_um8_un4_simd4_adaptive_half,
    gemm_p64_q256_um8_un4_simd4_equal_split,
    gemm_p64_q256_um8_un4_simd8_single_block,
    gemm_p64_q256_um8_un4_simd8_fixed_q,
    gemm_p64_q256_um8_un4_simd8_adaptive_half,
    gemm_p64_q256_um8_un4_simd8_equal_split,
    gemm_p64_q256_um16_un1_simd4_single_block,
    gemm_p64_q256_um16_un1_simd4_fixed_q,
    gemm_p64_q256_um16_un1_simd4_adaptive_half,
    gemm_p64_q256_um16_un1_simd4_equal_split,
    gemm_p64_q256_um16_un1_simd8_single_block,
    gemm_p64_q256_um16_un1_simd8_fixed_q,
    gemm_p64_q256_um16_un1_simd8_adaptive_half,
    gemm_p64_q256_um16_un1_simd8_equal_split,
    gemm_p64_q256_um16_un1_simd16_single_block,
    gemm_p64_q256_um16_un1_simd16_fixed_q,
    gemm_p64_q256_um16_un1_simd16_adaptive_half,
    gemm_p64_q256_um16_un1_simd16_equal_split,
    gemm_p64_q256_um16_un4_simd8_single_block,
    gemm_p64_q256_um16_un4_simd8_fixed_q,
    gemm_p64_q256_um16_un4_simd8_adaptive_half,
    gemm_p64_q256_um16_un4_simd8_equal_split,
    gemm_p64_q256_um16_un4_simd16_single_block,
    gemm_p64_q256_um16_un4_simd16_fixed_q,
    gemm_p64_q256_um16_un4_simd16_adaptive_half,
    gemm_p64_q256_um16_un4_simd16_equal_split,
    gemm_p64_q384_um1_un1_simd1_single_block,
    gemm_p64_q384_um1_un1_simd1_fixed_q,
    gemm_p64_q384_um1_un1_simd1_adaptive_half,
    gemm_p64_q384_um1_un1_simd1_equal_split,
    gemm_p64_q384_um1_un4_simd1_single_block,
    gemm_p64_q384_um1_un4_simd1_fixed_q,
    gemm_p64_q384_um1_un4_simd1_adaptive_half,
    gemm_p64_q384_um1_un4_simd1_equal_split,
    gemm_p64_q384_um4_un1_simd1_single_block,
    gemm_p64_q384_um4_un1_simd1_fixed_q,
    gemm_p64_q384_um4_un1_simd1_adaptive_half,
    gemm_p64_q384_um4_un1_simd1_equal_split,
    gemm_p64_q384_um4_un1_simd4_single_block,
    gemm_p64_q384_um4_un1_simd4_fixed_q,
    gemm_p64_q384_um4_un1_simd4_adaptive_half,
    gemm_p64_q384_um4_un1_simd4_equal_split,
    gemm_p64_q384_um4_un4_simd4_single_block,
    gemm_p64_q384_um4_un4_simd4_fixed_q,
    gemm_p64_q384_um4_un4_simd4_adaptive_half,
    gemm_p64_q384_um4_un4_simd4_equal_split,
    gemm_p64_q384_um8_un1_simd1_single_block,
    gemm_p64_q384_um8_un1_simd1_fixed_q,
    gemm_p64_q384_um8_un1_simd1_adaptive_half,
    gemm_p64_q384_um8_un1_simd1_equal_split,
    gemm_p64_q384_um8_un1_simd4_single_block,
    gemm_p64_q384_um8_un1_simd4_fixed_q,
    gemm_p64_q384_um8_un1_simd4_adaptive_half,
    gemm_p64_q384_um8_un1_simd4_equal_split,
    gemm_p64_q384_um8_un1_simd8_single_block,
    gemm_p64_q384_um8_un1_simd8_fixed_q,
    gemm_p64_q384_um8_un1_simd8_adaptive_half,
    gemm_p64_q384_um8_un1_simd8_equal_split,
    gemm_p64_q384_um8_un4_simd4_single_block,
    gemm_p64_q384_um8_un4_simd4_fixed_q,
    gemm_p64_q384_um8_un4_simd4_adaptive_half,
    gemm_p64_q384_um8_un4_simd4_equal_split,
    gemm_p64_q384_um8_un4_simd8_single_block,
    gemm_p64_q384_um8_un4_simd8_fixed_q,
    gemm_p64_q384_um8_un4_simd8_adaptive_half,
    gemm_p64_q384_um8_un4_simd8_equal_split,
    gemm_p64_q384_um16_un1_simd4_single_block,
    gemm_p64_q384_um16_un1_simd4_fixed_q,
    gemm_p64_q384_um16_un1_simd4_adaptive_half,
    gemm_p64_q384_um16_un1_simd4_equal_split,
    gemm_p64_q384_um16_un1_simd8_single_block,
    gemm_p64_q384_um16_un1_simd8_fixed_q,
    gemm_p64_q384_um16_un1_simd8_adaptive_half,
    gemm_p64_q384_um16_un1_simd8_equal_split,
    gemm_p64_q384_um16_un1_simd16_single_block,
    gemm_p64_q384_um16_un1_simd16_fixed_q,
    gemm_p64_q384_um16_un1_simd16_adaptive_half,
    gemm_p64_q384_um16_un1_simd16_equal_split,
    gemm_p64_q384_um16_un4_simd8_single_block,
    gemm_p64_q384_um16_un4_simd8_fixed_q,
    gemm_p64_q384_um16_un4_simd8_adaptive_half,
    gemm_p64_q384_um16_un4_simd8_equal_split,
    gemm_p64_q384_um16_un4_simd16_single_block,
    gemm_p64_q384_um16_un4_simd16_fixed_q,
    gemm_p64_q384_um16_un4_simd16_adaptive_half,
    gemm_p64_q384_um16_un4_simd16_equal_split,
    gemm_p64_q512_um1_un1_simd1_single_block,
    gemm_p64_q512_um1_un1_simd1_fixed_q,
    gemm_p64_q512_um1_un1_simd1_adaptive_half,
    gemm_p64_q512_um1_un1_simd1_equal_split,
    gemm_p64_q512_um1_un4_simd1_single_block,
    gemm_p64_q512_um1_un4_simd1_fixed_q,
    gemm_p64_q512_um1_un4_simd1_adaptive_half,
    gemm_p64_q512_um1_un4_simd1_equal_split,
    gemm_p64_q512_um4_un1_simd1_single_block,
    gemm_p64_q512_um4_un1_simd1_fixed_q,
    gemm_p64_q512_um4_un1_simd1_adaptive_half,
    gemm_p64_q512_um4_un1_simd1_equal_split,
    gemm_p64_q512_um4_un1_simd4_single_block,
    gemm_p64_q512_um4_un1_simd4_fixed_q,
    gemm_p64_q512_um4_un1_simd4_adaptive_half,
    gemm_p64_q512_um4_un1_simd4_equal_split,
    gemm_p64_q512_um4_un4_simd4_single_block,
    gemm_p64_q512_um4_un4_simd4_fixed_q,
    gemm_p64_q512_um4_un4_simd4_adaptive_half,
    gemm_p64_q512_um4_un4_simd4_equal_split,
    gemm_p64_q512_um8_un1_simd1_single_block,
    gemm_p64_q512_um8_un1_simd1_fixed_q,
    gemm_p64_q512_um8_un1_simd1_adaptive_half,
    gemm_p64_q512_um8_un1_simd1_equal_split,
    gemm_p64_q512_um8_un1_simd4_single_block,
    gemm_p64_q512_um8_un1_simd4_fixed_q,
    gemm_p64_q512_um8_un1_simd4_adaptive_half,
    gemm_p64_q512_um8_un1_simd4_equal_split,
    gemm_p64_q512_um8_un1_simd8_single_block,
    gemm_p64_q512_um8_un1_simd8_fixed_q,
    gemm_p64_q512_um8_un1_simd8_adaptive_half,
    gemm_p64_q512_um8_un1_simd8_equal_split,
    gemm_p64_q512_um8_un4_simd4_single_block,
    gemm_p64_q512_um8_un4_simd4_fixed_q,
    gemm_p64_q512_um8_un4_simd4_adaptive_half,
    gemm_p64_q512_um8_un4_simd4_equal_split,
    gemm_p64_q512_um8_un4_simd8_single_block,
    gemm_p64_q512_um8_un4_simd8_fixed_q,
    gemm_p64_q512_um8_un4_simd8_adaptive_half,
    gemm_p64_q512_um8_un4_simd8_equal_split,
    gemm_p128_q1_um1_un1_simd1_single_block,
    gemm_p128_q1_um1_un1_simd1_fixed_q,
    gemm_p128_q1_um1_un1_simd1_adaptive_half,
    gemm_p128_q1_um1_un1_simd1_equal_split,
    gemm_p128_q1_um1_un4_simd1_single_block,
    gemm_p128_q1_um1_un4_simd1_fixed_q,
    gemm_p128_q1_um1_un4_simd1_adaptive_half,
    gemm_p128_q1_um1_un4_simd1_equal_split,
    gemm_p128_q1_um4_un1_simd1_single_block,
    gemm_p128_q1_um4_un1_simd1_fixed_q,
    gemm_p128_q1_um4_un1_simd1_adaptive_half,
    gemm_p128_q1_um4_un1_simd1_equal_split,
    gemm_p128_q1_um4_un1_simd4_single_block,
    gemm_p128_q1_um4_un1_simd4_fixed_q,
    gemm_p128_q1_um4_un1_simd4_adaptive_half,
    gemm_p128_q1_um4_un1_simd4_equal_split,
    gemm_p128_q1_um4_un4_simd4_single_block,
    gemm_p128_q1_um4_un4_simd4_fixed_q,
    gemm_p128_q1_um4_un4_simd4_adaptive_half,
    gemm_p128_q1_um4_un4_simd4_equal_split,
    gemm_p128_q1_um8_un1_simd1_single_block,
    gemm_p128_q1_um8_un1_simd1_fixed_q,
    gemm_p128_q1_um8_un1_simd1_adaptive_half,
    gemm_p128_q1_um8_un1_simd1_equal_split,
    gemm_p128_q1_um8_un1_simd4_single_block,
    gemm_p128_q1_um8_un1_simd4_fixed_q,
    gemm_p128_q1_um8_un1_simd4_adaptive_half,
    gemm_p128_q1_um8_un1_simd4_equal_split,
    gemm_p128_q1_um8_un1_simd8_single_block,
    gemm_p128_q1_um8_un1_simd8_fixed_q,
    gemm_p128_q1_um8_un1_simd8_adaptive_half,
    gemm_p128_q1_um8_un1_simd8_equal_split,
    gemm_p128_q1_um8_un4_simd4_single_block,
    gemm_p128_q1_um8_un4_simd4_fixed_q,
    gemm_p128_q1_um8_un4_simd4_adaptive_half,
    gemm_p128_q1_um8_un4_simd4_equal_split,
    gemm_p128_q1_um8_un4_simd8_single_block,
    gemm_p128_q1_um8_un4_simd8_fixed_q,
    gemm_p128_q1_um8_un4_simd8_adaptive_half,
    gemm_p128_q1_um8_un4_simd8_equal_split,
    gemm_p128_q1_um16_un1_simd4_single_block,
    gemm_p128_q1_um16_un1_simd4_fixed_q,
    gemm_p128_q1_um16_un1_simd4_adaptive_half,
    gemm_p128_q1_um16_un1_simd4_equal_split,
    gemm_p128_q1_um16_un1_simd8_single_block,
    gemm_p128_q1_um16_un1_simd8_fixed_q,
    gemm_p128_q1_um16_un1_simd8_adaptive_half,
    gemm_p128_q1_um16_un1_simd8_equal_split,
    gemm_p128_q1_um16_un1_simd16_single_block,
    gemm_p128_q1_um16_un1_simd16_fixed_q,
    gemm_p128_q1_um16_un1_simd16_adaptive_half,
    gemm_p128_q1_um16_un1_simd16_equal_split,
    gemm_p128_q1_um16_un4_simd8_single_block,
    gemm_p128_q1_um16_un4_simd8_fixed_q,
    gemm_p128_q1_um16_un4_simd8_adaptive_half,
    gemm_p128_q1_um16_un4_simd8_equal_split,
    gemm_p128_q1_um16_un4_simd16_single_block,
    gemm_p128_q1_um16_un4_simd16_fixed_q,
    gemm_p128_q1_um16_un4_simd16_adaptive_half,
    gemm_p128_q1_um16_un4_simd16_equal_split,
    gemm_p128_q1_um32_un1_simd4_single_block,
    gemm_p128_q1_um32_un1_simd4_fixed_q,
    gemm_p128_q1_um32_un1_simd4_adaptive_half,
    gemm_p128_q1_um32_un1_simd4_equal_split,
    gemm_p128_q1_um32_un1_simd8_single_block,
    gemm_p128_q1_um32_un1_simd8_fixed_q,
    gemm_p128_q1_um32_un1_simd8_adaptive_half,
    gemm_p128_q1_um32_un1_simd8_equal_split,
    gemm_p128_q1_um32_un1_simd16_single_block,
    gemm_p128_q1_um32_un1_simd16_fixed_q,
    gemm_p128_q1_um32_un1_simd16_adaptive_half,
    gemm_p128_q1_um32_un1_simd16_equal_split,
    gemm_p128_q1_um32_un4_simd16_single_block,
    gemm_p128_q1_um32_un4_simd16_fixed_q,
    gemm_p128_q1_um32_un4_simd16_adaptive_half,
    gemm_p128_q1_um32_un4_simd16_equal_split,
    gemm_p128_q64_um1_un1_simd1_single_block,
    gemm_p128_q64_um1_un1_simd1_fixed_q,
    gemm_p128_q64_um1_un1_simd1_adaptive_half,
    gemm_p128_q64_um1_un1_simd1_equal_split,
    gemm_p128_q64_um1_un4_simd1_single_block,
    gemm_p128_q64_um1_un4_simd1_fixed_q,
    gemm_p128_q64_um1_un4_simd1_adaptive_half,
    gemm_p128_q64_um1_un4_simd1_equal_split,
    gemm_p128_q64_um4_un1_simd1_single_block,
    gemm_p128_q64_um4_un1_simd1_fixed_q,
    gemm_p128_q64_um4_un1_simd1_adaptive_half,
    gemm_p128_q64_um4_un1_simd1_equal_split,
    gemm_p128_q64_um4_un1_simd4_single_block,
    gemm_p128_q64_um4_un1_simd4_fixed_q,
    gemm_p128_q64_um4_un1_simd4_adaptive_half,
    gemm_p128_q64_um4_un1_simd4_equal_split,
    gemm_p128_q64_um4_un4_simd4_single_block,
    gemm_p128_q64_um4_un4_simd4_fixed_q,
    gemm_p128_q64_um4_un4_simd4_adaptive_half,
    gemm_p128_q64_um4_un4_simd4_equal_split,
    gemm_p128_q64_um8_un1_simd1_single_block,
    gemm_p128_q64_um8_un1_simd1_fixed_q,
    gemm_p128_q64_um8_un1_simd1_adaptive_half,
    gemm_p128_q64_um8_un1_simd1_equal_split,
    gemm_p128_q64_um8_un1_simd4_single_block,
    gemm_p128_q64_um8_un1_simd4_fixed_q,
    gemm_p128_q64_um8_un1_simd4_adaptive_half,
    gemm_p128_q64_um8_un1_simd4_equal_split,
    gemm_p128_q64_um8_un1_simd8_single_block,
    gemm_p128_q64_um8_un1_simd8_fixed_q,
    gemm_p128_q64_um8_un1_simd8_adaptive_half,
    gemm_p128_q64_um8_un1_simd8_equal_split,
    gemm_p128_q64_um8_un4_simd4_single_block,
    gemm_p128_q64_um8_un4_simd4_fixed_q,
    gemm_p128_q64_um8_un4_simd4_adaptive_half,
    gemm_p128_q64_um8_un4_simd4_equal_split,
    gemm_p128_q64_um8_un4_simd8_single_block,
    gemm_p128_q64_um8_un4_simd8_fixed_q,
    gemm_p128_q64_um8_un4_simd8_adaptive_half,
    gemm_p128_q64_um8_un4_simd8_equal_split,
    gemm_p128_q64_um16_un1_simd4_single_block,
    gemm_p128_q64_um16_un1_simd4_fixed_q,
    gemm_p128_q64_um16_un1_simd4_adaptive_half,
    gemm_p128_q64_um16_un1_simd4_equal_split,
    gemm_p128_q64_um16_un1_simd8_single_block,
    gemm_p128_q64_um16_un1_simd8_fixed_q,
    gemm_p128_q64_um16_un1_simd8_adaptive_half,
    gemm_p128_q64_um16_un1_simd8_equal_split,
    gemm_p128_q64_um16_un1_simd16_single_block,
    gemm_p128_q64_um16_un1_simd16_fixed_q,
    gemm_p128_q64_um16_un1_simd16_adaptive_half,
    gemm_p128_q64_um16_un1_simd16_equal_split,
    gemm_p128_q64_um16_un4_simd8_single_block,
    gemm_p128_q64_um16_un4_simd8_fixed_q,
    gemm_p128_q64_um16_un4_simd8_adaptive_half,
    gemm_p128_q64_um16_un4_simd8_equal_split,
    gemm_p128_q64_um16_un4_simd16_single_block,
    gemm_p128_q64_um16_un4_simd16_fixed_q,
    gemm_p128_q64_um16_un4_simd16_adaptive_half,
    gemm_p128_q64_um16_un4_simd16_equal_split,
    gemm_p128_q64_um32_un1_simd4_single_block,
    gemm_p128_q64_um32_un1_simd4_fixed_q,
    gemm_p128_q64_um32_un1_simd4_adaptive_half,
    gemm_p128_q64_um32_un1_simd4_equal_split,
    gemm_p128_q64_um32_un1_simd8_single_block,
    gemm_p128_q64_um32_un1_simd8_fixed_q,
    gemm_p128_q64_um32_un1_simd8_adaptive_half,
    gemm_p128_q64_um32_un1_simd8_equal_split,
    gemm_p128_q64_um32_un1_simd16_single_block,
    gemm_p128_q64_um32_un1_simd16_fixed_q,
    gemm_p128_q64_um32_un1_simd16_adaptive_half,
    gemm_p128_q64_um32_un1_simd16_equal_split,
    gemm_p128_q64_um32_un4_simd16_single_block,
    gemm_p128_q64_um32_un4_simd16_fixed_q,
    gemm_p128_q64_um32_un4_simd16_adaptive_half,
    gemm_p128_q64_um32_un4_simd16_equal_split,
    gemm_p128_q128_um1_un1_simd1_single_block,
    gemm_p128_q128_um1_un1_simd1_fixed_q,
    gemm_p128_q128_um1_un1_simd1_adaptive_half,
    gemm_p128_q128_um1_un1_simd1_equal_split,
    gemm_p128_q128_um1_un4_simd1_single_block,
    gemm_p128_q128_um1_un4_simd1_fixed_q,
    gemm_p128_q128_um1_un4_simd1_adaptive_half,
    gemm_p128_q128_um1_un4_simd1_equal_split,
    gemm_p128_q128_um4_un1_simd1_single_block,
    gemm_p128_q128_um4_un1_simd1_fixed_q,
    gemm_p128_q128_um4_un1_simd1_adaptive_half,
    gemm_p128_q128_um4_un1_simd1_equal_split,
    gemm_p128_q128_um4_un1_simd4_single_block,
    gemm_p128_q128_um4_un1_simd4_fixed_q,
    gemm_p128_q128_um4_un1_simd4_adaptive_half,
    gemm_p128_q128_um4_un1_simd4_equal_split,
    gemm_p128_q128_um4_un4_simd4_single_block,
    gemm_p128_q128_um4_un4_simd4_fixed_q,
    gemm_p128_q128_um4_un4_simd4_adaptive_half,
    gemm_p128_q128_um4_un4_simd4_equal_split,
    gemm_p128_q128_um8_un1_simd1_single_block,
    gemm_p128_q128_um8_un1_simd1_fixed_q,
    gemm_p128_q128_um8_un1_simd1_adaptive_half,
    gemm_p128_q128_um8_un1_simd1_equal_split,
    gemm_p128_q128_um8_un1_simd4_single_block,
    gemm_p128_q128_um8_un1_simd4_fixed_q,
    gemm_p128_q128_um8_un1_simd4_adaptive_half,
    gemm_p128_q128_um8_un1_simd4_equal_split,
    gemm_p128_q128_um8_un1_simd8_single_block,
    gemm_p128_q128_um8_un1_simd8_fixed_q,
    gemm_p128_q128_um8_un1_simd8_adaptive_half,
    gemm_p128_q128_um8_un1_simd8_equal_split,
    gemm_p128_q128_um8_un4_simd4_single_block,
    gemm_p128_q128_um8_un4_simd4_fixed_q,
    gemm_p128_q128_um8_un4_simd4_adaptive_half,
    gemm_p128_q128_um8_un4_simd4_equal_split,
    gemm_p128_q128_um8_un4_simd8_single_block,
    gemm_p128_q128_um8_un4_simd8_fixed_q,
    gemm_p128_q128_um8_un4_simd8_adaptive_half,
    gemm_p128_q128_um8_un4_simd8_equal_split,
    gemm_p128_q128_um16_un1_simd4_single_block,
    gemm_p128_q128_um16_un1_simd4_fixed_q,
    gemm_p128_q128_um16_un1_simd4_adaptive_half,
    gemm_p128_q128_um16_un1_simd4_equal_split,
    gemm_p128_q128_um16_un1_simd8_single_block,
    gemm_p128_q128_um16_un1_simd8_fixed_q,
    gemm_p128_q128_um16_un1_simd8_adaptive_half,
    gemm_p128_q128_um16_un1_simd8_equal_split,
    gemm_p128_q128_um16_un1_simd16_single_block,
    gemm_p128_q128_um16_un1_simd16_fixed_q,
    gemm_p128_q128_um16_un1_simd16_adaptive_half,
    gemm_p128_q128_um16_un1_simd16_equal_split,
    gemm_p128_q128_um16_un4_simd8_single_block,
    gemm_p128_q128_um16_un4_simd8_fixed_q,
    gemm_p128_q128_um16_un4_simd8_adaptive_half,
    gemm_p128_q128_um16_un4_simd8_equal_split,
    gemm_p128_q128_um16_un4_simd16_single_block,
    gemm_p128_q128_um16_un4_simd16_fixed_q,
    gemm_p128_q128_um16_un4_simd16_adaptive_half,
    gemm_p128_q128_um16_un4_simd16_equal_split,
    gemm_p128_q128_um32_un1_simd4_single_block,
    gemm_p128_q128_um32_un1_simd4_fixed_q,
    gemm_p128_q128_um32_un1_simd4_adaptive_half,
    gemm_p128_q128_um32_un1_simd4_equal_split,
    gemm_p128_q128_um32_un1_simd8_single_block,
    gemm_p128_q128_um32_un1_simd8_fixed_q,
    gemm_p128_q128_um32_un1_simd8_adaptive_half,
    gemm_p128_q128_um32_un1_simd8_equal_split,
    gemm_p128_q128_um32_un1_simd16_single_block,
    gemm_p128_q128_um32_un1_simd16_fixed_q,
    gemm_p128_q128_um32_un1_simd16_adaptive_half,
    gemm_p128_q128_um32_un1_simd16_equal_split,
    gemm_p128_q128_um32_un4_simd16_single_block,
    gemm_p128_q128_um32_un4_simd16_fixed_q,
    gemm_p128_q128_um32_un4_simd16_adaptive_half,
    gemm_p128_q128_um32_un4_simd16_equal_split,
    gemm_p128_q192_um1_un1_simd1_single_block,
    gemm_p128_q192_um1_un1_simd1_fixed_q,
    gemm_p128_q192_um1_un1_simd1_adaptive_half,
    gemm_p128_q192_um1_un1_simd1_equal_split,
    gemm_p128_q192_um1_un4_simd1_single_block,
    gemm_p128_q192_um1_un4_simd1_fixed_q,
    gemm_p128_q192_um1_un4_simd1_adaptive_half,
    gemm_p128_q192_um1_un4_simd1_equal_split,
    gemm_p128_q192_um4_un1_simd1_single_block,
    gemm_p128_q192_um4_un1_simd1_fixed_q,
    gemm_p128_q192_um4_un1_simd1_adaptive_half,
    gemm_p128_q192_um4_un1_simd1_equal_split,
    gemm_p128_q192_um4_un1_simd4_single_block,
    gemm_p128_q192_um4_un1_simd4_fixed_q,
    gemm_p128_q192_um4_un1_simd4_adaptive_half,
    gemm_p128_q192_um4_un1_simd4_equal_split,
    gemm_p128_q192_um4_un4_simd4_single_block,
    gemm_p128_q192_um4_un4_simd4_fixed_q,
    gemm_p128_q192_um4_un4_simd4_adaptive_half,
    gemm_p128_q192_um4_un4_simd4_equal_split,
    gemm_p128_q192_um8_un1_simd1_single_block,
    gemm_p128_q192_um8_un1_simd1_fixed_q,
    gemm_p128_q192_um8_un1_simd1_adaptive_half,
    gemm_p128_q192_um8_un1_simd1_equal_split,
    gemm_p128_q192_um8_un1_simd4_single_block,
    gemm_p128_q192_um8_un1_simd4_fixed_q,
    gemm_p128_q192_um8_un1_simd4_adaptive_half,
    gemm_p128_q192_um8_un1_simd4_equal_split,
    gemm_p128_q192_um8_un1_simd8_single_block,
    gemm_p128_q192_um8_un1_simd8_fixed_q,
    gemm_p128_q192_um8_un1_simd8_adaptive_half,
    gemm_p128_q192_um8_un1_simd8_equal_split,
    gemm_p128_q192_um8_un4_simd4_single_block,
    gemm_p128_q192_um8_un4_simd4_fixed_q,
    gemm_p128_q192_um8_un4_simd4_adaptive_half,
    gemm_p128_q192_um8_un4_simd4_equal_split,
    gemm_p128_q192_um8_un4_simd8_single_block,
    gemm_p128_q192_um8_un4_simd8_fixed_q,
    gemm_p128_q192_um8_un4_simd8_adaptive_half,
    gemm_p128_q192_um8_un4_simd8_equal_split,
    gemm_p128_q192_um16_un1_simd4_single_block,
    gemm_p128_q192_um16_un1_simd4_fixed_q,
    gemm_p128_q192_um16_un1_simd4_adaptive_half,
    gemm_p128_q192_um16_un1_simd4_equal_split,
    gemm_p128_q192_um16_un1_simd8_single_block,
    gemm_p128_q192_um16_un1_simd8_fixed_q,
    gemm_p128_q192_um16_un1_simd8_adaptive_half,
    gemm_p128_q192_um16_un1_simd8_equal_split,
    gemm_p128_q192_um16_un1_simd16_single_block,
    gemm_p128_q192_um16_un1_simd16_fixed_q,
    gemm_p128_q192_um16_un1_simd16_adaptive_half,
    gemm_p128_q192_um16_un1_simd16_equal_split,
    gemm_p128_q192_um16_un4_simd8_single_block,
    gemm_p128_q192_um16_un4_simd8_fixed_q,
    gemm_p128_q192_um16_un4_simd8_adaptive_half,
    gemm_p128_q192_um16_un4_simd8_equal_split,
    gemm_p128_q192_um16_un4_simd16_single_block,
    gemm_p128_q192_um16_un4_simd16_fixed_q,
    gemm_p128_q192_um16_un4_simd16_adaptive_half,
    gemm_p128_q192_um16_un4_simd16_equal_split,
    gemm_p128_q192_um32_un1_simd4_single_block,
    gemm_p128_q192_um32_un1_simd4_fixed_q,
    gemm_p128_q192_um32_un1_simd4_adaptive_half,
    gemm_p128_q192_um32_un1_simd4_equal_split,
    gemm_p128_q192_um32_un1_simd8_single_block,
    gemm_p128_q192_um32_un1_simd8_fixed_q,
    gemm_p128_q192_um32_un1_simd8_adaptive_half,
    gemm_p128_q192_um32_un1_simd8_equal_split,
    gemm_p128_q192_um32_un1_simd16_single_block,
    gemm_p128_q192_um32_un1_simd16_fixed_q,
    gemm_p128_q192_um32_un1_simd16_adaptive_half,
    gemm_p128_q192_um32_un1_simd16_equal_split,
    gemm_p128_q192_um32_un4_simd16_single_block,
    gemm_p128_q192_um32_un4_simd16_fixed_q,
    gemm_p128_q192_um32_un4_simd16_adaptive_half,
    gemm_p128_q192_um32_un4_simd16_equal_split,
    gemm_p128_q240_um1_un1_simd1_single_block,
    gemm_p128_q240_um1_un1_simd1_fixed_q,
    gemm_p128_q240_um1_un1_simd1_adaptive_half,
    gemm_p128_q240_um1_un1_simd1_equal_split,
    gemm_p128_q240_um1_un4_simd1_single_block,
    gemm_p128_q240_um1_un4_simd1_fixed_q,
    gemm_p128_q240_um1_un4_simd1_adaptive_half,
    gemm_p128_q240_um1_un4_simd1_equal_split,
    gemm_p128_q240_um4_un1_simd1_single_block,
    gemm_p128_q240_um4_un1_simd1_fixed_q,
    gemm_p128_q240_um4_un1_simd1_adaptive_half,
    gemm_p128_q240_um4_un1_simd1_equal_split,
    gemm_p128_q240_um4_un1_simd4_single_block,
    gemm_p128_q240_um4_un1_simd4_fixed_q,
    gemm_p128_q240_um4_un1_simd4_adaptive_half,
    gemm_p128_q240_um4_un1_simd4_equal_split,
    gemm_p128_q240_um4_un4_simd4_single_block,
    gemm_p128_q240_um4_un4_simd4_fixed_q,
    gemm_p128_q240_um4_un4_simd4_adaptive_half,
    gemm_p128_q240_um4_un4_simd4_equal_split,
    gemm_p128_q240_um8_un1_simd1_single_block,
    gemm_p128_q240_um8_un1_simd1_fixed_q,
    gemm_p128_q240_um8_un1_simd1_adaptive_half,
    gemm_p128_q240_um8_un1_simd1_equal_split,
    gemm_p128_q240_um8_un1_simd4_single_block,
    gemm_p128_q240_um8_un1_simd4_fixed_q,
    gemm_p128_q240_um8_un1_simd4_adaptive_half,
    gemm_p128_q240_um8_un1_simd4_equal_split,
    gemm_p128_q240_um8_un1_simd8_single_block,
    gemm_p128_q240_um8_un1_simd8_fixed_q,
    gemm_p128_q240_um8_un1_simd8_adaptive_half,
    gemm_p128_q240_um8_un1_simd8_equal_split,
    gemm_p128_q240_um8_un4_simd4_single_block,
    gemm_p128_q240_um8_un4_simd4_fixed_q,
    gemm_p128_q240_um8_un4_simd4_adaptive_half,
    gemm_p128_q240_um8_un4_simd4_equal_split,
    gemm_p128_q240_um8_un4_simd8_single_block,
    gemm_p128_q240_um8_un4_simd8_fixed_q,
    gemm_p128_q240_um8_un4_simd8_adaptive_half,
    gemm_p128_q240_um8_un4_simd8_equal_split,
    gemm_p128_q240_um16_un1_simd4_single_block,
    gemm_p128_q240_um16_un1_simd4_fixed_q,
    gemm_p128_q240_um16_un1_simd4_adaptive_half,
    gemm_p128_q240_um16_un1_simd4_equal_split,
    gemm_p128_q240_um16_un1_simd8_single_block,
    gemm_p128_q240_um16_un1_simd8_fixed_q,
    gemm_p128_q240_um16_un1_simd8_adaptive_half,
    gemm_p128_q240_um16_un1_simd8_equal_split,
    gemm_p128_q240_um16_un1_simd16_single_block,
    gemm_p128_q240_um16_un1_simd16_fixed_q,
    gemm_p128_q240_um16_un1_simd16_adaptive_half,
    gemm_p128_q240_um16_un1_simd16_equal_split,
    gemm_p128_q240_um16_un4_simd8_single_block,
    gemm_p128_q240_um16_un4_simd8_fixed_q,
    gemm_p128_q240_um16_un4_simd8_adaptive_half,
    gemm_p128_q240_um16_un4_simd8_equal_split,
    gemm_p128_q240_um16_un4_simd16_single_block,
    gemm_p128_q240_um16_un4_simd16_fixed_q,
    gemm_p128_q240_um16_un4_simd16_adaptive_half,
    gemm_p128_q240_um16_un4_simd16_equal_split,
    gemm_p128_q240_um32_un1_simd4_single_block,
    gemm_p128_q240_um32_un1_simd4_fixed_q,
    gemm_p128_q240_um32_un1_simd4_adaptive_half,
    gemm_p128_q240_um32_un1_simd4_equal_split,
    gemm_p128_q240_um32_un1_simd8_single_block,
    gemm_p128_q240_um32_un1_simd8_fixed_q,
    gemm_p128_q240_um32_un1_simd8_adaptive_half,
    gemm_p128_q240_um32_un1_simd8_equal_split,
    gemm_p128_q240_um32_un1_simd16_single_block,
    gemm_p128_q240_um32_un1_simd16_fixed_q,
    gemm_p128_q240_um32_un1_simd16_adaptive_half,
    gemm_p128_q240_um32_un1_simd16_equal_split,
    gemm_p128_q248_um1_un1_simd1_single_block,
    gemm_p128_q248_um1_un1_simd1_fixed_q,
    gemm_p128_q248_um1_un1_simd1_adaptive_half,
    gemm_p128_q248_um1_un1_simd1_equal_split,
    gemm_p128_q248_um1_un4_simd1_single_block,
    gemm_p128_q248_um1_un4_simd1_fixed_q,
    gemm_p128_q248_um1_un4_simd1_adaptive_half,
    gemm_p128_q248_um1_un4_simd1_equal_split,
    gemm_p128_q248_um4_un1_simd1_single_block,
    gemm_p128_q248_um4_un1_simd1_fixed_q,
    gemm_p128_q248_um4_un1_simd1_adaptive_half,
    gemm_p128_q248_um4_un1_simd1_equal_split,
    gemm_p128_q248_um4_un1_simd4_single_block,
    gemm_p128_q248_um4_un1_simd4_fixed_q,
    gemm_p128_q248_um4_un1_simd4_adaptive_half,
    gemm_p128_q248_um4_un1_simd4_equal_split,
    gemm_p128_q248_um4_un4_simd4_single_block,
    gemm_p128_q248_um4_un4_simd4_fixed_q,
    gemm_p128_q248_um4_un4_simd4_adaptive_half,
    gemm_p128_q248_um4_un4_simd4_equal_split,
    gemm_p128_q248_um8_un1_simd1_single_block,
    gemm_p128_q248_um8_un1_simd1_fixed_q,
    gemm_p128_q248_um8_un1_simd1_adaptive_half,
    gemm_p128_q248_um8_un1_simd1_equal_split,
    gemm_p128_q248_um8_un1_simd4_single_block,
    gemm_p128_q248_um8_un1_simd4_fixed_q,
    gemm_p128_q248_um8_un1_simd4_adaptive_half,
    gemm_p128_q248_um8_un1_simd4_equal_split,
    gemm_p128_q248_um8_un1_simd8_single_block,
    gemm_p128_q248_um8_un1_simd8_fixed_q,
    gemm_p128_q248_um8_un1_simd8_adaptive_half,
    gemm_p128_q248_um8_un1_simd8_equal_split,
    gemm_p128_q248_um8_un4_simd4_single_block,
    gemm_p128_q248_um8_un4_simd4_fixed_q,
    gemm_p128_q248_um8_un4_simd4_adaptive_half,
    gemm_p128_q248_um8_un4_simd4_equal_split,
    gemm_p128_q248_um8_un4_simd8_single_block,
    gemm_p128_q248_um8_un4_simd8_fixed_q,
    gemm_p128_q248_um8_un4_simd8_adaptive_half,
    gemm_p128_q248_um8_un4_simd8_equal_split,
    gemm_p128_q248_um16_un1_simd4_single_block,
    gemm_p128_q248_um16_un1_simd4_fixed_q,
    gemm_p128_q248_um16_un1_simd4_adaptive_half,
    gemm_p128_q248_um16_un1_simd4_equal_split,
    gemm_p128_q248_um16_un1_simd8_single_block,
    gemm_p128_q248_um16_un1_simd8_fixed_q,
    gemm_p128_q248_um16_un1_simd8_adaptive_half,
    gemm_p128_q248_um16_un1_simd8_equal_split,
    gemm_p128_q248_um16_un1_simd16_single_block,
    gemm_p128_q248_um16_un1_simd16_fixed_q,
    gemm_p128_q248_um16_un1_simd16_adaptive_half,
    gemm_p128_q248_um16_un1_simd16_equal_split,
    gemm_p128_q248_um16_un4_simd8_single_block,
    gemm_p128_q248_um16_un4_simd8_fixed_q,
    gemm_p128_q248_um16_un4_simd8_adaptive_half,
    gemm_p128_q248_um16_un4_simd8_equal_split,
    gemm_p128_q248_um16_un4_simd16_single_block,
    gemm_p128_q248_um16_un4_simd16_fixed_q,
    gemm_p128_q248_um16_un4_simd16_adaptive_half,
    gemm_p128_q248_um16_un4_simd16_equal_split,
    gemm_p128_q248_um32_un1_simd4_single_block,
    gemm_p128_q248_um32_un1_simd4_fixed_q,
    gemm_p128_q248_um32_un1_simd4_adaptive_half,
    gemm_p128_q248_um32_un1_simd4_equal_split,
    gemm_p128_q248_um32_un1_simd8_single_block,
    gemm_p128_q248_um32_un1_simd8_fixed_q,
    gemm_p128_q248_um32_un1_simd8_adaptive_half,
    gemm_p128_q248_um32_un1_simd8_equal_split,
    gemm_p128_q248_um32_un1_simd16_single_block,
    gemm_p128_q248_um32_un1_simd16_fixed_q,
    gemm_p128_q248_um32_un1_simd16_adaptive_half,
    gemm_p128_q248_um32_un1_simd16_equal_split,
    gemm_p128_q256_um1_un1_simd1_single_block,
    gemm_p128_q256_um1_un1_simd1_fixed_q,
    gemm_p128_q256_um1_un1_simd1_adaptive_half,
    gemm_p128_q256_um1_un1_simd1_equal_split,
    gemm_p128_q256_um1_un4_simd1_single_block,
    gemm_p128_q256_um1_un4_simd1_fixed_q,
    gemm_p128_q256_um1_un4_simd1_adaptive_half,
    gemm_p128_q256_um1_un4_simd1_equal_split,
    gemm_p128_q256_um4_un1_simd1_single_block,
    gemm_p128_q256_um4_un1_simd1_fixed_q,
    gemm_p128_q256_um4_un1_simd1_adaptive_half,
    gemm_p128_q256_um4_un1_simd1_equal_split,
    gemm_p128_q256_um4_un1_simd4_single_block,
    gemm_p128_q256_um4_un1_simd4_fixed_q,
    gemm_p128_q256_um4_un1_simd4_adaptive_half,
    gemm_p128_q256_um4_un1_simd4_equal_split,
    gemm_p128_q256_um4_un4_simd4_single_block,
    gemm_p128_q256_um4_un4_simd4_fixed_q,
    gemm_p128_q256_um4_un4_simd4_adaptive_half,
    gemm_p128_q256_um4_un4_simd4_equal_split,
    gemm_p128_q256_um8_un1_simd1_single_block,
    gemm_p128_q256_um8_un1_simd1_fixed_q,
    gemm_p128_q256_um8_un1_simd1_adaptive_half,
    gemm_p128_q256_um8_un1_simd1_equal_split,
    gemm_p128_q256_um8_un1_simd4_single_block,
    gemm_p128_q256_um8_un1_simd4_fixed_q,
    gemm_p128_q256_um8_un1_simd4_adaptive_half,
    gemm_p128_q256_um8_un1_simd4_equal_split,
    gemm_p128_q256_um8_un1_simd8_single_block,
    gemm_p128_q256_um8_un1_simd8_fixed_q,
    gemm_p128_q256_um8_un1_simd8_adaptive_half,
    gemm_p128_q256_um8_un1_simd8_equal_split,
    gemm_p128_q256_um8_un4_simd4_single_block,
    gemm_p128_q256_um8_un4_simd4_fixed_q,
    gemm_p128_q256_um8_un4_simd4_adaptive_half,
    gemm_p128_q256_um8_un4_simd4_equal_split,
    gemm_p128_q256_um8_un4_simd8_single_block,
    gemm_p128_q256_um8_un4_simd8_fixed_q,
    gemm_p128_q256_um8_un4_simd8_adaptive_half,
    gemm_p128_q256_um8_un4_simd8_equal_split,
    gemm_p128_q256_um16_un1_simd4_single_block,
    gemm_p128_q256_um16_un1_simd4_fixed_q,
    gemm_p128_q256_um16_un1_simd4_adaptive_half,
    gemm_p128_q256_um16_un1_simd4_equal_split,
    gemm_p128_q256_um16_un1_simd8_single_block,
    gemm_p128_q256_um16_un1_simd8_fixed_q,
    gemm_p128_q256_um16_un1_simd8_adaptive_half,
    gemm_p128_q256_um16_un1_simd8_equal_split,
    gemm_p128_q256_um16_un1_simd16_single_block,
    gemm_p128_q256_um16_un1_simd16_fixed_q,
    gemm_p128_q256_um16_un1_simd16_adaptive_half,
    gemm_p128_q256_um16_un1_simd16_equal_split,
    gemm_p128_q256_um16_un4_simd8_single_block,
    gemm_p128_q256_um16_un4_simd8_fixed_q,
    gemm_p128_q256_um16_un4_simd8_adaptive_half,
    gemm_p128_q256_um16_un4_simd8_equal_split,
    gemm_p128_q256_um16_un4_simd16_single_block,
    gemm_p128_q256_um16_un4_simd16_fixed_q,
    gemm_p128_q256_um16_un4_simd16_adaptive_half,
    gemm_p128_q256_um16_un4_simd16_equal_split,
    gemm_p128_q384_um1_un1_simd1_single_block,
    gemm_p128_q384_um1_un1_simd1_fixed_q,
    gemm_p128_q384_um1_un1_simd1_adaptive_half,
    gemm_p128_q384_um1_un1_simd1_equal_split,
    gemm_p128_q384_um1_un4_simd1_single_block,
    gemm_p128_q384_um1_un4_simd1_fixed_q,
    gemm_p128_q384_um1_un4_simd1_adaptive_half,
    gemm_p128_q384_um1_un4_simd1_equal_split,
    gemm_p128_q384_um4_un1_simd1_single_block,
    gemm_p128_q384_um4_un1_simd1_fixed_q,
    gemm_p128_q384_um4_un1_simd1_adaptive_half,
    gemm_p128_q384_um4_un1_simd1_equal_split,
    gemm_p128_q384_um4_un1_simd4_single_block,
    gemm_p128_q384_um4_un1_simd4_fixed_q,
    gemm_p128_q384_um4_un1_simd4_adaptive_half,
    gemm_p128_q384_um4_un1_simd4_equal_split,
    gemm_p128_q384_um4_un4_simd4_single_block,
    gemm_p128_q384_um4_un4_simd4_fixed_q,
    gemm_p128_q384_um4_un4_simd4_adaptive_half,
    gemm_p128_q384_um4_un4_simd4_equal_split,
    gemm_p128_q384_um8_un1_simd1_single_block,
    gemm_p128_q384_um8_un1_simd1_fixed_q,
    gemm_p128_q384_um8_un1_simd1_adaptive_half,
    gemm_p128_q384_um8_un1_simd1_equal_split,
    gemm_p128_q384_um8_un1_simd4_single_block,
    gemm_p128_q384_um8_un1_simd4_fixed_q,
    gemm_p128_q384_um8_un1_simd4_adaptive_half,
    gemm_p128_q384_um8_un1_simd4_equal_split,
    gemm_p128_q384_um8_un1_simd8_single_block,
    gemm_p128_q384_um8_un1_simd8_fixed_q,
    gemm_p128_q384_um8_un1_simd8_adaptive_half,
    gemm_p128_q384_um8_un1_simd8_equal_split,
    gemm_p128_q384_um8_un4_simd4_single_block,
    gemm_p128_q384_um8_un4_simd4_fixed_q,
    gemm_p128_q384_um8_un4_simd4_adaptive_half,
    gemm_p128_q384_um8_un4_simd4_equal_split,
    gemm_p128_q384_um8_un4_simd8_single_block,
    gemm_p128_q384_um8_un4_simd8_fixed_q,
    gemm_p128_q384_um8_un4_simd8_adaptive_half,
    gemm_p128_q384_um8_un4_simd8_equal_split,
    gemm_p128_q384_um16_un1_simd4_single_block,
    gemm_p128_q384_um16_un1_simd4_fixed_q,
    gemm_p128_q384_um16_un1_simd4_adaptive_half,
    gemm_p128_q384_um16_un1_simd4_equal_split,
    gemm_p128_q384_um16_un1_simd8_single_block,
    gemm_p128_q384_um16_un1_simd8_fixed_q,
    gemm_p128_q384_um16_un1_simd8_adaptive_half,
    gemm_p128_q384_um16_un1_simd8_equal_split,
    gemm_p128_q384_um16_un1_simd16_single_block,
    gemm_p128_q384_um16_un1_simd16_fixed_q,
    gemm_p128_q384_um16_un1_simd16_adaptive_half,
    gemm_p128_q384_um16_un1_simd16_equal_split,
    gemm_p128_q384_um16_un4_simd8_single_block,
    gemm_p128_q384_um16_un4_simd8_fixed_q,
    gemm_p128_q384_um16_un4_simd8_adaptive_half,
    gemm_p128_q384_um16_un4_simd8_equal_split,
    gemm_p128_q384_um16_un4_simd16_single_block,
    gemm_p128_q384_um16_un4_simd16_fixed_q,
    gemm_p128_q384_um16_un4_simd16_adaptive_half,
    gemm_p128_q384_um16_un4_simd16_equal_split,
    gemm_p128_q512_um1_un1_simd1_single_block,
    gemm_p128_q512_um1_un1_simd1_fixed_q,
    gemm_p128_q512_um1_un1_simd1_adaptive_half,
    gemm_p128_q512_um1_un1_simd1_equal_split,
    gemm_p128_q512_um1_un4_simd1_single_block,
    gemm_p128_q512_um1_un4_simd1_fixed_q,
    gemm_p128_q512_um1_un4_simd1_adaptive_half,
    gemm_p128_q512_um1_un4_simd1_equal_split,
    gemm_p128_q512_um4_un1_simd1_single_block,
    gemm_p128_q512_um4_un1_simd1_fixed_q,
    gemm_p128_q512_um4_un1_simd1_adaptive_half,
    gemm_p128_q512_um4_un1_simd1_equal_split,
    gemm_p128_q512_um4_un1_simd4_single_block,
    gemm_p128_q512_um4_un1_simd4_fixed_q,
    gemm_p128_q512_um4_un1_simd4_adaptive_half,
    gemm_p128_q512_um4_un1_simd4_equal_split,
    gemm_p128_q512_um4_un4_simd4_single_block,
    gemm_p128_q512_um4_un4_simd4_fixed_q,
    gemm_p128_q512_um4_un4_simd4_adaptive_half,
    gemm_p128_q512_um4_un4_simd4_equal_split,
    gemm_p128_q512_um8_un1_simd1_single_block,
    gemm_p128_q512_um8_un1_simd1_fixed_q,
    gemm_p128_q512_um8_un1_simd1_adaptive_half,
    gemm_p128_q512_um8_un1_simd1_equal_split,
    gemm_p128_q512_um8_un1_simd4_single_block,
    gemm_p128_q512_um8_un1_simd4_fixed_q,
    gemm_p128_q512_um8_un1_simd4_adaptive_half,
    gemm_p128_q512_um8_un1_simd4_equal_split,
    gemm_p128_q512_um8_un1_simd8_single_block,
    gemm_p128_q512_um8_un1_simd8_fixed_q,
    gemm_p128_q512_um8_un1_simd8_adaptive_half,
    gemm_p128_q512_um8_un1_simd8_equal_split,
    gemm_p128_q512_um8_un4_simd4_single_block,
    gemm_p128_q512_um8_un4_simd4_fixed_q,
    gemm_p128_q512_um8_un4_simd4_adaptive_half,
    gemm_p128_q512_um8_un4_simd4_equal_split,
    gemm_p128_q512_um8_un4_simd8_single_block,
    gemm_p128_q512_um8_un4_simd8_fixed_q,
    gemm_p128_q512_um8_un4_simd8_adaptive_half,
    gemm_p128_q512_um8_un4_simd8_equal_split,
    gemm_p256_q1_um1_un1_simd1_single_block,
    gemm_p256_q1_um1_un1_simd1_fixed_q,
    gemm_p256_q1_um1_un1_simd1_adaptive_half,
    gemm_p256_q1_um1_un1_simd1_equal_split,
    gemm_p256_q1_um1_un4_simd1_single_block,
    gemm_p256_q1_um1_un4_simd1_fixed_q,
    gemm_p256_q1_um1_un4_simd1_adaptive_half,
    gemm_p256_q1_um1_un4_simd1_equal_split,
    gemm_p256_q1_um4_un1_simd1_single_block,
    gemm_p256_q1_um4_un1_simd1_fixed_q,
    gemm_p256_q1_um4_un1_simd1_adaptive_half,
    gemm_p256_q1_um4_un1_simd1_equal_split,
    gemm_p256_q1_um4_un1_simd4_single_block,
    gemm_p256_q1_um4_un1_simd4_fixed_q,
    gemm_p256_q1_um4_un1_simd4_adaptive_half,
    gemm_p256_q1_um4_un1_simd4_equal_split,
    gemm_p256_q1_um4_un4_simd4_single_block,
    gemm_p256_q1_um4_un4_simd4_fixed_q,
    gemm_p256_q1_um4_un4_simd4_adaptive_half,
    gemm_p256_q1_um4_un4_simd4_equal_split,
    gemm_p256_q1_um8_un1_simd1_single_block,
    gemm_p256_q1_um8_un1_simd1_fixed_q,
    gemm_p256_q1_um8_un1_simd1_adaptive_half,
    gemm_p256_q1_um8_un1_simd1_equal_split,
    gemm_p256_q1_um8_un1_simd4_single_block,
    gemm_p256_q1_um8_un1_simd4_fixed_q,
    gemm_p256_q1_um8_un1_simd4_adaptive_half,
    gemm_p256_q1_um8_un1_simd4_equal_split,
    gemm_p256_q1_um8_un1_simd8_single_block,
    gemm_p256_q1_um8_un1_simd8_fixed_q,
    gemm_p256_q1_um8_un1_simd8_adaptive_half,
    gemm_p256_q1_um8_un1_simd8_equal_split,
    gemm_p256_q1_um8_un4_simd4_single_block,
    gemm_p256_q1_um8_un4_simd4_fixed_q,
    gemm_p256_q1_um8_un4_simd4_adaptive_half,
    gemm_p256_q1_um8_un4_simd4_equal_split,
    gemm_p256_q1_um8_un4_simd8_single_block,
    gemm_p256_q1_um8_un4_simd8_fixed_q,
    gemm_p256_q1_um8_un4_simd8_adaptive_half,
    gemm_p256_q1_um8_un4_simd8_equal_split,
    gemm_p256_q1_um16_un1_simd4_single_block,
    gemm_p256_q1_um16_un1_simd4_fixed_q,
    gemm_p256_q1_um16_un1_simd4_adaptive_half,
    gemm_p256_q1_um16_un1_simd4_equal_split,
    gemm_p256_q1_um16_un1_simd8_single_block,
    gemm_p256_q1_um16_un1_simd8_fixed_q,
    gemm_p256_q1_um16_un1_simd8_adaptive_half,
    gemm_p256_q1_um16_un1_simd8_equal_split,
    gemm_p256_q1_um16_un1_simd16_single_block,
    gemm_p256_q1_um16_un1_simd16_fixed_q,
    gemm_p256_q1_um16_un1_simd16_adaptive_half,
    gemm_p256_q1_um16_un1_simd16_equal_split,
    gemm_p256_q1_um16_un4_simd8_single_block,
    gemm_p256_q1_um16_un4_simd8_fixed_q,
    gemm_p256_q1_um16_un4_simd8_adaptive_half,
    gemm_p256_q1_um16_un4_simd8_equal_split,
    gemm_p256_q1_um16_un4_simd16_single_block,
    gemm_p256_q1_um16_un4_simd16_fixed_q,
    gemm_p256_q1_um16_un4_simd16_adaptive_half,
    gemm_p256_q1_um16_un4_simd16_equal_split,
    gemm_p256_q1_um32_un1_simd4_single_block,
    gemm_p256_q1_um32_un1_simd4_fixed_q,
    gemm_p256_q1_um32_un1_simd4_adaptive_half,
    gemm_p256_q1_um32_un1_simd4_equal_split,
    gemm_p256_q1_um32_un1_simd8_single_block,
    gemm_p256_q1_um32_un1_simd8_fixed_q,
    gemm_p256_q1_um32_un1_simd8_adaptive_half,
    gemm_p256_q1_um32_un1_simd8_equal_split,
    gemm_p256_q1_um32_un1_simd16_single_block,
    gemm_p256_q1_um32_un1_simd16_fixed_q,
    gemm_p256_q1_um32_un1_simd16_adaptive_half,
    gemm_p256_q1_um32_un1_simd16_equal_split,
    gemm_p256_q1_um32_un4_simd16_single_block,
    gemm_p256_q1_um32_un4_simd16_fixed_q,
    gemm_p256_q1_um32_un4_simd16_adaptive_half,
    gemm_p256_q1_um32_un4_simd16_equal_split,
    gemm_p256_q64_um1_un1_simd1_single_block,
    gemm_p256_q64_um1_un1_simd1_fixed_q,
    gemm_p256_q64_um1_un1_simd1_adaptive_half,
    gemm_p256_q64_um1_un1_simd1_equal_split,
    gemm_p256_q64_um1_un4_simd1_single_block,
    gemm_p256_q64_um1_un4_simd1_fixed_q,
    gemm_p256_q64_um1_un4_simd1_adaptive_half,
    gemm_p256_q64_um1_un4_simd1_equal_split,
    gemm_p256_q64_um4_un1_simd1_single_block,
    gemm_p256_q64_um4_un1_simd1_fixed_q,
    gemm_p256_q64_um4_un1_simd1_adaptive_half,
    gemm_p256_q64_um4_un1_simd1_equal_split,
    gemm_p256_q64_um4_un1_simd4_single_block,
    gemm_p256_q64_um4_un1_simd4_fixed_q,
    gemm_p256_q64_um4_un1_simd4_adaptive_half,
    gemm_p256_q64_um4_un1_simd4_equal_split,
    gemm_p256_q64_um4_un4_simd4_single_block,
    gemm_p256_q64_um4_un4_simd4_fixed_q,
    gemm_p256_q64_um4_un4_simd4_adaptive_half,
    gemm_p256_q64_um4_un4_simd4_equal_split,
    gemm_p256_q64_um8_un1_simd1_single_block,
    gemm_p256_q64_um8_un1_simd1_fixed_q,
    gemm_p256_q64_um8_un1_simd1_adaptive_half,
    gemm_p256_q64_um8_un1_simd1_equal_split,
    gemm_p256_q64_um8_un1_simd4_single_block,
    gemm_p256_q64_um8_un1_simd4_fixed_q,
    gemm_p256_q64_um8_un1_simd4_adaptive_half,
    gemm_p256_q64_um8_un1_simd4_equal_split,
    gemm_p256_q64_um8_un1_simd8_single_block,
    gemm_p256_q64_um8_un1_simd8_fixed_q,
    gemm_p256_q64_um8_un1_simd8_adaptive_half,
    gemm_p256_q64_um8_un1_simd8_equal_split,
    gemm_p256_q64_um8_un4_simd4_single_block,
    gemm_p256_q64_um8_un4_simd4_fixed_q,
    gemm_p256_q64_um8_un4_simd4_adaptive_half,
    gemm_p256_q64_um8_un4_simd4_equal_split,
    gemm_p256_q64_um8_un4_simd8_single_block,
    gemm_p256_q64_um8_un4_simd8_fixed_q,
    gemm_p256_q64_um8_un4_simd8_adaptive_half,
    gemm_p256_q64_um8_un4_simd8_equal_split,
    gemm_p256_q64_um16_un1_simd4_single_block,
    gemm_p256_q64_um16_un1_simd4_fixed_q,
    gemm_p256_q64_um16_un1_simd4_adaptive_half,
    gemm_p256_q64_um16_un1_simd4_equal_split,
    gemm_p256_q64_um16_un1_simd8_single_block,
    gemm_p256_q64_um16_un1_simd8_fixed_q,
    gemm_p256_q64_um16_un1_simd8_adaptive_half,
    gemm_p256_q64_um16_un1_simd8_equal_split,
    gemm_p256_q64_um16_un1_simd16_single_block,
    gemm_p256_q64_um16_un1_simd16_fixed_q,
    gemm_p256_q64_um16_un1_simd16_adaptive_half,
    gemm_p256_q64_um16_un1_simd16_equal_split,
    gemm_p256_q64_um16_un4_simd8_single_block,
    gemm_p256_q64_um16_un4_simd8_fixed_q,
    gemm_p256_q64_um16_un4_simd8_adaptive_half,
    gemm_p256_q64_um16_un4_simd8_equal_split,
    gemm_p256_q64_um16_un4_simd16_single_block,
    gemm_p256_q64_um16_un4_simd16_fixed_q,
    gemm_p256_q64_um16_un4_simd16_adaptive_half,
    gemm_p256_q64_um16_un4_simd16_equal_split,
    gemm_p256_q64_um32_un1_simd4_single_block,
    gemm_p256_q64_um32_un1_simd4_fixed_q,
    gemm_p256_q64_um32_un1_simd4_adaptive_half,
    gemm_p256_q64_um32_un1_simd4_equal_split,
    gemm_p256_q64_um32_un1_simd8_single_block,
    gemm_p256_q64_um32_un1_simd8_fixed_q,
    gemm_p256_q64_um32_un1_simd8_adaptive_half,
    gemm_p256_q64_um32_un1_simd8_equal_split,
    gemm_p256_q64_um32_un1_simd16_single_block,
    gemm_p256_q64_um32_un1_simd16_fixed_q,
    gemm_p256_q64_um32_un1_simd16_adaptive_half,
    gemm_p256_q64_um32_un1_simd16_equal_split,
    gemm_p256_q64_um32_un4_simd16_single_block,
    gemm_p256_q64_um32_un4_simd16_fixed_q,
    gemm_p256_q64_um32_un4_simd16_adaptive_half,
    gemm_p256_q64_um32_un4_simd16_equal_split,
    gemm_p256_q128_um1_un1_simd1_single_block,
    gemm_p256_q128_um1_un1_simd1_fixed_q,
    gemm_p256_q128_um1_un1_simd1_adaptive_half,
    gemm_p256_q128_um1_un1_simd1_equal_split,
    gemm_p256_q128_um1_un4_simd1_single_block,
    gemm_p256_q128_um1_un4_simd1_fixed_q,
    gemm_p256_q128_um1_un4_simd1_adaptive_half,
    gemm_p256_q128_um1_un4_simd1_equal_split,
    gemm_p256_q128_um4_un1_simd1_single_block,
    gemm_p256_q128_um4_un1_simd1_fixed_q,
    gemm_p256_q128_um4_un1_simd1_adaptive_half,
    gemm_p256_q128_um4_un1_simd1_equal_split,
    gemm_p256_q128_um4_un1_simd4_single_block,
    gemm_p256_q128_um4_un1_simd4_fixed_q,
    gemm_p256_q128_um4_un1_simd4_adaptive_half,
    gemm_p256_q128_um4_un1_simd4_equal_split,
    gemm_p256_q128_um4_un4_simd4_single_block,
    gemm_p256_q128_um4_un4_simd4_fixed_q,
    gemm_p256_q128_um4_un4_simd4_adaptive_half,
    gemm_p256_q128_um4_un4_simd4_equal_split,
    gemm_p256_q128_um8_un1_simd1_single_block,
    gemm_p256_q128_um8_un1_simd1_fixed_q,
    gemm_p256_q128_um8_un1_simd1_adaptive_half,
    gemm_p256_q128_um8_un1_simd1_equal_split,
    gemm_p256_q128_um8_un1_simd4_single_block,
    gemm_p256_q128_um8_un1_simd4_fixed_q,
    gemm_p256_q128_um8_un1_simd4_adaptive_half,
    gemm_p256_q128_um8_un1_simd4_equal_split,
    gemm_p256_q128_um8_un1_simd8_single_block,
    gemm_p256_q128_um8_un1_simd8_fixed_q,
    gemm_p256_q128_um8_un1_simd8_adaptive_half,
    gemm_p256_q128_um8_un1_simd8_equal_split,
    gemm_p256_q128_um8_un4_simd4_single_block,
    gemm_p256_q128_um8_un4_simd4_fixed_q,
    gemm_p256_q128_um8_un4_simd4_adaptive_half,
    gemm_p256_q128_um8_un4_simd4_equal_split,
    gemm_p256_q128_um8_un4_simd8_single_block,
    gemm_p256_q128_um8_un4_simd8_fixed_q,
    gemm_p256_q128_um8_un4_simd8_adaptive_half,
    gemm_p256_q128_um8_un4_simd8_equal_split,
    gemm_p256_q128_um16_un1_simd4_single_block,
    gemm_p256_q128_um16_un1_simd4_fixed_q,
    gemm_p256_q128_um16_un1_simd4_adaptive_half,
    gemm_p256_q128_um16_un1_simd4_equal_split,
    gemm_p256_q128_um16_un1_simd8_single_block,
    gemm_p256_q128_um16_un1_simd8_fixed_q,
    gemm_p256_q128_um16_un1_simd8_adaptive_half,
    gemm_p256_q128_um16_un1_simd8_equal_split,
    gemm_p256_q128_um16_un1_simd16_single_block,
    gemm_p256_q128_um16_un1_simd16_fixed_q,
    gemm_p256_q128_um16_un1_simd16_adaptive_half,
    gemm_p256_q128_um16_un1_simd16_equal_split,
    gemm_p256_q128_um16_un4_simd8_single_block,
    gemm_p256_q128_um16_un4_simd8_fixed_q,
    gemm_p256_q128_um16_un4_simd8_adaptive_half,
    gemm_p256_q128_um16_un4_simd8_equal_split,
    gemm_p256_q128_um16_un4_simd16_single_block,
    gemm_p256_q128_um16_un4_simd16_fixed_q,
    gemm_p256_q128_um16_un4_simd16_adaptive_half,
    gemm_p256_q128_um16_un4_simd16_equal_split,
    gemm_p256_q128_um32_un1_simd4_single_block,
    gemm_p256_q128_um32_un1_simd4_fixed_q,
    gemm_p256_q128_um32_un1_simd4_adaptive_half,
    gemm_p256_q128_um32_un1_simd4_equal_split,
    gemm_p256_q128_um32_un1_simd8_single_block,
    gemm_p256_q128_um32_un1_simd8_fixed_q,
    gemm_p256_q128_um32_un1_simd8_adaptive_half,
    gemm_p256_q128_um32_un1_simd8_equal_split,
    gemm_p256_q128_um32_un1_simd16_single_block,
    gemm_p256_q128_um32_un1_simd16_fixed_q,
    gemm_p256_q128_um32_un1_simd16_adaptive_half,
    gemm_p256_q128_um32_un1_simd16_equal_split,
    gemm_p256_q128_um32_un4_simd16_single_block,
    gemm_p256_q128_um32_un4_simd16_fixed_q,
    gemm_p256_q128_um32_un4_simd16_adaptive_half,
    gemm_p256_q128_um32_un4_simd16_equal_split,
    gemm_p256_q192_um1_un1_simd1_single_block,
    gemm_p256_q192_um1_un1_simd1_fixed_q,
    gemm_p256_q192_um1_un1_simd1_adaptive_half,
    gemm_p256_q192_um1_un1_simd1_equal_split,
    gemm_p256_q192_um1_un4_simd1_single_block,
    gemm_p256_q192_um1_un4_simd1_fixed_q,
    gemm_p256_q192_um1_un4_simd1_adaptive_half,
    gemm_p256_q192_um1_un4_simd1_equal_split,
    gemm_p256_q192_um4_un1_simd1_single_block,
    gemm_p256_q192_um4_un1_simd1_fixed_q,
    gemm_p256_q192_um4_un1_simd1_adaptive_half,
    gemm_p256_q192_um4_un1_simd1_equal_split,
    gemm_p256_q192_um4_un1_simd4_single_block,
    gemm_p256_q192_um4_un1_simd4_fixed_q,
    gemm_p256_q192_um4_un1_simd4_adaptive_half,
    gemm_p256_q192_um4_un1_simd4_equal_split,
    gemm_p256_q192_um4_un4_simd4_single_block,
    gemm_p256_q192_um4_un4_simd4_fixed_q,
    gemm_p256_q192_um4_un4_simd4_adaptive_half,
    gemm_p256_q192_um4_un4_simd4_equal_split,
    gemm_p256_q192_um8_un1_simd1_single_block,
    gemm_p256_q192_um8_un1_simd1_fixed_q,
    gemm_p256_q192_um8_un1_simd1_adaptive_half,
    gemm_p256_q192_um8_un1_simd1_equal_split,
    gemm_p256_q192_um8_un1_simd4_single_block,
    gemm_p256_q192_um8_un1_simd4_fixed_q,
    gemm_p256_q192_um8_un1_simd4_adaptive_half,
    gemm_p256_q192_um8_un1_simd4_equal_split,
    gemm_p256_q192_um8_un1_simd8_single_block,
    gemm_p256_q192_um8_un1_simd8_fixed_q,
    gemm_p256_q192_um8_un1_simd8_adaptive_half,
    gemm_p256_q192_um8_un1_simd8_equal_split,
    gemm_p256_q192_um8_un4_simd4_single_block,
    gemm_p256_q192_um8_un4_simd4_fixed_q,
    gemm_p256_q192_um8_un4_simd4_adaptive_half,
    gemm_p256_q192_um8_un4_simd4_equal_split,
    gemm_p256_q192_um8_un4_simd8_single_block,
    gemm_p256_q192_um8_un4_simd8_fixed_q,
    gemm_p256_q192_um8_un4_simd8_adaptive_half,
    gemm_p256_q192_um8_un4_simd8_equal_split,
    gemm_p256_q192_um16_un1_simd4_single_block,
    gemm_p256_q192_um16_un1_simd4_fixed_q,
    gemm_p256_q192_um16_un1_simd4_adaptive_half,
    gemm_p256_q192_um16_un1_simd4_equal_split,
    gemm_p256_q192_um16_un1_simd8_single_block,
    gemm_p256_q192_um16_un1_simd8_fixed_q,
    gemm_p256_q192_um16_un1_simd8_adaptive_half,
    gemm_p256_q192_um16_un1_simd8_equal_split,
    gemm_p256_q192_um16_un1_simd16_single_block,
    gemm_p256_q192_um16_un1_simd16_fixed_q,
    gemm_p256_q192_um16_un1_simd16_adaptive_half,
    gemm_p256_q192_um16_un1_simd16_equal_split,
    gemm_p256_q192_um16_un4_simd8_single_block,
    gemm_p256_q192_um16_un4_simd8_fixed_q,
    gemm_p256_q192_um16_un4_simd8_adaptive_half,
    gemm_p256_q192_um16_un4_simd8_equal_split,
    gemm_p256_q192_um16_un4_simd16_single_block,
    gemm_p256_q192_um16_un4_simd16_fixed_q,
    gemm_p256_q192_um16_un4_simd16_adaptive_half,
    gemm_p256_q192_um16_un4_simd16_equal_split,
    gemm_p256_q192_um32_un1_simd4_single_block,
    gemm_p256_q192_um32_un1_simd4_fixed_q,
    gemm_p256_q192_um32_un1_simd4_adaptive_half,
    gemm_p256_q192_um32_un1_simd4_equal_split,
    gemm_p256_q192_um32_un1_simd8_single_block,
    gemm_p256_q192_um32_un1_simd8_fixed_q,
    gemm_p256_q192_um32_un1_simd8_adaptive_half,
    gemm_p256_q192_um32_un1_simd8_equal_split,
    gemm_p256_q192_um32_un1_simd16_single_block,
    gemm_p256_q192_um32_un1_simd16_fixed_q,
    gemm_p256_q192_um32_un1_simd16_adaptive_half,
    gemm_p256_q192_um32_un1_simd16_equal_split,
    gemm_p256_q192_um32_un4_simd16_single_block,
    gemm_p256_q192_um32_un4_simd16_fixed_q,
    gemm_p256_q192_um32_un4_simd16_adaptive_half,
    gemm_p256_q192_um32_un4_simd16_equal_split,
    gemm_p256_q240_um1_un1_simd1_single_block,
    gemm_p256_q240_um1_un1_simd1_fixed_q,
    gemm_p256_q240_um1_un1_simd1_adaptive_half,
    gemm_p256_q240_um1_un1_simd1_equal_split,
    gemm_p256_q240_um1_un4_simd1_single_block,
    gemm_p256_q240_um1_un4_simd1_fixed_q,
    gemm_p256_q240_um1_un4_simd1_adaptive_half,
    gemm_p256_q240_um1_un4_simd1_equal_split,
    gemm_p256_q240_um4_un1_simd1_single_block,
    gemm_p256_q240_um4_un1_simd1_fixed_q,
    gemm_p256_q240_um4_un1_simd1_adaptive_half,
    gemm_p256_q240_um4_un1_simd1_equal_split,
    gemm_p256_q240_um4_un1_simd4_single_block,
    gemm_p256_q240_um4_un1_simd4_fixed_q,
    gemm_p256_q240_um4_un1_simd4_adaptive_half,
    gemm_p256_q240_um4_un1_simd4_equal_split,
    gemm_p256_q240_um4_un4_simd4_single_block,
    gemm_p256_q240_um4_un4_simd4_fixed_q,
    gemm_p256_q240_um4_un4_simd4_adaptive_half,
    gemm_p256_q240_um4_un4_simd4_equal_split,
    gemm_p256_q240_um8_un1_simd1_single_block,
    gemm_p256_q240_um8_un1_simd1_fixed_q,
    gemm_p256_q240_um8_un1_simd1_adaptive_half,
    gemm_p256_q240_um8_un1_simd1_equal_split,
    gemm_p256_q240_um8_un1_simd4_single_block,
    gemm_p256_q240_um8_un1_simd4_fixed_q,
    gemm_p256_q240_um8_un1_simd4_adaptive_half,
    gemm_p256_q240_um8_un1_simd4_equal_split,
    gemm_p256_q240_um8_un1_simd8_single_block,
    gemm_p256_q240_um8_un1_simd8_fixed_q,
    gemm_p256_q240_um8_un1_simd8_adaptive_half,
    gemm_p256_q240_um8_un1_simd8_equal_split,
    gemm_p256_q240_um8_un4_simd4_single_block,
    gemm_p256_q240_um8_un4_simd4_fixed_q,
    gemm_p256_q240_um8_un4_simd4_adaptive_half,
    gemm_p256_q240_um8_un4_simd4_equal_split,
    gemm_p256_q240_um8_un4_simd8_single_block,
    gemm_p256_q240_um8_un4_simd8_fixed_q,
    gemm_p256_q240_um8_un4_simd8_adaptive_half,
    gemm_p256_q240_um8_un4_simd8_equal_split,
    gemm_p256_q240_um16_un1_simd4_single_block,
    gemm_p256_q240_um16_un1_simd4_fixed_q,
    gemm_p256_q240_um16_un1_simd4_adaptive_half,
    gemm_p256_q240_um16_un1_simd4_equal_split,
    gemm_p256_q240_um16_un1_simd8_single_block,
    gemm_p256_q240_um16_un1_simd8_fixed_q,
    gemm_p256_q240_um16_un1_simd8_adaptive_half,
    gemm_p256_q240_um16_un1_simd8_equal_split,
    gemm_p256_q240_um16_un1_simd16_single_block,
    gemm_p256_q240_um16_un1_simd16_fixed_q,
    gemm_p256_q240_um16_un1_simd16_adaptive_half,
    gemm_p256_q240_um16_un1_simd16_equal_split,
    gemm_p256_q240_um16_un4_simd8_single_block,
    gemm_p256_q240_um16_un4_simd8_fixed_q,
    gemm_p256_q240_um16_un4_simd8_adaptive_half,
    gemm_p256_q240_um16_un4_simd8_equal_split,
    gemm_p256_q240_um16_un4_simd16_single_block,
    gemm_p256_q240_um16_un4_simd16_fixed_q,
    gemm_p256_q240_um16_un4_simd16_adaptive_half,
    gemm_p256_q240_um16_un4_simd16_equal_split,
    gemm_p256_q240_um32_un1_simd4_single_block,
    gemm_p256_q240_um32_un1_simd4_fixed_q,
    gemm_p256_q240_um32_un1_simd4_adaptive_half,
    gemm_p256_q240_um32_un1_simd4_equal_split,
    gemm_p256_q240_um32_un1_simd8_single_block,
    gemm_p256_q240_um32_un1_simd8_fixed_q,
    gemm_p256_q240_um32_un1_simd8_adaptive_half,
    gemm_p256_q240_um32_un1_simd8_equal_split,
    gemm_p256_q240_um32_un1_simd16_single_block,
    gemm_p256_q240_um32_un1_simd16_fixed_q,
    gemm_p256_q240_um32_un1_simd16_adaptive_half,
    gemm_p256_q240_um32_un1_simd16_equal_split,
    gemm_p256_q248_um1_un1_simd1_single_block,
    gemm_p256_q248_um1_un1_simd1_fixed_q,
    gemm_p256_q248_um1_un1_simd1_adaptive_half,
    gemm_p256_q248_um1_un1_simd1_equal_split,
    gemm_p256_q248_um1_un4_simd1_single_block,
    gemm_p256_q248_um1_un4_simd1_fixed_q,
    gemm_p256_q248_um1_un4_simd1_adaptive_half,
    gemm_p256_q248_um1_un4_simd1_equal_split,
    gemm_p256_q248_um4_un1_simd1_single_block,
    gemm_p256_q248_um4_un1_simd1_fixed_q,
    gemm_p256_q248_um4_un1_simd1_adaptive_half,
    gemm_p256_q248_um4_un1_simd1_equal_split,
    gemm_p256_q248_um4_un1_simd4_single_block,
    gemm_p256_q248_um4_un1_simd4_fixed_q,
    gemm_p256_q248_um4_un1_simd4_adaptive_half,
    gemm_p256_q248_um4_un1_simd4_equal_split,
    gemm_p256_q248_um4_un4_simd4_single_block,
    gemm_p256_q248_um4_un4_simd4_fixed_q,
    gemm_p256_q248_um4_un4_simd4_adaptive_half,
    gemm_p256_q248_um4_un4_simd4_equal_split,
    gemm_p256_q248_um8_un1_simd1_single_block,
    gemm_p256_q248_um8_un1_simd1_fixed_q,
    gemm_p256_q248_um8_un1_simd1_adaptive_half,
    gemm_p256_q248_um8_un1_simd1_equal_split,
    gemm_p256_q248_um8_un1_simd4_single_block,
    gemm_p256_q248_um8_un1_simd4_fixed_q,
    gemm_p256_q248_um8_un1_simd4_adaptive_half,
    gemm_p256_q248_um8_un1_simd4_equal_split,
    gemm_p256_q248_um8_un1_simd8_single_block,
    gemm_p256_q248_um8_un1_simd8_fixed_q,
    gemm_p256_q248_um8_un1_simd8_adaptive_half,
    gemm_p256_q248_um8_un1_simd8_equal_split,
    gemm_p256_q248_um8_un4_simd4_single_block,
    gemm_p256_q248_um8_un4_simd4_fixed_q,
    gemm_p256_q248_um8_un4_simd4_adaptive_half,
    gemm_p256_q248_um8_un4_simd4_equal_split,
    gemm_p256_q248_um8_un4_simd8_single_block,
    gemm_p256_q248_um8_un4_simd8_fixed_q,
    gemm_p256_q248_um8_un4_simd8_adaptive_half,
    gemm_p256_q248_um8_un4_simd8_equal_split,
    gemm_p256_q248_um16_un1_simd4_single_block,
    gemm_p256_q248_um16_un1_simd4_fixed_q,
    gemm_p256_q248_um16_un1_simd4_adaptive_half,
    gemm_p256_q248_um16_un1_simd4_equal_split,
    gemm_p256_q248_um16_un1_simd8_single_block,
    gemm_p256_q248_um16_un1_simd8_fixed_q,
    gemm_p256_q248_um16_un1_simd8_adaptive_half,
    gemm_p256_q248_um16_un1_simd8_equal_split,
    gemm_p256_q248_um16_un1_simd16_single_block,
    gemm_p256_q248_um16_un1_simd16_fixed_q,
    gemm_p256_q248_um16_un1_simd16_adaptive_half,
    gemm_p256_q248_um16_un1_simd16_equal_split,
    gemm_p256_q248_um16_un4_simd8_single_block,
    gemm_p256_q248_um16_un4_simd8_fixed_q,
    gemm_p256_q248_um16_un4_simd8_adaptive_half,
    gemm_p256_q248_um16_un4_simd8_equal_split,
    gemm_p256_q248_um16_un4_simd16_single_block,
    gemm_p256_q248_um16_un4_simd16_fixed_q,
    gemm_p256_q248_um16_un4_simd16_adaptive_half,
    gemm_p256_q248_um16_un4_simd16_equal_split,
    gemm_p256_q248_um32_un1_simd4_single_block,
    gemm_p256_q248_um32_un1_simd4_fixed_q,
    gemm_p256_q248_um32_un1_simd4_adaptive_half,
    gemm_p256_q248_um32_un1_simd4_equal_split,
    gemm_p256_q248_um32_un1_simd8_single_block,
    gemm_p256_q248_um32_un1_simd8_fixed_q,
    gemm_p256_q248_um32_un1_simd8_adaptive_half,
    gemm_p256_q248_um32_un1_simd8_equal_split,
    gemm_p256_q248_um32_un1_simd16_single_block,
    gemm_p256_q248_um32_un1_simd16_fixed_q,
    gemm_p256_q248_um32_un1_simd16_adaptive_half,
    gemm_p256_q248_um32_un1_simd16_equal_split,
    gemm_p256_q256_um1_un1_simd1_single_block,
    gemm_p256_q256_um1_un1_simd1_fixed_q,
    gemm_p256_q256_um1_un1_simd1_adaptive_half,
    gemm_p256_q256_um1_un1_simd1_equal_split,
    gemm_p256_q256_um1_un4_simd1_single_block,
    gemm_p256_q256_um1_un4_simd1_fixed_q,
    gemm_p256_q256_um1_un4_simd1_adaptive_half,
    gemm_p256_q256_um1_un4_simd1_equal_split,
    gemm_p256_q256_um4_un1_simd1_single_block,
    gemm_p256_q256_um4_un1_simd1_fixed_q,
    gemm_p256_q256_um4_un1_simd1_adaptive_half,
    gemm_p256_q256_um4_un1_simd1_equal_split,
    gemm_p256_q256_um4_un1_simd4_single_block,
    gemm_p256_q256_um4_un1_simd4_fixed_q,
    gemm_p256_q256_um4_un1_simd4_adaptive_half,
    gemm_p256_q256_um4_un1_simd4_equal_split,
    gemm_p256_q256_um4_un4_simd4_single_block,
    gemm_p256_q256_um4_un4_simd4_fixed_q,
    gemm_p256_q256_um4_un4_simd4_adaptive_half,
    gemm_p256_q256_um4_un4_simd4_equal_split,
    gemm_p256_q256_um8_un1_simd1_single_block,
    gemm_p256_q256_um8_un1_simd1_fixed_q,
    gemm_p256_q256_um8_un1_simd1_adaptive_half,
    gemm_p256_q256_um8_un1_simd1_equal_split,
    gemm_p256_q256_um8_un1_simd4_single_block,
    gemm_p256_q256_um8_un1_simd4_fixed_q,
    gemm_p256_q256_um8_un1_simd4_adaptive_half,
    gemm_p256_q256_um8_un1_simd4_equal_split,
    gemm_p256_q256_um8_un1_simd8_single_block,
    gemm_p256_q256_um8_un1_simd8_fixed_q,
    gemm_p256_q256_um8_un1_simd8_adaptive_half,
    gemm_p256_q256_um8_un1_simd8_equal_split,
    gemm_p256_q256_um8_un4_simd4_single_block,
    gemm_p256_q256_um8_un4_simd4_fixed_q,
    gemm_p256_q256_um8_un4_simd4_adaptive_half,
    gemm_p256_q256_um8_un4_simd4_equal_split,
    gemm_p256_q256_um8_un4_simd8_single_block,
    gemm_p256_q256_um8_un4_simd8_fixed_q,
    gemm_p256_q256_um8_un4_simd8_adaptive_half,
    gemm_p256_q256_um8_un4_simd8_equal_split,
    gemm_p256_q256_um16_un1_simd4_single_block,
    gemm_p256_q256_um16_un1_simd4_fixed_q,
    gemm_p256_q256_um16_un1_simd4_adaptive_half,
    gemm_p256_q256_um16_un1_simd4_equal_split,
    gemm_p256_q256_um16_un1_simd8_single_block,
    gemm_p256_q256_um16_un1_simd8_fixed_q,
    gemm_p256_q256_um16_un1_simd8_adaptive_half,
    gemm_p256_q256_um16_un1_simd8_equal_split,
    gemm_p256_q256_um16_un1_simd16_single_block,
    gemm_p256_q256_um16_un1_simd16_fixed_q,
    gemm_p256_q256_um16_un1_simd16_adaptive_half,
    gemm_p256_q256_um16_un1_simd16_equal_split,
    gemm_p256_q256_um16_un4_simd8_single_block,
    gemm_p256_q256_um16_un4_simd8_fixed_q,
    gemm_p256_q256_um16_un4_simd8_adaptive_half,
    gemm_p256_q256_um16_un4_simd8_equal_split,
    gemm_p256_q256_um16_un4_simd16_single_block,
    gemm_p256_q256_um16_un4_simd16_fixed_q,
    gemm_p256_q256_um16_un4_simd16_adaptive_half,
    gemm_p256_q256_um16_un4_simd16_equal_split,
    gemm_p256_q384_um1_un1_simd1_single_block,
    gemm_p256_q384_um1_un1_simd1_fixed_q,
    gemm_p256_q384_um1_un1_simd1_adaptive_half,
    gemm_p256_q384_um1_un1_simd1_equal_split,
    gemm_p256_q384_um1_un4_simd1_single_block,
    gemm_p256_q384_um1_un4_simd1_fixed_q,
    gemm_p256_q384_um1_un4_simd1_adaptive_half,
    gemm_p256_q384_um1_un4_simd1_equal_split,
    gemm_p256_q384_um4_un1_simd1_single_block,
    gemm_p256_q384_um4_un1_simd1_fixed_q,
    gemm_p256_q384_um4_un1_simd1_adaptive_half,
    gemm_p256_q384_um4_un1_simd1_equal_split,
    gemm_p256_q384_um4_un1_simd4_single_block,
    gemm_p256_q384_um4_un1_simd4_fixed_q,
    gemm_p256_q384_um4_un1_simd4_adaptive_half,
    gemm_p256_q384_um4_un1_simd4_equal_split,
    gemm_p256_q384_um4_un4_simd4_single_block,
    gemm_p256_q384_um4_un4_simd4_fixed_q,
    gemm_p256_q384_um4_un4_simd4_adaptive_half,
    gemm_p256_q384_um4_un4_simd4_equal_split,
    gemm_p256_q384_um8_un1_simd1_single_block,
    gemm_p256_q384_um8_un1_simd1_fixed_q,
    gemm_p256_q384_um8_un1_simd1_adaptive_half,
    gemm_p256_q384_um8_un1_simd1_equal_split,
    gemm_p256_q384_um8_un1_simd4_single_block,
    gemm_p256_q384_um8_un1_simd4_fixed_q,
    gemm_p256_q384_um8_un1_simd4_adaptive_half,
    gemm_p256_q384_um8_un1_simd4_equal_split,
    gemm_p256_q384_um8_un1_simd8_single_block,
    gemm_p256_q384_um8_un1_simd8_fixed_q,
    gemm_p256_q384_um8_un1_simd8_adaptive_half,
    gemm_p256_q384_um8_un1_simd8_equal_split,
    gemm_p256_q384_um8_un4_simd4_single_block,
    gemm_p256_q384_um8_un4_simd4_fixed_q,
    gemm_p256_q384_um8_un4_simd4_adaptive_half,
    gemm_p256_q384_um8_un4_simd4_equal_split,
    gemm_p256_q384_um8_un4_simd8_single_block,
    gemm_p256_q384_um8_un4_simd8_fixed_q,
    gemm_p256_q384_um8_un4_simd8_adaptive_half,
    gemm_p256_q384_um8_un4_simd8_equal_split,
    gemm_p256_q384_um16_un1_simd4_single_block,
    gemm_p256_q384_um16_un1_simd4_fixed_q,
    gemm_p256_q384_um16_un1_simd4_adaptive_half,
    gemm_p256_q384_um16_un1_simd4_equal_split,
    gemm_p256_q384_um16_un1_simd8_single_block,
    gemm_p256_q384_um16_un1_simd8_fixed_q,
    gemm_p256_q384_um16_un1_simd8_adaptive_half,
    gemm_p256_q384_um16_un1_simd8_equal_split,
    gemm_p256_q384_um16_un1_simd16_single_block,
    gemm_p256_q384_um16_un1_simd16_fixed_q,
    gemm_p256_q384_um16_un1_simd16_adaptive_half,
    gemm_p256_q384_um16_un1_simd16_equal_split,
    gemm_p256_q384_um16_un4_simd8_single_block,
    gemm_p256_q384_um16_un4_simd8_fixed_q,
    gemm_p256_q384_um16_un4_simd8_adaptive_half,
    gemm_p256_q384_um16_un4_simd8_equal_split,
    gemm_p256_q384_um16_un4_simd16_single_block,
    gemm_p256_q384_um16_un4_simd16_fixed_q,
    gemm_p256_q384_um16_un4_simd16_adaptive_half,
    gemm_p256_q384_um16_un4_simd16_equal_split,
    gemm_p256_q512_um1_un1_simd1_single_block,
    gemm_p256_q512_um1_un1_simd1_fixed_q,
    gemm_p256_q512_um1_un1_simd1_adaptive_half,
    gemm_p256_q512_um1_un1_simd1_equal_split,
    gemm_p256_q512_um1_un4_simd1_single_block,
    gemm_p256_q512_um1_un4_simd1_fixed_q,
    gemm_p256_q512_um1_un4_simd1_adaptive_half,
    gemm_p256_q512_um1_un4_simd1_equal_split,
    gemm_p256_q512_um4_un1_simd1_single_block,
    gemm_p256_q512_um4_un1_simd1_fixed_q,
    gemm_p256_q512_um4_un1_simd1_adaptive_half,
    gemm_p256_q512_um4_un1_simd1_equal_split,
    gemm_p256_q512_um4_un1_simd4_single_block,
    gemm_p256_q512_um4_un1_simd4_fixed_q,
    gemm_p256_q512_um4_un1_simd4_adaptive_half,
    gemm_p256_q512_um4_un1_simd4_equal_split,
    gemm_p256_q512_um4_un4_simd4_single_block,
    gemm_p256_q512_um4_un4_simd4_fixed_q,
    gemm_p256_q512_um4_un4_simd4_adaptive_half,
    gemm_p256_q512_um4_un4_simd4_equal_split,
    gemm_p256_q512_um8_un1_simd1_single_block,
    gemm_p256_q512_um8_un1_simd1_fixed_q,
    gemm_p256_q512_um8_un1_simd1_adaptive_half,
    gemm_p256_q512_um8_un1_simd1_equal_split,
    gemm_p256_q512_um8_un1_simd4_single_block,
    gemm_p256_q512_um8_un1_simd4_fixed_q,
    gemm_p256_q512_um8_un1_simd4_adaptive_half,
    gemm_p256_q512_um8_un1_simd4_equal_split,
    gemm_p256_q512_um8_un1_simd8_single_block,
    gemm_p256_q512_um8_un1_simd8_fixed_q,
    gemm_p256_q512_um8_un1_simd8_adaptive_half,
    gemm_p256_q512_um8_un1_simd8_equal_split,
    gemm_p256_q512_um8_un4_simd4_single_block,
    gemm_p256_q512_um8_un4_simd4_fixed_q,
    gemm_p256_q512_um8_un4_simd4_adaptive_half,
    gemm_p256_q512_um8_un4_simd4_equal_split,
    gemm_p256_q512_um8_un4_simd8_single_block,
    gemm_p256_q512_um8_un4_simd8_fixed_q,
    gemm_p256_q512_um8_un4_simd8_adaptive_half,
    gemm_p256_q512_um8_un4_simd8_equal_split,
    gemm_p384_q1_um1_un1_simd1_single_block,
    gemm_p384_q1_um1_un1_simd1_fixed_q,
    gemm_p384_q1_um1_un1_simd1_adaptive_half,
    gemm_p384_q1_um1_un1_simd1_equal_split,
    gemm_p384_q1_um1_un4_simd1_single_block,
    gemm_p384_q1_um1_un4_simd1_fixed_q,
    gemm_p384_q1_um1_un4_simd1_adaptive_half,
    gemm_p384_q1_um1_un4_simd1_equal_split,
    gemm_p384_q1_um4_un1_simd1_single_block,
    gemm_p384_q1_um4_un1_simd1_fixed_q,
    gemm_p384_q1_um4_un1_simd1_adaptive_half,
    gemm_p384_q1_um4_un1_simd1_equal_split,
    gemm_p384_q1_um4_un1_simd4_single_block,
    gemm_p384_q1_um4_un1_simd4_fixed_q,
    gemm_p384_q1_um4_un1_simd4_adaptive_half,
    gemm_p384_q1_um4_un1_simd4_equal_split,
    gemm_p384_q1_um4_un4_simd4_single_block,
    gemm_p384_q1_um4_un4_simd4_fixed_q,
    gemm_p384_q1_um4_un4_simd4_adaptive_half,
    gemm_p384_q1_um4_un4_simd4_equal_split,
    gemm_p384_q1_um8_un1_simd1_single_block,
    gemm_p384_q1_um8_un1_simd1_fixed_q,
    gemm_p384_q1_um8_un1_simd1_adaptive_half,
    gemm_p384_q1_um8_un1_simd1_equal_split,
    gemm_p384_q1_um8_un1_simd4_single_block,
    gemm_p384_q1_um8_un1_simd4_fixed_q,
    gemm_p384_q1_um8_un1_simd4_adaptive_half,
    gemm_p384_q1_um8_un1_simd4_equal_split,
    gemm_p384_q1_um8_un1_simd8_single_block,
    gemm_p384_q1_um8_un1_simd8_fixed_q,
    gemm_p384_q1_um8_un1_simd8_adaptive_half,
    gemm_p384_q1_um8_un1_simd8_equal_split,
    gemm_p384_q1_um8_un4_simd4_single_block,
    gemm_p384_q1_um8_un4_simd4_fixed_q,
    gemm_p384_q1_um8_un4_simd4_adaptive_half,
    gemm_p384_q1_um8_un4_simd4_equal_split,
    gemm_p384_q1_um8_un4_simd8_single_block,
    gemm_p384_q1_um8_un4_simd8_fixed_q,
    gemm_p384_q1_um8_un4_simd8_adaptive_half,
    gemm_p384_q1_um8_un4_simd8_equal_split,
    gemm_p384_q1_um16_un1_simd4_single_block,
    gemm_p384_q1_um16_un1_simd4_fixed_q,
    gemm_p384_q1_um16_un1_simd4_adaptive_half,
    gemm_p384_q1_um16_un1_simd4_equal_split,
    gemm_p384_q1_um16_un1_simd8_single_block,
    gemm_p384_q1_um16_un1_simd8_fixed_q,
    gemm_p384_q1_um16_un1_simd8_adaptive_half,
    gemm_p384_q1_um16_un1_simd8_equal_split,
    gemm_p384_q1_um16_un1_simd16_single_block,
    gemm_p384_q1_um16_un1_simd16_fixed_q,
    gemm_p384_q1_um16_un1_simd16_adaptive_half,
    gemm_p384_q1_um16_un1_simd16_equal_split,
    gemm_p384_q1_um16_un4_simd8_single_block,
    gemm_p384_q1_um16_un4_simd8_fixed_q,
    gemm_p384_q1_um16_un4_simd8_adaptive_half,
    gemm_p384_q1_um16_un4_simd8_equal_split,
    gemm_p384_q1_um16_un4_simd16_single_block,
    gemm_p384_q1_um16_un4_simd16_fixed_q,
    gemm_p384_q1_um16_un4_simd16_adaptive_half,
    gemm_p384_q1_um16_un4_simd16_equal_split,
    gemm_p384_q1_um32_un1_simd4_single_block,
    gemm_p384_q1_um32_un1_simd4_fixed_q,
    gemm_p384_q1_um32_un1_simd4_adaptive_half,
    gemm_p384_q1_um32_un1_simd4_equal_split,
    gemm_p384_q1_um32_un1_simd8_single_block,
    gemm_p384_q1_um32_un1_simd8_fixed_q,
    gemm_p384_q1_um32_un1_simd8_adaptive_half,
    gemm_p384_q1_um32_un1_simd8_equal_split,
    gemm_p384_q1_um32_un1_simd16_single_block,
    gemm_p384_q1_um32_un1_simd16_fixed_q,
    gemm_p384_q1_um32_un1_simd16_adaptive_half,
    gemm_p384_q1_um32_un1_simd16_equal_split,
    gemm_p384_q1_um32_un4_simd16_single_block,
    gemm_p384_q1_um32_un4_simd16_fixed_q,
    gemm_p384_q1_um32_un4_simd16_adaptive_half,
    gemm_p384_q1_um32_un4_simd16_equal_split,
    gemm_p384_q64_um1_un1_simd1_single_block,
    gemm_p384_q64_um1_un1_simd1_fixed_q,
    gemm_p384_q64_um1_un1_simd1_adaptive_half,
    gemm_p384_q64_um1_un1_simd1_equal_split,
    gemm_p384_q64_um1_un4_simd1_single_block,
    gemm_p384_q64_um1_un4_simd1_fixed_q,
    gemm_p384_q64_um1_un4_simd1_adaptive_half,
    gemm_p384_q64_um1_un4_simd1_equal_split,
    gemm_p384_q64_um4_un1_simd1_single_block,
    gemm_p384_q64_um4_un1_simd1_fixed_q,
    gemm_p384_q64_um4_un1_simd1_adaptive_half,
    gemm_p384_q64_um4_un1_simd1_equal_split,
    gemm_p384_q64_um4_un1_simd4_single_block,
    gemm_p384_q64_um4_un1_simd4_fixed_q,
    gemm_p384_q64_um4_un1_simd4_adaptive_half,
    gemm_p384_q64_um4_un1_simd4_equal_split,
    gemm_p384_q64_um4_un4_simd4_single_block,
    gemm_p384_q64_um4_un4_simd4_fixed_q,
    gemm_p384_q64_um4_un4_simd4_adaptive_half,
    gemm_p384_q64_um4_un4_simd4_equal_split,
    gemm_p384_q64_um8_un1_simd1_single_block,
    gemm_p384_q64_um8_un1_simd1_fixed_q,
    gemm_p384_q64_um8_un1_simd1_adaptive_half,
    gemm_p384_q64_um8_un1_simd1_equal_split,
    gemm_p384_q64_um8_un1_simd4_single_block,
    gemm_p384_q64_um8_un1_simd4_fixed_q,
    gemm_p384_q64_um8_un1_simd4_adaptive_half,
    gemm_p384_q64_um8_un1_simd4_equal_split,
    gemm_p384_q64_um8_un1_simd8_single_block,
    gemm_p384_q64_um8_un1_simd8_fixed_q,
    gemm_p384_q64_um8_un1_simd8_adaptive_half,
    gemm_p384_q64_um8_un1_simd8_equal_split,
    gemm_p384_q64_um8_un4_simd4_single_block,
    gemm_p384_q64_um8_un4_simd4_fixed_q,
    gemm_p384_q64_um8_un4_simd4_adaptive_half,
    gemm_p384_q64_um8_un4_simd4_equal_split,
    gemm_p384_q64_um8_un4_simd8_single_block,
    gemm_p384_q64_um8_un4_simd8_fixed_q,
    gemm_p384_q64_um8_un4_simd8_adaptive_half,
    gemm_p384_q64_um8_un4_simd8_equal_split,
    gemm_p384_q64_um16_un1_simd4_single_block,
    gemm_p384_q64_um16_un1_simd4_fixed_q,
    gemm_p384_q64_um16_un1_simd4_adaptive_half,
    gemm_p384_q64_um16_un1_simd4_equal_split,
    gemm_p384_q64_um16_un1_simd8_single_block,
    gemm_p384_q64_um16_un1_simd8_fixed_q,
    gemm_p384_q64_um16_un1_simd8_adaptive_half,
    gemm_p384_q64_um16_un1_simd8_equal_split,
    gemm_p384_q64_um16_un1_simd16_single_block,
    gemm_p384_q64_um16_un1_simd16_fixed_q,
    gemm_p384_q64_um16_un1_simd16_adaptive_half,
    gemm_p384_q64_um16_un1_simd16_equal_split,
    gemm_p384_q64_um16_un4_simd8_single_block,
    gemm_p384_q64_um16_un4_simd8_fixed_q,
    gemm_p384_q64_um16_un4_simd8_adaptive_half,
    gemm_p384_q64_um16_un4_simd8_equal_split,
    gemm_p384_q64_um16_un4_simd16_single_block,
    gemm_p384_q64_um16_un4_simd16_fixed_q,
    gemm_p384_q64_um16_un4_simd16_adaptive_half,
    gemm_p384_q64_um16_un4_simd16_equal_split,
    gemm_p384_q64_um32_un1_simd4_single_block,
    gemm_p384_q64_um32_un1_simd4_fixed_q,
    gemm_p384_q64_um32_un1_simd4_adaptive_half,
    gemm_p384_q64_um32_un1_simd4_equal_split,
    gemm_p384_q64_um32_un1_simd8_single_block,
    gemm_p384_q64_um32_un1_simd8_fixed_q,
    gemm_p384_q64_um32_un1_simd8_adaptive_half,
    gemm_p384_q64_um32_un1_simd8_equal_split,
    gemm_p384_q64_um32_un1_simd16_single_block,
    gemm_p384_q64_um32_un1_simd16_fixed_q,
    gemm_p384_q64_um32_un1_simd16_adaptive_half,
    gemm_p384_q64_um32_un1_simd16_equal_split,
    gemm_p384_q64_um32_un4_simd16_single_block,
    gemm_p384_q64_um32_un4_simd16_fixed_q,
    gemm_p384_q64_um32_un4_simd16_adaptive_half,
    gemm_p384_q64_um32_un4_simd16_equal_split,
    gemm_p384_q128_um1_un1_simd1_single_block,
    gemm_p384_q128_um1_un1_simd1_fixed_q,
    gemm_p384_q128_um1_un1_simd1_adaptive_half,
    gemm_p384_q128_um1_un1_simd1_equal_split,
    gemm_p384_q128_um1_un4_simd1_single_block,
    gemm_p384_q128_um1_un4_simd1_fixed_q,
    gemm_p384_q128_um1_un4_simd1_adaptive_half,
    gemm_p384_q128_um1_un4_simd1_equal_split,
    gemm_p384_q128_um4_un1_simd1_single_block,
    gemm_p384_q128_um4_un1_simd1_fixed_q,
    gemm_p384_q128_um4_un1_simd1_adaptive_half,
    gemm_p384_q128_um4_un1_simd1_equal_split,
    gemm_p384_q128_um4_un1_simd4_single_block,
    gemm_p384_q128_um4_un1_simd4_fixed_q,
    gemm_p384_q128_um4_un1_simd4_adaptive_half,
    gemm_p384_q128_um4_un1_simd4_equal_split,
    gemm_p384_q128_um4_un4_simd4_single_block,
    gemm_p384_q128_um4_un4_simd4_fixed_q,
    gemm_p384_q128_um4_un4_simd4_adaptive_half,
    gemm_p384_q128_um4_un4_simd4_equal_split,
    gemm_p384_q128_um8_un1_simd1_single_block,
    gemm_p384_q128_um8_un1_simd1_fixed_q,
    gemm_p384_q128_um8_un1_simd1_adaptive_half,
    gemm_p384_q128_um8_un1_simd1_equal_split,
    gemm_p384_q128_um8_un1_simd4_single_block,
    gemm_p384_q128_um8_un1_simd4_fixed_q,
    gemm_p384_q128_um8_un1_simd4_adaptive_half,
    gemm_p384_q128_um8_un1_simd4_equal_split,
    gemm_p384_q128_um8_un1_simd8_single_block,
    gemm_p384_q128_um8_un1_simd8_fixed_q,
    gemm_p384_q128_um8_un1_simd8_adaptive_half,
    gemm_p384_q128_um8_un1_simd8_equal_split,
    gemm_p384_q128_um8_un4_simd4_single_block,
    gemm_p384_q128_um8_un4_simd4_fixed_q,
    gemm_p384_q128_um8_un4_simd4_adaptive_half,
    gemm_p384_q128_um8_un4_simd4_equal_split,
    gemm_p384_q128_um8_un4_simd8_single_block,
    gemm_p384_q128_um8_un4_simd8_fixed_q,
    gemm_p384_q128_um8_un4_simd8_adaptive_half,
    gemm_p384_q128_um8_un4_simd8_equal_split,
    gemm_p384_q128_um16_un1_simd4_single_block,
    gemm_p384_q128_um16_un1_simd4_fixed_q,
    gemm_p384_q128_um16_un1_simd4_adaptive_half,
    gemm_p384_q128_um16_un1_simd4_equal_split,
    gemm_p384_q128_um16_un1_simd8_single_block,
    gemm_p384_q128_um16_un1_simd8_fixed_q,
    gemm_p384_q128_um16_un1_simd8_adaptive_half,
    gemm_p384_q128_um16_un1_simd8_equal_split,
    gemm_p384_q128_um16_un1_simd16_single_block,
    gemm_p384_q128_um16_un1_simd16_fixed_q,
    gemm_p384_q128_um16_un1_simd16_adaptive_half,
    gemm_p384_q128_um16_un1_simd16_equal_split,
    gemm_p384_q128_um16_un4_simd8_single_block,
    gemm_p384_q128_um16_un4_simd8_fixed_q,
    gemm_p384_q128_um16_un4_simd8_adaptive_half,
    gemm_p384_q128_um16_un4_simd8_equal_split,
    gemm_p384_q128_um16_un4_simd16_single_block,
    gemm_p384_q128_um16_un4_simd16_fixed_q,
    gemm_p384_q128_um16_un4_simd16_adaptive_half,
    gemm_p384_q128_um16_un4_simd16_equal_split,
    gemm_p384_q128_um32_un1_simd4_single_block,
    gemm_p384_q128_um32_un1_simd4_fixed_q,
    gemm_p384_q128_um32_un1_simd4_adaptive_half,
    gemm_p384_q128_um32_un1_simd4_equal_split,
    gemm_p384_q128_um32_un1_simd8_single_block,
    gemm_p384_q128_um32_un1_simd8_fixed_q,
    gemm_p384_q128_um32_un1_simd8_adaptive_half,
    gemm_p384_q128_um32_un1_simd8_equal_split,
    gemm_p384_q128_um32_un1_simd16_single_block,
    gemm_p384_q128_um32_un1_simd16_fixed_q,
    gemm_p384_q128_um32_un1_simd16_adaptive_half,
    gemm_p384_q128_um32_un1_simd16_equal_split,
    gemm_p384_q128_um32_un4_simd16_single_block,
    gemm_p384_q128_um32_un4_simd16_fixed_q,
    gemm_p384_q128_um32_un4_simd16_adaptive_half,
    gemm_p384_q128_um32_un4_simd16_equal_split,
    gemm_p384_q192_um1_un1_simd1_single_block,
    gemm_p384_q192_um1_un1_simd1_fixed_q,
    gemm_p384_q192_um1_un1_simd1_adaptive_half,
    gemm_p384_q192_um1_un1_simd1_equal_split,
    gemm_p384_q192_um1_un4_simd1_single_block,
    gemm_p384_q192_um1_un4_simd1_fixed_q,
    gemm_p384_q192_um1_un4_simd1_adaptive_half,
    gemm_p384_q192_um1_un4_simd1_equal_split,
    gemm_p384_q192_um4_un1_simd1_single_block,
    gemm_p384_q192_um4_un1_simd1_fixed_q,
    gemm_p384_q192_um4_un1_simd1_adaptive_half,
    gemm_p384_q192_um4_un1_simd1_equal_split,
    gemm_p384_q192_um4_un1_simd4_single_block,
    gemm_p384_q192_um4_un1_simd4_fixed_q,
    gemm_p384_q192_um4_un1_simd4_adaptive_half,
    gemm_p384_q192_um4_un1_simd4_equal_split,
    gemm_p384_q192_um4_un4_simd4_single_block,
    gemm_p384_q192_um4_un4_simd4_fixed_q,
    gemm_p384_q192_um4_un4_simd4_adaptive_half,
    gemm_p384_q192_um4_un4_simd4_equal_split,
    gemm_p384_q192_um8_un1_simd1_single_block,
    gemm_p384_q192_um8_un1_simd1_fixed_q,
    gemm_p384_q192_um8_un1_simd1_adaptive_half,
    gemm_p384_q192_um8_un1_simd1_equal_split,
    gemm_p384_q192_um8_un1_simd4_single_block,
    gemm_p384_q192_um8_un1_simd4_fixed_q,
    gemm_p384_q192_um8_un1_simd4_adaptive_half,
    gemm_p384_q192_um8_un1_simd4_equal_split,
    gemm_p384_q192_um8_un1_simd8_single_block,
    gemm_p384_q192_um8_un1_simd8_fixed_q,
    gemm_p384_q192_um8_un1_simd8_adaptive_half,
    gemm_p384_q192_um8_un1_simd8_equal_split,
    gemm_p384_q192_um8_un4_simd4_single_block,
    gemm_p384_q192_um8_un4_simd4_fixed_q,
    gemm_p384_q192_um8_un4_simd4_adaptive_half,
    gemm_p384_q192_um8_un4_simd4_equal_split,
    gemm_p384_q192_um8_un4_simd8_single_block,
    gemm_p384_q192_um8_un4_simd8_fixed_q,
    gemm_p384_q192_um8_un4_simd8_adaptive_half,
    gemm_p384_q192_um8_un4_simd8_equal_split,
    gemm_p384_q192_um16_un1_simd4_single_block,
    gemm_p384_q192_um16_un1_simd4_fixed_q,
    gemm_p384_q192_um16_un1_simd4_adaptive_half,
    gemm_p384_q192_um16_un1_simd4_equal_split,
    gemm_p384_q192_um16_un1_simd8_single_block,
    gemm_p384_q192_um16_un1_simd8_fixed_q,
    gemm_p384_q192_um16_un1_simd8_adaptive_half,
    gemm_p384_q192_um16_un1_simd8_equal_split,
    gemm_p384_q192_um16_un1_simd16_single_block,
    gemm_p384_q192_um16_un1_simd16_fixed_q,
    gemm_p384_q192_um16_un1_simd16_adaptive_half,
    gemm_p384_q192_um16_un1_simd16_equal_split,
    gemm_p384_q192_um16_un4_simd8_single_block,
    gemm_p384_q192_um16_un4_simd8_fixed_q,
    gemm_p384_q192_um16_un4_simd8_adaptive_half,
    gemm_p384_q192_um16_un4_simd8_equal_split,
    gemm_p384_q192_um16_un4_simd16_single_block,
    gemm_p384_q192_um16_un4_simd16_fixed_q,
    gemm_p384_q192_um16_un4_simd16_adaptive_half,
    gemm_p384_q192_um16_un4_simd16_equal_split,
    gemm_p384_q192_um32_un1_simd4_single_block,
    gemm_p384_q192_um32_un1_simd4_fixed_q,
    gemm_p384_q192_um32_un1_simd4_adaptive_half,
    gemm_p384_q192_um32_un1_simd4_equal_split,
    gemm_p384_q192_um32_un1_simd8_single_block,
    gemm_p384_q192_um32_un1_simd8_fixed_q,
    gemm_p384_q192_um32_un1_simd8_adaptive_half,
    gemm_p384_q192_um32_un1_simd8_equal_split,
    gemm_p384_q192_um32_un1_simd16_single_block,
    gemm_p384_q192_um32_un1_simd16_fixed_q,
    gemm_p384_q192_um32_un1_simd16_adaptive_half,
    gemm_p384_q192_um32_un1_simd16_equal_split,
    gemm_p384_q192_um32_un4_simd16_single_block,
    gemm_p384_q192_um32_un4_simd16_fixed_q,
    gemm_p384_q192_um32_un4_simd16_adaptive_half,
    gemm_p384_q192_um32_un4_simd16_equal_split,
    gemm_p384_q240_um1_un1_simd1_single_block,
    gemm_p384_q240_um1_un1_simd1_fixed_q,
    gemm_p384_q240_um1_un1_simd1_adaptive_half,
    gemm_p384_q240_um1_un1_simd1_equal_split,
    gemm_p384_q240_um1_un4_simd1_single_block,
    gemm_p384_q240_um1_un4_simd1_fixed_q,
    gemm_p384_q240_um1_un4_simd1_adaptive_half,
    gemm_p384_q240_um1_un4_simd1_equal_split,
    gemm_p384_q240_um4_un1_simd1_single_block,
    gemm_p384_q240_um4_un1_simd1_fixed_q,
    gemm_p384_q240_um4_un1_simd1_adaptive_half,
    gemm_p384_q240_um4_un1_simd1_equal_split,
    gemm_p384_q240_um4_un1_simd4_single_block,
    gemm_p384_q240_um4_un1_simd4_fixed_q,
    gemm_p384_q240_um4_un1_simd4_adaptive_half,
    gemm_p384_q240_um4_un1_simd4_equal_split,
    gemm_p384_q240_um4_un4_simd4_single_block,
    gemm_p384_q240_um4_un4_simd4_fixed_q,
    gemm_p384_q240_um4_un4_simd4_adaptive_half,
    gemm_p384_q240_um4_un4_simd4_equal_split,
    gemm_p384_q240_um8_un1_simd1_single_block,
    gemm_p384_q240_um8_un1_simd1_fixed_q,
    gemm_p384_q240_um8_un1_simd1_adaptive_half,
    gemm_p384_q240_um8_un1_simd1_equal_split,
    gemm_p384_q240_um8_un1_simd4_single_block,
    gemm_p384_q240_um8_un1_simd4_fixed_q,
    gemm_p384_q240_um8_un1_simd4_adaptive_half,
    gemm_p384_q240_um8_un1_simd4_equal_split,
    gemm_p384_q240_um8_un1_simd8_single_block,
    gemm_p384_q240_um8_un1_simd8_fixed_q,
    gemm_p384_q240_um8_un1_simd8_adaptive_half,
    gemm_p384_q240_um8_un1_simd8_equal_split,
    gemm_p384_q240_um8_un4_simd4_single_block,
    gemm_p384_q240_um8_un4_simd4_fixed_q,
    gemm_p384_q240_um8_un4_simd4_adaptive_half,
    gemm_p384_q240_um8_un4_simd4_equal_split,
    gemm_p384_q240_um8_un4_simd8_single_block,
    gemm_p384_q240_um8_un4_simd8_fixed_q,
    gemm_p384_q240_um8_un4_simd8_adaptive_half,
    gemm_p384_q240_um8_un4_simd8_equal_split,
    gemm_p384_q240_um16_un1_simd4_single_block,
    gemm_p384_q240_um16_un1_simd4_fixed_q,
    gemm_p384_q240_um16_un1_simd4_adaptive_half,
    gemm_p384_q240_um16_un1_simd4_equal_split,
    gemm_p384_q240_um16_un1_simd8_single_block,
    gemm_p384_q240_um16_un1_simd8_fixed_q,
    gemm_p384_q240_um16_un1_simd8_adaptive_half,
    gemm_p384_q240_um16_un1_simd8_equal_split,
    gemm_p384_q240_um16_un1_simd16_single_block,
    gemm_p384_q240_um16_un1_simd16_fixed_q,
    gemm_p384_q240_um16_un1_simd16_adaptive_half,
    gemm_p384_q240_um16_un1_simd16_equal_split,
    gemm_p384_q240_um16_un4_simd8_single_block,
    gemm_p384_q240_um16_un4_simd8_fixed_q,
    gemm_p384_q240_um16_un4_simd8_adaptive_half,
    gemm_p384_q240_um16_un4_simd8_equal_split,
    gemm_p384_q240_um16_un4_simd16_single_block,
    gemm_p384_q240_um16_un4_simd16_fixed_q,
    gemm_p384_q240_um16_un4_simd16_adaptive_half,
    gemm_p384_q240_um16_un4_simd16_equal_split,
    gemm_p384_q240_um32_un1_simd4_single_block,
    gemm_p384_q240_um32_un1_simd4_fixed_q,
    gemm_p384_q240_um32_un1_simd4_adaptive_half,
    gemm_p384_q240_um32_un1_simd4_equal_split,
    gemm_p384_q240_um32_un1_simd8_single_block,
    gemm_p384_q240_um32_un1_simd8_fixed_q,
    gemm_p384_q240_um32_un1_simd8_adaptive_half,
    gemm_p384_q240_um32_un1_simd8_equal_split,
    gemm_p384_q240_um32_un1_simd16_single_block,
    gemm_p384_q240_um32_un1_simd16_fixed_q,
    gemm_p384_q240_um32_un1_simd16_adaptive_half,
    gemm_p384_q240_um32_un1_simd16_equal_split,
    gemm_p384_q248_um1_un1_simd1_single_block,
    gemm_p384_q248_um1_un1_simd1_fixed_q,
    gemm_p384_q248_um1_un1_simd1_adaptive_half,
    gemm_p384_q248_um1_un1_simd1_equal_split,
    gemm_p384_q248_um1_un4_simd1_single_block,
    gemm_p384_q248_um1_un4_simd1_fixed_q,
    gemm_p384_q248_um1_un4_simd1_adaptive_half,
    gemm_p384_q248_um1_un4_simd1_equal_split,
    gemm_p384_q248_um4_un1_simd1_single_block,
    gemm_p384_q248_um4_un1_simd1_fixed_q,
    gemm_p384_q248_um4_un1_simd1_adaptive_half,
    gemm_p384_q248_um4_un1_simd1_equal_split,
    gemm_p384_q248_um4_un1_simd4_single_block,
    gemm_p384_q248_um4_un1_simd4_fixed_q,
    gemm_p384_q248_um4_un1_simd4_adaptive_half,
    gemm_p384_q248_um4_un1_simd4_equal_split,
    gemm_p384_q248_um4_un4_simd4_single_block,
    gemm_p384_q248_um4_un4_simd4_fixed_q,
    gemm_p384_q248_um4_un4_simd4_adaptive_half,
    gemm_p384_q248_um4_un4_simd4_equal_split,
    gemm_p384_q248_um8_un1_simd1_single_block,
    gemm_p384_q248_um8_un1_simd1_fixed_q,
    gemm_p384_q248_um8_un1_simd1_adaptive_half,
    gemm_p384_q248_um8_un1_simd1_equal_split,
    gemm_p384_q248_um8_un1_simd4_single_block,
    gemm_p384_q248_um8_un1_simd4_fixed_q,
    gemm_p384_q248_um8_un1_simd4_adaptive_half,
    gemm_p384_q248_um8_un1_simd4_equal_split,
    gemm_p384_q248_um8_un1_simd8_single_block,
    gemm_p384_q248_um8_un1_simd8_fixed_q,
    gemm_p384_q248_um8_un1_simd8_adaptive_half,
    gemm_p384_q248_um8_un1_simd8_equal_split,
    gemm_p384_q248_um8_un4_simd4_single_block,
    gemm_p384_q248_um8_un4_simd4_fixed_q,
    gemm_p384_q248_um8_un4_simd4_adaptive_half,
    gemm_p384_q248_um8_un4_simd4_equal_split,
    gemm_p384_q248_um8_un4_simd8_single_block,
    gemm_p384_q248_um8_un4_simd8_fixed_q,
    gemm_p384_q248_um8_un4_simd8_adaptive_half,
    gemm_p384_q248_um8_un4_simd8_equal_split,
    gemm_p384_q248_um16_un1_simd4_single_block,
    gemm_p384_q248_um16_un1_simd4_fixed_q,
    gemm_p384_q248_um16_un1_simd4_adaptive_half,
    gemm_p384_q248_um16_un1_simd4_equal_split,
    gemm_p384_q248_um16_un1_simd8_single_block,
    gemm_p384_q248_um16_un1_simd8_fixed_q,
    gemm_p384_q248_um16_un1_simd8_adaptive_half,
    gemm_p384_q248_um16_un1_simd8_equal_split,
    gemm_p384_q248_um16_un1_simd16_single_block,
    gemm_p384_q248_um16_un1_simd16_fixed_q,
    gemm_p384_q248_um16_un1_simd16_adaptive_half,
    gemm_p384_q248_um16_un1_simd16_equal_split,
    gemm_p384_q248_um16_un4_simd8_single_block,
    gemm_p384_q248_um16_un4_simd8_fixed_q,
    gemm_p384_q248_um16_un4_simd8_adaptive_half,
    gemm_p384_q248_um16_un4_simd8_equal_split,
    gemm_p384_q248_um16_un4_simd16_single_block,
    gemm_p384_q248_um16_un4_simd16_fixed_q,
    gemm_p384_q248_um16_un4_simd16_adaptive_half,
    gemm_p384_q248_um16_un4_simd16_equal_split,
    gemm_p384_q248_um32_un1_simd4_single_block,
    gemm_p384_q248_um32_un1_simd4_fixed_q,
    gemm_p384_q248_um32_un1_simd4_adaptive_half,
    gemm_p384_q248_um32_un1_simd4_equal_split,
    gemm_p384_q248_um32_un1_simd8_single_block,
    gemm_p384_q248_um32_un1_simd8_fixed_q,
    gemm_p384_q248_um32_un1_simd8_adaptive_half,
    gemm_p384_q248_um32_un1_simd8_equal_split,
    gemm_p384_q248_um32_un1_simd16_single_block,
    gemm_p384_q248_um32_un1_simd16_fixed_q,
    gemm_p384_q248_um32_un1_simd16_adaptive_half,
    gemm_p384_q248_um32_un1_simd16_equal_split,
    gemm_p384_q256_um1_un1_simd1_single_block,
    gemm_p384_q256_um1_un1_simd1_fixed_q,
    gemm_p384_q256_um1_un1_simd1_adaptive_half,
    gemm_p384_q256_um1_un1_simd1_equal_split,
    gemm_p384_q256_um1_un4_simd1_single_block,
    gemm_p384_q256_um1_un4_simd1_fixed_q,
    gemm_p384_q256_um1_un4_simd1_adaptive_half,
    gemm_p384_q256_um1_un4_simd1_equal_split,
    gemm_p384_q256_um4_un1_simd1_single_block,
    gemm_p384_q256_um4_un1_simd1_fixed_q,
    gemm_p384_q256_um4_un1_simd1_adaptive_half,
    gemm_p384_q256_um4_un1_simd1_equal_split,
    gemm_p384_q256_um4_un1_simd4_single_block,
    gemm_p384_q256_um4_un1_simd4_fixed_q,
    gemm_p384_q256_um4_un1_simd4_adaptive_half,
    gemm_p384_q256_um4_un1_simd4_equal_split,
    gemm_p384_q256_um4_un4_simd4_single_block,
    gemm_p384_q256_um4_un4_simd4_fixed_q,
    gemm_p384_q256_um4_un4_simd4_adaptive_half,
    gemm_p384_q256_um4_un4_simd4_equal_split,
    gemm_p384_q256_um8_un1_simd1_single_block,
    gemm_p384_q256_um8_un1_simd1_fixed_q,
    gemm_p384_q256_um8_un1_simd1_adaptive_half,
    gemm_p384_q256_um8_un1_simd1_equal_split,
    gemm_p384_q256_um8_un1_simd4_single_block,
    gemm_p384_q256_um8_un1_simd4_fixed_q,
    gemm_p384_q256_um8_un1_simd4_adaptive_half,
    gemm_p384_q256_um8_un1_simd4_equal_split,
    gemm_p384_q256_um8_un1_simd8_single_block,
    gemm_p384_q256_um8_un1_simd8_fixed_q,
    gemm_p384_q256_um8_un1_simd8_adaptive_half,
    gemm_p384_q256_um8_un1_simd8_equal_split,
    gemm_p384_q256_um8_un4_simd4_single_block,
    gemm_p384_q256_um8_un4_simd4_fixed_q,
    gemm_p384_q256_um8_un4_simd4_adaptive_half,
    gemm_p384_q256_um8_un4_simd4_equal_split,
    gemm_p384_q256_um8_un4_simd8_single_block,
    gemm_p384_q256_um8_un4_simd8_fixed_q,
    gemm_p384_q256_um8_un4_simd8_adaptive_half,
    gemm_p384_q256_um8_un4_simd8_equal_split,
    gemm_p384_q256_um16_un1_simd4_single_block,
    gemm_p384_q256_um16_un1_simd4_fixed_q,
    gemm_p384_q256_um16_un1_simd4_adaptive_half,
    gemm_p384_q256_um16_un1_simd4_equal_split,
    gemm_p384_q256_um16_un1_simd8_single_block,
    gemm_p384_q256_um16_un1_simd8_fixed_q,
    gemm_p384_q256_um16_un1_simd8_adaptive_half,
    gemm_p384_q256_um16_un1_simd8_equal_split,
    gemm_p384_q256_um16_un1_simd16_single_block,
    gemm_p384_q256_um16_un1_simd16_fixed_q,
    gemm_p384_q256_um16_un1_simd16_adaptive_half,
    gemm_p384_q256_um16_un1_simd16_equal_split,
    gemm_p384_q256_um16_un4_simd8_single_block,
    gemm_p384_q256_um16_un4_simd8_fixed_q,
    gemm_p384_q256_um16_un4_simd8_adaptive_half,
    gemm_p384_q256_um16_un4_simd8_equal_split,
    gemm_p384_q256_um16_un4_simd16_single_block,
    gemm_p384_q256_um16_un4_simd16_fixed_q,
    gemm_p384_q256_um16_un4_simd16_adaptive_half,
    gemm_p384_q256_um16_un4_simd16_equal_split,
    gemm_p384_q384_um1_un1_simd1_single_block,
    gemm_p384_q384_um1_un1_simd1_fixed_q,
    gemm_p384_q384_um1_un1_simd1_adaptive_half,
    gemm_p384_q384_um1_un1_simd1_equal_split,
    gemm_p384_q384_um1_un4_simd1_single_block,
    gemm_p384_q384_um1_un4_simd1_fixed_q,
    gemm_p384_q384_um1_un4_simd1_adaptive_half,
    gemm_p384_q384_um1_un4_simd1_equal_split,
    gemm_p384_q384_um4_un1_simd1_single_block,
    gemm_p384_q384_um4_un1_simd1_fixed_q,
    gemm_p384_q384_um4_un1_simd1_adaptive_half,
    gemm_p384_q384_um4_un1_simd1_equal_split,
    gemm_p384_q384_um4_un1_simd4_single_block,
    gemm_p384_q384_um4_un1_simd4_fixed_q,
    gemm_p384_q384_um4_un1_simd4_adaptive_half,
    gemm_p384_q384_um4_un1_simd4_equal_split,
    gemm_p384_q384_um4_un4_simd4_single_block,
    gemm_p384_q384_um4_un4_simd4_fixed_q,
    gemm_p384_q384_um4_un4_simd4_adaptive_half,
    gemm_p384_q384_um4_un4_simd4_equal_split,
    gemm_p384_q384_um8_un1_simd1_single_block,
    gemm_p384_q384_um8_un1_simd1_fixed_q,
    gemm_p384_q384_um8_un1_simd1_adaptive_half,
    gemm_p384_q384_um8_un1_simd1_equal_split,
    gemm_p384_q384_um8_un1_simd4_single_block,
    gemm_p384_q384_um8_un1_simd4_fixed_q,
    gemm_p384_q384_um8_un1_simd4_adaptive_half,
    gemm_p384_q384_um8_un1_simd4_equal_split,
    gemm_p384_q384_um8_un1_simd8_single_block,
    gemm_p384_q384_um8_un1_simd8_fixed_q,
    gemm_p384_q384_um8_un1_simd8_adaptive_half,
    gemm_p384_q384_um8_un1_simd8_equal_split,
    gemm_p384_q384_um8_un4_simd4_single_block,
    gemm_p384_q384_um8_un4_simd4_fixed_q,
    gemm_p384_q384_um8_un4_simd4_adaptive_half,
    gemm_p384_q384_um8_un4_simd4_equal_split,
    gemm_p384_q384_um8_un4_simd8_single_block,
    gemm_p384_q384_um8_un4_simd8_fixed_q,
    gemm_p384_q384_um8_un4_simd8_adaptive_half,
    gemm_p384_q384_um8_un4_simd8_equal_split,
    gemm_p384_q384_um16_un1_simd4_single_block,
    gemm_p384_q384_um16_un1_simd4_fixed_q,
    gemm_p384_q384_um16_un1_simd4_adaptive_half,
    gemm_p384_q384_um16_un1_simd4_equal_split,
    gemm_p384_q384_um16_un1_simd8_single_block,
    gemm_p384_q384_um16_un1_simd8_fixed_q,
    gemm_p384_q384_um16_un1_simd8_adaptive_half,
    gemm_p384_q384_um16_un1_simd8_equal_split,
    gemm_p384_q384_um16_un1_simd16_single_block,
    gemm_p384_q384_um16_un1_simd16_fixed_q,
    gemm_p384_q384_um16_un1_simd16_adaptive_half,
    gemm_p384_q384_um16_un1_simd16_equal_split,
    gemm_p384_q384_um16_un4_simd8_single_block,
    gemm_p384_q384_um16_un4_simd8_fixed_q,
    gemm_p384_q384_um16_un4_simd8_adaptive_half,
    gemm_p384_q384_um16_un4_simd8_equal_split,
    gemm_p384_q384_um16_un4_simd16_single_block,
    gemm_p384_q384_um16_un4_simd16_fixed_q,
    gemm_p384_q384_um16_un4_simd16_adaptive_half,
    gemm_p384_q384_um16_un4_simd16_equal_split,
    gemm_p384_q512_um1_un1_simd1_single_block,
    gemm_p384_q512_um1_un1_simd1_fixed_q,
    gemm_p384_q512_um1_un1_simd1_adaptive_half,
    gemm_p384_q512_um1_un1_simd1_equal_split,
    gemm_p384_q512_um1_un4_simd1_single_block,
    gemm_p384_q512_um1_un4_simd1_fixed_q,
    gemm_p384_q512_um1_un4_simd1_adaptive_half,
    gemm_p384_q512_um1_un4_simd1_equal_split,
    gemm_p384_q512_um4_un1_simd1_single_block,
    gemm_p384_q512_um4_un1_simd1_fixed_q,
    gemm_p384_q512_um4_un1_simd1_adaptive_half,
    gemm_p384_q512_um4_un1_simd1_equal_split,
    gemm_p384_q512_um4_un1_simd4_single_block,
    gemm_p384_q512_um4_un1_simd4_fixed_q,
    gemm_p384_q512_um4_un1_simd4_adaptive_half,
    gemm_p384_q512_um4_un1_simd4_equal_split,
    gemm_p384_q512_um4_un4_simd4_single_block,
    gemm_p384_q512_um4_un4_simd4_fixed_q,
    gemm_p384_q512_um4_un4_simd4_adaptive_half,
    gemm_p384_q512_um4_un4_simd4_equal_split,
    gemm_p384_q512_um8_un1_simd1_single_block,
    gemm_p384_q512_um8_un1_simd1_fixed_q,
    gemm_p384_q512_um8_un1_simd1_adaptive_half,
    gemm_p384_q512_um8_un1_simd1_equal_split,
    gemm_p384_q512_um8_un1_simd4_single_block,
    gemm_p384_q512_um8_un1_simd4_fixed_q,
    gemm_p384_q512_um8_un1_simd4_adaptive_half,
    gemm_p384_q512_um8_un1_simd4_equal_split,
    gemm_p384_q512_um8_un1_simd8_single_block,
    gemm_p384_q512_um8_un1_simd8_fixed_q,
    gemm_p384_q512_um8_un1_simd8_adaptive_half,
    gemm_p384_q512_um8_un1_simd8_equal_split,
    gemm_p384_q512_um8_un4_simd4_single_block,
    gemm_p384_q512_um8_un4_simd4_fixed_q,
    gemm_p384_q512_um8_un4_simd4_adaptive_half,
    gemm_p384_q512_um8_un4_simd4_equal_split,
    gemm_p384_q512_um8_un4_simd8_single_block,
    gemm_p384_q512_um8_un4_simd8_fixed_q,
    gemm_p384_q512_um8_un4_simd8_adaptive_half,
    gemm_p384_q512_um8_un4_simd8_equal_split,
    gemm_p512_q1_um1_un1_simd1_single_block,
    gemm_p512_q1_um1_un1_simd1_fixed_q,
    gemm_p512_q1_um1_un1_simd1_adaptive_half,
    gemm_p512_q1_um1_un1_simd1_equal_split,
    gemm_p512_q1_um1_un4_simd1_single_block,
    gemm_p512_q1_um1_un4_simd1_fixed_q,
    gemm_p512_q1_um1_un4_simd1_adaptive_half,
    gemm_p512_q1_um1_un4_simd1_equal_split,
    gemm_p512_q1_um4_un1_simd1_single_block,
    gemm_p512_q1_um4_un1_simd1_fixed_q,
    gemm_p512_q1_um4_un1_simd1_adaptive_half,
    gemm_p512_q1_um4_un1_simd1_equal_split,
    gemm_p512_q1_um4_un1_simd4_single_block,
    gemm_p512_q1_um4_un1_simd4_fixed_q,
    gemm_p512_q1_um4_un1_simd4_adaptive_half,
    gemm_p512_q1_um4_un1_simd4_equal_split,
    gemm_p512_q1_um4_un4_simd4_single_block,
    gemm_p512_q1_um4_un4_simd4_fixed_q,
    gemm_p512_q1_um4_un4_simd4_adaptive_half,
    gemm_p512_q1_um4_un4_simd4_equal_split,
    gemm_p512_q1_um8_un1_simd1_single_block,
    gemm_p512_q1_um8_un1_simd1_fixed_q,
    gemm_p512_q1_um8_un1_simd1_adaptive_half,
    gemm_p512_q1_um8_un1_simd1_equal_split,
    gemm_p512_q1_um8_un1_simd4_single_block,
    gemm_p512_q1_um8_un1_simd4_fixed_q,
    gemm_p512_q1_um8_un1_simd4_adaptive_half,
    gemm_p512_q1_um8_un1_simd4_equal_split,
    gemm_p512_q1_um8_un1_simd8_single_block,
    gemm_p512_q1_um8_un1_simd8_fixed_q,
    gemm_p512_q1_um8_un1_simd8_adaptive_half,
    gemm_p512_q1_um8_un1_simd8_equal_split,
    gemm_p512_q1_um8_un4_simd4_single_block,
    gemm_p512_q1_um8_un4_simd4_fixed_q,
    gemm_p512_q1_um8_un4_simd4_adaptive_half,
    gemm_p512_q1_um8_un4_simd4_equal_split,
    gemm_p512_q1_um8_un4_simd8_single_block,
    gemm_p512_q1_um8_un4_simd8_fixed_q,
    gemm_p512_q1_um8_un4_simd8_adaptive_half,
    gemm_p512_q1_um8_un4_simd8_equal_split,
    gemm_p512_q1_um16_un1_simd4_single_block,
    gemm_p512_q1_um16_un1_simd4_fixed_q,
    gemm_p512_q1_um16_un1_simd4_adaptive_half,
    gemm_p512_q1_um16_un1_simd4_equal_split,
    gemm_p512_q1_um16_un1_simd8_single_block,
    gemm_p512_q1_um16_un1_simd8_fixed_q,
    gemm_p512_q1_um16_un1_simd8_adaptive_half,
    gemm_p512_q1_um16_un1_simd8_equal_split,
    gemm_p512_q1_um16_un1_simd16_single_block,
    gemm_p512_q1_um16_un1_simd16_fixed_q,
    gemm_p512_q1_um16_un1_simd16_adaptive_half,
    gemm_p512_q1_um16_un1_simd16_equal_split,
    gemm_p512_q1_um16_un4_simd8_single_block,
    gemm_p512_q1_um16_un4_simd8_fixed_q,
    gemm_p512_q1_um16_un4_simd8_adaptive_half,
    gemm_p512_q1_um16_un4_simd8_equal_split,
    gemm_p512_q1_um16_un4_simd16_single_block,
    gemm_p512_q1_um16_un4_simd16_fixed_q,
    gemm_p512_q1_um16_un4_simd16_adaptive_half,
    gemm_p512_q1_um16_un4_simd16_equal_split,
    gemm_p512_q1_um32_un1_simd4_single_block,
    gemm_p512_q1_um32_un1_simd4_fixed_q,
    gemm_p512_q1_um32_un1_simd4_adaptive_half,
    gemm_p512_q1_um32_un1_simd4_equal_split,
    gemm_p512_q1_um32_un1_simd8_single_block,
    gemm_p512_q1_um32_un1_simd8_fixed_q,
    gemm_p512_q1_um32_un1_simd8_adaptive_half,
    gemm_p512_q1_um32_un1_simd8_equal_split,
    gemm_p512_q1_um32_un1_simd16_single_block,
    gemm_p512_q1_um32_un1_simd16_fixed_q,
    gemm_p512_q1_um32_un1_simd16_adaptive_half,
    gemm_p512_q1_um32_un1_simd16_equal_split,
    gemm_p512_q1_um32_un4_simd16_single_block,
    gemm_p512_q1_um32_un4_simd16_fixed_q,
    gemm_p512_q1_um32_un4_simd16_adaptive_half,
    gemm_p512_q1_um32_un4_simd16_equal_split,
    gemm_p512_q64_um1_un1_simd1_single_block,
    gemm_p512_q64_um1_un1_simd1_fixed_q,
    gemm_p512_q64_um1_un1_simd1_adaptive_half,
    gemm_p512_q64_um1_un1_simd1_equal_split,
    gemm_p512_q64_um1_un4_simd1_single_block,
    gemm_p512_q64_um1_un4_simd1_fixed_q,
    gemm_p512_q64_um1_un4_simd1_adaptive_half,
    gemm_p512_q64_um1_un4_simd1_equal_split,
    gemm_p512_q64_um4_un1_simd1_single_block,
    gemm_p512_q64_um4_un1_simd1_fixed_q,
    gemm_p512_q64_um4_un1_simd1_adaptive_half,
    gemm_p512_q64_um4_un1_simd1_equal_split,
    gemm_p512_q64_um4_un1_simd4_single_block,
    gemm_p512_q64_um4_un1_simd4_fixed_q,
    gemm_p512_q64_um4_un1_simd4_adaptive_half,
    gemm_p512_q64_um4_un1_simd4_equal_split,
    gemm_p512_q64_um4_un4_simd4_single_block,
    gemm_p512_q64_um4_un4_simd4_fixed_q,
    gemm_p512_q64_um4_un4_simd4_adaptive_half,
    gemm_p512_q64_um4_un4_simd4_equal_split,
    gemm_p512_q64_um8_un1_simd1_single_block,
    gemm_p512_q64_um8_un1_simd1_fixed_q,
    gemm_p512_q64_um8_un1_simd1_adaptive_half,
    gemm_p512_q64_um8_un1_simd1_equal_split,
    gemm_p512_q64_um8_un1_simd4_single_block,
    gemm_p512_q64_um8_un1_simd4_fixed_q,
    gemm_p512_q64_um8_un1_simd4_adaptive_half,
    gemm_p512_q64_um8_un1_simd4_equal_split,
    gemm_p512_q64_um8_un1_simd8_single_block,
    gemm_p512_q64_um8_un1_simd8_fixed_q,
    gemm_p512_q64_um8_un1_simd8_adaptive_half,
    gemm_p512_q64_um8_un1_simd8_equal_split,
    gemm_p512_q64_um8_un4_simd4_single_block,
    gemm_p512_q64_um8_un4_simd4_fixed_q,
    gemm_p512_q64_um8_un4_simd4_adaptive_half,
    gemm_p512_q64_um8_un4_simd4_equal_split,
    gemm_p512_q64_um8_un4_simd8_single_block,
    gemm_p512_q64_um8_un4_simd8_fixed_q,
    gemm_p512_q64_um8_un4_simd8_adaptive_half,
    gemm_p512_q64_um8_un4_simd8_equal_split,
    gemm_p512_q64_um16_un1_simd4_single_block,
    gemm_p512_q64_um16_un1_simd4_fixed_q,
    gemm_p512_q64_um16_un1_simd4_adaptive_half,
    gemm_p512_q64_um16_un1_simd4_equal_split,
    gemm_p512_q64_um16_un1_simd8_single_block,
    gemm_p512_q64_um16_un1_simd8_fixed_q,
    gemm_p512_q64_um16_un1_simd8_adaptive_half,
    gemm_p512_q64_um16_un1_simd8_equal_split,
    gemm_p512_q64_um16_un1_simd16_single_block,
    gemm_p512_q64_um16_un1_simd16_fixed_q,
    gemm_p512_q64_um16_un1_simd16_adaptive_half,
    gemm_p512_q64_um16_un1_simd16_equal_split,
    gemm_p512_q64_um16_un4_simd8_single_block,
    gemm_p512_q64_um16_un4_simd8_fixed_q,
    gemm_p512_q64_um16_un4_simd8_adaptive_half,
    gemm_p512_q64_um16_un4_simd8_equal_split,
    gemm_p512_q64_um16_un4_simd16_single_block,
    gemm_p512_q64_um16_un4_simd16_fixed_q,
    gemm_p512_q64_um16_un4_simd16_adaptive_half,
    gemm_p512_q64_um16_un4_simd16_equal_split,
    gemm_p512_q64_um32_un1_simd4_single_block,
    gemm_p512_q64_um32_un1_simd4_fixed_q,
    gemm_p512_q64_um32_un1_simd4_adaptive_half,
    gemm_p512_q64_um32_un1_simd4_equal_split,
    gemm_p512_q64_um32_un1_simd8_single_block,
    gemm_p512_q64_um32_un1_simd8_fixed_q,
    gemm_p512_q64_um32_un1_simd8_adaptive_half,
    gemm_p512_q64_um32_un1_simd8_equal_split,
    gemm_p512_q64_um32_un1_simd16_single_block,
    gemm_p512_q64_um32_un1_simd16_fixed_q,
    gemm_p512_q64_um32_un1_simd16_adaptive_half,
    gemm_p512_q64_um32_un1_simd16_equal_split,
    gemm_p512_q64_um32_un4_simd16_single_block,
    gemm_p512_q64_um32_un4_simd16_fixed_q,
    gemm_p512_q64_um32_un4_simd16_adaptive_half,
    gemm_p512_q64_um32_un4_simd16_equal_split,
    gemm_p512_q128_um1_un1_simd1_single_block,
    gemm_p512_q128_um1_un1_simd1_fixed_q,
    gemm_p512_q128_um1_un1_simd1_adaptive_half,
    gemm_p512_q128_um1_un1_simd1_equal_split,
    gemm_p512_q128_um1_un4_simd1_single_block,
    gemm_p512_q128_um1_un4_simd1_fixed_q,
    gemm_p512_q128_um1_un4_simd1_adaptive_half,
    gemm_p512_q128_um1_un4_simd1_equal_split,
    gemm_p512_q128_um4_un1_simd1_single_block,
    gemm_p512_q128_um4_un1_simd1_fixed_q,
    gemm_p512_q128_um4_un1_simd1_adaptive_half,
    gemm_p512_q128_um4_un1_simd1_equal_split,
    gemm_p512_q128_um4_un1_simd4_single_block,
    gemm_p512_q128_um4_un1_simd4_fixed_q,
    gemm_p512_q128_um4_un1_simd4_adaptive_half,
    gemm_p512_q128_um4_un1_simd4_equal_split,
    gemm_p512_q128_um4_un4_simd4_single_block,
    gemm_p512_q128_um4_un4_simd4_fixed_q,
    gemm_p512_q128_um4_un4_simd4_adaptive_half,
    gemm_p512_q128_um4_un4_simd4_equal_split,
    gemm_p512_q128_um8_un1_simd1_single_block,
    gemm_p512_q128_um8_un1_simd1_fixed_q,
    gemm_p512_q128_um8_un1_simd1_adaptive_half,
    gemm_p512_q128_um8_un1_simd1_equal_split,
    gemm_p512_q128_um8_un1_simd4_single_block,
    gemm_p512_q128_um8_un1_simd4_fixed_q,
    gemm_p512_q128_um8_un1_simd4_adaptive_half,
    gemm_p512_q128_um8_un1_simd4_equal_split,
    gemm_p512_q128_um8_un1_simd8_single_block,
    gemm_p512_q128_um8_un1_simd8_fixed_q,
    gemm_p512_q128_um8_un1_simd8_adaptive_half,
    gemm_p512_q128_um8_un1_simd8_equal_split,
    gemm_p512_q128_um8_un4_simd4_single_block,
    gemm_p512_q128_um8_un4_simd4_fixed_q,
    gemm_p512_q128_um8_un4_simd4_adaptive_half,
    gemm_p512_q128_um8_un4_simd4_equal_split,
    gemm_p512_q128_um8_un4_simd8_single_block,
    gemm_p512_q128_um8_un4_simd8_fixed_q,
    gemm_p512_q128_um8_un4_simd8_adaptive_half,
    gemm_p512_q128_um8_un4_simd8_equal_split,
    gemm_p512_q128_um16_un1_simd4_single_block,
    gemm_p512_q128_um16_un1_simd4_fixed_q,
    gemm_p512_q128_um16_un1_simd4_adaptive_half,
    gemm_p512_q128_um16_un1_simd4_equal_split,
    gemm_p512_q128_um16_un1_simd8_single_block,
    gemm_p512_q128_um16_un1_simd8_fixed_q,
    gemm_p512_q128_um16_un1_simd8_adaptive_half,
    gemm_p512_q128_um16_un1_simd8_equal_split,
    gemm_p512_q128_um16_un1_simd16_single_block,
    gemm_p512_q128_um16_un1_simd16_fixed_q,
    gemm_p512_q128_um16_un1_simd16_adaptive_half,
    gemm_p512_q128_um16_un1_simd16_equal_split,
    gemm_p512_q128_um16_un4_simd8_single_block,
    gemm_p512_q128_um16_un4_simd8_fixed_q,
    gemm_p512_q128_um16_un4_simd8_adaptive_half,
    gemm_p512_q128_um16_un4_simd8_equal_split,
    gemm_p512_q128_um16_un4_simd16_single_block,
    gemm_p512_q128_um16_un4_simd16_fixed_q,
    gemm_p512_q128_um16_un4_simd16_adaptive_half,
    gemm_p512_q128_um16_un4_simd16_equal_split,
    gemm_p512_q128_um32_un1_simd4_single_block,
    gemm_p512_q128_um32_un1_simd4_fixed_q,
    gemm_p512_q128_um32_un1_simd4_adaptive_half,
    gemm_p512_q128_um32_un1_simd4_equal_split,
    gemm_p512_q128_um32_un1_simd8_single_block,
    gemm_p512_q128_um32_un1_simd8_fixed_q,
    gemm_p512_q128_um32_un1_simd8_adaptive_half,
    gemm_p512_q128_um32_un1_simd8_equal_split,
    gemm_p512_q128_um32_un1_simd16_single_block,
    gemm_p512_q128_um32_un1_simd16_fixed_q,
    gemm_p512_q128_um32_un1_simd16_adaptive_half,
    gemm_p512_q128_um32_un1_simd16_equal_split,
    gemm_p512_q128_um32_un4_simd16_single_block,
    gemm_p512_q128_um32_un4_simd16_fixed_q,
    gemm_p512_q128_um32_un4_simd16_adaptive_half,
    gemm_p512_q128_um32_un4_simd16_equal_split,
    gemm_p512_q192_um1_un1_simd1_single_block,
    gemm_p512_q192_um1_un1_simd1_fixed_q,
    gemm_p512_q192_um1_un1_simd1_adaptive_half,
    gemm_p512_q192_um1_un1_simd1_equal_split,
    gemm_p512_q192_um1_un4_simd1_single_block,
    gemm_p512_q192_um1_un4_simd1_fixed_q,
    gemm_p512_q192_um1_un4_simd1_adaptive_half,
    gemm_p512_q192_um1_un4_simd1_equal_split,
    gemm_p512_q192_um4_un1_simd1_single_block,
    gemm_p512_q192_um4_un1_simd1_fixed_q,
    gemm_p512_q192_um4_un1_simd1_adaptive_half,
    gemm_p512_q192_um4_un1_simd1_equal_split,
    gemm_p512_q192_um4_un1_simd4_single_block,
    gemm_p512_q192_um4_un1_simd4_fixed_q,
    gemm_p512_q192_um4_un1_simd4_adaptive_half,
    gemm_p512_q192_um4_un1_simd4_equal_split,
    gemm_p512_q192_um4_un4_simd4_single_block,
    gemm_p512_q192_um4_un4_simd4_fixed_q,
    gemm_p512_q192_um4_un4_simd4_adaptive_half,
    gemm_p512_q192_um4_un4_simd4_equal_split,
    gemm_p512_q192_um8_un1_simd1_single_block,
    gemm_p512_q192_um8_un1_simd1_fixed_q,
    gemm_p512_q192_um8_un1_simd1_adaptive_half,
    gemm_p512_q192_um8_un1_simd1_equal_split,
    gemm_p512_q192_um8_un1_simd4_single_block,
    gemm_p512_q192_um8_un1_simd4_fixed_q,
    gemm_p512_q192_um8_un1_simd4_adaptive_half,
    gemm_p512_q192_um8_un1_simd4_equal_split,
    gemm_p512_q192_um8_un1_simd8_single_block,
    gemm_p512_q192_um8_un1_simd8_fixed_q,
    gemm_p512_q192_um8_un1_simd8_adaptive_half,
    gemm_p512_q192_um8_un1_simd8_equal_split,
    gemm_p512_q192_um8_un4_simd4_single_block,
    gemm_p512_q192_um8_un4_simd4_fixed_q,
    gemm_p512_q192_um8_un4_simd4_adaptive_half,
    gemm_p512_q192_um8_un4_simd4_equal_split,
    gemm_p512_q192_um8_un4_simd8_single_block,
    gemm_p512_q192_um8_un4_simd8_fixed_q,
    gemm_p512_q192_um8_un4_simd8_adaptive_half,
    gemm_p512_q192_um8_un4_simd8_equal_split,
    gemm_p512_q192_um16_un1_simd4_single_block,
    gemm_p512_q192_um16_un1_simd4_fixed_q,
    gemm_p512_q192_um16_un1_simd4_adaptive_half,
    gemm_p512_q192_um16_un1_simd4_equal_split,
    gemm_p512_q192_um16_un1_simd8_single_block,
    gemm_p512_q192_um16_un1_simd8_fixed_q,
    gemm_p512_q192_um16_un1_simd8_adaptive_half,
    gemm_p512_q192_um16_un1_simd8_equal_split,
    gemm_p512_q192_um16_un1_simd16_single_block,
    gemm_p512_q192_um16_un1_simd16_fixed_q,
    gemm_p512_q192_um16_un1_simd16_adaptive_half,
    gemm_p512_q192_um16_un1_simd16_equal_split,
    gemm_p512_q192_um16_un4_simd8_single_block,
    gemm_p512_q192_um16_un4_simd8_fixed_q,
    gemm_p512_q192_um16_un4_simd8_adaptive_half,
    gemm_p512_q192_um16_un4_simd8_equal_split,
    gemm_p512_q192_um16_un4_simd16_single_block,
    gemm_p512_q192_um16_un4_simd16_fixed_q,
    gemm_p512_q192_um16_un4_simd16_adaptive_half,
    gemm_p512_q192_um16_un4_simd16_equal_split,
    gemm_p512_q192_um32_un1_simd4_single_block,
    gemm_p512_q192_um32_un1_simd4_fixed_q,
    gemm_p512_q192_um32_un1_simd4_adaptive_half,
    gemm_p512_q192_um32_un1_simd4_equal_split,
    gemm_p512_q192_um32_un1_simd8_single_block,
    gemm_p512_q192_um32_un1_simd8_fixed_q,
    gemm_p512_q192_um32_un1_simd8_adaptive_half,
    gemm_p512_q192_um32_un1_simd8_equal_split,
    gemm_p512_q192_um32_un1_simd16_single_block,
    gemm_p512_q192_um32_un1_simd16_fixed_q,
    gemm_p512_q192_um32_un1_simd16_adaptive_half,
    gemm_p512_q192_um32_un1_simd16_equal_split,
    gemm_p512_q192_um32_un4_simd16_single_block,
    gemm_p512_q192_um32_un4_simd16_fixed_q,
    gemm_p512_q192_um32_un4_simd16_adaptive_half,
    gemm_p512_q192_um32_un4_simd16_equal_split,
    gemm_p512_q240_um1_un1_simd1_single_block,
    gemm_p512_q240_um1_un1_simd1_fixed_q,
    gemm_p512_q240_um1_un1_simd1_adaptive_half,
    gemm_p512_q240_um1_un1_simd1_equal_split,
    gemm_p512_q240_um1_un4_simd1_single_block,
    gemm_p512_q240_um1_un4_simd1_fixed_q,
    gemm_p512_q240_um1_un4_simd1_adaptive_half,
    gemm_p512_q240_um1_un4_simd1_equal_split,
    gemm_p512_q240_um4_un1_simd1_single_block,
    gemm_p512_q240_um4_un1_simd1_fixed_q,
    gemm_p512_q240_um4_un1_simd1_adaptive_half,
    gemm_p512_q240_um4_un1_simd1_equal_split,
    gemm_p512_q240_um4_un1_simd4_single_block,
    gemm_p512_q240_um4_un1_simd4_fixed_q,
    gemm_p512_q240_um4_un1_simd4_adaptive_half,
    gemm_p512_q240_um4_un1_simd4_equal_split,
    gemm_p512_q240_um4_un4_simd4_single_block,
    gemm_p512_q240_um4_un4_simd4_fixed_q,
    gemm_p512_q240_um4_un4_simd4_adaptive_half,
    gemm_p512_q240_um4_un4_simd4_equal_split,
    gemm_p512_q240_um8_un1_simd1_single_block,
    gemm_p512_q240_um8_un1_simd1_fixed_q,
    gemm_p512_q240_um8_un1_simd1_adaptive_half,
    gemm_p512_q240_um8_un1_simd1_equal_split,
    gemm_p512_q240_um8_un1_simd4_single_block,
    gemm_p512_q240_um8_un1_simd4_fixed_q,
    gemm_p512_q240_um8_un1_simd4_adaptive_half,
    gemm_p512_q240_um8_un1_simd4_equal_split,
    gemm_p512_q240_um8_un1_simd8_single_block,
    gemm_p512_q240_um8_un1_simd8_fixed_q,
    gemm_p512_q240_um8_un1_simd8_adaptive_half,
    gemm_p512_q240_um8_un1_simd8_equal_split,
    gemm_p512_q240_um8_un4_simd4_single_block,
    gemm_p512_q240_um8_un4_simd4_fixed_q,
    gemm_p512_q240_um8_un4_simd4_adaptive_half,
    gemm_p512_q240_um8_un4_simd4_equal_split,
    gemm_p512_q240_um8_un4_simd8_single_block,
    gemm_p512_q240_um8_un4_simd8_fixed_q,
    gemm_p512_q240_um8_un4_simd8_adaptive_half,
    gemm_p512_q240_um8_un4_simd8_equal_split,
    gemm_p512_q240_um16_un1_simd4_single_block,
    gemm_p512_q240_um16_un1_simd4_fixed_q,
    gemm_p512_q240_um16_un1_simd4_adaptive_half,
    gemm_p512_q240_um16_un1_simd4_equal_split,
    gemm_p512_q240_um16_un1_simd8_single_block,
    gemm_p512_q240_um16_un1_simd8_fixed_q,
    gemm_p512_q240_um16_un1_simd8_adaptive_half,
    gemm_p512_q240_um16_un1_simd8_equal_split,
    gemm_p512_q240_um16_un1_simd16_single_block,
    gemm_p512_q240_um16_un1_simd16_fixed_q,
    gemm_p512_q240_um16_un1_simd16_adaptive_half,
    gemm_p512_q240_um16_un1_simd16_equal_split,
    gemm_p512_q240_um16_un4_simd8_single_block,
    gemm_p512_q240_um16_un4_simd8_fixed_q,
    gemm_p512_q240_um16_un4_simd8_adaptive_half,
    gemm_p512_q240_um16_un4_simd8_equal_split,
    gemm_p512_q240_um16_un4_simd16_single_block,
    gemm_p512_q240_um16_un4_simd16_fixed_q,
    gemm_p512_q240_um16_un4_simd16_adaptive_half,
    gemm_p512_q240_um16_un4_simd16_equal_split,
    gemm_p512_q240_um32_un1_simd4_single_block,
    gemm_p512_q240_um32_un1_simd4_fixed_q,
    gemm_p512_q240_um32_un1_simd4_adaptive_half,
    gemm_p512_q240_um32_un1_simd4_equal_split,
    gemm_p512_q240_um32_un1_simd8_single_block,
    gemm_p512_q240_um32_un1_simd8_fixed_q,
    gemm_p512_q240_um32_un1_simd8_adaptive_half,
    gemm_p512_q240_um32_un1_simd8_equal_split,
    gemm_p512_q240_um32_un1_simd16_single_block,
    gemm_p512_q240_um32_un1_simd16_fixed_q,
    gemm_p512_q240_um32_un1_simd16_adaptive_half,
    gemm_p512_q240_um32_un1_simd16_equal_split,
    gemm_p512_q248_um1_un1_simd1_single_block,
    gemm_p512_q248_um1_un1_simd1_fixed_q,
    gemm_p512_q248_um1_un1_simd1_adaptive_half,
    gemm_p512_q248_um1_un1_simd1_equal_split,
    gemm_p512_q248_um1_un4_simd1_single_block,
    gemm_p512_q248_um1_un4_simd1_fixed_q,
    gemm_p512_q248_um1_un4_simd1_adaptive_half,
    gemm_p512_q248_um1_un4_simd1_equal_split,
    gemm_p512_q248_um4_un1_simd1_single_block,
    gemm_p512_q248_um4_un1_simd1_fixed_q,
    gemm_p512_q248_um4_un1_simd1_adaptive_half,
    gemm_p512_q248_um4_un1_simd1_equal_split,
    gemm_p512_q248_um4_un1_simd4_single_block,
    gemm_p512_q248_um4_un1_simd4_fixed_q,
    gemm_p512_q248_um4_un1_simd4_adaptive_half,
    gemm_p512_q248_um4_un1_simd4_equal_split,
    gemm_p512_q248_um4_un4_simd4_single_block,
    gemm_p512_q248_um4_un4_simd4_fixed_q,
    gemm_p512_q248_um4_un4_simd4_adaptive_half,
    gemm_p512_q248_um4_un4_simd4_equal_split,
    gemm_p512_q248_um8_un1_simd1_single_block,
    gemm_p512_q248_um8_un1_simd1_fixed_q,
    gemm_p512_q248_um8_un1_simd1_adaptive_half,
    gemm_p512_q248_um8_un1_simd1_equal_split,
    gemm_p512_q248_um8_un1_simd4_single_block,
    gemm_p512_q248_um8_un1_simd4_fixed_q,
    gemm_p512_q248_um8_un1_simd4_adaptive_half,
    gemm_p512_q248_um8_un1_simd4_equal_split,
    gemm_p512_q248_um8_un1_simd8_single_block,
    gemm_p512_q248_um8_un1_simd8_fixed_q,
    gemm_p512_q248_um8_un1_simd8_adaptive_half,
    gemm_p512_q248_um8_un1_simd8_equal_split,
    gemm_p512_q248_um8_un4_simd4_single_block,
    gemm_p512_q248_um8_un4_simd4_fixed_q,
    gemm_p512_q248_um8_un4_simd4_adaptive_half,
    gemm_p512_q248_um8_un4_simd4_equal_split,
    gemm_p512_q248_um8_un4_simd8_single_block,
    gemm_p512_q248_um8_un4_simd8_fixed_q,
    gemm_p512_q248_um8_un4_simd8_adaptive_half,
    gemm_p512_q248_um8_un4_simd8_equal_split,
    gemm_p512_q248_um16_un1_simd4_single_block,
    gemm_p512_q248_um16_un1_simd4_fixed_q,
    gemm_p512_q248_um16_un1_simd4_adaptive_half,
    gemm_p512_q248_um16_un1_simd4_equal_split,
    gemm_p512_q248_um16_un1_simd8_single_block,
    gemm_p512_q248_um16_un1_simd8_fixed_q,
    gemm_p512_q248_um16_un1_simd8_adaptive_half,
    gemm_p512_q248_um16_un1_simd8_equal_split,
    gemm_p512_q248_um16_un1_simd16_single_block,
    gemm_p512_q248_um16_un1_simd16_fixed_q,
    gemm_p512_q248_um16_un1_simd16_adaptive_half,
    gemm_p512_q248_um16_un1_simd16_equal_split,
    gemm_p512_q248_um16_un4_simd8_single_block,
    gemm_p512_q248_um16_un4_simd8_fixed_q,
    gemm_p512_q248_um16_un4_simd8_adaptive_half,
    gemm_p512_q248_um16_un4_simd8_equal_split,
    gemm_p512_q248_um16_un4_simd16_single_block,
    gemm_p512_q248_um16_un4_simd16_fixed_q,
    gemm_p512_q248_um16_un4_simd16_adaptive_half,
    gemm_p512_q248_um16_un4_simd16_equal_split,
    gemm_p512_q248_um32_un1_simd4_single_block,
    gemm_p512_q248_um32_un1_simd4_fixed_q,
    gemm_p512_q248_um32_un1_simd4_adaptive_half,
    gemm_p512_q248_um32_un1_simd4_equal_split,
    gemm_p512_q248_um32_un1_simd8_single_block,
    gemm_p512_q248_um32_un1_simd8_fixed_q,
    gemm_p512_q248_um32_un1_simd8_adaptive_half,
    gemm_p512_q248_um32_un1_simd8_equal_split,
    gemm_p512_q248_um32_un1_simd16_single_block,
    gemm_p512_q248_um32_un1_simd16_fixed_q,
    gemm_p512_q248_um32_un1_simd16_adaptive_half,
    gemm_p512_q248_um32_un1_simd16_equal_split,
    gemm_p512_q256_um1_un1_simd1_single_block,
    gemm_p512_q256_um1_un1_simd1_fixed_q,
    gemm_p512_q256_um1_un1_simd1_adaptive_half,
    gemm_p512_q256_um1_un1_simd1_equal_split,
    gemm_p512_q256_um1_un4_simd1_single_block,
    gemm_p512_q256_um1_un4_simd1_fixed_q,
    gemm_p512_q256_um1_un4_simd1_adaptive_half,
    gemm_p512_q256_um1_un4_simd1_equal_split,
    gemm_p512_q256_um4_un1_simd1_single_block,
    gemm_p512_q256_um4_un1_simd1_fixed_q,
    gemm_p512_q256_um4_un1_simd1_adaptive_half,
    gemm_p512_q256_um4_un1_simd1_equal_split,
    gemm_p512_q256_um4_un1_simd4_single_block,
    gemm_p512_q256_um4_un1_simd4_fixed_q,
    gemm_p512_q256_um4_un1_simd4_adaptive_half,
    gemm_p512_q256_um4_un1_simd4_equal_split,
    gemm_p512_q256_um4_un4_simd4_single_block,
    gemm_p512_q256_um4_un4_simd4_fixed_q,
    gemm_p512_q256_um4_un4_simd4_adaptive_half,
    gemm_p512_q256_um4_un4_simd4_equal_split,
    gemm_p512_q256_um8_un1_simd1_single_block,
    gemm_p512_q256_um8_un1_simd1_fixed_q,
    gemm_p512_q256_um8_un1_simd1_adaptive_half,
    gemm_p512_q256_um8_un1_simd1_equal_split,
    gemm_p512_q256_um8_un1_simd4_single_block,
    gemm_p512_q256_um8_un1_simd4_fixed_q,
    gemm_p512_q256_um8_un1_simd4_adaptive_half,
    gemm_p512_q256_um8_un1_simd4_equal_split,
    gemm_p512_q256_um8_un1_simd8_single_block,
    gemm_p512_q256_um8_un1_simd8_fixed_q,
    gemm_p512_q256_um8_un1_simd8_adaptive_half,
    gemm_p512_q256_um8_un1_simd8_equal_split,
    gemm_p512_q256_um8_un4_simd4_single_block,
    gemm_p512_q256_um8_un4_simd4_fixed_q,
    gemm_p512_q256_um8_un4_simd4_adaptive_half,
    gemm_p512_q256_um8_un4_simd4_equal_split,
    gemm_p512_q256_um8_un4_simd8_single_block,
    gemm_p512_q256_um8_un4_simd8_fixed_q,
    gemm_p512_q256_um8_un4_simd8_adaptive_half,
    gemm_p512_q256_um8_un4_simd8_equal_split,
    gemm_p512_q256_um16_un1_simd4_single_block,
    gemm_p512_q256_um16_un1_simd4_fixed_q,
    gemm_p512_q256_um16_un1_simd4_adaptive_half,
    gemm_p512_q256_um16_un1_simd4_equal_split,
    gemm_p512_q256_um16_un1_simd8_single_block,
    gemm_p512_q256_um16_un1_simd8_fixed_q,
    gemm_p512_q256_um16_un1_simd8_adaptive_half,
    gemm_p512_q256_um16_un1_simd8_equal_split,
    gemm_p512_q256_um16_un1_simd16_single_block,
    gemm_p512_q256_um16_un1_simd16_fixed_q,
    gemm_p512_q256_um16_un1_simd16_adaptive_half,
    gemm_p512_q256_um16_un1_simd16_equal_split,
    gemm_p512_q256_um16_un4_simd8_single_block,
    gemm_p512_q256_um16_un4_simd8_fixed_q,
    gemm_p512_q256_um16_un4_simd8_adaptive_half,
    gemm_p512_q256_um16_un4_simd8_equal_split,
    gemm_p512_q256_um16_un4_simd16_single_block,
    gemm_p512_q256_um16_un4_simd16_fixed_q,
    gemm_p512_q256_um16_un4_simd16_adaptive_half,
    gemm_p512_q256_um16_un4_simd16_equal_split,
    gemm_p512_q384_um1_un1_simd1_single_block,
    gemm_p512_q384_um1_un1_simd1_fixed_q,
    gemm_p512_q384_um1_un1_simd1_adaptive_half,
    gemm_p512_q384_um1_un1_simd1_equal_split,
    gemm_p512_q384_um1_un4_simd1_single_block,
    gemm_p512_q384_um1_un4_simd1_fixed_q,
    gemm_p512_q384_um1_un4_simd1_adaptive_half,
    gemm_p512_q384_um1_un4_simd1_equal_split,
    gemm_p512_q384_um4_un1_simd1_single_block,
    gemm_p512_q384_um4_un1_simd1_fixed_q,
    gemm_p512_q384_um4_un1_simd1_adaptive_half,
    gemm_p512_q384_um4_un1_simd1_equal_split,
    gemm_p512_q384_um4_un1_simd4_single_block,
    gemm_p512_q384_um4_un1_simd4_fixed_q,
    gemm_p512_q384_um4_un1_simd4_adaptive_half,
    gemm_p512_q384_um4_un1_simd4_equal_split,
    gemm_p512_q384_um4_un4_simd4_single_block,
    gemm_p512_q384_um4_un4_simd4_fixed_q,
    gemm_p512_q384_um4_un4_simd4_adaptive_half,
    gemm_p512_q384_um4_un4_simd4_equal_split,
    gemm_p512_q384_um8_un1_simd1_single_block,
    gemm_p512_q384_um8_un1_simd1_fixed_q,
    gemm_p512_q384_um8_un1_simd1_adaptive_half,
    gemm_p512_q384_um8_un1_simd1_equal_split,
    gemm_p512_q384_um8_un1_simd4_single_block,
    gemm_p512_q384_um8_un1_simd4_fixed_q,
    gemm_p512_q384_um8_un1_simd4_adaptive_half,
    gemm_p512_q384_um8_un1_simd4_equal_split,
    gemm_p512_q384_um8_un1_simd8_single_block,
    gemm_p512_q384_um8_un1_simd8_fixed_q,
    gemm_p512_q384_um8_un1_simd8_adaptive_half,
    gemm_p512_q384_um8_un1_simd8_equal_split,
    gemm_p512_q384_um8_un4_simd4_single_block,
    gemm_p512_q384_um8_un4_simd4_fixed_q,
    gemm_p512_q384_um8_un4_simd4_adaptive_half,
    gemm_p512_q384_um8_un4_simd4_equal_split,
    gemm_p512_q384_um8_un4_simd8_single_block,
    gemm_p512_q384_um8_un4_simd8_fixed_q,
    gemm_p512_q384_um8_un4_simd8_adaptive_half,
    gemm_p512_q384_um8_un4_simd8_equal_split,
    gemm_p512_q384_um16_un1_simd4_single_block,
    gemm_p512_q384_um16_un1_simd4_fixed_q,
    gemm_p512_q384_um16_un1_simd4_adaptive_half,
    gemm_p512_q384_um16_un1_simd4_equal_split,
    gemm_p512_q384_um16_un1_simd8_single_block,
    gemm_p512_q384_um16_un1_simd8_fixed_q,
    gemm_p512_q384_um16_un1_simd8_adaptive_half,
    gemm_p512_q384_um16_un1_simd8_equal_split,
    gemm_p512_q384_um16_un1_simd16_single_block,
    gemm_p512_q384_um16_un1_simd16_fixed_q,
    gemm_p512_q384_um16_un1_simd16_adaptive_half,
    gemm_p512_q384_um16_un1_simd16_equal_split,
    gemm_p512_q384_um16_un4_simd8_single_block,
    gemm_p512_q384_um16_un4_simd8_fixed_q,
    gemm_p512_q384_um16_un4_simd8_adaptive_half,
    gemm_p512_q384_um16_un4_simd8_equal_split,
    gemm_p512_q384_um16_un4_simd16_single_block,
    gemm_p512_q384_um16_un4_simd16_fixed_q,
    gemm_p512_q384_um16_un4_simd16_adaptive_half,
    gemm_p512_q384_um16_un4_simd16_equal_split,
    gemm_p512_q512_um1_un1_simd1_single_block,
    gemm_p512_q512_um1_un1_simd1_fixed_q,
    gemm_p512_q512_um1_un1_simd1_adaptive_half,
    gemm_p512_q512_um1_un1_simd1_equal_split,
    gemm_p512_q512_um1_un4_simd1_single_block,
    gemm_p512_q512_um1_un4_simd1_fixed_q,
    gemm_p512_q512_um1_un4_simd1_adaptive_half,
    gemm_p512_q512_um1_un4_simd1_equal_split,
    gemm_p512_q512_um4_un1_simd1_single_block,
    gemm_p512_q512_um4_un1_simd1_fixed_q,
    gemm_p512_q512_um4_un1_simd1_adaptive_half,
    gemm_p512_q512_um4_un1_simd1_equal_split,
    gemm_p512_q512_um4_un1_simd4_single_block,
    gemm_p512_q512_um4_un1_simd4_fixed_q,
    gemm_p512_q512_um4_un1_simd4_adaptive_half,
    gemm_p512_q512_um4_un1_simd4_equal_split,
    gemm_p512_q512_um4_un4_simd4_single_block,
    gemm_p512_q512_um4_un4_simd4_fixed_q,
    gemm_p512_q512_um4_un4_simd4_adaptive_half,
    gemm_p512_q512_um4_un4_simd4_equal_split,
    gemm_p512_q512_um8_un1_simd1_single_block,
    gemm_p512_q512_um8_un1_simd1_fixed_q,
    gemm_p512_q512_um8_un1_simd1_adaptive_half,
    gemm_p512_q512_um8_un1_simd1_equal_split,
    gemm_p512_q512_um8_un1_simd4_single_block,
    gemm_p512_q512_um8_un1_simd4_fixed_q,
    gemm_p512_q512_um8_un1_simd4_adaptive_half,
    gemm_p512_q512_um8_un1_simd4_equal_split,
    gemm_p512_q512_um8_un1_simd8_single_block,
    gemm_p512_q512_um8_un1_simd8_fixed_q,
    gemm_p512_q512_um8_un1_simd8_adaptive_half,
    gemm_p512_q512_um8_un1_simd8_equal_split,
    gemm_p512_q512_um8_un4_simd4_single_block,
    gemm_p512_q512_um8_un4_simd4_fixed_q,
    gemm_p512_q512_um8_un4_simd4_adaptive_half,
    gemm_p512_q512_um8_un4_simd4_equal_split,
    gemm_p512_q512_um8_un4_simd8_single_block,
    gemm_p512_q512_um8_un4_simd8_fixed_q,
    gemm_p512_q512_um8_un4_simd8_adaptive_half,
    gemm_p512_q512_um8_un4_simd8_equal_split,
    gemm_p768_q1_um1_un1_simd1_single_block,
    gemm_p768_q1_um1_un1_simd1_fixed_q,
    gemm_p768_q1_um1_un1_simd1_adaptive_half,
    gemm_p768_q1_um1_un1_simd1_equal_split,
    gemm_p768_q1_um1_un4_simd1_single_block,
    gemm_p768_q1_um1_un4_simd1_fixed_q,
    gemm_p768_q1_um1_un4_simd1_adaptive_half,
    gemm_p768_q1_um1_un4_simd1_equal_split,
    gemm_p768_q1_um4_un1_simd1_single_block,
    gemm_p768_q1_um4_un1_simd1_fixed_q,
    gemm_p768_q1_um4_un1_simd1_adaptive_half,
    gemm_p768_q1_um4_un1_simd1_equal_split,
    gemm_p768_q1_um4_un1_simd4_single_block,
    gemm_p768_q1_um4_un1_simd4_fixed_q,
    gemm_p768_q1_um4_un1_simd4_adaptive_half,
    gemm_p768_q1_um4_un1_simd4_equal_split,
    gemm_p768_q1_um4_un4_simd4_single_block,
    gemm_p768_q1_um4_un4_simd4_fixed_q,
    gemm_p768_q1_um4_un4_simd4_adaptive_half,
    gemm_p768_q1_um4_un4_simd4_equal_split,
    gemm_p768_q1_um8_un1_simd1_single_block,
    gemm_p768_q1_um8_un1_simd1_fixed_q,
    gemm_p768_q1_um8_un1_simd1_adaptive_half,
    gemm_p768_q1_um8_un1_simd1_equal_split,
    gemm_p768_q1_um8_un1_simd4_single_block,
    gemm_p768_q1_um8_un1_simd4_fixed_q,
    gemm_p768_q1_um8_un1_simd4_adaptive_half,
    gemm_p768_q1_um8_un1_simd4_equal_split,
    gemm_p768_q1_um8_un1_simd8_single_block,
    gemm_p768_q1_um8_un1_simd8_fixed_q,
    gemm_p768_q1_um8_un1_simd8_adaptive_half,
    gemm_p768_q1_um8_un1_simd8_equal_split,
    gemm_p768_q1_um8_un4_simd4_single_block,
    gemm_p768_q1_um8_un4_simd4_fixed_q,
    gemm_p768_q1_um8_un4_simd4_adaptive_half,
    gemm_p768_q1_um8_un4_simd4_equal_split,
    gemm_p768_q1_um8_un4_simd8_single_block,
    gemm_p768_q1_um8_un4_simd8_fixed_q,
    gemm_p768_q1_um8_un4_simd8_adaptive_half,
    gemm_p768_q1_um8_un4_simd8_equal_split,
    gemm_p768_q1_um16_un1_simd4_single_block,
    gemm_p768_q1_um16_un1_simd4_fixed_q,
    gemm_p768_q1_um16_un1_simd4_adaptive_half,
    gemm_p768_q1_um16_un1_simd4_equal_split,
    gemm_p768_q1_um16_un1_simd8_single_block,
    gemm_p768_q1_um16_un1_simd8_fixed_q,
    gemm_p768_q1_um16_un1_simd8_adaptive_half,
    gemm_p768_q1_um16_un1_simd8_equal_split,
    gemm_p768_q1_um16_un1_simd16_single_block,
    gemm_p768_q1_um16_un1_simd16_fixed_q,
    gemm_p768_q1_um16_un1_simd16_adaptive_half,
    gemm_p768_q1_um16_un1_simd16_equal_split,
    gemm_p768_q1_um16_un4_simd8_single_block,
    gemm_p768_q1_um16_un4_simd8_fixed_q,
    gemm_p768_q1_um16_un4_simd8_adaptive_half,
    gemm_p768_q1_um16_un4_simd8_equal_split,
    gemm_p768_q1_um16_un4_simd16_single_block,
    gemm_p768_q1_um16_un4_simd16_fixed_q,
    gemm_p768_q1_um16_un4_simd16_adaptive_half,
    gemm_p768_q1_um16_un4_simd16_equal_split,
    gemm_p768_q1_um32_un1_simd4_single_block,
    gemm_p768_q1_um32_un1_simd4_fixed_q,
    gemm_p768_q1_um32_un1_simd4_adaptive_half,
    gemm_p768_q1_um32_un1_simd4_equal_split,
    gemm_p768_q1_um32_un1_simd8_single_block,
    gemm_p768_q1_um32_un1_simd8_fixed_q,
    gemm_p768_q1_um32_un1_simd8_adaptive_half,
    gemm_p768_q1_um32_un1_simd8_equal_split,
    gemm_p768_q1_um32_un1_simd16_single_block,
    gemm_p768_q1_um32_un1_simd16_fixed_q,
    gemm_p768_q1_um32_un1_simd16_adaptive_half,
    gemm_p768_q1_um32_un1_simd16_equal_split,
    gemm_p768_q1_um32_un4_simd16_single_block,
    gemm_p768_q1_um32_un4_simd16_fixed_q,
    gemm_p768_q1_um32_un4_simd16_adaptive_half,
    gemm_p768_q1_um32_un4_simd16_equal_split,
    gemm_p768_q64_um1_un1_simd1_single_block,
    gemm_p768_q64_um1_un1_simd1_fixed_q,
    gemm_p768_q64_um1_un1_simd1_adaptive_half,
    gemm_p768_q64_um1_un1_simd1_equal_split,
    gemm_p768_q64_um1_un4_simd1_single_block,
    gemm_p768_q64_um1_un4_simd1_fixed_q,
    gemm_p768_q64_um1_un4_simd1_adaptive_half,
    gemm_p768_q64_um1_un4_simd1_equal_split,
    gemm_p768_q64_um4_un1_simd1_single_block,
    gemm_p768_q64_um4_un1_simd1_fixed_q,
    gemm_p768_q64_um4_un1_simd1_adaptive_half,
    gemm_p768_q64_um4_un1_simd1_equal_split,
    gemm_p768_q64_um4_un1_simd4_single_block,
    gemm_p768_q64_um4_un1_simd4_fixed_q,
    gemm_p768_q64_um4_un1_simd4_adaptive_half,
    gemm_p768_q64_um4_un1_simd4_equal_split,
    gemm_p768_q64_um4_un4_simd4_single_block,
    gemm_p768_q64_um4_un4_simd4_fixed_q,
    gemm_p768_q64_um4_un4_simd4_adaptive_half,
    gemm_p768_q64_um4_un4_simd4_equal_split,
    gemm_p768_q64_um8_un1_simd1_single_block,
    gemm_p768_q64_um8_un1_simd1_fixed_q,
    gemm_p768_q64_um8_un1_simd1_adaptive_half,
    gemm_p768_q64_um8_un1_simd1_equal_split,
    gemm_p768_q64_um8_un1_simd4_single_block,
    gemm_p768_q64_um8_un1_simd4_fixed_q,
    gemm_p768_q64_um8_un1_simd4_adaptive_half,
    gemm_p768_q64_um8_un1_simd4_equal_split,
    gemm_p768_q64_um8_un1_simd8_single_block,
    gemm_p768_q64_um8_un1_simd8_fixed_q,
    gemm_p768_q64_um8_un1_simd8_adaptive_half,
    gemm_p768_q64_um8_un1_simd8_equal_split,
    gemm_p768_q64_um8_un4_simd4_single_block,
    gemm_p768_q64_um8_un4_simd4_fixed_q,
    gemm_p768_q64_um8_un4_simd4_adaptive_half,
    gemm_p768_q64_um8_un4_simd4_equal_split,
    gemm_p768_q64_um8_un4_simd8_single_block,
    gemm_p768_q64_um8_un4_simd8_fixed_q,
    gemm_p768_q64_um8_un4_simd8_adaptive_half,
    gemm_p768_q64_um8_un4_simd8_equal_split,
    gemm_p768_q64_um16_un1_simd4_single_block,
    gemm_p768_q64_um16_un1_simd4_fixed_q,
    gemm_p768_q64_um16_un1_simd4_adaptive_half,
    gemm_p768_q64_um16_un1_simd4_equal_split,
    gemm_p768_q64_um16_un1_simd8_single_block,
    gemm_p768_q64_um16_un1_simd8_fixed_q,
    gemm_p768_q64_um16_un1_simd8_adaptive_half,
    gemm_p768_q64_um16_un1_simd8_equal_split,
    gemm_p768_q64_um16_un1_simd16_single_block,
    gemm_p768_q64_um16_un1_simd16_fixed_q,
    gemm_p768_q64_um16_un1_simd16_adaptive_half,
    gemm_p768_q64_um16_un1_simd16_equal_split,
    gemm_p768_q64_um16_un4_simd8_single_block,
    gemm_p768_q64_um16_un4_simd8_fixed_q,
    gemm_p768_q64_um16_un4_simd8_adaptive_half,
    gemm_p768_q64_um16_un4_simd8_equal_split,
    gemm_p768_q64_um16_un4_simd16_single_block,
    gemm_p768_q64_um16_un4_simd16_fixed_q,
    gemm_p768_q64_um16_un4_simd16_adaptive_half,
    gemm_p768_q64_um16_un4_simd16_equal_split,
    gemm_p768_q64_um32_un1_simd4_single_block,
    gemm_p768_q64_um32_un1_simd4_fixed_q,
    gemm_p768_q64_um32_un1_simd4_adaptive_half,
    gemm_p768_q64_um32_un1_simd4_equal_split,
    gemm_p768_q64_um32_un1_simd8_single_block,
    gemm_p768_q64_um32_un1_simd8_fixed_q,
    gemm_p768_q64_um32_un1_simd8_adaptive_half,
    gemm_p768_q64_um32_un1_simd8_equal_split,
    gemm_p768_q64_um32_un1_simd16_single_block,
    gemm_p768_q64_um32_un1_simd16_fixed_q,
    gemm_p768_q64_um32_un1_simd16_adaptive_half,
    gemm_p768_q64_um32_un1_simd16_equal_split,
    gemm_p768_q64_um32_un4_simd16_single_block,
    gemm_p768_q64_um32_un4_simd16_fixed_q,
    gemm_p768_q64_um32_un4_simd16_adaptive_half,
    gemm_p768_q64_um32_un4_simd16_equal_split,
    gemm_p768_q128_um1_un1_simd1_single_block,
    gemm_p768_q128_um1_un1_simd1_fixed_q,
    gemm_p768_q128_um1_un1_simd1_adaptive_half,
    gemm_p768_q128_um1_un1_simd1_equal_split,
    gemm_p768_q128_um1_un4_simd1_single_block,
    gemm_p768_q128_um1_un4_simd1_fixed_q,
    gemm_p768_q128_um1_un4_simd1_adaptive_half,
    gemm_p768_q128_um1_un4_simd1_equal_split,
    gemm_p768_q128_um4_un1_simd1_single_block,
    gemm_p768_q128_um4_un1_simd1_fixed_q,
    gemm_p768_q128_um4_un1_simd1_adaptive_half,
    gemm_p768_q128_um4_un1_simd1_equal_split,
    gemm_p768_q128_um4_un1_simd4_single_block,
    gemm_p768_q128_um4_un1_simd4_fixed_q,
    gemm_p768_q128_um4_un1_simd4_adaptive_half,
    gemm_p768_q128_um4_un1_simd4_equal_split,
    gemm_p768_q128_um4_un4_simd4_single_block,
    gemm_p768_q128_um4_un4_simd4_fixed_q,
    gemm_p768_q128_um4_un4_simd4_adaptive_half,
    gemm_p768_q128_um4_un4_simd4_equal_split,
    gemm_p768_q128_um8_un1_simd1_single_block,
    gemm_p768_q128_um8_un1_simd1_fixed_q,
    gemm_p768_q128_um8_un1_simd1_adaptive_half,
    gemm_p768_q128_um8_un1_simd1_equal_split,
    gemm_p768_q128_um8_un1_simd4_single_block,
    gemm_p768_q128_um8_un1_simd4_fixed_q,
    gemm_p768_q128_um8_un1_simd4_adaptive_half,
    gemm_p768_q128_um8_un1_simd4_equal_split,
    gemm_p768_q128_um8_un1_simd8_single_block,
    gemm_p768_q128_um8_un1_simd8_fixed_q,
    gemm_p768_q128_um8_un1_simd8_adaptive_half,
    gemm_p768_q128_um8_un1_simd8_equal_split,
    gemm_p768_q128_um8_un4_simd4_single_block,
    gemm_p768_q128_um8_un4_simd4_fixed_q,
    gemm_p768_q128_um8_un4_simd4_adaptive_half,
    gemm_p768_q128_um8_un4_simd4_equal_split,
    gemm_p768_q128_um8_un4_simd8_single_block,
    gemm_p768_q128_um8_un4_simd8_fixed_q,
    gemm_p768_q128_um8_un4_simd8_adaptive_half,
    gemm_p768_q128_um8_un4_simd8_equal_split,
    gemm_p768_q128_um16_un1_simd4_single_block,
    gemm_p768_q128_um16_un1_simd4_fixed_q,
    gemm_p768_q128_um16_un1_simd4_adaptive_half,
    gemm_p768_q128_um16_un1_simd4_equal_split,
    gemm_p768_q128_um16_un1_simd8_single_block,
    gemm_p768_q128_um16_un1_simd8_fixed_q,
    gemm_p768_q128_um16_un1_simd8_adaptive_half,
    gemm_p768_q128_um16_un1_simd8_equal_split,
    gemm_p768_q128_um16_un1_simd16_single_block,
    gemm_p768_q128_um16_un1_simd16_fixed_q,
    gemm_p768_q128_um16_un1_simd16_adaptive_half,
    gemm_p768_q128_um16_un1_simd16_equal_split,
    gemm_p768_q128_um16_un4_simd8_single_block,
    gemm_p768_q128_um16_un4_simd8_fixed_q,
    gemm_p768_q128_um16_un4_simd8_adaptive_half,
    gemm_p768_q128_um16_un4_simd8_equal_split,
    gemm_p768_q128_um16_un4_simd16_single_block,
    gemm_p768_q128_um16_un4_simd16_fixed_q,
    gemm_p768_q128_um16_un4_simd16_adaptive_half,
    gemm_p768_q128_um16_un4_simd16_equal_split,
    gemm_p768_q128_um32_un1_simd4_single_block,
    gemm_p768_q128_um32_un1_simd4_fixed_q,
    gemm_p768_q128_um32_un1_simd4_adaptive_half,
    gemm_p768_q128_um32_un1_simd4_equal_split,
    gemm_p768_q128_um32_un1_simd8_single_block,
    gemm_p768_q128_um32_un1_simd8_fixed_q,
    gemm_p768_q128_um32_un1_simd8_adaptive_half,
    gemm_p768_q128_um32_un1_simd8_equal_split,
    gemm_p768_q128_um32_un1_simd16_single_block,
    gemm_p768_q128_um32_un1_simd16_fixed_q,
    gemm_p768_q128_um32_un1_simd16_adaptive_half,
    gemm_p768_q128_um32_un1_simd16_equal_split,
    gemm_p768_q128_um32_un4_simd16_single_block,
    gemm_p768_q128_um32_un4_simd16_fixed_q,
    gemm_p768_q128_um32_un4_simd16_adaptive_half,
    gemm_p768_q128_um32_un4_simd16_equal_split,
    gemm_p768_q192_um1_un1_simd1_single_block,
    gemm_p768_q192_um1_un1_simd1_fixed_q,
    gemm_p768_q192_um1_un1_simd1_adaptive_half,
    gemm_p768_q192_um1_un1_simd1_equal_split,
    gemm_p768_q192_um1_un4_simd1_single_block,
    gemm_p768_q192_um1_un4_simd1_fixed_q,
    gemm_p768_q192_um1_un4_simd1_adaptive_half,
    gemm_p768_q192_um1_un4_simd1_equal_split,
    gemm_p768_q192_um4_un1_simd1_single_block,
    gemm_p768_q192_um4_un1_simd1_fixed_q,
    gemm_p768_q192_um4_un1_simd1_adaptive_half,
    gemm_p768_q192_um4_un1_simd1_equal_split,
    gemm_p768_q192_um4_un1_simd4_single_block,
    gemm_p768_q192_um4_un1_simd4_fixed_q,
    gemm_p768_q192_um4_un1_simd4_adaptive_half,
    gemm_p768_q192_um4_un1_simd4_equal_split,
    gemm_p768_q192_um4_un4_simd4_single_block,
    gemm_p768_q192_um4_un4_simd4_fixed_q,
    gemm_p768_q192_um4_un4_simd4_adaptive_half,
    gemm_p768_q192_um4_un4_simd4_equal_split,
    gemm_p768_q192_um8_un1_simd1_single_block,
    gemm_p768_q192_um8_un1_simd1_fixed_q,
    gemm_p768_q192_um8_un1_simd1_adaptive_half,
    gemm_p768_q192_um8_un1_simd1_equal_split,
    gemm_p768_q192_um8_un1_simd4_single_block,
    gemm_p768_q192_um8_un1_simd4_fixed_q,
    gemm_p768_q192_um8_un1_simd4_adaptive_half,
    gemm_p768_q192_um8_un1_simd4_equal_split,
    gemm_p768_q192_um8_un1_simd8_single_block,
    gemm_p768_q192_um8_un1_simd8_fixed_q,
    gemm_p768_q192_um8_un1_simd8_adaptive_half,
    gemm_p768_q192_um8_un1_simd8_equal_split,
    gemm_p768_q192_um8_un4_simd4_single_block,
    gemm_p768_q192_um8_un4_simd4_fixed_q,
    gemm_p768_q192_um8_un4_simd4_adaptive_half,
    gemm_p768_q192_um8_un4_simd4_equal_split,
    gemm_p768_q192_um8_un4_simd8_single_block,
    gemm_p768_q192_um8_un4_simd8_fixed_q,
    gemm_p768_q192_um8_un4_simd8_adaptive_half,
    gemm_p768_q192_um8_un4_simd8_equal_split,
    gemm_p768_q192_um16_un1_simd4_single_block,
    gemm_p768_q192_um16_un1_simd4_fixed_q,
    gemm_p768_q192_um16_un1_simd4_adaptive_half,
    gemm_p768_q192_um16_un1_simd4_equal_split,
    gemm_p768_q192_um16_un1_simd8_single_block,
    gemm_p768_q192_um16_un1_simd8_fixed_q,
    gemm_p768_q192_um16_un1_simd8_adaptive_half,
    gemm_p768_q192_um16_un1_simd8_equal_split,
    gemm_p768_q192_um16_un1_simd16_single_block,
    gemm_p768_q192_um16_un1_simd16_fixed_q,
    gemm_p768_q192_um16_un1_simd16_adaptive_half,
    gemm_p768_q192_um16_un1_simd16_equal_split,
    gemm_p768_q192_um16_un4_simd8_single_block,
    gemm_p768_q192_um16_un4_simd8_fixed_q,
    gemm_p768_q192_um16_un4_simd8_adaptive_half,
    gemm_p768_q192_um16_un4_simd8_equal_split,
    gemm_p768_q192_um16_un4_simd16_single_block,
    gemm_p768_q192_um16_un4_simd16_fixed_q,
    gemm_p768_q192_um16_un4_simd16_adaptive_half,
    gemm_p768_q192_um16_un4_simd16_equal_split,
    gemm_p768_q192_um32_un1_simd4_single_block,
    gemm_p768_q192_um32_un1_simd4_fixed_q,
    gemm_p768_q192_um32_un1_simd4_adaptive_half,
    gemm_p768_q192_um32_un1_simd4_equal_split,
    gemm_p768_q192_um32_un1_simd8_single_block,
    gemm_p768_q192_um32_un1_simd8_fixed_q,
    gemm_p768_q192_um32_un1_simd8_adaptive_half,
    gemm_p768_q192_um32_un1_simd8_equal_split,
    gemm_p768_q192_um32_un1_simd16_single_block,
    gemm_p768_q192_um32_un1_simd16_fixed_q,
    gemm_p768_q192_um32_un1_simd16_adaptive_half,
    gemm_p768_q192_um32_un1_simd16_equal_split,
    gemm_p768_q192_um32_un4_simd16_single_block,
    gemm_p768_q192_um32_un4_simd16_fixed_q,
    gemm_p768_q192_um32_un4_simd16_adaptive_half,
    gemm_p768_q192_um32_un4_simd16_equal_split,
    gemm_p768_q240_um1_un1_simd1_single_block,
    gemm_p768_q240_um1_un1_simd1_fixed_q,
    gemm_p768_q240_um1_un1_simd1_adaptive_half,
    gemm_p768_q240_um1_un1_simd1_equal_split,
    gemm_p768_q240_um1_un4_simd1_single_block,
    gemm_p768_q240_um1_un4_simd1_fixed_q,
    gemm_p768_q240_um1_un4_simd1_adaptive_half,
    gemm_p768_q240_um1_un4_simd1_equal_split,
    gemm_p768_q240_um4_un1_simd1_single_block,
    gemm_p768_q240_um4_un1_simd1_fixed_q,
    gemm_p768_q240_um4_un1_simd1_adaptive_half,
    gemm_p768_q240_um4_un1_simd1_equal_split,
    gemm_p768_q240_um4_un1_simd4_single_block,
    gemm_p768_q240_um4_un1_simd4_fixed_q,
    gemm_p768_q240_um4_un1_simd4_adaptive_half,
    gemm_p768_q240_um4_un1_simd4_equal_split,
    gemm_p768_q240_um4_un4_simd4_single_block,
    gemm_p768_q240_um4_un4_simd4_fixed_q,
    gemm_p768_q240_um4_un4_simd4_adaptive_half,
    gemm_p768_q240_um4_un4_simd4_equal_split,
    gemm_p768_q240_um8_un1_simd1_single_block,
    gemm_p768_q240_um8_un1_simd1_fixed_q,
    gemm_p768_q240_um8_un1_simd1_adaptive_half,
    gemm_p768_q240_um8_un1_simd1_equal_split,
    gemm_p768_q240_um8_un1_simd4_single_block,
    gemm_p768_q240_um8_un1_simd4_fixed_q,
    gemm_p768_q240_um8_un1_simd4_adaptive_half,
    gemm_p768_q240_um8_un1_simd4_equal_split,
    gemm_p768_q240_um8_un1_simd8_single_block,
    gemm_p768_q240_um8_un1_simd8_fixed_q,
    gemm_p768_q240_um8_un1_simd8_adaptive_half,
    gemm_p768_q240_um8_un1_simd8_equal_split,
    gemm_p768_q240_um8_un4_simd4_single_block,
    gemm_p768_q240_um8_un4_simd4_fixed_q,
    gemm_p768_q240_um8_un4_simd4_adaptive_half,
    gemm_p768_q240_um8_un4_simd4_equal_split,
    gemm_p768_q240_um8_un4_simd8_single_block,
    gemm_p768_q240_um8_un4_simd8_fixed_q,
    gemm_p768_q240_um8_un4_simd8_adaptive_half,
    gemm_p768_q240_um8_un4_simd8_equal_split,
    gemm_p768_q240_um16_un1_simd4_single_block,
    gemm_p768_q240_um16_un1_simd4_fixed_q,
    gemm_p768_q240_um16_un1_simd4_adaptive_half,
    gemm_p768_q240_um16_un1_simd4_equal_split,
    gemm_p768_q240_um16_un1_simd8_single_block,
    gemm_p768_q240_um16_un1_simd8_fixed_q,
    gemm_p768_q240_um16_un1_simd8_adaptive_half,
    gemm_p768_q240_um16_un1_simd8_equal_split,
    gemm_p768_q240_um16_un1_simd16_single_block,
    gemm_p768_q240_um16_un1_simd16_fixed_q,
    gemm_p768_q240_um16_un1_simd16_adaptive_half,
    gemm_p768_q240_um16_un1_simd16_equal_split,
    gemm_p768_q240_um16_un4_simd8_single_block,
    gemm_p768_q240_um16_un4_simd8_fixed_q,
    gemm_p768_q240_um16_un4_simd8_adaptive_half,
    gemm_p768_q240_um16_un4_simd8_equal_split,
    gemm_p768_q240_um16_un4_simd16_single_block,
    gemm_p768_q240_um16_un4_simd16_fixed_q,
    gemm_p768_q240_um16_un4_simd16_adaptive_half,
    gemm_p768_q240_um16_un4_simd16_equal_split,
    gemm_p768_q240_um32_un1_simd4_single_block,
    gemm_p768_q240_um32_un1_simd4_fixed_q,
    gemm_p768_q240_um32_un1_simd4_adaptive_half,
    gemm_p768_q240_um32_un1_simd4_equal_split,
    gemm_p768_q240_um32_un1_simd8_single_block,
    gemm_p768_q240_um32_un1_simd8_fixed_q,
    gemm_p768_q240_um32_un1_simd8_adaptive_half,
    gemm_p768_q240_um32_un1_simd8_equal_split,
    gemm_p768_q240_um32_un1_simd16_single_block,
    gemm_p768_q240_um32_un1_simd16_fixed_q,
    gemm_p768_q240_um32_un1_simd16_adaptive_half,
    gemm_p768_q240_um32_un1_simd16_equal_split,
    gemm_p768_q248_um1_un1_simd1_single_block,
    gemm_p768_q248_um1_un1_simd1_fixed_q,
    gemm_p768_q248_um1_un1_simd1_adaptive_half,
    gemm_p768_q248_um1_un1_simd1_equal_split,
    gemm_p768_q248_um1_un4_simd1_single_block,
    gemm_p768_q248_um1_un4_simd1_fixed_q,
    gemm_p768_q248_um1_un4_simd1_adaptive_half,
    gemm_p768_q248_um1_un4_simd1_equal_split,
    gemm_p768_q248_um4_un1_simd1_single_block,
    gemm_p768_q248_um4_un1_simd1_fixed_q,
    gemm_p768_q248_um4_un1_simd1_adaptive_half,
    gemm_p768_q248_um4_un1_simd1_equal_split,
    gemm_p768_q248_um4_un1_simd4_single_block,
    gemm_p768_q248_um4_un1_simd4_fixed_q,
    gemm_p768_q248_um4_un1_simd4_adaptive_half,
    gemm_p768_q248_um4_un1_simd4_equal_split,
    gemm_p768_q248_um4_un4_simd4_single_block,
    gemm_p768_q248_um4_un4_simd4_fixed_q,
    gemm_p768_q248_um4_un4_simd4_adaptive_half,
    gemm_p768_q248_um4_un4_simd4_equal_split,
    gemm_p768_q248_um8_un1_simd1_single_block,
    gemm_p768_q248_um8_un1_simd1_fixed_q,
    gemm_p768_q248_um8_un1_simd1_adaptive_half,
    gemm_p768_q248_um8_un1_simd1_equal_split,
    gemm_p768_q248_um8_un1_simd4_single_block,
    gemm_p768_q248_um8_un1_simd4_fixed_q,
    gemm_p768_q248_um8_un1_simd4_adaptive_half,
    gemm_p768_q248_um8_un1_simd4_equal_split,
    gemm_p768_q248_um8_un1_simd8_single_block,
    gemm_p768_q248_um8_un1_simd8_fixed_q,
    gemm_p768_q248_um8_un1_simd8_adaptive_half,
    gemm_p768_q248_um8_un1_simd8_equal_split,
    gemm_p768_q248_um8_un4_simd4_single_block,
    gemm_p768_q248_um8_un4_simd4_fixed_q,
    gemm_p768_q248_um8_un4_simd4_adaptive_half,
    gemm_p768_q248_um8_un4_simd4_equal_split,
    gemm_p768_q248_um8_un4_simd8_single_block,
    gemm_p768_q248_um8_un4_simd8_fixed_q,
    gemm_p768_q248_um8_un4_simd8_adaptive_half,
    gemm_p768_q248_um8_un4_simd8_equal_split,
    gemm_p768_q248_um16_un1_simd4_single_block,
    gemm_p768_q248_um16_un1_simd4_fixed_q,
    gemm_p768_q248_um16_un1_simd4_adaptive_half,
    gemm_p768_q248_um16_un1_simd4_equal_split,
    gemm_p768_q248_um16_un1_simd8_single_block,
    gemm_p768_q248_um16_un1_simd8_fixed_q,
    gemm_p768_q248_um16_un1_simd8_adaptive_half,
    gemm_p768_q248_um16_un1_simd8_equal_split,
    gemm_p768_q248_um16_un1_simd16_single_block,
    gemm_p768_q248_um16_un1_simd16_fixed_q,
    gemm_p768_q248_um16_un1_simd16_adaptive_half,
    gemm_p768_q248_um16_un1_simd16_equal_split,
    gemm_p768_q248_um16_un4_simd8_single_block,
    gemm_p768_q248_um16_un4_simd8_fixed_q,
    gemm_p768_q248_um16_un4_simd8_adaptive_half,
    gemm_p768_q248_um16_un4_simd8_equal_split,
    gemm_p768_q248_um16_un4_simd16_single_block,
    gemm_p768_q248_um16_un4_simd16_fixed_q,
    gemm_p768_q248_um16_un4_simd16_adaptive_half,
    gemm_p768_q248_um16_un4_simd16_equal_split,
    gemm_p768_q248_um32_un1_simd4_single_block,
    gemm_p768_q248_um32_un1_simd4_fixed_q,
    gemm_p768_q248_um32_un1_simd4_adaptive_half,
    gemm_p768_q248_um32_un1_simd4_equal_split,
    gemm_p768_q248_um32_un1_simd8_single_block,
    gemm_p768_q248_um32_un1_simd8_fixed_q,
    gemm_p768_q248_um32_un1_simd8_adaptive_half,
    gemm_p768_q248_um32_un1_simd8_equal_split,
    gemm_p768_q248_um32_un1_simd16_single_block,
    gemm_p768_q248_um32_un1_simd16_fixed_q,
    gemm_p768_q248_um32_un1_simd16_adaptive_half,
    gemm_p768_q248_um32_un1_simd16_equal_split,
    gemm_p768_q256_um1_un1_simd1_single_block,
    gemm_p768_q256_um1_un1_simd1_fixed_q,
    gemm_p768_q256_um1_un1_simd1_adaptive_half,
    gemm_p768_q256_um1_un1_simd1_equal_split,
    gemm_p768_q256_um1_un4_simd1_single_block,
    gemm_p768_q256_um1_un4_simd1_fixed_q,
    gemm_p768_q256_um1_un4_simd1_adaptive_half,
    gemm_p768_q256_um1_un4_simd1_equal_split,
    gemm_p768_q256_um4_un1_simd1_single_block,
    gemm_p768_q256_um4_un1_simd1_fixed_q,
    gemm_p768_q256_um4_un1_simd1_adaptive_half,
    gemm_p768_q256_um4_un1_simd1_equal_split,
    gemm_p768_q256_um4_un1_simd4_single_block,
    gemm_p768_q256_um4_un1_simd4_fixed_q,
    gemm_p768_q256_um4_un1_simd4_adaptive_half,
    gemm_p768_q256_um4_un1_simd4_equal_split,
    gemm_p768_q256_um4_un4_simd4_single_block,
    gemm_p768_q256_um4_un4_simd4_fixed_q,
    gemm_p768_q256_um4_un4_simd4_adaptive_half,
    gemm_p768_q256_um4_un4_simd4_equal_split,
    gemm_p768_q256_um8_un1_simd1_single_block,
    gemm_p768_q256_um8_un1_simd1_fixed_q,
    gemm_p768_q256_um8_un1_simd1_adaptive_half,
    gemm_p768_q256_um8_un1_simd1_equal_split,
    gemm_p768_q256_um8_un1_simd4_single_block,
    gemm_p768_q256_um8_un1_simd4_fixed_q,
    gemm_p768_q256_um8_un1_simd4_adaptive_half,
    gemm_p768_q256_um8_un1_simd4_equal_split,
    gemm_p768_q256_um8_un1_simd8_single_block,
    gemm_p768_q256_um8_un1_simd8_fixed_q,
    gemm_p768_q256_um8_un1_simd8_adaptive_half,
    gemm_p768_q256_um8_un1_simd8_equal_split,
    gemm_p768_q256_um8_un4_simd4_single_block,
    gemm_p768_q256_um8_un4_simd4_fixed_q,
    gemm_p768_q256_um8_un4_simd4_adaptive_half,
    gemm_p768_q256_um8_un4_simd4_equal_split,
    gemm_p768_q256_um8_un4_simd8_single_block,
    gemm_p768_q256_um8_un4_simd8_fixed_q,
    gemm_p768_q256_um8_un4_simd8_adaptive_half,
    gemm_p768_q256_um8_un4_simd8_equal_split,
    gemm_p768_q256_um16_un1_simd4_single_block,
    gemm_p768_q256_um16_un1_simd4_fixed_q,
    gemm_p768_q256_um16_un1_simd4_adaptive_half,
    gemm_p768_q256_um16_un1_simd4_equal_split,
    gemm_p768_q256_um16_un1_simd8_single_block,
    gemm_p768_q256_um16_un1_simd8_fixed_q,
    gemm_p768_q256_um16_un1_simd8_adaptive_half,
    gemm_p768_q256_um16_un1_simd8_equal_split,
    gemm_p768_q256_um16_un1_simd16_single_block,
    gemm_p768_q256_um16_un1_simd16_fixed_q,
    gemm_p768_q256_um16_un1_simd16_adaptive_half,
    gemm_p768_q256_um16_un1_simd16_equal_split,
    gemm_p768_q256_um16_un4_simd8_single_block,
    gemm_p768_q256_um16_un4_simd8_fixed_q,
    gemm_p768_q256_um16_un4_simd8_adaptive_half,
    gemm_p768_q256_um16_un4_simd8_equal_split,
    gemm_p768_q256_um16_un4_simd16_single_block,
    gemm_p768_q256_um16_un4_simd16_fixed_q,
    gemm_p768_q256_um16_un4_simd16_adaptive_half,
    gemm_p768_q256_um16_un4_simd16_equal_split,
    gemm_p768_q384_um1_un1_simd1_single_block,
    gemm_p768_q384_um1_un1_simd1_fixed_q,
    gemm_p768_q384_um1_un1_simd1_adaptive_half,
    gemm_p768_q384_um1_un1_simd1_equal_split,
    gemm_p768_q384_um1_un4_simd1_single_block,
    gemm_p768_q384_um1_un4_simd1_fixed_q,
    gemm_p768_q384_um1_un4_simd1_adaptive_half,
    gemm_p768_q384_um1_un4_simd1_equal_split,
    gemm_p768_q384_um4_un1_simd1_single_block,
    gemm_p768_q384_um4_un1_simd1_fixed_q,
    gemm_p768_q384_um4_un1_simd1_adaptive_half,
    gemm_p768_q384_um4_un1_simd1_equal_split,
    gemm_p768_q384_um4_un1_simd4_single_block,
    gemm_p768_q384_um4_un1_simd4_fixed_q,
    gemm_p768_q384_um4_un1_simd4_adaptive_half,
    gemm_p768_q384_um4_un1_simd4_equal_split,
    gemm_p768_q384_um4_un4_simd4_single_block,
    gemm_p768_q384_um4_un4_simd4_fixed_q,
    gemm_p768_q384_um4_un4_simd4_adaptive_half,
    gemm_p768_q384_um4_un4_simd4_equal_split,
    gemm_p768_q384_um8_un1_simd1_single_block,
    gemm_p768_q384_um8_un1_simd1_fixed_q,
    gemm_p768_q384_um8_un1_simd1_adaptive_half,
    gemm_p768_q384_um8_un1_simd1_equal_split,
    gemm_p768_q384_um8_un1_simd4_single_block,
    gemm_p768_q384_um8_un1_simd4_fixed_q,
    gemm_p768_q384_um8_un1_simd4_adaptive_half,
    gemm_p768_q384_um8_un1_simd4_equal_split,
    gemm_p768_q384_um8_un1_simd8_single_block,
    gemm_p768_q384_um8_un1_simd8_fixed_q,
    gemm_p768_q384_um8_un1_simd8_adaptive_half,
    gemm_p768_q384_um8_un1_simd8_equal_split,
    gemm_p768_q384_um8_un4_simd4_single_block,
    gemm_p768_q384_um8_un4_simd4_fixed_q,
    gemm_p768_q384_um8_un4_simd4_adaptive_half,
    gemm_p768_q384_um8_un4_simd4_equal_split,
    gemm_p768_q384_um8_un4_simd8_single_block,
    gemm_p768_q384_um8_un4_simd8_fixed_q,
    gemm_p768_q384_um8_un4_simd8_adaptive_half,
    gemm_p768_q384_um8_un4_simd8_equal_split,
    gemm_p768_q384_um16_un1_simd4_single_block,
    gemm_p768_q384_um16_un1_simd4_fixed_q,
    gemm_p768_q384_um16_un1_simd4_adaptive_half,
    gemm_p768_q384_um16_un1_simd4_equal_split,
    gemm_p768_q384_um16_un1_simd8_single_block,
    gemm_p768_q384_um16_un1_simd8_fixed_q,
    gemm_p768_q384_um16_un1_simd8_adaptive_half,
    gemm_p768_q384_um16_un1_simd8_equal_split,
    gemm_p768_q384_um16_un1_simd16_single_block,
    gemm_p768_q384_um16_un1_simd16_fixed_q,
    gemm_p768_q384_um16_un1_simd16_adaptive_half,
    gemm_p768_q384_um16_un1_simd16_equal_split,
    gemm_p768_q384_um16_un4_simd8_single_block,
    gemm_p768_q384_um16_un4_simd8_fixed_q,
    gemm_p768_q384_um16_un4_simd8_adaptive_half,
    gemm_p768_q384_um16_un4_simd8_equal_split,
    gemm_p768_q384_um16_un4_simd16_single_block,
    gemm_p768_q384_um16_un4_simd16_fixed_q,
    gemm_p768_q384_um16_un4_simd16_adaptive_half,
    gemm_p768_q384_um16_un4_simd16_equal_split,
    gemm_p768_q512_um1_un1_simd1_single_block,
    gemm_p768_q512_um1_un1_simd1_fixed_q,
    gemm_p768_q512_um1_un1_simd1_adaptive_half,
    gemm_p768_q512_um1_un1_simd1_equal_split,
    gemm_p768_q512_um1_un4_simd1_single_block,
    gemm_p768_q512_um1_un4_simd1_fixed_q,
    gemm_p768_q512_um1_un4_simd1_adaptive_half,
    gemm_p768_q512_um1_un4_simd1_equal_split,
    gemm_p768_q512_um4_un1_simd1_single_block,
    gemm_p768_q512_um4_un1_simd1_fixed_q,
    gemm_p768_q512_um4_un1_simd1_adaptive_half,
    gemm_p768_q512_um4_un1_simd1_equal_split,
    gemm_p768_q512_um4_un1_simd4_single_block,
    gemm_p768_q512_um4_un1_simd4_fixed_q,
    gemm_p768_q512_um4_un1_simd4_adaptive_half,
    gemm_p768_q512_um4_un1_simd4_equal_split,
    gemm_p768_q512_um4_un4_simd4_single_block,
    gemm_p768_q512_um4_un4_simd4_fixed_q,
    gemm_p768_q512_um4_un4_simd4_adaptive_half,
    gemm_p768_q512_um4_un4_simd4_equal_split,
    gemm_p768_q512_um8_un1_simd1_single_block,
    gemm_p768_q512_um8_un1_simd1_fixed_q,
    gemm_p768_q512_um8_un1_simd1_adaptive_half,
    gemm_p768_q512_um8_un1_simd1_equal_split,
    gemm_p768_q512_um8_un1_simd4_single_block,
    gemm_p768_q512_um8_un1_simd4_fixed_q,
    gemm_p768_q512_um8_un1_simd4_adaptive_half,
    gemm_p768_q512_um8_un1_simd4_equal_split,
    gemm_p768_q512_um8_un1_simd8_single_block,
    gemm_p768_q512_um8_un1_simd8_fixed_q,
    gemm_p768_q512_um8_un1_simd8_adaptive_half,
    gemm_p768_q512_um8_un1_simd8_equal_split,
    gemm_p768_q512_um8_un4_simd4_single_block,
    gemm_p768_q512_um8_un4_simd4_fixed_q,
    gemm_p768_q512_um8_un4_simd4_adaptive_half,
    gemm_p768_q512_um8_un4_simd4_equal_split,
    gemm_p768_q512_um8_un4_simd8_single_block,
    gemm_p768_q512_um8_un4_simd8_fixed_q,
    gemm_p768_q512_um8_un4_simd8_adaptive_half,
    gemm_p768_q512_um8_un4_simd8_equal_split,
    gemm_p1024_q1_um1_un1_simd1_single_block,
    gemm_p1024_q1_um1_un1_simd1_fixed_q,
    gemm_p1024_q1_um1_un1_simd1_adaptive_half,
    gemm_p1024_q1_um1_un1_simd1_equal_split,
    gemm_p1024_q1_um1_un4_simd1_single_block,
    gemm_p1024_q1_um1_un4_simd1_fixed_q,
    gemm_p1024_q1_um1_un4_simd1_adaptive_half,
    gemm_p1024_q1_um1_un4_simd1_equal_split,
    gemm_p1024_q1_um4_un1_simd1_single_block,
    gemm_p1024_q1_um4_un1_simd1_fixed_q,
    gemm_p1024_q1_um4_un1_simd1_adaptive_half,
    gemm_p1024_q1_um4_un1_simd1_equal_split,
    gemm_p1024_q1_um4_un1_simd4_single_block,
    gemm_p1024_q1_um4_un1_simd4_fixed_q,
    gemm_p1024_q1_um4_un1_simd4_adaptive_half,
    gemm_p1024_q1_um4_un1_simd4_equal_split,
    gemm_p1024_q1_um4_un4_simd4_single_block,
    gemm_p1024_q1_um4_un4_simd4_fixed_q,
    gemm_p1024_q1_um4_un4_simd4_adaptive_half,
    gemm_p1024_q1_um4_un4_simd4_equal_split,
    gemm_p1024_q1_um8_un1_simd1_single_block,
    gemm_p1024_q1_um8_un1_simd1_fixed_q,
    gemm_p1024_q1_um8_un1_simd1_adaptive_half,
    gemm_p1024_q1_um8_un1_simd1_equal_split,
    gemm_p1024_q1_um8_un1_simd4_single_block,
    gemm_p1024_q1_um8_un1_simd4_fixed_q,
    gemm_p1024_q1_um8_un1_simd4_adaptive_half,
    gemm_p1024_q1_um8_un1_simd4_equal_split,
    gemm_p1024_q1_um8_un1_simd8_single_block,
    gemm_p1024_q1_um8_un1_simd8_fixed_q,
    gemm_p1024_q1_um8_un1_simd8_adaptive_half,
    gemm_p1024_q1_um8_un1_simd8_equal_split,
    gemm_p1024_q1_um8_un4_simd4_single_block,
    gemm_p1024_q1_um8_un4_simd4_fixed_q,
    gemm_p1024_q1_um8_un4_simd4_adaptive_half,
    gemm_p1024_q1_um8_un4_simd4_equal_split,
    gemm_p1024_q1_um8_un4_simd8_single_block,
    gemm_p1024_q1_um8_un4_simd8_fixed_q,
    gemm_p1024_q1_um8_un4_simd8_adaptive_half,
    gemm_p1024_q1_um8_un4_simd8_equal_split,
    gemm_p1024_q1_um16_un1_simd4_single_block,
    gemm_p1024_q1_um16_un1_simd4_fixed_q,
    gemm_p1024_q1_um16_un1_simd4_adaptive_half,
    gemm_p1024_q1_um16_un1_simd4_equal_split,
    gemm_p1024_q1_um16_un1_simd8_single_block,
    gemm_p1024_q1_um16_un1_simd8_fixed_q,
    gemm_p1024_q1_um16_un1_simd8_adaptive_half,
    gemm_p1024_q1_um16_un1_simd8_equal_split,
    gemm_p1024_q1_um16_un1_simd16_single_block,
    gemm_p1024_q1_um16_un1_simd16_fixed_q,
    gemm_p1024_q1_um16_un1_simd16_adaptive_half,
    gemm_p1024_q1_um16_un1_simd16_equal_split,
    gemm_p1024_q1_um16_un4_simd8_single_block,
    gemm_p1024_q1_um16_un4_simd8_fixed_q,
    gemm_p1024_q1_um16_un4_simd8_adaptive_half,
    gemm_p1024_q1_um16_un4_simd8_equal_split,
    gemm_p1024_q1_um16_un4_simd16_single_block,
    gemm_p1024_q1_um16_un4_simd16_fixed_q,
    gemm_p1024_q1_um16_un4_simd16_adaptive_half,
    gemm_p1024_q1_um16_un4_simd16_equal_split,
    gemm_p1024_q1_um32_un1_simd4_single_block,
    gemm_p1024_q1_um32_un1_simd4_fixed_q,
    gemm_p1024_q1_um32_un1_simd4_adaptive_half,
    gemm_p1024_q1_um32_un1_simd4_equal_split,
    gemm_p1024_q1_um32_un1_simd8_single_block,
    gemm_p1024_q1_um32_un1_simd8_fixed_q,
    gemm_p1024_q1_um32_un1_simd8_adaptive_half,
    gemm_p1024_q1_um32_un1_simd8_equal_split,
    gemm_p1024_q1_um32_un1_simd16_single_block,
    gemm_p1024_q1_um32_un1_simd16_fixed_q,
    gemm_p1024_q1_um32_un1_simd16_adaptive_half,
    gemm_p1024_q1_um32_un1_simd16_equal_split,
    gemm_p1024_q1_um32_un4_simd16_single_block,
    gemm_p1024_q1_um32_un4_simd16_fixed_q,
    gemm_p1024_q1_um32_un4_simd16_adaptive_half,
    gemm_p1024_q1_um32_un4_simd16_equal_split,
    gemm_p1024_q64_um1_un1_simd1_single_block,
    gemm_p1024_q64_um1_un1_simd1_fixed_q,
    gemm_p1024_q64_um1_un1_simd1_adaptive_half,
    gemm_p1024_q64_um1_un1_simd1_equal_split,
    gemm_p1024_q64_um1_un4_simd1_single_block,
    gemm_p1024_q64_um1_un4_simd1_fixed_q,
    gemm_p1024_q64_um1_un4_simd1_adaptive_half,
    gemm_p1024_q64_um1_un4_simd1_equal_split,
    gemm_p1024_q64_um4_un1_simd1_single_block,
    gemm_p1024_q64_um4_un1_simd1_fixed_q,
    gemm_p1024_q64_um4_un1_simd1_adaptive_half,
    gemm_p1024_q64_um4_un1_simd1_equal_split,
    gemm_p1024_q64_um4_un1_simd4_single_block,
    gemm_p1024_q64_um4_un1_simd4_fixed_q,
    gemm_p1024_q64_um4_un1_simd4_adaptive_half,
    gemm_p1024_q64_um4_un1_simd4_equal_split,
    gemm_p1024_q64_um4_un4_simd4_single_block,
    gemm_p1024_q64_um4_un4_simd4_fixed_q,
    gemm_p1024_q64_um4_un4_simd4_adaptive_half,
    gemm_p1024_q64_um4_un4_simd4_equal_split,
    gemm_p1024_q64_um8_un1_simd1_single_block,
    gemm_p1024_q64_um8_un1_simd1_fixed_q,
    gemm_p1024_q64_um8_un1_simd1_adaptive_half,
    gemm_p1024_q64_um8_un1_simd1_equal_split,
    gemm_p1024_q64_um8_un1_simd4_single_block,
    gemm_p1024_q64_um8_un1_simd4_fixed_q,
    gemm_p1024_q64_um8_un1_simd4_adaptive_half,
    gemm_p1024_q64_um8_un1_simd4_equal_split,
    gemm_p1024_q64_um8_un1_simd8_single_block,
    gemm_p1024_q64_um8_un1_simd8_fixed_q,
    gemm_p1024_q64_um8_un1_simd8_adaptive_half,
    gemm_p1024_q64_um8_un1_simd8_equal_split,
    gemm_p1024_q64_um8_un4_simd4_single_block,
    gemm_p1024_q64_um8_un4_simd4_fixed_q,
    gemm_p1024_q64_um8_un4_simd4_adaptive_half,
    gemm_p1024_q64_um8_un4_simd4_equal_split,
    gemm_p1024_q64_um8_un4_simd8_single_block,
    gemm_p1024_q64_um8_un4_simd8_fixed_q,
    gemm_p1024_q64_um8_un4_simd8_adaptive_half,
    gemm_p1024_q64_um8_un4_simd8_equal_split,
    gemm_p1024_q64_um16_un1_simd4_single_block,
    gemm_p1024_q64_um16_un1_simd4_fixed_q,
    gemm_p1024_q64_um16_un1_simd4_adaptive_half,
    gemm_p1024_q64_um16_un1_simd4_equal_split,
    gemm_p1024_q64_um16_un1_simd8_single_block,
    gemm_p1024_q64_um16_un1_simd8_fixed_q,
    gemm_p1024_q64_um16_un1_simd8_adaptive_half,
    gemm_p1024_q64_um16_un1_simd8_equal_split,
    gemm_p1024_q64_um16_un1_simd16_single_block,
    gemm_p1024_q64_um16_un1_simd16_fixed_q,
    gemm_p1024_q64_um16_un1_simd16_adaptive_half,
    gemm_p1024_q64_um16_un1_simd16_equal_split,
    gemm_p1024_q64_um16_un4_simd8_single_block,
    gemm_p1024_q64_um16_un4_simd8_fixed_q,
    gemm_p1024_q64_um16_un4_simd8_adaptive_half,
    gemm_p1024_q64_um16_un4_simd8_equal_split,
    gemm_p1024_q64_um16_un4_simd16_single_block,
    gemm_p1024_q64_um16_un4_simd16_fixed_q,
    gemm_p1024_q64_um16_un4_simd16_adaptive_half,
    gemm_p1024_q64_um16_un4_simd16_equal_split,
    gemm_p1024_q64_um32_un1_simd4_single_block,
    gemm_p1024_q64_um32_un1_simd4_fixed_q,
    gemm_p1024_q64_um32_un1_simd4_adaptive_half,
    gemm_p1024_q64_um32_un1_simd4_equal_split,
    gemm_p1024_q64_um32_un1_simd8_single_block,
    gemm_p1024_q64_um32_un1_simd8_fixed_q,
    gemm_p1024_q64_um32_un1_simd8_adaptive_half,
    gemm_p1024_q64_um32_un1_simd8_equal_split,
    gemm_p1024_q64_um32_un1_simd16_single_block,
    gemm_p1024_q64_um32_un1_simd16_fixed_q,
    gemm_p1024_q64_um32_un1_simd16_adaptive_half,
    gemm_p1024_q64_um32_un1_simd16_equal_split,
    gemm_p1024_q64_um32_un4_simd16_single_block,
    gemm_p1024_q64_um32_un4_simd16_fixed_q,
    gemm_p1024_q64_um32_un4_simd16_adaptive_half,
    gemm_p1024_q64_um32_un4_simd16_equal_split,
    gemm_p1024_q128_um1_un1_simd1_single_block,
    gemm_p1024_q128_um1_un1_simd1_fixed_q,
    gemm_p1024_q128_um1_un1_simd1_adaptive_half,
    gemm_p1024_q128_um1_un1_simd1_equal_split,
    gemm_p1024_q128_um1_un4_simd1_single_block,
    gemm_p1024_q128_um1_un4_simd1_fixed_q,
    gemm_p1024_q128_um1_un4_simd1_adaptive_half,
    gemm_p1024_q128_um1_un4_simd1_equal_split,
    gemm_p1024_q128_um4_un1_simd1_single_block,
    gemm_p1024_q128_um4_un1_simd1_fixed_q,
    gemm_p1024_q128_um4_un1_simd1_adaptive_half,
    gemm_p1024_q128_um4_un1_simd1_equal_split,
    gemm_p1024_q128_um4_un1_simd4_single_block,
    gemm_p1024_q128_um4_un1_simd4_fixed_q,
    gemm_p1024_q128_um4_un1_simd4_adaptive_half,
    gemm_p1024_q128_um4_un1_simd4_equal_split,
    gemm_p1024_q128_um4_un4_simd4_single_block,
    gemm_p1024_q128_um4_un4_simd4_fixed_q,
    gemm_p1024_q128_um4_un4_simd4_adaptive_half,
    gemm_p1024_q128_um4_un4_simd4_equal_split,
    gemm_p1024_q128_um8_un1_simd1_single_block,
    gemm_p1024_q128_um8_un1_simd1_fixed_q,
    gemm_p1024_q128_um8_un1_simd1_adaptive_half,
    gemm_p1024_q128_um8_un1_simd1_equal_split,
    gemm_p1024_q128_um8_un1_simd4_single_block,
    gemm_p1024_q128_um8_un1_simd4_fixed_q,
    gemm_p1024_q128_um8_un1_simd4_adaptive_half,
    gemm_p1024_q128_um8_un1_simd4_equal_split,
    gemm_p1024_q128_um8_un1_simd8_single_block,
    gemm_p1024_q128_um8_un1_simd8_fixed_q,
    gemm_p1024_q128_um8_un1_simd8_adaptive_half,
    gemm_p1024_q128_um8_un1_simd8_equal_split,
    gemm_p1024_q128_um8_un4_simd4_single_block,
    gemm_p1024_q128_um8_un4_simd4_fixed_q,
    gemm_p1024_q128_um8_un4_simd4_adaptive_half,
    gemm_p1024_q128_um8_un4_simd4_equal_split,
    gemm_p1024_q128_um8_un4_simd8_single_block,
    gemm_p1024_q128_um8_un4_simd8_fixed_q,
    gemm_p1024_q128_um8_un4_simd8_adaptive_half,
    gemm_p1024_q128_um8_un4_simd8_equal_split,
    gemm_p1024_q128_um16_un1_simd4_single_block,
    gemm_p1024_q128_um16_un1_simd4_fixed_q,
    gemm_p1024_q128_um16_un1_simd4_adaptive_half,
    gemm_p1024_q128_um16_un1_simd4_equal_split,
    gemm_p1024_q128_um16_un1_simd8_single_block,
    gemm_p1024_q128_um16_un1_simd8_fixed_q,
    gemm_p1024_q128_um16_un1_simd8_adaptive_half,
    gemm_p1024_q128_um16_un1_simd8_equal_split,
    gemm_p1024_q128_um16_un1_simd16_single_block,
    gemm_p1024_q128_um16_un1_simd16_fixed_q,
    gemm_p1024_q128_um16_un1_simd16_adaptive_half,
    gemm_p1024_q128_um16_un1_simd16_equal_split,
    gemm_p1024_q128_um16_un4_simd8_single_block,
    gemm_p1024_q128_um16_un4_simd8_fixed_q,
    gemm_p1024_q128_um16_un4_simd8_adaptive_half,
    gemm_p1024_q128_um16_un4_simd8_equal_split,
    gemm_p1024_q128_um16_un4_simd16_single_block,
    gemm_p1024_q128_um16_un4_simd16_fixed_q,
    gemm_p1024_q128_um16_un4_simd16_adaptive_half,
    gemm_p1024_q128_um16_un4_simd16_equal_split,
    gemm_p1024_q128_um32_un1_simd4_single_block,
    gemm_p1024_q128_um32_un1_simd4_fixed_q,
    gemm_p1024_q128_um32_un1_simd4_adaptive_half,
    gemm_p1024_q128_um32_un1_simd4_equal_split,
    gemm_p1024_q128_um32_un1_simd8_single_block,
    gemm_p1024_q128_um32_un1_simd8_fixed_q,
    gemm_p1024_q128_um32_un1_simd8_adaptive_half,
    gemm_p1024_q128_um32_un1_simd8_equal_split,
    gemm_p1024_q128_um32_un1_simd16_single_block,
    gemm_p1024_q128_um32_un1_simd16_fixed_q,
    gemm_p1024_q128_um32_un1_simd16_adaptive_half,
    gemm_p1024_q128_um32_un1_simd16_equal_split,
    gemm_p1024_q128_um32_un4_simd16_single_block,
    gemm_p1024_q128_um32_un4_simd16_fixed_q,
    gemm_p1024_q128_um32_un4_simd16_adaptive_half,
    gemm_p1024_q128_um32_un4_simd16_equal_split,
    gemm_p1024_q192_um1_un1_simd1_single_block,
    gemm_p1024_q192_um1_un1_simd1_fixed_q,
    gemm_p1024_q192_um1_un1_simd1_adaptive_half,
    gemm_p1024_q192_um1_un1_simd1_equal_split,
    gemm_p1024_q192_um1_un4_simd1_single_block,
    gemm_p1024_q192_um1_un4_simd1_fixed_q,
    gemm_p1024_q192_um1_un4_simd1_adaptive_half,
    gemm_p1024_q192_um1_un4_simd1_equal_split,
    gemm_p1024_q192_um4_un1_simd1_single_block,
    gemm_p1024_q192_um4_un1_simd1_fixed_q,
    gemm_p1024_q192_um4_un1_simd1_adaptive_half,
    gemm_p1024_q192_um4_un1_simd1_equal_split,
    gemm_p1024_q192_um4_un1_simd4_single_block,
    gemm_p1024_q192_um4_un1_simd4_fixed_q,
    gemm_p1024_q192_um4_un1_simd4_adaptive_half,
    gemm_p1024_q192_um4_un1_simd4_equal_split,
    gemm_p1024_q192_um4_un4_simd4_single_block,
    gemm_p1024_q192_um4_un4_simd4_fixed_q,
    gemm_p1024_q192_um4_un4_simd4_adaptive_half,
    gemm_p1024_q192_um4_un4_simd4_equal_split,
    gemm_p1024_q192_um8_un1_simd1_single_block,
    gemm_p1024_q192_um8_un1_simd1_fixed_q,
    gemm_p1024_q192_um8_un1_simd1_adaptive_half,
    gemm_p1024_q192_um8_un1_simd1_equal_split,
    gemm_p1024_q192_um8_un1_simd4_single_block,
    gemm_p1024_q192_um8_un1_simd4_fixed_q,
    gemm_p1024_q192_um8_un1_simd4_adaptive_half,
    gemm_p1024_q192_um8_un1_simd4_equal_split,
    gemm_p1024_q192_um8_un1_simd8_single_block,
    gemm_p1024_q192_um8_un1_simd8_fixed_q,
    gemm_p1024_q192_um8_un1_simd8_adaptive_half,
    gemm_p1024_q192_um8_un1_simd8_equal_split,
    gemm_p1024_q192_um8_un4_simd4_single_block,
    gemm_p1024_q192_um8_un4_simd4_fixed_q,
    gemm_p1024_q192_um8_un4_simd4_adaptive_half,
    gemm_p1024_q192_um8_un4_simd4_equal_split,
    gemm_p1024_q192_um8_un4_simd8_single_block,
    gemm_p1024_q192_um8_un4_simd8_fixed_q,
    gemm_p1024_q192_um8_un4_simd8_adaptive_half,
    gemm_p1024_q192_um8_un4_simd8_equal_split,
    gemm_p1024_q192_um16_un1_simd4_single_block,
    gemm_p1024_q192_um16_un1_simd4_fixed_q,
    gemm_p1024_q192_um16_un1_simd4_adaptive_half,
    gemm_p1024_q192_um16_un1_simd4_equal_split,
    gemm_p1024_q192_um16_un1_simd8_single_block,
    gemm_p1024_q192_um16_un1_simd8_fixed_q,
    gemm_p1024_q192_um16_un1_simd8_adaptive_half,
    gemm_p1024_q192_um16_un1_simd8_equal_split,
    gemm_p1024_q192_um16_un1_simd16_single_block,
    gemm_p1024_q192_um16_un1_simd16_fixed_q,
    gemm_p1024_q192_um16_un1_simd16_adaptive_half,
    gemm_p1024_q192_um16_un1_simd16_equal_split,
    gemm_p1024_q192_um16_un4_simd8_single_block,
    gemm_p1024_q192_um16_un4_simd8_fixed_q,
    gemm_p1024_q192_um16_un4_simd8_adaptive_half,
    gemm_p1024_q192_um16_un4_simd8_equal_split,
    gemm_p1024_q192_um16_un4_simd16_single_block,
    gemm_p1024_q192_um16_un4_simd16_fixed_q,
    gemm_p1024_q192_um16_un4_simd16_adaptive_half,
    gemm_p1024_q192_um16_un4_simd16_equal_split,
    gemm_p1024_q192_um32_un1_simd4_single_block,
    gemm_p1024_q192_um32_un1_simd4_fixed_q,
    gemm_p1024_q192_um32_un1_simd4_adaptive_half,
    gemm_p1024_q192_um32_un1_simd4_equal_split,
    gemm_p1024_q192_um32_un1_simd8_single_block,
    gemm_p1024_q192_um32_un1_simd8_fixed_q,
    gemm_p1024_q192_um32_un1_simd8_adaptive_half,
    gemm_p1024_q192_um32_un1_simd8_equal_split,
    gemm_p1024_q192_um32_un1_simd16_single_block,
    gemm_p1024_q192_um32_un1_simd16_fixed_q,
    gemm_p1024_q192_um32_un1_simd16_adaptive_half,
    gemm_p1024_q192_um32_un1_simd16_equal_split,
    gemm_p1024_q192_um32_un4_simd16_single_block,
    gemm_p1024_q192_um32_un4_simd16_fixed_q,
    gemm_p1024_q192_um32_un4_simd16_adaptive_half,
    gemm_p1024_q192_um32_un4_simd16_equal_split,
    gemm_p1024_q240_um1_un1_simd1_single_block,
    gemm_p1024_q240_um1_un1_simd1_fixed_q,
    gemm_p1024_q240_um1_un1_simd1_adaptive_half,
    gemm_p1024_q240_um1_un1_simd1_equal_split,
    gemm_p1024_q240_um1_un4_simd1_single_block,
    gemm_p1024_q240_um1_un4_simd1_fixed_q,
    gemm_p1024_q240_um1_un4_simd1_adaptive_half,
    gemm_p1024_q240_um1_un4_simd1_equal_split,
    gemm_p1024_q240_um4_un1_simd1_single_block,
    gemm_p1024_q240_um4_un1_simd1_fixed_q,
    gemm_p1024_q240_um4_un1_simd1_adaptive_half,
    gemm_p1024_q240_um4_un1_simd1_equal_split,
    gemm_p1024_q240_um4_un1_simd4_single_block,
    gemm_p1024_q240_um4_un1_simd4_fixed_q,
    gemm_p1024_q240_um4_un1_simd4_adaptive_half,
    gemm_p1024_q240_um4_un1_simd4_equal_split,
    gemm_p1024_q240_um4_un4_simd4_single_block,
    gemm_p1024_q240_um4_un4_simd4_fixed_q,
    gemm_p1024_q240_um4_un4_simd4_adaptive_half,
    gemm_p1024_q240_um4_un4_simd4_equal_split,
    gemm_p1024_q240_um8_un1_simd1_single_block,
    gemm_p1024_q240_um8_un1_simd1_fixed_q,
    gemm_p1024_q240_um8_un1_simd1_adaptive_half,
    gemm_p1024_q240_um8_un1_simd1_equal_split,
    gemm_p1024_q240_um8_un1_simd4_single_block,
    gemm_p1024_q240_um8_un1_simd4_fixed_q,
    gemm_p1024_q240_um8_un1_simd4_adaptive_half,
    gemm_p1024_q240_um8_un1_simd4_equal_split,
    gemm_p1024_q240_um8_un1_simd8_single_block,
    gemm_p1024_q240_um8_un1_simd8_fixed_q,
    gemm_p1024_q240_um8_un1_simd8_adaptive_half,
    gemm_p1024_q240_um8_un1_simd8_equal_split,
    gemm_p1024_q240_um8_un4_simd4_single_block,
    gemm_p1024_q240_um8_un4_simd4_fixed_q,
    gemm_p1024_q240_um8_un4_simd4_adaptive_half,
    gemm_p1024_q240_um8_un4_simd4_equal_split,
    gemm_p1024_q240_um8_un4_simd8_single_block,
    gemm_p1024_q240_um8_un4_simd8_fixed_q,
    gemm_p1024_q240_um8_un4_simd8_adaptive_half,
    gemm_p1024_q240_um8_un4_simd8_equal_split,
    gemm_p1024_q240_um16_un1_simd4_single_block,
    gemm_p1024_q240_um16_un1_simd4_fixed_q,
    gemm_p1024_q240_um16_un1_simd4_adaptive_half,
    gemm_p1024_q240_um16_un1_simd4_equal_split,
    gemm_p1024_q240_um16_un1_simd8_single_block,
    gemm_p1024_q240_um16_un1_simd8_fixed_q,
    gemm_p1024_q240_um16_un1_simd8_adaptive_half,
    gemm_p1024_q240_um16_un1_simd8_equal_split,
    gemm_p1024_q240_um16_un1_simd16_single_block,
    gemm_p1024_q240_um16_un1_simd16_fixed_q,
    gemm_p1024_q240_um16_un1_simd16_adaptive_half,
    gemm_p1024_q240_um16_un1_simd16_equal_split,
    gemm_p1024_q240_um16_un4_simd8_single_block,
    gemm_p1024_q240_um16_un4_simd8_fixed_q,
    gemm_p1024_q240_um16_un4_simd8_adaptive_half,
    gemm_p1024_q240_um16_un4_simd8_equal_split,
    gemm_p1024_q240_um16_un4_simd16_single_block,
    gemm_p1024_q240_um16_un4_simd16_fixed_q,
    gemm_p1024_q240_um16_un4_simd16_adaptive_half,
    gemm_p1024_q240_um16_un4_simd16_equal_split,
    gemm_p1024_q240_um32_un1_simd4_single_block,
    gemm_p1024_q240_um32_un1_simd4_fixed_q,
    gemm_p1024_q240_um32_un1_simd4_adaptive_half,
    gemm_p1024_q240_um32_un1_simd4_equal_split,
    gemm_p1024_q240_um32_un1_simd8_single_block,
    gemm_p1024_q240_um32_un1_simd8_fixed_q,
    gemm_p1024_q240_um32_un1_simd8_adaptive_half,
    gemm_p1024_q240_um32_un1_simd8_equal_split,
    gemm_p1024_q240_um32_un1_simd16_single_block,
    gemm_p1024_q240_um32_un1_simd16_fixed_q,
    gemm_p1024_q240_um32_un1_simd16_adaptive_half,
    gemm_p1024_q240_um32_un1_simd16_equal_split,
    gemm_p1024_q248_um1_un1_simd1_single_block,
    gemm_p1024_q248_um1_un1_simd1_fixed_q,
    gemm_p1024_q248_um1_un1_simd1_adaptive_half,
    gemm_p1024_q248_um1_un1_simd1_equal_split,
    gemm_p1024_q248_um1_un4_simd1_single_block,
    gemm_p1024_q248_um1_un4_simd1_fixed_q,
    gemm_p1024_q248_um1_un4_simd1_adaptive_half,
    gemm_p1024_q248_um1_un4_simd1_equal_split,
    gemm_p1024_q248_um4_un1_simd1_single_block,
    gemm_p1024_q248_um4_un1_simd1_fixed_q,
    gemm_p1024_q248_um4_un1_simd1_adaptive_half,
    gemm_p1024_q248_um4_un1_simd1_equal_split,
    gemm_p1024_q248_um4_un1_simd4_single_block,
    gemm_p1024_q248_um4_un1_simd4_fixed_q,
    gemm_p1024_q248_um4_un1_simd4_adaptive_half,
    gemm_p1024_q248_um4_un1_simd4_equal_split,
    gemm_p1024_q248_um4_un4_simd4_single_block,
    gemm_p1024_q248_um4_un4_simd4_fixed_q,
    gemm_p1024_q248_um4_un4_simd4_adaptive_half,
    gemm_p1024_q248_um4_un4_simd4_equal_split,
    gemm_p1024_q248_um8_un1_simd1_single_block,
    gemm_p1024_q248_um8_un1_simd1_fixed_q,
    gemm_p1024_q248_um8_un1_simd1_adaptive_half,
    gemm_p1024_q248_um8_un1_simd1_equal_split,
    gemm_p1024_q248_um8_un1_simd4_single_block,
    gemm_p1024_q248_um8_un1_simd4_fixed_q,
    gemm_p1024_q248_um8_un1_simd4_adaptive_half,
    gemm_p1024_q248_um8_un1_simd4_equal_split,
    gemm_p1024_q248_um8_un1_simd8_single_block,
    gemm_p1024_q248_um8_un1_simd8_fixed_q,
    gemm_p1024_q248_um8_un1_simd8_adaptive_half,
    gemm_p1024_q248_um8_un1_simd8_equal_split,
    gemm_p1024_q248_um8_un4_simd4_single_block,
    gemm_p1024_q248_um8_un4_simd4_fixed_q,
    gemm_p1024_q248_um8_un4_simd4_adaptive_half,
    gemm_p1024_q248_um8_un4_simd4_equal_split,
    gemm_p1024_q248_um8_un4_simd8_single_block,
    gemm_p1024_q248_um8_un4_simd8_fixed_q,
    gemm_p1024_q248_um8_un4_simd8_adaptive_half,
    gemm_p1024_q248_um8_un4_simd8_equal_split,
    gemm_p1024_q248_um16_un1_simd4_single_block,
    gemm_p1024_q248_um16_un1_simd4_fixed_q,
    gemm_p1024_q248_um16_un1_simd4_adaptive_half,
    gemm_p1024_q248_um16_un1_simd4_equal_split,
    gemm_p1024_q248_um16_un1_simd8_single_block,
    gemm_p1024_q248_um16_un1_simd8_fixed_q,
    gemm_p1024_q248_um16_un1_simd8_adaptive_half,
    gemm_p1024_q248_um16_un1_simd8_equal_split,
    gemm_p1024_q248_um16_un1_simd16_single_block,
    gemm_p1024_q248_um16_un1_simd16_fixed_q,
    gemm_p1024_q248_um16_un1_simd16_adaptive_half,
    gemm_p1024_q248_um16_un1_simd16_equal_split,
    gemm_p1024_q248_um16_un4_simd8_single_block,
    gemm_p1024_q248_um16_un4_simd8_fixed_q,
    gemm_p1024_q248_um16_un4_simd8_adaptive_half,
    gemm_p1024_q248_um16_un4_simd8_equal_split,
    gemm_p1024_q248_um16_un4_simd16_single_block,
    gemm_p1024_q248_um16_un4_simd16_fixed_q,
    gemm_p1024_q248_um16_un4_simd16_adaptive_half,
    gemm_p1024_q248_um16_un4_simd16_equal_split,
    gemm_p1024_q248_um32_un1_simd4_single_block,
    gemm_p1024_q248_um32_un1_simd4_fixed_q,
    gemm_p1024_q248_um32_un1_simd4_adaptive_half,
    gemm_p1024_q248_um32_un1_simd4_equal_split,
    gemm_p1024_q248_um32_un1_simd8_single_block,
    gemm_p1024_q248_um32_un1_simd8_fixed_q,
    gemm_p1024_q248_um32_un1_simd8_adaptive_half,
    gemm_p1024_q248_um32_un1_simd8_equal_split,
    gemm_p1024_q248_um32_un1_simd16_single_block,
    gemm_p1024_q248_um32_un1_simd16_fixed_q,
    gemm_p1024_q248_um32_un1_simd16_adaptive_half,
    gemm_p1024_q248_um32_un1_simd16_equal_split,
    gemm_p1024_q256_um1_un1_simd1_single_block,
    gemm_p1024_q256_um1_un1_simd1_fixed_q,
    gemm_p1024_q256_um1_un1_simd1_adaptive_half,
    gemm_p1024_q256_um1_un1_simd1_equal_split,
    gemm_p1024_q256_um1_un4_simd1_single_block,
    gemm_p1024_q256_um1_un4_simd1_fixed_q,
    gemm_p1024_q256_um1_un4_simd1_adaptive_half,
    gemm_p1024_q256_um1_un4_simd1_equal_split,
    gemm_p1024_q256_um4_un1_simd1_single_block,
    gemm_p1024_q256_um4_un1_simd1_fixed_q,
    gemm_p1024_q256_um4_un1_simd1_adaptive_half,
    gemm_p1024_q256_um4_un1_simd1_equal_split,
    gemm_p1024_q256_um4_un1_simd4_single_block,
    gemm_p1024_q256_um4_un1_simd4_fixed_q,
    gemm_p1024_q256_um4_un1_simd4_adaptive_half,
    gemm_p1024_q256_um4_un1_simd4_equal_split,
    gemm_p1024_q256_um4_un4_simd4_single_block,
    gemm_p1024_q256_um4_un4_simd4_fixed_q,
    gemm_p1024_q256_um4_un4_simd4_adaptive_half,
    gemm_p1024_q256_um4_un4_simd4_equal_split,
    gemm_p1024_q256_um8_un1_simd1_single_block,
    gemm_p1024_q256_um8_un1_simd1_fixed_q,
    gemm_p1024_q256_um8_un1_simd1_adaptive_half,
    gemm_p1024_q256_um8_un1_simd1_equal_split,
    gemm_p1024_q256_um8_un1_simd4_single_block,
    gemm_p1024_q256_um8_un1_simd4_fixed_q,
    gemm_p1024_q256_um8_un1_simd4_adaptive_half,
    gemm_p1024_q256_um8_un1_simd4_equal_split,
    gemm_p1024_q256_um8_un1_simd8_single_block,
    gemm_p1024_q256_um8_un1_simd8_fixed_q,
    gemm_p1024_q256_um8_un1_simd8_adaptive_half,
    gemm_p1024_q256_um8_un1_simd8_equal_split,
    gemm_p1024_q256_um8_un4_simd4_single_block,
    gemm_p1024_q256_um8_un4_simd4_fixed_q,
    gemm_p1024_q256_um8_un4_simd4_adaptive_half,
    gemm_p1024_q256_um8_un4_simd4_equal_split,
    gemm_p1024_q256_um8_un4_simd8_single_block,
    gemm_p1024_q256_um8_un4_simd8_fixed_q,
    gemm_p1024_q256_um8_un4_simd8_adaptive_half,
    gemm_p1024_q256_um8_un4_simd8_equal_split,
    gemm_p1024_q256_um16_un1_simd4_single_block,
    gemm_p1024_q256_um16_un1_simd4_fixed_q,
    gemm_p1024_q256_um16_un1_simd4_adaptive_half,
    gemm_p1024_q256_um16_un1_simd4_equal_split,
    gemm_p1024_q256_um16_un1_simd8_single_block,
    gemm_p1024_q256_um16_un1_simd8_fixed_q,
    gemm_p1024_q256_um16_un1_simd8_adaptive_half,
    gemm_p1024_q256_um16_un1_simd8_equal_split,
    gemm_p1024_q256_um16_un1_simd16_single_block,
    gemm_p1024_q256_um16_un1_simd16_fixed_q,
    gemm_p1024_q256_um16_un1_simd16_adaptive_half,
    gemm_p1024_q256_um16_un1_simd16_equal_split,
    gemm_p1024_q256_um16_un4_simd8_single_block,
    gemm_p1024_q256_um16_un4_simd8_fixed_q,
    gemm_p1024_q256_um16_un4_simd8_adaptive_half,
    gemm_p1024_q256_um16_un4_simd8_equal_split,
    gemm_p1024_q256_um16_un4_simd16_single_block,
    gemm_p1024_q256_um16_un4_simd16_fixed_q,
    gemm_p1024_q256_um16_un4_simd16_adaptive_half,
    gemm_p1024_q256_um16_un4_simd16_equal_split,
    gemm_p1024_q384_um1_un1_simd1_single_block,
    gemm_p1024_q384_um1_un1_simd1_fixed_q,
    gemm_p1024_q384_um1_un1_simd1_adaptive_half,
    gemm_p1024_q384_um1_un1_simd1_equal_split,
    gemm_p1024_q384_um1_un4_simd1_single_block,
    gemm_p1024_q384_um1_un4_simd1_fixed_q,
    gemm_p1024_q384_um1_un4_simd1_adaptive_half,
    gemm_p1024_q384_um1_un4_simd1_equal_split,
    gemm_p1024_q384_um4_un1_simd1_single_block,
    gemm_p1024_q384_um4_un1_simd1_fixed_q,
    gemm_p1024_q384_um4_un1_simd1_adaptive_half,
    gemm_p1024_q384_um4_un1_simd1_equal_split,
    gemm_p1024_q384_um4_un1_simd4_single_block,
    gemm_p1024_q384_um4_un1_simd4_fixed_q,
    gemm_p1024_q384_um4_un1_simd4_adaptive_half,
    gemm_p1024_q384_um4_un1_simd4_equal_split,
    gemm_p1024_q384_um4_un4_simd4_single_block,
    gemm_p1024_q384_um4_un4_simd4_fixed_q,
    gemm_p1024_q384_um4_un4_simd4_adaptive_half,
    gemm_p1024_q384_um4_un4_simd4_equal_split,
    gemm_p1024_q384_um8_un1_simd1_single_block,
    gemm_p1024_q384_um8_un1_simd1_fixed_q,
    gemm_p1024_q384_um8_un1_simd1_adaptive_half,
    gemm_p1024_q384_um8_un1_simd1_equal_split,
    gemm_p1024_q384_um8_un1_simd4_single_block,
    gemm_p1024_q384_um8_un1_simd4_fixed_q,
    gemm_p1024_q384_um8_un1_simd4_adaptive_half,
    gemm_p1024_q384_um8_un1_simd4_equal_split,
    gemm_p1024_q384_um8_un1_simd8_single_block,
    gemm_p1024_q384_um8_un1_simd8_fixed_q,
    gemm_p1024_q384_um8_un1_simd8_adaptive_half,
    gemm_p1024_q384_um8_un1_simd8_equal_split,
    gemm_p1024_q384_um8_un4_simd4_single_block,
    gemm_p1024_q384_um8_un4_simd4_fixed_q,
    gemm_p1024_q384_um8_un4_simd4_adaptive_half,
    gemm_p1024_q384_um8_un4_simd4_equal_split,
    gemm_p1024_q384_um8_un4_simd8_single_block,
    gemm_p1024_q384_um8_un4_simd8_fixed_q,
    gemm_p1024_q384_um8_un4_simd8_adaptive_half,
    gemm_p1024_q384_um8_un4_simd8_equal_split,
    gemm_p1024_q384_um16_un1_simd4_single_block,
    gemm_p1024_q384_um16_un1_simd4_fixed_q,
    gemm_p1024_q384_um16_un1_simd4_adaptive_half,
    gemm_p1024_q384_um16_un1_simd4_equal_split,
    gemm_p1024_q384_um16_un1_simd8_single_block,
    gemm_p1024_q384_um16_un1_simd8_fixed_q,
    gemm_p1024_q384_um16_un1_simd8_adaptive_half,
    gemm_p1024_q384_um16_un1_simd8_equal_split,
    gemm_p1024_q384_um16_un1_simd16_single_block,
    gemm_p1024_q384_um16_un1_simd16_fixed_q,
    gemm_p1024_q384_um16_un1_simd16_adaptive_half,
    gemm_p1024_q384_um16_un1_simd16_equal_split,
    gemm_p1024_q384_um16_un4_simd8_single_block,
    gemm_p1024_q384_um16_un4_simd8_fixed_q,
    gemm_p1024_q384_um16_un4_simd8_adaptive_half,
    gemm_p1024_q384_um16_un4_simd8_equal_split,
    gemm_p1024_q384_um16_un4_simd16_single_block,
    gemm_p1024_q384_um16_un4_simd16_fixed_q,
    gemm_p1024_q384_um16_un4_simd16_adaptive_half,
    gemm_p1024_q384_um16_un4_simd16_equal_split,
    gemm_p1024_q512_um1_un1_simd1_single_block,
    gemm_p1024_q512_um1_un1_simd1_fixed_q,
    gemm_p1024_q512_um1_un1_simd1_adaptive_half,
    gemm_p1024_q512_um1_un1_simd1_equal_split,
    gemm_p1024_q512_um1_un4_simd1_single_block,
    gemm_p1024_q512_um1_un4_simd1_fixed_q,
    gemm_p1024_q512_um1_un4_simd1_adaptive_half,
    gemm_p1024_q512_um1_un4_simd1_equal_split,
    gemm_p1024_q512_um4_un1_simd1_single_block,
    gemm_p1024_q512_um4_un1_simd1_fixed_q,
    gemm_p1024_q512_um4_un1_simd1_adaptive_half,
    gemm_p1024_q512_um4_un1_simd1_equal_split,
    gemm_p1024_q512_um4_un1_simd4_single_block,
    gemm_p1024_q512_um4_un1_simd4_fixed_q,
    gemm_p1024_q512_um4_un1_simd4_adaptive_half,
    gemm_p1024_q512_um4_un1_simd4_equal_split,
    gemm_p1024_q512_um4_un4_simd4_single_block,
    gemm_p1024_q512_um4_un4_simd4_fixed_q,
    gemm_p1024_q512_um4_un4_simd4_adaptive_half,
    gemm_p1024_q512_um4_un4_simd4_equal_split,
    gemm_p1024_q512_um8_un1_simd1_single_block,
    gemm_p1024_q512_um8_un1_simd1_fixed_q,
    gemm_p1024_q512_um8_un1_simd1_adaptive_half,
    gemm_p1024_q512_um8_un1_simd1_equal_split,
    gemm_p1024_q512_um8_un1_simd4_single_block,
    gemm_p1024_q512_um8_un1_simd4_fixed_q,
    gemm_p1024_q512_um8_un1_simd4_adaptive_half,
    gemm_p1024_q512_um8_un1_simd4_equal_split,
    gemm_p1024_q512_um8_un1_simd8_single_block,
    gemm_p1024_q512_um8_un1_simd8_fixed_q,
    gemm_p1024_q512_um8_un1_simd8_adaptive_half,
    gemm_p1024_q512_um8_un1_simd8_equal_split,
    gemm_p1024_q512_um8_un4_simd4_single_block,
    gemm_p1024_q512_um8_un4_simd4_fixed_q,
    gemm_p1024_q512_um8_un4_simd4_adaptive_half,
    gemm_p1024_q512_um8_un4_simd4_equal_split,
    gemm_p1024_q512_um8_un4_simd8_single_block,
    gemm_p1024_q512_um8_un4_simd8_fixed_q,
    gemm_p1024_q512_um8_un4_simd8_adaptive_half,
    gemm_p1024_q512_um8_un4_simd8_equal_split,
};

const char* gemm_dispatch_names[] = {
    "gemm_p1_q1_um1_un1_simd1_single_block",
    "gemm_p1_q1_um1_un1_simd1_fixed_q",
    "gemm_p1_q1_um1_un1_simd1_adaptive_half",
    "gemm_p1_q1_um1_un1_simd1_equal_split",
    "gemm_p1_q1_um1_un4_simd1_single_block",
    "gemm_p1_q1_um1_un4_simd1_fixed_q",
    "gemm_p1_q1_um1_un4_simd1_adaptive_half",
    "gemm_p1_q1_um1_un4_simd1_equal_split",
    "gemm_p1_q64_um1_un1_simd1_single_block",
    "gemm_p1_q64_um1_un1_simd1_fixed_q",
    "gemm_p1_q64_um1_un1_simd1_adaptive_half",
    "gemm_p1_q64_um1_un1_simd1_equal_split",
    "gemm_p1_q64_um1_un4_simd1_single_block",
    "gemm_p1_q64_um1_un4_simd1_fixed_q",
    "gemm_p1_q64_um1_un4_simd1_adaptive_half",
    "gemm_p1_q64_um1_un4_simd1_equal_split",
    "gemm_p1_q128_um1_un1_simd1_single_block",
    "gemm_p1_q128_um1_un1_simd1_fixed_q",
    "gemm_p1_q128_um1_un1_simd1_adaptive_half",
    "gemm_p1_q128_um1_un1_simd1_equal_split",
    "gemm_p1_q128_um1_un4_simd1_single_block",
    "gemm_p1_q128_um1_un4_simd1_fixed_q",
    "gemm_p1_q128_um1_un4_simd1_adaptive_half",
    "gemm_p1_q128_um1_un4_simd1_equal_split",
    "gemm_p1_q192_um1_un1_simd1_single_block",
    "gemm_p1_q192_um1_un1_simd1_fixed_q",
    "gemm_p1_q192_um1_un1_simd1_adaptive_half",
    "gemm_p1_q192_um1_un1_simd1_equal_split",
    "gemm_p1_q192_um1_un4_simd1_single_block",
    "gemm_p1_q192_um1_un4_simd1_fixed_q",
    "gemm_p1_q192_um1_un4_simd1_adaptive_half",
    "gemm_p1_q192_um1_un4_simd1_equal_split",
    "gemm_p1_q240_um1_un1_simd1_single_block",
    "gemm_p1_q240_um1_un1_simd1_fixed_q",
    "gemm_p1_q240_um1_un1_simd1_adaptive_half",
    "gemm_p1_q240_um1_un1_simd1_equal_split",
    "gemm_p1_q240_um1_un4_simd1_single_block",
    "gemm_p1_q240_um1_un4_simd1_fixed_q",
    "gemm_p1_q240_um1_un4_simd1_adaptive_half",
    "gemm_p1_q240_um1_un4_simd1_equal_split",
    "gemm_p1_q248_um1_un1_simd1_single_block",
    "gemm_p1_q248_um1_un1_simd1_fixed_q",
    "gemm_p1_q248_um1_un1_simd1_adaptive_half",
    "gemm_p1_q248_um1_un1_simd1_equal_split",
    "gemm_p1_q248_um1_un4_simd1_single_block",
    "gemm_p1_q248_um1_un4_simd1_fixed_q",
    "gemm_p1_q248_um1_un4_simd1_adaptive_half",
    "gemm_p1_q248_um1_un4_simd1_equal_split",
    "gemm_p1_q256_um1_un1_simd1_single_block",
    "gemm_p1_q256_um1_un1_simd1_fixed_q",
    "gemm_p1_q256_um1_un1_simd1_adaptive_half",
    "gemm_p1_q256_um1_un1_simd1_equal_split",
    "gemm_p1_q256_um1_un4_simd1_single_block",
    "gemm_p1_q256_um1_un4_simd1_fixed_q",
    "gemm_p1_q256_um1_un4_simd1_adaptive_half",
    "gemm_p1_q256_um1_un4_simd1_equal_split",
    "gemm_p1_q384_um1_un1_simd1_single_block",
    "gemm_p1_q384_um1_un1_simd1_fixed_q",
    "gemm_p1_q384_um1_un1_simd1_adaptive_half",
    "gemm_p1_q384_um1_un1_simd1_equal_split",
    "gemm_p1_q384_um1_un4_simd1_single_block",
    "gemm_p1_q384_um1_un4_simd1_fixed_q",
    "gemm_p1_q384_um1_un4_simd1_adaptive_half",
    "gemm_p1_q384_um1_un4_simd1_equal_split",
    "gemm_p1_q512_um1_un1_simd1_single_block",
    "gemm_p1_q512_um1_un1_simd1_fixed_q",
    "gemm_p1_q512_um1_un1_simd1_adaptive_half",
    "gemm_p1_q512_um1_un1_simd1_equal_split",
    "gemm_p1_q512_um1_un4_simd1_single_block",
    "gemm_p1_q512_um1_un4_simd1_fixed_q",
    "gemm_p1_q512_um1_un4_simd1_adaptive_half",
    "gemm_p1_q512_um1_un4_simd1_equal_split",
    "gemm_p64_q1_um1_un1_simd1_single_block",
    "gemm_p64_q1_um1_un1_simd1_fixed_q",
    "gemm_p64_q1_um1_un1_simd1_adaptive_half",
    "gemm_p64_q1_um1_un1_simd1_equal_split",
    "gemm_p64_q1_um1_un4_simd1_single_block",
    "gemm_p64_q1_um1_un4_simd1_fixed_q",
    "gemm_p64_q1_um1_un4_simd1_adaptive_half",
    "gemm_p64_q1_um1_un4_simd1_equal_split",
    "gemm_p64_q1_um4_un1_simd1_single_block",
    "gemm_p64_q1_um4_un1_simd1_fixed_q",
    "gemm_p64_q1_um4_un1_simd1_adaptive_half",
    "gemm_p64_q1_um4_un1_simd1_equal_split",
    "gemm_p64_q1_um4_un1_simd4_single_block",
    "gemm_p64_q1_um4_un1_simd4_fixed_q",
    "gemm_p64_q1_um4_un1_simd4_adaptive_half",
    "gemm_p64_q1_um4_un1_simd4_equal_split",
    "gemm_p64_q1_um4_un4_simd4_single_block",
    "gemm_p64_q1_um4_un4_simd4_fixed_q",
    "gemm_p64_q1_um4_un4_simd4_adaptive_half",
    "gemm_p64_q1_um4_un4_simd4_equal_split",
    "gemm_p64_q1_um8_un1_simd1_single_block",
    "gemm_p64_q1_um8_un1_simd1_fixed_q",
    "gemm_p64_q1_um8_un1_simd1_adaptive_half",
    "gemm_p64_q1_um8_un1_simd1_equal_split",
    "gemm_p64_q1_um8_un1_simd4_single_block",
    "gemm_p64_q1_um8_un1_simd4_fixed_q",
    "gemm_p64_q1_um8_un1_simd4_adaptive_half",
    "gemm_p64_q1_um8_un1_simd4_equal_split",
    "gemm_p64_q1_um8_un1_simd8_single_block",
    "gemm_p64_q1_um8_un1_simd8_fixed_q",
    "gemm_p64_q1_um8_un1_simd8_adaptive_half",
    "gemm_p64_q1_um8_un1_simd8_equal_split",
    "gemm_p64_q1_um8_un4_simd4_single_block",
    "gemm_p64_q1_um8_un4_simd4_fixed_q",
    "gemm_p64_q1_um8_un4_simd4_adaptive_half",
    "gemm_p64_q1_um8_un4_simd4_equal_split",
    "gemm_p64_q1_um8_un4_simd8_single_block",
    "gemm_p64_q1_um8_un4_simd8_fixed_q",
    "gemm_p64_q1_um8_un4_simd8_adaptive_half",
    "gemm_p64_q1_um8_un4_simd8_equal_split",
    "gemm_p64_q1_um16_un1_simd4_single_block",
    "gemm_p64_q1_um16_un1_simd4_fixed_q",
    "gemm_p64_q1_um16_un1_simd4_adaptive_half",
    "gemm_p64_q1_um16_un1_simd4_equal_split",
    "gemm_p64_q1_um16_un1_simd8_single_block",
    "gemm_p64_q1_um16_un1_simd8_fixed_q",
    "gemm_p64_q1_um16_un1_simd8_adaptive_half",
    "gemm_p64_q1_um16_un1_simd8_equal_split",
    "gemm_p64_q1_um16_un1_simd16_single_block",
    "gemm_p64_q1_um16_un1_simd16_fixed_q",
    "gemm_p64_q1_um16_un1_simd16_adaptive_half",
    "gemm_p64_q1_um16_un1_simd16_equal_split",
    "gemm_p64_q1_um16_un4_simd8_single_block",
    "gemm_p64_q1_um16_un4_simd8_fixed_q",
    "gemm_p64_q1_um16_un4_simd8_adaptive_half",
    "gemm_p64_q1_um16_un4_simd8_equal_split",
    "gemm_p64_q1_um16_un4_simd16_single_block",
    "gemm_p64_q1_um16_un4_simd16_fixed_q",
    "gemm_p64_q1_um16_un4_simd16_adaptive_half",
    "gemm_p64_q1_um16_un4_simd16_equal_split",
    "gemm_p64_q1_um32_un1_simd4_single_block",
    "gemm_p64_q1_um32_un1_simd4_fixed_q",
    "gemm_p64_q1_um32_un1_simd4_adaptive_half",
    "gemm_p64_q1_um32_un1_simd4_equal_split",
    "gemm_p64_q1_um32_un1_simd8_single_block",
    "gemm_p64_q1_um32_un1_simd8_fixed_q",
    "gemm_p64_q1_um32_un1_simd8_adaptive_half",
    "gemm_p64_q1_um32_un1_simd8_equal_split",
    "gemm_p64_q1_um32_un1_simd16_single_block",
    "gemm_p64_q1_um32_un1_simd16_fixed_q",
    "gemm_p64_q1_um32_un1_simd16_adaptive_half",
    "gemm_p64_q1_um32_un1_simd16_equal_split",
    "gemm_p64_q1_um32_un4_simd16_single_block",
    "gemm_p64_q1_um32_un4_simd16_fixed_q",
    "gemm_p64_q1_um32_un4_simd16_adaptive_half",
    "gemm_p64_q1_um32_un4_simd16_equal_split",
    "gemm_p64_q64_um1_un1_simd1_single_block",
    "gemm_p64_q64_um1_un1_simd1_fixed_q",
    "gemm_p64_q64_um1_un1_simd1_adaptive_half",
    "gemm_p64_q64_um1_un1_simd1_equal_split",
    "gemm_p64_q64_um1_un4_simd1_single_block",
    "gemm_p64_q64_um1_un4_simd1_fixed_q",
    "gemm_p64_q64_um1_un4_simd1_adaptive_half",
    "gemm_p64_q64_um1_un4_simd1_equal_split",
    "gemm_p64_q64_um4_un1_simd1_single_block",
    "gemm_p64_q64_um4_un1_simd1_fixed_q",
    "gemm_p64_q64_um4_un1_simd1_adaptive_half",
    "gemm_p64_q64_um4_un1_simd1_equal_split",
    "gemm_p64_q64_um4_un1_simd4_single_block",
    "gemm_p64_q64_um4_un1_simd4_fixed_q",
    "gemm_p64_q64_um4_un1_simd4_adaptive_half",
    "gemm_p64_q64_um4_un1_simd4_equal_split",
    "gemm_p64_q64_um4_un4_simd4_single_block",
    "gemm_p64_q64_um4_un4_simd4_fixed_q",
    "gemm_p64_q64_um4_un4_simd4_adaptive_half",
    "gemm_p64_q64_um4_un4_simd4_equal_split",
    "gemm_p64_q64_um8_un1_simd1_single_block",
    "gemm_p64_q64_um8_un1_simd1_fixed_q",
    "gemm_p64_q64_um8_un1_simd1_adaptive_half",
    "gemm_p64_q64_um8_un1_simd1_equal_split",
    "gemm_p64_q64_um8_un1_simd4_single_block",
    "gemm_p64_q64_um8_un1_simd4_fixed_q",
    "gemm_p64_q64_um8_un1_simd4_adaptive_half",
    "gemm_p64_q64_um8_un1_simd4_equal_split",
    "gemm_p64_q64_um8_un1_simd8_single_block",
    "gemm_p64_q64_um8_un1_simd8_fixed_q",
    "gemm_p64_q64_um8_un1_simd8_adaptive_half",
    "gemm_p64_q64_um8_un1_simd8_equal_split",
    "gemm_p64_q64_um8_un4_simd4_single_block",
    "gemm_p64_q64_um8_un4_simd4_fixed_q",
    "gemm_p64_q64_um8_un4_simd4_adaptive_half",
    "gemm_p64_q64_um8_un4_simd4_equal_split",
    "gemm_p64_q64_um8_un4_simd8_single_block",
    "gemm_p64_q64_um8_un4_simd8_fixed_q",
    "gemm_p64_q64_um8_un4_simd8_adaptive_half",
    "gemm_p64_q64_um8_un4_simd8_equal_split",
    "gemm_p64_q64_um16_un1_simd4_single_block",
    "gemm_p64_q64_um16_un1_simd4_fixed_q",
    "gemm_p64_q64_um16_un1_simd4_adaptive_half",
    "gemm_p64_q64_um16_un1_simd4_equal_split",
    "gemm_p64_q64_um16_un1_simd8_single_block",
    "gemm_p64_q64_um16_un1_simd8_fixed_q",
    "gemm_p64_q64_um16_un1_simd8_adaptive_half",
    "gemm_p64_q64_um16_un1_simd8_equal_split",
    "gemm_p64_q64_um16_un1_simd16_single_block",
    "gemm_p64_q64_um16_un1_simd16_fixed_q",
    "gemm_p64_q64_um16_un1_simd16_adaptive_half",
    "gemm_p64_q64_um16_un1_simd16_equal_split",
    "gemm_p64_q64_um16_un4_simd8_single_block",
    "gemm_p64_q64_um16_un4_simd8_fixed_q",
    "gemm_p64_q64_um16_un4_simd8_adaptive_half",
    "gemm_p64_q64_um16_un4_simd8_equal_split",
    "gemm_p64_q64_um16_un4_simd16_single_block",
    "gemm_p64_q64_um16_un4_simd16_fixed_q",
    "gemm_p64_q64_um16_un4_simd16_adaptive_half",
    "gemm_p64_q64_um16_un4_simd16_equal_split",
    "gemm_p64_q64_um32_un1_simd4_single_block",
    "gemm_p64_q64_um32_un1_simd4_fixed_q",
    "gemm_p64_q64_um32_un1_simd4_adaptive_half",
    "gemm_p64_q64_um32_un1_simd4_equal_split",
    "gemm_p64_q64_um32_un1_simd8_single_block",
    "gemm_p64_q64_um32_un1_simd8_fixed_q",
    "gemm_p64_q64_um32_un1_simd8_adaptive_half",
    "gemm_p64_q64_um32_un1_simd8_equal_split",
    "gemm_p64_q64_um32_un1_simd16_single_block",
    "gemm_p64_q64_um32_un1_simd16_fixed_q",
    "gemm_p64_q64_um32_un1_simd16_adaptive_half",
    "gemm_p64_q64_um32_un1_simd16_equal_split",
    "gemm_p64_q64_um32_un4_simd16_single_block",
    "gemm_p64_q64_um32_un4_simd16_fixed_q",
    "gemm_p64_q64_um32_un4_simd16_adaptive_half",
    "gemm_p64_q64_um32_un4_simd16_equal_split",
    "gemm_p64_q128_um1_un1_simd1_single_block",
    "gemm_p64_q128_um1_un1_simd1_fixed_q",
    "gemm_p64_q128_um1_un1_simd1_adaptive_half",
    "gemm_p64_q128_um1_un1_simd1_equal_split",
    "gemm_p64_q128_um1_un4_simd1_single_block",
    "gemm_p64_q128_um1_un4_simd1_fixed_q",
    "gemm_p64_q128_um1_un4_simd1_adaptive_half",
    "gemm_p64_q128_um1_un4_simd1_equal_split",
    "gemm_p64_q128_um4_un1_simd1_single_block",
    "gemm_p64_q128_um4_un1_simd1_fixed_q",
    "gemm_p64_q128_um4_un1_simd1_adaptive_half",
    "gemm_p64_q128_um4_un1_simd1_equal_split",
    "gemm_p64_q128_um4_un1_simd4_single_block",
    "gemm_p64_q128_um4_un1_simd4_fixed_q",
    "gemm_p64_q128_um4_un1_simd4_adaptive_half",
    "gemm_p64_q128_um4_un1_simd4_equal_split",
    "gemm_p64_q128_um4_un4_simd4_single_block",
    "gemm_p64_q128_um4_un4_simd4_fixed_q",
    "gemm_p64_q128_um4_un4_simd4_adaptive_half",
    "gemm_p64_q128_um4_un4_simd4_equal_split",
    "gemm_p64_q128_um8_un1_simd1_single_block",
    "gemm_p64_q128_um8_un1_simd1_fixed_q",
    "gemm_p64_q128_um8_un1_simd1_adaptive_half",
    "gemm_p64_q128_um8_un1_simd1_equal_split",
    "gemm_p64_q128_um8_un1_simd4_single_block",
    "gemm_p64_q128_um8_un1_simd4_fixed_q",
    "gemm_p64_q128_um8_un1_simd4_adaptive_half",
    "gemm_p64_q128_um8_un1_simd4_equal_split",
    "gemm_p64_q128_um8_un1_simd8_single_block",
    "gemm_p64_q128_um8_un1_simd8_fixed_q",
    "gemm_p64_q128_um8_un1_simd8_adaptive_half",
    "gemm_p64_q128_um8_un1_simd8_equal_split",
    "gemm_p64_q128_um8_un4_simd4_single_block",
    "gemm_p64_q128_um8_un4_simd4_fixed_q",
    "gemm_p64_q128_um8_un4_simd4_adaptive_half",
    "gemm_p64_q128_um8_un4_simd4_equal_split",
    "gemm_p64_q128_um8_un4_simd8_single_block",
    "gemm_p64_q128_um8_un4_simd8_fixed_q",
    "gemm_p64_q128_um8_un4_simd8_adaptive_half",
    "gemm_p64_q128_um8_un4_simd8_equal_split",
    "gemm_p64_q128_um16_un1_simd4_single_block",
    "gemm_p64_q128_um16_un1_simd4_fixed_q",
    "gemm_p64_q128_um16_un1_simd4_adaptive_half",
    "gemm_p64_q128_um16_un1_simd4_equal_split",
    "gemm_p64_q128_um16_un1_simd8_single_block",
    "gemm_p64_q128_um16_un1_simd8_fixed_q",
    "gemm_p64_q128_um16_un1_simd8_adaptive_half",
    "gemm_p64_q128_um16_un1_simd8_equal_split",
    "gemm_p64_q128_um16_un1_simd16_single_block",
    "gemm_p64_q128_um16_un1_simd16_fixed_q",
    "gemm_p64_q128_um16_un1_simd16_adaptive_half",
    "gemm_p64_q128_um16_un1_simd16_equal_split",
    "gemm_p64_q128_um16_un4_simd8_single_block",
    "gemm_p64_q128_um16_un4_simd8_fixed_q",
    "gemm_p64_q128_um16_un4_simd8_adaptive_half",
    "gemm_p64_q128_um16_un4_simd8_equal_split",
    "gemm_p64_q128_um16_un4_simd16_single_block",
    "gemm_p64_q128_um16_un4_simd16_fixed_q",
    "gemm_p64_q128_um16_un4_simd16_adaptive_half",
    "gemm_p64_q128_um16_un4_simd16_equal_split",
    "gemm_p64_q128_um32_un1_simd4_single_block",
    "gemm_p64_q128_um32_un1_simd4_fixed_q",
    "gemm_p64_q128_um32_un1_simd4_adaptive_half",
    "gemm_p64_q128_um32_un1_simd4_equal_split",
    "gemm_p64_q128_um32_un1_simd8_single_block",
    "gemm_p64_q128_um32_un1_simd8_fixed_q",
    "gemm_p64_q128_um32_un1_simd8_adaptive_half",
    "gemm_p64_q128_um32_un1_simd8_equal_split",
    "gemm_p64_q128_um32_un1_simd16_single_block",
    "gemm_p64_q128_um32_un1_simd16_fixed_q",
    "gemm_p64_q128_um32_un1_simd16_adaptive_half",
    "gemm_p64_q128_um32_un1_simd16_equal_split",
    "gemm_p64_q128_um32_un4_simd16_single_block",
    "gemm_p64_q128_um32_un4_simd16_fixed_q",
    "gemm_p64_q128_um32_un4_simd16_adaptive_half",
    "gemm_p64_q128_um32_un4_simd16_equal_split",
    "gemm_p64_q192_um1_un1_simd1_single_block",
    "gemm_p64_q192_um1_un1_simd1_fixed_q",
    "gemm_p64_q192_um1_un1_simd1_adaptive_half",
    "gemm_p64_q192_um1_un1_simd1_equal_split",
    "gemm_p64_q192_um1_un4_simd1_single_block",
    "gemm_p64_q192_um1_un4_simd1_fixed_q",
    "gemm_p64_q192_um1_un4_simd1_adaptive_half",
    "gemm_p64_q192_um1_un4_simd1_equal_split",
    "gemm_p64_q192_um4_un1_simd1_single_block",
    "gemm_p64_q192_um4_un1_simd1_fixed_q",
    "gemm_p64_q192_um4_un1_simd1_adaptive_half",
    "gemm_p64_q192_um4_un1_simd1_equal_split",
    "gemm_p64_q192_um4_un1_simd4_single_block",
    "gemm_p64_q192_um4_un1_simd4_fixed_q",
    "gemm_p64_q192_um4_un1_simd4_adaptive_half",
    "gemm_p64_q192_um4_un1_simd4_equal_split",
    "gemm_p64_q192_um4_un4_simd4_single_block",
    "gemm_p64_q192_um4_un4_simd4_fixed_q",
    "gemm_p64_q192_um4_un4_simd4_adaptive_half",
    "gemm_p64_q192_um4_un4_simd4_equal_split",
    "gemm_p64_q192_um8_un1_simd1_single_block",
    "gemm_p64_q192_um8_un1_simd1_fixed_q",
    "gemm_p64_q192_um8_un1_simd1_adaptive_half",
    "gemm_p64_q192_um8_un1_simd1_equal_split",
    "gemm_p64_q192_um8_un1_simd4_single_block",
    "gemm_p64_q192_um8_un1_simd4_fixed_q",
    "gemm_p64_q192_um8_un1_simd4_adaptive_half",
    "gemm_p64_q192_um8_un1_simd4_equal_split",
    "gemm_p64_q192_um8_un1_simd8_single_block",
    "gemm_p64_q192_um8_un1_simd8_fixed_q",
    "gemm_p64_q192_um8_un1_simd8_adaptive_half",
    "gemm_p64_q192_um8_un1_simd8_equal_split",
    "gemm_p64_q192_um8_un4_simd4_single_block",
    "gemm_p64_q192_um8_un4_simd4_fixed_q",
    "gemm_p64_q192_um8_un4_simd4_adaptive_half",
    "gemm_p64_q192_um8_un4_simd4_equal_split",
    "gemm_p64_q192_um8_un4_simd8_single_block",
    "gemm_p64_q192_um8_un4_simd8_fixed_q",
    "gemm_p64_q192_um8_un4_simd8_adaptive_half",
    "gemm_p64_q192_um8_un4_simd8_equal_split",
    "gemm_p64_q192_um16_un1_simd4_single_block",
    "gemm_p64_q192_um16_un1_simd4_fixed_q",
    "gemm_p64_q192_um16_un1_simd4_adaptive_half",
    "gemm_p64_q192_um16_un1_simd4_equal_split",
    "gemm_p64_q192_um16_un1_simd8_single_block",
    "gemm_p64_q192_um16_un1_simd8_fixed_q",
    "gemm_p64_q192_um16_un1_simd8_adaptive_half",
    "gemm_p64_q192_um16_un1_simd8_equal_split",
    "gemm_p64_q192_um16_un1_simd16_single_block",
    "gemm_p64_q192_um16_un1_simd16_fixed_q",
    "gemm_p64_q192_um16_un1_simd16_adaptive_half",
    "gemm_p64_q192_um16_un1_simd16_equal_split",
    "gemm_p64_q192_um16_un4_simd8_single_block",
    "gemm_p64_q192_um16_un4_simd8_fixed_q",
    "gemm_p64_q192_um16_un4_simd8_adaptive_half",
    "gemm_p64_q192_um16_un4_simd8_equal_split",
    "gemm_p64_q192_um16_un4_simd16_single_block",
    "gemm_p64_q192_um16_un4_simd16_fixed_q",
    "gemm_p64_q192_um16_un4_simd16_adaptive_half",
    "gemm_p64_q192_um16_un4_simd16_equal_split",
    "gemm_p64_q192_um32_un1_simd4_single_block",
    "gemm_p64_q192_um32_un1_simd4_fixed_q",
    "gemm_p64_q192_um32_un1_simd4_adaptive_half",
    "gemm_p64_q192_um32_un1_simd4_equal_split",
    "gemm_p64_q192_um32_un1_simd8_single_block",
    "gemm_p64_q192_um32_un1_simd8_fixed_q",
    "gemm_p64_q192_um32_un1_simd8_adaptive_half",
    "gemm_p64_q192_um32_un1_simd8_equal_split",
    "gemm_p64_q192_um32_un1_simd16_single_block",
    "gemm_p64_q192_um32_un1_simd16_fixed_q",
    "gemm_p64_q192_um32_un1_simd16_adaptive_half",
    "gemm_p64_q192_um32_un1_simd16_equal_split",
    "gemm_p64_q192_um32_un4_simd16_single_block",
    "gemm_p64_q192_um32_un4_simd16_fixed_q",
    "gemm_p64_q192_um32_un4_simd16_adaptive_half",
    "gemm_p64_q192_um32_un4_simd16_equal_split",
    "gemm_p64_q240_um1_un1_simd1_single_block",
    "gemm_p64_q240_um1_un1_simd1_fixed_q",
    "gemm_p64_q240_um1_un1_simd1_adaptive_half",
    "gemm_p64_q240_um1_un1_simd1_equal_split",
    "gemm_p64_q240_um1_un4_simd1_single_block",
    "gemm_p64_q240_um1_un4_simd1_fixed_q",
    "gemm_p64_q240_um1_un4_simd1_adaptive_half",
    "gemm_p64_q240_um1_un4_simd1_equal_split",
    "gemm_p64_q240_um4_un1_simd1_single_block",
    "gemm_p64_q240_um4_un1_simd1_fixed_q",
    "gemm_p64_q240_um4_un1_simd1_adaptive_half",
    "gemm_p64_q240_um4_un1_simd1_equal_split",
    "gemm_p64_q240_um4_un1_simd4_single_block",
    "gemm_p64_q240_um4_un1_simd4_fixed_q",
    "gemm_p64_q240_um4_un1_simd4_adaptive_half",
    "gemm_p64_q240_um4_un1_simd4_equal_split",
    "gemm_p64_q240_um4_un4_simd4_single_block",
    "gemm_p64_q240_um4_un4_simd4_fixed_q",
    "gemm_p64_q240_um4_un4_simd4_adaptive_half",
    "gemm_p64_q240_um4_un4_simd4_equal_split",
    "gemm_p64_q240_um8_un1_simd1_single_block",
    "gemm_p64_q240_um8_un1_simd1_fixed_q",
    "gemm_p64_q240_um8_un1_simd1_adaptive_half",
    "gemm_p64_q240_um8_un1_simd1_equal_split",
    "gemm_p64_q240_um8_un1_simd4_single_block",
    "gemm_p64_q240_um8_un1_simd4_fixed_q",
    "gemm_p64_q240_um8_un1_simd4_adaptive_half",
    "gemm_p64_q240_um8_un1_simd4_equal_split",
    "gemm_p64_q240_um8_un1_simd8_single_block",
    "gemm_p64_q240_um8_un1_simd8_fixed_q",
    "gemm_p64_q240_um8_un1_simd8_adaptive_half",
    "gemm_p64_q240_um8_un1_simd8_equal_split",
    "gemm_p64_q240_um8_un4_simd4_single_block",
    "gemm_p64_q240_um8_un4_simd4_fixed_q",
    "gemm_p64_q240_um8_un4_simd4_adaptive_half",
    "gemm_p64_q240_um8_un4_simd4_equal_split",
    "gemm_p64_q240_um8_un4_simd8_single_block",
    "gemm_p64_q240_um8_un4_simd8_fixed_q",
    "gemm_p64_q240_um8_un4_simd8_adaptive_half",
    "gemm_p64_q240_um8_un4_simd8_equal_split",
    "gemm_p64_q240_um16_un1_simd4_single_block",
    "gemm_p64_q240_um16_un1_simd4_fixed_q",
    "gemm_p64_q240_um16_un1_simd4_adaptive_half",
    "gemm_p64_q240_um16_un1_simd4_equal_split",
    "gemm_p64_q240_um16_un1_simd8_single_block",
    "gemm_p64_q240_um16_un1_simd8_fixed_q",
    "gemm_p64_q240_um16_un1_simd8_adaptive_half",
    "gemm_p64_q240_um16_un1_simd8_equal_split",
    "gemm_p64_q240_um16_un1_simd16_single_block",
    "gemm_p64_q240_um16_un1_simd16_fixed_q",
    "gemm_p64_q240_um16_un1_simd16_adaptive_half",
    "gemm_p64_q240_um16_un1_simd16_equal_split",
    "gemm_p64_q240_um16_un4_simd8_single_block",
    "gemm_p64_q240_um16_un4_simd8_fixed_q",
    "gemm_p64_q240_um16_un4_simd8_adaptive_half",
    "gemm_p64_q240_um16_un4_simd8_equal_split",
    "gemm_p64_q240_um16_un4_simd16_single_block",
    "gemm_p64_q240_um16_un4_simd16_fixed_q",
    "gemm_p64_q240_um16_un4_simd16_adaptive_half",
    "gemm_p64_q240_um16_un4_simd16_equal_split",
    "gemm_p64_q240_um32_un1_simd4_single_block",
    "gemm_p64_q240_um32_un1_simd4_fixed_q",
    "gemm_p64_q240_um32_un1_simd4_adaptive_half",
    "gemm_p64_q240_um32_un1_simd4_equal_split",
    "gemm_p64_q240_um32_un1_simd8_single_block",
    "gemm_p64_q240_um32_un1_simd8_fixed_q",
    "gemm_p64_q240_um32_un1_simd8_adaptive_half",
    "gemm_p64_q240_um32_un1_simd8_equal_split",
    "gemm_p64_q240_um32_un1_simd16_single_block",
    "gemm_p64_q240_um32_un1_simd16_fixed_q",
    "gemm_p64_q240_um32_un1_simd16_adaptive_half",
    "gemm_p64_q240_um32_un1_simd16_equal_split",
    "gemm_p64_q248_um1_un1_simd1_single_block",
    "gemm_p64_q248_um1_un1_simd1_fixed_q",
    "gemm_p64_q248_um1_un1_simd1_adaptive_half",
    "gemm_p64_q248_um1_un1_simd1_equal_split",
    "gemm_p64_q248_um1_un4_simd1_single_block",
    "gemm_p64_q248_um1_un4_simd1_fixed_q",
    "gemm_p64_q248_um1_un4_simd1_adaptive_half",
    "gemm_p64_q248_um1_un4_simd1_equal_split",
    "gemm_p64_q248_um4_un1_simd1_single_block",
    "gemm_p64_q248_um4_un1_simd1_fixed_q",
    "gemm_p64_q248_um4_un1_simd1_adaptive_half",
    "gemm_p64_q248_um4_un1_simd1_equal_split",
    "gemm_p64_q248_um4_un1_simd4_single_block",
    "gemm_p64_q248_um4_un1_simd4_fixed_q",
    "gemm_p64_q248_um4_un1_simd4_adaptive_half",
    "gemm_p64_q248_um4_un1_simd4_equal_split",
    "gemm_p64_q248_um4_un4_simd4_single_block",
    "gemm_p64_q248_um4_un4_simd4_fixed_q",
    "gemm_p64_q248_um4_un4_simd4_adaptive_half",
    "gemm_p64_q248_um4_un4_simd4_equal_split",
    "gemm_p64_q248_um8_un1_simd1_single_block",
    "gemm_p64_q248_um8_un1_simd1_fixed_q",
    "gemm_p64_q248_um8_un1_simd1_adaptive_half",
    "gemm_p64_q248_um8_un1_simd1_equal_split",
    "gemm_p64_q248_um8_un1_simd4_single_block",
    "gemm_p64_q248_um8_un1_simd4_fixed_q",
    "gemm_p64_q248_um8_un1_simd4_adaptive_half",
    "gemm_p64_q248_um8_un1_simd4_equal_split",
    "gemm_p64_q248_um8_un1_simd8_single_block",
    "gemm_p64_q248_um8_un1_simd8_fixed_q",
    "gemm_p64_q248_um8_un1_simd8_adaptive_half",
    "gemm_p64_q248_um8_un1_simd8_equal_split",
    "gemm_p64_q248_um8_un4_simd4_single_block",
    "gemm_p64_q248_um8_un4_simd4_fixed_q",
    "gemm_p64_q248_um8_un4_simd4_adaptive_half",
    "gemm_p64_q248_um8_un4_simd4_equal_split",
    "gemm_p64_q248_um8_un4_simd8_single_block",
    "gemm_p64_q248_um8_un4_simd8_fixed_q",
    "gemm_p64_q248_um8_un4_simd8_adaptive_half",
    "gemm_p64_q248_um8_un4_simd8_equal_split",
    "gemm_p64_q248_um16_un1_simd4_single_block",
    "gemm_p64_q248_um16_un1_simd4_fixed_q",
    "gemm_p64_q248_um16_un1_simd4_adaptive_half",
    "gemm_p64_q248_um16_un1_simd4_equal_split",
    "gemm_p64_q248_um16_un1_simd8_single_block",
    "gemm_p64_q248_um16_un1_simd8_fixed_q",
    "gemm_p64_q248_um16_un1_simd8_adaptive_half",
    "gemm_p64_q248_um16_un1_simd8_equal_split",
    "gemm_p64_q248_um16_un1_simd16_single_block",
    "gemm_p64_q248_um16_un1_simd16_fixed_q",
    "gemm_p64_q248_um16_un1_simd16_adaptive_half",
    "gemm_p64_q248_um16_un1_simd16_equal_split",
    "gemm_p64_q248_um16_un4_simd8_single_block",
    "gemm_p64_q248_um16_un4_simd8_fixed_q",
    "gemm_p64_q248_um16_un4_simd8_adaptive_half",
    "gemm_p64_q248_um16_un4_simd8_equal_split",
    "gemm_p64_q248_um16_un4_simd16_single_block",
    "gemm_p64_q248_um16_un4_simd16_fixed_q",
    "gemm_p64_q248_um16_un4_simd16_adaptive_half",
    "gemm_p64_q248_um16_un4_simd16_equal_split",
    "gemm_p64_q248_um32_un1_simd4_single_block",
    "gemm_p64_q248_um32_un1_simd4_fixed_q",
    "gemm_p64_q248_um32_un1_simd4_adaptive_half",
    "gemm_p64_q248_um32_un1_simd4_equal_split",
    "gemm_p64_q248_um32_un1_simd8_single_block",
    "gemm_p64_q248_um32_un1_simd8_fixed_q",
    "gemm_p64_q248_um32_un1_simd8_adaptive_half",
    "gemm_p64_q248_um32_un1_simd8_equal_split",
    "gemm_p64_q248_um32_un1_simd16_single_block",
    "gemm_p64_q248_um32_un1_simd16_fixed_q",
    "gemm_p64_q248_um32_un1_simd16_adaptive_half",
    "gemm_p64_q248_um32_un1_simd16_equal_split",
    "gemm_p64_q256_um1_un1_simd1_single_block",
    "gemm_p64_q256_um1_un1_simd1_fixed_q",
    "gemm_p64_q256_um1_un1_simd1_adaptive_half",
    "gemm_p64_q256_um1_un1_simd1_equal_split",
    "gemm_p64_q256_um1_un4_simd1_single_block",
    "gemm_p64_q256_um1_un4_simd1_fixed_q",
    "gemm_p64_q256_um1_un4_simd1_adaptive_half",
    "gemm_p64_q256_um1_un4_simd1_equal_split",
    "gemm_p64_q256_um4_un1_simd1_single_block",
    "gemm_p64_q256_um4_un1_simd1_fixed_q",
    "gemm_p64_q256_um4_un1_simd1_adaptive_half",
    "gemm_p64_q256_um4_un1_simd1_equal_split",
    "gemm_p64_q256_um4_un1_simd4_single_block",
    "gemm_p64_q256_um4_un1_simd4_fixed_q",
    "gemm_p64_q256_um4_un1_simd4_adaptive_half",
    "gemm_p64_q256_um4_un1_simd4_equal_split",
    "gemm_p64_q256_um4_un4_simd4_single_block",
    "gemm_p64_q256_um4_un4_simd4_fixed_q",
    "gemm_p64_q256_um4_un4_simd4_adaptive_half",
    "gemm_p64_q256_um4_un4_simd4_equal_split",
    "gemm_p64_q256_um8_un1_simd1_single_block",
    "gemm_p64_q256_um8_un1_simd1_fixed_q",
    "gemm_p64_q256_um8_un1_simd1_adaptive_half",
    "gemm_p64_q256_um8_un1_simd1_equal_split",
    "gemm_p64_q256_um8_un1_simd4_single_block",
    "gemm_p64_q256_um8_un1_simd4_fixed_q",
    "gemm_p64_q256_um8_un1_simd4_adaptive_half",
    "gemm_p64_q256_um8_un1_simd4_equal_split",
    "gemm_p64_q256_um8_un1_simd8_single_block",
    "gemm_p64_q256_um8_un1_simd8_fixed_q",
    "gemm_p64_q256_um8_un1_simd8_adaptive_half",
    "gemm_p64_q256_um8_un1_simd8_equal_split",
    "gemm_p64_q256_um8_un4_simd4_single_block",
    "gemm_p64_q256_um8_un4_simd4_fixed_q",
    "gemm_p64_q256_um8_un4_simd4_adaptive_half",
    "gemm_p64_q256_um8_un4_simd4_equal_split",
    "gemm_p64_q256_um8_un4_simd8_single_block",
    "gemm_p64_q256_um8_un4_simd8_fixed_q",
    "gemm_p64_q256_um8_un4_simd8_adaptive_half",
    "gemm_p64_q256_um8_un4_simd8_equal_split",
    "gemm_p64_q256_um16_un1_simd4_single_block",
    "gemm_p64_q256_um16_un1_simd4_fixed_q",
    "gemm_p64_q256_um16_un1_simd4_adaptive_half",
    "gemm_p64_q256_um16_un1_simd4_equal_split",
    "gemm_p64_q256_um16_un1_simd8_single_block",
    "gemm_p64_q256_um16_un1_simd8_fixed_q",
    "gemm_p64_q256_um16_un1_simd8_adaptive_half",
    "gemm_p64_q256_um16_un1_simd8_equal_split",
    "gemm_p64_q256_um16_un1_simd16_single_block",
    "gemm_p64_q256_um16_un1_simd16_fixed_q",
    "gemm_p64_q256_um16_un1_simd16_adaptive_half",
    "gemm_p64_q256_um16_un1_simd16_equal_split",
    "gemm_p64_q256_um16_un4_simd8_single_block",
    "gemm_p64_q256_um16_un4_simd8_fixed_q",
    "gemm_p64_q256_um16_un4_simd8_adaptive_half",
    "gemm_p64_q256_um16_un4_simd8_equal_split",
    "gemm_p64_q256_um16_un4_simd16_single_block",
    "gemm_p64_q256_um16_un4_simd16_fixed_q",
    "gemm_p64_q256_um16_un4_simd16_adaptive_half",
    "gemm_p64_q256_um16_un4_simd16_equal_split",
    "gemm_p64_q384_um1_un1_simd1_single_block",
    "gemm_p64_q384_um1_un1_simd1_fixed_q",
    "gemm_p64_q384_um1_un1_simd1_adaptive_half",
    "gemm_p64_q384_um1_un1_simd1_equal_split",
    "gemm_p64_q384_um1_un4_simd1_single_block",
    "gemm_p64_q384_um1_un4_simd1_fixed_q",
    "gemm_p64_q384_um1_un4_simd1_adaptive_half",
    "gemm_p64_q384_um1_un4_simd1_equal_split",
    "gemm_p64_q384_um4_un1_simd1_single_block",
    "gemm_p64_q384_um4_un1_simd1_fixed_q",
    "gemm_p64_q384_um4_un1_simd1_adaptive_half",
    "gemm_p64_q384_um4_un1_simd1_equal_split",
    "gemm_p64_q384_um4_un1_simd4_single_block",
    "gemm_p64_q384_um4_un1_simd4_fixed_q",
    "gemm_p64_q384_um4_un1_simd4_adaptive_half",
    "gemm_p64_q384_um4_un1_simd4_equal_split",
    "gemm_p64_q384_um4_un4_simd4_single_block",
    "gemm_p64_q384_um4_un4_simd4_fixed_q",
    "gemm_p64_q384_um4_un4_simd4_adaptive_half",
    "gemm_p64_q384_um4_un4_simd4_equal_split",
    "gemm_p64_q384_um8_un1_simd1_single_block",
    "gemm_p64_q384_um8_un1_simd1_fixed_q",
    "gemm_p64_q384_um8_un1_simd1_adaptive_half",
    "gemm_p64_q384_um8_un1_simd1_equal_split",
    "gemm_p64_q384_um8_un1_simd4_single_block",
    "gemm_p64_q384_um8_un1_simd4_fixed_q",
    "gemm_p64_q384_um8_un1_simd4_adaptive_half",
    "gemm_p64_q384_um8_un1_simd4_equal_split",
    "gemm_p64_q384_um8_un1_simd8_single_block",
    "gemm_p64_q384_um8_un1_simd8_fixed_q",
    "gemm_p64_q384_um8_un1_simd8_adaptive_half",
    "gemm_p64_q384_um8_un1_simd8_equal_split",
    "gemm_p64_q384_um8_un4_simd4_single_block",
    "gemm_p64_q384_um8_un4_simd4_fixed_q",
    "gemm_p64_q384_um8_un4_simd4_adaptive_half",
    "gemm_p64_q384_um8_un4_simd4_equal_split",
    "gemm_p64_q384_um8_un4_simd8_single_block",
    "gemm_p64_q384_um8_un4_simd8_fixed_q",
    "gemm_p64_q384_um8_un4_simd8_adaptive_half",
    "gemm_p64_q384_um8_un4_simd8_equal_split",
    "gemm_p64_q384_um16_un1_simd4_single_block",
    "gemm_p64_q384_um16_un1_simd4_fixed_q",
    "gemm_p64_q384_um16_un1_simd4_adaptive_half",
    "gemm_p64_q384_um16_un1_simd4_equal_split",
    "gemm_p64_q384_um16_un1_simd8_single_block",
    "gemm_p64_q384_um16_un1_simd8_fixed_q",
    "gemm_p64_q384_um16_un1_simd8_adaptive_half",
    "gemm_p64_q384_um16_un1_simd8_equal_split",
    "gemm_p64_q384_um16_un1_simd16_single_block",
    "gemm_p64_q384_um16_un1_simd16_fixed_q",
    "gemm_p64_q384_um16_un1_simd16_adaptive_half",
    "gemm_p64_q384_um16_un1_simd16_equal_split",
    "gemm_p64_q384_um16_un4_simd8_single_block",
    "gemm_p64_q384_um16_un4_simd8_fixed_q",
    "gemm_p64_q384_um16_un4_simd8_adaptive_half",
    "gemm_p64_q384_um16_un4_simd8_equal_split",
    "gemm_p64_q384_um16_un4_simd16_single_block",
    "gemm_p64_q384_um16_un4_simd16_fixed_q",
    "gemm_p64_q384_um16_un4_simd16_adaptive_half",
    "gemm_p64_q384_um16_un4_simd16_equal_split",
    "gemm_p64_q512_um1_un1_simd1_single_block",
    "gemm_p64_q512_um1_un1_simd1_fixed_q",
    "gemm_p64_q512_um1_un1_simd1_adaptive_half",
    "gemm_p64_q512_um1_un1_simd1_equal_split",
    "gemm_p64_q512_um1_un4_simd1_single_block",
    "gemm_p64_q512_um1_un4_simd1_fixed_q",
    "gemm_p64_q512_um1_un4_simd1_adaptive_half",
    "gemm_p64_q512_um1_un4_simd1_equal_split",
    "gemm_p64_q512_um4_un1_simd1_single_block",
    "gemm_p64_q512_um4_un1_simd1_fixed_q",
    "gemm_p64_q512_um4_un1_simd1_adaptive_half",
    "gemm_p64_q512_um4_un1_simd1_equal_split",
    "gemm_p64_q512_um4_un1_simd4_single_block",
    "gemm_p64_q512_um4_un1_simd4_fixed_q",
    "gemm_p64_q512_um4_un1_simd4_adaptive_half",
    "gemm_p64_q512_um4_un1_simd4_equal_split",
    "gemm_p64_q512_um4_un4_simd4_single_block",
    "gemm_p64_q512_um4_un4_simd4_fixed_q",
    "gemm_p64_q512_um4_un4_simd4_adaptive_half",
    "gemm_p64_q512_um4_un4_simd4_equal_split",
    "gemm_p64_q512_um8_un1_simd1_single_block",
    "gemm_p64_q512_um8_un1_simd1_fixed_q",
    "gemm_p64_q512_um8_un1_simd1_adaptive_half",
    "gemm_p64_q512_um8_un1_simd1_equal_split",
    "gemm_p64_q512_um8_un1_simd4_single_block",
    "gemm_p64_q512_um8_un1_simd4_fixed_q",
    "gemm_p64_q512_um8_un1_simd4_adaptive_half",
    "gemm_p64_q512_um8_un1_simd4_equal_split",
    "gemm_p64_q512_um8_un1_simd8_single_block",
    "gemm_p64_q512_um8_un1_simd8_fixed_q",
    "gemm_p64_q512_um8_un1_simd8_adaptive_half",
    "gemm_p64_q512_um8_un1_simd8_equal_split",
    "gemm_p64_q512_um8_un4_simd4_single_block",
    "gemm_p64_q512_um8_un4_simd4_fixed_q",
    "gemm_p64_q512_um8_un4_simd4_adaptive_half",
    "gemm_p64_q512_um8_un4_simd4_equal_split",
    "gemm_p64_q512_um8_un4_simd8_single_block",
    "gemm_p64_q512_um8_un4_simd8_fixed_q",
    "gemm_p64_q512_um8_un4_simd8_adaptive_half",
    "gemm_p64_q512_um8_un4_simd8_equal_split",
    "gemm_p128_q1_um1_un1_simd1_single_block",
    "gemm_p128_q1_um1_un1_simd1_fixed_q",
    "gemm_p128_q1_um1_un1_simd1_adaptive_half",
    "gemm_p128_q1_um1_un1_simd1_equal_split",
    "gemm_p128_q1_um1_un4_simd1_single_block",
    "gemm_p128_q1_um1_un4_simd1_fixed_q",
    "gemm_p128_q1_um1_un4_simd1_adaptive_half",
    "gemm_p128_q1_um1_un4_simd1_equal_split",
    "gemm_p128_q1_um4_un1_simd1_single_block",
    "gemm_p128_q1_um4_un1_simd1_fixed_q",
    "gemm_p128_q1_um4_un1_simd1_adaptive_half",
    "gemm_p128_q1_um4_un1_simd1_equal_split",
    "gemm_p128_q1_um4_un1_simd4_single_block",
    "gemm_p128_q1_um4_un1_simd4_fixed_q",
    "gemm_p128_q1_um4_un1_simd4_adaptive_half",
    "gemm_p128_q1_um4_un1_simd4_equal_split",
    "gemm_p128_q1_um4_un4_simd4_single_block",
    "gemm_p128_q1_um4_un4_simd4_fixed_q",
    "gemm_p128_q1_um4_un4_simd4_adaptive_half",
    "gemm_p128_q1_um4_un4_simd4_equal_split",
    "gemm_p128_q1_um8_un1_simd1_single_block",
    "gemm_p128_q1_um8_un1_simd1_fixed_q",
    "gemm_p128_q1_um8_un1_simd1_adaptive_half",
    "gemm_p128_q1_um8_un1_simd1_equal_split",
    "gemm_p128_q1_um8_un1_simd4_single_block",
    "gemm_p128_q1_um8_un1_simd4_fixed_q",
    "gemm_p128_q1_um8_un1_simd4_adaptive_half",
    "gemm_p128_q1_um8_un1_simd4_equal_split",
    "gemm_p128_q1_um8_un1_simd8_single_block",
    "gemm_p128_q1_um8_un1_simd8_fixed_q",
    "gemm_p128_q1_um8_un1_simd8_adaptive_half",
    "gemm_p128_q1_um8_un1_simd8_equal_split",
    "gemm_p128_q1_um8_un4_simd4_single_block",
    "gemm_p128_q1_um8_un4_simd4_fixed_q",
    "gemm_p128_q1_um8_un4_simd4_adaptive_half",
    "gemm_p128_q1_um8_un4_simd4_equal_split",
    "gemm_p128_q1_um8_un4_simd8_single_block",
    "gemm_p128_q1_um8_un4_simd8_fixed_q",
    "gemm_p128_q1_um8_un4_simd8_adaptive_half",
    "gemm_p128_q1_um8_un4_simd8_equal_split",
    "gemm_p128_q1_um16_un1_simd4_single_block",
    "gemm_p128_q1_um16_un1_simd4_fixed_q",
    "gemm_p128_q1_um16_un1_simd4_adaptive_half",
    "gemm_p128_q1_um16_un1_simd4_equal_split",
    "gemm_p128_q1_um16_un1_simd8_single_block",
    "gemm_p128_q1_um16_un1_simd8_fixed_q",
    "gemm_p128_q1_um16_un1_simd8_adaptive_half",
    "gemm_p128_q1_um16_un1_simd8_equal_split",
    "gemm_p128_q1_um16_un1_simd16_single_block",
    "gemm_p128_q1_um16_un1_simd16_fixed_q",
    "gemm_p128_q1_um16_un1_simd16_adaptive_half",
    "gemm_p128_q1_um16_un1_simd16_equal_split",
    "gemm_p128_q1_um16_un4_simd8_single_block",
    "gemm_p128_q1_um16_un4_simd8_fixed_q",
    "gemm_p128_q1_um16_un4_simd8_adaptive_half",
    "gemm_p128_q1_um16_un4_simd8_equal_split",
    "gemm_p128_q1_um16_un4_simd16_single_block",
    "gemm_p128_q1_um16_un4_simd16_fixed_q",
    "gemm_p128_q1_um16_un4_simd16_adaptive_half",
    "gemm_p128_q1_um16_un4_simd16_equal_split",
    "gemm_p128_q1_um32_un1_simd4_single_block",
    "gemm_p128_q1_um32_un1_simd4_fixed_q",
    "gemm_p128_q1_um32_un1_simd4_adaptive_half",
    "gemm_p128_q1_um32_un1_simd4_equal_split",
    "gemm_p128_q1_um32_un1_simd8_single_block",
    "gemm_p128_q1_um32_un1_simd8_fixed_q",
    "gemm_p128_q1_um32_un1_simd8_adaptive_half",
    "gemm_p128_q1_um32_un1_simd8_equal_split",
    "gemm_p128_q1_um32_un1_simd16_single_block",
    "gemm_p128_q1_um32_un1_simd16_fixed_q",
    "gemm_p128_q1_um32_un1_simd16_adaptive_half",
    "gemm_p128_q1_um32_un1_simd16_equal_split",
    "gemm_p128_q1_um32_un4_simd16_single_block",
    "gemm_p128_q1_um32_un4_simd16_fixed_q",
    "gemm_p128_q1_um32_un4_simd16_adaptive_half",
    "gemm_p128_q1_um32_un4_simd16_equal_split",
    "gemm_p128_q64_um1_un1_simd1_single_block",
    "gemm_p128_q64_um1_un1_simd1_fixed_q",
    "gemm_p128_q64_um1_un1_simd1_adaptive_half",
    "gemm_p128_q64_um1_un1_simd1_equal_split",
    "gemm_p128_q64_um1_un4_simd1_single_block",
    "gemm_p128_q64_um1_un4_simd1_fixed_q",
    "gemm_p128_q64_um1_un4_simd1_adaptive_half",
    "gemm_p128_q64_um1_un4_simd1_equal_split",
    "gemm_p128_q64_um4_un1_simd1_single_block",
    "gemm_p128_q64_um4_un1_simd1_fixed_q",
    "gemm_p128_q64_um4_un1_simd1_adaptive_half",
    "gemm_p128_q64_um4_un1_simd1_equal_split",
    "gemm_p128_q64_um4_un1_simd4_single_block",
    "gemm_p128_q64_um4_un1_simd4_fixed_q",
    "gemm_p128_q64_um4_un1_simd4_adaptive_half",
    "gemm_p128_q64_um4_un1_simd4_equal_split",
    "gemm_p128_q64_um4_un4_simd4_single_block",
    "gemm_p128_q64_um4_un4_simd4_fixed_q",
    "gemm_p128_q64_um4_un4_simd4_adaptive_half",
    "gemm_p128_q64_um4_un4_simd4_equal_split",
    "gemm_p128_q64_um8_un1_simd1_single_block",
    "gemm_p128_q64_um8_un1_simd1_fixed_q",
    "gemm_p128_q64_um8_un1_simd1_adaptive_half",
    "gemm_p128_q64_um8_un1_simd1_equal_split",
    "gemm_p128_q64_um8_un1_simd4_single_block",
    "gemm_p128_q64_um8_un1_simd4_fixed_q",
    "gemm_p128_q64_um8_un1_simd4_adaptive_half",
    "gemm_p128_q64_um8_un1_simd4_equal_split",
    "gemm_p128_q64_um8_un1_simd8_single_block",
    "gemm_p128_q64_um8_un1_simd8_fixed_q",
    "gemm_p128_q64_um8_un1_simd8_adaptive_half",
    "gemm_p128_q64_um8_un1_simd8_equal_split",
    "gemm_p128_q64_um8_un4_simd4_single_block",
    "gemm_p128_q64_um8_un4_simd4_fixed_q",
    "gemm_p128_q64_um8_un4_simd4_adaptive_half",
    "gemm_p128_q64_um8_un4_simd4_equal_split",
    "gemm_p128_q64_um8_un4_simd8_single_block",
    "gemm_p128_q64_um8_un4_simd8_fixed_q",
    "gemm_p128_q64_um8_un4_simd8_adaptive_half",
    "gemm_p128_q64_um8_un4_simd8_equal_split",
    "gemm_p128_q64_um16_un1_simd4_single_block",
    "gemm_p128_q64_um16_un1_simd4_fixed_q",
    "gemm_p128_q64_um16_un1_simd4_adaptive_half",
    "gemm_p128_q64_um16_un1_simd4_equal_split",
    "gemm_p128_q64_um16_un1_simd8_single_block",
    "gemm_p128_q64_um16_un1_simd8_fixed_q",
    "gemm_p128_q64_um16_un1_simd8_adaptive_half",
    "gemm_p128_q64_um16_un1_simd8_equal_split",
    "gemm_p128_q64_um16_un1_simd16_single_block",
    "gemm_p128_q64_um16_un1_simd16_fixed_q",
    "gemm_p128_q64_um16_un1_simd16_adaptive_half",
    "gemm_p128_q64_um16_un1_simd16_equal_split",
    "gemm_p128_q64_um16_un4_simd8_single_block",
    "gemm_p128_q64_um16_un4_simd8_fixed_q",
    "gemm_p128_q64_um16_un4_simd8_adaptive_half",
    "gemm_p128_q64_um16_un4_simd8_equal_split",
    "gemm_p128_q64_um16_un4_simd16_single_block",
    "gemm_p128_q64_um16_un4_simd16_fixed_q",
    "gemm_p128_q64_um16_un4_simd16_adaptive_half",
    "gemm_p128_q64_um16_un4_simd16_equal_split",
    "gemm_p128_q64_um32_un1_simd4_single_block",
    "gemm_p128_q64_um32_un1_simd4_fixed_q",
    "gemm_p128_q64_um32_un1_simd4_adaptive_half",
    "gemm_p128_q64_um32_un1_simd4_equal_split",
    "gemm_p128_q64_um32_un1_simd8_single_block",
    "gemm_p128_q64_um32_un1_simd8_fixed_q",
    "gemm_p128_q64_um32_un1_simd8_adaptive_half",
    "gemm_p128_q64_um32_un1_simd8_equal_split",
    "gemm_p128_q64_um32_un1_simd16_single_block",
    "gemm_p128_q64_um32_un1_simd16_fixed_q",
    "gemm_p128_q64_um32_un1_simd16_adaptive_half",
    "gemm_p128_q64_um32_un1_simd16_equal_split",
    "gemm_p128_q64_um32_un4_simd16_single_block",
    "gemm_p128_q64_um32_un4_simd16_fixed_q",
    "gemm_p128_q64_um32_un4_simd16_adaptive_half",
    "gemm_p128_q64_um32_un4_simd16_equal_split",
    "gemm_p128_q128_um1_un1_simd1_single_block",
    "gemm_p128_q128_um1_un1_simd1_fixed_q",
    "gemm_p128_q128_um1_un1_simd1_adaptive_half",
    "gemm_p128_q128_um1_un1_simd1_equal_split",
    "gemm_p128_q128_um1_un4_simd1_single_block",
    "gemm_p128_q128_um1_un4_simd1_fixed_q",
    "gemm_p128_q128_um1_un4_simd1_adaptive_half",
    "gemm_p128_q128_um1_un4_simd1_equal_split",
    "gemm_p128_q128_um4_un1_simd1_single_block",
    "gemm_p128_q128_um4_un1_simd1_fixed_q",
    "gemm_p128_q128_um4_un1_simd1_adaptive_half",
    "gemm_p128_q128_um4_un1_simd1_equal_split",
    "gemm_p128_q128_um4_un1_simd4_single_block",
    "gemm_p128_q128_um4_un1_simd4_fixed_q",
    "gemm_p128_q128_um4_un1_simd4_adaptive_half",
    "gemm_p128_q128_um4_un1_simd4_equal_split",
    "gemm_p128_q128_um4_un4_simd4_single_block",
    "gemm_p128_q128_um4_un4_simd4_fixed_q",
    "gemm_p128_q128_um4_un4_simd4_adaptive_half",
    "gemm_p128_q128_um4_un4_simd4_equal_split",
    "gemm_p128_q128_um8_un1_simd1_single_block",
    "gemm_p128_q128_um8_un1_simd1_fixed_q",
    "gemm_p128_q128_um8_un1_simd1_adaptive_half",
    "gemm_p128_q128_um8_un1_simd1_equal_split",
    "gemm_p128_q128_um8_un1_simd4_single_block",
    "gemm_p128_q128_um8_un1_simd4_fixed_q",
    "gemm_p128_q128_um8_un1_simd4_adaptive_half",
    "gemm_p128_q128_um8_un1_simd4_equal_split",
    "gemm_p128_q128_um8_un1_simd8_single_block",
    "gemm_p128_q128_um8_un1_simd8_fixed_q",
    "gemm_p128_q128_um8_un1_simd8_adaptive_half",
    "gemm_p128_q128_um8_un1_simd8_equal_split",
    "gemm_p128_q128_um8_un4_simd4_single_block",
    "gemm_p128_q128_um8_un4_simd4_fixed_q",
    "gemm_p128_q128_um8_un4_simd4_adaptive_half",
    "gemm_p128_q128_um8_un4_simd4_equal_split",
    "gemm_p128_q128_um8_un4_simd8_single_block",
    "gemm_p128_q128_um8_un4_simd8_fixed_q",
    "gemm_p128_q128_um8_un4_simd8_adaptive_half",
    "gemm_p128_q128_um8_un4_simd8_equal_split",
    "gemm_p128_q128_um16_un1_simd4_single_block",
    "gemm_p128_q128_um16_un1_simd4_fixed_q",
    "gemm_p128_q128_um16_un1_simd4_adaptive_half",
    "gemm_p128_q128_um16_un1_simd4_equal_split",
    "gemm_p128_q128_um16_un1_simd8_single_block",
    "gemm_p128_q128_um16_un1_simd8_fixed_q",
    "gemm_p128_q128_um16_un1_simd8_adaptive_half",
    "gemm_p128_q128_um16_un1_simd8_equal_split",
    "gemm_p128_q128_um16_un1_simd16_single_block",
    "gemm_p128_q128_um16_un1_simd16_fixed_q",
    "gemm_p128_q128_um16_un1_simd16_adaptive_half",
    "gemm_p128_q128_um16_un1_simd16_equal_split",
    "gemm_p128_q128_um16_un4_simd8_single_block",
    "gemm_p128_q128_um16_un4_simd8_fixed_q",
    "gemm_p128_q128_um16_un4_simd8_adaptive_half",
    "gemm_p128_q128_um16_un4_simd8_equal_split",
    "gemm_p128_q128_um16_un4_simd16_single_block",
    "gemm_p128_q128_um16_un4_simd16_fixed_q",
    "gemm_p128_q128_um16_un4_simd16_adaptive_half",
    "gemm_p128_q128_um16_un4_simd16_equal_split",
    "gemm_p128_q128_um32_un1_simd4_single_block",
    "gemm_p128_q128_um32_un1_simd4_fixed_q",
    "gemm_p128_q128_um32_un1_simd4_adaptive_half",
    "gemm_p128_q128_um32_un1_simd4_equal_split",
    "gemm_p128_q128_um32_un1_simd8_single_block",
    "gemm_p128_q128_um32_un1_simd8_fixed_q",
    "gemm_p128_q128_um32_un1_simd8_adaptive_half",
    "gemm_p128_q128_um32_un1_simd8_equal_split",
    "gemm_p128_q128_um32_un1_simd16_single_block",
    "gemm_p128_q128_um32_un1_simd16_fixed_q",
    "gemm_p128_q128_um32_un1_simd16_adaptive_half",
    "gemm_p128_q128_um32_un1_simd16_equal_split",
    "gemm_p128_q128_um32_un4_simd16_single_block",
    "gemm_p128_q128_um32_un4_simd16_fixed_q",
    "gemm_p128_q128_um32_un4_simd16_adaptive_half",
    "gemm_p128_q128_um32_un4_simd16_equal_split",
    "gemm_p128_q192_um1_un1_simd1_single_block",
    "gemm_p128_q192_um1_un1_simd1_fixed_q",
    "gemm_p128_q192_um1_un1_simd1_adaptive_half",
    "gemm_p128_q192_um1_un1_simd1_equal_split",
    "gemm_p128_q192_um1_un4_simd1_single_block",
    "gemm_p128_q192_um1_un4_simd1_fixed_q",
    "gemm_p128_q192_um1_un4_simd1_adaptive_half",
    "gemm_p128_q192_um1_un4_simd1_equal_split",
    "gemm_p128_q192_um4_un1_simd1_single_block",
    "gemm_p128_q192_um4_un1_simd1_fixed_q",
    "gemm_p128_q192_um4_un1_simd1_adaptive_half",
    "gemm_p128_q192_um4_un1_simd1_equal_split",
    "gemm_p128_q192_um4_un1_simd4_single_block",
    "gemm_p128_q192_um4_un1_simd4_fixed_q",
    "gemm_p128_q192_um4_un1_simd4_adaptive_half",
    "gemm_p128_q192_um4_un1_simd4_equal_split",
    "gemm_p128_q192_um4_un4_simd4_single_block",
    "gemm_p128_q192_um4_un4_simd4_fixed_q",
    "gemm_p128_q192_um4_un4_simd4_adaptive_half",
    "gemm_p128_q192_um4_un4_simd4_equal_split",
    "gemm_p128_q192_um8_un1_simd1_single_block",
    "gemm_p128_q192_um8_un1_simd1_fixed_q",
    "gemm_p128_q192_um8_un1_simd1_adaptive_half",
    "gemm_p128_q192_um8_un1_simd1_equal_split",
    "gemm_p128_q192_um8_un1_simd4_single_block",
    "gemm_p128_q192_um8_un1_simd4_fixed_q",
    "gemm_p128_q192_um8_un1_simd4_adaptive_half",
    "gemm_p128_q192_um8_un1_simd4_equal_split",
    "gemm_p128_q192_um8_un1_simd8_single_block",
    "gemm_p128_q192_um8_un1_simd8_fixed_q",
    "gemm_p128_q192_um8_un1_simd8_adaptive_half",
    "gemm_p128_q192_um8_un1_simd8_equal_split",
    "gemm_p128_q192_um8_un4_simd4_single_block",
    "gemm_p128_q192_um8_un4_simd4_fixed_q",
    "gemm_p128_q192_um8_un4_simd4_adaptive_half",
    "gemm_p128_q192_um8_un4_simd4_equal_split",
    "gemm_p128_q192_um8_un4_simd8_single_block",
    "gemm_p128_q192_um8_un4_simd8_fixed_q",
    "gemm_p128_q192_um8_un4_simd8_adaptive_half",
    "gemm_p128_q192_um8_un4_simd8_equal_split",
    "gemm_p128_q192_um16_un1_simd4_single_block",
    "gemm_p128_q192_um16_un1_simd4_fixed_q",
    "gemm_p128_q192_um16_un1_simd4_adaptive_half",
    "gemm_p128_q192_um16_un1_simd4_equal_split",
    "gemm_p128_q192_um16_un1_simd8_single_block",
    "gemm_p128_q192_um16_un1_simd8_fixed_q",
    "gemm_p128_q192_um16_un1_simd8_adaptive_half",
    "gemm_p128_q192_um16_un1_simd8_equal_split",
    "gemm_p128_q192_um16_un1_simd16_single_block",
    "gemm_p128_q192_um16_un1_simd16_fixed_q",
    "gemm_p128_q192_um16_un1_simd16_adaptive_half",
    "gemm_p128_q192_um16_un1_simd16_equal_split",
    "gemm_p128_q192_um16_un4_simd8_single_block",
    "gemm_p128_q192_um16_un4_simd8_fixed_q",
    "gemm_p128_q192_um16_un4_simd8_adaptive_half",
    "gemm_p128_q192_um16_un4_simd8_equal_split",
    "gemm_p128_q192_um16_un4_simd16_single_block",
    "gemm_p128_q192_um16_un4_simd16_fixed_q",
    "gemm_p128_q192_um16_un4_simd16_adaptive_half",
    "gemm_p128_q192_um16_un4_simd16_equal_split",
    "gemm_p128_q192_um32_un1_simd4_single_block",
    "gemm_p128_q192_um32_un1_simd4_fixed_q",
    "gemm_p128_q192_um32_un1_simd4_adaptive_half",
    "gemm_p128_q192_um32_un1_simd4_equal_split",
    "gemm_p128_q192_um32_un1_simd8_single_block",
    "gemm_p128_q192_um32_un1_simd8_fixed_q",
    "gemm_p128_q192_um32_un1_simd8_adaptive_half",
    "gemm_p128_q192_um32_un1_simd8_equal_split",
    "gemm_p128_q192_um32_un1_simd16_single_block",
    "gemm_p128_q192_um32_un1_simd16_fixed_q",
    "gemm_p128_q192_um32_un1_simd16_adaptive_half",
    "gemm_p128_q192_um32_un1_simd16_equal_split",
    "gemm_p128_q192_um32_un4_simd16_single_block",
    "gemm_p128_q192_um32_un4_simd16_fixed_q",
    "gemm_p128_q192_um32_un4_simd16_adaptive_half",
    "gemm_p128_q192_um32_un4_simd16_equal_split",
    "gemm_p128_q240_um1_un1_simd1_single_block",
    "gemm_p128_q240_um1_un1_simd1_fixed_q",
    "gemm_p128_q240_um1_un1_simd1_adaptive_half",
    "gemm_p128_q240_um1_un1_simd1_equal_split",
    "gemm_p128_q240_um1_un4_simd1_single_block",
    "gemm_p128_q240_um1_un4_simd1_fixed_q",
    "gemm_p128_q240_um1_un4_simd1_adaptive_half",
    "gemm_p128_q240_um1_un4_simd1_equal_split",
    "gemm_p128_q240_um4_un1_simd1_single_block",
    "gemm_p128_q240_um4_un1_simd1_fixed_q",
    "gemm_p128_q240_um4_un1_simd1_adaptive_half",
    "gemm_p128_q240_um4_un1_simd1_equal_split",
    "gemm_p128_q240_um4_un1_simd4_single_block",
    "gemm_p128_q240_um4_un1_simd4_fixed_q",
    "gemm_p128_q240_um4_un1_simd4_adaptive_half",
    "gemm_p128_q240_um4_un1_simd4_equal_split",
    "gemm_p128_q240_um4_un4_simd4_single_block",
    "gemm_p128_q240_um4_un4_simd4_fixed_q",
    "gemm_p128_q240_um4_un4_simd4_adaptive_half",
    "gemm_p128_q240_um4_un4_simd4_equal_split",
    "gemm_p128_q240_um8_un1_simd1_single_block",
    "gemm_p128_q240_um8_un1_simd1_fixed_q",
    "gemm_p128_q240_um8_un1_simd1_adaptive_half",
    "gemm_p128_q240_um8_un1_simd1_equal_split",
    "gemm_p128_q240_um8_un1_simd4_single_block",
    "gemm_p128_q240_um8_un1_simd4_fixed_q",
    "gemm_p128_q240_um8_un1_simd4_adaptive_half",
    "gemm_p128_q240_um8_un1_simd4_equal_split",
    "gemm_p128_q240_um8_un1_simd8_single_block",
    "gemm_p128_q240_um8_un1_simd8_fixed_q",
    "gemm_p128_q240_um8_un1_simd8_adaptive_half",
    "gemm_p128_q240_um8_un1_simd8_equal_split",
    "gemm_p128_q240_um8_un4_simd4_single_block",
    "gemm_p128_q240_um8_un4_simd4_fixed_q",
    "gemm_p128_q240_um8_un4_simd4_adaptive_half",
    "gemm_p128_q240_um8_un4_simd4_equal_split",
    "gemm_p128_q240_um8_un4_simd8_single_block",
    "gemm_p128_q240_um8_un4_simd8_fixed_q",
    "gemm_p128_q240_um8_un4_simd8_adaptive_half",
    "gemm_p128_q240_um8_un4_simd8_equal_split",
    "gemm_p128_q240_um16_un1_simd4_single_block",
    "gemm_p128_q240_um16_un1_simd4_fixed_q",
    "gemm_p128_q240_um16_un1_simd4_adaptive_half",
    "gemm_p128_q240_um16_un1_simd4_equal_split",
    "gemm_p128_q240_um16_un1_simd8_single_block",
    "gemm_p128_q240_um16_un1_simd8_fixed_q",
    "gemm_p128_q240_um16_un1_simd8_adaptive_half",
    "gemm_p128_q240_um16_un1_simd8_equal_split",
    "gemm_p128_q240_um16_un1_simd16_single_block",
    "gemm_p128_q240_um16_un1_simd16_fixed_q",
    "gemm_p128_q240_um16_un1_simd16_adaptive_half",
    "gemm_p128_q240_um16_un1_simd16_equal_split",
    "gemm_p128_q240_um16_un4_simd8_single_block",
    "gemm_p128_q240_um16_un4_simd8_fixed_q",
    "gemm_p128_q240_um16_un4_simd8_adaptive_half",
    "gemm_p128_q240_um16_un4_simd8_equal_split",
    "gemm_p128_q240_um16_un4_simd16_single_block",
    "gemm_p128_q240_um16_un4_simd16_fixed_q",
    "gemm_p128_q240_um16_un4_simd16_adaptive_half",
    "gemm_p128_q240_um16_un4_simd16_equal_split",
    "gemm_p128_q240_um32_un1_simd4_single_block",
    "gemm_p128_q240_um32_un1_simd4_fixed_q",
    "gemm_p128_q240_um32_un1_simd4_adaptive_half",
    "gemm_p128_q240_um32_un1_simd4_equal_split",
    "gemm_p128_q240_um32_un1_simd8_single_block",
    "gemm_p128_q240_um32_un1_simd8_fixed_q",
    "gemm_p128_q240_um32_un1_simd8_adaptive_half",
    "gemm_p128_q240_um32_un1_simd8_equal_split",
    "gemm_p128_q240_um32_un1_simd16_single_block",
    "gemm_p128_q240_um32_un1_simd16_fixed_q",
    "gemm_p128_q240_um32_un1_simd16_adaptive_half",
    "gemm_p128_q240_um32_un1_simd16_equal_split",
    "gemm_p128_q248_um1_un1_simd1_single_block",
    "gemm_p128_q248_um1_un1_simd1_fixed_q",
    "gemm_p128_q248_um1_un1_simd1_adaptive_half",
    "gemm_p128_q248_um1_un1_simd1_equal_split",
    "gemm_p128_q248_um1_un4_simd1_single_block",
    "gemm_p128_q248_um1_un4_simd1_fixed_q",
    "gemm_p128_q248_um1_un4_simd1_adaptive_half",
    "gemm_p128_q248_um1_un4_simd1_equal_split",
    "gemm_p128_q248_um4_un1_simd1_single_block",
    "gemm_p128_q248_um4_un1_simd1_fixed_q",
    "gemm_p128_q248_um4_un1_simd1_adaptive_half",
    "gemm_p128_q248_um4_un1_simd1_equal_split",
    "gemm_p128_q248_um4_un1_simd4_single_block",
    "gemm_p128_q248_um4_un1_simd4_fixed_q",
    "gemm_p128_q248_um4_un1_simd4_adaptive_half",
    "gemm_p128_q248_um4_un1_simd4_equal_split",
    "gemm_p128_q248_um4_un4_simd4_single_block",
    "gemm_p128_q248_um4_un4_simd4_fixed_q",
    "gemm_p128_q248_um4_un4_simd4_adaptive_half",
    "gemm_p128_q248_um4_un4_simd4_equal_split",
    "gemm_p128_q248_um8_un1_simd1_single_block",
    "gemm_p128_q248_um8_un1_simd1_fixed_q",
    "gemm_p128_q248_um8_un1_simd1_adaptive_half",
    "gemm_p128_q248_um8_un1_simd1_equal_split",
    "gemm_p128_q248_um8_un1_simd4_single_block",
    "gemm_p128_q248_um8_un1_simd4_fixed_q",
    "gemm_p128_q248_um8_un1_simd4_adaptive_half",
    "gemm_p128_q248_um8_un1_simd4_equal_split",
    "gemm_p128_q248_um8_un1_simd8_single_block",
    "gemm_p128_q248_um8_un1_simd8_fixed_q",
    "gemm_p128_q248_um8_un1_simd8_adaptive_half",
    "gemm_p128_q248_um8_un1_simd8_equal_split",
    "gemm_p128_q248_um8_un4_simd4_single_block",
    "gemm_p128_q248_um8_un4_simd4_fixed_q",
    "gemm_p128_q248_um8_un4_simd4_adaptive_half",
    "gemm_p128_q248_um8_un4_simd4_equal_split",
    "gemm_p128_q248_um8_un4_simd8_single_block",
    "gemm_p128_q248_um8_un4_simd8_fixed_q",
    "gemm_p128_q248_um8_un4_simd8_adaptive_half",
    "gemm_p128_q248_um8_un4_simd8_equal_split",
    "gemm_p128_q248_um16_un1_simd4_single_block",
    "gemm_p128_q248_um16_un1_simd4_fixed_q",
    "gemm_p128_q248_um16_un1_simd4_adaptive_half",
    "gemm_p128_q248_um16_un1_simd4_equal_split",
    "gemm_p128_q248_um16_un1_simd8_single_block",
    "gemm_p128_q248_um16_un1_simd8_fixed_q",
    "gemm_p128_q248_um16_un1_simd8_adaptive_half",
    "gemm_p128_q248_um16_un1_simd8_equal_split",
    "gemm_p128_q248_um16_un1_simd16_single_block",
    "gemm_p128_q248_um16_un1_simd16_fixed_q",
    "gemm_p128_q248_um16_un1_simd16_adaptive_half",
    "gemm_p128_q248_um16_un1_simd16_equal_split",
    "gemm_p128_q248_um16_un4_simd8_single_block",
    "gemm_p128_q248_um16_un4_simd8_fixed_q",
    "gemm_p128_q248_um16_un4_simd8_adaptive_half",
    "gemm_p128_q248_um16_un4_simd8_equal_split",
    "gemm_p128_q248_um16_un4_simd16_single_block",
    "gemm_p128_q248_um16_un4_simd16_fixed_q",
    "gemm_p128_q248_um16_un4_simd16_adaptive_half",
    "gemm_p128_q248_um16_un4_simd16_equal_split",
    "gemm_p128_q248_um32_un1_simd4_single_block",
    "gemm_p128_q248_um32_un1_simd4_fixed_q",
    "gemm_p128_q248_um32_un1_simd4_adaptive_half",
    "gemm_p128_q248_um32_un1_simd4_equal_split",
    "gemm_p128_q248_um32_un1_simd8_single_block",
    "gemm_p128_q248_um32_un1_simd8_fixed_q",
    "gemm_p128_q248_um32_un1_simd8_adaptive_half",
    "gemm_p128_q248_um32_un1_simd8_equal_split",
    "gemm_p128_q248_um32_un1_simd16_single_block",
    "gemm_p128_q248_um32_un1_simd16_fixed_q",
    "gemm_p128_q248_um32_un1_simd16_adaptive_half",
    "gemm_p128_q248_um32_un1_simd16_equal_split",
    "gemm_p128_q256_um1_un1_simd1_single_block",
    "gemm_p128_q256_um1_un1_simd1_fixed_q",
    "gemm_p128_q256_um1_un1_simd1_adaptive_half",
    "gemm_p128_q256_um1_un1_simd1_equal_split",
    "gemm_p128_q256_um1_un4_simd1_single_block",
    "gemm_p128_q256_um1_un4_simd1_fixed_q",
    "gemm_p128_q256_um1_un4_simd1_adaptive_half",
    "gemm_p128_q256_um1_un4_simd1_equal_split",
    "gemm_p128_q256_um4_un1_simd1_single_block",
    "gemm_p128_q256_um4_un1_simd1_fixed_q",
    "gemm_p128_q256_um4_un1_simd1_adaptive_half",
    "gemm_p128_q256_um4_un1_simd1_equal_split",
    "gemm_p128_q256_um4_un1_simd4_single_block",
    "gemm_p128_q256_um4_un1_simd4_fixed_q",
    "gemm_p128_q256_um4_un1_simd4_adaptive_half",
    "gemm_p128_q256_um4_un1_simd4_equal_split",
    "gemm_p128_q256_um4_un4_simd4_single_block",
    "gemm_p128_q256_um4_un4_simd4_fixed_q",
    "gemm_p128_q256_um4_un4_simd4_adaptive_half",
    "gemm_p128_q256_um4_un4_simd4_equal_split",
    "gemm_p128_q256_um8_un1_simd1_single_block",
    "gemm_p128_q256_um8_un1_simd1_fixed_q",
    "gemm_p128_q256_um8_un1_simd1_adaptive_half",
    "gemm_p128_q256_um8_un1_simd1_equal_split",
    "gemm_p128_q256_um8_un1_simd4_single_block",
    "gemm_p128_q256_um8_un1_simd4_fixed_q",
    "gemm_p128_q256_um8_un1_simd4_adaptive_half",
    "gemm_p128_q256_um8_un1_simd4_equal_split",
    "gemm_p128_q256_um8_un1_simd8_single_block",
    "gemm_p128_q256_um8_un1_simd8_fixed_q",
    "gemm_p128_q256_um8_un1_simd8_adaptive_half",
    "gemm_p128_q256_um8_un1_simd8_equal_split",
    "gemm_p128_q256_um8_un4_simd4_single_block",
    "gemm_p128_q256_um8_un4_simd4_fixed_q",
    "gemm_p128_q256_um8_un4_simd4_adaptive_half",
    "gemm_p128_q256_um8_un4_simd4_equal_split",
    "gemm_p128_q256_um8_un4_simd8_single_block",
    "gemm_p128_q256_um8_un4_simd8_fixed_q",
    "gemm_p128_q256_um8_un4_simd8_adaptive_half",
    "gemm_p128_q256_um8_un4_simd8_equal_split",
    "gemm_p128_q256_um16_un1_simd4_single_block",
    "gemm_p128_q256_um16_un1_simd4_fixed_q",
    "gemm_p128_q256_um16_un1_simd4_adaptive_half",
    "gemm_p128_q256_um16_un1_simd4_equal_split",
    "gemm_p128_q256_um16_un1_simd8_single_block",
    "gemm_p128_q256_um16_un1_simd8_fixed_q",
    "gemm_p128_q256_um16_un1_simd8_adaptive_half",
    "gemm_p128_q256_um16_un1_simd8_equal_split",
    "gemm_p128_q256_um16_un1_simd16_single_block",
    "gemm_p128_q256_um16_un1_simd16_fixed_q",
    "gemm_p128_q256_um16_un1_simd16_adaptive_half",
    "gemm_p128_q256_um16_un1_simd16_equal_split",
    "gemm_p128_q256_um16_un4_simd8_single_block",
    "gemm_p128_q256_um16_un4_simd8_fixed_q",
    "gemm_p128_q256_um16_un4_simd8_adaptive_half",
    "gemm_p128_q256_um16_un4_simd8_equal_split",
    "gemm_p128_q256_um16_un4_simd16_single_block",
    "gemm_p128_q256_um16_un4_simd16_fixed_q",
    "gemm_p128_q256_um16_un4_simd16_adaptive_half",
    "gemm_p128_q256_um16_un4_simd16_equal_split",
    "gemm_p128_q384_um1_un1_simd1_single_block",
    "gemm_p128_q384_um1_un1_simd1_fixed_q",
    "gemm_p128_q384_um1_un1_simd1_adaptive_half",
    "gemm_p128_q384_um1_un1_simd1_equal_split",
    "gemm_p128_q384_um1_un4_simd1_single_block",
    "gemm_p128_q384_um1_un4_simd1_fixed_q",
    "gemm_p128_q384_um1_un4_simd1_adaptive_half",
    "gemm_p128_q384_um1_un4_simd1_equal_split",
    "gemm_p128_q384_um4_un1_simd1_single_block",
    "gemm_p128_q384_um4_un1_simd1_fixed_q",
    "gemm_p128_q384_um4_un1_simd1_adaptive_half",
    "gemm_p128_q384_um4_un1_simd1_equal_split",
    "gemm_p128_q384_um4_un1_simd4_single_block",
    "gemm_p128_q384_um4_un1_simd4_fixed_q",
    "gemm_p128_q384_um4_un1_simd4_adaptive_half",
    "gemm_p128_q384_um4_un1_simd4_equal_split",
    "gemm_p128_q384_um4_un4_simd4_single_block",
    "gemm_p128_q384_um4_un4_simd4_fixed_q",
    "gemm_p128_q384_um4_un4_simd4_adaptive_half",
    "gemm_p128_q384_um4_un4_simd4_equal_split",
    "gemm_p128_q384_um8_un1_simd1_single_block",
    "gemm_p128_q384_um8_un1_simd1_fixed_q",
    "gemm_p128_q384_um8_un1_simd1_adaptive_half",
    "gemm_p128_q384_um8_un1_simd1_equal_split",
    "gemm_p128_q384_um8_un1_simd4_single_block",
    "gemm_p128_q384_um8_un1_simd4_fixed_q",
    "gemm_p128_q384_um8_un1_simd4_adaptive_half",
    "gemm_p128_q384_um8_un1_simd4_equal_split",
    "gemm_p128_q384_um8_un1_simd8_single_block",
    "gemm_p128_q384_um8_un1_simd8_fixed_q",
    "gemm_p128_q384_um8_un1_simd8_adaptive_half",
    "gemm_p128_q384_um8_un1_simd8_equal_split",
    "gemm_p128_q384_um8_un4_simd4_single_block",
    "gemm_p128_q384_um8_un4_simd4_fixed_q",
    "gemm_p128_q384_um8_un4_simd4_adaptive_half",
    "gemm_p128_q384_um8_un4_simd4_equal_split",
    "gemm_p128_q384_um8_un4_simd8_single_block",
    "gemm_p128_q384_um8_un4_simd8_fixed_q",
    "gemm_p128_q384_um8_un4_simd8_adaptive_half",
    "gemm_p128_q384_um8_un4_simd8_equal_split",
    "gemm_p128_q384_um16_un1_simd4_single_block",
    "gemm_p128_q384_um16_un1_simd4_fixed_q",
    "gemm_p128_q384_um16_un1_simd4_adaptive_half",
    "gemm_p128_q384_um16_un1_simd4_equal_split",
    "gemm_p128_q384_um16_un1_simd8_single_block",
    "gemm_p128_q384_um16_un1_simd8_fixed_q",
    "gemm_p128_q384_um16_un1_simd8_adaptive_half",
    "gemm_p128_q384_um16_un1_simd8_equal_split",
    "gemm_p128_q384_um16_un1_simd16_single_block",
    "gemm_p128_q384_um16_un1_simd16_fixed_q",
    "gemm_p128_q384_um16_un1_simd16_adaptive_half",
    "gemm_p128_q384_um16_un1_simd16_equal_split",
    "gemm_p128_q384_um16_un4_simd8_single_block",
    "gemm_p128_q384_um16_un4_simd8_fixed_q",
    "gemm_p128_q384_um16_un4_simd8_adaptive_half",
    "gemm_p128_q384_um16_un4_simd8_equal_split",
    "gemm_p128_q384_um16_un4_simd16_single_block",
    "gemm_p128_q384_um16_un4_simd16_fixed_q",
    "gemm_p128_q384_um16_un4_simd16_adaptive_half",
    "gemm_p128_q384_um16_un4_simd16_equal_split",
    "gemm_p128_q512_um1_un1_simd1_single_block",
    "gemm_p128_q512_um1_un1_simd1_fixed_q",
    "gemm_p128_q512_um1_un1_simd1_adaptive_half",
    "gemm_p128_q512_um1_un1_simd1_equal_split",
    "gemm_p128_q512_um1_un4_simd1_single_block",
    "gemm_p128_q512_um1_un4_simd1_fixed_q",
    "gemm_p128_q512_um1_un4_simd1_adaptive_half",
    "gemm_p128_q512_um1_un4_simd1_equal_split",
    "gemm_p128_q512_um4_un1_simd1_single_block",
    "gemm_p128_q512_um4_un1_simd1_fixed_q",
    "gemm_p128_q512_um4_un1_simd1_adaptive_half",
    "gemm_p128_q512_um4_un1_simd1_equal_split",
    "gemm_p128_q512_um4_un1_simd4_single_block",
    "gemm_p128_q512_um4_un1_simd4_fixed_q",
    "gemm_p128_q512_um4_un1_simd4_adaptive_half",
    "gemm_p128_q512_um4_un1_simd4_equal_split",
    "gemm_p128_q512_um4_un4_simd4_single_block",
    "gemm_p128_q512_um4_un4_simd4_fixed_q",
    "gemm_p128_q512_um4_un4_simd4_adaptive_half",
    "gemm_p128_q512_um4_un4_simd4_equal_split",
    "gemm_p128_q512_um8_un1_simd1_single_block",
    "gemm_p128_q512_um8_un1_simd1_fixed_q",
    "gemm_p128_q512_um8_un1_simd1_adaptive_half",
    "gemm_p128_q512_um8_un1_simd1_equal_split",
    "gemm_p128_q512_um8_un1_simd4_single_block",
    "gemm_p128_q512_um8_un1_simd4_fixed_q",
    "gemm_p128_q512_um8_un1_simd4_adaptive_half",
    "gemm_p128_q512_um8_un1_simd4_equal_split",
    "gemm_p128_q512_um8_un1_simd8_single_block",
    "gemm_p128_q512_um8_un1_simd8_fixed_q",
    "gemm_p128_q512_um8_un1_simd8_adaptive_half",
    "gemm_p128_q512_um8_un1_simd8_equal_split",
    "gemm_p128_q512_um8_un4_simd4_single_block",
    "gemm_p128_q512_um8_un4_simd4_fixed_q",
    "gemm_p128_q512_um8_un4_simd4_adaptive_half",
    "gemm_p128_q512_um8_un4_simd4_equal_split",
    "gemm_p128_q512_um8_un4_simd8_single_block",
    "gemm_p128_q512_um8_un4_simd8_fixed_q",
    "gemm_p128_q512_um8_un4_simd8_adaptive_half",
    "gemm_p128_q512_um8_un4_simd8_equal_split",
    "gemm_p256_q1_um1_un1_simd1_single_block",
    "gemm_p256_q1_um1_un1_simd1_fixed_q",
    "gemm_p256_q1_um1_un1_simd1_adaptive_half",
    "gemm_p256_q1_um1_un1_simd1_equal_split",
    "gemm_p256_q1_um1_un4_simd1_single_block",
    "gemm_p256_q1_um1_un4_simd1_fixed_q",
    "gemm_p256_q1_um1_un4_simd1_adaptive_half",
    "gemm_p256_q1_um1_un4_simd1_equal_split",
    "gemm_p256_q1_um4_un1_simd1_single_block",
    "gemm_p256_q1_um4_un1_simd1_fixed_q",
    "gemm_p256_q1_um4_un1_simd1_adaptive_half",
    "gemm_p256_q1_um4_un1_simd1_equal_split",
    "gemm_p256_q1_um4_un1_simd4_single_block",
    "gemm_p256_q1_um4_un1_simd4_fixed_q",
    "gemm_p256_q1_um4_un1_simd4_adaptive_half",
    "gemm_p256_q1_um4_un1_simd4_equal_split",
    "gemm_p256_q1_um4_un4_simd4_single_block",
    "gemm_p256_q1_um4_un4_simd4_fixed_q",
    "gemm_p256_q1_um4_un4_simd4_adaptive_half",
    "gemm_p256_q1_um4_un4_simd4_equal_split",
    "gemm_p256_q1_um8_un1_simd1_single_block",
    "gemm_p256_q1_um8_un1_simd1_fixed_q",
    "gemm_p256_q1_um8_un1_simd1_adaptive_half",
    "gemm_p256_q1_um8_un1_simd1_equal_split",
    "gemm_p256_q1_um8_un1_simd4_single_block",
    "gemm_p256_q1_um8_un1_simd4_fixed_q",
    "gemm_p256_q1_um8_un1_simd4_adaptive_half",
    "gemm_p256_q1_um8_un1_simd4_equal_split",
    "gemm_p256_q1_um8_un1_simd8_single_block",
    "gemm_p256_q1_um8_un1_simd8_fixed_q",
    "gemm_p256_q1_um8_un1_simd8_adaptive_half",
    "gemm_p256_q1_um8_un1_simd8_equal_split",
    "gemm_p256_q1_um8_un4_simd4_single_block",
    "gemm_p256_q1_um8_un4_simd4_fixed_q",
    "gemm_p256_q1_um8_un4_simd4_adaptive_half",
    "gemm_p256_q1_um8_un4_simd4_equal_split",
    "gemm_p256_q1_um8_un4_simd8_single_block",
    "gemm_p256_q1_um8_un4_simd8_fixed_q",
    "gemm_p256_q1_um8_un4_simd8_adaptive_half",
    "gemm_p256_q1_um8_un4_simd8_equal_split",
    "gemm_p256_q1_um16_un1_simd4_single_block",
    "gemm_p256_q1_um16_un1_simd4_fixed_q",
    "gemm_p256_q1_um16_un1_simd4_adaptive_half",
    "gemm_p256_q1_um16_un1_simd4_equal_split",
    "gemm_p256_q1_um16_un1_simd8_single_block",
    "gemm_p256_q1_um16_un1_simd8_fixed_q",
    "gemm_p256_q1_um16_un1_simd8_adaptive_half",
    "gemm_p256_q1_um16_un1_simd8_equal_split",
    "gemm_p256_q1_um16_un1_simd16_single_block",
    "gemm_p256_q1_um16_un1_simd16_fixed_q",
    "gemm_p256_q1_um16_un1_simd16_adaptive_half",
    "gemm_p256_q1_um16_un1_simd16_equal_split",
    "gemm_p256_q1_um16_un4_simd8_single_block",
    "gemm_p256_q1_um16_un4_simd8_fixed_q",
    "gemm_p256_q1_um16_un4_simd8_adaptive_half",
    "gemm_p256_q1_um16_un4_simd8_equal_split",
    "gemm_p256_q1_um16_un4_simd16_single_block",
    "gemm_p256_q1_um16_un4_simd16_fixed_q",
    "gemm_p256_q1_um16_un4_simd16_adaptive_half",
    "gemm_p256_q1_um16_un4_simd16_equal_split",
    "gemm_p256_q1_um32_un1_simd4_single_block",
    "gemm_p256_q1_um32_un1_simd4_fixed_q",
    "gemm_p256_q1_um32_un1_simd4_adaptive_half",
    "gemm_p256_q1_um32_un1_simd4_equal_split",
    "gemm_p256_q1_um32_un1_simd8_single_block",
    "gemm_p256_q1_um32_un1_simd8_fixed_q",
    "gemm_p256_q1_um32_un1_simd8_adaptive_half",
    "gemm_p256_q1_um32_un1_simd8_equal_split",
    "gemm_p256_q1_um32_un1_simd16_single_block",
    "gemm_p256_q1_um32_un1_simd16_fixed_q",
    "gemm_p256_q1_um32_un1_simd16_adaptive_half",
    "gemm_p256_q1_um32_un1_simd16_equal_split",
    "gemm_p256_q1_um32_un4_simd16_single_block",
    "gemm_p256_q1_um32_un4_simd16_fixed_q",
    "gemm_p256_q1_um32_un4_simd16_adaptive_half",
    "gemm_p256_q1_um32_un4_simd16_equal_split",
    "gemm_p256_q64_um1_un1_simd1_single_block",
    "gemm_p256_q64_um1_un1_simd1_fixed_q",
    "gemm_p256_q64_um1_un1_simd1_adaptive_half",
    "gemm_p256_q64_um1_un1_simd1_equal_split",
    "gemm_p256_q64_um1_un4_simd1_single_block",
    "gemm_p256_q64_um1_un4_simd1_fixed_q",
    "gemm_p256_q64_um1_un4_simd1_adaptive_half",
    "gemm_p256_q64_um1_un4_simd1_equal_split",
    "gemm_p256_q64_um4_un1_simd1_single_block",
    "gemm_p256_q64_um4_un1_simd1_fixed_q",
    "gemm_p256_q64_um4_un1_simd1_adaptive_half",
    "gemm_p256_q64_um4_un1_simd1_equal_split",
    "gemm_p256_q64_um4_un1_simd4_single_block",
    "gemm_p256_q64_um4_un1_simd4_fixed_q",
    "gemm_p256_q64_um4_un1_simd4_adaptive_half",
    "gemm_p256_q64_um4_un1_simd4_equal_split",
    "gemm_p256_q64_um4_un4_simd4_single_block",
    "gemm_p256_q64_um4_un4_simd4_fixed_q",
    "gemm_p256_q64_um4_un4_simd4_adaptive_half",
    "gemm_p256_q64_um4_un4_simd4_equal_split",
    "gemm_p256_q64_um8_un1_simd1_single_block",
    "gemm_p256_q64_um8_un1_simd1_fixed_q",
    "gemm_p256_q64_um8_un1_simd1_adaptive_half",
    "gemm_p256_q64_um8_un1_simd1_equal_split",
    "gemm_p256_q64_um8_un1_simd4_single_block",
    "gemm_p256_q64_um8_un1_simd4_fixed_q",
    "gemm_p256_q64_um8_un1_simd4_adaptive_half",
    "gemm_p256_q64_um8_un1_simd4_equal_split",
    "gemm_p256_q64_um8_un1_simd8_single_block",
    "gemm_p256_q64_um8_un1_simd8_fixed_q",
    "gemm_p256_q64_um8_un1_simd8_adaptive_half",
    "gemm_p256_q64_um8_un1_simd8_equal_split",
    "gemm_p256_q64_um8_un4_simd4_single_block",
    "gemm_p256_q64_um8_un4_simd4_fixed_q",
    "gemm_p256_q64_um8_un4_simd4_adaptive_half",
    "gemm_p256_q64_um8_un4_simd4_equal_split",
    "gemm_p256_q64_um8_un4_simd8_single_block",
    "gemm_p256_q64_um8_un4_simd8_fixed_q",
    "gemm_p256_q64_um8_un4_simd8_adaptive_half",
    "gemm_p256_q64_um8_un4_simd8_equal_split",
    "gemm_p256_q64_um16_un1_simd4_single_block",
    "gemm_p256_q64_um16_un1_simd4_fixed_q",
    "gemm_p256_q64_um16_un1_simd4_adaptive_half",
    "gemm_p256_q64_um16_un1_simd4_equal_split",
    "gemm_p256_q64_um16_un1_simd8_single_block",
    "gemm_p256_q64_um16_un1_simd8_fixed_q",
    "gemm_p256_q64_um16_un1_simd8_adaptive_half",
    "gemm_p256_q64_um16_un1_simd8_equal_split",
    "gemm_p256_q64_um16_un1_simd16_single_block",
    "gemm_p256_q64_um16_un1_simd16_fixed_q",
    "gemm_p256_q64_um16_un1_simd16_adaptive_half",
    "gemm_p256_q64_um16_un1_simd16_equal_split",
    "gemm_p256_q64_um16_un4_simd8_single_block",
    "gemm_p256_q64_um16_un4_simd8_fixed_q",
    "gemm_p256_q64_um16_un4_simd8_adaptive_half",
    "gemm_p256_q64_um16_un4_simd8_equal_split",
    "gemm_p256_q64_um16_un4_simd16_single_block",
    "gemm_p256_q64_um16_un4_simd16_fixed_q",
    "gemm_p256_q64_um16_un4_simd16_adaptive_half",
    "gemm_p256_q64_um16_un4_simd16_equal_split",
    "gemm_p256_q64_um32_un1_simd4_single_block",
    "gemm_p256_q64_um32_un1_simd4_fixed_q",
    "gemm_p256_q64_um32_un1_simd4_adaptive_half",
    "gemm_p256_q64_um32_un1_simd4_equal_split",
    "gemm_p256_q64_um32_un1_simd8_single_block",
    "gemm_p256_q64_um32_un1_simd8_fixed_q",
    "gemm_p256_q64_um32_un1_simd8_adaptive_half",
    "gemm_p256_q64_um32_un1_simd8_equal_split",
    "gemm_p256_q64_um32_un1_simd16_single_block",
    "gemm_p256_q64_um32_un1_simd16_fixed_q",
    "gemm_p256_q64_um32_un1_simd16_adaptive_half",
    "gemm_p256_q64_um32_un1_simd16_equal_split",
    "gemm_p256_q64_um32_un4_simd16_single_block",
    "gemm_p256_q64_um32_un4_simd16_fixed_q",
    "gemm_p256_q64_um32_un4_simd16_adaptive_half",
    "gemm_p256_q64_um32_un4_simd16_equal_split",
    "gemm_p256_q128_um1_un1_simd1_single_block",
    "gemm_p256_q128_um1_un1_simd1_fixed_q",
    "gemm_p256_q128_um1_un1_simd1_adaptive_half",
    "gemm_p256_q128_um1_un1_simd1_equal_split",
    "gemm_p256_q128_um1_un4_simd1_single_block",
    "gemm_p256_q128_um1_un4_simd1_fixed_q",
    "gemm_p256_q128_um1_un4_simd1_adaptive_half",
    "gemm_p256_q128_um1_un4_simd1_equal_split",
    "gemm_p256_q128_um4_un1_simd1_single_block",
    "gemm_p256_q128_um4_un1_simd1_fixed_q",
    "gemm_p256_q128_um4_un1_simd1_adaptive_half",
    "gemm_p256_q128_um4_un1_simd1_equal_split",
    "gemm_p256_q128_um4_un1_simd4_single_block",
    "gemm_p256_q128_um4_un1_simd4_fixed_q",
    "gemm_p256_q128_um4_un1_simd4_adaptive_half",
    "gemm_p256_q128_um4_un1_simd4_equal_split",
    "gemm_p256_q128_um4_un4_simd4_single_block",
    "gemm_p256_q128_um4_un4_simd4_fixed_q",
    "gemm_p256_q128_um4_un4_simd4_adaptive_half",
    "gemm_p256_q128_um4_un4_simd4_equal_split",
    "gemm_p256_q128_um8_un1_simd1_single_block",
    "gemm_p256_q128_um8_un1_simd1_fixed_q",
    "gemm_p256_q128_um8_un1_simd1_adaptive_half",
    "gemm_p256_q128_um8_un1_simd1_equal_split",
    "gemm_p256_q128_um8_un1_simd4_single_block",
    "gemm_p256_q128_um8_un1_simd4_fixed_q",
    "gemm_p256_q128_um8_un1_simd4_adaptive_half",
    "gemm_p256_q128_um8_un1_simd4_equal_split",
    "gemm_p256_q128_um8_un1_simd8_single_block",
    "gemm_p256_q128_um8_un1_simd8_fixed_q",
    "gemm_p256_q128_um8_un1_simd8_adaptive_half",
    "gemm_p256_q128_um8_un1_simd8_equal_split",
    "gemm_p256_q128_um8_un4_simd4_single_block",
    "gemm_p256_q128_um8_un4_simd4_fixed_q",
    "gemm_p256_q128_um8_un4_simd4_adaptive_half",
    "gemm_p256_q128_um8_un4_simd4_equal_split",
    "gemm_p256_q128_um8_un4_simd8_single_block",
    "gemm_p256_q128_um8_un4_simd8_fixed_q",
    "gemm_p256_q128_um8_un4_simd8_adaptive_half",
    "gemm_p256_q128_um8_un4_simd8_equal_split",
    "gemm_p256_q128_um16_un1_simd4_single_block",
    "gemm_p256_q128_um16_un1_simd4_fixed_q",
    "gemm_p256_q128_um16_un1_simd4_adaptive_half",
    "gemm_p256_q128_um16_un1_simd4_equal_split",
    "gemm_p256_q128_um16_un1_simd8_single_block",
    "gemm_p256_q128_um16_un1_simd8_fixed_q",
    "gemm_p256_q128_um16_un1_simd8_adaptive_half",
    "gemm_p256_q128_um16_un1_simd8_equal_split",
    "gemm_p256_q128_um16_un1_simd16_single_block",
    "gemm_p256_q128_um16_un1_simd16_fixed_q",
    "gemm_p256_q128_um16_un1_simd16_adaptive_half",
    "gemm_p256_q128_um16_un1_simd16_equal_split",
    "gemm_p256_q128_um16_un4_simd8_single_block",
    "gemm_p256_q128_um16_un4_simd8_fixed_q",
    "gemm_p256_q128_um16_un4_simd8_adaptive_half",
    "gemm_p256_q128_um16_un4_simd8_equal_split",
    "gemm_p256_q128_um16_un4_simd16_single_block",
    "gemm_p256_q128_um16_un4_simd16_fixed_q",
    "gemm_p256_q128_um16_un4_simd16_adaptive_half",
    "gemm_p256_q128_um16_un4_simd16_equal_split",
    "gemm_p256_q128_um32_un1_simd4_single_block",
    "gemm_p256_q128_um32_un1_simd4_fixed_q",
    "gemm_p256_q128_um32_un1_simd4_adaptive_half",
    "gemm_p256_q128_um32_un1_simd4_equal_split",
    "gemm_p256_q128_um32_un1_simd8_single_block",
    "gemm_p256_q128_um32_un1_simd8_fixed_q",
    "gemm_p256_q128_um32_un1_simd8_adaptive_half",
    "gemm_p256_q128_um32_un1_simd8_equal_split",
    "gemm_p256_q128_um32_un1_simd16_single_block",
    "gemm_p256_q128_um32_un1_simd16_fixed_q",
    "gemm_p256_q128_um32_un1_simd16_adaptive_half",
    "gemm_p256_q128_um32_un1_simd16_equal_split",
    "gemm_p256_q128_um32_un4_simd16_single_block",
    "gemm_p256_q128_um32_un4_simd16_fixed_q",
    "gemm_p256_q128_um32_un4_simd16_adaptive_half",
    "gemm_p256_q128_um32_un4_simd16_equal_split",
    "gemm_p256_q192_um1_un1_simd1_single_block",
    "gemm_p256_q192_um1_un1_simd1_fixed_q",
    "gemm_p256_q192_um1_un1_simd1_adaptive_half",
    "gemm_p256_q192_um1_un1_simd1_equal_split",
    "gemm_p256_q192_um1_un4_simd1_single_block",
    "gemm_p256_q192_um1_un4_simd1_fixed_q",
    "gemm_p256_q192_um1_un4_simd1_adaptive_half",
    "gemm_p256_q192_um1_un4_simd1_equal_split",
    "gemm_p256_q192_um4_un1_simd1_single_block",
    "gemm_p256_q192_um4_un1_simd1_fixed_q",
    "gemm_p256_q192_um4_un1_simd1_adaptive_half",
    "gemm_p256_q192_um4_un1_simd1_equal_split",
    "gemm_p256_q192_um4_un1_simd4_single_block",
    "gemm_p256_q192_um4_un1_simd4_fixed_q",
    "gemm_p256_q192_um4_un1_simd4_adaptive_half",
    "gemm_p256_q192_um4_un1_simd4_equal_split",
    "gemm_p256_q192_um4_un4_simd4_single_block",
    "gemm_p256_q192_um4_un4_simd4_fixed_q",
    "gemm_p256_q192_um4_un4_simd4_adaptive_half",
    "gemm_p256_q192_um4_un4_simd4_equal_split",
    "gemm_p256_q192_um8_un1_simd1_single_block",
    "gemm_p256_q192_um8_un1_simd1_fixed_q",
    "gemm_p256_q192_um8_un1_simd1_adaptive_half",
    "gemm_p256_q192_um8_un1_simd1_equal_split",
    "gemm_p256_q192_um8_un1_simd4_single_block",
    "gemm_p256_q192_um8_un1_simd4_fixed_q",
    "gemm_p256_q192_um8_un1_simd4_adaptive_half",
    "gemm_p256_q192_um8_un1_simd4_equal_split",
    "gemm_p256_q192_um8_un1_simd8_single_block",
    "gemm_p256_q192_um8_un1_simd8_fixed_q",
    "gemm_p256_q192_um8_un1_simd8_adaptive_half",
    "gemm_p256_q192_um8_un1_simd8_equal_split",
    "gemm_p256_q192_um8_un4_simd4_single_block",
    "gemm_p256_q192_um8_un4_simd4_fixed_q",
    "gemm_p256_q192_um8_un4_simd4_adaptive_half",
    "gemm_p256_q192_um8_un4_simd4_equal_split",
    "gemm_p256_q192_um8_un4_simd8_single_block",
    "gemm_p256_q192_um8_un4_simd8_fixed_q",
    "gemm_p256_q192_um8_un4_simd8_adaptive_half",
    "gemm_p256_q192_um8_un4_simd8_equal_split",
    "gemm_p256_q192_um16_un1_simd4_single_block",
    "gemm_p256_q192_um16_un1_simd4_fixed_q",
    "gemm_p256_q192_um16_un1_simd4_adaptive_half",
    "gemm_p256_q192_um16_un1_simd4_equal_split",
    "gemm_p256_q192_um16_un1_simd8_single_block",
    "gemm_p256_q192_um16_un1_simd8_fixed_q",
    "gemm_p256_q192_um16_un1_simd8_adaptive_half",
    "gemm_p256_q192_um16_un1_simd8_equal_split",
    "gemm_p256_q192_um16_un1_simd16_single_block",
    "gemm_p256_q192_um16_un1_simd16_fixed_q",
    "gemm_p256_q192_um16_un1_simd16_adaptive_half",
    "gemm_p256_q192_um16_un1_simd16_equal_split",
    "gemm_p256_q192_um16_un4_simd8_single_block",
    "gemm_p256_q192_um16_un4_simd8_fixed_q",
    "gemm_p256_q192_um16_un4_simd8_adaptive_half",
    "gemm_p256_q192_um16_un4_simd8_equal_split",
    "gemm_p256_q192_um16_un4_simd16_single_block",
    "gemm_p256_q192_um16_un4_simd16_fixed_q",
    "gemm_p256_q192_um16_un4_simd16_adaptive_half",
    "gemm_p256_q192_um16_un4_simd16_equal_split",
    "gemm_p256_q192_um32_un1_simd4_single_block",
    "gemm_p256_q192_um32_un1_simd4_fixed_q",
    "gemm_p256_q192_um32_un1_simd4_adaptive_half",
    "gemm_p256_q192_um32_un1_simd4_equal_split",
    "gemm_p256_q192_um32_un1_simd8_single_block",
    "gemm_p256_q192_um32_un1_simd8_fixed_q",
    "gemm_p256_q192_um32_un1_simd8_adaptive_half",
    "gemm_p256_q192_um32_un1_simd8_equal_split",
    "gemm_p256_q192_um32_un1_simd16_single_block",
    "gemm_p256_q192_um32_un1_simd16_fixed_q",
    "gemm_p256_q192_um32_un1_simd16_adaptive_half",
    "gemm_p256_q192_um32_un1_simd16_equal_split",
    "gemm_p256_q192_um32_un4_simd16_single_block",
    "gemm_p256_q192_um32_un4_simd16_fixed_q",
    "gemm_p256_q192_um32_un4_simd16_adaptive_half",
    "gemm_p256_q192_um32_un4_simd16_equal_split",
    "gemm_p256_q240_um1_un1_simd1_single_block",
    "gemm_p256_q240_um1_un1_simd1_fixed_q",
    "gemm_p256_q240_um1_un1_simd1_adaptive_half",
    "gemm_p256_q240_um1_un1_simd1_equal_split",
    "gemm_p256_q240_um1_un4_simd1_single_block",
    "gemm_p256_q240_um1_un4_simd1_fixed_q",
    "gemm_p256_q240_um1_un4_simd1_adaptive_half",
    "gemm_p256_q240_um1_un4_simd1_equal_split",
    "gemm_p256_q240_um4_un1_simd1_single_block",
    "gemm_p256_q240_um4_un1_simd1_fixed_q",
    "gemm_p256_q240_um4_un1_simd1_adaptive_half",
    "gemm_p256_q240_um4_un1_simd1_equal_split",
    "gemm_p256_q240_um4_un1_simd4_single_block",
    "gemm_p256_q240_um4_un1_simd4_fixed_q",
    "gemm_p256_q240_um4_un1_simd4_adaptive_half",
    "gemm_p256_q240_um4_un1_simd4_equal_split",
    "gemm_p256_q240_um4_un4_simd4_single_block",
    "gemm_p256_q240_um4_un4_simd4_fixed_q",
    "gemm_p256_q240_um4_un4_simd4_adaptive_half",
    "gemm_p256_q240_um4_un4_simd4_equal_split",
    "gemm_p256_q240_um8_un1_simd1_single_block",
    "gemm_p256_q240_um8_un1_simd1_fixed_q",
    "gemm_p256_q240_um8_un1_simd1_adaptive_half",
    "gemm_p256_q240_um8_un1_simd1_equal_split",
    "gemm_p256_q240_um8_un1_simd4_single_block",
    "gemm_p256_q240_um8_un1_simd4_fixed_q",
    "gemm_p256_q240_um8_un1_simd4_adaptive_half",
    "gemm_p256_q240_um8_un1_simd4_equal_split",
    "gemm_p256_q240_um8_un1_simd8_single_block",
    "gemm_p256_q240_um8_un1_simd8_fixed_q",
    "gemm_p256_q240_um8_un1_simd8_adaptive_half",
    "gemm_p256_q240_um8_un1_simd8_equal_split",
    "gemm_p256_q240_um8_un4_simd4_single_block",
    "gemm_p256_q240_um8_un4_simd4_fixed_q",
    "gemm_p256_q240_um8_un4_simd4_adaptive_half",
    "gemm_p256_q240_um8_un4_simd4_equal_split",
    "gemm_p256_q240_um8_un4_simd8_single_block",
    "gemm_p256_q240_um8_un4_simd8_fixed_q",
    "gemm_p256_q240_um8_un4_simd8_adaptive_half",
    "gemm_p256_q240_um8_un4_simd8_equal_split",
    "gemm_p256_q240_um16_un1_simd4_single_block",
    "gemm_p256_q240_um16_un1_simd4_fixed_q",
    "gemm_p256_q240_um16_un1_simd4_adaptive_half",
    "gemm_p256_q240_um16_un1_simd4_equal_split",
    "gemm_p256_q240_um16_un1_simd8_single_block",
    "gemm_p256_q240_um16_un1_simd8_fixed_q",
    "gemm_p256_q240_um16_un1_simd8_adaptive_half",
    "gemm_p256_q240_um16_un1_simd8_equal_split",
    "gemm_p256_q240_um16_un1_simd16_single_block",
    "gemm_p256_q240_um16_un1_simd16_fixed_q",
    "gemm_p256_q240_um16_un1_simd16_adaptive_half",
    "gemm_p256_q240_um16_un1_simd16_equal_split",
    "gemm_p256_q240_um16_un4_simd8_single_block",
    "gemm_p256_q240_um16_un4_simd8_fixed_q",
    "gemm_p256_q240_um16_un4_simd8_adaptive_half",
    "gemm_p256_q240_um16_un4_simd8_equal_split",
    "gemm_p256_q240_um16_un4_simd16_single_block",
    "gemm_p256_q240_um16_un4_simd16_fixed_q",
    "gemm_p256_q240_um16_un4_simd16_adaptive_half",
    "gemm_p256_q240_um16_un4_simd16_equal_split",
    "gemm_p256_q240_um32_un1_simd4_single_block",
    "gemm_p256_q240_um32_un1_simd4_fixed_q",
    "gemm_p256_q240_um32_un1_simd4_adaptive_half",
    "gemm_p256_q240_um32_un1_simd4_equal_split",
    "gemm_p256_q240_um32_un1_simd8_single_block",
    "gemm_p256_q240_um32_un1_simd8_fixed_q",
    "gemm_p256_q240_um32_un1_simd8_adaptive_half",
    "gemm_p256_q240_um32_un1_simd8_equal_split",
    "gemm_p256_q240_um32_un1_simd16_single_block",
    "gemm_p256_q240_um32_un1_simd16_fixed_q",
    "gemm_p256_q240_um32_un1_simd16_adaptive_half",
    "gemm_p256_q240_um32_un1_simd16_equal_split",
    "gemm_p256_q248_um1_un1_simd1_single_block",
    "gemm_p256_q248_um1_un1_simd1_fixed_q",
    "gemm_p256_q248_um1_un1_simd1_adaptive_half",
    "gemm_p256_q248_um1_un1_simd1_equal_split",
    "gemm_p256_q248_um1_un4_simd1_single_block",
    "gemm_p256_q248_um1_un4_simd1_fixed_q",
    "gemm_p256_q248_um1_un4_simd1_adaptive_half",
    "gemm_p256_q248_um1_un4_simd1_equal_split",
    "gemm_p256_q248_um4_un1_simd1_single_block",
    "gemm_p256_q248_um4_un1_simd1_fixed_q",
    "gemm_p256_q248_um4_un1_simd1_adaptive_half",
    "gemm_p256_q248_um4_un1_simd1_equal_split",
    "gemm_p256_q248_um4_un1_simd4_single_block",
    "gemm_p256_q248_um4_un1_simd4_fixed_q",
    "gemm_p256_q248_um4_un1_simd4_adaptive_half",
    "gemm_p256_q248_um4_un1_simd4_equal_split",
    "gemm_p256_q248_um4_un4_simd4_single_block",
    "gemm_p256_q248_um4_un4_simd4_fixed_q",
    "gemm_p256_q248_um4_un4_simd4_adaptive_half",
    "gemm_p256_q248_um4_un4_simd4_equal_split",
    "gemm_p256_q248_um8_un1_simd1_single_block",
    "gemm_p256_q248_um8_un1_simd1_fixed_q",
    "gemm_p256_q248_um8_un1_simd1_adaptive_half",
    "gemm_p256_q248_um8_un1_simd1_equal_split",
    "gemm_p256_q248_um8_un1_simd4_single_block",
    "gemm_p256_q248_um8_un1_simd4_fixed_q",
    "gemm_p256_q248_um8_un1_simd4_adaptive_half",
    "gemm_p256_q248_um8_un1_simd4_equal_split",
    "gemm_p256_q248_um8_un1_simd8_single_block",
    "gemm_p256_q248_um8_un1_simd8_fixed_q",
    "gemm_p256_q248_um8_un1_simd8_adaptive_half",
    "gemm_p256_q248_um8_un1_simd8_equal_split",
    "gemm_p256_q248_um8_un4_simd4_single_block",
    "gemm_p256_q248_um8_un4_simd4_fixed_q",
    "gemm_p256_q248_um8_un4_simd4_adaptive_half",
    "gemm_p256_q248_um8_un4_simd4_equal_split",
    "gemm_p256_q248_um8_un4_simd8_single_block",
    "gemm_p256_q248_um8_un4_simd8_fixed_q",
    "gemm_p256_q248_um8_un4_simd8_adaptive_half",
    "gemm_p256_q248_um8_un4_simd8_equal_split",
    "gemm_p256_q248_um16_un1_simd4_single_block",
    "gemm_p256_q248_um16_un1_simd4_fixed_q",
    "gemm_p256_q248_um16_un1_simd4_adaptive_half",
    "gemm_p256_q248_um16_un1_simd4_equal_split",
    "gemm_p256_q248_um16_un1_simd8_single_block",
    "gemm_p256_q248_um16_un1_simd8_fixed_q",
    "gemm_p256_q248_um16_un1_simd8_adaptive_half",
    "gemm_p256_q248_um16_un1_simd8_equal_split",
    "gemm_p256_q248_um16_un1_simd16_single_block",
    "gemm_p256_q248_um16_un1_simd16_fixed_q",
    "gemm_p256_q248_um16_un1_simd16_adaptive_half",
    "gemm_p256_q248_um16_un1_simd16_equal_split",
    "gemm_p256_q248_um16_un4_simd8_single_block",
    "gemm_p256_q248_um16_un4_simd8_fixed_q",
    "gemm_p256_q248_um16_un4_simd8_adaptive_half",
    "gemm_p256_q248_um16_un4_simd8_equal_split",
    "gemm_p256_q248_um16_un4_simd16_single_block",
    "gemm_p256_q248_um16_un4_simd16_fixed_q",
    "gemm_p256_q248_um16_un4_simd16_adaptive_half",
    "gemm_p256_q248_um16_un4_simd16_equal_split",
    "gemm_p256_q248_um32_un1_simd4_single_block",
    "gemm_p256_q248_um32_un1_simd4_fixed_q",
    "gemm_p256_q248_um32_un1_simd4_adaptive_half",
    "gemm_p256_q248_um32_un1_simd4_equal_split",
    "gemm_p256_q248_um32_un1_simd8_single_block",
    "gemm_p256_q248_um32_un1_simd8_fixed_q",
    "gemm_p256_q248_um32_un1_simd8_adaptive_half",
    "gemm_p256_q248_um32_un1_simd8_equal_split",
    "gemm_p256_q248_um32_un1_simd16_single_block",
    "gemm_p256_q248_um32_un1_simd16_fixed_q",
    "gemm_p256_q248_um32_un1_simd16_adaptive_half",
    "gemm_p256_q248_um32_un1_simd16_equal_split",
    "gemm_p256_q256_um1_un1_simd1_single_block",
    "gemm_p256_q256_um1_un1_simd1_fixed_q",
    "gemm_p256_q256_um1_un1_simd1_adaptive_half",
    "gemm_p256_q256_um1_un1_simd1_equal_split",
    "gemm_p256_q256_um1_un4_simd1_single_block",
    "gemm_p256_q256_um1_un4_simd1_fixed_q",
    "gemm_p256_q256_um1_un4_simd1_adaptive_half",
    "gemm_p256_q256_um1_un4_simd1_equal_split",
    "gemm_p256_q256_um4_un1_simd1_single_block",
    "gemm_p256_q256_um4_un1_simd1_fixed_q",
    "gemm_p256_q256_um4_un1_simd1_adaptive_half",
    "gemm_p256_q256_um4_un1_simd1_equal_split",
    "gemm_p256_q256_um4_un1_simd4_single_block",
    "gemm_p256_q256_um4_un1_simd4_fixed_q",
    "gemm_p256_q256_um4_un1_simd4_adaptive_half",
    "gemm_p256_q256_um4_un1_simd4_equal_split",
    "gemm_p256_q256_um4_un4_simd4_single_block",
    "gemm_p256_q256_um4_un4_simd4_fixed_q",
    "gemm_p256_q256_um4_un4_simd4_adaptive_half",
    "gemm_p256_q256_um4_un4_simd4_equal_split",
    "gemm_p256_q256_um8_un1_simd1_single_block",
    "gemm_p256_q256_um8_un1_simd1_fixed_q",
    "gemm_p256_q256_um8_un1_simd1_adaptive_half",
    "gemm_p256_q256_um8_un1_simd1_equal_split",
    "gemm_p256_q256_um8_un1_simd4_single_block",
    "gemm_p256_q256_um8_un1_simd4_fixed_q",
    "gemm_p256_q256_um8_un1_simd4_adaptive_half",
    "gemm_p256_q256_um8_un1_simd4_equal_split",
    "gemm_p256_q256_um8_un1_simd8_single_block",
    "gemm_p256_q256_um8_un1_simd8_fixed_q",
    "gemm_p256_q256_um8_un1_simd8_adaptive_half",
    "gemm_p256_q256_um8_un1_simd8_equal_split",
    "gemm_p256_q256_um8_un4_simd4_single_block",
    "gemm_p256_q256_um8_un4_simd4_fixed_q",
    "gemm_p256_q256_um8_un4_simd4_adaptive_half",
    "gemm_p256_q256_um8_un4_simd4_equal_split",
    "gemm_p256_q256_um8_un4_simd8_single_block",
    "gemm_p256_q256_um8_un4_simd8_fixed_q",
    "gemm_p256_q256_um8_un4_simd8_adaptive_half",
    "gemm_p256_q256_um8_un4_simd8_equal_split",
    "gemm_p256_q256_um16_un1_simd4_single_block",
    "gemm_p256_q256_um16_un1_simd4_fixed_q",
    "gemm_p256_q256_um16_un1_simd4_adaptive_half",
    "gemm_p256_q256_um16_un1_simd4_equal_split",
    "gemm_p256_q256_um16_un1_simd8_single_block",
    "gemm_p256_q256_um16_un1_simd8_fixed_q",
    "gemm_p256_q256_um16_un1_simd8_adaptive_half",
    "gemm_p256_q256_um16_un1_simd8_equal_split",
    "gemm_p256_q256_um16_un1_simd16_single_block",
    "gemm_p256_q256_um16_un1_simd16_fixed_q",
    "gemm_p256_q256_um16_un1_simd16_adaptive_half",
    "gemm_p256_q256_um16_un1_simd16_equal_split",
    "gemm_p256_q256_um16_un4_simd8_single_block",
    "gemm_p256_q256_um16_un4_simd8_fixed_q",
    "gemm_p256_q256_um16_un4_simd8_adaptive_half",
    "gemm_p256_q256_um16_un4_simd8_equal_split",
    "gemm_p256_q256_um16_un4_simd16_single_block",
    "gemm_p256_q256_um16_un4_simd16_fixed_q",
    "gemm_p256_q256_um16_un4_simd16_adaptive_half",
    "gemm_p256_q256_um16_un4_simd16_equal_split",
    "gemm_p256_q384_um1_un1_simd1_single_block",
    "gemm_p256_q384_um1_un1_simd1_fixed_q",
    "gemm_p256_q384_um1_un1_simd1_adaptive_half",
    "gemm_p256_q384_um1_un1_simd1_equal_split",
    "gemm_p256_q384_um1_un4_simd1_single_block",
    "gemm_p256_q384_um1_un4_simd1_fixed_q",
    "gemm_p256_q384_um1_un4_simd1_adaptive_half",
    "gemm_p256_q384_um1_un4_simd1_equal_split",
    "gemm_p256_q384_um4_un1_simd1_single_block",
    "gemm_p256_q384_um4_un1_simd1_fixed_q",
    "gemm_p256_q384_um4_un1_simd1_adaptive_half",
    "gemm_p256_q384_um4_un1_simd1_equal_split",
    "gemm_p256_q384_um4_un1_simd4_single_block",
    "gemm_p256_q384_um4_un1_simd4_fixed_q",
    "gemm_p256_q384_um4_un1_simd4_adaptive_half",
    "gemm_p256_q384_um4_un1_simd4_equal_split",
    "gemm_p256_q384_um4_un4_simd4_single_block",
    "gemm_p256_q384_um4_un4_simd4_fixed_q",
    "gemm_p256_q384_um4_un4_simd4_adaptive_half",
    "gemm_p256_q384_um4_un4_simd4_equal_split",
    "gemm_p256_q384_um8_un1_simd1_single_block",
    "gemm_p256_q384_um8_un1_simd1_fixed_q",
    "gemm_p256_q384_um8_un1_simd1_adaptive_half",
    "gemm_p256_q384_um8_un1_simd1_equal_split",
    "gemm_p256_q384_um8_un1_simd4_single_block",
    "gemm_p256_q384_um8_un1_simd4_fixed_q",
    "gemm_p256_q384_um8_un1_simd4_adaptive_half",
    "gemm_p256_q384_um8_un1_simd4_equal_split",
    "gemm_p256_q384_um8_un1_simd8_single_block",
    "gemm_p256_q384_um8_un1_simd8_fixed_q",
    "gemm_p256_q384_um8_un1_simd8_adaptive_half",
    "gemm_p256_q384_um8_un1_simd8_equal_split",
    "gemm_p256_q384_um8_un4_simd4_single_block",
    "gemm_p256_q384_um8_un4_simd4_fixed_q",
    "gemm_p256_q384_um8_un4_simd4_adaptive_half",
    "gemm_p256_q384_um8_un4_simd4_equal_split",
    "gemm_p256_q384_um8_un4_simd8_single_block",
    "gemm_p256_q384_um8_un4_simd8_fixed_q",
    "gemm_p256_q384_um8_un4_simd8_adaptive_half",
    "gemm_p256_q384_um8_un4_simd8_equal_split",
    "gemm_p256_q384_um16_un1_simd4_single_block",
    "gemm_p256_q384_um16_un1_simd4_fixed_q",
    "gemm_p256_q384_um16_un1_simd4_adaptive_half",
    "gemm_p256_q384_um16_un1_simd4_equal_split",
    "gemm_p256_q384_um16_un1_simd8_single_block",
    "gemm_p256_q384_um16_un1_simd8_fixed_q",
    "gemm_p256_q384_um16_un1_simd8_adaptive_half",
    "gemm_p256_q384_um16_un1_simd8_equal_split",
    "gemm_p256_q384_um16_un1_simd16_single_block",
    "gemm_p256_q384_um16_un1_simd16_fixed_q",
    "gemm_p256_q384_um16_un1_simd16_adaptive_half",
    "gemm_p256_q384_um16_un1_simd16_equal_split",
    "gemm_p256_q384_um16_un4_simd8_single_block",
    "gemm_p256_q384_um16_un4_simd8_fixed_q",
    "gemm_p256_q384_um16_un4_simd8_adaptive_half",
    "gemm_p256_q384_um16_un4_simd8_equal_split",
    "gemm_p256_q384_um16_un4_simd16_single_block",
    "gemm_p256_q384_um16_un4_simd16_fixed_q",
    "gemm_p256_q384_um16_un4_simd16_adaptive_half",
    "gemm_p256_q384_um16_un4_simd16_equal_split",
    "gemm_p256_q512_um1_un1_simd1_single_block",
    "gemm_p256_q512_um1_un1_simd1_fixed_q",
    "gemm_p256_q512_um1_un1_simd1_adaptive_half",
    "gemm_p256_q512_um1_un1_simd1_equal_split",
    "gemm_p256_q512_um1_un4_simd1_single_block",
    "gemm_p256_q512_um1_un4_simd1_fixed_q",
    "gemm_p256_q512_um1_un4_simd1_adaptive_half",
    "gemm_p256_q512_um1_un4_simd1_equal_split",
    "gemm_p256_q512_um4_un1_simd1_single_block",
    "gemm_p256_q512_um4_un1_simd1_fixed_q",
    "gemm_p256_q512_um4_un1_simd1_adaptive_half",
    "gemm_p256_q512_um4_un1_simd1_equal_split",
    "gemm_p256_q512_um4_un1_simd4_single_block",
    "gemm_p256_q512_um4_un1_simd4_fixed_q",
    "gemm_p256_q512_um4_un1_simd4_adaptive_half",
    "gemm_p256_q512_um4_un1_simd4_equal_split",
    "gemm_p256_q512_um4_un4_simd4_single_block",
    "gemm_p256_q512_um4_un4_simd4_fixed_q",
    "gemm_p256_q512_um4_un4_simd4_adaptive_half",
    "gemm_p256_q512_um4_un4_simd4_equal_split",
    "gemm_p256_q512_um8_un1_simd1_single_block",
    "gemm_p256_q512_um8_un1_simd1_fixed_q",
    "gemm_p256_q512_um8_un1_simd1_adaptive_half",
    "gemm_p256_q512_um8_un1_simd1_equal_split",
    "gemm_p256_q512_um8_un1_simd4_single_block",
    "gemm_p256_q512_um8_un1_simd4_fixed_q",
    "gemm_p256_q512_um8_un1_simd4_adaptive_half",
    "gemm_p256_q512_um8_un1_simd4_equal_split",
    "gemm_p256_q512_um8_un1_simd8_single_block",
    "gemm_p256_q512_um8_un1_simd8_fixed_q",
    "gemm_p256_q512_um8_un1_simd8_adaptive_half",
    "gemm_p256_q512_um8_un1_simd8_equal_split",
    "gemm_p256_q512_um8_un4_simd4_single_block",
    "gemm_p256_q512_um8_un4_simd4_fixed_q",
    "gemm_p256_q512_um8_un4_simd4_adaptive_half",
    "gemm_p256_q512_um8_un4_simd4_equal_split",
    "gemm_p256_q512_um8_un4_simd8_single_block",
    "gemm_p256_q512_um8_un4_simd8_fixed_q",
    "gemm_p256_q512_um8_un4_simd8_adaptive_half",
    "gemm_p256_q512_um8_un4_simd8_equal_split",
    "gemm_p384_q1_um1_un1_simd1_single_block",
    "gemm_p384_q1_um1_un1_simd1_fixed_q",
    "gemm_p384_q1_um1_un1_simd1_adaptive_half",
    "gemm_p384_q1_um1_un1_simd1_equal_split",
    "gemm_p384_q1_um1_un4_simd1_single_block",
    "gemm_p384_q1_um1_un4_simd1_fixed_q",
    "gemm_p384_q1_um1_un4_simd1_adaptive_half",
    "gemm_p384_q1_um1_un4_simd1_equal_split",
    "gemm_p384_q1_um4_un1_simd1_single_block",
    "gemm_p384_q1_um4_un1_simd1_fixed_q",
    "gemm_p384_q1_um4_un1_simd1_adaptive_half",
    "gemm_p384_q1_um4_un1_simd1_equal_split",
    "gemm_p384_q1_um4_un1_simd4_single_block",
    "gemm_p384_q1_um4_un1_simd4_fixed_q",
    "gemm_p384_q1_um4_un1_simd4_adaptive_half",
    "gemm_p384_q1_um4_un1_simd4_equal_split",
    "gemm_p384_q1_um4_un4_simd4_single_block",
    "gemm_p384_q1_um4_un4_simd4_fixed_q",
    "gemm_p384_q1_um4_un4_simd4_adaptive_half",
    "gemm_p384_q1_um4_un4_simd4_equal_split",
    "gemm_p384_q1_um8_un1_simd1_single_block",
    "gemm_p384_q1_um8_un1_simd1_fixed_q",
    "gemm_p384_q1_um8_un1_simd1_adaptive_half",
    "gemm_p384_q1_um8_un1_simd1_equal_split",
    "gemm_p384_q1_um8_un1_simd4_single_block",
    "gemm_p384_q1_um8_un1_simd4_fixed_q",
    "gemm_p384_q1_um8_un1_simd4_adaptive_half",
    "gemm_p384_q1_um8_un1_simd4_equal_split",
    "gemm_p384_q1_um8_un1_simd8_single_block",
    "gemm_p384_q1_um8_un1_simd8_fixed_q",
    "gemm_p384_q1_um8_un1_simd8_adaptive_half",
    "gemm_p384_q1_um8_un1_simd8_equal_split",
    "gemm_p384_q1_um8_un4_simd4_single_block",
    "gemm_p384_q1_um8_un4_simd4_fixed_q",
    "gemm_p384_q1_um8_un4_simd4_adaptive_half",
    "gemm_p384_q1_um8_un4_simd4_equal_split",
    "gemm_p384_q1_um8_un4_simd8_single_block",
    "gemm_p384_q1_um8_un4_simd8_fixed_q",
    "gemm_p384_q1_um8_un4_simd8_adaptive_half",
    "gemm_p384_q1_um8_un4_simd8_equal_split",
    "gemm_p384_q1_um16_un1_simd4_single_block",
    "gemm_p384_q1_um16_un1_simd4_fixed_q",
    "gemm_p384_q1_um16_un1_simd4_adaptive_half",
    "gemm_p384_q1_um16_un1_simd4_equal_split",
    "gemm_p384_q1_um16_un1_simd8_single_block",
    "gemm_p384_q1_um16_un1_simd8_fixed_q",
    "gemm_p384_q1_um16_un1_simd8_adaptive_half",
    "gemm_p384_q1_um16_un1_simd8_equal_split",
    "gemm_p384_q1_um16_un1_simd16_single_block",
    "gemm_p384_q1_um16_un1_simd16_fixed_q",
    "gemm_p384_q1_um16_un1_simd16_adaptive_half",
    "gemm_p384_q1_um16_un1_simd16_equal_split",
    "gemm_p384_q1_um16_un4_simd8_single_block",
    "gemm_p384_q1_um16_un4_simd8_fixed_q",
    "gemm_p384_q1_um16_un4_simd8_adaptive_half",
    "gemm_p384_q1_um16_un4_simd8_equal_split",
    "gemm_p384_q1_um16_un4_simd16_single_block",
    "gemm_p384_q1_um16_un4_simd16_fixed_q",
    "gemm_p384_q1_um16_un4_simd16_adaptive_half",
    "gemm_p384_q1_um16_un4_simd16_equal_split",
    "gemm_p384_q1_um32_un1_simd4_single_block",
    "gemm_p384_q1_um32_un1_simd4_fixed_q",
    "gemm_p384_q1_um32_un1_simd4_adaptive_half",
    "gemm_p384_q1_um32_un1_simd4_equal_split",
    "gemm_p384_q1_um32_un1_simd8_single_block",
    "gemm_p384_q1_um32_un1_simd8_fixed_q",
    "gemm_p384_q1_um32_un1_simd8_adaptive_half",
    "gemm_p384_q1_um32_un1_simd8_equal_split",
    "gemm_p384_q1_um32_un1_simd16_single_block",
    "gemm_p384_q1_um32_un1_simd16_fixed_q",
    "gemm_p384_q1_um32_un1_simd16_adaptive_half",
    "gemm_p384_q1_um32_un1_simd16_equal_split",
    "gemm_p384_q1_um32_un4_simd16_single_block",
    "gemm_p384_q1_um32_un4_simd16_fixed_q",
    "gemm_p384_q1_um32_un4_simd16_adaptive_half",
    "gemm_p384_q1_um32_un4_simd16_equal_split",
    "gemm_p384_q64_um1_un1_simd1_single_block",
    "gemm_p384_q64_um1_un1_simd1_fixed_q",
    "gemm_p384_q64_um1_un1_simd1_adaptive_half",
    "gemm_p384_q64_um1_un1_simd1_equal_split",
    "gemm_p384_q64_um1_un4_simd1_single_block",
    "gemm_p384_q64_um1_un4_simd1_fixed_q",
    "gemm_p384_q64_um1_un4_simd1_adaptive_half",
    "gemm_p384_q64_um1_un4_simd1_equal_split",
    "gemm_p384_q64_um4_un1_simd1_single_block",
    "gemm_p384_q64_um4_un1_simd1_fixed_q",
    "gemm_p384_q64_um4_un1_simd1_adaptive_half",
    "gemm_p384_q64_um4_un1_simd1_equal_split",
    "gemm_p384_q64_um4_un1_simd4_single_block",
    "gemm_p384_q64_um4_un1_simd4_fixed_q",
    "gemm_p384_q64_um4_un1_simd4_adaptive_half",
    "gemm_p384_q64_um4_un1_simd4_equal_split",
    "gemm_p384_q64_um4_un4_simd4_single_block",
    "gemm_p384_q64_um4_un4_simd4_fixed_q",
    "gemm_p384_q64_um4_un4_simd4_adaptive_half",
    "gemm_p384_q64_um4_un4_simd4_equal_split",
    "gemm_p384_q64_um8_un1_simd1_single_block",
    "gemm_p384_q64_um8_un1_simd1_fixed_q",
    "gemm_p384_q64_um8_un1_simd1_adaptive_half",
    "gemm_p384_q64_um8_un1_simd1_equal_split",
    "gemm_p384_q64_um8_un1_simd4_single_block",
    "gemm_p384_q64_um8_un1_simd4_fixed_q",
    "gemm_p384_q64_um8_un1_simd4_adaptive_half",
    "gemm_p384_q64_um8_un1_simd4_equal_split",
    "gemm_p384_q64_um8_un1_simd8_single_block",
    "gemm_p384_q64_um8_un1_simd8_fixed_q",
    "gemm_p384_q64_um8_un1_simd8_adaptive_half",
    "gemm_p384_q64_um8_un1_simd8_equal_split",
    "gemm_p384_q64_um8_un4_simd4_single_block",
    "gemm_p384_q64_um8_un4_simd4_fixed_q",
    "gemm_p384_q64_um8_un4_simd4_adaptive_half",
    "gemm_p384_q64_um8_un4_simd4_equal_split",
    "gemm_p384_q64_um8_un4_simd8_single_block",
    "gemm_p384_q64_um8_un4_simd8_fixed_q",
    "gemm_p384_q64_um8_un4_simd8_adaptive_half",
    "gemm_p384_q64_um8_un4_simd8_equal_split",
    "gemm_p384_q64_um16_un1_simd4_single_block",
    "gemm_p384_q64_um16_un1_simd4_fixed_q",
    "gemm_p384_q64_um16_un1_simd4_adaptive_half",
    "gemm_p384_q64_um16_un1_simd4_equal_split",
    "gemm_p384_q64_um16_un1_simd8_single_block",
    "gemm_p384_q64_um16_un1_simd8_fixed_q",
    "gemm_p384_q64_um16_un1_simd8_adaptive_half",
    "gemm_p384_q64_um16_un1_simd8_equal_split",
    "gemm_p384_q64_um16_un1_simd16_single_block",
    "gemm_p384_q64_um16_un1_simd16_fixed_q",
    "gemm_p384_q64_um16_un1_simd16_adaptive_half",
    "gemm_p384_q64_um16_un1_simd16_equal_split",
    "gemm_p384_q64_um16_un4_simd8_single_block",
    "gemm_p384_q64_um16_un4_simd8_fixed_q",
    "gemm_p384_q64_um16_un4_simd8_adaptive_half",
    "gemm_p384_q64_um16_un4_simd8_equal_split",
    "gemm_p384_q64_um16_un4_simd16_single_block",
    "gemm_p384_q64_um16_un4_simd16_fixed_q",
    "gemm_p384_q64_um16_un4_simd16_adaptive_half",
    "gemm_p384_q64_um16_un4_simd16_equal_split",
    "gemm_p384_q64_um32_un1_simd4_single_block",
    "gemm_p384_q64_um32_un1_simd4_fixed_q",
    "gemm_p384_q64_um32_un1_simd4_adaptive_half",
    "gemm_p384_q64_um32_un1_simd4_equal_split",
    "gemm_p384_q64_um32_un1_simd8_single_block",
    "gemm_p384_q64_um32_un1_simd8_fixed_q",
    "gemm_p384_q64_um32_un1_simd8_adaptive_half",
    "gemm_p384_q64_um32_un1_simd8_equal_split",
    "gemm_p384_q64_um32_un1_simd16_single_block",
    "gemm_p384_q64_um32_un1_simd16_fixed_q",
    "gemm_p384_q64_um32_un1_simd16_adaptive_half",
    "gemm_p384_q64_um32_un1_simd16_equal_split",
    "gemm_p384_q64_um32_un4_simd16_single_block",
    "gemm_p384_q64_um32_un4_simd16_fixed_q",
    "gemm_p384_q64_um32_un4_simd16_adaptive_half",
    "gemm_p384_q64_um32_un4_simd16_equal_split",
    "gemm_p384_q128_um1_un1_simd1_single_block",
    "gemm_p384_q128_um1_un1_simd1_fixed_q",
    "gemm_p384_q128_um1_un1_simd1_adaptive_half",
    "gemm_p384_q128_um1_un1_simd1_equal_split",
    "gemm_p384_q128_um1_un4_simd1_single_block",
    "gemm_p384_q128_um1_un4_simd1_fixed_q",
    "gemm_p384_q128_um1_un4_simd1_adaptive_half",
    "gemm_p384_q128_um1_un4_simd1_equal_split",
    "gemm_p384_q128_um4_un1_simd1_single_block",
    "gemm_p384_q128_um4_un1_simd1_fixed_q",
    "gemm_p384_q128_um4_un1_simd1_adaptive_half",
    "gemm_p384_q128_um4_un1_simd1_equal_split",
    "gemm_p384_q128_um4_un1_simd4_single_block",
    "gemm_p384_q128_um4_un1_simd4_fixed_q",
    "gemm_p384_q128_um4_un1_simd4_adaptive_half",
    "gemm_p384_q128_um4_un1_simd4_equal_split",
    "gemm_p384_q128_um4_un4_simd4_single_block",
    "gemm_p384_q128_um4_un4_simd4_fixed_q",
    "gemm_p384_q128_um4_un4_simd4_adaptive_half",
    "gemm_p384_q128_um4_un4_simd4_equal_split",
    "gemm_p384_q128_um8_un1_simd1_single_block",
    "gemm_p384_q128_um8_un1_simd1_fixed_q",
    "gemm_p384_q128_um8_un1_simd1_adaptive_half",
    "gemm_p384_q128_um8_un1_simd1_equal_split",
    "gemm_p384_q128_um8_un1_simd4_single_block",
    "gemm_p384_q128_um8_un1_simd4_fixed_q",
    "gemm_p384_q128_um8_un1_simd4_adaptive_half",
    "gemm_p384_q128_um8_un1_simd4_equal_split",
    "gemm_p384_q128_um8_un1_simd8_single_block",
    "gemm_p384_q128_um8_un1_simd8_fixed_q",
    "gemm_p384_q128_um8_un1_simd8_adaptive_half",
    "gemm_p384_q128_um8_un1_simd8_equal_split",
    "gemm_p384_q128_um8_un4_simd4_single_block",
    "gemm_p384_q128_um8_un4_simd4_fixed_q",
    "gemm_p384_q128_um8_un4_simd4_adaptive_half",
    "gemm_p384_q128_um8_un4_simd4_equal_split",
    "gemm_p384_q128_um8_un4_simd8_single_block",
    "gemm_p384_q128_um8_un4_simd8_fixed_q",
    "gemm_p384_q128_um8_un4_simd8_adaptive_half",
    "gemm_p384_q128_um8_un4_simd8_equal_split",
    "gemm_p384_q128_um16_un1_simd4_single_block",
    "gemm_p384_q128_um16_un1_simd4_fixed_q",
    "gemm_p384_q128_um16_un1_simd4_adaptive_half",
    "gemm_p384_q128_um16_un1_simd4_equal_split",
    "gemm_p384_q128_um16_un1_simd8_single_block",
    "gemm_p384_q128_um16_un1_simd8_fixed_q",
    "gemm_p384_q128_um16_un1_simd8_adaptive_half",
    "gemm_p384_q128_um16_un1_simd8_equal_split",
    "gemm_p384_q128_um16_un1_simd16_single_block",
    "gemm_p384_q128_um16_un1_simd16_fixed_q",
    "gemm_p384_q128_um16_un1_simd16_adaptive_half",
    "gemm_p384_q128_um16_un1_simd16_equal_split",
    "gemm_p384_q128_um16_un4_simd8_single_block",
    "gemm_p384_q128_um16_un4_simd8_fixed_q",
    "gemm_p384_q128_um16_un4_simd8_adaptive_half",
    "gemm_p384_q128_um16_un4_simd8_equal_split",
    "gemm_p384_q128_um16_un4_simd16_single_block",
    "gemm_p384_q128_um16_un4_simd16_fixed_q",
    "gemm_p384_q128_um16_un4_simd16_adaptive_half",
    "gemm_p384_q128_um16_un4_simd16_equal_split",
    "gemm_p384_q128_um32_un1_simd4_single_block",
    "gemm_p384_q128_um32_un1_simd4_fixed_q",
    "gemm_p384_q128_um32_un1_simd4_adaptive_half",
    "gemm_p384_q128_um32_un1_simd4_equal_split",
    "gemm_p384_q128_um32_un1_simd8_single_block",
    "gemm_p384_q128_um32_un1_simd8_fixed_q",
    "gemm_p384_q128_um32_un1_simd8_adaptive_half",
    "gemm_p384_q128_um32_un1_simd8_equal_split",
    "gemm_p384_q128_um32_un1_simd16_single_block",
    "gemm_p384_q128_um32_un1_simd16_fixed_q",
    "gemm_p384_q128_um32_un1_simd16_adaptive_half",
    "gemm_p384_q128_um32_un1_simd16_equal_split",
    "gemm_p384_q128_um32_un4_simd16_single_block",
    "gemm_p384_q128_um32_un4_simd16_fixed_q",
    "gemm_p384_q128_um32_un4_simd16_adaptive_half",
    "gemm_p384_q128_um32_un4_simd16_equal_split",
    "gemm_p384_q192_um1_un1_simd1_single_block",
    "gemm_p384_q192_um1_un1_simd1_fixed_q",
    "gemm_p384_q192_um1_un1_simd1_adaptive_half",
    "gemm_p384_q192_um1_un1_simd1_equal_split",
    "gemm_p384_q192_um1_un4_simd1_single_block",
    "gemm_p384_q192_um1_un4_simd1_fixed_q",
    "gemm_p384_q192_um1_un4_simd1_adaptive_half",
    "gemm_p384_q192_um1_un4_simd1_equal_split",
    "gemm_p384_q192_um4_un1_simd1_single_block",
    "gemm_p384_q192_um4_un1_simd1_fixed_q",
    "gemm_p384_q192_um4_un1_simd1_adaptive_half",
    "gemm_p384_q192_um4_un1_simd1_equal_split",
    "gemm_p384_q192_um4_un1_simd4_single_block",
    "gemm_p384_q192_um4_un1_simd4_fixed_q",
    "gemm_p384_q192_um4_un1_simd4_adaptive_half",
    "gemm_p384_q192_um4_un1_simd4_equal_split",
    "gemm_p384_q192_um4_un4_simd4_single_block",
    "gemm_p384_q192_um4_un4_simd4_fixed_q",
    "gemm_p384_q192_um4_un4_simd4_adaptive_half",
    "gemm_p384_q192_um4_un4_simd4_equal_split",
    "gemm_p384_q192_um8_un1_simd1_single_block",
    "gemm_p384_q192_um8_un1_simd1_fixed_q",
    "gemm_p384_q192_um8_un1_simd1_adaptive_half",
    "gemm_p384_q192_um8_un1_simd1_equal_split",
    "gemm_p384_q192_um8_un1_simd4_single_block",
    "gemm_p384_q192_um8_un1_simd4_fixed_q",
    "gemm_p384_q192_um8_un1_simd4_adaptive_half",
    "gemm_p384_q192_um8_un1_simd4_equal_split",
    "gemm_p384_q192_um8_un1_simd8_single_block",
    "gemm_p384_q192_um8_un1_simd8_fixed_q",
    "gemm_p384_q192_um8_un1_simd8_adaptive_half",
    "gemm_p384_q192_um8_un1_simd8_equal_split",
    "gemm_p384_q192_um8_un4_simd4_single_block",
    "gemm_p384_q192_um8_un4_simd4_fixed_q",
    "gemm_p384_q192_um8_un4_simd4_adaptive_half",
    "gemm_p384_q192_um8_un4_simd4_equal_split",
    "gemm_p384_q192_um8_un4_simd8_single_block",
    "gemm_p384_q192_um8_un4_simd8_fixed_q",
    "gemm_p384_q192_um8_un4_simd8_adaptive_half",
    "gemm_p384_q192_um8_un4_simd8_equal_split",
    "gemm_p384_q192_um16_un1_simd4_single_block",
    "gemm_p384_q192_um16_un1_simd4_fixed_q",
    "gemm_p384_q192_um16_un1_simd4_adaptive_half",
    "gemm_p384_q192_um16_un1_simd4_equal_split",
    "gemm_p384_q192_um16_un1_simd8_single_block",
    "gemm_p384_q192_um16_un1_simd8_fixed_q",
    "gemm_p384_q192_um16_un1_simd8_adaptive_half",
    "gemm_p384_q192_um16_un1_simd8_equal_split",
    "gemm_p384_q192_um16_un1_simd16_single_block",
    "gemm_p384_q192_um16_un1_simd16_fixed_q",
    "gemm_p384_q192_um16_un1_simd16_adaptive_half",
    "gemm_p384_q192_um16_un1_simd16_equal_split",
    "gemm_p384_q192_um16_un4_simd8_single_block",
    "gemm_p384_q192_um16_un4_simd8_fixed_q",
    "gemm_p384_q192_um16_un4_simd8_adaptive_half",
    "gemm_p384_q192_um16_un4_simd8_equal_split",
    "gemm_p384_q192_um16_un4_simd16_single_block",
    "gemm_p384_q192_um16_un4_simd16_fixed_q",
    "gemm_p384_q192_um16_un4_simd16_adaptive_half",
    "gemm_p384_q192_um16_un4_simd16_equal_split",
    "gemm_p384_q192_um32_un1_simd4_single_block",
    "gemm_p384_q192_um32_un1_simd4_fixed_q",
    "gemm_p384_q192_um32_un1_simd4_adaptive_half",
    "gemm_p384_q192_um32_un1_simd4_equal_split",
    "gemm_p384_q192_um32_un1_simd8_single_block",
    "gemm_p384_q192_um32_un1_simd8_fixed_q",
    "gemm_p384_q192_um32_un1_simd8_adaptive_half",
    "gemm_p384_q192_um32_un1_simd8_equal_split",
    "gemm_p384_q192_um32_un1_simd16_single_block",
    "gemm_p384_q192_um32_un1_simd16_fixed_q",
    "gemm_p384_q192_um32_un1_simd16_adaptive_half",
    "gemm_p384_q192_um32_un1_simd16_equal_split",
    "gemm_p384_q192_um32_un4_simd16_single_block",
    "gemm_p384_q192_um32_un4_simd16_fixed_q",
    "gemm_p384_q192_um32_un4_simd16_adaptive_half",
    "gemm_p384_q192_um32_un4_simd16_equal_split",
    "gemm_p384_q240_um1_un1_simd1_single_block",
    "gemm_p384_q240_um1_un1_simd1_fixed_q",
    "gemm_p384_q240_um1_un1_simd1_adaptive_half",
    "gemm_p384_q240_um1_un1_simd1_equal_split",
    "gemm_p384_q240_um1_un4_simd1_single_block",
    "gemm_p384_q240_um1_un4_simd1_fixed_q",
    "gemm_p384_q240_um1_un4_simd1_adaptive_half",
    "gemm_p384_q240_um1_un4_simd1_equal_split",
    "gemm_p384_q240_um4_un1_simd1_single_block",
    "gemm_p384_q240_um4_un1_simd1_fixed_q",
    "gemm_p384_q240_um4_un1_simd1_adaptive_half",
    "gemm_p384_q240_um4_un1_simd1_equal_split",
    "gemm_p384_q240_um4_un1_simd4_single_block",
    "gemm_p384_q240_um4_un1_simd4_fixed_q",
    "gemm_p384_q240_um4_un1_simd4_adaptive_half",
    "gemm_p384_q240_um4_un1_simd4_equal_split",
    "gemm_p384_q240_um4_un4_simd4_single_block",
    "gemm_p384_q240_um4_un4_simd4_fixed_q",
    "gemm_p384_q240_um4_un4_simd4_adaptive_half",
    "gemm_p384_q240_um4_un4_simd4_equal_split",
    "gemm_p384_q240_um8_un1_simd1_single_block",
    "gemm_p384_q240_um8_un1_simd1_fixed_q",
    "gemm_p384_q240_um8_un1_simd1_adaptive_half",
    "gemm_p384_q240_um8_un1_simd1_equal_split",
    "gemm_p384_q240_um8_un1_simd4_single_block",
    "gemm_p384_q240_um8_un1_simd4_fixed_q",
    "gemm_p384_q240_um8_un1_simd4_adaptive_half",
    "gemm_p384_q240_um8_un1_simd4_equal_split",
    "gemm_p384_q240_um8_un1_simd8_single_block",
    "gemm_p384_q240_um8_un1_simd8_fixed_q",
    "gemm_p384_q240_um8_un1_simd8_adaptive_half",
    "gemm_p384_q240_um8_un1_simd8_equal_split",
    "gemm_p384_q240_um8_un4_simd4_single_block",
    "gemm_p384_q240_um8_un4_simd4_fixed_q",
    "gemm_p384_q240_um8_un4_simd4_adaptive_half",
    "gemm_p384_q240_um8_un4_simd4_equal_split",
    "gemm_p384_q240_um8_un4_simd8_single_block",
    "gemm_p384_q240_um8_un4_simd8_fixed_q",
    "gemm_p384_q240_um8_un4_simd8_adaptive_half",
    "gemm_p384_q240_um8_un4_simd8_equal_split",
    "gemm_p384_q240_um16_un1_simd4_single_block",
    "gemm_p384_q240_um16_un1_simd4_fixed_q",
    "gemm_p384_q240_um16_un1_simd4_adaptive_half",
    "gemm_p384_q240_um16_un1_simd4_equal_split",
    "gemm_p384_q240_um16_un1_simd8_single_block",
    "gemm_p384_q240_um16_un1_simd8_fixed_q",
    "gemm_p384_q240_um16_un1_simd8_adaptive_half",
    "gemm_p384_q240_um16_un1_simd8_equal_split",
    "gemm_p384_q240_um16_un1_simd16_single_block",
    "gemm_p384_q240_um16_un1_simd16_fixed_q",
    "gemm_p384_q240_um16_un1_simd16_adaptive_half",
    "gemm_p384_q240_um16_un1_simd16_equal_split",
    "gemm_p384_q240_um16_un4_simd8_single_block",
    "gemm_p384_q240_um16_un4_simd8_fixed_q",
    "gemm_p384_q240_um16_un4_simd8_adaptive_half",
    "gemm_p384_q240_um16_un4_simd8_equal_split",
    "gemm_p384_q240_um16_un4_simd16_single_block",
    "gemm_p384_q240_um16_un4_simd16_fixed_q",
    "gemm_p384_q240_um16_un4_simd16_adaptive_half",
    "gemm_p384_q240_um16_un4_simd16_equal_split",
    "gemm_p384_q240_um32_un1_simd4_single_block",
    "gemm_p384_q240_um32_un1_simd4_fixed_q",
    "gemm_p384_q240_um32_un1_simd4_adaptive_half",
    "gemm_p384_q240_um32_un1_simd4_equal_split",
    "gemm_p384_q240_um32_un1_simd8_single_block",
    "gemm_p384_q240_um32_un1_simd8_fixed_q",
    "gemm_p384_q240_um32_un1_simd8_adaptive_half",
    "gemm_p384_q240_um32_un1_simd8_equal_split",
    "gemm_p384_q240_um32_un1_simd16_single_block",
    "gemm_p384_q240_um32_un1_simd16_fixed_q",
    "gemm_p384_q240_um32_un1_simd16_adaptive_half",
    "gemm_p384_q240_um32_un1_simd16_equal_split",
    "gemm_p384_q248_um1_un1_simd1_single_block",
    "gemm_p384_q248_um1_un1_simd1_fixed_q",
    "gemm_p384_q248_um1_un1_simd1_adaptive_half",
    "gemm_p384_q248_um1_un1_simd1_equal_split",
    "gemm_p384_q248_um1_un4_simd1_single_block",
    "gemm_p384_q248_um1_un4_simd1_fixed_q",
    "gemm_p384_q248_um1_un4_simd1_adaptive_half",
    "gemm_p384_q248_um1_un4_simd1_equal_split",
    "gemm_p384_q248_um4_un1_simd1_single_block",
    "gemm_p384_q248_um4_un1_simd1_fixed_q",
    "gemm_p384_q248_um4_un1_simd1_adaptive_half",
    "gemm_p384_q248_um4_un1_simd1_equal_split",
    "gemm_p384_q248_um4_un1_simd4_single_block",
    "gemm_p384_q248_um4_un1_simd4_fixed_q",
    "gemm_p384_q248_um4_un1_simd4_adaptive_half",
    "gemm_p384_q248_um4_un1_simd4_equal_split",
    "gemm_p384_q248_um4_un4_simd4_single_block",
    "gemm_p384_q248_um4_un4_simd4_fixed_q",
    "gemm_p384_q248_um4_un4_simd4_adaptive_half",
    "gemm_p384_q248_um4_un4_simd4_equal_split",
    "gemm_p384_q248_um8_un1_simd1_single_block",
    "gemm_p384_q248_um8_un1_simd1_fixed_q",
    "gemm_p384_q248_um8_un1_simd1_adaptive_half",
    "gemm_p384_q248_um8_un1_simd1_equal_split",
    "gemm_p384_q248_um8_un1_simd4_single_block",
    "gemm_p384_q248_um8_un1_simd4_fixed_q",
    "gemm_p384_q248_um8_un1_simd4_adaptive_half",
    "gemm_p384_q248_um8_un1_simd4_equal_split",
    "gemm_p384_q248_um8_un1_simd8_single_block",
    "gemm_p384_q248_um8_un1_simd8_fixed_q",
    "gemm_p384_q248_um8_un1_simd8_adaptive_half",
    "gemm_p384_q248_um8_un1_simd8_equal_split",
    "gemm_p384_q248_um8_un4_simd4_single_block",
    "gemm_p384_q248_um8_un4_simd4_fixed_q",
    "gemm_p384_q248_um8_un4_simd4_adaptive_half",
    "gemm_p384_q248_um8_un4_simd4_equal_split",
    "gemm_p384_q248_um8_un4_simd8_single_block",
    "gemm_p384_q248_um8_un4_simd8_fixed_q",
    "gemm_p384_q248_um8_un4_simd8_adaptive_half",
    "gemm_p384_q248_um8_un4_simd8_equal_split",
    "gemm_p384_q248_um16_un1_simd4_single_block",
    "gemm_p384_q248_um16_un1_simd4_fixed_q",
    "gemm_p384_q248_um16_un1_simd4_adaptive_half",
    "gemm_p384_q248_um16_un1_simd4_equal_split",
    "gemm_p384_q248_um16_un1_simd8_single_block",
    "gemm_p384_q248_um16_un1_simd8_fixed_q",
    "gemm_p384_q248_um16_un1_simd8_adaptive_half",
    "gemm_p384_q248_um16_un1_simd8_equal_split",
    "gemm_p384_q248_um16_un1_simd16_single_block",
    "gemm_p384_q248_um16_un1_simd16_fixed_q",
    "gemm_p384_q248_um16_un1_simd16_adaptive_half",
    "gemm_p384_q248_um16_un1_simd16_equal_split",
    "gemm_p384_q248_um16_un4_simd8_single_block",
    "gemm_p384_q248_um16_un4_simd8_fixed_q",
    "gemm_p384_q248_um16_un4_simd8_adaptive_half",
    "gemm_p384_q248_um16_un4_simd8_equal_split",
    "gemm_p384_q248_um16_un4_simd16_single_block",
    "gemm_p384_q248_um16_un4_simd16_fixed_q",
    "gemm_p384_q248_um16_un4_simd16_adaptive_half",
    "gemm_p384_q248_um16_un4_simd16_equal_split",
    "gemm_p384_q248_um32_un1_simd4_single_block",
    "gemm_p384_q248_um32_un1_simd4_fixed_q",
    "gemm_p384_q248_um32_un1_simd4_adaptive_half",
    "gemm_p384_q248_um32_un1_simd4_equal_split",
    "gemm_p384_q248_um32_un1_simd8_single_block",
    "gemm_p384_q248_um32_un1_simd8_fixed_q",
    "gemm_p384_q248_um32_un1_simd8_adaptive_half",
    "gemm_p384_q248_um32_un1_simd8_equal_split",
    "gemm_p384_q248_um32_un1_simd16_single_block",
    "gemm_p384_q248_um32_un1_simd16_fixed_q",
    "gemm_p384_q248_um32_un1_simd16_adaptive_half",
    "gemm_p384_q248_um32_un1_simd16_equal_split",
    "gemm_p384_q256_um1_un1_simd1_single_block",
    "gemm_p384_q256_um1_un1_simd1_fixed_q",
    "gemm_p384_q256_um1_un1_simd1_adaptive_half",
    "gemm_p384_q256_um1_un1_simd1_equal_split",
    "gemm_p384_q256_um1_un4_simd1_single_block",
    "gemm_p384_q256_um1_un4_simd1_fixed_q",
    "gemm_p384_q256_um1_un4_simd1_adaptive_half",
    "gemm_p384_q256_um1_un4_simd1_equal_split",
    "gemm_p384_q256_um4_un1_simd1_single_block",
    "gemm_p384_q256_um4_un1_simd1_fixed_q",
    "gemm_p384_q256_um4_un1_simd1_adaptive_half",
    "gemm_p384_q256_um4_un1_simd1_equal_split",
    "gemm_p384_q256_um4_un1_simd4_single_block",
    "gemm_p384_q256_um4_un1_simd4_fixed_q",
    "gemm_p384_q256_um4_un1_simd4_adaptive_half",
    "gemm_p384_q256_um4_un1_simd4_equal_split",
    "gemm_p384_q256_um4_un4_simd4_single_block",
    "gemm_p384_q256_um4_un4_simd4_fixed_q",
    "gemm_p384_q256_um4_un4_simd4_adaptive_half",
    "gemm_p384_q256_um4_un4_simd4_equal_split",
    "gemm_p384_q256_um8_un1_simd1_single_block",
    "gemm_p384_q256_um8_un1_simd1_fixed_q",
    "gemm_p384_q256_um8_un1_simd1_adaptive_half",
    "gemm_p384_q256_um8_un1_simd1_equal_split",
    "gemm_p384_q256_um8_un1_simd4_single_block",
    "gemm_p384_q256_um8_un1_simd4_fixed_q",
    "gemm_p384_q256_um8_un1_simd4_adaptive_half",
    "gemm_p384_q256_um8_un1_simd4_equal_split",
    "gemm_p384_q256_um8_un1_simd8_single_block",
    "gemm_p384_q256_um8_un1_simd8_fixed_q",
    "gemm_p384_q256_um8_un1_simd8_adaptive_half",
    "gemm_p384_q256_um8_un1_simd8_equal_split",
    "gemm_p384_q256_um8_un4_simd4_single_block",
    "gemm_p384_q256_um8_un4_simd4_fixed_q",
    "gemm_p384_q256_um8_un4_simd4_adaptive_half",
    "gemm_p384_q256_um8_un4_simd4_equal_split",
    "gemm_p384_q256_um8_un4_simd8_single_block",
    "gemm_p384_q256_um8_un4_simd8_fixed_q",
    "gemm_p384_q256_um8_un4_simd8_adaptive_half",
    "gemm_p384_q256_um8_un4_simd8_equal_split",
    "gemm_p384_q256_um16_un1_simd4_single_block",
    "gemm_p384_q256_um16_un1_simd4_fixed_q",
    "gemm_p384_q256_um16_un1_simd4_adaptive_half",
    "gemm_p384_q256_um16_un1_simd4_equal_split",
    "gemm_p384_q256_um16_un1_simd8_single_block",
    "gemm_p384_q256_um16_un1_simd8_fixed_q",
    "gemm_p384_q256_um16_un1_simd8_adaptive_half",
    "gemm_p384_q256_um16_un1_simd8_equal_split",
    "gemm_p384_q256_um16_un1_simd16_single_block",
    "gemm_p384_q256_um16_un1_simd16_fixed_q",
    "gemm_p384_q256_um16_un1_simd16_adaptive_half",
    "gemm_p384_q256_um16_un1_simd16_equal_split",
    "gemm_p384_q256_um16_un4_simd8_single_block",
    "gemm_p384_q256_um16_un4_simd8_fixed_q",
    "gemm_p384_q256_um16_un4_simd8_adaptive_half",
    "gemm_p384_q256_um16_un4_simd8_equal_split",
    "gemm_p384_q256_um16_un4_simd16_single_block",
    "gemm_p384_q256_um16_un4_simd16_fixed_q",
    "gemm_p384_q256_um16_un4_simd16_adaptive_half",
    "gemm_p384_q256_um16_un4_simd16_equal_split",
    "gemm_p384_q384_um1_un1_simd1_single_block",
    "gemm_p384_q384_um1_un1_simd1_fixed_q",
    "gemm_p384_q384_um1_un1_simd1_adaptive_half",
    "gemm_p384_q384_um1_un1_simd1_equal_split",
    "gemm_p384_q384_um1_un4_simd1_single_block",
    "gemm_p384_q384_um1_un4_simd1_fixed_q",
    "gemm_p384_q384_um1_un4_simd1_adaptive_half",
    "gemm_p384_q384_um1_un4_simd1_equal_split",
    "gemm_p384_q384_um4_un1_simd1_single_block",
    "gemm_p384_q384_um4_un1_simd1_fixed_q",
    "gemm_p384_q384_um4_un1_simd1_adaptive_half",
    "gemm_p384_q384_um4_un1_simd1_equal_split",
    "gemm_p384_q384_um4_un1_simd4_single_block",
    "gemm_p384_q384_um4_un1_simd4_fixed_q",
    "gemm_p384_q384_um4_un1_simd4_adaptive_half",
    "gemm_p384_q384_um4_un1_simd4_equal_split",
    "gemm_p384_q384_um4_un4_simd4_single_block",
    "gemm_p384_q384_um4_un4_simd4_fixed_q",
    "gemm_p384_q384_um4_un4_simd4_adaptive_half",
    "gemm_p384_q384_um4_un4_simd4_equal_split",
    "gemm_p384_q384_um8_un1_simd1_single_block",
    "gemm_p384_q384_um8_un1_simd1_fixed_q",
    "gemm_p384_q384_um8_un1_simd1_adaptive_half",
    "gemm_p384_q384_um8_un1_simd1_equal_split",
    "gemm_p384_q384_um8_un1_simd4_single_block",
    "gemm_p384_q384_um8_un1_simd4_fixed_q",
    "gemm_p384_q384_um8_un1_simd4_adaptive_half",
    "gemm_p384_q384_um8_un1_simd4_equal_split",
    "gemm_p384_q384_um8_un1_simd8_single_block",
    "gemm_p384_q384_um8_un1_simd8_fixed_q",
    "gemm_p384_q384_um8_un1_simd8_adaptive_half",
    "gemm_p384_q384_um8_un1_simd8_equal_split",
    "gemm_p384_q384_um8_un4_simd4_single_block",
    "gemm_p384_q384_um8_un4_simd4_fixed_q",
    "gemm_p384_q384_um8_un4_simd4_adaptive_half",
    "gemm_p384_q384_um8_un4_simd4_equal_split",
    "gemm_p384_q384_um8_un4_simd8_single_block",
    "gemm_p384_q384_um8_un4_simd8_fixed_q",
    "gemm_p384_q384_um8_un4_simd8_adaptive_half",
    "gemm_p384_q384_um8_un4_simd8_equal_split",
    "gemm_p384_q384_um16_un1_simd4_single_block",
    "gemm_p384_q384_um16_un1_simd4_fixed_q",
    "gemm_p384_q384_um16_un1_simd4_adaptive_half",
    "gemm_p384_q384_um16_un1_simd4_equal_split",
    "gemm_p384_q384_um16_un1_simd8_single_block",
    "gemm_p384_q384_um16_un1_simd8_fixed_q",
    "gemm_p384_q384_um16_un1_simd8_adaptive_half",
    "gemm_p384_q384_um16_un1_simd8_equal_split",
    "gemm_p384_q384_um16_un1_simd16_single_block",
    "gemm_p384_q384_um16_un1_simd16_fixed_q",
    "gemm_p384_q384_um16_un1_simd16_adaptive_half",
    "gemm_p384_q384_um16_un1_simd16_equal_split",
    "gemm_p384_q384_um16_un4_simd8_single_block",
    "gemm_p384_q384_um16_un4_simd8_fixed_q",
    "gemm_p384_q384_um16_un4_simd8_adaptive_half",
    "gemm_p384_q384_um16_un4_simd8_equal_split",
    "gemm_p384_q384_um16_un4_simd16_single_block",
    "gemm_p384_q384_um16_un4_simd16_fixed_q",
    "gemm_p384_q384_um16_un4_simd16_adaptive_half",
    "gemm_p384_q384_um16_un4_simd16_equal_split",
    "gemm_p384_q512_um1_un1_simd1_single_block",
    "gemm_p384_q512_um1_un1_simd1_fixed_q",
    "gemm_p384_q512_um1_un1_simd1_adaptive_half",
    "gemm_p384_q512_um1_un1_simd1_equal_split",
    "gemm_p384_q512_um1_un4_simd1_single_block",
    "gemm_p384_q512_um1_un4_simd1_fixed_q",
    "gemm_p384_q512_um1_un4_simd1_adaptive_half",
    "gemm_p384_q512_um1_un4_simd1_equal_split",
    "gemm_p384_q512_um4_un1_simd1_single_block",
    "gemm_p384_q512_um4_un1_simd1_fixed_q",
    "gemm_p384_q512_um4_un1_simd1_adaptive_half",
    "gemm_p384_q512_um4_un1_simd1_equal_split",
    "gemm_p384_q512_um4_un1_simd4_single_block",
    "gemm_p384_q512_um4_un1_simd4_fixed_q",
    "gemm_p384_q512_um4_un1_simd4_adaptive_half",
    "gemm_p384_q512_um4_un1_simd4_equal_split",
    "gemm_p384_q512_um4_un4_simd4_single_block",
    "gemm_p384_q512_um4_un4_simd4_fixed_q",
    "gemm_p384_q512_um4_un4_simd4_adaptive_half",
    "gemm_p384_q512_um4_un4_simd4_equal_split",
    "gemm_p384_q512_um8_un1_simd1_single_block",
    "gemm_p384_q512_um8_un1_simd1_fixed_q",
    "gemm_p384_q512_um8_un1_simd1_adaptive_half",
    "gemm_p384_q512_um8_un1_simd1_equal_split",
    "gemm_p384_q512_um8_un1_simd4_single_block",
    "gemm_p384_q512_um8_un1_simd4_fixed_q",
    "gemm_p384_q512_um8_un1_simd4_adaptive_half",
    "gemm_p384_q512_um8_un1_simd4_equal_split",
    "gemm_p384_q512_um8_un1_simd8_single_block",
    "gemm_p384_q512_um8_un1_simd8_fixed_q",
    "gemm_p384_q512_um8_un1_simd8_adaptive_half",
    "gemm_p384_q512_um8_un1_simd8_equal_split",
    "gemm_p384_q512_um8_un4_simd4_single_block",
    "gemm_p384_q512_um8_un4_simd4_fixed_q",
    "gemm_p384_q512_um8_un4_simd4_adaptive_half",
    "gemm_p384_q512_um8_un4_simd4_equal_split",
    "gemm_p384_q512_um8_un4_simd8_single_block",
    "gemm_p384_q512_um8_un4_simd8_fixed_q",
    "gemm_p384_q512_um8_un4_simd8_adaptive_half",
    "gemm_p384_q512_um8_un4_simd8_equal_split",
    "gemm_p512_q1_um1_un1_simd1_single_block",
    "gemm_p512_q1_um1_un1_simd1_fixed_q",
    "gemm_p512_q1_um1_un1_simd1_adaptive_half",
    "gemm_p512_q1_um1_un1_simd1_equal_split",
    "gemm_p512_q1_um1_un4_simd1_single_block",
    "gemm_p512_q1_um1_un4_simd1_fixed_q",
    "gemm_p512_q1_um1_un4_simd1_adaptive_half",
    "gemm_p512_q1_um1_un4_simd1_equal_split",
    "gemm_p512_q1_um4_un1_simd1_single_block",
    "gemm_p512_q1_um4_un1_simd1_fixed_q",
    "gemm_p512_q1_um4_un1_simd1_adaptive_half",
    "gemm_p512_q1_um4_un1_simd1_equal_split",
    "gemm_p512_q1_um4_un1_simd4_single_block",
    "gemm_p512_q1_um4_un1_simd4_fixed_q",
    "gemm_p512_q1_um4_un1_simd4_adaptive_half",
    "gemm_p512_q1_um4_un1_simd4_equal_split",
    "gemm_p512_q1_um4_un4_simd4_single_block",
    "gemm_p512_q1_um4_un4_simd4_fixed_q",
    "gemm_p512_q1_um4_un4_simd4_adaptive_half",
    "gemm_p512_q1_um4_un4_simd4_equal_split",
    "gemm_p512_q1_um8_un1_simd1_single_block",
    "gemm_p512_q1_um8_un1_simd1_fixed_q",
    "gemm_p512_q1_um8_un1_simd1_adaptive_half",
    "gemm_p512_q1_um8_un1_simd1_equal_split",
    "gemm_p512_q1_um8_un1_simd4_single_block",
    "gemm_p512_q1_um8_un1_simd4_fixed_q",
    "gemm_p512_q1_um8_un1_simd4_adaptive_half",
    "gemm_p512_q1_um8_un1_simd4_equal_split",
    "gemm_p512_q1_um8_un1_simd8_single_block",
    "gemm_p512_q1_um8_un1_simd8_fixed_q",
    "gemm_p512_q1_um8_un1_simd8_adaptive_half",
    "gemm_p512_q1_um8_un1_simd8_equal_split",
    "gemm_p512_q1_um8_un4_simd4_single_block",
    "gemm_p512_q1_um8_un4_simd4_fixed_q",
    "gemm_p512_q1_um8_un4_simd4_adaptive_half",
    "gemm_p512_q1_um8_un4_simd4_equal_split",
    "gemm_p512_q1_um8_un4_simd8_single_block",
    "gemm_p512_q1_um8_un4_simd8_fixed_q",
    "gemm_p512_q1_um8_un4_simd8_adaptive_half",
    "gemm_p512_q1_um8_un4_simd8_equal_split",
    "gemm_p512_q1_um16_un1_simd4_single_block",
    "gemm_p512_q1_um16_un1_simd4_fixed_q",
    "gemm_p512_q1_um16_un1_simd4_adaptive_half",
    "gemm_p512_q1_um16_un1_simd4_equal_split",
    "gemm_p512_q1_um16_un1_simd8_single_block",
    "gemm_p512_q1_um16_un1_simd8_fixed_q",
    "gemm_p512_q1_um16_un1_simd8_adaptive_half",
    "gemm_p512_q1_um16_un1_simd8_equal_split",
    "gemm_p512_q1_um16_un1_simd16_single_block",
    "gemm_p512_q1_um16_un1_simd16_fixed_q",
    "gemm_p512_q1_um16_un1_simd16_adaptive_half",
    "gemm_p512_q1_um16_un1_simd16_equal_split",
    "gemm_p512_q1_um16_un4_simd8_single_block",
    "gemm_p512_q1_um16_un4_simd8_fixed_q",
    "gemm_p512_q1_um16_un4_simd8_adaptive_half",
    "gemm_p512_q1_um16_un4_simd8_equal_split",
    "gemm_p512_q1_um16_un4_simd16_single_block",
    "gemm_p512_q1_um16_un4_simd16_fixed_q",
    "gemm_p512_q1_um16_un4_simd16_adaptive_half",
    "gemm_p512_q1_um16_un4_simd16_equal_split",
    "gemm_p512_q1_um32_un1_simd4_single_block",
    "gemm_p512_q1_um32_un1_simd4_fixed_q",
    "gemm_p512_q1_um32_un1_simd4_adaptive_half",
    "gemm_p512_q1_um32_un1_simd4_equal_split",
    "gemm_p512_q1_um32_un1_simd8_single_block",
    "gemm_p512_q1_um32_un1_simd8_fixed_q",
    "gemm_p512_q1_um32_un1_simd8_adaptive_half",
    "gemm_p512_q1_um32_un1_simd8_equal_split",
    "gemm_p512_q1_um32_un1_simd16_single_block",
    "gemm_p512_q1_um32_un1_simd16_fixed_q",
    "gemm_p512_q1_um32_un1_simd16_adaptive_half",
    "gemm_p512_q1_um32_un1_simd16_equal_split",
    "gemm_p512_q1_um32_un4_simd16_single_block",
    "gemm_p512_q1_um32_un4_simd16_fixed_q",
    "gemm_p512_q1_um32_un4_simd16_adaptive_half",
    "gemm_p512_q1_um32_un4_simd16_equal_split",
    "gemm_p512_q64_um1_un1_simd1_single_block",
    "gemm_p512_q64_um1_un1_simd1_fixed_q",
    "gemm_p512_q64_um1_un1_simd1_adaptive_half",
    "gemm_p512_q64_um1_un1_simd1_equal_split",
    "gemm_p512_q64_um1_un4_simd1_single_block",
    "gemm_p512_q64_um1_un4_simd1_fixed_q",
    "gemm_p512_q64_um1_un4_simd1_adaptive_half",
    "gemm_p512_q64_um1_un4_simd1_equal_split",
    "gemm_p512_q64_um4_un1_simd1_single_block",
    "gemm_p512_q64_um4_un1_simd1_fixed_q",
    "gemm_p512_q64_um4_un1_simd1_adaptive_half",
    "gemm_p512_q64_um4_un1_simd1_equal_split",
    "gemm_p512_q64_um4_un1_simd4_single_block",
    "gemm_p512_q64_um4_un1_simd4_fixed_q",
    "gemm_p512_q64_um4_un1_simd4_adaptive_half",
    "gemm_p512_q64_um4_un1_simd4_equal_split",
    "gemm_p512_q64_um4_un4_simd4_single_block",
    "gemm_p512_q64_um4_un4_simd4_fixed_q",
    "gemm_p512_q64_um4_un4_simd4_adaptive_half",
    "gemm_p512_q64_um4_un4_simd4_equal_split",
    "gemm_p512_q64_um8_un1_simd1_single_block",
    "gemm_p512_q64_um8_un1_simd1_fixed_q",
    "gemm_p512_q64_um8_un1_simd1_adaptive_half",
    "gemm_p512_q64_um8_un1_simd1_equal_split",
    "gemm_p512_q64_um8_un1_simd4_single_block",
    "gemm_p512_q64_um8_un1_simd4_fixed_q",
    "gemm_p512_q64_um8_un1_simd4_adaptive_half",
    "gemm_p512_q64_um8_un1_simd4_equal_split",
    "gemm_p512_q64_um8_un1_simd8_single_block",
    "gemm_p512_q64_um8_un1_simd8_fixed_q",
    "gemm_p512_q64_um8_un1_simd8_adaptive_half",
    "gemm_p512_q64_um8_un1_simd8_equal_split",
    "gemm_p512_q64_um8_un4_simd4_single_block",
    "gemm_p512_q64_um8_un4_simd4_fixed_q",
    "gemm_p512_q64_um8_un4_simd4_adaptive_half",
    "gemm_p512_q64_um8_un4_simd4_equal_split",
    "gemm_p512_q64_um8_un4_simd8_single_block",
    "gemm_p512_q64_um8_un4_simd8_fixed_q",
    "gemm_p512_q64_um8_un4_simd8_adaptive_half",
    "gemm_p512_q64_um8_un4_simd8_equal_split",
    "gemm_p512_q64_um16_un1_simd4_single_block",
    "gemm_p512_q64_um16_un1_simd4_fixed_q",
    "gemm_p512_q64_um16_un1_simd4_adaptive_half",
    "gemm_p512_q64_um16_un1_simd4_equal_split",
    "gemm_p512_q64_um16_un1_simd8_single_block",
    "gemm_p512_q64_um16_un1_simd8_fixed_q",
    "gemm_p512_q64_um16_un1_simd8_adaptive_half",
    "gemm_p512_q64_um16_un1_simd8_equal_split",
    "gemm_p512_q64_um16_un1_simd16_single_block",
    "gemm_p512_q64_um16_un1_simd16_fixed_q",
    "gemm_p512_q64_um16_un1_simd16_adaptive_half",
    "gemm_p512_q64_um16_un1_simd16_equal_split",
    "gemm_p512_q64_um16_un4_simd8_single_block",
    "gemm_p512_q64_um16_un4_simd8_fixed_q",
    "gemm_p512_q64_um16_un4_simd8_adaptive_half",
    "gemm_p512_q64_um16_un4_simd8_equal_split",
    "gemm_p512_q64_um16_un4_simd16_single_block",
    "gemm_p512_q64_um16_un4_simd16_fixed_q",
    "gemm_p512_q64_um16_un4_simd16_adaptive_half",
    "gemm_p512_q64_um16_un4_simd16_equal_split",
    "gemm_p512_q64_um32_un1_simd4_single_block",
    "gemm_p512_q64_um32_un1_simd4_fixed_q",
    "gemm_p512_q64_um32_un1_simd4_adaptive_half",
    "gemm_p512_q64_um32_un1_simd4_equal_split",
    "gemm_p512_q64_um32_un1_simd8_single_block",
    "gemm_p512_q64_um32_un1_simd8_fixed_q",
    "gemm_p512_q64_um32_un1_simd8_adaptive_half",
    "gemm_p512_q64_um32_un1_simd8_equal_split",
    "gemm_p512_q64_um32_un1_simd16_single_block",
    "gemm_p512_q64_um32_un1_simd16_fixed_q",
    "gemm_p512_q64_um32_un1_simd16_adaptive_half",
    "gemm_p512_q64_um32_un1_simd16_equal_split",
    "gemm_p512_q64_um32_un4_simd16_single_block",
    "gemm_p512_q64_um32_un4_simd16_fixed_q",
    "gemm_p512_q64_um32_un4_simd16_adaptive_half",
    "gemm_p512_q64_um32_un4_simd16_equal_split",
    "gemm_p512_q128_um1_un1_simd1_single_block",
    "gemm_p512_q128_um1_un1_simd1_fixed_q",
    "gemm_p512_q128_um1_un1_simd1_adaptive_half",
    "gemm_p512_q128_um1_un1_simd1_equal_split",
    "gemm_p512_q128_um1_un4_simd1_single_block",
    "gemm_p512_q128_um1_un4_simd1_fixed_q",
    "gemm_p512_q128_um1_un4_simd1_adaptive_half",
    "gemm_p512_q128_um1_un4_simd1_equal_split",
    "gemm_p512_q128_um4_un1_simd1_single_block",
    "gemm_p512_q128_um4_un1_simd1_fixed_q",
    "gemm_p512_q128_um4_un1_simd1_adaptive_half",
    "gemm_p512_q128_um4_un1_simd1_equal_split",
    "gemm_p512_q128_um4_un1_simd4_single_block",
    "gemm_p512_q128_um4_un1_simd4_fixed_q",
    "gemm_p512_q128_um4_un1_simd4_adaptive_half",
    "gemm_p512_q128_um4_un1_simd4_equal_split",
    "gemm_p512_q128_um4_un4_simd4_single_block",
    "gemm_p512_q128_um4_un4_simd4_fixed_q",
    "gemm_p512_q128_um4_un4_simd4_adaptive_half",
    "gemm_p512_q128_um4_un4_simd4_equal_split",
    "gemm_p512_q128_um8_un1_simd1_single_block",
    "gemm_p512_q128_um8_un1_simd1_fixed_q",
    "gemm_p512_q128_um8_un1_simd1_adaptive_half",
    "gemm_p512_q128_um8_un1_simd1_equal_split",
    "gemm_p512_q128_um8_un1_simd4_single_block",
    "gemm_p512_q128_um8_un1_simd4_fixed_q",
    "gemm_p512_q128_um8_un1_simd4_adaptive_half",
    "gemm_p512_q128_um8_un1_simd4_equal_split",
    "gemm_p512_q128_um8_un1_simd8_single_block",
    "gemm_p512_q128_um8_un1_simd8_fixed_q",
    "gemm_p512_q128_um8_un1_simd8_adaptive_half",
    "gemm_p512_q128_um8_un1_simd8_equal_split",
    "gemm_p512_q128_um8_un4_simd4_single_block",
    "gemm_p512_q128_um8_un4_simd4_fixed_q",
    "gemm_p512_q128_um8_un4_simd4_adaptive_half",
    "gemm_p512_q128_um8_un4_simd4_equal_split",
    "gemm_p512_q128_um8_un4_simd8_single_block",
    "gemm_p512_q128_um8_un4_simd8_fixed_q",
    "gemm_p512_q128_um8_un4_simd8_adaptive_half",
    "gemm_p512_q128_um8_un4_simd8_equal_split",
    "gemm_p512_q128_um16_un1_simd4_single_block",
    "gemm_p512_q128_um16_un1_simd4_fixed_q",
    "gemm_p512_q128_um16_un1_simd4_adaptive_half",
    "gemm_p512_q128_um16_un1_simd4_equal_split",
    "gemm_p512_q128_um16_un1_simd8_single_block",
    "gemm_p512_q128_um16_un1_simd8_fixed_q",
    "gemm_p512_q128_um16_un1_simd8_adaptive_half",
    "gemm_p512_q128_um16_un1_simd8_equal_split",
    "gemm_p512_q128_um16_un1_simd16_single_block",
    "gemm_p512_q128_um16_un1_simd16_fixed_q",
    "gemm_p512_q128_um16_un1_simd16_adaptive_half",
    "gemm_p512_q128_um16_un1_simd16_equal_split",
    "gemm_p512_q128_um16_un4_simd8_single_block",
    "gemm_p512_q128_um16_un4_simd8_fixed_q",
    "gemm_p512_q128_um16_un4_simd8_adaptive_half",
    "gemm_p512_q128_um16_un4_simd8_equal_split",
    "gemm_p512_q128_um16_un4_simd16_single_block",
    "gemm_p512_q128_um16_un4_simd16_fixed_q",
    "gemm_p512_q128_um16_un4_simd16_adaptive_half",
    "gemm_p512_q128_um16_un4_simd16_equal_split",
    "gemm_p512_q128_um32_un1_simd4_single_block",
    "gemm_p512_q128_um32_un1_simd4_fixed_q",
    "gemm_p512_q128_um32_un1_simd4_adaptive_half",
    "gemm_p512_q128_um32_un1_simd4_equal_split",
    "gemm_p512_q128_um32_un1_simd8_single_block",
    "gemm_p512_q128_um32_un1_simd8_fixed_q",
    "gemm_p512_q128_um32_un1_simd8_adaptive_half",
    "gemm_p512_q128_um32_un1_simd8_equal_split",
    "gemm_p512_q128_um32_un1_simd16_single_block",
    "gemm_p512_q128_um32_un1_simd16_fixed_q",
    "gemm_p512_q128_um32_un1_simd16_adaptive_half",
    "gemm_p512_q128_um32_un1_simd16_equal_split",
    "gemm_p512_q128_um32_un4_simd16_single_block",
    "gemm_p512_q128_um32_un4_simd16_fixed_q",
    "gemm_p512_q128_um32_un4_simd16_adaptive_half",
    "gemm_p512_q128_um32_un4_simd16_equal_split",
    "gemm_p512_q192_um1_un1_simd1_single_block",
    "gemm_p512_q192_um1_un1_simd1_fixed_q",
    "gemm_p512_q192_um1_un1_simd1_adaptive_half",
    "gemm_p512_q192_um1_un1_simd1_equal_split",
    "gemm_p512_q192_um1_un4_simd1_single_block",
    "gemm_p512_q192_um1_un4_simd1_fixed_q",
    "gemm_p512_q192_um1_un4_simd1_adaptive_half",
    "gemm_p512_q192_um1_un4_simd1_equal_split",
    "gemm_p512_q192_um4_un1_simd1_single_block",
    "gemm_p512_q192_um4_un1_simd1_fixed_q",
    "gemm_p512_q192_um4_un1_simd1_adaptive_half",
    "gemm_p512_q192_um4_un1_simd1_equal_split",
    "gemm_p512_q192_um4_un1_simd4_single_block",
    "gemm_p512_q192_um4_un1_simd4_fixed_q",
    "gemm_p512_q192_um4_un1_simd4_adaptive_half",
    "gemm_p512_q192_um4_un1_simd4_equal_split",
    "gemm_p512_q192_um4_un4_simd4_single_block",
    "gemm_p512_q192_um4_un4_simd4_fixed_q",
    "gemm_p512_q192_um4_un4_simd4_adaptive_half",
    "gemm_p512_q192_um4_un4_simd4_equal_split",
    "gemm_p512_q192_um8_un1_simd1_single_block",
    "gemm_p512_q192_um8_un1_simd1_fixed_q",
    "gemm_p512_q192_um8_un1_simd1_adaptive_half",
    "gemm_p512_q192_um8_un1_simd1_equal_split",
    "gemm_p512_q192_um8_un1_simd4_single_block",
    "gemm_p512_q192_um8_un1_simd4_fixed_q",
    "gemm_p512_q192_um8_un1_simd4_adaptive_half",
    "gemm_p512_q192_um8_un1_simd4_equal_split",
    "gemm_p512_q192_um8_un1_simd8_single_block",
    "gemm_p512_q192_um8_un1_simd8_fixed_q",
    "gemm_p512_q192_um8_un1_simd8_adaptive_half",
    "gemm_p512_q192_um8_un1_simd8_equal_split",
    "gemm_p512_q192_um8_un4_simd4_single_block",
    "gemm_p512_q192_um8_un4_simd4_fixed_q",
    "gemm_p512_q192_um8_un4_simd4_adaptive_half",
    "gemm_p512_q192_um8_un4_simd4_equal_split",
    "gemm_p512_q192_um8_un4_simd8_single_block",
    "gemm_p512_q192_um8_un4_simd8_fixed_q",
    "gemm_p512_q192_um8_un4_simd8_adaptive_half",
    "gemm_p512_q192_um8_un4_simd8_equal_split",
    "gemm_p512_q192_um16_un1_simd4_single_block",
    "gemm_p512_q192_um16_un1_simd4_fixed_q",
    "gemm_p512_q192_um16_un1_simd4_adaptive_half",
    "gemm_p512_q192_um16_un1_simd4_equal_split",
    "gemm_p512_q192_um16_un1_simd8_single_block",
    "gemm_p512_q192_um16_un1_simd8_fixed_q",
    "gemm_p512_q192_um16_un1_simd8_adaptive_half",
    "gemm_p512_q192_um16_un1_simd8_equal_split",
    "gemm_p512_q192_um16_un1_simd16_single_block",
    "gemm_p512_q192_um16_un1_simd16_fixed_q",
    "gemm_p512_q192_um16_un1_simd16_adaptive_half",
    "gemm_p512_q192_um16_un1_simd16_equal_split",
    "gemm_p512_q192_um16_un4_simd8_single_block",
    "gemm_p512_q192_um16_un4_simd8_fixed_q",
    "gemm_p512_q192_um16_un4_simd8_adaptive_half",
    "gemm_p512_q192_um16_un4_simd8_equal_split",
    "gemm_p512_q192_um16_un4_simd16_single_block",
    "gemm_p512_q192_um16_un4_simd16_fixed_q",
    "gemm_p512_q192_um16_un4_simd16_adaptive_half",
    "gemm_p512_q192_um16_un4_simd16_equal_split",
    "gemm_p512_q192_um32_un1_simd4_single_block",
    "gemm_p512_q192_um32_un1_simd4_fixed_q",
    "gemm_p512_q192_um32_un1_simd4_adaptive_half",
    "gemm_p512_q192_um32_un1_simd4_equal_split",
    "gemm_p512_q192_um32_un1_simd8_single_block",
    "gemm_p512_q192_um32_un1_simd8_fixed_q",
    "gemm_p512_q192_um32_un1_simd8_adaptive_half",
    "gemm_p512_q192_um32_un1_simd8_equal_split",
    "gemm_p512_q192_um32_un1_simd16_single_block",
    "gemm_p512_q192_um32_un1_simd16_fixed_q",
    "gemm_p512_q192_um32_un1_simd16_adaptive_half",
    "gemm_p512_q192_um32_un1_simd16_equal_split",
    "gemm_p512_q192_um32_un4_simd16_single_block",
    "gemm_p512_q192_um32_un4_simd16_fixed_q",
    "gemm_p512_q192_um32_un4_simd16_adaptive_half",
    "gemm_p512_q192_um32_un4_simd16_equal_split",
    "gemm_p512_q240_um1_un1_simd1_single_block",
    "gemm_p512_q240_um1_un1_simd1_fixed_q",
    "gemm_p512_q240_um1_un1_simd1_adaptive_half",
    "gemm_p512_q240_um1_un1_simd1_equal_split",
    "gemm_p512_q240_um1_un4_simd1_single_block",
    "gemm_p512_q240_um1_un4_simd1_fixed_q",
    "gemm_p512_q240_um1_un4_simd1_adaptive_half",
    "gemm_p512_q240_um1_un4_simd1_equal_split",
    "gemm_p512_q240_um4_un1_simd1_single_block",
    "gemm_p512_q240_um4_un1_simd1_fixed_q",
    "gemm_p512_q240_um4_un1_simd1_adaptive_half",
    "gemm_p512_q240_um4_un1_simd1_equal_split",
    "gemm_p512_q240_um4_un1_simd4_single_block",
    "gemm_p512_q240_um4_un1_simd4_fixed_q",
    "gemm_p512_q240_um4_un1_simd4_adaptive_half",
    "gemm_p512_q240_um4_un1_simd4_equal_split",
    "gemm_p512_q240_um4_un4_simd4_single_block",
    "gemm_p512_q240_um4_un4_simd4_fixed_q",
    "gemm_p512_q240_um4_un4_simd4_adaptive_half",
    "gemm_p512_q240_um4_un4_simd4_equal_split",
    "gemm_p512_q240_um8_un1_simd1_single_block",
    "gemm_p512_q240_um8_un1_simd1_fixed_q",
    "gemm_p512_q240_um8_un1_simd1_adaptive_half",
    "gemm_p512_q240_um8_un1_simd1_equal_split",
    "gemm_p512_q240_um8_un1_simd4_single_block",
    "gemm_p512_q240_um8_un1_simd4_fixed_q",
    "gemm_p512_q240_um8_un1_simd4_adaptive_half",
    "gemm_p512_q240_um8_un1_simd4_equal_split",
    "gemm_p512_q240_um8_un1_simd8_single_block",
    "gemm_p512_q240_um8_un1_simd8_fixed_q",
    "gemm_p512_q240_um8_un1_simd8_adaptive_half",
    "gemm_p512_q240_um8_un1_simd8_equal_split",
    "gemm_p512_q240_um8_un4_simd4_single_block",
    "gemm_p512_q240_um8_un4_simd4_fixed_q",
    "gemm_p512_q240_um8_un4_simd4_adaptive_half",
    "gemm_p512_q240_um8_un4_simd4_equal_split",
    "gemm_p512_q240_um8_un4_simd8_single_block",
    "gemm_p512_q240_um8_un4_simd8_fixed_q",
    "gemm_p512_q240_um8_un4_simd8_adaptive_half",
    "gemm_p512_q240_um8_un4_simd8_equal_split",
    "gemm_p512_q240_um16_un1_simd4_single_block",
    "gemm_p512_q240_um16_un1_simd4_fixed_q",
    "gemm_p512_q240_um16_un1_simd4_adaptive_half",
    "gemm_p512_q240_um16_un1_simd4_equal_split",
    "gemm_p512_q240_um16_un1_simd8_single_block",
    "gemm_p512_q240_um16_un1_simd8_fixed_q",
    "gemm_p512_q240_um16_un1_simd8_adaptive_half",
    "gemm_p512_q240_um16_un1_simd8_equal_split",
    "gemm_p512_q240_um16_un1_simd16_single_block",
    "gemm_p512_q240_um16_un1_simd16_fixed_q",
    "gemm_p512_q240_um16_un1_simd16_adaptive_half",
    "gemm_p512_q240_um16_un1_simd16_equal_split",
    "gemm_p512_q240_um16_un4_simd8_single_block",
    "gemm_p512_q240_um16_un4_simd8_fixed_q",
    "gemm_p512_q240_um16_un4_simd8_adaptive_half",
    "gemm_p512_q240_um16_un4_simd8_equal_split",
    "gemm_p512_q240_um16_un4_simd16_single_block",
    "gemm_p512_q240_um16_un4_simd16_fixed_q",
    "gemm_p512_q240_um16_un4_simd16_adaptive_half",
    "gemm_p512_q240_um16_un4_simd16_equal_split",
    "gemm_p512_q240_um32_un1_simd4_single_block",
    "gemm_p512_q240_um32_un1_simd4_fixed_q",
    "gemm_p512_q240_um32_un1_simd4_adaptive_half",
    "gemm_p512_q240_um32_un1_simd4_equal_split",
    "gemm_p512_q240_um32_un1_simd8_single_block",
    "gemm_p512_q240_um32_un1_simd8_fixed_q",
    "gemm_p512_q240_um32_un1_simd8_adaptive_half",
    "gemm_p512_q240_um32_un1_simd8_equal_split",
    "gemm_p512_q240_um32_un1_simd16_single_block",
    "gemm_p512_q240_um32_un1_simd16_fixed_q",
    "gemm_p512_q240_um32_un1_simd16_adaptive_half",
    "gemm_p512_q240_um32_un1_simd16_equal_split",
    "gemm_p512_q248_um1_un1_simd1_single_block",
    "gemm_p512_q248_um1_un1_simd1_fixed_q",
    "gemm_p512_q248_um1_un1_simd1_adaptive_half",
    "gemm_p512_q248_um1_un1_simd1_equal_split",
    "gemm_p512_q248_um1_un4_simd1_single_block",
    "gemm_p512_q248_um1_un4_simd1_fixed_q",
    "gemm_p512_q248_um1_un4_simd1_adaptive_half",
    "gemm_p512_q248_um1_un4_simd1_equal_split",
    "gemm_p512_q248_um4_un1_simd1_single_block",
    "gemm_p512_q248_um4_un1_simd1_fixed_q",
    "gemm_p512_q248_um4_un1_simd1_adaptive_half",
    "gemm_p512_q248_um4_un1_simd1_equal_split",
    "gemm_p512_q248_um4_un1_simd4_single_block",
    "gemm_p512_q248_um4_un1_simd4_fixed_q",
    "gemm_p512_q248_um4_un1_simd4_adaptive_half",
    "gemm_p512_q248_um4_un1_simd4_equal_split",
    "gemm_p512_q248_um4_un4_simd4_single_block",
    "gemm_p512_q248_um4_un4_simd4_fixed_q",
    "gemm_p512_q248_um4_un4_simd4_adaptive_half",
    "gemm_p512_q248_um4_un4_simd4_equal_split",
    "gemm_p512_q248_um8_un1_simd1_single_block",
    "gemm_p512_q248_um8_un1_simd1_fixed_q",
    "gemm_p512_q248_um8_un1_simd1_adaptive_half",
    "gemm_p512_q248_um8_un1_simd1_equal_split",
    "gemm_p512_q248_um8_un1_simd4_single_block",
    "gemm_p512_q248_um8_un1_simd4_fixed_q",
    "gemm_p512_q248_um8_un1_simd4_adaptive_half",
    "gemm_p512_q248_um8_un1_simd4_equal_split",
    "gemm_p512_q248_um8_un1_simd8_single_block",
    "gemm_p512_q248_um8_un1_simd8_fixed_q",
    "gemm_p512_q248_um8_un1_simd8_adaptive_half",
    "gemm_p512_q248_um8_un1_simd8_equal_split",
    "gemm_p512_q248_um8_un4_simd4_single_block",
    "gemm_p512_q248_um8_un4_simd4_fixed_q",
    "gemm_p512_q248_um8_un4_simd4_adaptive_half",
    "gemm_p512_q248_um8_un4_simd4_equal_split",
    "gemm_p512_q248_um8_un4_simd8_single_block",
    "gemm_p512_q248_um8_un4_simd8_fixed_q",
    "gemm_p512_q248_um8_un4_simd8_adaptive_half",
    "gemm_p512_q248_um8_un4_simd8_equal_split",
    "gemm_p512_q248_um16_un1_simd4_single_block",
    "gemm_p512_q248_um16_un1_simd4_fixed_q",
    "gemm_p512_q248_um16_un1_simd4_adaptive_half",
    "gemm_p512_q248_um16_un1_simd4_equal_split",
    "gemm_p512_q248_um16_un1_simd8_single_block",
    "gemm_p512_q248_um16_un1_simd8_fixed_q",
    "gemm_p512_q248_um16_un1_simd8_adaptive_half",
    "gemm_p512_q248_um16_un1_simd8_equal_split",
    "gemm_p512_q248_um16_un1_simd16_single_block",
    "gemm_p512_q248_um16_un1_simd16_fixed_q",
    "gemm_p512_q248_um16_un1_simd16_adaptive_half",
    "gemm_p512_q248_um16_un1_simd16_equal_split",
    "gemm_p512_q248_um16_un4_simd8_single_block",
    "gemm_p512_q248_um16_un4_simd8_fixed_q",
    "gemm_p512_q248_um16_un4_simd8_adaptive_half",
    "gemm_p512_q248_um16_un4_simd8_equal_split",
    "gemm_p512_q248_um16_un4_simd16_single_block",
    "gemm_p512_q248_um16_un4_simd16_fixed_q",
    "gemm_p512_q248_um16_un4_simd16_adaptive_half",
    "gemm_p512_q248_um16_un4_simd16_equal_split",
    "gemm_p512_q248_um32_un1_simd4_single_block",
    "gemm_p512_q248_um32_un1_simd4_fixed_q",
    "gemm_p512_q248_um32_un1_simd4_adaptive_half",
    "gemm_p512_q248_um32_un1_simd4_equal_split",
    "gemm_p512_q248_um32_un1_simd8_single_block",
    "gemm_p512_q248_um32_un1_simd8_fixed_q",
    "gemm_p512_q248_um32_un1_simd8_adaptive_half",
    "gemm_p512_q248_um32_un1_simd8_equal_split",
    "gemm_p512_q248_um32_un1_simd16_single_block",
    "gemm_p512_q248_um32_un1_simd16_fixed_q",
    "gemm_p512_q248_um32_un1_simd16_adaptive_half",
    "gemm_p512_q248_um32_un1_simd16_equal_split",
    "gemm_p512_q256_um1_un1_simd1_single_block",
    "gemm_p512_q256_um1_un1_simd1_fixed_q",
    "gemm_p512_q256_um1_un1_simd1_adaptive_half",
    "gemm_p512_q256_um1_un1_simd1_equal_split",
    "gemm_p512_q256_um1_un4_simd1_single_block",
    "gemm_p512_q256_um1_un4_simd1_fixed_q",
    "gemm_p512_q256_um1_un4_simd1_adaptive_half",
    "gemm_p512_q256_um1_un4_simd1_equal_split",
    "gemm_p512_q256_um4_un1_simd1_single_block",
    "gemm_p512_q256_um4_un1_simd1_fixed_q",
    "gemm_p512_q256_um4_un1_simd1_adaptive_half",
    "gemm_p512_q256_um4_un1_simd1_equal_split",
    "gemm_p512_q256_um4_un1_simd4_single_block",
    "gemm_p512_q256_um4_un1_simd4_fixed_q",
    "gemm_p512_q256_um4_un1_simd4_adaptive_half",
    "gemm_p512_q256_um4_un1_simd4_equal_split",
    "gemm_p512_q256_um4_un4_simd4_single_block",
    "gemm_p512_q256_um4_un4_simd4_fixed_q",
    "gemm_p512_q256_um4_un4_simd4_adaptive_half",
    "gemm_p512_q256_um4_un4_simd4_equal_split",
    "gemm_p512_q256_um8_un1_simd1_single_block",
    "gemm_p512_q256_um8_un1_simd1_fixed_q",
    "gemm_p512_q256_um8_un1_simd1_adaptive_half",
    "gemm_p512_q256_um8_un1_simd1_equal_split",
    "gemm_p512_q256_um8_un1_simd4_single_block",
    "gemm_p512_q256_um8_un1_simd4_fixed_q",
    "gemm_p512_q256_um8_un1_simd4_adaptive_half",
    "gemm_p512_q256_um8_un1_simd4_equal_split",
    "gemm_p512_q256_um8_un1_simd8_single_block",
    "gemm_p512_q256_um8_un1_simd8_fixed_q",
    "gemm_p512_q256_um8_un1_simd8_adaptive_half",
    "gemm_p512_q256_um8_un1_simd8_equal_split",
    "gemm_p512_q256_um8_un4_simd4_single_block",
    "gemm_p512_q256_um8_un4_simd4_fixed_q",
    "gemm_p512_q256_um8_un4_simd4_adaptive_half",
    "gemm_p512_q256_um8_un4_simd4_equal_split",
    "gemm_p512_q256_um8_un4_simd8_single_block",
    "gemm_p512_q256_um8_un4_simd8_fixed_q",
    "gemm_p512_q256_um8_un4_simd8_adaptive_half",
    "gemm_p512_q256_um8_un4_simd8_equal_split",
    "gemm_p512_q256_um16_un1_simd4_single_block",
    "gemm_p512_q256_um16_un1_simd4_fixed_q",
    "gemm_p512_q256_um16_un1_simd4_adaptive_half",
    "gemm_p512_q256_um16_un1_simd4_equal_split",
    "gemm_p512_q256_um16_un1_simd8_single_block",
    "gemm_p512_q256_um16_un1_simd8_fixed_q",
    "gemm_p512_q256_um16_un1_simd8_adaptive_half",
    "gemm_p512_q256_um16_un1_simd8_equal_split",
    "gemm_p512_q256_um16_un1_simd16_single_block",
    "gemm_p512_q256_um16_un1_simd16_fixed_q",
    "gemm_p512_q256_um16_un1_simd16_adaptive_half",
    "gemm_p512_q256_um16_un1_simd16_equal_split",
    "gemm_p512_q256_um16_un4_simd8_single_block",
    "gemm_p512_q256_um16_un4_simd8_fixed_q",
    "gemm_p512_q256_um16_un4_simd8_adaptive_half",
    "gemm_p512_q256_um16_un4_simd8_equal_split",
    "gemm_p512_q256_um16_un4_simd16_single_block",
    "gemm_p512_q256_um16_un4_simd16_fixed_q",
    "gemm_p512_q256_um16_un4_simd16_adaptive_half",
    "gemm_p512_q256_um16_un4_simd16_equal_split",
    "gemm_p512_q384_um1_un1_simd1_single_block",
    "gemm_p512_q384_um1_un1_simd1_fixed_q",
    "gemm_p512_q384_um1_un1_simd1_adaptive_half",
    "gemm_p512_q384_um1_un1_simd1_equal_split",
    "gemm_p512_q384_um1_un4_simd1_single_block",
    "gemm_p512_q384_um1_un4_simd1_fixed_q",
    "gemm_p512_q384_um1_un4_simd1_adaptive_half",
    "gemm_p512_q384_um1_un4_simd1_equal_split",
    "gemm_p512_q384_um4_un1_simd1_single_block",
    "gemm_p512_q384_um4_un1_simd1_fixed_q",
    "gemm_p512_q384_um4_un1_simd1_adaptive_half",
    "gemm_p512_q384_um4_un1_simd1_equal_split",
    "gemm_p512_q384_um4_un1_simd4_single_block",
    "gemm_p512_q384_um4_un1_simd4_fixed_q",
    "gemm_p512_q384_um4_un1_simd4_adaptive_half",
    "gemm_p512_q384_um4_un1_simd4_equal_split",
    "gemm_p512_q384_um4_un4_simd4_single_block",
    "gemm_p512_q384_um4_un4_simd4_fixed_q",
    "gemm_p512_q384_um4_un4_simd4_adaptive_half",
    "gemm_p512_q384_um4_un4_simd4_equal_split",
    "gemm_p512_q384_um8_un1_simd1_single_block",
    "gemm_p512_q384_um8_un1_simd1_fixed_q",
    "gemm_p512_q384_um8_un1_simd1_adaptive_half",
    "gemm_p512_q384_um8_un1_simd1_equal_split",
    "gemm_p512_q384_um8_un1_simd4_single_block",
    "gemm_p512_q384_um8_un1_simd4_fixed_q",
    "gemm_p512_q384_um8_un1_simd4_adaptive_half",
    "gemm_p512_q384_um8_un1_simd4_equal_split",
    "gemm_p512_q384_um8_un1_simd8_single_block",
    "gemm_p512_q384_um8_un1_simd8_fixed_q",
    "gemm_p512_q384_um8_un1_simd8_adaptive_half",
    "gemm_p512_q384_um8_un1_simd8_equal_split",
    "gemm_p512_q384_um8_un4_simd4_single_block",
    "gemm_p512_q384_um8_un4_simd4_fixed_q",
    "gemm_p512_q384_um8_un4_simd4_adaptive_half",
    "gemm_p512_q384_um8_un4_simd4_equal_split",
    "gemm_p512_q384_um8_un4_simd8_single_block",
    "gemm_p512_q384_um8_un4_simd8_fixed_q",
    "gemm_p512_q384_um8_un4_simd8_adaptive_half",
    "gemm_p512_q384_um8_un4_simd8_equal_split",
    "gemm_p512_q384_um16_un1_simd4_single_block",
    "gemm_p512_q384_um16_un1_simd4_fixed_q",
    "gemm_p512_q384_um16_un1_simd4_adaptive_half",
    "gemm_p512_q384_um16_un1_simd4_equal_split",
    "gemm_p512_q384_um16_un1_simd8_single_block",
    "gemm_p512_q384_um16_un1_simd8_fixed_q",
    "gemm_p512_q384_um16_un1_simd8_adaptive_half",
    "gemm_p512_q384_um16_un1_simd8_equal_split",
    "gemm_p512_q384_um16_un1_simd16_single_block",
    "gemm_p512_q384_um16_un1_simd16_fixed_q",
    "gemm_p512_q384_um16_un1_simd16_adaptive_half",
    "gemm_p512_q384_um16_un1_simd16_equal_split",
    "gemm_p512_q384_um16_un4_simd8_single_block",
    "gemm_p512_q384_um16_un4_simd8_fixed_q",
    "gemm_p512_q384_um16_un4_simd8_adaptive_half",
    "gemm_p512_q384_um16_un4_simd8_equal_split",
    "gemm_p512_q384_um16_un4_simd16_single_block",
    "gemm_p512_q384_um16_un4_simd16_fixed_q",
    "gemm_p512_q384_um16_un4_simd16_adaptive_half",
    "gemm_p512_q384_um16_un4_simd16_equal_split",
    "gemm_p512_q512_um1_un1_simd1_single_block",
    "gemm_p512_q512_um1_un1_simd1_fixed_q",
    "gemm_p512_q512_um1_un1_simd1_adaptive_half",
    "gemm_p512_q512_um1_un1_simd1_equal_split",
    "gemm_p512_q512_um1_un4_simd1_single_block",
    "gemm_p512_q512_um1_un4_simd1_fixed_q",
    "gemm_p512_q512_um1_un4_simd1_adaptive_half",
    "gemm_p512_q512_um1_un4_simd1_equal_split",
    "gemm_p512_q512_um4_un1_simd1_single_block",
    "gemm_p512_q512_um4_un1_simd1_fixed_q",
    "gemm_p512_q512_um4_un1_simd1_adaptive_half",
    "gemm_p512_q512_um4_un1_simd1_equal_split",
    "gemm_p512_q512_um4_un1_simd4_single_block",
    "gemm_p512_q512_um4_un1_simd4_fixed_q",
    "gemm_p512_q512_um4_un1_simd4_adaptive_half",
    "gemm_p512_q512_um4_un1_simd4_equal_split",
    "gemm_p512_q512_um4_un4_simd4_single_block",
    "gemm_p512_q512_um4_un4_simd4_fixed_q",
    "gemm_p512_q512_um4_un4_simd4_adaptive_half",
    "gemm_p512_q512_um4_un4_simd4_equal_split",
    "gemm_p512_q512_um8_un1_simd1_single_block",
    "gemm_p512_q512_um8_un1_simd1_fixed_q",
    "gemm_p512_q512_um8_un1_simd1_adaptive_half",
    "gemm_p512_q512_um8_un1_simd1_equal_split",
    "gemm_p512_q512_um8_un1_simd4_single_block",
    "gemm_p512_q512_um8_un1_simd4_fixed_q",
    "gemm_p512_q512_um8_un1_simd4_adaptive_half",
    "gemm_p512_q512_um8_un1_simd4_equal_split",
    "gemm_p512_q512_um8_un1_simd8_single_block",
    "gemm_p512_q512_um8_un1_simd8_fixed_q",
    "gemm_p512_q512_um8_un1_simd8_adaptive_half",
    "gemm_p512_q512_um8_un1_simd8_equal_split",
    "gemm_p512_q512_um8_un4_simd4_single_block",
    "gemm_p512_q512_um8_un4_simd4_fixed_q",
    "gemm_p512_q512_um8_un4_simd4_adaptive_half",
    "gemm_p512_q512_um8_un4_simd4_equal_split",
    "gemm_p512_q512_um8_un4_simd8_single_block",
    "gemm_p512_q512_um8_un4_simd8_fixed_q",
    "gemm_p512_q512_um8_un4_simd8_adaptive_half",
    "gemm_p512_q512_um8_un4_simd8_equal_split",
    "gemm_p768_q1_um1_un1_simd1_single_block",
    "gemm_p768_q1_um1_un1_simd1_fixed_q",
    "gemm_p768_q1_um1_un1_simd1_adaptive_half",
    "gemm_p768_q1_um1_un1_simd1_equal_split",
    "gemm_p768_q1_um1_un4_simd1_single_block",
    "gemm_p768_q1_um1_un4_simd1_fixed_q",
    "gemm_p768_q1_um1_un4_simd1_adaptive_half",
    "gemm_p768_q1_um1_un4_simd1_equal_split",
    "gemm_p768_q1_um4_un1_simd1_single_block",
    "gemm_p768_q1_um4_un1_simd1_fixed_q",
    "gemm_p768_q1_um4_un1_simd1_adaptive_half",
    "gemm_p768_q1_um4_un1_simd1_equal_split",
    "gemm_p768_q1_um4_un1_simd4_single_block",
    "gemm_p768_q1_um4_un1_simd4_fixed_q",
    "gemm_p768_q1_um4_un1_simd4_adaptive_half",
    "gemm_p768_q1_um4_un1_simd4_equal_split",
    "gemm_p768_q1_um4_un4_simd4_single_block",
    "gemm_p768_q1_um4_un4_simd4_fixed_q",
    "gemm_p768_q1_um4_un4_simd4_adaptive_half",
    "gemm_p768_q1_um4_un4_simd4_equal_split",
    "gemm_p768_q1_um8_un1_simd1_single_block",
    "gemm_p768_q1_um8_un1_simd1_fixed_q",
    "gemm_p768_q1_um8_un1_simd1_adaptive_half",
    "gemm_p768_q1_um8_un1_simd1_equal_split",
    "gemm_p768_q1_um8_un1_simd4_single_block",
    "gemm_p768_q1_um8_un1_simd4_fixed_q",
    "gemm_p768_q1_um8_un1_simd4_adaptive_half",
    "gemm_p768_q1_um8_un1_simd4_equal_split",
    "gemm_p768_q1_um8_un1_simd8_single_block",
    "gemm_p768_q1_um8_un1_simd8_fixed_q",
    "gemm_p768_q1_um8_un1_simd8_adaptive_half",
    "gemm_p768_q1_um8_un1_simd8_equal_split",
    "gemm_p768_q1_um8_un4_simd4_single_block",
    "gemm_p768_q1_um8_un4_simd4_fixed_q",
    "gemm_p768_q1_um8_un4_simd4_adaptive_half",
    "gemm_p768_q1_um8_un4_simd4_equal_split",
    "gemm_p768_q1_um8_un4_simd8_single_block",
    "gemm_p768_q1_um8_un4_simd8_fixed_q",
    "gemm_p768_q1_um8_un4_simd8_adaptive_half",
    "gemm_p768_q1_um8_un4_simd8_equal_split",
    "gemm_p768_q1_um16_un1_simd4_single_block",
    "gemm_p768_q1_um16_un1_simd4_fixed_q",
    "gemm_p768_q1_um16_un1_simd4_adaptive_half",
    "gemm_p768_q1_um16_un1_simd4_equal_split",
    "gemm_p768_q1_um16_un1_simd8_single_block",
    "gemm_p768_q1_um16_un1_simd8_fixed_q",
    "gemm_p768_q1_um16_un1_simd8_adaptive_half",
    "gemm_p768_q1_um16_un1_simd8_equal_split",
    "gemm_p768_q1_um16_un1_simd16_single_block",
    "gemm_p768_q1_um16_un1_simd16_fixed_q",
    "gemm_p768_q1_um16_un1_simd16_adaptive_half",
    "gemm_p768_q1_um16_un1_simd16_equal_split",
    "gemm_p768_q1_um16_un4_simd8_single_block",
    "gemm_p768_q1_um16_un4_simd8_fixed_q",
    "gemm_p768_q1_um16_un4_simd8_adaptive_half",
    "gemm_p768_q1_um16_un4_simd8_equal_split",
    "gemm_p768_q1_um16_un4_simd16_single_block",
    "gemm_p768_q1_um16_un4_simd16_fixed_q",
    "gemm_p768_q1_um16_un4_simd16_adaptive_half",
    "gemm_p768_q1_um16_un4_simd16_equal_split",
    "gemm_p768_q1_um32_un1_simd4_single_block",
    "gemm_p768_q1_um32_un1_simd4_fixed_q",
    "gemm_p768_q1_um32_un1_simd4_adaptive_half",
    "gemm_p768_q1_um32_un1_simd4_equal_split",
    "gemm_p768_q1_um32_un1_simd8_single_block",
    "gemm_p768_q1_um32_un1_simd8_fixed_q",
    "gemm_p768_q1_um32_un1_simd8_adaptive_half",
    "gemm_p768_q1_um32_un1_simd8_equal_split",
    "gemm_p768_q1_um32_un1_simd16_single_block",
    "gemm_p768_q1_um32_un1_simd16_fixed_q",
    "gemm_p768_q1_um32_un1_simd16_adaptive_half",
    "gemm_p768_q1_um32_un1_simd16_equal_split",
    "gemm_p768_q1_um32_un4_simd16_single_block",
    "gemm_p768_q1_um32_un4_simd16_fixed_q",
    "gemm_p768_q1_um32_un4_simd16_adaptive_half",
    "gemm_p768_q1_um32_un4_simd16_equal_split",
    "gemm_p768_q64_um1_un1_simd1_single_block",
    "gemm_p768_q64_um1_un1_simd1_fixed_q",
    "gemm_p768_q64_um1_un1_simd1_adaptive_half",
    "gemm_p768_q64_um1_un1_simd1_equal_split",
    "gemm_p768_q64_um1_un4_simd1_single_block",
    "gemm_p768_q64_um1_un4_simd1_fixed_q",
    "gemm_p768_q64_um1_un4_simd1_adaptive_half",
    "gemm_p768_q64_um1_un4_simd1_equal_split",
    "gemm_p768_q64_um4_un1_simd1_single_block",
    "gemm_p768_q64_um4_un1_simd1_fixed_q",
    "gemm_p768_q64_um4_un1_simd1_adaptive_half",
    "gemm_p768_q64_um4_un1_simd1_equal_split",
    "gemm_p768_q64_um4_un1_simd4_single_block",
    "gemm_p768_q64_um4_un1_simd4_fixed_q",
    "gemm_p768_q64_um4_un1_simd4_adaptive_half",
    "gemm_p768_q64_um4_un1_simd4_equal_split",
    "gemm_p768_q64_um4_un4_simd4_single_block",
    "gemm_p768_q64_um4_un4_simd4_fixed_q",
    "gemm_p768_q64_um4_un4_simd4_adaptive_half",
    "gemm_p768_q64_um4_un4_simd4_equal_split",
    "gemm_p768_q64_um8_un1_simd1_single_block",
    "gemm_p768_q64_um8_un1_simd1_fixed_q",
    "gemm_p768_q64_um8_un1_simd1_adaptive_half",
    "gemm_p768_q64_um8_un1_simd1_equal_split",
    "gemm_p768_q64_um8_un1_simd4_single_block",
    "gemm_p768_q64_um8_un1_simd4_fixed_q",
    "gemm_p768_q64_um8_un1_simd4_adaptive_half",
    "gemm_p768_q64_um8_un1_simd4_equal_split",
    "gemm_p768_q64_um8_un1_simd8_single_block",
    "gemm_p768_q64_um8_un1_simd8_fixed_q",
    "gemm_p768_q64_um8_un1_simd8_adaptive_half",
    "gemm_p768_q64_um8_un1_simd8_equal_split",
    "gemm_p768_q64_um8_un4_simd4_single_block",
    "gemm_p768_q64_um8_un4_simd4_fixed_q",
    "gemm_p768_q64_um8_un4_simd4_adaptive_half",
    "gemm_p768_q64_um8_un4_simd4_equal_split",
    "gemm_p768_q64_um8_un4_simd8_single_block",
    "gemm_p768_q64_um8_un4_simd8_fixed_q",
    "gemm_p768_q64_um8_un4_simd8_adaptive_half",
    "gemm_p768_q64_um8_un4_simd8_equal_split",
    "gemm_p768_q64_um16_un1_simd4_single_block",
    "gemm_p768_q64_um16_un1_simd4_fixed_q",
    "gemm_p768_q64_um16_un1_simd4_adaptive_half",
    "gemm_p768_q64_um16_un1_simd4_equal_split",
    "gemm_p768_q64_um16_un1_simd8_single_block",
    "gemm_p768_q64_um16_un1_simd8_fixed_q",
    "gemm_p768_q64_um16_un1_simd8_adaptive_half",
    "gemm_p768_q64_um16_un1_simd8_equal_split",
    "gemm_p768_q64_um16_un1_simd16_single_block",
    "gemm_p768_q64_um16_un1_simd16_fixed_q",
    "gemm_p768_q64_um16_un1_simd16_adaptive_half",
    "gemm_p768_q64_um16_un1_simd16_equal_split",
    "gemm_p768_q64_um16_un4_simd8_single_block",
    "gemm_p768_q64_um16_un4_simd8_fixed_q",
    "gemm_p768_q64_um16_un4_simd8_adaptive_half",
    "gemm_p768_q64_um16_un4_simd8_equal_split",
    "gemm_p768_q64_um16_un4_simd16_single_block",
    "gemm_p768_q64_um16_un4_simd16_fixed_q",
    "gemm_p768_q64_um16_un4_simd16_adaptive_half",
    "gemm_p768_q64_um16_un4_simd16_equal_split",
    "gemm_p768_q64_um32_un1_simd4_single_block",
    "gemm_p768_q64_um32_un1_simd4_fixed_q",
    "gemm_p768_q64_um32_un1_simd4_adaptive_half",
    "gemm_p768_q64_um32_un1_simd4_equal_split",
    "gemm_p768_q64_um32_un1_simd8_single_block",
    "gemm_p768_q64_um32_un1_simd8_fixed_q",
    "gemm_p768_q64_um32_un1_simd8_adaptive_half",
    "gemm_p768_q64_um32_un1_simd8_equal_split",
    "gemm_p768_q64_um32_un1_simd16_single_block",
    "gemm_p768_q64_um32_un1_simd16_fixed_q",
    "gemm_p768_q64_um32_un1_simd16_adaptive_half",
    "gemm_p768_q64_um32_un1_simd16_equal_split",
    "gemm_p768_q64_um32_un4_simd16_single_block",
    "gemm_p768_q64_um32_un4_simd16_fixed_q",
    "gemm_p768_q64_um32_un4_simd16_adaptive_half",
    "gemm_p768_q64_um32_un4_simd16_equal_split",
    "gemm_p768_q128_um1_un1_simd1_single_block",
    "gemm_p768_q128_um1_un1_simd1_fixed_q",
    "gemm_p768_q128_um1_un1_simd1_adaptive_half",
    "gemm_p768_q128_um1_un1_simd1_equal_split",
    "gemm_p768_q128_um1_un4_simd1_single_block",
    "gemm_p768_q128_um1_un4_simd1_fixed_q",
    "gemm_p768_q128_um1_un4_simd1_adaptive_half",
    "gemm_p768_q128_um1_un4_simd1_equal_split",
    "gemm_p768_q128_um4_un1_simd1_single_block",
    "gemm_p768_q128_um4_un1_simd1_fixed_q",
    "gemm_p768_q128_um4_un1_simd1_adaptive_half",
    "gemm_p768_q128_um4_un1_simd1_equal_split",
    "gemm_p768_q128_um4_un1_simd4_single_block",
    "gemm_p768_q128_um4_un1_simd4_fixed_q",
    "gemm_p768_q128_um4_un1_simd4_adaptive_half",
    "gemm_p768_q128_um4_un1_simd4_equal_split",
    "gemm_p768_q128_um4_un4_simd4_single_block",
    "gemm_p768_q128_um4_un4_simd4_fixed_q",
    "gemm_p768_q128_um4_un4_simd4_adaptive_half",
    "gemm_p768_q128_um4_un4_simd4_equal_split",
    "gemm_p768_q128_um8_un1_simd1_single_block",
    "gemm_p768_q128_um8_un1_simd1_fixed_q",
    "gemm_p768_q128_um8_un1_simd1_adaptive_half",
    "gemm_p768_q128_um8_un1_simd1_equal_split",
    "gemm_p768_q128_um8_un1_simd4_single_block",
    "gemm_p768_q128_um8_un1_simd4_fixed_q",
    "gemm_p768_q128_um8_un1_simd4_adaptive_half",
    "gemm_p768_q128_um8_un1_simd4_equal_split",
    "gemm_p768_q128_um8_un1_simd8_single_block",
    "gemm_p768_q128_um8_un1_simd8_fixed_q",
    "gemm_p768_q128_um8_un1_simd8_adaptive_half",
    "gemm_p768_q128_um8_un1_simd8_equal_split",
    "gemm_p768_q128_um8_un4_simd4_single_block",
    "gemm_p768_q128_um8_un4_simd4_fixed_q",
    "gemm_p768_q128_um8_un4_simd4_adaptive_half",
    "gemm_p768_q128_um8_un4_simd4_equal_split",
    "gemm_p768_q128_um8_un4_simd8_single_block",
    "gemm_p768_q128_um8_un4_simd8_fixed_q",
    "gemm_p768_q128_um8_un4_simd8_adaptive_half",
    "gemm_p768_q128_um8_un4_simd8_equal_split",
    "gemm_p768_q128_um16_un1_simd4_single_block",
    "gemm_p768_q128_um16_un1_simd4_fixed_q",
    "gemm_p768_q128_um16_un1_simd4_adaptive_half",
    "gemm_p768_q128_um16_un1_simd4_equal_split",
    "gemm_p768_q128_um16_un1_simd8_single_block",
    "gemm_p768_q128_um16_un1_simd8_fixed_q",
    "gemm_p768_q128_um16_un1_simd8_adaptive_half",
    "gemm_p768_q128_um16_un1_simd8_equal_split",
    "gemm_p768_q128_um16_un1_simd16_single_block",
    "gemm_p768_q128_um16_un1_simd16_fixed_q",
    "gemm_p768_q128_um16_un1_simd16_adaptive_half",
    "gemm_p768_q128_um16_un1_simd16_equal_split",
    "gemm_p768_q128_um16_un4_simd8_single_block",
    "gemm_p768_q128_um16_un4_simd8_fixed_q",
    "gemm_p768_q128_um16_un4_simd8_adaptive_half",
    "gemm_p768_q128_um16_un4_simd8_equal_split",
    "gemm_p768_q128_um16_un4_simd16_single_block",
    "gemm_p768_q128_um16_un4_simd16_fixed_q",
    "gemm_p768_q128_um16_un4_simd16_adaptive_half",
    "gemm_p768_q128_um16_un4_simd16_equal_split",
    "gemm_p768_q128_um32_un1_simd4_single_block",
    "gemm_p768_q128_um32_un1_simd4_fixed_q",
    "gemm_p768_q128_um32_un1_simd4_adaptive_half",
    "gemm_p768_q128_um32_un1_simd4_equal_split",
    "gemm_p768_q128_um32_un1_simd8_single_block",
    "gemm_p768_q128_um32_un1_simd8_fixed_q",
    "gemm_p768_q128_um32_un1_simd8_adaptive_half",
    "gemm_p768_q128_um32_un1_simd8_equal_split",
    "gemm_p768_q128_um32_un1_simd16_single_block",
    "gemm_p768_q128_um32_un1_simd16_fixed_q",
    "gemm_p768_q128_um32_un1_simd16_adaptive_half",
    "gemm_p768_q128_um32_un1_simd16_equal_split",
    "gemm_p768_q128_um32_un4_simd16_single_block",
    "gemm_p768_q128_um32_un4_simd16_fixed_q",
    "gemm_p768_q128_um32_un4_simd16_adaptive_half",
    "gemm_p768_q128_um32_un4_simd16_equal_split",
    "gemm_p768_q192_um1_un1_simd1_single_block",
    "gemm_p768_q192_um1_un1_simd1_fixed_q",
    "gemm_p768_q192_um1_un1_simd1_adaptive_half",
    "gemm_p768_q192_um1_un1_simd1_equal_split",
    "gemm_p768_q192_um1_un4_simd1_single_block",
    "gemm_p768_q192_um1_un4_simd1_fixed_q",
    "gemm_p768_q192_um1_un4_simd1_adaptive_half",
    "gemm_p768_q192_um1_un4_simd1_equal_split",
    "gemm_p768_q192_um4_un1_simd1_single_block",
    "gemm_p768_q192_um4_un1_simd1_fixed_q",
    "gemm_p768_q192_um4_un1_simd1_adaptive_half",
    "gemm_p768_q192_um4_un1_simd1_equal_split",
    "gemm_p768_q192_um4_un1_simd4_single_block",
    "gemm_p768_q192_um4_un1_simd4_fixed_q",
    "gemm_p768_q192_um4_un1_simd4_adaptive_half",
    "gemm_p768_q192_um4_un1_simd4_equal_split",
    "gemm_p768_q192_um4_un4_simd4_single_block",
    "gemm_p768_q192_um4_un4_simd4_fixed_q",
    "gemm_p768_q192_um4_un4_simd4_adaptive_half",
    "gemm_p768_q192_um4_un4_simd4_equal_split",
    "gemm_p768_q192_um8_un1_simd1_single_block",
    "gemm_p768_q192_um8_un1_simd1_fixed_q",
    "gemm_p768_q192_um8_un1_simd1_adaptive_half",
    "gemm_p768_q192_um8_un1_simd1_equal_split",
    "gemm_p768_q192_um8_un1_simd4_single_block",
    "gemm_p768_q192_um8_un1_simd4_fixed_q",
    "gemm_p768_q192_um8_un1_simd4_adaptive_half",
    "gemm_p768_q192_um8_un1_simd4_equal_split",
    "gemm_p768_q192_um8_un1_simd8_single_block",
    "gemm_p768_q192_um8_un1_simd8_fixed_q",
    "gemm_p768_q192_um8_un1_simd8_adaptive_half",
    "gemm_p768_q192_um8_un1_simd8_equal_split",
    "gemm_p768_q192_um8_un4_simd4_single_block",
    "gemm_p768_q192_um8_un4_simd4_fixed_q",
    "gemm_p768_q192_um8_un4_simd4_adaptive_half",
    "gemm_p768_q192_um8_un4_simd4_equal_split",
    "gemm_p768_q192_um8_un4_simd8_single_block",
    "gemm_p768_q192_um8_un4_simd8_fixed_q",
    "gemm_p768_q192_um8_un4_simd8_adaptive_half",
    "gemm_p768_q192_um8_un4_simd8_equal_split",
    "gemm_p768_q192_um16_un1_simd4_single_block",
    "gemm_p768_q192_um16_un1_simd4_fixed_q",
    "gemm_p768_q192_um16_un1_simd4_adaptive_half",
    "gemm_p768_q192_um16_un1_simd4_equal_split",
    "gemm_p768_q192_um16_un1_simd8_single_block",
    "gemm_p768_q192_um16_un1_simd8_fixed_q",
    "gemm_p768_q192_um16_un1_simd8_adaptive_half",
    "gemm_p768_q192_um16_un1_simd8_equal_split",
    "gemm_p768_q192_um16_un1_simd16_single_block",
    "gemm_p768_q192_um16_un1_simd16_fixed_q",
    "gemm_p768_q192_um16_un1_simd16_adaptive_half",
    "gemm_p768_q192_um16_un1_simd16_equal_split",
    "gemm_p768_q192_um16_un4_simd8_single_block",
    "gemm_p768_q192_um16_un4_simd8_fixed_q",
    "gemm_p768_q192_um16_un4_simd8_adaptive_half",
    "gemm_p768_q192_um16_un4_simd8_equal_split",
    "gemm_p768_q192_um16_un4_simd16_single_block",
    "gemm_p768_q192_um16_un4_simd16_fixed_q",
    "gemm_p768_q192_um16_un4_simd16_adaptive_half",
    "gemm_p768_q192_um16_un4_simd16_equal_split",
    "gemm_p768_q192_um32_un1_simd4_single_block",
    "gemm_p768_q192_um32_un1_simd4_fixed_q",
    "gemm_p768_q192_um32_un1_simd4_adaptive_half",
    "gemm_p768_q192_um32_un1_simd4_equal_split",
    "gemm_p768_q192_um32_un1_simd8_single_block",
    "gemm_p768_q192_um32_un1_simd8_fixed_q",
    "gemm_p768_q192_um32_un1_simd8_adaptive_half",
    "gemm_p768_q192_um32_un1_simd8_equal_split",
    "gemm_p768_q192_um32_un1_simd16_single_block",
    "gemm_p768_q192_um32_un1_simd16_fixed_q",
    "gemm_p768_q192_um32_un1_simd16_adaptive_half",
    "gemm_p768_q192_um32_un1_simd16_equal_split",
    "gemm_p768_q192_um32_un4_simd16_single_block",
    "gemm_p768_q192_um32_un4_simd16_fixed_q",
    "gemm_p768_q192_um32_un4_simd16_adaptive_half",
    "gemm_p768_q192_um32_un4_simd16_equal_split",
    "gemm_p768_q240_um1_un1_simd1_single_block",
    "gemm_p768_q240_um1_un1_simd1_fixed_q",
    "gemm_p768_q240_um1_un1_simd1_adaptive_half",
    "gemm_p768_q240_um1_un1_simd1_equal_split",
    "gemm_p768_q240_um1_un4_simd1_single_block",
    "gemm_p768_q240_um1_un4_simd1_fixed_q",
    "gemm_p768_q240_um1_un4_simd1_adaptive_half",
    "gemm_p768_q240_um1_un4_simd1_equal_split",
    "gemm_p768_q240_um4_un1_simd1_single_block",
    "gemm_p768_q240_um4_un1_simd1_fixed_q",
    "gemm_p768_q240_um4_un1_simd1_adaptive_half",
    "gemm_p768_q240_um4_un1_simd1_equal_split",
    "gemm_p768_q240_um4_un1_simd4_single_block",
    "gemm_p768_q240_um4_un1_simd4_fixed_q",
    "gemm_p768_q240_um4_un1_simd4_adaptive_half",
    "gemm_p768_q240_um4_un1_simd4_equal_split",
    "gemm_p768_q240_um4_un4_simd4_single_block",
    "gemm_p768_q240_um4_un4_simd4_fixed_q",
    "gemm_p768_q240_um4_un4_simd4_adaptive_half",
    "gemm_p768_q240_um4_un4_simd4_equal_split",
    "gemm_p768_q240_um8_un1_simd1_single_block",
    "gemm_p768_q240_um8_un1_simd1_fixed_q",
    "gemm_p768_q240_um8_un1_simd1_adaptive_half",
    "gemm_p768_q240_um8_un1_simd1_equal_split",
    "gemm_p768_q240_um8_un1_simd4_single_block",
    "gemm_p768_q240_um8_un1_simd4_fixed_q",
    "gemm_p768_q240_um8_un1_simd4_adaptive_half",
    "gemm_p768_q240_um8_un1_simd4_equal_split",
    "gemm_p768_q240_um8_un1_simd8_single_block",
    "gemm_p768_q240_um8_un1_simd8_fixed_q",
    "gemm_p768_q240_um8_un1_simd8_adaptive_half",
    "gemm_p768_q240_um8_un1_simd8_equal_split",
    "gemm_p768_q240_um8_un4_simd4_single_block",
    "gemm_p768_q240_um8_un4_simd4_fixed_q",
    "gemm_p768_q240_um8_un4_simd4_adaptive_half",
    "gemm_p768_q240_um8_un4_simd4_equal_split",
    "gemm_p768_q240_um8_un4_simd8_single_block",
    "gemm_p768_q240_um8_un4_simd8_fixed_q",
    "gemm_p768_q240_um8_un4_simd8_adaptive_half",
    "gemm_p768_q240_um8_un4_simd8_equal_split",
    "gemm_p768_q240_um16_un1_simd4_single_block",
    "gemm_p768_q240_um16_un1_simd4_fixed_q",
    "gemm_p768_q240_um16_un1_simd4_adaptive_half",
    "gemm_p768_q240_um16_un1_simd4_equal_split",
    "gemm_p768_q240_um16_un1_simd8_single_block",
    "gemm_p768_q240_um16_un1_simd8_fixed_q",
    "gemm_p768_q240_um16_un1_simd8_adaptive_half",
    "gemm_p768_q240_um16_un1_simd8_equal_split",
    "gemm_p768_q240_um16_un1_simd16_single_block",
    "gemm_p768_q240_um16_un1_simd16_fixed_q",
    "gemm_p768_q240_um16_un1_simd16_adaptive_half",
    "gemm_p768_q240_um16_un1_simd16_equal_split",
    "gemm_p768_q240_um16_un4_simd8_single_block",
    "gemm_p768_q240_um16_un4_simd8_fixed_q",
    "gemm_p768_q240_um16_un4_simd8_adaptive_half",
    "gemm_p768_q240_um16_un4_simd8_equal_split",
    "gemm_p768_q240_um16_un4_simd16_single_block",
    "gemm_p768_q240_um16_un4_simd16_fixed_q",
    "gemm_p768_q240_um16_un4_simd16_adaptive_half",
    "gemm_p768_q240_um16_un4_simd16_equal_split",
    "gemm_p768_q240_um32_un1_simd4_single_block",
    "gemm_p768_q240_um32_un1_simd4_fixed_q",
    "gemm_p768_q240_um32_un1_simd4_adaptive_half",
    "gemm_p768_q240_um32_un1_simd4_equal_split",
    "gemm_p768_q240_um32_un1_simd8_single_block",
    "gemm_p768_q240_um32_un1_simd8_fixed_q",
    "gemm_p768_q240_um32_un1_simd8_adaptive_half",
    "gemm_p768_q240_um32_un1_simd8_equal_split",
    "gemm_p768_q240_um32_un1_simd16_single_block",
    "gemm_p768_q240_um32_un1_simd16_fixed_q",
    "gemm_p768_q240_um32_un1_simd16_adaptive_half",
    "gemm_p768_q240_um32_un1_simd16_equal_split",
    "gemm_p768_q248_um1_un1_simd1_single_block",
    "gemm_p768_q248_um1_un1_simd1_fixed_q",
    "gemm_p768_q248_um1_un1_simd1_adaptive_half",
    "gemm_p768_q248_um1_un1_simd1_equal_split",
    "gemm_p768_q248_um1_un4_simd1_single_block",
    "gemm_p768_q248_um1_un4_simd1_fixed_q",
    "gemm_p768_q248_um1_un4_simd1_adaptive_half",
    "gemm_p768_q248_um1_un4_simd1_equal_split",
    "gemm_p768_q248_um4_un1_simd1_single_block",
    "gemm_p768_q248_um4_un1_simd1_fixed_q",
    "gemm_p768_q248_um4_un1_simd1_adaptive_half",
    "gemm_p768_q248_um4_un1_simd1_equal_split",
    "gemm_p768_q248_um4_un1_simd4_single_block",
    "gemm_p768_q248_um4_un1_simd4_fixed_q",
    "gemm_p768_q248_um4_un1_simd4_adaptive_half",
    "gemm_p768_q248_um4_un1_simd4_equal_split",
    "gemm_p768_q248_um4_un4_simd4_single_block",
    "gemm_p768_q248_um4_un4_simd4_fixed_q",
    "gemm_p768_q248_um4_un4_simd4_adaptive_half",
    "gemm_p768_q248_um4_un4_simd4_equal_split",
    "gemm_p768_q248_um8_un1_simd1_single_block",
    "gemm_p768_q248_um8_un1_simd1_fixed_q",
    "gemm_p768_q248_um8_un1_simd1_adaptive_half",
    "gemm_p768_q248_um8_un1_simd1_equal_split",
    "gemm_p768_q248_um8_un1_simd4_single_block",
    "gemm_p768_q248_um8_un1_simd4_fixed_q",
    "gemm_p768_q248_um8_un1_simd4_adaptive_half",
    "gemm_p768_q248_um8_un1_simd4_equal_split",
    "gemm_p768_q248_um8_un1_simd8_single_block",
    "gemm_p768_q248_um8_un1_simd8_fixed_q",
    "gemm_p768_q248_um8_un1_simd8_adaptive_half",
    "gemm_p768_q248_um8_un1_simd8_equal_split",
    "gemm_p768_q248_um8_un4_simd4_single_block",
    "gemm_p768_q248_um8_un4_simd4_fixed_q",
    "gemm_p768_q248_um8_un4_simd4_adaptive_half",
    "gemm_p768_q248_um8_un4_simd4_equal_split",
    "gemm_p768_q248_um8_un4_simd8_single_block",
    "gemm_p768_q248_um8_un4_simd8_fixed_q",
    "gemm_p768_q248_um8_un4_simd8_adaptive_half",
    "gemm_p768_q248_um8_un4_simd8_equal_split",
    "gemm_p768_q248_um16_un1_simd4_single_block",
    "gemm_p768_q248_um16_un1_simd4_fixed_q",
    "gemm_p768_q248_um16_un1_simd4_adaptive_half",
    "gemm_p768_q248_um16_un1_simd4_equal_split",
    "gemm_p768_q248_um16_un1_simd8_single_block",
    "gemm_p768_q248_um16_un1_simd8_fixed_q",
    "gemm_p768_q248_um16_un1_simd8_adaptive_half",
    "gemm_p768_q248_um16_un1_simd8_equal_split",
    "gemm_p768_q248_um16_un1_simd16_single_block",
    "gemm_p768_q248_um16_un1_simd16_fixed_q",
    "gemm_p768_q248_um16_un1_simd16_adaptive_half",
    "gemm_p768_q248_um16_un1_simd16_equal_split",
    "gemm_p768_q248_um16_un4_simd8_single_block",
    "gemm_p768_q248_um16_un4_simd8_fixed_q",
    "gemm_p768_q248_um16_un4_simd8_adaptive_half",
    "gemm_p768_q248_um16_un4_simd8_equal_split",
    "gemm_p768_q248_um16_un4_simd16_single_block",
    "gemm_p768_q248_um16_un4_simd16_fixed_q",
    "gemm_p768_q248_um16_un4_simd16_adaptive_half",
    "gemm_p768_q248_um16_un4_simd16_equal_split",
    "gemm_p768_q248_um32_un1_simd4_single_block",
    "gemm_p768_q248_um32_un1_simd4_fixed_q",
    "gemm_p768_q248_um32_un1_simd4_adaptive_half",
    "gemm_p768_q248_um32_un1_simd4_equal_split",
    "gemm_p768_q248_um32_un1_simd8_single_block",
    "gemm_p768_q248_um32_un1_simd8_fixed_q",
    "gemm_p768_q248_um32_un1_simd8_adaptive_half",
    "gemm_p768_q248_um32_un1_simd8_equal_split",
    "gemm_p768_q248_um32_un1_simd16_single_block",
    "gemm_p768_q248_um32_un1_simd16_fixed_q",
    "gemm_p768_q248_um32_un1_simd16_adaptive_half",
    "gemm_p768_q248_um32_un1_simd16_equal_split",
    "gemm_p768_q256_um1_un1_simd1_single_block",
    "gemm_p768_q256_um1_un1_simd1_fixed_q",
    "gemm_p768_q256_um1_un1_simd1_adaptive_half",
    "gemm_p768_q256_um1_un1_simd1_equal_split",
    "gemm_p768_q256_um1_un4_simd1_single_block",
    "gemm_p768_q256_um1_un4_simd1_fixed_q",
    "gemm_p768_q256_um1_un4_simd1_adaptive_half",
    "gemm_p768_q256_um1_un4_simd1_equal_split",
    "gemm_p768_q256_um4_un1_simd1_single_block",
    "gemm_p768_q256_um4_un1_simd1_fixed_q",
    "gemm_p768_q256_um4_un1_simd1_adaptive_half",
    "gemm_p768_q256_um4_un1_simd1_equal_split",
    "gemm_p768_q256_um4_un1_simd4_single_block",
    "gemm_p768_q256_um4_un1_simd4_fixed_q",
    "gemm_p768_q256_um4_un1_simd4_adaptive_half",
    "gemm_p768_q256_um4_un1_simd4_equal_split",
    "gemm_p768_q256_um4_un4_simd4_single_block",
    "gemm_p768_q256_um4_un4_simd4_fixed_q",
    "gemm_p768_q256_um4_un4_simd4_adaptive_half",
    "gemm_p768_q256_um4_un4_simd4_equal_split",
    "gemm_p768_q256_um8_un1_simd1_single_block",
    "gemm_p768_q256_um8_un1_simd1_fixed_q",
    "gemm_p768_q256_um8_un1_simd1_adaptive_half",
    "gemm_p768_q256_um8_un1_simd1_equal_split",
    "gemm_p768_q256_um8_un1_simd4_single_block",
    "gemm_p768_q256_um8_un1_simd4_fixed_q",
    "gemm_p768_q256_um8_un1_simd4_adaptive_half",
    "gemm_p768_q256_um8_un1_simd4_equal_split",
    "gemm_p768_q256_um8_un1_simd8_single_block",
    "gemm_p768_q256_um8_un1_simd8_fixed_q",
    "gemm_p768_q256_um8_un1_simd8_adaptive_half",
    "gemm_p768_q256_um8_un1_simd8_equal_split",
    "gemm_p768_q256_um8_un4_simd4_single_block",
    "gemm_p768_q256_um8_un4_simd4_fixed_q",
    "gemm_p768_q256_um8_un4_simd4_adaptive_half",
    "gemm_p768_q256_um8_un4_simd4_equal_split",
    "gemm_p768_q256_um8_un4_simd8_single_block",
    "gemm_p768_q256_um8_un4_simd8_fixed_q",
    "gemm_p768_q256_um8_un4_simd8_adaptive_half",
    "gemm_p768_q256_um8_un4_simd8_equal_split",
    "gemm_p768_q256_um16_un1_simd4_single_block",
    "gemm_p768_q256_um16_un1_simd4_fixed_q",
    "gemm_p768_q256_um16_un1_simd4_adaptive_half",
    "gemm_p768_q256_um16_un1_simd4_equal_split",
    "gemm_p768_q256_um16_un1_simd8_single_block",
    "gemm_p768_q256_um16_un1_simd8_fixed_q",
    "gemm_p768_q256_um16_un1_simd8_adaptive_half",
    "gemm_p768_q256_um16_un1_simd8_equal_split",
    "gemm_p768_q256_um16_un1_simd16_single_block",
    "gemm_p768_q256_um16_un1_simd16_fixed_q",
    "gemm_p768_q256_um16_un1_simd16_adaptive_half",
    "gemm_p768_q256_um16_un1_simd16_equal_split",
    "gemm_p768_q256_um16_un4_simd8_single_block",
    "gemm_p768_q256_um16_un4_simd8_fixed_q",
    "gemm_p768_q256_um16_un4_simd8_adaptive_half",
    "gemm_p768_q256_um16_un4_simd8_equal_split",
    "gemm_p768_q256_um16_un4_simd16_single_block",
    "gemm_p768_q256_um16_un4_simd16_fixed_q",
    "gemm_p768_q256_um16_un4_simd16_adaptive_half",
    "gemm_p768_q256_um16_un4_simd16_equal_split",
    "gemm_p768_q384_um1_un1_simd1_single_block",
    "gemm_p768_q384_um1_un1_simd1_fixed_q",
    "gemm_p768_q384_um1_un1_simd1_adaptive_half",
    "gemm_p768_q384_um1_un1_simd1_equal_split",
    "gemm_p768_q384_um1_un4_simd1_single_block",
    "gemm_p768_q384_um1_un4_simd1_fixed_q",
    "gemm_p768_q384_um1_un4_simd1_adaptive_half",
    "gemm_p768_q384_um1_un4_simd1_equal_split",
    "gemm_p768_q384_um4_un1_simd1_single_block",
    "gemm_p768_q384_um4_un1_simd1_fixed_q",
    "gemm_p768_q384_um4_un1_simd1_adaptive_half",
    "gemm_p768_q384_um4_un1_simd1_equal_split",
    "gemm_p768_q384_um4_un1_simd4_single_block",
    "gemm_p768_q384_um4_un1_simd4_fixed_q",
    "gemm_p768_q384_um4_un1_simd4_adaptive_half",
    "gemm_p768_q384_um4_un1_simd4_equal_split",
    "gemm_p768_q384_um4_un4_simd4_single_block",
    "gemm_p768_q384_um4_un4_simd4_fixed_q",
    "gemm_p768_q384_um4_un4_simd4_adaptive_half",
    "gemm_p768_q384_um4_un4_simd4_equal_split",
    "gemm_p768_q384_um8_un1_simd1_single_block",
    "gemm_p768_q384_um8_un1_simd1_fixed_q",
    "gemm_p768_q384_um8_un1_simd1_adaptive_half",
    "gemm_p768_q384_um8_un1_simd1_equal_split",
    "gemm_p768_q384_um8_un1_simd4_single_block",
    "gemm_p768_q384_um8_un1_simd4_fixed_q",
    "gemm_p768_q384_um8_un1_simd4_adaptive_half",
    "gemm_p768_q384_um8_un1_simd4_equal_split",
    "gemm_p768_q384_um8_un1_simd8_single_block",
    "gemm_p768_q384_um8_un1_simd8_fixed_q",
    "gemm_p768_q384_um8_un1_simd8_adaptive_half",
    "gemm_p768_q384_um8_un1_simd8_equal_split",
    "gemm_p768_q384_um8_un4_simd4_single_block",
    "gemm_p768_q384_um8_un4_simd4_fixed_q",
    "gemm_p768_q384_um8_un4_simd4_adaptive_half",
    "gemm_p768_q384_um8_un4_simd4_equal_split",
    "gemm_p768_q384_um8_un4_simd8_single_block",
    "gemm_p768_q384_um8_un4_simd8_fixed_q",
    "gemm_p768_q384_um8_un4_simd8_adaptive_half",
    "gemm_p768_q384_um8_un4_simd8_equal_split",
    "gemm_p768_q384_um16_un1_simd4_single_block",
    "gemm_p768_q384_um16_un1_simd4_fixed_q",
    "gemm_p768_q384_um16_un1_simd4_adaptive_half",
    "gemm_p768_q384_um16_un1_simd4_equal_split",
    "gemm_p768_q384_um16_un1_simd8_single_block",
    "gemm_p768_q384_um16_un1_simd8_fixed_q",
    "gemm_p768_q384_um16_un1_simd8_adaptive_half",
    "gemm_p768_q384_um16_un1_simd8_equal_split",
    "gemm_p768_q384_um16_un1_simd16_single_block",
    "gemm_p768_q384_um16_un1_simd16_fixed_q",
    "gemm_p768_q384_um16_un1_simd16_adaptive_half",
    "gemm_p768_q384_um16_un1_simd16_equal_split",
    "gemm_p768_q384_um16_un4_simd8_single_block",
    "gemm_p768_q384_um16_un4_simd8_fixed_q",
    "gemm_p768_q384_um16_un4_simd8_adaptive_half",
    "gemm_p768_q384_um16_un4_simd8_equal_split",
    "gemm_p768_q384_um16_un4_simd16_single_block",
    "gemm_p768_q384_um16_un4_simd16_fixed_q",
    "gemm_p768_q384_um16_un4_simd16_adaptive_half",
    "gemm_p768_q384_um16_un4_simd16_equal_split",
    "gemm_p768_q512_um1_un1_simd1_single_block",
    "gemm_p768_q512_um1_un1_simd1_fixed_q",
    "gemm_p768_q512_um1_un1_simd1_adaptive_half",
    "gemm_p768_q512_um1_un1_simd1_equal_split",
    "gemm_p768_q512_um1_un4_simd1_single_block",
    "gemm_p768_q512_um1_un4_simd1_fixed_q",
    "gemm_p768_q512_um1_un4_simd1_adaptive_half",
    "gemm_p768_q512_um1_un4_simd1_equal_split",
    "gemm_p768_q512_um4_un1_simd1_single_block",
    "gemm_p768_q512_um4_un1_simd1_fixed_q",
    "gemm_p768_q512_um4_un1_simd1_adaptive_half",
    "gemm_p768_q512_um4_un1_simd1_equal_split",
    "gemm_p768_q512_um4_un1_simd4_single_block",
    "gemm_p768_q512_um4_un1_simd4_fixed_q",
    "gemm_p768_q512_um4_un1_simd4_adaptive_half",
    "gemm_p768_q512_um4_un1_simd4_equal_split",
    "gemm_p768_q512_um4_un4_simd4_single_block",
    "gemm_p768_q512_um4_un4_simd4_fixed_q",
    "gemm_p768_q512_um4_un4_simd4_adaptive_half",
    "gemm_p768_q512_um4_un4_simd4_equal_split",
    "gemm_p768_q512_um8_un1_simd1_single_block",
    "gemm_p768_q512_um8_un1_simd1_fixed_q",
    "gemm_p768_q512_um8_un1_simd1_adaptive_half",
    "gemm_p768_q512_um8_un1_simd1_equal_split",
    "gemm_p768_q512_um8_un1_simd4_single_block",
    "gemm_p768_q512_um8_un1_simd4_fixed_q",
    "gemm_p768_q512_um8_un1_simd4_adaptive_half",
    "gemm_p768_q512_um8_un1_simd4_equal_split",
    "gemm_p768_q512_um8_un1_simd8_single_block",
    "gemm_p768_q512_um8_un1_simd8_fixed_q",
    "gemm_p768_q512_um8_un1_simd8_adaptive_half",
    "gemm_p768_q512_um8_un1_simd8_equal_split",
    "gemm_p768_q512_um8_un4_simd4_single_block",
    "gemm_p768_q512_um8_un4_simd4_fixed_q",
    "gemm_p768_q512_um8_un4_simd4_adaptive_half",
    "gemm_p768_q512_um8_un4_simd4_equal_split",
    "gemm_p768_q512_um8_un4_simd8_single_block",
    "gemm_p768_q512_um8_un4_simd8_fixed_q",
    "gemm_p768_q512_um8_un4_simd8_adaptive_half",
    "gemm_p768_q512_um8_un4_simd8_equal_split",
    "gemm_p1024_q1_um1_un1_simd1_single_block",
    "gemm_p1024_q1_um1_un1_simd1_fixed_q",
    "gemm_p1024_q1_um1_un1_simd1_adaptive_half",
    "gemm_p1024_q1_um1_un1_simd1_equal_split",
    "gemm_p1024_q1_um1_un4_simd1_single_block",
    "gemm_p1024_q1_um1_un4_simd1_fixed_q",
    "gemm_p1024_q1_um1_un4_simd1_adaptive_half",
    "gemm_p1024_q1_um1_un4_simd1_equal_split",
    "gemm_p1024_q1_um4_un1_simd1_single_block",
    "gemm_p1024_q1_um4_un1_simd1_fixed_q",
    "gemm_p1024_q1_um4_un1_simd1_adaptive_half",
    "gemm_p1024_q1_um4_un1_simd1_equal_split",
    "gemm_p1024_q1_um4_un1_simd4_single_block",
    "gemm_p1024_q1_um4_un1_simd4_fixed_q",
    "gemm_p1024_q1_um4_un1_simd4_adaptive_half",
    "gemm_p1024_q1_um4_un1_simd4_equal_split",
    "gemm_p1024_q1_um4_un4_simd4_single_block",
    "gemm_p1024_q1_um4_un4_simd4_fixed_q",
    "gemm_p1024_q1_um4_un4_simd4_adaptive_half",
    "gemm_p1024_q1_um4_un4_simd4_equal_split",
    "gemm_p1024_q1_um8_un1_simd1_single_block",
    "gemm_p1024_q1_um8_un1_simd1_fixed_q",
    "gemm_p1024_q1_um8_un1_simd1_adaptive_half",
    "gemm_p1024_q1_um8_un1_simd1_equal_split",
    "gemm_p1024_q1_um8_un1_simd4_single_block",
    "gemm_p1024_q1_um8_un1_simd4_fixed_q",
    "gemm_p1024_q1_um8_un1_simd4_adaptive_half",
    "gemm_p1024_q1_um8_un1_simd4_equal_split",
    "gemm_p1024_q1_um8_un1_simd8_single_block",
    "gemm_p1024_q1_um8_un1_simd8_fixed_q",
    "gemm_p1024_q1_um8_un1_simd8_adaptive_half",
    "gemm_p1024_q1_um8_un1_simd8_equal_split",
    "gemm_p1024_q1_um8_un4_simd4_single_block",
    "gemm_p1024_q1_um8_un4_simd4_fixed_q",
    "gemm_p1024_q1_um8_un4_simd4_adaptive_half",
    "gemm_p1024_q1_um8_un4_simd4_equal_split",
    "gemm_p1024_q1_um8_un4_simd8_single_block",
    "gemm_p1024_q1_um8_un4_simd8_fixed_q",
    "gemm_p1024_q1_um8_un4_simd8_adaptive_half",
    "gemm_p1024_q1_um8_un4_simd8_equal_split",
    "gemm_p1024_q1_um16_un1_simd4_single_block",
    "gemm_p1024_q1_um16_un1_simd4_fixed_q",
    "gemm_p1024_q1_um16_un1_simd4_adaptive_half",
    "gemm_p1024_q1_um16_un1_simd4_equal_split",
    "gemm_p1024_q1_um16_un1_simd8_single_block",
    "gemm_p1024_q1_um16_un1_simd8_fixed_q",
    "gemm_p1024_q1_um16_un1_simd8_adaptive_half",
    "gemm_p1024_q1_um16_un1_simd8_equal_split",
    "gemm_p1024_q1_um16_un1_simd16_single_block",
    "gemm_p1024_q1_um16_un1_simd16_fixed_q",
    "gemm_p1024_q1_um16_un1_simd16_adaptive_half",
    "gemm_p1024_q1_um16_un1_simd16_equal_split",
    "gemm_p1024_q1_um16_un4_simd8_single_block",
    "gemm_p1024_q1_um16_un4_simd8_fixed_q",
    "gemm_p1024_q1_um16_un4_simd8_adaptive_half",
    "gemm_p1024_q1_um16_un4_simd8_equal_split",
    "gemm_p1024_q1_um16_un4_simd16_single_block",
    "gemm_p1024_q1_um16_un4_simd16_fixed_q",
    "gemm_p1024_q1_um16_un4_simd16_adaptive_half",
    "gemm_p1024_q1_um16_un4_simd16_equal_split",
    "gemm_p1024_q1_um32_un1_simd4_single_block",
    "gemm_p1024_q1_um32_un1_simd4_fixed_q",
    "gemm_p1024_q1_um32_un1_simd4_adaptive_half",
    "gemm_p1024_q1_um32_un1_simd4_equal_split",
    "gemm_p1024_q1_um32_un1_simd8_single_block",
    "gemm_p1024_q1_um32_un1_simd8_fixed_q",
    "gemm_p1024_q1_um32_un1_simd8_adaptive_half",
    "gemm_p1024_q1_um32_un1_simd8_equal_split",
    "gemm_p1024_q1_um32_un1_simd16_single_block",
    "gemm_p1024_q1_um32_un1_simd16_fixed_q",
    "gemm_p1024_q1_um32_un1_simd16_adaptive_half",
    "gemm_p1024_q1_um32_un1_simd16_equal_split",
    "gemm_p1024_q1_um32_un4_simd16_single_block",
    "gemm_p1024_q1_um32_un4_simd16_fixed_q",
    "gemm_p1024_q1_um32_un4_simd16_adaptive_half",
    "gemm_p1024_q1_um32_un4_simd16_equal_split",
    "gemm_p1024_q64_um1_un1_simd1_single_block",
    "gemm_p1024_q64_um1_un1_simd1_fixed_q",
    "gemm_p1024_q64_um1_un1_simd1_adaptive_half",
    "gemm_p1024_q64_um1_un1_simd1_equal_split",
    "gemm_p1024_q64_um1_un4_simd1_single_block",
    "gemm_p1024_q64_um1_un4_simd1_fixed_q",
    "gemm_p1024_q64_um1_un4_simd1_adaptive_half",
    "gemm_p1024_q64_um1_un4_simd1_equal_split",
    "gemm_p1024_q64_um4_un1_simd1_single_block",
    "gemm_p1024_q64_um4_un1_simd1_fixed_q",
    "gemm_p1024_q64_um4_un1_simd1_adaptive_half",
    "gemm_p1024_q64_um4_un1_simd1_equal_split",
    "gemm_p1024_q64_um4_un1_simd4_single_block",
    "gemm_p1024_q64_um4_un1_simd4_fixed_q",
    "gemm_p1024_q64_um4_un1_simd4_adaptive_half",
    "gemm_p1024_q64_um4_un1_simd4_equal_split",
    "gemm_p1024_q64_um4_un4_simd4_single_block",
    "gemm_p1024_q64_um4_un4_simd4_fixed_q",
    "gemm_p1024_q64_um4_un4_simd4_adaptive_half",
    "gemm_p1024_q64_um4_un4_simd4_equal_split",
    "gemm_p1024_q64_um8_un1_simd1_single_block",
    "gemm_p1024_q64_um8_un1_simd1_fixed_q",
    "gemm_p1024_q64_um8_un1_simd1_adaptive_half",
    "gemm_p1024_q64_um8_un1_simd1_equal_split",
    "gemm_p1024_q64_um8_un1_simd4_single_block",
    "gemm_p1024_q64_um8_un1_simd4_fixed_q",
    "gemm_p1024_q64_um8_un1_simd4_adaptive_half",
    "gemm_p1024_q64_um8_un1_simd4_equal_split",
    "gemm_p1024_q64_um8_un1_simd8_single_block",
    "gemm_p1024_q64_um8_un1_simd8_fixed_q",
    "gemm_p1024_q64_um8_un1_simd8_adaptive_half",
    "gemm_p1024_q64_um8_un1_simd8_equal_split",
    "gemm_p1024_q64_um8_un4_simd4_single_block",
    "gemm_p1024_q64_um8_un4_simd4_fixed_q",
    "gemm_p1024_q64_um8_un4_simd4_adaptive_half",
    "gemm_p1024_q64_um8_un4_simd4_equal_split",
    "gemm_p1024_q64_um8_un4_simd8_single_block",
    "gemm_p1024_q64_um8_un4_simd8_fixed_q",
    "gemm_p1024_q64_um8_un4_simd8_adaptive_half",
    "gemm_p1024_q64_um8_un4_simd8_equal_split",
    "gemm_p1024_q64_um16_un1_simd4_single_block",
    "gemm_p1024_q64_um16_un1_simd4_fixed_q",
    "gemm_p1024_q64_um16_un1_simd4_adaptive_half",
    "gemm_p1024_q64_um16_un1_simd4_equal_split",
    "gemm_p1024_q64_um16_un1_simd8_single_block",
    "gemm_p1024_q64_um16_un1_simd8_fixed_q",
    "gemm_p1024_q64_um16_un1_simd8_adaptive_half",
    "gemm_p1024_q64_um16_un1_simd8_equal_split",
    "gemm_p1024_q64_um16_un1_simd16_single_block",
    "gemm_p1024_q64_um16_un1_simd16_fixed_q",
    "gemm_p1024_q64_um16_un1_simd16_adaptive_half",
    "gemm_p1024_q64_um16_un1_simd16_equal_split",
    "gemm_p1024_q64_um16_un4_simd8_single_block",
    "gemm_p1024_q64_um16_un4_simd8_fixed_q",
    "gemm_p1024_q64_um16_un4_simd8_adaptive_half",
    "gemm_p1024_q64_um16_un4_simd8_equal_split",
    "gemm_p1024_q64_um16_un4_simd16_single_block",
    "gemm_p1024_q64_um16_un4_simd16_fixed_q",
    "gemm_p1024_q64_um16_un4_simd16_adaptive_half",
    "gemm_p1024_q64_um16_un4_simd16_equal_split",
    "gemm_p1024_q64_um32_un1_simd4_single_block",
    "gemm_p1024_q64_um32_un1_simd4_fixed_q",
    "gemm_p1024_q64_um32_un1_simd4_adaptive_half",
    "gemm_p1024_q64_um32_un1_simd4_equal_split",
    "gemm_p1024_q64_um32_un1_simd8_single_block",
    "gemm_p1024_q64_um32_un1_simd8_fixed_q",
    "gemm_p1024_q64_um32_un1_simd8_adaptive_half",
    "gemm_p1024_q64_um32_un1_simd8_equal_split",
    "gemm_p1024_q64_um32_un1_simd16_single_block",
    "gemm_p1024_q64_um32_un1_simd16_fixed_q",
    "gemm_p1024_q64_um32_un1_simd16_adaptive_half",
    "gemm_p1024_q64_um32_un1_simd16_equal_split",
    "gemm_p1024_q64_um32_un4_simd16_single_block",
    "gemm_p1024_q64_um32_un4_simd16_fixed_q",
    "gemm_p1024_q64_um32_un4_simd16_adaptive_half",
    "gemm_p1024_q64_um32_un4_simd16_equal_split",
    "gemm_p1024_q128_um1_un1_simd1_single_block",
    "gemm_p1024_q128_um1_un1_simd1_fixed_q",
    "gemm_p1024_q128_um1_un1_simd1_adaptive_half",
    "gemm_p1024_q128_um1_un1_simd1_equal_split",
    "gemm_p1024_q128_um1_un4_simd1_single_block",
    "gemm_p1024_q128_um1_un4_simd1_fixed_q",
    "gemm_p1024_q128_um1_un4_simd1_adaptive_half",
    "gemm_p1024_q128_um1_un4_simd1_equal_split",
    "gemm_p1024_q128_um4_un1_simd1_single_block",
    "gemm_p1024_q128_um4_un1_simd1_fixed_q",
    "gemm_p1024_q128_um4_un1_simd1_adaptive_half",
    "gemm_p1024_q128_um4_un1_simd1_equal_split",
    "gemm_p1024_q128_um4_un1_simd4_single_block",
    "gemm_p1024_q128_um4_un1_simd4_fixed_q",
    "gemm_p1024_q128_um4_un1_simd4_adaptive_half",
    "gemm_p1024_q128_um4_un1_simd4_equal_split",
    "gemm_p1024_q128_um4_un4_simd4_single_block",
    "gemm_p1024_q128_um4_un4_simd4_fixed_q",
    "gemm_p1024_q128_um4_un4_simd4_adaptive_half",
    "gemm_p1024_q128_um4_un4_simd4_equal_split",
    "gemm_p1024_q128_um8_un1_simd1_single_block",
    "gemm_p1024_q128_um8_un1_simd1_fixed_q",
    "gemm_p1024_q128_um8_un1_simd1_adaptive_half",
    "gemm_p1024_q128_um8_un1_simd1_equal_split",
    "gemm_p1024_q128_um8_un1_simd4_single_block",
    "gemm_p1024_q128_um8_un1_simd4_fixed_q",
    "gemm_p1024_q128_um8_un1_simd4_adaptive_half",
    "gemm_p1024_q128_um8_un1_simd4_equal_split",
    "gemm_p1024_q128_um8_un1_simd8_single_block",
    "gemm_p1024_q128_um8_un1_simd8_fixed_q",
    "gemm_p1024_q128_um8_un1_simd8_adaptive_half",
    "gemm_p1024_q128_um8_un1_simd8_equal_split",
    "gemm_p1024_q128_um8_un4_simd4_single_block",
    "gemm_p1024_q128_um8_un4_simd4_fixed_q",
    "gemm_p1024_q128_um8_un4_simd4_adaptive_half",
    "gemm_p1024_q128_um8_un4_simd4_equal_split",
    "gemm_p1024_q128_um8_un4_simd8_single_block",
    "gemm_p1024_q128_um8_un4_simd8_fixed_q",
    "gemm_p1024_q128_um8_un4_simd8_adaptive_half",
    "gemm_p1024_q128_um8_un4_simd8_equal_split",
    "gemm_p1024_q128_um16_un1_simd4_single_block",
    "gemm_p1024_q128_um16_un1_simd4_fixed_q",
    "gemm_p1024_q128_um16_un1_simd4_adaptive_half",
    "gemm_p1024_q128_um16_un1_simd4_equal_split",
    "gemm_p1024_q128_um16_un1_simd8_single_block",
    "gemm_p1024_q128_um16_un1_simd8_fixed_q",
    "gemm_p1024_q128_um16_un1_simd8_adaptive_half",
    "gemm_p1024_q128_um16_un1_simd8_equal_split",
    "gemm_p1024_q128_um16_un1_simd16_single_block",
    "gemm_p1024_q128_um16_un1_simd16_fixed_q",
    "gemm_p1024_q128_um16_un1_simd16_adaptive_half",
    "gemm_p1024_q128_um16_un1_simd16_equal_split",
    "gemm_p1024_q128_um16_un4_simd8_single_block",
    "gemm_p1024_q128_um16_un4_simd8_fixed_q",
    "gemm_p1024_q128_um16_un4_simd8_adaptive_half",
    "gemm_p1024_q128_um16_un4_simd8_equal_split",
    "gemm_p1024_q128_um16_un4_simd16_single_block",
    "gemm_p1024_q128_um16_un4_simd16_fixed_q",
    "gemm_p1024_q128_um16_un4_simd16_adaptive_half",
    "gemm_p1024_q128_um16_un4_simd16_equal_split",
    "gemm_p1024_q128_um32_un1_simd4_single_block",
    "gemm_p1024_q128_um32_un1_simd4_fixed_q",
    "gemm_p1024_q128_um32_un1_simd4_adaptive_half",
    "gemm_p1024_q128_um32_un1_simd4_equal_split",
    "gemm_p1024_q128_um32_un1_simd8_single_block",
    "gemm_p1024_q128_um32_un1_simd8_fixed_q",
    "gemm_p1024_q128_um32_un1_simd8_adaptive_half",
    "gemm_p1024_q128_um32_un1_simd8_equal_split",
    "gemm_p1024_q128_um32_un1_simd16_single_block",
    "gemm_p1024_q128_um32_un1_simd16_fixed_q",
    "gemm_p1024_q128_um32_un1_simd16_adaptive_half",
    "gemm_p1024_q128_um32_un1_simd16_equal_split",
    "gemm_p1024_q128_um32_un4_simd16_single_block",
    "gemm_p1024_q128_um32_un4_simd16_fixed_q",
    "gemm_p1024_q128_um32_un4_simd16_adaptive_half",
    "gemm_p1024_q128_um32_un4_simd16_equal_split",
    "gemm_p1024_q192_um1_un1_simd1_single_block",
    "gemm_p1024_q192_um1_un1_simd1_fixed_q",
    "gemm_p1024_q192_um1_un1_simd1_adaptive_half",
    "gemm_p1024_q192_um1_un1_simd1_equal_split",
    "gemm_p1024_q192_um1_un4_simd1_single_block",
    "gemm_p1024_q192_um1_un4_simd1_fixed_q",
    "gemm_p1024_q192_um1_un4_simd1_adaptive_half",
    "gemm_p1024_q192_um1_un4_simd1_equal_split",
    "gemm_p1024_q192_um4_un1_simd1_single_block",
    "gemm_p1024_q192_um4_un1_simd1_fixed_q",
    "gemm_p1024_q192_um4_un1_simd1_adaptive_half",
    "gemm_p1024_q192_um4_un1_simd1_equal_split",
    "gemm_p1024_q192_um4_un1_simd4_single_block",
    "gemm_p1024_q192_um4_un1_simd4_fixed_q",
    "gemm_p1024_q192_um4_un1_simd4_adaptive_half",
    "gemm_p1024_q192_um4_un1_simd4_equal_split",
    "gemm_p1024_q192_um4_un4_simd4_single_block",
    "gemm_p1024_q192_um4_un4_simd4_fixed_q",
    "gemm_p1024_q192_um4_un4_simd4_adaptive_half",
    "gemm_p1024_q192_um4_un4_simd4_equal_split",
    "gemm_p1024_q192_um8_un1_simd1_single_block",
    "gemm_p1024_q192_um8_un1_simd1_fixed_q",
    "gemm_p1024_q192_um8_un1_simd1_adaptive_half",
    "gemm_p1024_q192_um8_un1_simd1_equal_split",
    "gemm_p1024_q192_um8_un1_simd4_single_block",
    "gemm_p1024_q192_um8_un1_simd4_fixed_q",
    "gemm_p1024_q192_um8_un1_simd4_adaptive_half",
    "gemm_p1024_q192_um8_un1_simd4_equal_split",
    "gemm_p1024_q192_um8_un1_simd8_single_block",
    "gemm_p1024_q192_um8_un1_simd8_fixed_q",
    "gemm_p1024_q192_um8_un1_simd8_adaptive_half",
    "gemm_p1024_q192_um8_un1_simd8_equal_split",
    "gemm_p1024_q192_um8_un4_simd4_single_block",
    "gemm_p1024_q192_um8_un4_simd4_fixed_q",
    "gemm_p1024_q192_um8_un4_simd4_adaptive_half",
    "gemm_p1024_q192_um8_un4_simd4_equal_split",
    "gemm_p1024_q192_um8_un4_simd8_single_block",
    "gemm_p1024_q192_um8_un4_simd8_fixed_q",
    "gemm_p1024_q192_um8_un4_simd8_adaptive_half",
    "gemm_p1024_q192_um8_un4_simd8_equal_split",
    "gemm_p1024_q192_um16_un1_simd4_single_block",
    "gemm_p1024_q192_um16_un1_simd4_fixed_q",
    "gemm_p1024_q192_um16_un1_simd4_adaptive_half",
    "gemm_p1024_q192_um16_un1_simd4_equal_split",
    "gemm_p1024_q192_um16_un1_simd8_single_block",
    "gemm_p1024_q192_um16_un1_simd8_fixed_q",
    "gemm_p1024_q192_um16_un1_simd8_adaptive_half",
    "gemm_p1024_q192_um16_un1_simd8_equal_split",
    "gemm_p1024_q192_um16_un1_simd16_single_block",
    "gemm_p1024_q192_um16_un1_simd16_fixed_q",
    "gemm_p1024_q192_um16_un1_simd16_adaptive_half",
    "gemm_p1024_q192_um16_un1_simd16_equal_split",
    "gemm_p1024_q192_um16_un4_simd8_single_block",
    "gemm_p1024_q192_um16_un4_simd8_fixed_q",
    "gemm_p1024_q192_um16_un4_simd8_adaptive_half",
    "gemm_p1024_q192_um16_un4_simd8_equal_split",
    "gemm_p1024_q192_um16_un4_simd16_single_block",
    "gemm_p1024_q192_um16_un4_simd16_fixed_q",
    "gemm_p1024_q192_um16_un4_simd16_adaptive_half",
    "gemm_p1024_q192_um16_un4_simd16_equal_split",
    "gemm_p1024_q192_um32_un1_simd4_single_block",
    "gemm_p1024_q192_um32_un1_simd4_fixed_q",
    "gemm_p1024_q192_um32_un1_simd4_adaptive_half",
    "gemm_p1024_q192_um32_un1_simd4_equal_split",
    "gemm_p1024_q192_um32_un1_simd8_single_block",
    "gemm_p1024_q192_um32_un1_simd8_fixed_q",
    "gemm_p1024_q192_um32_un1_simd8_adaptive_half",
    "gemm_p1024_q192_um32_un1_simd8_equal_split",
    "gemm_p1024_q192_um32_un1_simd16_single_block",
    "gemm_p1024_q192_um32_un1_simd16_fixed_q",
    "gemm_p1024_q192_um32_un1_simd16_adaptive_half",
    "gemm_p1024_q192_um32_un1_simd16_equal_split",
    "gemm_p1024_q192_um32_un4_simd16_single_block",
    "gemm_p1024_q192_um32_un4_simd16_fixed_q",
    "gemm_p1024_q192_um32_un4_simd16_adaptive_half",
    "gemm_p1024_q192_um32_un4_simd16_equal_split",
    "gemm_p1024_q240_um1_un1_simd1_single_block",
    "gemm_p1024_q240_um1_un1_simd1_fixed_q",
    "gemm_p1024_q240_um1_un1_simd1_adaptive_half",
    "gemm_p1024_q240_um1_un1_simd1_equal_split",
    "gemm_p1024_q240_um1_un4_simd1_single_block",
    "gemm_p1024_q240_um1_un4_simd1_fixed_q",
    "gemm_p1024_q240_um1_un4_simd1_adaptive_half",
    "gemm_p1024_q240_um1_un4_simd1_equal_split",
    "gemm_p1024_q240_um4_un1_simd1_single_block",
    "gemm_p1024_q240_um4_un1_simd1_fixed_q",
    "gemm_p1024_q240_um4_un1_simd1_adaptive_half",
    "gemm_p1024_q240_um4_un1_simd1_equal_split",
    "gemm_p1024_q240_um4_un1_simd4_single_block",
    "gemm_p1024_q240_um4_un1_simd4_fixed_q",
    "gemm_p1024_q240_um4_un1_simd4_adaptive_half",
    "gemm_p1024_q240_um4_un1_simd4_equal_split",
    "gemm_p1024_q240_um4_un4_simd4_single_block",
    "gemm_p1024_q240_um4_un4_simd4_fixed_q",
    "gemm_p1024_q240_um4_un4_simd4_adaptive_half",
    "gemm_p1024_q240_um4_un4_simd4_equal_split",
    "gemm_p1024_q240_um8_un1_simd1_single_block",
    "gemm_p1024_q240_um8_un1_simd1_fixed_q",
    "gemm_p1024_q240_um8_un1_simd1_adaptive_half",
    "gemm_p1024_q240_um8_un1_simd1_equal_split",
    "gemm_p1024_q240_um8_un1_simd4_single_block",
    "gemm_p1024_q240_um8_un1_simd4_fixed_q",
    "gemm_p1024_q240_um8_un1_simd4_adaptive_half",
    "gemm_p1024_q240_um8_un1_simd4_equal_split",
    "gemm_p1024_q240_um8_un1_simd8_single_block",
    "gemm_p1024_q240_um8_un1_simd8_fixed_q",
    "gemm_p1024_q240_um8_un1_simd8_adaptive_half",
    "gemm_p1024_q240_um8_un1_simd8_equal_split",
    "gemm_p1024_q240_um8_un4_simd4_single_block",
    "gemm_p1024_q240_um8_un4_simd4_fixed_q",
    "gemm_p1024_q240_um8_un4_simd4_adaptive_half",
    "gemm_p1024_q240_um8_un4_simd4_equal_split",
    "gemm_p1024_q240_um8_un4_simd8_single_block",
    "gemm_p1024_q240_um8_un4_simd8_fixed_q",
    "gemm_p1024_q240_um8_un4_simd8_adaptive_half",
    "gemm_p1024_q240_um8_un4_simd8_equal_split",
    "gemm_p1024_q240_um16_un1_simd4_single_block",
    "gemm_p1024_q240_um16_un1_simd4_fixed_q",
    "gemm_p1024_q240_um16_un1_simd4_adaptive_half",
    "gemm_p1024_q240_um16_un1_simd4_equal_split",
    "gemm_p1024_q240_um16_un1_simd8_single_block",
    "gemm_p1024_q240_um16_un1_simd8_fixed_q",
    "gemm_p1024_q240_um16_un1_simd8_adaptive_half",
    "gemm_p1024_q240_um16_un1_simd8_equal_split",
    "gemm_p1024_q240_um16_un1_simd16_single_block",
    "gemm_p1024_q240_um16_un1_simd16_fixed_q",
    "gemm_p1024_q240_um16_un1_simd16_adaptive_half",
    "gemm_p1024_q240_um16_un1_simd16_equal_split",
    "gemm_p1024_q240_um16_un4_simd8_single_block",
    "gemm_p1024_q240_um16_un4_simd8_fixed_q",
    "gemm_p1024_q240_um16_un4_simd8_adaptive_half",
    "gemm_p1024_q240_um16_un4_simd8_equal_split",
    "gemm_p1024_q240_um16_un4_simd16_single_block",
    "gemm_p1024_q240_um16_un4_simd16_fixed_q",
    "gemm_p1024_q240_um16_un4_simd16_adaptive_half",
    "gemm_p1024_q240_um16_un4_simd16_equal_split",
    "gemm_p1024_q240_um32_un1_simd4_single_block",
    "gemm_p1024_q240_um32_un1_simd4_fixed_q",
    "gemm_p1024_q240_um32_un1_simd4_adaptive_half",
    "gemm_p1024_q240_um32_un1_simd4_equal_split",
    "gemm_p1024_q240_um32_un1_simd8_single_block",
    "gemm_p1024_q240_um32_un1_simd8_fixed_q",
    "gemm_p1024_q240_um32_un1_simd8_adaptive_half",
    "gemm_p1024_q240_um32_un1_simd8_equal_split",
    "gemm_p1024_q240_um32_un1_simd16_single_block",
    "gemm_p1024_q240_um32_un1_simd16_fixed_q",
    "gemm_p1024_q240_um32_un1_simd16_adaptive_half",
    "gemm_p1024_q240_um32_un1_simd16_equal_split",
    "gemm_p1024_q248_um1_un1_simd1_single_block",
    "gemm_p1024_q248_um1_un1_simd1_fixed_q",
    "gemm_p1024_q248_um1_un1_simd1_adaptive_half",
    "gemm_p1024_q248_um1_un1_simd1_equal_split",
    "gemm_p1024_q248_um1_un4_simd1_single_block",
    "gemm_p1024_q248_um1_un4_simd1_fixed_q",
    "gemm_p1024_q248_um1_un4_simd1_adaptive_half",
    "gemm_p1024_q248_um1_un4_simd1_equal_split",
    "gemm_p1024_q248_um4_un1_simd1_single_block",
    "gemm_p1024_q248_um4_un1_simd1_fixed_q",
    "gemm_p1024_q248_um4_un1_simd1_adaptive_half",
    "gemm_p1024_q248_um4_un1_simd1_equal_split",
    "gemm_p1024_q248_um4_un1_simd4_single_block",
    "gemm_p1024_q248_um4_un1_simd4_fixed_q",
    "gemm_p1024_q248_um4_un1_simd4_adaptive_half",
    "gemm_p1024_q248_um4_un1_simd4_equal_split",
    "gemm_p1024_q248_um4_un4_simd4_single_block",
    "gemm_p1024_q248_um4_un4_simd4_fixed_q",
    "gemm_p1024_q248_um4_un4_simd4_adaptive_half",
    "gemm_p1024_q248_um4_un4_simd4_equal_split",
    "gemm_p1024_q248_um8_un1_simd1_single_block",
    "gemm_p1024_q248_um8_un1_simd1_fixed_q",
    "gemm_p1024_q248_um8_un1_simd1_adaptive_half",
    "gemm_p1024_q248_um8_un1_simd1_equal_split",
    "gemm_p1024_q248_um8_un1_simd4_single_block",
    "gemm_p1024_q248_um8_un1_simd4_fixed_q",
    "gemm_p1024_q248_um8_un1_simd4_adaptive_half",
    "gemm_p1024_q248_um8_un1_simd4_equal_split",
    "gemm_p1024_q248_um8_un1_simd8_single_block",
    "gemm_p1024_q248_um8_un1_simd8_fixed_q",
    "gemm_p1024_q248_um8_un1_simd8_adaptive_half",
    "gemm_p1024_q248_um8_un1_simd8_equal_split",
    "gemm_p1024_q248_um8_un4_simd4_single_block",
    "gemm_p1024_q248_um8_un4_simd4_fixed_q",
    "gemm_p1024_q248_um8_un4_simd4_adaptive_half",
    "gemm_p1024_q248_um8_un4_simd4_equal_split",
    "gemm_p1024_q248_um8_un4_simd8_single_block",
    "gemm_p1024_q248_um8_un4_simd8_fixed_q",
    "gemm_p1024_q248_um8_un4_simd8_adaptive_half",
    "gemm_p1024_q248_um8_un4_simd8_equal_split",
    "gemm_p1024_q248_um16_un1_simd4_single_block",
    "gemm_p1024_q248_um16_un1_simd4_fixed_q",
    "gemm_p1024_q248_um16_un1_simd4_adaptive_half",
    "gemm_p1024_q248_um16_un1_simd4_equal_split",
    "gemm_p1024_q248_um16_un1_simd8_single_block",
    "gemm_p1024_q248_um16_un1_simd8_fixed_q",
    "gemm_p1024_q248_um16_un1_simd8_adaptive_half",
    "gemm_p1024_q248_um16_un1_simd8_equal_split",
    "gemm_p1024_q248_um16_un1_simd16_single_block",
    "gemm_p1024_q248_um16_un1_simd16_fixed_q",
    "gemm_p1024_q248_um16_un1_simd16_adaptive_half",
    "gemm_p1024_q248_um16_un1_simd16_equal_split",
    "gemm_p1024_q248_um16_un4_simd8_single_block",
    "gemm_p1024_q248_um16_un4_simd8_fixed_q",
    "gemm_p1024_q248_um16_un4_simd8_adaptive_half",
    "gemm_p1024_q248_um16_un4_simd8_equal_split",
    "gemm_p1024_q248_um16_un4_simd16_single_block",
    "gemm_p1024_q248_um16_un4_simd16_fixed_q",
    "gemm_p1024_q248_um16_un4_simd16_adaptive_half",
    "gemm_p1024_q248_um16_un4_simd16_equal_split",
    "gemm_p1024_q248_um32_un1_simd4_single_block",
    "gemm_p1024_q248_um32_un1_simd4_fixed_q",
    "gemm_p1024_q248_um32_un1_simd4_adaptive_half",
    "gemm_p1024_q248_um32_un1_simd4_equal_split",
    "gemm_p1024_q248_um32_un1_simd8_single_block",
    "gemm_p1024_q248_um32_un1_simd8_fixed_q",
    "gemm_p1024_q248_um32_un1_simd8_adaptive_half",
    "gemm_p1024_q248_um32_un1_simd8_equal_split",
    "gemm_p1024_q248_um32_un1_simd16_single_block",
    "gemm_p1024_q248_um32_un1_simd16_fixed_q",
    "gemm_p1024_q248_um32_un1_simd16_adaptive_half",
    "gemm_p1024_q248_um32_un1_simd16_equal_split",
    "gemm_p1024_q256_um1_un1_simd1_single_block",
    "gemm_p1024_q256_um1_un1_simd1_fixed_q",
    "gemm_p1024_q256_um1_un1_simd1_adaptive_half",
    "gemm_p1024_q256_um1_un1_simd1_equal_split",
    "gemm_p1024_q256_um1_un4_simd1_single_block",
    "gemm_p1024_q256_um1_un4_simd1_fixed_q",
    "gemm_p1024_q256_um1_un4_simd1_adaptive_half",
    "gemm_p1024_q256_um1_un4_simd1_equal_split",
    "gemm_p1024_q256_um4_un1_simd1_single_block",
    "gemm_p1024_q256_um4_un1_simd1_fixed_q",
    "gemm_p1024_q256_um4_un1_simd1_adaptive_half",
    "gemm_p1024_q256_um4_un1_simd1_equal_split",
    "gemm_p1024_q256_um4_un1_simd4_single_block",
    "gemm_p1024_q256_um4_un1_simd4_fixed_q",
    "gemm_p1024_q256_um4_un1_simd4_adaptive_half",
    "gemm_p1024_q256_um4_un1_simd4_equal_split",
    "gemm_p1024_q256_um4_un4_simd4_single_block",
    "gemm_p1024_q256_um4_un4_simd4_fixed_q",
    "gemm_p1024_q256_um4_un4_simd4_adaptive_half",
    "gemm_p1024_q256_um4_un4_simd4_equal_split",
    "gemm_p1024_q256_um8_un1_simd1_single_block",
    "gemm_p1024_q256_um8_un1_simd1_fixed_q",
    "gemm_p1024_q256_um8_un1_simd1_adaptive_half",
    "gemm_p1024_q256_um8_un1_simd1_equal_split",
    "gemm_p1024_q256_um8_un1_simd4_single_block",
    "gemm_p1024_q256_um8_un1_simd4_fixed_q",
    "gemm_p1024_q256_um8_un1_simd4_adaptive_half",
    "gemm_p1024_q256_um8_un1_simd4_equal_split",
    "gemm_p1024_q256_um8_un1_simd8_single_block",
    "gemm_p1024_q256_um8_un1_simd8_fixed_q",
    "gemm_p1024_q256_um8_un1_simd8_adaptive_half",
    "gemm_p1024_q256_um8_un1_simd8_equal_split",
    "gemm_p1024_q256_um8_un4_simd4_single_block",
    "gemm_p1024_q256_um8_un4_simd4_fixed_q",
    "gemm_p1024_q256_um8_un4_simd4_adaptive_half",
    "gemm_p1024_q256_um8_un4_simd4_equal_split",
    "gemm_p1024_q256_um8_un4_simd8_single_block",
    "gemm_p1024_q256_um8_un4_simd8_fixed_q",
    "gemm_p1024_q256_um8_un4_simd8_adaptive_half",
    "gemm_p1024_q256_um8_un4_simd8_equal_split",
    "gemm_p1024_q256_um16_un1_simd4_single_block",
    "gemm_p1024_q256_um16_un1_simd4_fixed_q",
    "gemm_p1024_q256_um16_un1_simd4_adaptive_half",
    "gemm_p1024_q256_um16_un1_simd4_equal_split",
    "gemm_p1024_q256_um16_un1_simd8_single_block",
    "gemm_p1024_q256_um16_un1_simd8_fixed_q",
    "gemm_p1024_q256_um16_un1_simd8_adaptive_half",
    "gemm_p1024_q256_um16_un1_simd8_equal_split",
    "gemm_p1024_q256_um16_un1_simd16_single_block",
    "gemm_p1024_q256_um16_un1_simd16_fixed_q",
    "gemm_p1024_q256_um16_un1_simd16_adaptive_half",
    "gemm_p1024_q256_um16_un1_simd16_equal_split",
    "gemm_p1024_q256_um16_un4_simd8_single_block",
    "gemm_p1024_q256_um16_un4_simd8_fixed_q",
    "gemm_p1024_q256_um16_un4_simd8_adaptive_half",
    "gemm_p1024_q256_um16_un4_simd8_equal_split",
    "gemm_p1024_q256_um16_un4_simd16_single_block",
    "gemm_p1024_q256_um16_un4_simd16_fixed_q",
    "gemm_p1024_q256_um16_un4_simd16_adaptive_half",
    "gemm_p1024_q256_um16_un4_simd16_equal_split",
    "gemm_p1024_q384_um1_un1_simd1_single_block",
    "gemm_p1024_q384_um1_un1_simd1_fixed_q",
    "gemm_p1024_q384_um1_un1_simd1_adaptive_half",
    "gemm_p1024_q384_um1_un1_simd1_equal_split",
    "gemm_p1024_q384_um1_un4_simd1_single_block",
    "gemm_p1024_q384_um1_un4_simd1_fixed_q",
    "gemm_p1024_q384_um1_un4_simd1_adaptive_half",
    "gemm_p1024_q384_um1_un4_simd1_equal_split",
    "gemm_p1024_q384_um4_un1_simd1_single_block",
    "gemm_p1024_q384_um4_un1_simd1_fixed_q",
    "gemm_p1024_q384_um4_un1_simd1_adaptive_half",
    "gemm_p1024_q384_um4_un1_simd1_equal_split",
    "gemm_p1024_q384_um4_un1_simd4_single_block",
    "gemm_p1024_q384_um4_un1_simd4_fixed_q",
    "gemm_p1024_q384_um4_un1_simd4_adaptive_half",
    "gemm_p1024_q384_um4_un1_simd4_equal_split",
    "gemm_p1024_q384_um4_un4_simd4_single_block",
    "gemm_p1024_q384_um4_un4_simd4_fixed_q",
    "gemm_p1024_q384_um4_un4_simd4_adaptive_half",
    "gemm_p1024_q384_um4_un4_simd4_equal_split",
    "gemm_p1024_q384_um8_un1_simd1_single_block",
    "gemm_p1024_q384_um8_un1_simd1_fixed_q",
    "gemm_p1024_q384_um8_un1_simd1_adaptive_half",
    "gemm_p1024_q384_um8_un1_simd1_equal_split",
    "gemm_p1024_q384_um8_un1_simd4_single_block",
    "gemm_p1024_q384_um8_un1_simd4_fixed_q",
    "gemm_p1024_q384_um8_un1_simd4_adaptive_half",
    "gemm_p1024_q384_um8_un1_simd4_equal_split",
    "gemm_p1024_q384_um8_un1_simd8_single_block",
    "gemm_p1024_q384_um8_un1_simd8_fixed_q",
    "gemm_p1024_q384_um8_un1_simd8_adaptive_half",
    "gemm_p1024_q384_um8_un1_simd8_equal_split",
    "gemm_p1024_q384_um8_un4_simd4_single_block",
    "gemm_p1024_q384_um8_un4_simd4_fixed_q",
    "gemm_p1024_q384_um8_un4_simd4_adaptive_half",
    "gemm_p1024_q384_um8_un4_simd4_equal_split",
    "gemm_p1024_q384_um8_un4_simd8_single_block",
    "gemm_p1024_q384_um8_un4_simd8_fixed_q",
    "gemm_p1024_q384_um8_un4_simd8_adaptive_half",
    "gemm_p1024_q384_um8_un4_simd8_equal_split",
    "gemm_p1024_q384_um16_un1_simd4_single_block",
    "gemm_p1024_q384_um16_un1_simd4_fixed_q",
    "gemm_p1024_q384_um16_un1_simd4_adaptive_half",
    "gemm_p1024_q384_um16_un1_simd4_equal_split",
    "gemm_p1024_q384_um16_un1_simd8_single_block",
    "gemm_p1024_q384_um16_un1_simd8_fixed_q",
    "gemm_p1024_q384_um16_un1_simd8_adaptive_half",
    "gemm_p1024_q384_um16_un1_simd8_equal_split",
    "gemm_p1024_q384_um16_un1_simd16_single_block",
    "gemm_p1024_q384_um16_un1_simd16_fixed_q",
    "gemm_p1024_q384_um16_un1_simd16_adaptive_half",
    "gemm_p1024_q384_um16_un1_simd16_equal_split",
    "gemm_p1024_q384_um16_un4_simd8_single_block",
    "gemm_p1024_q384_um16_un4_simd8_fixed_q",
    "gemm_p1024_q384_um16_un4_simd8_adaptive_half",
    "gemm_p1024_q384_um16_un4_simd8_equal_split",
    "gemm_p1024_q384_um16_un4_simd16_single_block",
    "gemm_p1024_q384_um16_un4_simd16_fixed_q",
    "gemm_p1024_q384_um16_un4_simd16_adaptive_half",
    "gemm_p1024_q384_um16_un4_simd16_equal_split",
    "gemm_p1024_q512_um1_un1_simd1_single_block",
    "gemm_p1024_q512_um1_un1_simd1_fixed_q",
    "gemm_p1024_q512_um1_un1_simd1_adaptive_half",
    "gemm_p1024_q512_um1_un1_simd1_equal_split",
    "gemm_p1024_q512_um1_un4_simd1_single_block",
    "gemm_p1024_q512_um1_un4_simd1_fixed_q",
    "gemm_p1024_q512_um1_un4_simd1_adaptive_half",
    "gemm_p1024_q512_um1_un4_simd1_equal_split",
    "gemm_p1024_q512_um4_un1_simd1_single_block",
    "gemm_p1024_q512_um4_un1_simd1_fixed_q",
    "gemm_p1024_q512_um4_un1_simd1_adaptive_half",
    "gemm_p1024_q512_um4_un1_simd1_equal_split",
    "gemm_p1024_q512_um4_un1_simd4_single_block",
    "gemm_p1024_q512_um4_un1_simd4_fixed_q",
    "gemm_p1024_q512_um4_un1_simd4_adaptive_half",
    "gemm_p1024_q512_um4_un1_simd4_equal_split",
    "gemm_p1024_q512_um4_un4_simd4_single_block",
    "gemm_p1024_q512_um4_un4_simd4_fixed_q",
    "gemm_p1024_q512_um4_un4_simd4_adaptive_half",
    "gemm_p1024_q512_um4_un4_simd4_equal_split",
    "gemm_p1024_q512_um8_un1_simd1_single_block",
    "gemm_p1024_q512_um8_un1_simd1_fixed_q",
    "gemm_p1024_q512_um8_un1_simd1_adaptive_half",
    "gemm_p1024_q512_um8_un1_simd1_equal_split",
    "gemm_p1024_q512_um8_un1_simd4_single_block",
    "gemm_p1024_q512_um8_un1_simd4_fixed_q",
    "gemm_p1024_q512_um8_un1_simd4_adaptive_half",
    "gemm_p1024_q512_um8_un1_simd4_equal_split",
    "gemm_p1024_q512_um8_un1_simd8_single_block",
    "gemm_p1024_q512_um8_un1_simd8_fixed_q",
    "gemm_p1024_q512_um8_un1_simd8_adaptive_half",
    "gemm_p1024_q512_um8_un1_simd8_equal_split",
    "gemm_p1024_q512_um8_un4_simd4_single_block",
    "gemm_p1024_q512_um8_un4_simd4_fixed_q",
    "gemm_p1024_q512_um8_un4_simd4_adaptive_half",
    "gemm_p1024_q512_um8_un4_simd4_equal_split",
    "gemm_p1024_q512_um8_un4_simd8_single_block",
    "gemm_p1024_q512_um8_un4_simd8_fixed_q",
    "gemm_p1024_q512_um8_un4_simd8_adaptive_half",
    "gemm_p1024_q512_um8_un4_simd8_equal_split",
};
