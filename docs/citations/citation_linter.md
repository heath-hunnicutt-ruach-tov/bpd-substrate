# Citations for lib/citation_linter.pl

This bibliography is generated from the cites/2 annotations in the source
file by the Truth Flow citation_markdown_emitter. The annotations
themselves are the substrate-of-record; this Markdown is a projection
of that substrate into a human-readable form.

See also: docs/truth-flow-bpd-integration-design.md for the architectural
design that produces this output.

## `check_adjacency/3`

- `authority:internal(adjacency_rule_2026_06_01)` — `section(rule_statement)`
- `authority:internal(adjacency_rule_2026_06_03)` — `section(rule_b_extension)`

## `collect_citation_chain/5` (no citation needed)

Marked as `no_citation_needed/1` in the source. Internal helper or glue
code whose absence of citation was a deliberate decision rather than an
oversight.

## `count_clauses/4` (no citation needed)

Marked as `no_citation_needed/1` in the source. Internal helper or glue
code whose absence of citation was a deliberate decision rather than an
oversight.

## `count_clauses_walk/8` (no citation needed)

Marked as `no_citation_needed/1` in the source. Internal helper or glue
code whose absence of citation was a deliberate decision rather than an
oversight.

## `finding_format/2` (no citation needed)

Marked as `no_citation_needed/1` in the source. Internal helper or glue
code whose absence of citation was a deliberate decision rather than an
oversight.

## `is_any_clause_of/2` (no citation needed)

Marked as `no_citation_needed/1` in the source. Internal helper or glue
code whose absence of citation was a deliberate decision rather than an
oversight.

## `is_cites_annotation/3` (no citation needed)

Marked as `no_citation_needed/1` in the source. Internal helper or glue
code whose absence of citation was a deliberate decision rather than an
oversight.

## `is_no_citation_needed/2` (no citation needed)

Marked as `no_citation_needed/1` in the source. Internal helper or glue
code whose absence of citation was a deliberate decision rather than an
oversight.

## `is_rule_clause/2` (no citation needed)

Marked as `no_citation_needed/1` in the source. Internal helper or glue
code whose absence of citation was a deliberate decision rather than an
oversight.

## `lint_file/2`

- `authority:internal(truth_flow_bpd_integration_design)` — `section(layer_3_linter)`
- `authority:internal(adjacency_rule_2026_06_01)` — `section(rule_statement)`
- `authority:internal(adjacency_rule_2026_06_03)` — `section(rule_b_extension)`

## `lint_file_summary/1` (no citation needed)

Marked as `no_citation_needed/1` in the source. Internal helper or glue
code whose absence of citation was a deliberate decision rather than an
oversight.

## `lint_file_summary/2` (no citation needed)

Marked as `no_citation_needed/1` in the source. Internal helper or glue
code whose absence of citation was a deliberate decision rather than an
oversight.

## `read_source_terms/2` (no citation needed)

Marked as `no_citation_needed/1` in the source. Internal helper or glue
code whose absence of citation was a deliberate decision rather than an
oversight.

## `read_terms_loop/2` (no citation needed)

Marked as `no_citation_needed/1` in the source. Internal helper or glue
code whose absence of citation was a deliberate decision rather than an
oversight.

## `walk_terms/5` (no citation needed)

Marked as `no_citation_needed/1` in the source. Internal helper or glue
code whose absence of citation was a deliberate decision rather than an
oversight.

## `walk_terms_for_findings/3` (no citation needed)

Marked as `no_citation_needed/1` in the source. Internal helper or glue
code whose absence of citation was a deliberate decision rather than an
oversight.

