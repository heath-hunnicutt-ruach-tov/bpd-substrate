# Citations for lib/gguf_native_reader.pl

This bibliography is generated from the cites/2 annotations in the source
file by the Truth Flow citation_markdown_emitter. The annotations
themselves are the substrate-of-record; this Markdown is a projection
of that substrate into a human-readable form.

See also: docs/truth-flow-bpd-integration-design.md for the architectural
design that produces this output.

## `gguf_architecture_native/2`

- `authority:internal(gguf_spec_v3)` — `section(general_architecture_key)`
- `authority:internal(llamacpp_gguf_reference)` — `section(architecture_naming)`

## `gguf_data_start/2` (no citation needed)

Marked as `no_citation_needed/1` in the source. Internal helper or glue
code whose absence of citation was a deliberate decision rather than an
oversight.

## `gguf_read/4`

- `authority:internal(gguf_spec_v3)` — `section(file_layout)`
- `authority:internal(llamacpp_gguf_reference)` — `section(reading_a_file)`

## `gguf_read/5`

- `authority:internal(gguf_spec_v3)` — `section(tensor_data_alignment)`
- `authority:internal(llamacpp_gguf_reference)` — `section(general_alignment)`

## `gguf_read_full/4`

- `authority:internal(gguf_spec_v3)` — `section(value_types)`
- `authority:internal(llamacpp_gguf_reference)` — `section(arrays_and_tokenizers)`

## `gguf_read_full/5`

- `authority:internal(gguf_spec_v3)` — `section(tensor_data_alignment)`
- `authority:internal(llamacpp_gguf_reference)` — `section(arrays_and_tokenizers)`

## `gguf_read_inner/7`

- `authority:internal(gguf_spec_v3)` — `section(file_header)`
- `authority:internal(llamacpp_gguf_reference)` — `section(magic_and_version)`

## `gguf_tensor_list/2`

- `authority:internal(gguf_spec_v3)` — `section(tensor_info)`

## `read_array_elements/5` (no citation needed)

Marked as `no_citation_needed/1` in the source. Internal helper or glue
code whose absence of citation was a deliberate decision rather than an
oversight.

## `read_dims/4`

- `authority:internal(gguf_spec_v3)` — `section(tensor_dimensions)`

## `read_kv_pairs/4`

- `authority:internal(gguf_spec_v3)` — `section(metadata_kv_pairs)`
- `authority:internal(llamacpp_gguf_reference)` — `section(metadata_kv)`

## `read_kv_value/4`

- `authority:internal(gguf_spec_v3)` — `section(value_types)`
- `authority:internal(llamacpp_gguf_reference)` — `section(kv_value_types)`
- `authority:ieee(754)` — `section(binary_formats)`
- `authority:internal(gguf_spec_v3)` — `section(value_types)`
- `authority:internal(llamacpp_gguf_reference)` — `section(kv_value_types)`
- `authority:ieee(754)` — `section(binary_formats)`

## `read_tensor_infos/4`

- `authority:internal(gguf_spec_v3)` — `section(tensor_info)`
- `authority:internal(llamacpp_gguf_reference)` — `section(tensor_info)`

## `skip_array_elems/3` (no citation needed)

Marked as `no_citation_needed/1` in the source. Internal helper or glue
code whose absence of citation was a deliberate decision rather than an
oversight.

## `skip_large_array/4` (no citation needed)

Marked as `no_citation_needed/1` in the source. Internal helper or glue
code whose absence of citation was a deliberate decision rather than an
oversight.

## `skip_n_strings/2` (no citation needed)

Marked as `no_citation_needed/1` in the source. Internal helper or glue
code whose absence of citation was a deliberate decision rather than an
oversight.

